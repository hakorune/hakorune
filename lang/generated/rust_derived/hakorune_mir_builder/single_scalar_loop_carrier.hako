// @generated by tools/rust_lifecycle/generate_single_scalar_loop_carrier_artifact.py
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/single_scalar_loop_carrier.artifact.json
// family: hakorune_mir_builder::single_scalar_loop_carrier
// pilot-scope: SingleScalarLoopCarrier_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box SingleScalarLoopCarrierPilot {
}

static box SingleScalarLoopCarrierPilotApi {
    sum_values(values: ArrayBox): i64 {
        local i = 0
        local sum = 0
        loop(i < values.length()) {
            sum = sum + BoxHelpers.array_get(values, i)
            i = i + 1
        }
        return sum
    }
}

static box Main {
    main() {
        local values = new ArrayBox()
        values.push(3)
        values.push(5)
        values.push(8)
        local sum = SingleScalarLoopCarrierPilotApi.sum_values(values)
        if sum != 16 {
            print("single_scalar_loop_sum=fail")
            return 1
        }
        print("single_scalar_loop_carrier_direct_artifact=ok")
        return 0
    }
}

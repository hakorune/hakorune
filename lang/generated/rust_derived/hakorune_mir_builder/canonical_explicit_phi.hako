// @generated by tools/rust_lifecycle/convert_mirbuilder_lightweight_facts.py --family explicit_phi
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/canonical_explicit_phi.artifact.json
// family: hakorune_mir_builder::canonical_explicit_phi
// pilot-scope: CanonicalExplicitPhi_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box CanonicalExplicitPhiPilot {
}

static box CanonicalExplicitPhiPilotApi {
    choose_value(flag: i64): i64 {
        local value = 0
        if flag == 1 {
            value = 10
        } else {
            value = 20
        }
        return value
    }
}

static box Main {
    main() {
        local then_value = CanonicalExplicitPhiPilotApi.choose_value(1)
        if then_value != 10 {
            print("explicit_phi_then=fail")
            return 1
        }
        local else_value = CanonicalExplicitPhiPilotApi.choose_value(0)
        if else_value != 20 {
            print("explicit_phi_else=fail")
            return 2
        }
        print("canonical_explicit_phi_direct_artifact=ok")
        return 0
    }
}

// @generated by tools/rust_lifecycle/convert_mirbuilder_lightweight_facts.py --family mirbuilder-minimal-path-composed-execution-closure
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/mirbuilder_minimal_path_composed_execution_closure.artifact.json
// family: hakorune_mir_builder::minimal_path_composed_execution_closure
// pilot-scope: MinimalMirBuilderComposedExecutionClosure_prepared_state_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box MinimalPathComposedExecutionClosureKernel {
}


box PreparedComposedExecutionClosureStateShellBox {
    same_state_handoff_observed: i64
    selected_existing_contracts_consumed: i64
    generated_hako_executable_closure_closed: i64
    route_chain_closed: i64

    birth() {
        me.same_state_handoff_observed = 1
        me.selected_existing_contracts_consumed = 1
        me.generated_hako_executable_closure_closed = 0
        me.route_chain_closed = 0
    }
}

static box MinimalPathComposedExecutionClosureApi {
    seal(state): PreparedComposedExecutionClosureStateShellBox {
        state.generated_hako_executable_closure_closed = 1
        state.route_chain_closed = 1
        return state
    }
}

static box Main {
    main() {
        local state = new PreparedComposedExecutionClosureStateShellBox()
        local sealed = MinimalPathComposedExecutionClosureApi.seal(state)
        if sealed.same_state_handoff_observed != 1 {
            print("composed_execution_closure_same_state=fail")
            return 1
        }
        if sealed.selected_existing_contracts_consumed != 1 {
            print("composed_execution_closure_contracts=fail")
            return 2
        }
        if sealed.generated_hako_executable_closure_closed != 1 {
            print("composed_execution_closure_closed=fail")
            return 3
        }
        if sealed.route_chain_closed != 1 {
            print("composed_execution_closure_route_chain=fail")
            return 4
        }
        print("mirbuilder_minimal_path_composed_execution_closure_derived_hako=ok")
        return 0
    }
}

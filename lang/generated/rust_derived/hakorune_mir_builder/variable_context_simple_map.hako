// @generated by tools/rust_lifecycle/generate_variable_context_simple_map_artifact.py
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/variable_context_simple_map.artifact.json
// family: hakorune_mir_builder::variable_context
// pilot-scope: VariableContext_simple_map_only
// manual-edit: forbidden

using apps.lib.collections.ordered_map as OrderedMap

using selfhost.shared.common.box_helpers as BoxHelpers

box VariableContext {
    variable_map: OrderedMapBox

    birth() {
        me.variable_map = OrderedMap.create()
    }
}

static box VariableContextApi {
    is_empty(ctx): i64 {
        local keys = ctx.variable_map.keys_value
        if keys.length() == 0 {
            return 1
        }
        return 0
    }

    len(ctx): i64 {
        local keys = ctx.variable_map.keys_value
        return keys.length()
    }

    contains(ctx, name): i64 {
        local keys = ctx.variable_map.keys_value
        if name == null {
            return 0
        }
        local key = name
        local i = 0
        loop(i < keys.length()) {
            if BoxHelpers.array_get(keys, i) == key {
                return 1
            }
            i = i + 1
        }
        return 0
    }

    lookup(ctx, name) {
        local keys = ctx.variable_map.keys_value
        local values = ctx.variable_map.values_value
        if name == null {
            return null
        }
        local key = name
        local i = 0
        loop(i < keys.length()) {
            if BoxHelpers.array_get(keys, i) == key {
                return BoxHelpers.array_get(values, i)
            }
            i = i + 1
        }
        return null
    }

    insert(ctx, name, value_id): i64 {
        if name == null {
            return 0
        }
        local key = name
        local i = 0
        loop(i < ctx.variable_map.keys_value.length()) {
            if BoxHelpers.array_get(ctx.variable_map.keys_value, i) == key {
                ctx.variable_map.values_value.set(i, value_id)
                return 1
            }
            i = i + 1
        }
        ctx.variable_map.keys_value.push(key)
        ctx.variable_map.values_value.push(value_id)
        local pos = ctx.variable_map.keys_value.length() - 1
        loop(pos > 0) {
            local prev = pos - 1
            local a = BoxHelpers.array_get(ctx.variable_map.keys_value, prev)
            local b = BoxHelpers.array_get(ctx.variable_map.keys_value, pos)
            if a < b or a == b {
                return 1
            }
            local av = BoxHelpers.array_get(ctx.variable_map.values_value, prev)
            local bv = BoxHelpers.array_get(ctx.variable_map.values_value, pos)
            ctx.variable_map.keys_value.set(prev, b)
            ctx.variable_map.keys_value.set(pos, a)
            ctx.variable_map.values_value.set(prev, bv)
            ctx.variable_map.values_value.set(pos, av)
            pos = pos - 1
        }
        return 1
        return 0
    }

    remove(ctx, name) {
        return ctx.variable_map.remove(name)
    }
}

static box Main {
    main() {
        local ctx = new VariableContext()
        local new_empty = VariableContextApi.is_empty(ctx)
        if new_empty != 1 {
            print("variable_context_new_empty=fail")
            return 1
        }
        local new_len = VariableContextApi.len(ctx)
        if new_len != 0 {
            print("variable_context_new_len=fail")
            return 2
        }
        VariableContextApi.insert(ctx, "x", 42)
        local lookup_x = VariableContextApi.lookup(ctx, "x")
        if lookup_x != 42 {
            print("variable_context_lookup_x=fail")
            return 3
        }
        local len_after_insert = VariableContextApi.len(ctx)
        if len_after_insert != 1 {
            print("variable_context_len_after_insert=fail")
            return 4
        }
        local empty_after_insert = VariableContextApi.is_empty(ctx)
        if empty_after_insert != 0 {
            print("variable_context_empty_after_insert=fail")
            return 5
        }
        local remove_x = VariableContextApi.remove(ctx, "x")
        if remove_x != 42 {
            print("variable_context_remove_x=fail")
            return 6
        }
        local lookup_removed = VariableContextApi.lookup(ctx, "x")
        if lookup_removed != null {
            print("variable_context_lookup_removed=fail")
            return 7
        }
        local contains_ctx = new VariableContext()
        local contains_empty = VariableContextApi.contains(contains_ctx, "x")
        if contains_empty != 0 {
            print("variable_context_contains_empty=fail")
            return 8
        }
        VariableContextApi.insert(contains_ctx, "x", 1)
        local contains_x = VariableContextApi.contains(contains_ctx, "x")
        if contains_x != 1 {
            print("variable_context_contains_x=fail")
            return 9
        }
        local ssa_ctx = new VariableContext()
        VariableContextApi.insert(ssa_ctx, "x", 1)
        VariableContextApi.insert(ssa_ctx, "x", 2)
        VariableContextApi.insert(ssa_ctx, "x", 4)
        local ssa_lookup_x = VariableContextApi.lookup(ssa_ctx, "x")
        if ssa_lookup_x != 4 {
            print("variable_context_ssa_update=fail")
            return 10
        }
        print("variable_context_simple_map_derived_artifact=ok")
        return 0
    }
}

// @generated by tools/rust_lifecycle/convert_mirbuilder_lightweight_facts.py --family mirbuilder-typed-value-verification
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/mirbuilder_typed_value_verification.artifact.json
// family: hakorune_mir_builder::typed_value_verification
// pilot-scope: TypedValueVerification_minimal_literal_success_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box TypedValueVerificationKernel {
}


box TypedValueVerificationResultBox {
    verified: i64
    missing_count: i64
    fatal_missing: i64
    stale_cleanup_count: i64

    birth() {
        me.verified = 0
        me.missing_count = 0
        me.fatal_missing = 0
        me.stale_cleanup_count = 0
    }
}

static box TypedValueVerificationApi {
    verify(result_value, typed_value, defined_value, param_value): TypedValueVerificationResultBox {
        if typed_value == defined_value {
            local result = new TypedValueVerificationResultBox()
            result.verified = 1
            result.missing_count = 0
            result.fatal_missing = 0
            result.stale_cleanup_count = 0
            return result
        } else {
            local result = new TypedValueVerificationResultBox()
            result.verified = 0
            result.missing_count = 1
            result.fatal_missing = 1
            result.stale_cleanup_count = 0
            return result
        }
    }
}

static box Main {
    main() {
        local verified = TypedValueVerificationApi.verify(7, 7, 7, 0)
        if verified.verified != 1 {
            print("typed_value_verified=fail")
            return 1
        }
        if verified.missing_count != 0 {
            print("typed_value_missing=fail")
            return 2
        }
        if verified.fatal_missing != 0 {
            print("typed_value_fatal=fail")
            return 3
        }
        if verified.stale_cleanup_count != 0 {
            print("typed_value_stale_cleanup=fail")
            return 4
        }
        print("mirbuilder_typed_value_verification_derived_hako=ok")
        return 0
    }
}

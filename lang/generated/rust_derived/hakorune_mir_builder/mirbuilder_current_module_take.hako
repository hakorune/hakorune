// @generated by tools/rust_lifecycle/convert_mirbuilder_lightweight_facts.py --family mirbuilder-current-module-take
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/mirbuilder_current_module_take.artifact.json
// family: hakorune_mir_builder::current_module_take
// pilot-scope: CurrentModuleTake_present_module_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box CurrentModuleTakeKernel {
}


box PreparedCurrentModuleStateShellBox {
    current_module_present: i64
    taken_module_present: i64

    birth() {
        me.current_module_present = 1
        me.taken_module_present = 0
    }
}


box CurrentModuleTakeModuleShellBox {
    name_is_main: i64
    functions_empty: i64
    globals_empty: i64
    metadata_default: i64

    birth() {
        me.name_is_main = 1
        me.functions_empty = 1
        me.globals_empty = 1
        me.metadata_default = 1
    }
}

static box CurrentModuleTakeApi {
    take(state, module): CurrentModuleTakeModuleShellBox {
        state.current_module_present = 0
        state.taken_module_present = 1
        return module
    }
}

static box Main {
    main() {
        local state = new PreparedCurrentModuleStateShellBox()
        local module = new CurrentModuleTakeModuleShellBox()
        local taken = CurrentModuleTakeApi.take(state, module)
        if state.current_module_present != 0 {
            print("current_module_present=fail")
            return 1
        }
        if state.taken_module_present != 1 {
            print("taken_module_present=fail")
            return 2
        }
        if taken.name_is_main != 1 {
            print("module_name=fail")
            return 3
        }
        if taken.functions_empty != 1 {
            print("module_functions=fail")
            return 4
        }
        if taken.globals_empty != 1 {
            print("module_globals=fail")
            return 5
        }
        print("mirbuilder_current_module_take_derived_hako=ok")
        return 0
    }
}

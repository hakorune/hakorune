// @generated by tools/rust_lifecycle/generate_box_compilation_context_artifact.py
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/box_compilation_context.artifact.json
// family: hakorune_mir_builder::context
// pilot-scope: BoxCompilationContext_ctor_is_empty_only
// manual-edit: forbidden

using apps.lib.collections.ordered_map as OrderedMap

using selfhost.shared.common.box_helpers as BoxHelpers

box BoxCompilationContext {
    variable_map: OrderedMapBox
    value_origin_newbox: OrderedMapBox
    value_types: OrderedMapBox

    birth() {
        me.variable_map = OrderedMap.create()
        me.value_origin_newbox = OrderedMap.create()
        me.value_types = OrderedMap.create()
    }
}

static box BoxCompilationContextApi {
    is_empty(ctx): i64 {
        if ctx.variable_map.is_empty() && ctx.value_origin_newbox.is_empty() && ctx.value_types.is_empty() {
            return 1
        }
        return 0
    }
}

static box Main {
    main() {
        local ctx = new BoxCompilationContext()
        local is_empty_result = BoxCompilationContextApi.is_empty(ctx)
        if is_empty_result != 1 {
            print("box_compilation_context_new_empty=fail")
            return 1
        }
        print("box_compilation_context_derived_artifact=ok")
        return 0
    }
}

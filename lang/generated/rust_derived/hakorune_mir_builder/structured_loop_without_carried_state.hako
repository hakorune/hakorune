// @generated by tools/rust_lifecycle/generate_structured_loop_artifact.py
// artifact-manifest: lang/generated/rust_derived/hakorune_mir_builder/structured_loop_without_carried_state.artifact.json
// family: hakorune_mir_builder::structured_loop_without_carried_state
// pilot-scope: StructuredLoop_without_carried_state_only
// manual-edit: forbidden

using selfhost.shared.common.box_helpers as BoxHelpers

box StructuredLoopPilot {
}

static box StructuredLoopPilotApi {
    copy_values(values: ArrayBox, out: ArrayBox): i64 {
        local i = 0
        loop(i < values.length()) {
            out.push(BoxHelpers.array_get(values, i))
            i = i + 1
        }
        return 0
    }
}

static box Main {
    main() {
        local values = new ArrayBox()
        local out = new ArrayBox()
        values.push(3)
        values.push(5)
        values.push(8)
        local status = StructuredLoopPilotApi.copy_values(values, out)
        if status != 0 {
            print("structured_loop_status=fail")
            return 1
        }
        if BoxHelpers.array_get(out, 0) != 3 {
            print("structured_loop_value_0=fail")
            return 2
        }
        if BoxHelpers.array_get(out, 1) != 5 {
            print("structured_loop_value_1=fail")
            return 3
        }
        if BoxHelpers.array_get(out, 2) != 8 {
            print("structured_loop_value_2=fail")
            return 4
        }
        print("structured_loop_without_carried_state_direct_artifact=ok")
        return 0
    }
}

// launcher_native_entry.hako
//
// Purpose:
// - Provide a tiny argv-based bootstrap wrapper for launcher build artifacts.
// - Keep the active path free of same-file helper boxes, file IO, and env reads.

static box Main {
  main(args) {
    return 0
  }
}

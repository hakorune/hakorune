// stage1_cli.hako — thin compatibility wrapper
//
// Canonical compat owner now lives under `compat/stage1_cli.hako`.

using lang.runner.compat.stage1_cli as Stage1CliCompatBox

static box Stage1CliMain {
  main() {
    return Stage1CliCompatBox.Stage1CliMain.main()
  }
}

static box Main {
  main() {
    return Stage1CliMain.main()
  }
}

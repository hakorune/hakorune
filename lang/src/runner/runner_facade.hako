// runner_facade.hako — thin compatibility wrapper
//
// Canonical facade owner now lives under `facade/runner_facade.hako`.

using lang.runner.facade.runner_facade as RunnerFacadeBox

static box Runner {
  run(entry, args) {
    return RunnerFacadeBox.run(entry, args)
  }
}

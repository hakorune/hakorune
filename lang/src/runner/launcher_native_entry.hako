// launcher_native_entry.hako — thin compatibility wrapper
//
// Canonical entry stub now lives under `entry/launcher_native_entry.hako`.

using lang.runner.entry.launcher_native_entry as LauncherNativeEntryBox

static box Main {
  main(args) {
    return LauncherNativeEntryBox.main(args)
  }
}

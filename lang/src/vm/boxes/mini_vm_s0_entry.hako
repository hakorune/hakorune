// mini_vm_s0_entry.hako — thin stable entry for vm-hako S0 runner
using selfhost.vm.helpers.mir_s0_block_runner as MirVmS0BlockRunnerBox

static box MiniVmS0EntryBox {
  run_min(json) { return MirVmS0BlockRunnerBox.run_min(json) }
}

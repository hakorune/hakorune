// segment_arena_backing_modeled_allocation_ledger_release_apply_box.hako -- MIMAP-288A
// Responsibility:
// - record scalar/model release-apply facts from an accepted modeled
//   allocation-ledger release-intent report
// - keep release application in model space before real arena backing release
//   opens
// Non-responsibility:
// - creating real pointer residence, pointer-derived lookup, arena backing
//   release/allocation, segment-map mutation, atomic bitmap execution, OSVM
//   calls, scheduling, providers, or backend shortcuts

record HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReportFields {
    accepted: i64
    reason: i64
    release_apply_present: i64
    modeled_release_apply_present: i64
    release_intent_present: i64
    release_intent_reason: i64
    row_index: i64
    segment_id: i64
    arena_id: i64
    lifetime_generation: i64
    residence_token: i64
    binding_token: i64
    arena_slot_token: i64
    source_kind: i64
    source_token: i64
    plan_token: i64
    apply_token: i64
    ledger_token: i64
    release_candidate_token: i64
    release_intent_token: i64
    release_apply_token: i64
    source_capacity: usize
    source_committed_bytes: usize
    source_uncommitted_bytes: usize
    padded_bytes: usize
    slot_capacity: usize
    planned_backing_bytes: usize
    planned_committed_bytes: usize
    applied_backing_bytes: usize
    applied_committed_bytes: usize
    remaining_source_bytes: usize
    closed_substrate_blocker_count: i64
    requires_pointer_residence: i64
    requires_pointer_lookup: i64
    requires_arena_backing: i64
    requires_segment_map: i64
    requires_atomic_bitmap: i64
    requires_osvm: i64
    requires_worker: i64
    requires_provider: i64
    requires_backend_matcher: i64
}

box HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport {
    accepted: i64 = 0
    reason: i64 = 0
    release_apply_present: i64 = 1
    modeled_release_apply_present: i64 = 1
    release_intent_present: i64 = 0
    release_intent_reason: i64 = 0
    row_index: i64 = -1
    segment_id: i64 = -1
    arena_id: i64 = -1
    lifetime_generation: i64 = 0
    residence_token: i64 = 0
    binding_token: i64 = 0
    arena_slot_token: i64 = 0
    source_kind: i64 = 0
    source_token: i64 = 0
    plan_token: i64 = 0
    apply_token: i64 = 0
    ledger_token: i64 = 0
    release_candidate_token: i64 = 0
    release_intent_token: i64 = 0
    release_apply_token: i64 = 0
    source_capacity: usize = 0
    source_committed_bytes: usize = 0
    source_uncommitted_bytes: usize = 0
    padded_bytes: usize = 0
    slot_capacity: usize = 0
    planned_backing_bytes: usize = 0
    planned_committed_bytes: usize = 0
    applied_backing_bytes: usize = 0
    applied_committed_bytes: usize = 0
    remaining_source_bytes: usize = 0
    closed_substrate_blocker_count: i64 = 0
    requires_pointer_residence: i64 = 0
    requires_pointer_lookup: i64 = 0
    requires_arena_backing: i64 = 0
    requires_segment_map: i64 = 0
    requires_atomic_bitmap: i64 = 0
    requires_osvm: i64 = 0
    requires_worker: i64 = 0
    requires_provider: i64 = 0
    requires_backend_matcher: i64 = 0
    would_create_pointer_residence: i64 = 0
    would_lookup_by_pointer: i64 = 0
    would_release_arena_backing: i64 = 0
    would_use_segment_map: i64 = 0
    would_execute_atomic_bitmap: i64 = 0
    would_call_osvm: i64 = 0
    would_run_thread: i64 = 0
    would_activate_provider: i64 = 0
    would_add_backend_matcher: i64 = 0
}

box HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyInventory {
    inventory_count: i64 = 0
    accepted_count: i64 = 0
    reject_count: i64 = 0
    missing_intent_reject_count: i64 = 0
    rejected_intent_reject_count: i64 = 0
    invalid_release_apply_token_reject_count: i64 = 0
    duplicate_release_apply_token_reject_count: i64 = 0
    closed_substrate_reject_count: i64 = 0
    last_reason: i64 = 0
    last_segment_id: i64 = -1
    last_arena_id: i64 = -1
    last_release_apply_token: i64 = 0

    closedSubstrateBlockerCount(intent_report): i64 {
        local count = intent_report.closed_substrate_blocker_count
        if intent_report.requires_pointer_residence != 0 {
            count += 1
        }
        if intent_report.requires_pointer_lookup != 0 {
            count += 1
        }
        if intent_report.requires_arena_backing != 0 {
            count += 1
        }
        if intent_report.requires_segment_map != 0 {
            count += 1
        }
        if intent_report.requires_atomic_bitmap != 0 {
            count += 1
        }
        if intent_report.requires_osvm != 0 {
            count += 1
        }
        if intent_report.requires_worker != 0 {
            count += 1
        }
        if intent_report.requires_provider != 0 {
            count += 1
        }
        if intent_report.requires_backend_matcher != 0 {
            count += 1
        }
        return count
    }

    hasReleaseApplyToken(release_apply_token): i64 {
        if me.accepted_count <= 0 {
            return 0
        }
        if me.last_release_apply_token == release_apply_token {
            return 1
        }
        return 0
    }

    makeReleaseApplyReport(fields: HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReportFields): HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport {
        local result = new HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport()
        result.accepted = fields.accepted
        result.reason = fields.reason
        result.release_apply_present = fields.release_apply_present
        result.modeled_release_apply_present = fields.modeled_release_apply_present
        result.release_intent_present = fields.release_intent_present
        result.release_intent_reason = fields.release_intent_reason
        result.row_index = fields.row_index
        result.segment_id = fields.segment_id
        result.arena_id = fields.arena_id
        result.lifetime_generation = fields.lifetime_generation
        result.residence_token = fields.residence_token
        result.binding_token = fields.binding_token
        result.arena_slot_token = fields.arena_slot_token
        result.source_kind = fields.source_kind
        result.source_token = fields.source_token
        result.plan_token = fields.plan_token
        result.apply_token = fields.apply_token
        result.ledger_token = fields.ledger_token
        result.release_candidate_token = fields.release_candidate_token
        result.release_intent_token = fields.release_intent_token
        result.release_apply_token = fields.release_apply_token
        result.source_capacity = fields.source_capacity
        result.source_committed_bytes = fields.source_committed_bytes
        result.source_uncommitted_bytes = fields.source_uncommitted_bytes
        result.padded_bytes = fields.padded_bytes
        result.slot_capacity = fields.slot_capacity
        result.planned_backing_bytes = fields.planned_backing_bytes
        result.planned_committed_bytes = fields.planned_committed_bytes
        result.applied_backing_bytes = fields.applied_backing_bytes
        result.applied_committed_bytes = fields.applied_committed_bytes
        result.remaining_source_bytes = fields.remaining_source_bytes
        result.closed_substrate_blocker_count = fields.closed_substrate_blocker_count
        result.requires_pointer_residence = fields.requires_pointer_residence
        result.requires_pointer_lookup = fields.requires_pointer_lookup
        result.requires_arena_backing = fields.requires_arena_backing
        result.requires_segment_map = fields.requires_segment_map
        result.requires_atomic_bitmap = fields.requires_atomic_bitmap
        result.requires_osvm = fields.requires_osvm
        result.requires_worker = fields.requires_worker
        result.requires_provider = fields.requires_provider
        result.requires_backend_matcher = fields.requires_backend_matcher
        return result
    }

    makeReport(accepted, reason, intent_report, release_apply_token): HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport {
        local fields = HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReportFields {
            accepted: accepted,
            reason: reason,
            release_apply_present: 1,
            modeled_release_apply_present: 1,
            release_intent_present: intent_report.release_intent_present,
            release_intent_reason: intent_report.reason,
            row_index: me.accepted_count,
            segment_id: intent_report.segment_id,
            arena_id: intent_report.arena_id,
            lifetime_generation: intent_report.lifetime_generation,
            residence_token: intent_report.residence_token,
            binding_token: intent_report.binding_token,
            arena_slot_token: intent_report.arena_slot_token,
            source_kind: intent_report.source_kind,
            source_token: intent_report.source_token,
            plan_token: intent_report.plan_token,
            apply_token: intent_report.apply_token,
            ledger_token: intent_report.ledger_token,
            release_candidate_token: intent_report.release_candidate_token,
            release_intent_token: intent_report.release_intent_token,
            release_apply_token: release_apply_token,
            source_capacity: intent_report.source_capacity,
            source_committed_bytes: intent_report.source_committed_bytes,
            source_uncommitted_bytes: intent_report.source_uncommitted_bytes,
            padded_bytes: intent_report.padded_bytes,
            slot_capacity: intent_report.slot_capacity,
            planned_backing_bytes: intent_report.planned_backing_bytes,
            planned_committed_bytes: intent_report.planned_committed_bytes,
            applied_backing_bytes: intent_report.applied_backing_bytes,
            applied_committed_bytes: intent_report.applied_committed_bytes,
            remaining_source_bytes: intent_report.remaining_source_bytes,
            closed_substrate_blocker_count: me.closedSubstrateBlockerCount(intent_report),
            requires_pointer_residence: intent_report.requires_pointer_residence,
            requires_pointer_lookup: intent_report.requires_pointer_lookup,
            requires_arena_backing: intent_report.requires_arena_backing,
            requires_segment_map: intent_report.requires_segment_map,
            requires_atomic_bitmap: intent_report.requires_atomic_bitmap,
            requires_osvm: intent_report.requires_osvm,
            requires_worker: intent_report.requires_worker,
            requires_provider: intent_report.requires_provider,
            requires_backend_matcher: intent_report.requires_backend_matcher,
        }
        me.last_reason = reason
        me.last_segment_id = intent_report.segment_id
        me.last_arena_id = intent_report.arena_id
        return me.makeReleaseApplyReport(fields)
    }

    reject(reason, intent_report, release_apply_token): HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport {
        me.reject_count += 1
        if reason == 1 {
            me.missing_intent_reject_count += 1
        }
        if reason == 2 {
            me.rejected_intent_reject_count += 1
        }
        if reason == 3 {
            me.invalid_release_apply_token_reject_count += 1
        }
        if reason == 4 {
            me.duplicate_release_apply_token_reject_count += 1
        }
        if reason == 5 {
            me.closed_substrate_reject_count += 1
        }
        return me.makeReport(0, reason, intent_report, release_apply_token)
    }

    recordReleaseApply(intent_report, release_apply_token): HakoAllocSegmentArenaBackingModeledAllocationLedgerReleaseApplyReport {
        me.inventory_count += 1

        if intent_report.release_intent_present != 1 {
            return me.reject(1, intent_report, release_apply_token)
        }
        if intent_report.modeled_release_intent_present != 1 {
            return me.reject(1, intent_report, release_apply_token)
        }
        if intent_report.accepted != 1 {
            return me.reject(2, intent_report, release_apply_token)
        }
        if release_apply_token <= 0 {
            return me.reject(3, intent_report, release_apply_token)
        }
        if me.hasReleaseApplyToken(release_apply_token) == 1 {
            return me.reject(4, intent_report, release_apply_token)
        }
        if me.closedSubstrateBlockerCount(intent_report) != 0 {
            return me.reject(5, intent_report, release_apply_token)
        }

        me.accepted_count += 1
        me.last_release_apply_token = release_apply_token
        return me.makeReport(1, 0, intent_report, release_apply_token)
    }
}

// codegen_bridge_box.hako — legacy shim only
// Keep the owner-looking path thin. Actual compat/proof implementation lives in
// `selfhost.compat.codegen.legacy_emit_object_bridge`.

using selfhost.compat.codegen.legacy_emit_object_bridge as LegacyEmitObjectBridgeBox

static box CodegenBridgeBox {
  method emit_object_args(args) {
    return LegacyEmitObjectBridgeBox.emit_object_args(args)
  }

  method link_object_args(args) {
    return LegacyEmitObjectBridgeBox.link_object_args(args)
  }
}

// LLVMTypesBox — 代表的なプリミティブ型のクエリ（v0）
static box LLVMTypesBox {
  i1(){ return "i1" }
  i8(){ return "i8" }
  i32(){ return "i32" }
  i64(){ return "i64" }
  f64(){ return "f64" }
  ptr(ty){ return ty + "*" }
}


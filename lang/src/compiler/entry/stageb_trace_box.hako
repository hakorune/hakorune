// StageBTraceBox — dev-only trace helper for mode-B compatibility.
// - Enabled when HAKO_STAGEB_TRACE=1
// - Keeps mode-B compatibility behavior unchanged（ログのみ追加）

static box StageBTraceBox {
  log(label) {
    local flag = env.get("HAKO_STAGEB_TRACE")
    if flag == null { return 0 }
    if ("" + flag) != "1" { return 0 }
    local msg = "[stageb/trace]"
    if label != null {
      msg = msg + " " + ("" + label)
    }
    print(msg)
    return 0
  }
}

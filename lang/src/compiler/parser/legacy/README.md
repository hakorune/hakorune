# Legacy Grammar Rejection Boundaries

This directory recognizes legacy spellings only to reject semantic parser
entry with registry-stable tags. It never produces compatibility transport,
AST, ProgramJSON evidence, MIR, runtime, or backend input.

Compatibility transport remains owned by Rust migration tooling.

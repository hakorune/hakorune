// emit_mir_json_v0_from_program_json_v0.hako
// Legacy Program(JSON v0) wrapper.
//
// Canonical Program(JSON v0) compat behavior now lives under
// `lang/src/mir/builder/compat/program_json_v0_entry.hako`.

using lang.mir.builder.compat.program_json_v0_entry as MirBuilderProgramJsonV0EntryBox

static box Main {
  main() {
    return MirBuilderProgramJsonV0EntryBox.main()
  }
}

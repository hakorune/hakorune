// emit_mir_json_v0.hako
// Legacy Phase-0 wrapper.
//
// Canonical AST-JSON compat behavior now lives under
// `lang/src/mir/builder/compat/emit_mir_json_v0.hako`.

using lang.mir.builder.compat.emit_mir_json_v0 as MirBuilderPhase0CompatBox

static box Main {
  main() {
    return MirBuilderPhase0CompatBox.main()
  }
}

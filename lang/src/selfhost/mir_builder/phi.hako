// phi.hako — PHI helpers (Scaffold)
// Phase-20.12b: define names only; fail-fast until real logic exists.

static box SelfhostPhiBox {
  merge2(dst, in1, in2) {
    print("[selfhost/mir_builder/phi:unimplemented:merge2]")
    return null
  }

  mergeN(dst, inputs) {
    print("[selfhost/mir_builder/phi:unimplemented:mergeN]")
    return null
  }
}

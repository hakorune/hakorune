// verify.hako — Selfhost MIR verifier (Scaffold)
// Phase-20.12b: minimal contract; fail-fast until real checks exist.

static box SelfhostMirVerify {
  verify(json_path) {
    print("[selfhost/mir_builder/verify:unimplemented]")
    return 2
  }
}

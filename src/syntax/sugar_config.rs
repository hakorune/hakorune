/*!
 * Syntax sugar config compatibility facade.
 *
 * `SugarConfig` and `SugarLevel` live in `hakorune-frontend-grammar`; this
 * module preserves the historical `crate::syntax::sugar_config::*` path.
 */

pub use hakorune_frontend_grammar::sugar_config::*;

//! Input providers for the Box callable registry.
//!
//! Providers translate existing domain truth into `BoxCallableRegistry` entries.
//! They are seed sources only; execution still goes through route plans.

pub mod builtin_type_registry;

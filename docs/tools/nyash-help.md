# `nyash --help` reference

Current CLI truth:

- [`docs/tools/cli-options.md`](./cli-options.md)
- `src/cli/args.rs`

Historical snapshot:

- `docs/archive/tools/nyash-help.md`

This path is intentionally a thin stub. The old captured help text was archived
in `phase-30x / 30xG2` so that `docs/tools/` keeps current operator-facing
material only.

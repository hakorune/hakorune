# Getting Started

Status: Historical
Scope: legacy getting-started path kept as a redirect stub.

The old guide described an earlier `nyash-rust` layout and `nyash` binary
commands. The active repository now uses Hakorune current-lane docs and staged
artifacts, so this page must not be used as an execution or language reference.

Use these current entries instead:

- `README.md` - repository-level build/run overview
- `CURRENT_TASK.md` - current development lane pointer
- `docs/reference/language/README.md` - current language reference entry
- `docs/reference/language/quick-reference.md` - current syntax summary
- `docs/reference/language/stage-profiles.md` - Stage0 / Stage1 usable surface
- `lang/README.md` - current `.hako` / Stage1 artifact lane notes

Historical tutorial content remains in git history. Rebuild user-facing
getting-started material from the current references before making it active
again.

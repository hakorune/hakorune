# Phase 138x Task Board

- [ ] A1 lock final owner graph (`Rust host microkernel` / `.hako semantic kernel` / `native accelerators`)
- [ ] A2 lock `ABI facade` as thin keep
- [ ] A3 lock `compat quarantine` as non-owner
- [ ] B1 select `Array owner` as the first `.hako` pilot
- [ ] B2 define `Map owner` and `String` as successor cuts
- [ ] C1 refresh current pointers / stop-lines
- [ ] D1 hand off to `phase-139x array owner pilot`

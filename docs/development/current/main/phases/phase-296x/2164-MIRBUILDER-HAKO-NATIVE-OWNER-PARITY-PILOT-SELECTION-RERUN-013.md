# 2164 - MIRBUILDER-HAKO-NATIVE-OWNER-PARITY-PILOT-SELECTION-RERUN-013

## Token

```text
MIRBUILDER-HAKO-NATIVE-OWNER-PARITY-PILOT-SELECTION-RERUN-013
```

## Purpose

Select the fourteenth small hand-authored `.hako` native owner parity pilot
after the `array_text_observer_executor_tag_formatter` adoption.

This card selects only a pilot target. It does not adopt new `.hako` code and
does not claim Source Selfhost.

## Selected Pilot

```text
selected_owner:
  sum_local_aggregate_layout_tag_formatter

selected_rust_surface:
  src/mir/sum_placement_layout.rs SumLocalAggregateLayout Display tags

selected_next_card:
  MIRBUILDER-SUM-LOCAL-AGGREGATE-LAYOUT-TAG-FORMATTER-RUST-ORACLE-FIXTURE-001
```

## Included Surface

```text
SumLocalAggregateLayout Display text
```

## Excluded Surface

```text
payload-type layout binding
sum placement layout refresh
layout summary formatting
lowering execution
MIR mutation
```

## Decision

```text
decision:
  SelectRustOracleFixture

reason_token:
  SumLocalAggregateLayoutTagFormatterSelectedAsFourteenthParityPilot

selected_next_card:
  MIRBUILDER-SUM-LOCAL-AGGREGATE-LAYOUT-TAG-FORMATTER-RUST-ORACLE-FIXTURE-001
```

## Non-Claims

```text
no Source Selfhost claim
no Hako adoption decision
no generated artifact edit authority
no payload-type layout binding migration
no sum placement layout refresh migration
no lowering execution migration
no MIR mutation migration
```

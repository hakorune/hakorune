# 3448 - MIRBUILDER-SCALAR-KNOWN-FASTPATH-POST-STRING-CALLER-ORIENTATION-PILOT-DESIGN-CONSULTATION-001

## Status

Design consultation stop. The String three-row caller-orientation authority
pilot and freshness rerun are green. Do not implement the next axis until the
proof boundary is selected.

## Evidence

```text
StringScalarI64Routes = authority-bearing caller-orientation pilot
CollectionScalarI64Routes = assertion-only, mixed receiver domains
NonDeleteWrite = assertion-only, mutation/Any-write boundary
DeleteSurfacePolicy = retired special case
```

## Consultation Question

Which next proof axis is authorized?

```text
A: one Collection row with an explicit receiver-domain contract
B: the full Collection four-row mixed-domain island
C: one non-Delete Write row with mutation boundary retained
D: formally scope a non-Delete caller-orientation basis, runtime=0
E: open Delete revival as a separate lane
F: park caller orientation and return to Source Selfhost route selection
```

The answer must name source authority, non-authority, fail-fast boundary,
exhaustive scope, and promotion conditions. Until then, keep runtime,
backend, mutation, publication, Delete, wide, and Source Selfhost claims at
`0`. Counts, names, paths, membership, and coverage are not proof.

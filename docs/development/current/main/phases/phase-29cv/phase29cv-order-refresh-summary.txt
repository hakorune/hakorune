Phase-29cv Order Refresh Summary
================================

Date: 2026-05-06
Todo: phase29cv-order-refresh

Scope:
- Refreshed P381FI and P381FN through the post-P381GG state
- Updated blocker descriptions to close the parser proof as diagnostics-only
- Clarified that wrapper cleanup plus imports/enum/defs owner cleanup are complete

Changes:

1. P381FI-STAGE0-CLEANUP-REMAINING-INVENTORY.md:
   - Updated blocker list to show concrete blockers with full call chains
   - Changed "Unsupported" labels to specific blockers (e.g., ParserBox.parse_program2)
   - Clarified that wrapper cleanup is complete (P381FO/FP/FQ)
   - Updated ordered checklist to reflect post-wrapper-collapse state

2. P381FN-CONCRETE-BLOCKER-ORDER.md:
   - Added P381FO/FP/FQ to the complete wrapper collapse list
   - Updated scope to include all six wrapper collapse cards
   - Clarified that injection methods are now direct (no wrappers)
   - Updated Result section to explicitly state wrapper cleanup is complete
   - Refreshed again after P381FT/P381FU/P381FV/P381FW/P381FX/P381FY/P381FZ/P381GA/P381GB/P381GC/P381GD/P381GE/P381GF/P381GG so enum_decls and defs are
     DirectAbi on the public BuildBox path, and defs no longer consumes
     FuncScannerBox.scan_all_boxes object records or parse_block2 result
     stripping; stale object-defs builders are removed and the method-body
     program seam is named by its Program(JSON v0) contract; the parser proof is
     closed as diagnostics-only; generic string-or-void sentinel const
     publication is shared; PatternUtil proof/return ownership is shared by its
     body module; BoxTypeInspector proof/return ownership is shared by its body
     module; T6 smoke/archive bucket inventory is locked as a mixed
     protected/referenced surface; smoke inventory summary orphan counts read
     the TSV class column; the first zero-ref v2 archive candidate list is
     fixed and deleted; the legacy root-smoke zero-ref group is lifecycle-
     classified

Validation:
- tools/checks/current_state_pointer_guard.sh: PASS

No conflicts with code worker:
- CURRENT_STATE.toml was updated only for latest-card/current-blocker pointers
- Code change is limited to same-module sentinel const helper extraction

Next steps:
- The remaining blocker lane is T6 legacy root-smoke zero-ref deletion
- Order now: delete only the P381GG legacy root-smoke group

# 216x-91 Task Board

Status: Closed

## Tasks

- [x] add the minimal folded route transport needed by current sum routes
- [x] read folded `placement_effect_routes` first in the sum local seed metadata helper
- [x] keep legacy sum metadata as compatibility fallback
- [x] pin the folded route path with metadata-bearing sum fixtures
- [x] sync current pointers

## Non-Goals

- no lowering shape rewrite
- no metadata lane deletion
- no generic transform widening in the same cut

## Exit

- the next `generic placement / effect` proving slice is landed for the current sum local seed metadata helper


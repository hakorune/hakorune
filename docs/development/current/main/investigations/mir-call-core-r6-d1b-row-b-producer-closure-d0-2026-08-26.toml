title = "MIR Call core R6 D1B Row B producer closure design"
status = "accepted_design_no_safe_slice"
observed_commit = "6149d2692a"
observed_at = "2026-08-26"
implementation_permission = false

[boundary]
starts = "function_call_preflight_route::Ordinary / Builder call target selection"
ends = "Builder physical Call publication after resolver, recovery, and name-Const paths"
includes = [
  "src/mir/builder/calls/method_resolution.rs:31-55",
  "src/mir/builder/calls/unified_emitter.rs:409-463",
  "src/mir/builder/calls/build.rs:104,307-364",
  "src/mir/builder/calls/static_resolution.rs:87-171",
  "src/mir/builder/normal_callable_semantic_package/install.rs:308-316,388-405 (source-authority observation only)",
  "same-brand catalog handoff, local-shadow precedence, and methodize profile parity",
]
excludes = [
  "ArrayElementWrite projection and selected-native decoder/ABI work",
  "JSON-v0/v1 ingress and the v1-error-to-v0 fallback hierarchy",
  "core_bridge, Rust VM, selected C terminal, and non-selected backends",
  "Method(Some) execution, MirCall/CallFlags, final Call schema, and new receipts",
]

[brief]
decision = "The two Builder Method(None) producer literals are an exhausted syntactic census; the six-level pre-effect precedence is accepted, but Row B semantic closure is NoSafeSlice until exact catalog transport, RuntimeData disposition, and recovery retirement are supplied."
source_authority = "same-brand declaration/source catalog -> CanonicalSameModuleCallableKeyV1 -> mir_symbol_projection()"
canonical_issuer = "qualified CallTarget::Global(owner.method/arity) -> CalleeResolverBox -> MirInstruction::call"
non_authority = [
  "current_static_box and hardcoded has_method",
  "StaticMethodId parsing, methodize env/trace, variable/name/Const text",
  "tail/module lookup, registry, receiver autoscan, args[0] recovery",
]
fail_fast = "the accepted owner/method/arity/brand precedence must be carried by the existing preflight owner before build.rs argument descent, child effects, name Const allocation, block insertion, or physical publication"
smallest_next_slice = "Design-only closure of the existing preflight target transport, RuntimeData Global disposition, and post-argument recovery delete set; no code or guard expansion yet."
non_claims = [
  "the two literal sites alone prove caller-zero or semantic closure",
  "D1C1 bare FunctionCall target claim",
  "Builder-external Method(None) or ingress/runtime retirement",
  "fallback removal, backend activation, warning cleanup, or schema cutover",
]

[census]
syntactic_producer_count = 2
syntactic_producers = [
  "method_resolution_static_none",
  "unified_emitter_methodize_none",
]
semantic_delete_boundary = [
  "build.rs post-argument unique-static recovery and direct publication",
  "build.rs target-name Const construction for this route",
  "static_resolution.rs tail/suffix alternate-target routes",
  "materializer.rs:41-119 Global recovery/materialization edge",
]
outside_blockers = [
  "JSON-v0 Method(None) ingress",
  "core_bridge Const/singleton methodize reissuers",
  "Rust VM and selected native terminal recovery",
]

[finite_state]
current_owner_exact_static = "same-brand current owner + exact method/arity -> qualified Global; this outranks a same-name local Value"
unique_other_static = "same-brand unique other-owner static + exact name/arity -> qualified Global only after Local Value and Extern are absent"
exact_receiver = "exact source/resolver receiver -> preserve Method(Some)"
hardcoded_static_probe = "current_static_box + has_method without catalog key -> CutoverBlockerOpen; never publish Method(None)"
local_shadow = "observed current order is hardcoded static Method(None) > local Value; this is parity evidence only, not the final precedence decision"
catalog_absent = "absent/foreign/ambiguous/arity mismatch -> typed reject before argument effects"
runtime_data_global = "incoming qualified Global without provenance -> cohort census required; methodize mutation cannot be deleted by guess"
methodize_profile = "unset/0/1 -> canonical target and result parity; profile projection remains separate authority"
outside_builder = "Builder-external Method(None) -> retain as D1B blocker, not this row's closure"

[reaudit_2026_08_26]
decision = "Parent Row B remains NoSafeSlice after the bounded exact-target child landed; no parent implementation or guard expansion is authorized."
head = "6149d2692a"
worktree = "child implementation is landed; focused tests, child/corridor/pointer guards, rustfmt, and diff checks are green"
same_brand_product = "Normal package install co-seals the same-brand catalog (normal_callable_semantic_package/install.rs:308-316,388-405); the landed child transports the Cataloged caller through RawBrandCallAuthorityV1 and the existing Ordinary preflight, while parent RuntimeData/recovery remains open."
normal_direct_target = "ResolvedDirectCallTargetV1 is unavailable on this normal source path because the source package is built without a callable index (normal_callable_semantic_source.rs:142-169,203-215,499-504)."
preflight_gap = "The exact-target transport gap is closed by 6149d2692a; the remaining parent gap is RuntimeData Global provenance plus post-argument materializer/unified-emitter recovery disposition."
required_design_amendment = "Accepted candidate: lend the existing same-brand token through the raw scope and retain the canonical key or qualified Global in the existing preflight owner; no new Verified/Prepared semantic receipt is required."

[shadow_order]
status = "AcceptedDecisionRemainingBlockers"
observed_order = "builtin Global > hardcoded static Method(None) > local Value > Extern"
evidence = "method_resolution.rs:26-65,126-154; current collision test preserves static-over-local"
required_decision = "Accepted total order: BuiltinGlobal > exact same-brand CurrentOwnerStatic > Local Value > Extern > unique other-owner catalog static > typed reject."
non_authority = "resolver shadow observation currently records non-Brand FunctionCall without a lexical shadow fact; it cannot justify a precedence change."

[precedence_decision]
name = "D1B-ROW-B-PRE-EFFECT-TARGET-PRECEDENCE-D0"
status = "accepted_decision_with_remaining_row_blockers"
order = [
  "BuiltinGlobal",
  "CurrentOwnerStatic(same-brand, exact owner/name/arity)",
  "LocalValue",
  "Extern",
  "UniqueOtherStatic(same-brand, exact name/arity)",
  "TypedReject",
]
source_rule = "CurrentOwnerStatic requires RawInvocationRootLineageV1::Cataloged caller owner plus catalog.declaration_for(StaticBoxMethod, owner, name, arity); UniqueOtherStatic reuses BareStaticRecoveryDecisionV1 only after LocalValue and Extern absence."
positive = [
  "builtin + same-name local/catalog -> BuiltinGlobal",
  "current-owner exact + same-name local -> qualified current-owner Global",
  "no current-owner exact + local + unique other-owner static -> LocalValue",
  "local env.* + Extern spelling -> LocalValue; no local -> Extern",
  "no local/Extern + unique static -> qualified Global",
]
negative = [
  "foreign/missing brand or caller lineage -> pre-effect typed reject",
  "current-owner method family with no exact arity -> no local/unique fallback",
  "ambiguous or arity-overflow unique static -> typed reject; no retry",
]
parity = "Preserve builtin > current-static > local > Extern > unique-static semantics; only valid current-static representation changes from Method(None) to qualified Global."
collision_zero_claim = "not accepted; existing source facts do not prove zero and method_resolution.rs:126-154 contains a real static/local collision."

[runtime_data_global]
status = "CutoverBlockerOpen"
scope = "census-only; families outside the stated preflight boundary are dispositioned as outside Row B and are not modified by this row"
authority_gap = "CallTarget::Global(String) and resolver.rs:67-74 erase catalog provenance before unified_emitter.rs:409-463 methodize classification."
producer_families = [
  "build/static_resolution",
  "rewrite/known.rs",
  "stmts/print_stmt.rs",
  "ops/{arithmetic,comparison,unary}.rs",
  "control_flow/plan/lowerer/{emission_port,effect_emission}.rs",
  "calls/method_call_terminal.rs",
  "calls/emit.rs",
]
required_disposition = "Inventory each family as preserve-qualified-Global, typed-reject, or outside Row B; do not delete methodize mutation by String/name inference."

[recovery_disposition]
status = "CutoverBlockerOpen"
materializer = "src/mir/builder/calls/materializer.rs:41-119"
materializer_caller = "src/mir/builder/calls/unified_emitter.rs:384-405"
finding = "The materializer caller and its Err-side Global recovery are separate from materializer.rs:81-119 unique-static recovery; both remain old edges in the delete census."
next_decision = "Mark the edge RetainGuarded with a reopen trigger or include it in the finite delete set; either choice must be documented before fast."

[closure_decision]
name = "D1B-ROW-B-PRE-EFFECT-TARGET-CLOSURE-D0"
subchecks = [
  "existing preflight target transport without a new semantic receipt",
  "existing same-brand product transport",
  "total local-shadow order",
  "RuntimeData qualified-Global adjacent census",
  "materializer/unified-emitter recovery disposition",
]
implementation_permission = false

[preflight_transport]
status = "AcceptedDesignAmendmentChildRowLanded"
existing_owner = "RawBrandCallAuthorityV1 + PreparedRawFunctionPreflightRouteV1::Ordinary"
source_authority = "same-allocation VerifiedSameModuleCallableDeclarationCatalogV1 -> BareStaticRecoveryDecisionV1::Unique(CanonicalSameModuleCallableKeyV1) -> mir_symbol_projection()"
candidate = "Landed bounded child: extend the existing brand/lowering scope and Ordinary preflight payload, then consume the canonical Callee once before drive_call_arguments_v1; no new Verified*/Prepared* semantic receipt was introduced."
evidence = "6149d2692a; function_call_preflight_route.rs, build.rs, raw_expression_dispatch/mod.rs:341-353, callable_declaration_catalog/recovery.rs:39-55, callable_declaration_catalog/key.rs:50-52"
current_gap = "The parent Row B gap is no longer target transport; RuntimeData Global provenance and materializer/unified-emitter recovery remain unresolved."
fail_fast = "foreign, ambiguous, unresolved, or precedence-invalid target must terminalize at the dispatcher/preflight seal before argument lowering, child effects, name Const, or block publication"
blocker = "The bounded child consumed the target in build.rs and published the canonical Call; parent Row B RuntimeData/recovery blockers remain open."

[exact_target_prepare_consume_row]
name = "D1B-ROW-B-EXACT-TARGET-PREPARE-CONSUME-I0"
status = "landed_bounded_child_row"
implementation_permission = false
landed_commit = "6149d2692a"
focused_test = "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib function_call_preflight_route::tests (8 passed)"
focused_guard = "tools/checks/mir_call_d1b_exact_target_child_guard.sh"
parent_status = "MIR-CALL-CORE-R6-D1B-ROW-B-PRODUCER-CLOSURE-D0 remains design_stop; this child does not close the parent row"
boundary_starts = "RawInvocationRootLineageV1::Cataloged(caller) entering RawBrandCallAuthorityV1::InstalledNonBrand"
boundary_ends = "the selected Cataloged ordinary FunctionCall emits one canonical MirInstruction::call after one ordered argument descent"
includes = [
  "src/mir/builder/calls/function_call_brand_source_demand.rs: caller transport from the existing located root",
  "src/mir/builder/calls/function_call_preflight_route.rs: six-level target selection and existing Ordinary completion payload",
  "src/mir/builder/calls/build.rs: by-value target consume, argument descent, and canonical publication",
  "src/mir/builder/raw_expression_dispatch/mod.rs:341-353 existing production ingress",
  "function_call_preflight_route_tests.rs: positive/negative target-selection evidence",
]
excludes = [
  "relationless compatibility, non-Cataloged roots, Brand/TypeOp/Math/FastMem/str normalization routes",
  "RuntimeData Global methodize and unified_emitter.rs shared reissuer",
  "materializer and unique-static recovery outside the selected Cataloged cohort",
  "JSON ingress, VM/C/non-selected backends, MirCall/CallFlags, and final Call schema cutover",
  "new Verified*/Prepared* semantic receipts and global caller-zero claims",
]
source_authority = "RawInvocationRootLineageV1::Cataloged(caller) plus the same installed VerifiedSameModuleCallableDeclarationCatalogV1"
canonical_issuer = "existing CalleeResolverBox for Global/Extern/Value target forms, then MirBuilder::call / MirInstruction::call as the physical issuer"
non_authority = [
  "current_static_box and hardcoded has_method",
  "StaticMethodId, name Const text, methodize env/trace, tail/module/registry lookup, receiver autoscan, args[0] recovery",
]
fail_fast = "foreign/missing catalog, current-owner arity mismatch, ambiguous/overflow recovery, or any precedence conflict rejects at preflight before argument descent, child effects, name Const allocation, block insertion, or Call publication"
one_pass = "same-brand authority -> existing Ordinary completion seals a canonical Callee -> build.rs consumes it once -> drive_call_arguments_v1 once left-to-right -> canonical Call publication"

[exact_target_prepare_consume_row.precedence]
order = [
  "BuiltinGlobal",
  "CurrentOwnerStatic(same-brand, exact owner/name/arity)",
  "LocalValue",
  "Extern",
  "UniqueOtherStatic(same-brand, exact name/arity)",
  "TypedReject",
]
issuer_rule = "Builtin/Extern use existing CalleeResolverBox classification; current-owner and unique static use CanonicalSameModuleCallableKeyV1::mir_symbol_projection() through the same Global resolver; local uses the existing variable ValueId through the resolver's Value route"
arity_rule = "current-owner method family with no exact arity is TypedReject and may not fall through to local, Extern, or unique-static recovery"

[exact_target_prepare_consume_row.states]
states = [
  "ReadyBuiltinGlobal",
  "ReadyCurrentOwnerGlobal",
  "ReadyLocalValue",
  "ReadyExtern",
  "ReadyUniqueOtherGlobal",
  "RejectedForeignOrMissingCatalog",
  "RejectedCurrentOwnerArityMismatch",
  "RejectedAmbiguousOrOverflow",
]
terminal_policy = "every state is issued by the preflight owner; Ready states carry an exact Callee into the existing completion, Rejected states return a typed pre-effect error, and no state retries or defaults"

[exact_target_prepare_consume_row.acceptance]
positive = [
  "Cataloged same-owner static resolves to the identical owner.method/arity Global key even when a same-name local exists",
  "builtin beats catalog/local, local beats Extern and unique other static, and Extern is retained when no local exists",
  "target is consumed exactly once, arguments descend exactly once in source order, and dst/effects/result parity is preserved",
]
negative = [
  "foreign/missing catalog, current-owner wrong arity, ambiguous candidate, and arity overflow reject before child effect, name Const, block, or Call",
  "argument failure produces no Call and no retry/reprepare after the target was consumed",
]
guard = "tools/checks/mir_call_d1b_exact_target_child_guard.sh plus focused tests prove one target issue, one by-value consume, zero target drop, zero selected late resolver/recovery/name-Const re-entry, and one canonical MirInstruction::call"
non_claims = [
  "parent Row B RuntimeData Global provenance or unified-emitter methodize closure",
  "all Builder Method(None) literals, all recovery callers, or final mandatory-Callee schema",
  "JSON-v0/v1 ingress behavior, backend activation, and warning/panic cleanup",
]

[acceptance]
required_before_fast = [
  "exact catalog/owner handoff is available before build.rs argument descent",
  "local Value, builtin Global, Extern, and static catalog precedence is total and tested",
  "RuntimeData qualified-Global provenance cohort is finite and its caller disposition is named",
  "post-argument recovery, name Const, tail/suffix retry, and direct publication delete set is complete",
  "methodize unset/0/1 positive parity and missing/ambiguous/foreign/arity negative pre-effect rejection are available",
  "guard is function-window scoped and does not global-grep JSON compatibility or non-Callee receiver fields",
  "dirty build.rs overlap is resolved before implementation permission (observed clean at 2ba2cdff97)",
  "the re-audit transport, shadow, RuntimeData Global, and materializer dispositions are accepted in this design card",
  "existing preflight transport amendment is accepted without minting a new semantic receipt",
  "the six-level precedence Decision and its positive/negative/parity matrix are accepted",
]
positive = [
  "exact static catalog -> same qualified Global symbol, args, dst, effects, and result",
  "instance Method(Some), local Value, builtin Global, and Extern precedence unchanged",
]
negative = [
  "hardcoded Method(None) publication = 0 for the selected Builder window",
  "absent/ambiguous/foreign/arity mismatch rejects before child effect/name Const/block publication",
  "tail/name/module/registry/receiver/args[0] retry = 0 inside the closed boundary",
]
parity = ["methodize unset vs 0 vs 1", "static/instance/local/builtin/extern target and result behavior"]
guard = "extend only a scoped existing corridor guard after the row is fast; no 760-line guard growth and no global receiver/Method(None) grep"

[reopen]
triggers = [
  "same-brand catalog product is issued before argument descent",
  "local-shadow and RuntimeData Global cohorts gain exact owner evidence",
  "a new Builder producer/recovery caller appears",
  "D1B ingress or selected terminal disposition changes",
]

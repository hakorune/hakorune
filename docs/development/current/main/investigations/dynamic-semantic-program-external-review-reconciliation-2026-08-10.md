# Dynamic semantic-program external review reconciliation

Status: reconciled task map; second external audit folded; not the current execution pointer
Date: 2026-08-10

## Current-first note

The external review was taken against the earlier `aa52052d` snapshot. The
current pointer has since advanced to `CALLABLE-PARAMETER-DEMAND-I0`; this
review must not rewind the active lane to the already-closed Dynamic
Home-capability stop. `CURRENT_STATE.toml` remains the only current-row
authority.

This reconciliation is therefore a parked-debt index, not a copied current
dashboard. No new investigation card is created for an item already owned by
the tasks below.

## Accepted architecture

The recursive Recipe verifier, neutral V1/V2 JoinSig engine, internally
derived root After, external Fault cut-point catalog, and non-splittable
Dynamic semantic program remain the authority. There is no V2-to-V1
conversion, profile callback, Fault JoinSig edge, or Dynamic-to-Home inference.

## Disposition

| Review item | Current disposition | Task boundary |
| --- | --- | --- |
| stale `CURRENT_STATE.toml` pointers | already corrected before this reconciliation | no duplicate edit |
| stale `10-Now.md` concrete row | current-doc policy already makes it a thin SSOT reader | no copied row/history |
| Fault operation wildcard | closed by `DYNAMIC-OPERATOR-EXECUTION-CONTRACT-I0` exhaustive classification | compiler-enforced |
| V9/V17 operator lifecycle | closed by `DYNAMIC-OPERATOR-CARRIER-LIFECYCLE-I0` | exact private co-seal |
| current-state cross-field guard | valid tooling debt | `CURRENT-POINTER-CROSSFIELD-CONSISTENCY-R0`, separate BoxShape commit |
| Fault reject detail preservation and caller-zero visibility | valid diagnostic/API debt | remaining slice of `DYNAMIC-FAULT-CATALOG-EXHAUSTIVE-R0`, before first production consumer |
| nested-predicate whitelist in common JoinSig flow | valid bounded-core debt | `LOOP-JOINSIG-RECURSIVE-TOPOLOGY-AUTHORITY-D0`, after current Dynamic exit lane |
| both-arm exit pair enumeration | future control-algebra debt | `LOOP-JOINSIG-BRANCH-DISPOSITION-D0`, only before a new required pair |
| sole-root-carrier closure | intentional bounded helper | `LOOP-JOINSIG-MULTI-CARRIER-CLOSURE-D0`, only before a multi-carrier V2 consumer |
| fixed-shape Dynamic producer | intentional migration ingress | recursive Facts-to-Recipe assembler only when a second admitted Dynamic shape exists |
| `flow.rs` / `typed_schema_v2.rs` growth | valid physical debt | split before adding vocabulary to either threshold file |

## Executable task receipts

### P0 tooling — existing task

`CURRENT-POINTER-CROSSFIELD-CONSISTENCY-R0` owns the remaining guard work.
The live pointer and thin `10-Now.md` mirror are already repaired; do not
repeat those edits. The implementation row must add table-driven fixtures
that reject stale row/card/mode combinations while leaving compiler behavior
unchanged.

### P1 Fault surface — existing task

`DYNAMIC-FAULT-CATALOG-EXHAUSTIVE-R0` owns the remaining two changes only:

```text
typed Fault-cut-point reject preservation
caller-zero Fault view visibility narrowing
```

The exhaustive `LoopOperationExecutionClassV2` projection is already landed.
Do not reopen operation classification, add another faultability table, or
move Fault into JoinSig. Acceptance requires focused negative coverage for
missing relation, identity conflict, shape mismatch, and coverage mismatch,
plus a structural guard that prevents a new public/caller-zero surface before
the exit-transaction consumer exists.

### P2 bounded recursive-core debt — trigger-gated tasks

These task tokens are intentionally recorded here rather than expanded into
standalone cards before their first consumer exists:

| Task | Open only when | Required direction |
| --- | --- | --- |
| `LOOP-JOINSIG-RECURSIVE-TOPOLOGY-AUTHORITY-D0` | a second admitted nested topology needs the common engine | remove the shape whitelist through a verifier-issued topology capability or a genuinely general recursive algorithm; never use a profile callback |
| `LOOP-JOINSIG-BRANCH-DISPOSITION-D0` | a new both-arm exit pair is required | verify typed branch disposition, target compatibility, and payload compatibility instead of adding another exact pair special case |
| `LOOP-JOINSIG-MULTI-CARRIER-CLOSURE-D0` | the first multi-carrier V2 consumer appears | add a sibling closure-set product; do not widen or rename the bounded `sole_root_carrier` helper |
| `DYNAMIC-RECIPE-RECURSIVE-ASSEMBLER-D0` | a second fixed Dynamic producer would otherwise be added | move neutral structural Facts through one recursive assembler; keep current fixed producer as bounded ingress |
| `CURRENT-STATE-REGISTRY-COMPACTION-R1` | the active compiler row is at a clean docs-topology boundary | move historical indexes out of `CURRENT_STATE.toml` without changing any live scalar or restart route |

`typed_schema_v2.rs` is at the effective no-additions threshold. Any new
operation/control vocabulary first splits its verification/value/control
owners. Fault, Home, or Completion logic must not be added to the common
JoinSig `flow.rs`.

## Ordered tasks

1. Do not interrupt the current `CALLABLE-PARAMETER-DEMAND-I0`.
2. Close `DYNAMIC-CARRIER-INGRESS-LIFECYCLE-I0`, then the already-selected
   rebind and CFG-complete flow rows.
3. Finish the remaining Fault reject/visibility slice no later than the first
   exit-transaction or production consumer.
4. Continue cleanup projection, Completion, exit transaction, and
   physicalization without importing the parked JoinSig generalizations.
5. Run `CURRENT-POINTER-CROSSFIELD-CONSISTENCY-R0` at an isolated
   docs-tooling BoxShape boundary. It validates `work_mode`,
   `current_execution_row`, the active rolling-card body,
   `latest_card_path`, and the mode-specific next-card fields together.
6. Open a trigger-gated P2 task only for its first real consumer; never widen
   the recursive algebra speculatively.

## Nonclaims

This reconciliation creates no Home, cleanup, rebind, Completion, physical
CFG/MIR, new Dynamic source shape, profile callback, retry, or fallback. It
does not make parked cleanup rows current.

title = "MIR Call D1B direct-call source inventory co-seal design"
status = "b1_current_head_c0_design_stop"
base_observed_commit = "632c674a76"
base_observed_at = "2026-08-26"
observed_commit = "a54680b7c7"
observed_at = "2026-08-28"
implementation_permission = false
permission_scope = "MIR-CALL-D1B-FOREST-WIDE-OBSERVATION-GATE-R0 is closed; B1 current-head disposition and simultaneous Callee/CallTarget cutover remain design-gated; package/loan/issuer/dispatcher publication is closed"
parent_row = "docs/development/current/main/investigations/mir-call-d1b-toplevel-direct-target-index-handoff-d0-2026-08-26.toml"
decision_id = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-INVENTORY-COSEAL-D0"
safety_child = "docs/development/current/main/investigations/mir-call-d1b-index-none-direct-calls-failfast-d0-2026-08-26.toml"
observation_child = "docs/development/current/main/investigations/mir-call-d1b-call-observation-profile-d0-2026-08-26.toml"
safety_i0 = "docs/development/current/main/investigations/mir-call-d1b-index-none-direct-calls-failfast-i0-2026-08-26.toml"
decision = "Before a general identity-only callable index can exist, the same resolver session must issue a complete owner-qualified direct-call source inventory. An observed FunctionCall may become Targeted, KnownNonDirect, TypedReject, or Parked, but it may not disappear when callable_index is absent or when SelectedCallable traversal is closed."

[boundary]
starts = "selected normal-source and InstanceConstructor source bodies entering the same resolver/semantic-package session"
ends = "complete owner/site direct-call inventory and finite disposition co-sealed before package publication, body lowering, argument effects, or Call publication"
includes = [
  "SelectedCallable traversal and canonicalize_draft direct-call accounting",
  "callable_index=None with non-empty direct_calls as an explicit typed failure",
  "RawInvocationRootLineageV1 preservation for Cataloged, TopLevel, and InstanceConstructor",
  "owner-qualified SourceExprSiteV1 inventory and same-session constructor forest/root relation",
  "duplicate, missing, foreign, and ambiguous direct-call site disposition",
]
excludes = [
  "target synthesis from name, arity, physical_symbol, constructor symbol, box name, or collector order",
  "general callable-index representation or ResolvedDirectCallTargetV1 issuance",
  "Method(None), methodize, CalleeGuard, UnknownBox, JSON ingress, Call schema, VM/native, and warning cleanup",
  "RawCompatibility, special-form implementation, and existing exact-i64 index generalization",
]

[brief]
decision = "Direct-call inventory is a prerequisite to the parent identity-index handoff. The inventory owner is the existing semantic package/resolver issuer; it records source identity and disposition only, and never reconstructs a Callee. A closed traversal or an empty callable index must not silently drop an observed FunctionCall. Before a new Cataloged issuer is opened, the B1 structural global carrier must be connected to the canonical Callee/CallTarget shape; issuing a new Callee::Global(String) would recreate the retired String authority."
source_authority = "parser/source identity plus the same-brand source catalog and resolver compilation session; RawInvocationRootLineageV1 is provenance and remains intact until disposition"
canonical_issuer = "the existing normal semantic package issuer and its same-session resolver continuation co-seal an owner-qualified direct-call inventory before package publication"
non_authority = [
  "SelectedCallable rejection by itself",
  "callable_index=None -> empty map",
  "NormalTopLevelFunctionDraftAdmissionV1 physical_symbol/arity",
  "source name/arity formatting, collector last-wins, physical symbol lookup, or a second resolver continuation",
]
fail_fast = "If a FunctionCall is observed but cannot be assigned an owner-qualified disposition in the same resolver session, abort the unpublished package before install, body lowering, argument descent/effects, block mutation, or Call publication."
smallest_next_slice = "MIR-CALL-GLOBAL-TARGET-B1-CURRENT-HEAD-C0: exhaust the finite Global producer/consumer disposition and freeze the simultaneous Callee/CallTarget structural cutover. The forest-wide observation gate is landed; no target, loan, dispatcher, or Call publication is open."
non_claims = [
  "the inventory does not issue a target or generalize the exact-i64 index",
  "lineage preservation does not authorize Global synthesis from names or arity",
  "no post-argument recovery deletion or mandatory-Callee cutover",
]

[observed]
selected_traversal = "SelectedCallableV1 now permits ordinary FunctionCall traversal in src/mir/resolved_semantics/shadow/traversal_profile.rs and shadow/expr.rs; the call records an unissued observation and visits arguments once."
silent_drop = "The forest-wide observation gate landed at b176c98276; package batch and source admission now share one all-owner predicate, so root-only checks cannot silently drop nested observations. Target/package/loan publication remains closed by the B1 C0 boundary."
lineage_collapse = "function_call_brand_source_demand.rs preserves caller only for Cataloged and collapses TopLevel/InstanceConstructor into InstalledNonBrand { caller: None }; RawInvocationRootLineageV1 exists upstream but is lost before disposition."
package_order = "normal_callable_semantic_package issuer co-seals catalog, selected batch, and constructor batch before prepare_install/commit, but no direct-call inventory field or completeness check exists before install/body lowering."
late_route = "caller-less ordinary calls still enter PreparedRawOrdinaryFunctionCompletionV1::Resolved and can lower arguments before resolve_call_target, unique-static/tail recovery, and name-Const publication."
existing_authority = "VerifiedSelectedNormalCallableSourceInventoryV1 and VerifiedResolvedSourceSiteInventoryV1 carry source/declaration facts but no body-call inventory; the exact-i64 VerifiedCallableIndexV1 is a separate cohort and cannot be generalized sideways."
resolver_entrances = "The selected/Brand resolver, InstanceConstructor semantic batch, and older builder normal_callable_semantic_source ingress are distinct entrances; production/caller-zero classification of the older ingress is a prerequisite to any all-callers claim."
lineage_source = "RawInvocationRootLineageV1 already carries Cataloged, TopLevel, and InstanceConstructor upstream; the bounded task is to preserve this provenance through disposition without turning names, box keys, or symbols into targets."
i0_connection = "The landed index-none safety I0 is a typed abort at canonicalize_draft; it is a child safety edge and does not replace the same-session owner/site inventory or pre-shadow observation design."

[worker_audit]
source = "six earlier read-only audits plus three D0-C read-only audits at a01f2bbed9; no code, fixture, or Cargo change"
verdict = "NoSafeSlice remains: package issuer has no direct-call inventory/co-seal field, SelectedCallable closes ordinary FunctionCall traversal, and TopLevel/InstanceConstructor lineage still collapses to caller=None before disposition."
next_bounded_design = "D0-C package co-seal boundary census: fix the exact prepare_install/commit boundary, finite SelectedNormal/Constructor/non-direct/missing/foreign/ambiguous/index-none matrix, one owner/site issuance point, and pre-effect abort contract."
implementation_gate = "Only the forest-wide observation helper row is open; target publication, package/loan transport, lineage mapping, dispatcher changes, and backend changes remain closed."
next_design_row = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-OWNER-LINEAGE-COSEAL-D1: reuse existing forest/site authorities for Cataloged, TopLevel, and InstanceConstructor; no target or public receipt."

[d0_c_review]
verdict = "Architecture Accept with amendments; implementation remains NoSafeSlice. The three audits agree that the existing shadow/resolver observation and existing semantic package issuer must be reused, while no second AST scan, target reconstruction, or recursive loan transport is allowed in this design row."
observation_owner = "Use the existing shadow/resolver traversal as the single FunctionCall observation point. SelectedNormal and InstanceConstructor currently close ordinary FunctionCall traversal, so D0-C specifies the observation contract only; it does not authorize a second AST walk or package-side AST rescan."
inventory_payload = "The semantic inventory records existing OwnedExprSiteV1 plus a typed disposition and same-session owner/brand evidence. It does not carry Callee, CallTarget, String/name/arity, physical symbol, ValueId, AST, or a resolver continuation. RawInvocationRootLineageV1 remains provenance in the raw transport and is not converted into target authority."
issuer_boundary = "The existing normal semantic package issuer is the sole co-seal point after its catalog/selected/constructor batches are available (issuer.rs:146-160) and before the unpublished package is returned. prepare_install may validate completeness, but commit remains a non-fallible publication step and must not resolve missing calls."
lineage_rule = "Cataloged, TopLevel, and InstanceConstructor provenance must survive to disposition without InstalledNonBrand { caller: None }. Provenance does not authorize Global synthesis from a name, arity, box key, or constructor symbol; absent exact relation is TypedReject or explicit compatibility park."
pre_effect_rule = "For this row, pre-effect means before Builder/MIR block mutation, collector mutation, argument lowering, and Call publication. Read-only semantic shadow observation may record a Call effect before the raw lowering boundary; requiring semantic-effect zero would be a separate design row."
loan_boundary = "No direct-call loan field, tuple bundle, recursive reborrow, Clone, Rc, Arc, or RefCell is added by D0-C. Those are later I0 mechanics only after issuer/co-seal and lineage authority are closed."
finite_states = [
  "OwnerQualifiedSiteReady",
  "KnownNonDirectDisposition",
  "TopLevelLineagePreserved",
  "InstanceConstructorLineagePreserved",
  "IndexNoneWithDirectCalls",
  "DuplicateSite",
  "MissingSite",
  "ForeignSite",
  "AmbiguousSite",
  "PackageAbortBeforeEffects",
  "InventoryCoSealed",
  "ParkedCompatibility",
]
acceptance = [
  "SelectedNormal and InstanceConstructor FunctionCall observations are recorded exactly once in the existing traversal contract and receive one owner-qualified site/disposition in the same resolver session.",
  "TopLevel/InstanceConstructor provenance is preserved; no caller=None collapse occurs before disposition.",
  "Missing, duplicate, foreign, ambiguous, and index-none-with-calls states abort the unpublished package before Builder/MIR/collector/argument effects.",
  "InventoryCoSealed is the only terminal for this design; no Callee is reconstructed and no post-argument resolver or recovery is introduced.",
]
no_safe_slice = [
  "SelectedCallable requires a second AST traversal or package-side AST rescan.",
  "The only available relation is name/arity/physical-symbol synthesis.",
  "A new public Verified*/Prepared* semantic receipt or generic target payload is required.",
  "Lineage must be collapsed to caller=None, or a loan must use Clone/Rc/RefCell/unsafe to cross recursion.",
  "The direct inventory can only be completed after argument lowering or commit publication.",
]
next_task = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-OWNER-LINEAGE-COSEAL-D1"

[forest_wide_observation_gate_r0]
current_execution_row = "MIR-CALL-D1B-FOREST-WIDE-OBSERVATION-GATE-R0"
status = "landed"
implementation_permission = false
commit = "b176c98276"
decision = "Make the package batch and source admission use one private forest-wide predicate over every Function owner, including nested lambdas. This closes the root-only inspection gap without issuing a target or changing the lowering route."
source_authority = "Existing SelectedCallable shadow/resolver forest and its owner-scoped direct_call_observations"
canonical_issuer = "No issuer is opened by this row; the existing package/source terminals only consume the shared predicate"
non_authority = [
  "forest.roots() as a completeness check",
  "name/arity lookup, CalleeResolverBox, package inventory, RawInvocationRootLineage, or target synthesis",
  "new Verified*/Prepared* receipt, loan, dispatcher, or Call publication",
]
fail_fast = "If any Function owner in any selected forest contains an unissued direct-call observation, reject before package installation; root-only, nested-only, and mixed-owner cases must agree."
allowed_files = [
  "src/mir/resolved_semantics/direct_call_inventory_gate.rs (new private helper module)",
  "src/mir/resolved_semantics/mod.rs (module declaration)",
  "src/mir/callable_semantic_batch/issuer.rs (delegate existing check)",
  "src/mir/builder/normal_callable_semantic_source.rs (delegate existing check)",
  "existing resolved-semantics or normal-callable package test files for focused negative coverage",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh (extend existing reusable observer guard)",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/builder/recursive_child_lowering.rs",
  "crates/hakorune_mir_defs/src/call_unified.rs",
]
acceptance = [
  "one shared helper inspects every Function owner in each forest",
  "root-only, nested-only, and root-plus-nested observations all reject the existing package batch before install",
  "forests without observations preserve the existing success path",
  "no target, loan, CalleeResolverBox, argument lowering, MIR block mutation, or backend behavior changes",
]
tests = "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib normal_callable_semantic_package::resolver_deferred_tests: 5 passed, 0 failed; exact nested-only test: 1 passed, 0 failed"
guard = "bash tools/checks/current_state_pointer_guard.sh: ok; bash tools/checks/run_row_guard.sh --only mir-call-d1b-cataloged-affine-loan-lifecycle: observer_i0 ok; touched normal_callable_semantic_source.rs remains 730 lines (<760)"
result = "Root-only, nested-only, and mixed-owner unissued observations now share the same forest-wide package/source gate. Target issuance, package/loan transport, raw dispatcher, and Call publication remain zero."
no_safe_slice = [
  "the helper requires a second AST traversal or a new semantic receipt",
  "the package batch and source admission cannot share the same owner-wide predicate",
  "nested coverage requires changing target publication, package install, or the raw dispatcher",
]

[boundary_census]
package_issuer = "src/mir/normal_callable_semantic_package/issuer.rs:134-161 issues instance-constructor, source catalog, resolved batch, and selected mapping; src/mir/normal_callable_semantic_package/model.rs:35-52 has no direct-call inventory field."
prepare_install = "src/mir/normal_callable_semantic_package/install.rs:272-284 checks only callable_declaration_catalog_vacant and returns the unpublished package; no direct-call coverage is validated."
commit = "src/mir/normal_callable_semantic_package/install.rs:287-333 discards the root projection, installs the catalog, and creates the ordinary-new ledger; this is the package publication boundary to guard."
normal_lifecycle = "src/mir/builder/normal_default_root_catalog_lifecycle.rs:247-267 issues the package, :271-395 consumes pre-effect projections, and :429-440 calls prepare_install/commit before the installed package is handed to lowering."
installed_port = "src/mir/normal_callable_semantic_package/install.rs:723-737 only completes selected-key and ordinary-new claim consumption; it does not verify direct-call site coverage."
raw_call_entry = "src/mir/builder/raw_expression_dispatch/mod.rs:341-353 asks Brand authority and immediately prepares/lowers the raw FunctionCall; src/mir/builder/calls/function_call_preflight_route.rs:141-178 maps caller=None to relationless/ordinary routes."
post_effect_route = "src/mir/builder/calls/build.rs:90-125 lowers Targeted arguments safely, but Resolved arguments at :111-124 are lowered before :311-369 resolve_call_target, unique-static recovery, tail recovery, and name-Const publication."
lineage_loss = "src/mir/builder/calls/function_call_brand_source_demand.rs:75-91 retains caller only for Cataloged and maps TopLevel/InstanceConstructor to InstalledNonBrand { caller: None }; D0-C must carry root/site without making it a target authority."
co_seal_owner = "the existing semantic package issuer must validate the complete owner/site/disposition inventory before prepare_install; raw Builder ports, collector order, and a second resolver are non-authority."

[boundary_matrix]
selected_normal = "Observed FunctionCall + owner/site + same-session lineage -> disposition; closed traversal or missing relation -> PackageAbortBeforeEffects."
instance_constructor = "Constructor-owned source lineage remains provenance; ordinary calls require owner/site coverage, while constructor-special rows use the existing constructor disposition and are not reclassified as ordinary targets."
known_non_direct = "Builtin/typeop/math/fastmem/ExplicitExtern/Brand-special forms must use their existing typed route or an explicit KnownNonDirect disposition; no name synthesis."
index_none = "non-empty direct_calls with callable_index=None is a typed abort (existing I0), not an empty successful inventory and not proof of observation."
duplicates = "duplicate, missing, foreign, or ambiguous owner/site rows abort the unpublished package; no last-wins merge."
post_effect = "ResolvedAfterArgumentsForbidden is a guard failure, never a terminal disposition."

[finite_state]
states = [
  "SourceInventoryUnsealed",
  "FunctionCallObserved",
  "FunctionCallDispositionMissing",
  "SelectedTraversalClosedWithCalls",
  "IndexNoneWithDirectCalls",
  "OwnerQualifiedSiteReady",
  "KnownNonDirectDisposition",
  "TopLevelLineagePreserved",
  "InstanceConstructorLineagePreserved",
  "DuplicateSite",
  "MissingSite",
  "ForeignSite",
  "AmbiguousSite",
  "ResolvedAfterArgumentsForbidden",
  "SameSessionOwnerSiteBijectionReady",
  "PackageAbortBeforeEffects",
  "InventoryCoSealed",
  "ParkedCompatibility",
]
terminal_policy = "Only InventoryCoSealed after same-session owner/site bijection is terminal for this design. Observed-but-undispositioned calls, closed traversal, index-none-with-calls, duplicate/missing/foreign/ambiguous sites, and second-session ownership remain CutoverBlockerOpen; explicit compatibility-only cohorts may be ParkedSealed."

[subtasks]
d0_a_observation_owner = "Design the single existing shadow/resolver observation point that records every ordinary FunctionCall before argument descent; no AST re-scan and no target receipt."
d0_b_lineage = "Carry RawInvocationRootLineageV1::{Cataloged,TopLevel,InstanceConstructor} through brand authority without collapsing to caller=None; keep provenance separate from target authority."
d0_c_co_seal = "Next bounded design slice (D0-C): fix the same-session owner-qualified (FunctionOwnerId, SourceExprSiteV1) inventory boundary at semantic package prepare_install/commit; include constructor forest owners, SelectedNormal/non-direct, index-none, duplicate, missing, foreign, and ambiguous checks."
d0_d_local_value = "Decide whether LocalValue has an existing exact source relation; otherwise disposition it as typed reject or explicit park rather than consulting Builder variable_map or a name fallback."
d0_e_guard_matrix = "Specify one future guard and focused negative matrix for silent drop, index-none-with-calls, lineage collapse, second resolver, post-effect Resolved, and package/body/argument effects before co-seal."
d0_f_index_none_safety = "Track MIR-CALL-D1B-INDEX-NONE-DIRECT-CALLS-FAILFAST-D0 separately: existing DraftInvariant can close the silent empty-map edge, but it cannot claim source observation, lineage, or pre-argument resolution."

[acceptance]
positive = [
  "every selected and InstanceConstructor FunctionCall is counted exactly once with an owner-qualified source site",
  "Cataloged, TopLevel, and InstanceConstructor lineage reaches disposition without collapsing to caller=None",
  "known builtin/typeop/local/Extern/Brand special forms receive an explicit non-direct or targeted disposition from existing authority",
  "same resolver session co-seals the inventory and owner/site bijection before package publication",
]
negative = [
  "callable_index=None with non-empty direct_calls cannot become an empty successful map",
  "SelectedCallable traversal cannot silently drop FunctionCall",
  "no name/arity/physical-symbol/constructor-symbol target synthesis",
  "no second resolver continuation or post-effect recovery is introduced",
]
parity = "SelectedNormal self/sibling, InstanceConstructor, known special forms, missing/foreign/ambiguous, and an empty-call baseline remain distinguishable; no argument effect or MIR block mutation occurs for unresolved dispositions."
guard = "future guard must assert direct-call inventory coverage, zero silent drop to empty index, preserved root lineage, same-session owner/site bijection, and zero package/body/argument effects before a complete inventory."

[reopen]
triggers = [
  "SelectedCallable traversal remains closed while FunctionCall is present",
  "canonicalize_draft converts direct_calls to an empty map without a typed terminal",
  "TopLevel or InstanceConstructor lineage is collapsed to caller=None before disposition",
  "a target is synthesized from a source name, arity, physical symbol, or constructor symbol",
  "inventory is issued by a second resolver continuation or after argument effects",
]

[d1_owner_lineage_coseal]
status = "design_stop"
observed_commit = "a10e294b28"
decision = "D1 cannot publish a new Cataloged target through the transitional Callee::Global(String). The existing structural CanonicalGlobalTargetV1 is carrier-only and is not yet connected to Callee/CallTarget; that B1 integration is a prerequisite, not a reason to add another String issuer."
source_authority = "VerifiedCatalogCallableOwnerLinkV1 and the same resolver-session catalog brand prove Cataloged lineage and declaration membership. RawInvocationRootLineageV1 remains provenance; SourceExprSiteV1 is the exact site key."
canonical_issuer = "After the structural carrier is connected, the existing semantic-package issuer is the sole co-seal owner. It may issue a typed static-box-method target only from the owner-link plus exact site relation; it may not derive a target from name, arity, box name, physical symbol, or registry lookup."
carrier_prerequisite = "B1 must atomically adapt CanonicalGlobalTargetV1 into the canonical Callee/CallTarget consumers (or the already-accepted equivalent shape) without a parallel String variant, formatter/reparse bridge, fallback, or retry. The carrier constructor checks components only; catalog/session/brand authority stays in the caller."
carrier_policy = "Use the strict structural path: do not use the existing Callee::Global(String) as a new D1 physical projection. Callee and CallTarget must be changed together, and every surviving Global family must first receive a finite Builtin/SameModule/Extern/Method/retire disposition."
consumer_census = "Path-based observation only: Callee::Global appears in 143 Rust files/272 occurrences (about 68 files/115 production-ish); CallTarget::Global appears in 20 files/45 occurrences (about 17 files/36 production-ish). This is a B1 scope hint, not semantic completion evidence."
b1_consumer_families = [
  "core/producers: call_unified, call_target, resolver, method_resolution, unified_emitter, emit, build, JoinIR, direct-call and physical-thunk paths",
  "analysis/optimizer: value consumers, escape/ownership/query, remapper, CSE, callsite canonicalization, storage/generic method plans",
  "wire/compatibility: v2 structural codec plus owner-private v0/v1 adapters; no schema retry",
  "terminals: LLVM/AOT and any retained reference/wasm route; retired VM paths are isolated rather than adapted by fallback",
  "printer/tests/guards: structural projection and fixture disposition without formatter/reparse authority",
  "MirCall/CallFlags transport: keep the live MirCall physical terminal; classify CallFlags-only writers, public exports, generated projections, and external/feature obligations in its separate retirement row",
]
lineage_mapper = "Map the located Cataloged raw site to SourceExprSiteV1 exactly once, verify the same owner-link, session brand, observation name/arity, and one-to-one site membership, then move the structural target into the direct-call handoff. TopLevel and InstanceConstructor provenance is preserved but cannot synthesize Global."
package_boundary = "The existing with_normal_callable_install_once bridge remains the only install authority. Direct-call completeness is checked before prepare_install/commit. The package-only begin_lowering(&self) bypass is replaced in the later atomic row by a consuming lowering scope that owns the package and the direct-call loan together."
raw_port_boundary = "The lowering scope lends one concrete non-Clone/non-Copy RawDirectCallDispositionLoanV1 to RawInvocationChildPortV1 by short &mut reborrow. No Rc, Arc, RefCell, tuple-splitting, package getter, or second child ledger is allowed."
required_sequence = [
  "close the separate MirCall/CallFlags feature/public/projection matrix and retire writer-only edges without deleting the live MirCall physical terminal",
  "B1 structural carrier integration design and finite consumer census",
  "B1 carrier cutover with Callee/CallTarget consumers adapted atomically",
  "D1 exact Cataloged issuer through VerifiedCatalogCallableOwnerLinkV1",
  "same-session owner/site bijection and lineage mapper",
  "private sealed package plus direct-call loan co-seal",
  "consuming lowering scope and short raw-port reborrow",
  "exact-site take_once before argument descent, Call once, finish_empty",
  "delete CatalogedTargeted direct payload and prove old ordinary resolver callers zero",
]
non_authority = [
  "transitional Callee::Global(String) as a new D1 issuer",
  "function_call_preflight_route.rs, CallTarget, CalleeResolverBox, mir_symbol_projection, registry, or formatter as target authority",
  "root-only inventory checks, caller=None, TopLevel/Constructor names, or static-candidate uniqueness",
  "package-only begin_lowering, a splittable tuple, Clone/Rc/Arc/RefCell loan transport, or consumer-side resolver repair",
]
acceptance = [
  "B1 carrier integration has one canonical Callee/CallTarget shape and zero new String target issuers",
  "Cataloged target issuance is backed by the same-session owner-link, exact site, same brand, and one declaration relation",
  "missing, foreign, duplicate, ambiguous, wrong-kind, and lineage-mismatch cases abort before package/body/argument effects",
  "package and loan are one private non-splittable lifecycle; raw recursion receives only a short mutable borrow",
  "take_once precedes argument descent, arguments are lowered once, MirInstruction::call is emitted once, and residual loan is zero",
]
no_safe_slice = [
  "the current Callee::Global(String) must be used to publish a new D1 target",
  "B1 requires a parallel transitional variant or a formatter/reparse bridge",
  "Callee and CallTarget are changed separately, or an unclassified Global string family is wrapped mechanically",
  "wire/backend/optimizer treatment is left to a later retry after a partial B1 cutover",
  "an exact owner/site relation is unavailable and name/arity/physical symbol would be used instead",
  "package-only install or shared begin_lowering remains the only way to reach the raw dispatcher",
  "lineage or loan transport requires caller=None collapse, Clone/Rc/RefCell, unsafe, or a second resolver",
]

[b1_current_head_c0]
task_id = "MIR-CALL-GLOBAL-TARGET-B1-CURRENT-HEAD-C0"
status = "design_stop"
implementation_permission = false
decision = "Complete a finite semantic disposition for every compiled Global producer and consumer before the structural carrier cutover. Callee and CallTarget change together to the accepted structural shape; no LegacyGlobal/IssuedTarget/Generic wrapper or new String issuer is allowed."
boundary = "Starts at every compiled Callee::Global/CallTarget::Global producer or consumer and ends at the selected structural wire/terminal or explicit isolate/retire disposition. Path counts are scope hints only; completion requires owner/action evidence for each family."
boundary_scope_amendment = "C0 closes the compiled Rust producer/consumer boundary and records explicit dispositions for adjacent wire/fixture/external surfaces. JSON-v0/v1 codecs, Python/llvmlite readers, and apps/selfhost-runtime copies remain outside the compiled carrier cutover as named ParkedSealed owners; they are not silently treated as proof of Rust semantic coverage."
observed_census = "Current path census: Callee::Global 143 Rust files/272 occurrences (about 68 production-ish files/115 occurrences); CallTarget::Global 20 files/45 occurrences (about 17 production-ish files/36 occurrences). These counts do not prove semantic coverage and must not be used as the completion claim."
current_head_reaudit = "At HEAD a30bb7ba6a (2026-08-28), the compiled source boundary remains unchanged after the b8bc5cc105 dual-surface guard and the docs-only tail: Callee::Global remains 143 Rust files/272 occurrences and CallTarget::Global remains 20 files/45 occurrences. The readiness guard derives both aggregates and compact buckets, but C0 successor evidence remains open."
dual_surface_coverage_next = "MIR-CALL-GLOBAL-TARGET-B1-C0-DUAL-SURFACE-COVERAGE-D0: derive and classify both Callee::Global and CallTarget::Global across the compiled boundary, while separating test-only, generated, wire, and external ParkedSealed owners. Do not open B1 until both surfaces have observed owner/action/terminal-or-successor/reopen evidence."
final_shape = "Callee::Global(CanonicalGlobalTargetV1) and CallTarget::Global(CanonicalGlobalTargetV1) are one atomic core decision. CanonicalGlobalTargetV1 remains serde-free and source-proof-free; its sole production construction authority is the exact declaration-backed callable catalog."
finite_disposition = [
  "Builtin(Print) for exact print/1 only",
  "SameModule(FreeFunction { name, arity }) for an exact final-module declaration",
  "SameModule(StaticBoxMethod { owner, method, arity }) for an exact final-module declaration",
  "Extern for an explicit declared provider or the exact panic/1 and exit/1 provider rows",
  "Method or construction owner when the target is not a Global family",
  "TypedReject or Retire/ParkedSealed for authority-free, compatibility-only, malformed, or unsupported rows",
]
owner_action_matrix = [
  "core and builder producers: adapt exact issuers or reject/retire; delete formatter, registry, methodize, and resolver repair",
  "analysis/optimizer/remapper: preserve structural identity and operands; no target issuance or Global-to-Method repair",
  "wire ingress/egress: one structural v2 codec; v0/v1 are owner-private compatibility projections with no schema retry",
  "LLVM/AOT and retained reference terminals: consume typed families; non-selected VM/WASM paths are explicitly isolated or retired",
  "printer/tests/guards: project structural data for display/fixtures only; never parse or reconstitute a target",
]
unregistered_calltarget_families = [
  "CallTarget schema/transport: src/mir/builder/calls/call_target.rs, calls/emit.rs, calls/unified_emitter/compat_entrypoints.rs, and calls/resolver.rs",
  "ordinary/recovery: calls/build.rs, calls/function_call_preflight_route.rs, calls/static_resolution.rs, and calls/unified_emitter.rs",
  "static terminal/publication: calls/method_call_terminal.rs, normal_script_direct_static_physical_publication.rs, and rewrite/known.rs",
  "builtin: stmts/print_stmt.rs",
]
additional_compiled_global_surfaces = [
  "canonical direct-call materializer (src/mir/canonical_direct_call.rs): exact declaration-backed successor or typed reject; never rebuild a target from a physical symbol",
  "normal-module physical thunk (src/mir/builder/normal_module_transaction/physical_thunk.rs): retain only a named owner/parked terminal; physical symbol lookup is not a target authority",
  "JoinIR call generator (src/mir/join_ir_to_mir/call_generator.rs): remove Const String/func target transport or isolate it as explicit compatibility; no generated-name fallback",
  "CorePlan GlobalCall and selected emission port (src/mir/builder/control_flow/plan/lowerer/effect_emission.rs, emission_port.rs): exact relation or typed reject; mir_symbol_projection is a transitional projector only",
  "environment-gated arithmetic/comparison/unary global publishers (src/mir/builder/ops/{arithmetic,comparison,unary}.rs): named provider/typed route or pre-effect retirement; env/name is not authority",
  "ordinary New birth claim (src/mir/builder/ordinary_new_admission.rs): keep source claim/NewBox boundary explicit; never classify a birth symbol as a Global call",
  "public MirCall::global compatibility constructor (crates/hakorune_mir_defs/src/call_unified.rs): explicit ParkedSealed API or accepted breaking-retirement row; no new core issuer",
]
compiled_owner_action_inventory = [
  "CallTarget schema/transport (calls/call_target.rs, calls/emit.rs, calls/unified_emitter/compat_entrypoints.rs, calls/resolver.rs): owner=atomic B1 carrier; action=adapt Callee and CallTarget together and retire direct String transport; terminal=CanonicalGlobalTargetV1 or typed compatibility ingress; reopen=any surviving String constructor/formatter",
  "ordinary/recovery (calls/build.rs, calls/function_call_preflight_route.rs, calls/static_resolution.rs, calls/unified_emitter.rs): owner=D1 exact relation issuer; action=retire name/arity, mir_symbol_projection, unique-static, tail recovery, and env-gated reclassification; terminal=TypedRejectBeforeArguments when relation is absent; reopen=an exact catalog caller relation is discovered",
  "static terminal/publication (calls/method_call_terminal.rs, normal_script_direct_static_physical_publication.rs, rewrite/known.rs): owner=declaration-backed catalog for production and named test owner for fixtures; action=adapt exact relation or isolate/retire rewrite; terminal=typed Global/Method or fixture-only projection; reopen=a production caller outside the catalog appears",
  "builtin (stmts/print_stmt.rs): owner=builtin declaration policy; action=adapt exact print/1; terminal=Builtin(Print), otherwise typed reject or named provider; reopen=print is proven to have a non-builtin callable meaning",
  "MirCall/CallFlags transport (call_unified.rs, unified_emitter/physical_terminal.rs, crate facades, RustSubset projections): owner=MirCall physical terminal plus separate CallFlags API row; action=retain terminal, co-update/retire writer-only flags; terminal=MirInstruction::call; reopen=any semantic flag reader or second terminal appears",
  "additional compiled global surfaces (canonical_direct_call.rs, normal_module_transaction/physical_thunk.rs, join_ir_to_mir/call_generator.rs, control_flow/plan/lowerer/{effect_emission,emission_port}.rs, ops/{arithmetic,comparison,unary}.rs, ordinary_new_admission.rs, hakorune_mir_defs/call_unified.rs): owner=their existing declaration/claim/plan owner or explicit compatibility owner; action=adapt exact relation, typed-reject, isolate, or retain ParkedSealed; terminal=no physical-symbol/name/func reconstruction; reopen=an exact source-backed production relation or selected external reader is demonstrated",
]
calltarget_census = "Current compiled CallTarget::Global census is 20 Rust files/45 occurrences (17 production-ish files/36 occurrences). The twelve files above are the previously unregistered family members; normal_script_direct_static_physical_publication.rs is test-only and is isolated, while recursive test helpers and apps/selfhost-runtime copies are outside the compiled owner boundary and must not be counted as production issuers."
observed_successor_gaps = [
  "calls/unified_emitter/compat_entrypoints.rs:11-29 and calls/resolver.rs:66-75 still publish or reissue Callee::Global(String); their normalized evidence rows are open until a typed successor is implemented.",
  "calls/build.rs:336-370, function_call_preflight_route.rs:257-371, and static_resolution.rs:86-171 still perform post-argument recovery or name/arity Global reconstruction; the TypedRejectBeforeArguments terminal remains design-only.",
  "calls/unified_emitter.rs:153-170 and :397-446 still use env-gated legacy fallback and Global/Method reclassification; the source-bound successor remains open.",
  "calls/method_call_terminal.rs:289-359 and rewrite/known.rs:176-534 still generate Global strings; declaration-backed typed publication remains unwired.",
  "stmts/print_stmt.rs:287-295 still routes print through CallTarget::Global and an Extern fallback; Builtin(Print) terminal remains unwired.",
  "CallFlags remains a retained ParkedSealed public/projection surface; writer-only co-update and reusable guard are not yet landed while MirCall physical_terminal remains live.",
  "The additional compiled surfaces now have normalized manifest evidence rows, but their compiled dispositions remain CutoverBlockerOpen until owner-specific successors or explicit terminals land, even when no direct CallTarget::Global token is present.",
]
guard_reuse_boundary = "The existing B0 census, B1 S0 carrier, D1B index/target/loan guards, and canonical corridor remain component evidence. The C0 guard now validates normalized disposition schema and evidence linkage, but it does not prove that a compiled successor is implemented or that CallFlags co-update is complete. Do not claim C0 exhausted or open B1 implementation while a compiled disposition remains CutoverBlockerOpen."
c0_guard_spec = "The landed readiness guard derives the compiled Callee::Global and CallTarget::Global aggregate lists and compact selector buckets. A future C0 closeout phase must consume those aggregate digests, occurrence-level normalized use-shapes, disposition dictionary, and explicit projection/external rows: every occurrence must map to exactly one bucket and disposition; unclassified or overlapping sites, new String issuers, independent Callee/CallTarget authority, CallFlags field readers, and unregistered projection/external rows fail closed. Existing B0/S0 guards remain component checks, not C0 completion proof."
c0_exit_audit = "A bounded partial-retirement review found no safe code exit before C0: deleting suffix recovery alone leaves unique-static recovery, post-argument lowering, name-Const/legacy Call publication, and the same relationless lifecycle. Without an exact source/catalog relation, converting that lifecycle to pre-effect reject would be a half-connected authority change; keep C0 design-stopped and do not open a tail-only row."
mircall_flags_census = "At HEAD a30bb7ba6a (2026-08-28), bounded Rust source contains CallFlags in 4 files/19 occurrences; the explicitly adjacent generated RustSubset .hako/.json pair and two lifecycle classifiers add 4 files/22 occurrences (8 files/41 occurrences across the Rust plus fixture/tool boundary). The tail after b8bc5cc105 is docs/CURRENT_STATE-only, so this source census is unchanged. MirCall remains live at unified_emitter/physical_terminal.rs; Hako flags.optionality and Python/llvmlite mir_call JSON readers are separate wire/reference owners, not Rust CallFlags readers."
callflags_public_api_disposition = "Existing D1-A policy is retained: the public hakorune_mir_defs/MIR facade is ParkedSealed as an owner-local transport surface, not deleted or reinterpreted in B1. A future CallFlags-only fast retirement requires an explicitly accepted breaking-API decision; until then, the named reopen trigger is a selected external reader or dependency on non-default fields."
test_and_external_boundary = "normal_script_direct_static_physical_publication.rs and the RustSubset expected .hako/.json are co-update/isolate obligations, not production target authority. apps/selfhost-runtime copies and Python/llvmlite JSON operation names remain outside the Rust structural carrier boundary unless an explicit owner reopens them."
completion_status = "C0 remains incomplete: the dual-surface readiness guard and normalized disposition/evidence linkage now cover the lexical aggregates, but they do not prove semantic successor/terminal implementation or occurrence-level use-shape classification. Each compiled family still needs an observed successor/terminal and reopen evidence with no CutoverBlockerOpen state. The CallFlags public/projection matrix is explicitly ParkedSealed/retained under the existing D1-A policy; its co-update set remains a separate prerequisite before any implementation row is selected."
mircall_boundary = "MirCall remains a live transport through unified_emitter/physical_terminal.rs. CallFlags is a separate writer/public/projection retirement candidate; its design row must classify create_mir_call/create_call_flags, public re-exports, generated RustSubset projections, and external/feature obligations before any deletion."
mircall_prerequisite = "MIR-CALL-MIRCALL-CALLFLAGS-RETIRE-D0 is a prerequisite design row, not part of B1 carrier implementation: retain create_mir_call -> physical_terminal -> MirInstruction::call and Hako wire flags.optionality; only writer-only Rust CallFlags edges may later be removed after the public/projection matrix is exhausted."
required_evidence = [
  "each surviving producer has one owner, one action (adapt/isolate/retire), one successor or typed terminal, and one reopen condition",
  "Callee and CallTarget have no independent Global authority and no parallel legacy variant",
  "all wire/backend/optimizer treatment is decided before the atomic cutover; partial compile repair is not a terminal",
  "the structural construction guard names the callable-declaration catalog as the only production issuer",
  "CallTarget::Global production edges are owner-classified, and the MirCall/CallFlags matrix records the live physical terminal separately from writer-only candidates",
  "test-only fixtures, apps/selfhost-runtime copies, and Python/llvmlite JSON operation names are explicitly outside or co-updated at the named boundary",
  "C0 inventory is exhausted and all open remediation rows are closed before B1 cutover implementation permission",
]
non_authority = [
  "Callee::Global(String), CallTarget::Global(String), mir_symbol_projection, formatter/reparse, registry lookup, or static-candidate cardinality",
  "a mechanical wrapper around every Global string, a Legacy/Issued/Generic intermediate variant, or a consumer-side retry",
  "root-only or path-count census, fixture-only acceptance, backend fallback, or optimizer target inference",
]
no_safe_slice = [
  "Callee and CallTarget cannot be changed in one semantic boundary",
  "an unclassified Global family must be wrapped to keep the tree compiling",
  "wire/backend/optimizer adaptation is deferred until after a partial carrier cutover",
  "the only target evidence is name/arity/physical symbol/registry or the existing String projection",
  "a production constructor appears outside the declaration catalog or the construction guard cannot observe it",
  "MirCall transport is deleted or CallFlags semantics are silently dropped before its separate retirement row closes",
  "CallFlags public/feature/generated obligations remain unresolved but B1 is opened as if they were caller-zero",
  "a test/projection copy is treated as a production issuer, or an app/Python JSON operation name is used to justify structural carrier construction",
]
next_row = "MIR-CALL-GLOBAL-TARGET-B1-C0-DUAL-SURFACE-COVERAGE-D0"
next_prerequisite_row = "MIR-CALL-MIRCALL-CALLFLAGS-RETIRE-D0"

[b1_c0_dual_surface_coverage_d0]
task_id = "MIR-CALL-GLOBAL-TARGET-B1-C0-DUAL-SURFACE-COVERAGE-D0"
status = "design_stop"
implementation_permission = false
decision = "Close the C0 evidence gap without changing source: derive both Callee::Global and CallTarget::Global surfaces, then assign each occurrence to a small selector bucket and shared disposition dictionary. Do not expand the card into one row per path; unclassified or overlapping occurrences fail closed, and a readiness pass alone must not open B1."
census_boundary = "Tracked Rust under src/ and crates/ from each Global carrier producer/consumer to its terminal or explicit isolate/retire disposition; includes test-only Rust projections for classification, excludes JSON-v0/v1, Hako wire, Python/llvmlite, apps/selfhost-runtime, and other external surfaces which remain named ParkedSealed owners."
observed_gap = "Current C0 readiness derives 20 CallTarget::Global files/45 occurrences, but the declared C0 boundary also has 143 Callee::Global files/272 occurrences, including Callee-only producers such as method_resolution.rs and compatibility entrypoints."
machine_unit = "Readiness phase uses (token family, tracked relative path, file count) with deterministic path buckets. It deliberately does not claim comment/string exclusion or producer/consumer use-shape classification; the C0 closeout phase must add occurrence-level normalized use-shape and reject ambiguity rather than guessing."
aggregate_baseline = "Callee::Global = 143 files/272 occurrences; CallTarget::Global = 20 files/45 occurrences. A future closeout guard records an accepted site digest for each aggregate and never auto-updates it."
coverage_buckets = [
  "compiled-producers: constructor expressions owned by the declaration/catalog or named typed-reject owner",
  "compiled-carrier-consumers: core Callee/CallTarget schema and typed terminal projections",
  "compiled-recovery: resolver/preflight/static-recovery/methodize paths scheduled for retire-before-effects",
  "compiled-adjacent: plan/thunk/JoinIR/operator/New surfaces with an existing owner or explicit ParkedSealed boundary",
  "test-fixtures: cfg(test), tests/, *_tests.rs, and fixture modules isolated from production authority",
  "external-projections: JSON/Hako/Python/apps/generated surfaces retained as owner-private ParkedSealed obligations",
]
disposition_dictionary = [
  "catalog-issued-or-typed-reject",
  "typed-carrier-consumer",
  "retire-recovery-before-effects",
  "isolate-fixture",
  "parked-sealed-external",
]
required_evidence = [
  "derived aggregate site sets and accepted digests for both Callee::Global and CallTarget::Global, with compiled/test-only/external classification and no wildcard folding",
  "every occurrence assigned exactly once to a coverage bucket and shared disposition, with source-backed owner/action/terminal-or-successor/reopen evidence for compiled buckets",
  "explicit ParkedSealed owner, non-authority, and reopen trigger for generated, wire, feature, external, and fixture surfaces outside the compiled cutover",
  "CallFlags/MirCall retained transport and separate retirement prerequisite recorded without silently dropping Hako wire optionality",
  "C0 closeout guard design that rejects an unlisted, overlapping, or ambiguous dual-surface occurrence and keeps B1 permission false until the evidence inventory is exhausted",
]
allowed_work = [
  "read-only source census and worker audit",
  "existing active-card and manifest evidence updates",
  "no new semantic receipt, target issuer, carrier, package, loan, dispatcher, fixture, or fallback",
]
forbidden_work = [
  "src/ or crates/ schema/issuer/consumer edits",
  "B1 Callee/CallTarget cutover",
  "CallFlags or MirCall deletion",
  "new String wrapper, formatter/reparse, methodize, resolver retry, or default terminal",
]
finite_states = [
  "CompiledProducerNeedsSuccessor",
  "CompiledConsumerNeedsSuccessor",
  "TestOnlyProjection",
  "ParkedExternalOwner",
  "TypedRejectOrRetireTerminal",
  "UnclassifiedOccurrence",
  "OverlappingBucket",
  "EvidenceMissing",
  "C0Exhausted",
]
no_safe_slice = [
  "a Callee-only producer cannot be assigned an exact successor or explicit isolate/retire owner",
  "the dual census requires name/arity/physical symbol inference or a wildcard family row",
  "an occurrence is unclassified, assigned to multiple buckets, or silently folded into a path aggregate",
  "B1 would change one carrier while the other remains String-based",
  "a public/generated/wire surface is treated as caller-zero without an owner and reopen trigger",
]
next_row = "MIR-CALL-GLOBAL-TARGET-B1-CUTOVER"
closeout_guard_design = "Reuse this single guard with a c0_dual_closeout phase. Keep the existing dual aggregate/digest and selector buckets, normalize the shared disposition dictionary to a finite action/state vocabulary, require exactly one terminal_kind or successor_ref, and connect every evidence row to one disposition_ref. The 18 evidence rows are bounded to family/file anchors; they do not expand into function- or occurrence-level cards. Do not add an AST scanner, new guard, or semantic target inference."
closeout_disposition_schema = "action ∈ {adapt, isolate, retire, retain}; state ∈ {CutoverBlockerOpen, CutoverBlockerClosed, ParkedSealed}; exactly one of terminal_kind/successor_ref is non-empty; non_authority and reopen are explicit; ParkedSealed requires a named owner and non-authority. A closed disposition may carry successor_ref when its decision is sealed for the next implementation row; readiness may retain open blockers; closeout rejects any CutoverBlockerOpen row. Before opening closeout, the existing guard must enforce the same finite action vocabulary on evidence rows."
closeout_transition = "After the existing compiled-family evidence is complete and the closeout phase passes, update the same card and CURRENT_STATE atomically to the B1 cutover row. A guard pass alone never grants B1; all compiled dispositions currently remain CutoverBlockerOpen."
c0_boundary_decision = "Do not narrow C0 to the canonical Call corridor. The compiled adjacent routes still have reachable production emitters; ParkedSealed without an executable isolation/retirement boundary would leave competing Global authorities. Only the public MirCall compatibility constructor is currently parkable under its named external-owner policy."
c0_remaining_family_blockers = [
  "direct/static terminal cluster (canonical_direct_call.rs, method_call_terminal.rs, print_stmt.rs): issue a typed target from an exact declaration relation or fail before effect; a physical/name projection is not authority.",
  "normal-module physical thunk: prove source-header and catalog declaration identity in one brand, or install an explicit owner-local isolation/retirement terminal; a physical symbol is not authority.",
  "JoinIR declaration/call cluster: co-seal JoinFuncId, declaration, and call relation in the existing JoinIR owner; generated name/arity and Const String cannot issue a target.",
  "CorePlan/operator/emission cluster: split exact source-backed relation from unlocated/env/name branches; exact branches adapt to typed Global and the rest reject before effect.",
  "ordinary-New boundary: keep NewBox/birth claim as its owner terminal and remove or typed-reject fallback Global publication.",
]
c0_family_exit_rule = "Every compiled family above stays CutoverBlockerOpen until it has one source-backed owner, one finite action, one executable typed successor or pre-effect terminal, and one reopen condition. A textual ParkedSealed label is insufficient; no family may be hidden by a wildcard or by narrowing the C0 boundary."
c0_compiled_other_resolution = "The compiled-other bucket is exhausted by the feature-gated src/bin/rc_insertion_selfcheck/cases/basic.rs projection (one Callee::Global occurrence); its evidence and bucket now share isolate-fixture/ParkedSealed semantics. Any additional path must fail the guard rather than enter a wildcard."
c0_completed_evidence_steps = [
  "existing evidence action fields are finite single actions",
  "rc-insertion-selfcheck-global is an explicit isolate fixture row",
  "compiled-other bucket metadata matches the selfcheck fixture disposition",
  "canonical-Call-only boundary narrowing was rejected because compiled adjacent production emitters remain reachable",
]
c0_followup_task_order = [
  "when closeout is opened, update the existing guard to allow CutoverBlockerClosed with successor_ref and to validate evidence action vocabulary",
  "resolve each compiled responsibility cluster with source-backed owner evidence; if an exact relation is absent, use an executable pre-effect typed reject or an explicit owner-local retirement terminal and keep B1 closed",
  "run the readiness guard and pointer guard; only an exhausted compiled disposition permits the c0_dual_closeout phase",
]

[b1_c0_guard_i0]
task_id = "MIR-CALL-GLOBAL-TARGET-B1-C0-DUAL-SURFACE-GUARD-I0"
status = "landed_readiness"
implementation_permission = false
prior_readiness_commit = "1948f7b76a"
commit = "b8bc5cc105"
implementation_commit = "a54680b7c7"
decision = "Extend the existing single C0 guard with a finite disposition schema and a reserved c0_dual_closeout phase. Readiness remains the active phase for this guard-only cell; closeout will require terminal/successor exclusivity, explicit non-authority, evidence-to-disposition linkage, and zero open blockers without changing production source."
allowed_files = [
  "tools/checks/lib/mir_call_global_target_b1_c0_matrix_guard.py",
  "tools/checks/manifests/mir_call_global_target_b1_c0_matrix.toml",
  "tools/checks/guard_rows.toml",
  "docs/tools/check-scripts-index.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-d1b-direct-call-source-inventory-coseal-d0-2026-08-26.toml",
]
forbidden_files = [
  "src/",
  "crates/",
  "apps/",
  "tools/smokes/",
]
guard_phase = "c0_dual_readiness"
guard_contract = "Fail closed on an unlisted, overlapping, or ambiguous Callee::Global/CallTarget::Global occurrence, aggregate count/digest drift, malformed bucket/disposition tuple, missing B0/S0 registry dependency, or active-card boundary drift. Readiness output must keep C0 incomplete and B1 implementation permission closed."
reserved_closeout_phase = "c0_dual_closeout"
closeout_contract = "The same guard will accept c0_dual_closeout only when all disposition rows use the finite schema, each evidence row references one disposition, no compiled blocker remains open, every parked row names non-authority/reopen, and the dual inventory remains unchanged. Closeout still does not alter source or open B1 by itself."
scope = "All tracked Rust files under src/ and crates/ for dual Global surface derivation; JSON-v0/v1, Python/llvmlite, apps/selfhost-runtime, and generated wire copies remain explicit out-of-boundary ParkedSealed surfaces recorded by the C0 card."
non_claims = [
  "no Callee or CallTarget schema change",
  "no String issuer deletion or addition",
  "no target issuer, package, loan, raw dispatcher, Call publication, or CallFlags deletion",
  "no production source/fixture behavior change",
]
acceptance = [
  "both dual token aggregate site sets equal the manifest counts and accepted digest",
  "each observed occurrence is assigned exactly once to a bounded coverage bucket and disposition",
  "each bucket and disposition has one owner/action/terminal-or-successor/reopen tuple",
  "B0 machine census and B1 S0 rows are present exactly once as named component dependencies",
  "readiness phase remains fail-closed with C0 incomplete and implementation permission false for B1 cutover",
  "guard-only changes keep all production source files untouched and below the existing size limits",
]
result = "Dual readiness guard and finite disposition schema landed as one extension of the existing C0 guard: it derives 143 Callee::Global files/272 occurrences and 20 CallTarget::Global files/45 occurrences, validates compact selector/disposition coverage, finite action/state fields, terminal-or-successor exclusivity, 18 bounded family/file evidence links, and B0/S0 dependencies, and keeps C0 incomplete."
verification = "python3 tools/checks/lib/mir_call_global_target_b1_c0_matrix_guard.py .; bash tools/checks/run_row_guard.sh --only mir-call-global-target-b1-c0-matrix; bash tools/checks/current_state_pointer_guard.sh"
no_safe_slice = [
  "the guard needs to interpret target meaning or construct a Callee/CallTarget",
  "the manifest must enumerate arbitrary AST/function sites instead of path/token surfaces",
  "a missing successor is hidden by a default, String projection, or fallback",
  "the guard is used as proof of C0 closeout before all family evidence is observed",
]

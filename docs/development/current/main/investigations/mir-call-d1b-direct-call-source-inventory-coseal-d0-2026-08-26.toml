title = "MIR Call D1B direct-call source inventory co-seal design"
status = "forest_wide_observation_gate_landed"
observed_commit = "784f3af770"
observed_at = "2026-08-26"
implementation_permission = false
permission_scope = "MIR-CALL-D1B-FOREST-WIDE-OBSERVATION-GATE-R0 is closed; package/loan/issuer/dispatcher publication remains design-gated"
parent_row = "docs/development/current/main/investigations/mir-call-d1b-toplevel-direct-target-index-handoff-d0-2026-08-26.toml"
decision_id = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-INVENTORY-COSEAL-D0"
safety_child = "docs/development/current/main/investigations/mir-call-d1b-index-none-direct-calls-failfast-d0-2026-08-26.toml"
observation_child = "docs/development/current/main/investigations/mir-call-d1b-call-observation-profile-d0-2026-08-26.toml"
safety_i0 = "docs/development/current/main/investigations/mir-call-d1b-index-none-direct-calls-failfast-i0-2026-08-26.toml"
decision = "Before a general identity-only callable index can exist, the same resolver session must issue a complete owner-qualified direct-call source inventory. An observed FunctionCall may become Targeted, KnownNonDirect, TypedReject, or Parked, but it may not disappear when callable_index is absent or when SelectedCallable traversal is closed."

[boundary]
starts = "selected normal-source and InstanceConstructor source bodies entering the same resolver/semantic-package session"
ends = "complete owner/site direct-call inventory and finite disposition co-sealed before package publication, body lowering, argument effects, or Call publication"
includes = [
  "SelectedCallable traversal and canonicalize_draft direct-call accounting",
  "callable_index=None with non-empty direct_calls as an explicit typed failure",
  "RawInvocationRootLineageV1 preservation for Cataloged, TopLevel, and InstanceConstructor",
  "owner-qualified SourceExprSiteV1 inventory and same-session constructor forest/root relation",
  "duplicate, missing, foreign, and ambiguous direct-call site disposition",
]
excludes = [
  "target synthesis from name, arity, physical_symbol, constructor symbol, box name, or collector order",
  "general callable-index representation or ResolvedDirectCallTargetV1 issuance",
  "Method(None), methodize, CalleeGuard, UnknownBox, JSON ingress, Call schema, VM/native, and warning cleanup",
  "RawCompatibility, special-form implementation, and existing exact-i64 index generalization",
]

[brief]
decision = "Direct-call inventory is a prerequisite to the parent identity-index handoff. The inventory owner is the existing semantic package/resolver issuer; it records source identity and disposition only, and never reconstructs a Callee. A closed traversal or an empty callable index must not silently drop an observed FunctionCall."
source_authority = "parser/source identity plus the same-brand source catalog and resolver compilation session; RawInvocationRootLineageV1 is provenance and remains intact until disposition"
canonical_issuer = "the existing normal semantic package issuer and its same-session resolver continuation co-seal an owner-qualified direct-call inventory before package publication"
non_authority = [
  "SelectedCallable rejection by itself",
  "callable_index=None -> empty map",
  "NormalTopLevelFunctionDraftAdmissionV1 physical_symbol/arity",
  "source name/arity formatting, collector last-wins, physical symbol lookup, or a second resolver continuation",
]
fail_fast = "If a FunctionCall is observed but cannot be assigned an owner-qualified disposition in the same resolver session, abort the unpublished package before install, body lowering, argument descent/effects, block mutation, or Call publication."
smallest_next_slice = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-OWNER-LINEAGE-COSEAL-D1: name the exact Cataloged issuer, lineage/site mapper, and package co-seal boundary. The forest-wide observation gate is landed; no target, loan, dispatcher, or Call publication is open."
non_claims = [
  "the inventory does not issue a target or generalize the exact-i64 index",
  "lineage preservation does not authorize Global synthesis from names or arity",
  "no post-argument recovery deletion or mandatory-Callee cutover",
]

[observed]
selected_traversal = "SelectedCallableV1 now permits ordinary FunctionCall traversal in src/mir/resolved_semantics/shadow/traversal_profile.rs and shadow/expr.rs; the call records an unissued observation and visits arguments once."
silent_drop = "resolver.rs separates ObserveOnly facts from target publication; the remaining package gap is that callable_semantic_batch/issuer.rs checks only forest.roots(), while normal_callable_semantic_source.rs checks every forest owner."
lineage_collapse = "function_call_brand_source_demand.rs preserves caller only for Cataloged and collapses TopLevel/InstanceConstructor into InstalledNonBrand { caller: None }; RawInvocationRootLineageV1 exists upstream but is lost before disposition."
package_order = "normal_callable_semantic_package issuer co-seals catalog, selected batch, and constructor batch before prepare_install/commit, but no direct-call inventory field or completeness check exists before install/body lowering."
late_route = "caller-less ordinary calls still enter PreparedRawOrdinaryFunctionCompletionV1::Resolved and can lower arguments before resolve_call_target, unique-static/tail recovery, and name-Const publication."
existing_authority = "VerifiedSelectedNormalCallableSourceInventoryV1 and VerifiedResolvedSourceSiteInventoryV1 carry source/declaration facts but no body-call inventory; the exact-i64 VerifiedCallableIndexV1 is a separate cohort and cannot be generalized sideways."
resolver_entrances = "The selected/Brand resolver, InstanceConstructor semantic batch, and older builder normal_callable_semantic_source ingress are distinct entrances; production/caller-zero classification of the older ingress is a prerequisite to any all-callers claim."
lineage_source = "RawInvocationRootLineageV1 already carries Cataloged, TopLevel, and InstanceConstructor upstream; the bounded task is to preserve this provenance through disposition without turning names, box keys, or symbols into targets."
i0_connection = "The landed index-none safety I0 is a typed abort at canonicalize_draft; it is a child safety edge and does not replace the same-session owner/site inventory or pre-shadow observation design."

[worker_audit]
source = "six earlier read-only audits plus three D0-C read-only audits at a01f2bbed9; no code, fixture, or Cargo change"
verdict = "NoSafeSlice remains: package issuer has no direct-call inventory/co-seal field, SelectedCallable closes ordinary FunctionCall traversal, and TopLevel/InstanceConstructor lineage still collapses to caller=None before disposition."
next_bounded_design = "D0-C package co-seal boundary census: fix the exact prepare_install/commit boundary, finite SelectedNormal/Constructor/non-direct/missing/foreign/ambiguous/index-none matrix, one owner/site issuance point, and pre-effect abort contract."
implementation_gate = "Only the forest-wide observation helper row is open; target publication, package/loan transport, lineage mapping, dispatcher changes, and backend changes remain closed."
next_design_row = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-OWNER-LINEAGE-COSEAL-D1: reuse existing forest/site authorities for Cataloged, TopLevel, and InstanceConstructor; no target or public receipt."

[d0_c_review]
verdict = "Architecture Accept with amendments; implementation remains NoSafeSlice. The three audits agree that the existing shadow/resolver observation and existing semantic package issuer must be reused, while no second AST scan, target reconstruction, or recursive loan transport is allowed in this design row."
observation_owner = "Use the existing shadow/resolver traversal as the single FunctionCall observation point. SelectedNormal and InstanceConstructor currently close ordinary FunctionCall traversal, so D0-C specifies the observation contract only; it does not authorize a second AST walk or package-side AST rescan."
inventory_payload = "The semantic inventory records existing OwnedExprSiteV1 plus a typed disposition and same-session owner/brand evidence. It does not carry Callee, CallTarget, String/name/arity, physical symbol, ValueId, AST, or a resolver continuation. RawInvocationRootLineageV1 remains provenance in the raw transport and is not converted into target authority."
issuer_boundary = "The existing normal semantic package issuer is the sole co-seal point after its catalog/selected/constructor batches are available (issuer.rs:146-160) and before the unpublished package is returned. prepare_install may validate completeness, but commit remains a non-fallible publication step and must not resolve missing calls."
lineage_rule = "Cataloged, TopLevel, and InstanceConstructor provenance must survive to disposition without InstalledNonBrand { caller: None }. Provenance does not authorize Global synthesis from a name, arity, box key, or constructor symbol; absent exact relation is TypedReject or explicit compatibility park."
pre_effect_rule = "For this row, pre-effect means before Builder/MIR block mutation, collector mutation, argument lowering, and Call publication. Read-only semantic shadow observation may record a Call effect before the raw lowering boundary; requiring semantic-effect zero would be a separate design row."
loan_boundary = "No direct-call loan field, tuple bundle, recursive reborrow, Clone, Rc, Arc, or RefCell is added by D0-C. Those are later I0 mechanics only after issuer/co-seal and lineage authority are closed."
finite_states = [
  "OwnerQualifiedSiteReady",
  "KnownNonDirectDisposition",
  "TopLevelLineagePreserved",
  "InstanceConstructorLineagePreserved",
  "IndexNoneWithDirectCalls",
  "DuplicateSite",
  "MissingSite",
  "ForeignSite",
  "AmbiguousSite",
  "PackageAbortBeforeEffects",
  "InventoryCoSealed",
  "ParkedCompatibility",
]
acceptance = [
  "SelectedNormal and InstanceConstructor FunctionCall observations are recorded exactly once in the existing traversal contract and receive one owner-qualified site/disposition in the same resolver session.",
  "TopLevel/InstanceConstructor provenance is preserved; no caller=None collapse occurs before disposition.",
  "Missing, duplicate, foreign, ambiguous, and index-none-with-calls states abort the unpublished package before Builder/MIR/collector/argument effects.",
  "InventoryCoSealed is the only terminal for this design; no Callee is reconstructed and no post-argument resolver or recovery is introduced.",
]
no_safe_slice = [
  "SelectedCallable requires a second AST traversal or package-side AST rescan.",
  "The only available relation is name/arity/physical-symbol synthesis.",
  "A new public Verified*/Prepared* semantic receipt or generic target payload is required.",
  "Lineage must be collapsed to caller=None, or a loan must use Clone/Rc/RefCell/unsafe to cross recursion.",
  "The direct inventory can only be completed after argument lowering or commit publication.",
]
next_task = "MIR-CALL-D1B-DIRECT-CALL-SOURCE-OWNER-LINEAGE-COSEAL-D1"

[forest_wide_observation_gate_r0]
current_execution_row = "MIR-CALL-D1B-FOREST-WIDE-OBSERVATION-GATE-R0"
status = "landed"
implementation_permission = false
commit = "b176c98276"
decision = "Make the package batch and source admission use one private forest-wide predicate over every Function owner, including nested lambdas. This closes the root-only inspection gap without issuing a target or changing the lowering route."
source_authority = "Existing SelectedCallable shadow/resolver forest and its owner-scoped direct_call_observations"
canonical_issuer = "No issuer is opened by this row; the existing package/source terminals only consume the shared predicate"
non_authority = [
  "forest.roots() as a completeness check",
  "name/arity lookup, CalleeResolverBox, package inventory, RawInvocationRootLineage, or target synthesis",
  "new Verified*/Prepared* receipt, loan, dispatcher, or Call publication",
]
fail_fast = "If any Function owner in any selected forest contains an unissued direct-call observation, reject before package installation; root-only, nested-only, and mixed-owner cases must agree."
allowed_files = [
  "src/mir/resolved_semantics/direct_call_inventory_gate.rs (new private helper module)",
  "src/mir/resolved_semantics/mod.rs (module declaration)",
  "src/mir/callable_semantic_batch/issuer.rs (delegate existing check)",
  "src/mir/builder/normal_callable_semantic_source.rs (delegate existing check)",
  "existing resolved-semantics or normal-callable package test files for focused negative coverage",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh (extend existing reusable observer guard)",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/builder/recursive_child_lowering.rs",
  "crates/hakorune_mir_defs/src/call_unified.rs",
]
acceptance = [
  "one shared helper inspects every Function owner in each forest",
  "root-only, nested-only, and root-plus-nested observations all reject the existing package batch before install",
  "forests without observations preserve the existing success path",
  "no target, loan, CalleeResolverBox, argument lowering, MIR block mutation, or backend behavior changes",
]
tests = "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib normal_callable_semantic_package::resolver_deferred_tests: 5 passed, 0 failed; exact nested-only test: 1 passed, 0 failed"
guard = "bash tools/checks/current_state_pointer_guard.sh: ok; bash tools/checks/run_row_guard.sh --only mir-call-d1b-cataloged-affine-loan-lifecycle: observer_i0 ok; touched normal_callable_semantic_source.rs remains 730 lines (<760)"
result = "Root-only, nested-only, and mixed-owner unissued observations now share the same forest-wide package/source gate. Target issuance, package/loan transport, raw dispatcher, and Call publication remain zero."
no_safe_slice = [
  "the helper requires a second AST traversal or a new semantic receipt",
  "the package batch and source admission cannot share the same owner-wide predicate",
  "nested coverage requires changing target publication, package install, or the raw dispatcher",
]

[boundary_census]
package_issuer = "src/mir/normal_callable_semantic_package/issuer.rs:134-161 issues instance-constructor, source catalog, resolved batch, and selected mapping; src/mir/normal_callable_semantic_package/model.rs:35-52 has no direct-call inventory field."
prepare_install = "src/mir/normal_callable_semantic_package/install.rs:272-284 checks only callable_declaration_catalog_vacant and returns the unpublished package; no direct-call coverage is validated."
commit = "src/mir/normal_callable_semantic_package/install.rs:287-333 discards the root projection, installs the catalog, and creates the ordinary-new ledger; this is the package publication boundary to guard."
normal_lifecycle = "src/mir/builder/normal_default_root_catalog_lifecycle.rs:247-267 issues the package, :271-395 consumes pre-effect projections, and :429-440 calls prepare_install/commit before the installed package is handed to lowering."
installed_port = "src/mir/normal_callable_semantic_package/install.rs:723-737 only completes selected-key and ordinary-new claim consumption; it does not verify direct-call site coverage."
raw_call_entry = "src/mir/builder/raw_expression_dispatch/mod.rs:341-353 asks Brand authority and immediately prepares/lowers the raw FunctionCall; src/mir/builder/calls/function_call_preflight_route.rs:141-178 maps caller=None to relationless/ordinary routes."
post_effect_route = "src/mir/builder/calls/build.rs:90-125 lowers Targeted arguments safely, but Resolved arguments at :111-124 are lowered before :311-369 resolve_call_target, unique-static recovery, tail recovery, and name-Const publication."
lineage_loss = "src/mir/builder/calls/function_call_brand_source_demand.rs:75-91 retains caller only for Cataloged and maps TopLevel/InstanceConstructor to InstalledNonBrand { caller: None }; D0-C must carry root/site without making it a target authority."
co_seal_owner = "the existing semantic package issuer must validate the complete owner/site/disposition inventory before prepare_install; raw Builder ports, collector order, and a second resolver are non-authority."

[boundary_matrix]
selected_normal = "Observed FunctionCall + owner/site + same-session lineage -> disposition; closed traversal or missing relation -> PackageAbortBeforeEffects."
instance_constructor = "Constructor-owned source lineage remains provenance; ordinary calls require owner/site coverage, while constructor-special rows use the existing constructor disposition and are not reclassified as ordinary targets."
known_non_direct = "Builtin/typeop/math/fastmem/ExplicitExtern/Brand-special forms must use their existing typed route or an explicit KnownNonDirect disposition; no name synthesis."
index_none = "non-empty direct_calls with callable_index=None is a typed abort (existing I0), not an empty successful inventory and not proof of observation."
duplicates = "duplicate, missing, foreign, or ambiguous owner/site rows abort the unpublished package; no last-wins merge."
post_effect = "ResolvedAfterArgumentsForbidden is a guard failure, never a terminal disposition."

[finite_state]
states = [
  "SourceInventoryUnsealed",
  "FunctionCallObserved",
  "FunctionCallDispositionMissing",
  "SelectedTraversalClosedWithCalls",
  "IndexNoneWithDirectCalls",
  "OwnerQualifiedSiteReady",
  "KnownNonDirectDisposition",
  "TopLevelLineagePreserved",
  "InstanceConstructorLineagePreserved",
  "DuplicateSite",
  "MissingSite",
  "ForeignSite",
  "AmbiguousSite",
  "ResolvedAfterArgumentsForbidden",
  "SameSessionOwnerSiteBijectionReady",
  "PackageAbortBeforeEffects",
  "InventoryCoSealed",
  "ParkedCompatibility",
]
terminal_policy = "Only InventoryCoSealed after same-session owner/site bijection is terminal for this design. Observed-but-undispositioned calls, closed traversal, index-none-with-calls, duplicate/missing/foreign/ambiguous sites, and second-session ownership remain CutoverBlockerOpen; explicit compatibility-only cohorts may be ParkedSealed."

[subtasks]
d0_a_observation_owner = "Design the single existing shadow/resolver observation point that records every ordinary FunctionCall before argument descent; no AST re-scan and no target receipt."
d0_b_lineage = "Carry RawInvocationRootLineageV1::{Cataloged,TopLevel,InstanceConstructor} through brand authority without collapsing to caller=None; keep provenance separate from target authority."
d0_c_co_seal = "Next bounded design slice (D0-C): fix the same-session owner-qualified (FunctionOwnerId, SourceExprSiteV1) inventory boundary at semantic package prepare_install/commit; include constructor forest owners, SelectedNormal/non-direct, index-none, duplicate, missing, foreign, and ambiguous checks."
d0_d_local_value = "Decide whether LocalValue has an existing exact source relation; otherwise disposition it as typed reject or explicit park rather than consulting Builder variable_map or a name fallback."
d0_e_guard_matrix = "Specify one future guard and focused negative matrix for silent drop, index-none-with-calls, lineage collapse, second resolver, post-effect Resolved, and package/body/argument effects before co-seal."
d0_f_index_none_safety = "Track MIR-CALL-D1B-INDEX-NONE-DIRECT-CALLS-FAILFAST-D0 separately: existing DraftInvariant can close the silent empty-map edge, but it cannot claim source observation, lineage, or pre-argument resolution."

[acceptance]
positive = [
  "every selected and InstanceConstructor FunctionCall is counted exactly once with an owner-qualified source site",
  "Cataloged, TopLevel, and InstanceConstructor lineage reaches disposition without collapsing to caller=None",
  "known builtin/typeop/local/Extern/Brand special forms receive an explicit non-direct or targeted disposition from existing authority",
  "same resolver session co-seals the inventory and owner/site bijection before package publication",
]
negative = [
  "callable_index=None with non-empty direct_calls cannot become an empty successful map",
  "SelectedCallable traversal cannot silently drop FunctionCall",
  "no name/arity/physical-symbol/constructor-symbol target synthesis",
  "no second resolver continuation or post-effect recovery is introduced",
]
parity = "SelectedNormal self/sibling, InstanceConstructor, known special forms, missing/foreign/ambiguous, and an empty-call baseline remain distinguishable; no argument effect or MIR block mutation occurs for unresolved dispositions."
guard = "future guard must assert direct-call inventory coverage, zero silent drop to empty index, preserved root lineage, same-session owner/site bijection, and zero package/body/argument effects before a complete inventory."

[reopen]
triggers = [
  "SelectedCallable traversal remains closed while FunctionCall is present",
  "canonicalize_draft converts direct_calls to an empty map without a typed terminal",
  "TopLevel or InstanceConstructor lineage is collapsed to caller=None before disposition",
  "a target is synthesized from a source name, arity, physical symbol, or constructor symbol",
  "inventory is issued by a second resolver continuation or after argument effects",
]

title = "MIR Call D1B Main raw Cataloged handoff design"
status = "landed_route"
observed_at = "2026-08-28"
observed_commit = "a9cb2d289a"
implementation_permission = false
permission_scope = "MIR-CALL-D1B-MAIN-RAW-SCOPE-CATALOGED-ROUTE-R0 only; target issuer, loan, dispatcher, argument order, Call schema, and recovery remain closed."
guard_phase = "main_raw_cataloged_route_r0"
parent_row = "docs/development/current/main/investigations/mir-call-d1b-direct-call-source-owner-lineage-coseal-d1-2026-08-26.toml"
decision_id = "MIR-CALL-D1B-MAIN-RAW-CATALOGED-HANDOFF-D0"
decision = "Main-to-helper direct calls are an established normal-source shape in NormalMainDirectCallPreflightV1, so a permanent park is not the final contract. The required A-style handoff is still design-only: a private, one-shot root callback must co-seal existing Main identity, owner forest, session witness, and exact raw root/site before any target or argument effect. ScriptRoot/Main(locator) compatibility and canonical-core-to-Builder wiring remain separate and unchanged."

[brief]
decision = "Design the smallest existing-product handoff that makes the normal App Main root raw lineage exact; do not implement the ScriptRoot-to-Cataloged semantic switch in this row."
source_authority = "AppMainCatalogCoSealV1 owns parser identity/catalog key/brand; VerifiedSemanticOwnerForestV1 owns FunctionOwnerId/root/nested structure; RawInvocationSourceTransportV1 owns raw lineage; the installed package/root callback is only the private co-seal consumer."
non_authority = "Names, arity, symbols, statement ordinals, ScriptRoot, Main(locator), canonical-core products, selected child maps, and a brand comparison alone cannot synthesize a Cataloged lineage or target."
fail_fast = "Missing/foreign/mixed identity, owner, session, brand, root, site, or body-kind rejects before package install, body/argument/effect descent, collector mutation, or MIR publication."
smallest_next_slice = "Specify one private root callback, its exact existing caller, and a same-session witness; root-only is the first supported scope and any nested direct-call observation remains the existing forest-wide typed reject."
non_claims = "No target/Callee issuer, package+loan bundle, raw dispatcher, Call schema, Resolved recovery retirement, VM/backend change, or canonical-core route connection."

[boundary]
starts = "source-backed App Main admission, AppMainCatalogCoSealV1, the validated owner forest, and the installed normal root lifecycle"
ends = "an exact raw root/site/body-kind relation is available to the normal Main callback before effects; otherwise the route remains typed reject/park"
includes = [
  "existing Main parser identity, catalog key/brand, forest root, nested-owner observation gate, and raw source context",
  "one-shot private callback and same-session/co-brand proof without a public session ID",
  "root-only initial scope and compatibility route separation",
]
excludes = [
  "ScriptRoot or Main(locator) compatibility, ProgramRuntime, selected static children, canonical-core dispatch, target issuance, loan/dispatcher, and argument lowering",
  "new public Verified*/Prepared* receipt, AST/parser rescan, name/arity/symbol inference, and nested-owner flattening",
]

[existing_product_evidence]
main_identity = "src/mir/builder/callable_declaration_catalog/source_backed.rs:38-74,282-317 retains AppMainCatalogCoSealV1 parser identity, catalog key, and catalog brand."
main_validation = "src/mir/normal_callable_semantic_package/issuer.rs:193-275,391-392 validates Main identity/owner/forest and the forest-wide direct-call gate before install."
normal_root = "src/mir/builder/normal_default_root_catalog_lifecycle.rs:169-220,246-293,428-444,542-582 and src/mir/builder/program_root_lowering.rs:171-227,245-278 currently start normal Main as ScriptRoot or script semantic scope."
cataloged_children = "src/mir/builder/normal_callable_semantic_loan_port.rs:75-151,427-459 constructs Cataloged lineage for selected children, not the normal Main root."
raw_compat = "src/mir/builder/recursive_child_lowering.rs:496-525 constructs Main(locator) only for raw compatibility."
actual_scope_recheck = "Read-only recheck at 77372809ec: App preflight leaves script_source=None (normal_default_root_catalog_lifecycle.rs:169-175,343-352,590-616), so program_root_lowering.rs:197-227 installs RawInvocationSourceTransportV1::script_root(); InstalledNormalCallableSemanticPackageV1 exposes no actual raw-context/root accessor (normal_callable_package_bridge.rs:69-91, install.rs:386-415)."
missing_witness = "The named root callback is real, but InstalledNormalCallableSemanticPackageV1 has no production accessor that also receives the actual RawInvocationSourceContext; ModuleInvocationBrandV1, SameModuleCallableCatalogBrandV1, and FunctionOwnerIdV1::compilation_brand() still have no existing common comparison witness, and raw context lacks exact semantic owner/site/body-kind equality."
root_witness_audit = "Read-only recheck at d57a72fbb2: a private callback can co-seal the existing AppMain/forest products with raw root() function-body/Function evidence in one borrow scope, but the normal production path currently constructs script_root() inside program_root_lowering.rs. A temporary Cataloged projection alone would not prove that live route, so no ScriptRoot-to-Cataloged switch or witness implementation is authorized yet; no public receipt, AST rescan, or third lifetime is justified."

[design_tasks]
route = "Design decision: once the actual installed App Main raw scope is available, that root will use Cataloged(app_main.catalog_key()) as its canonical raw lineage; ScriptRoot remains the named compatibility route. Record the decision here but do not perform the switch in this D0."
callback = "Use finish_normal_default_root_after_pre_effect_bind (src/mir/builder/normal_default_root_catalog_post_install.rs:29-128), reached from normal_default_root_catalog_lifecycle.rs:590-616, as the private one-shot callback; extend its scope to borrow the installed Main batch/forest and raw root evidence together rather than adding a disconnected comparator."
witness = "Existing ModuleInvocationBrandV1, SameModuleCallableCatalogBrandV1, and FunctionOwnerIdV1 are disjoint domains. The next design sub-row must specify a private same-session/co-brand check through installed_in plus collector with_invocation_brand; no value conversion or public session ID."
raw_exactness = "RawInvocationSourceTransportV1::root can establish Cataloged(key), function-body site, and Function body-kind, but context lacks the semantic comparison accessor. Specify one private DeclaredFunction-to-raw-function/body-kind helper; never derive it from Main spelling, arity, symbol, or statement index."
live_scope = "The witness must observe the actual raw scope at the existing program_root_lowering.rs callback boundary; constructing a detached RawInvocationSourceTransportV1::root(Cataloged(...)) is only a shape probe and cannot be treated as production lineage evidence."
nested = "Keep root owner exact; reject nested-only or mixed direct-call observations before effects and never inherit the parent Cataloged lineage across owner transitions."
compatibility = "Prove ScriptRoot, ProgramRuntime, Main(locator), selected children, and synthetic main/0 remain on their named owners."

[acceptance]
positive = "One source-backed App Main with no direct-call observations yields one exact root owner/site/session relation through an existing production callback; no target/loan/Call is emitted."
negative = "Missing/duplicate/foreign/mixed identity or brand, wrong owner/root/site/body-kind, nested direct call, ScriptRoot fallback, and Main(locator) reuse all reject or remain explicitly parked before effects."
structural = "No AST rescan, no name/arity/symbol synthesis, no selected-map insertion, no package-only bypass, no Clone/Rc/RefCell/extra lifetime, and no new public Verified*/Prepared* receipt."
guard = "Reuse the existing D1 lifecycle guard; add no new guard family. The eventual implementation row must pin the named caller, witness, root-only boundary, zero-delta failure, and compatibility parity."
route_acceptance = "The Cataloged root route is accepted only for installed source-backed App Main after a live raw-scope witness; Complete script, RawCompatibility, Main(locator), selected children, and synthetic main/0 retain their existing owners."

[no_safe_slice]
conditions = [
  "no existing production callback can receive the co-sealed products",
  "same-session/brand equality needs unrelated-type inference, persistent ambient state, or a public session ID",
  "raw site/body-kind equality is unavailable without a second parser/AST traversal or synthetic site",
  "ScriptRoot-to-Cataloged changes other routes, or nested owners inherit the root lineage",
  "target, loan, dispatcher, argument order, Call schema, fallback, or recovery must be changed in the same row",
]

[next]
task = "MIR-CALL-D1B-MAIN-RAW-SCOPE-CATALOGED-ROUTE-R0: implement only the accepted root-only scope handoff at the live App Main body boundary."
implementation_gate = "The route row may change one installed App Main root body edge, prove positive/negative zero-delta behavior, and delete no unrelated compatibility path."
witness_subrow = "MIR-CALL-D1B-MAIN-RAW-CATALOGED-ROOT-WITNESS-D0: design-only private callback at the live raw-scope boundary, same-session check, and semantic-to-raw function/body-kind equality; a detached Cataloged shape probe is insufficient; no target, loan, dispatcher, or ScriptRoot-to-Cataloged switch."
route_subrow = "MIR-CALL-D1B-MAIN-RAW-SCOPE-CATALOGED-ROUTE-R0: selected implementation row; it switches only the installed App Main root body scope and keeps compatibility routes unchanged."

[route_r0_decision_2026_08_28]
status = "accepted_bounded_implementation"
source_authority = "AppMainCatalogCoSealV1, installed package batch/forest, and the parser-issued Main identity already carried by VerifiedMainStaticChildV1."
canonical_issuer = "The installed package port privately verifies the existing Main relation and lends its existing batch lowering input; the RootCallableCapturePort body hook consumes that relation without issuing a target."
raw_witness = "At the live root lowering boundary, create one RawInvocationSourceTransportV1::root((), Cataloged(existing_key)) and require Located(FunctionBody) with function-body site; use a private predicate/accessor, never a detached probe or name/arity/symbol synthesis."
borrow_order = "Borrow compilation context only for relation validation, release it, then enter the HRTB batch input callback; install semantic state and Cataloged raw scope for the body callback only, restore both once."
allowed = [
    "private package-port App Main relation/input callback",
    "RootCallableCapturePortV1 body-only hook with a default lower_body delegation",
    "installed App Main branch omits the outer ScriptRoot wrapper so the body hook is the sole Cataloged root scope",
    "private raw context exact FunctionBody predicate/accessor",
    "focused positive/negative zero-delta tests and the existing D1 lifecycle guard",
]
forbidden = [
    "Callee/target issuer, RawDirectCallDispositionLoanV1, RawInvocationChildPortV1 field or lifetime, dispatcher, argument descent, Call writer, Resolved recovery, or Call schema",
    "AST rescan, public receipt/session ID, ScriptRoot/Main(locator) compatibility changes, nested lineage inheritance, or selected-map insertion",
]
acceptance = "Installed source-backed App Main body alone enters Cataloged scope once; parser identity, catalog key/brand, semantic root owner/forest, raw FunctionBody site/kind all match; mismatch/foreign/duplicate/nested cases reject before body effects; static children and non-App/compatibility routes remain unchanged."
non_claims = "This row does not support Main direct-call publication; the existing pre-install forest gate still rejects direct-call observations. It does not open package+loan, raw dispatcher, target-before-arguments, or Call retirement."

[route_r0]
task_id = "MIR-CALL-D1B-MAIN-RAW-SCOPE-CATALOGED-ROUTE-R0"
execution_row = "MIR-CALL-D1B-MAIN-RAW-SCOPE-CATALOGED-ROUTE-R0"
status = "landed"
implementation_permission = false
implementation_commit = "46fdf7a4fa"
allowed_files = [
  "src/mir/builder/decls.rs",
  "src/mir/builder/module_lifecycle.rs",
  "src/mir/builder/program_root_lowering.rs",
  "src/mir/builder/raw_invocation_source_transport/context.rs",
  "src/mir/builder/raw_invocation_source_transport/lineage_witness_tests.rs",
  "src/mir/builder/raw_static_main_compat_batch.rs",
  "src/mir/builder/normal_default_root_catalog_lifecycle_tests.rs",
  "src/mir/builder/normal_callable_semantic_loan_port.rs",
  "src/mir/builder/normal_callable_semantic_loan_port/main_root.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/normal_callable_semantic_package/install/lowering_port.rs",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
  "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
forbidden = "No target/Callee issuer, direct-call loan, RawInvocationChildPort field/lifetime, dispatcher, argument-order change, Call writer/schema, resolver/recovery, AST rescan, compatibility-route change, or D1 broad permission."
acceptance = "The installed source-backed App Main body is entered through one package-verified Cataloged function-body scope; exact root/site/body-kind checks are tested; raw transport focused tests, pointer guard, lifecycle guard, cargo check, and focused lifecycle tests are green."
closeout = "After tests and guard evidence land, mark this row landed and return the active pointer to the next design stop; do not claim direct-call publication or loan/dispatcher cutover."

[route_r0_verification_2026_08_28]
status = "landed"
implementation_commit = "46fdf7a4fa"
positive = "source_backed_app_main_root_uses_cataloged_scope passed; the installed App Main body entered the package-owned Cataloged function-body scope once."
negative = "exact_function_root_witness_rejects_script_and_descendant_contexts passed; compatibility lifecycle baseline remains unchanged (the three pre-existing fixture failures reproduce on the parent HEAD)."
focused_tests = [
  "cargo test --profile quick --lib source_backed_app_main_root_uses_cataloged_scope (1 passed)",
  "cargo test --profile quick --lib normal_callable_semantic_package::resolver_deferred_tests (11 passed)",
  "cargo test --profile quick --lib raw_invocation_source_transport (17 passed)",
]
guards = [
  "cargo check --profile quick --lib (passed)",
  "current_state_pointer_guard.sh (passed)",
  "mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh (passed)",
]
known_baseline = "cargo test --profile quick --lib normal_default_root_catalog_lifecycle reports 7 passed and 3 pre-existing fixture failures on both the parent HEAD and this row; those failures are outside the installed App Main scope handoff."

[next_design_stop]
task_id = "MIR-CALL-D1B-MAIN-RAW-LINEAGE-HANDOFF-D1"
status = "design_stop"
implementation_permission = false
scope = "Design-only source owner/Raw lineage co-seal and same-session witness. Target publication, package+loan bundle, raw dispatcher, argument order, recovery, and Call schema remain closed."
ordered_prerequisites = [
    "MIR-CALL-D1B-LIFECYCLE-D1-PHASE-DISPATCH-GUARD-R0: make the existing no-argument lifecycle guard resolve the active D1 phase explicitly; do not treat the historical R0 phase as current evidence.",
    "MIR-CALL-D1B-MAIN-RAW-LINEAGE-WITNESS-HARDEN-R0: validate the existing semantic owner against the raw callable owner inside the landed Main callback, with zero target/loan/dispatcher effects.",
    "Only after both prerequisites are closed may a separate atomic target/loan/dispatcher design be considered."
]

[guard_phase_drift_2026_08_28]
task_id = "MIR-CALL-D1B-LIFECYCLE-D1-PHASE-DISPATCH-GUARD-R0"
execution_row = "MIR-CALL-D1B-LIFECYCLE-D1-PHASE-DISPATCH-GUARD-R0"
guard_phase = "main_raw_lineage_handoff_d1"
status = "landed"
implementation_permission = false
observed_commit = "b8e7f350e7"
decision = "The active CURRENT_STATE row is MIR-CALL-D1B-MAIN-RAW-LINEAGE-HANDOFF-D1, but the existing no-argument lifecycle guard has no explicit D1 branch and can select the historical R0 card guard_phase. Treat that as evidence drift, not as D1 proof."
owner = "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh"
scope = "Reuse the existing lifecycle guard family. Resolve current_execution_row to an explicit D1 design phase, read the D1 card from current_execution_design, require design_stop and implementation_permission=false, and keep target/Callee/loan/dispatcher/argument/Call changes at zero."
allowed_files = [
    "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "tools/checks/guard_rows.toml",
    "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
    "docs/development/current/main/CURRENT_STATE.toml"
]
forbidden = [
    "a new guard family",
    "src/mir or fixture changes",
    "opening semantic D1 permission",
    "target/Callee issuance, loan, dispatcher, argument descent, Call schema, fallback, or recovery changes"
]
acceptance = [
    "no-argument dispatch selects the active D1 phase rather than the historical R0 phase",
    "explicit historical phases remain selectable for audit",
    "unknown or unregistered phases fail closed",
    "D1 design_stop and implementation_permission=false are enforced",
    "the existing pointer/registry guard remains green"
]
implementation_commit = "15b7a0c6f1"
closeout = "bash -n, no-argument lifecycle guard, explicit D1/route phase checks, unknown-phase rejection, pointer guard, and row runner passed; no src/mir or semantic Call files changed. The pointer returned to D1 design_stop and semantic permission remains false."

[main_raw_lineage_witness_harden_2026_08_28]
task_id = "MIR-CALL-D1B-MAIN-RAW-LINEAGE-WITNESS-HARDEN-R0"
execution_row = "MIR-CALL-D1B-MAIN-RAW-LINEAGE-WITNESS-HARDEN-R0"
guard_phase = "main_raw_lineage_witness_harden_r0"
status = "landed"
implementation_permission = false
scope = "A validator-only hardening row inside the already-landed installed App Main root callback. Compare the existing VerifiedResolvedCallableSourceIdentityV1::owner() with the active raw callable owner from RawInvocationChildPortV1::callable_owner_v1(), and fail closed before body effects when they differ."
source_authority = "AppMainCatalogCoSealV1 and the installed semantic batch issue the parser identity, catalog key/brand, and VerifiedResolvedCallableSourceIdentityV1 owner; the active raw callable ledger is the only raw owner witness."
canonical_issuer = "No new issuer is created. The existing package callback and existing raw scope remain the sole consumers; the row only adds an explicit owner-equality validation at the live Main callback boundary."
non_authority = [
    "parser anchor identity has no owner and must not be treated as one",
    "name, arity, symbol, box/key spelling, statement order, ScriptRoot, Main(locator), or a synthetic session id",
    "nested-owner inheritance, target/Callee construction, loan creation, dispatcher recovery, or argument facts"
]
fail_fast = "If the existing semantic owner is absent or differs from the active raw callable owner, return the existing typed/freeze relation rejection before body/argument/effect, collector, or MIR publication."
allowed_files = [
    "src/mir/builder/normal_callable_semantic_loan_port/main_root.rs",
    "src/mir/builder/normal_default_root_catalog_lifecycle_tests.rs",
    "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "tools/checks/guard_rows.toml",
    "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
    "docs/development/current/main/CURRENT_STATE.toml"
]
forbidden_files = [
    "calls/build.rs and the raw FunctionCall dispatcher",
    "RawInvocationChildPortV1 field/lifetime expansion for a direct-call loan",
    "package+loan bundle, target/Callee issuer, argument lowering, Call writer/schema, fallback/recovery, VM/backend, or compatibility route changes",
    "AST/parser rescan, public Verified*/Prepared* receipt, Clone/Rc/Arc/RefCell, or a third lifetime"
]
positive = "Installed source-backed App Main with one semantic root and one active raw callable owner passes the equality check and still emits no target/loan/Call."
negative = [
    "missing raw callable owner",
    "foreign or mismatched owner",
    "wrong key/brand/root/site/body-kind",
    "ScriptRoot/Main(locator), descendant, nested-only, and mixed direct-call observation contexts"
]
acceptance = [
    "the check runs in the existing live Main callback, not in a detached shape probe",
    "the positive live route and owner-mismatch validator test cover the callback gate; body/effect work is reached only after the check",
    "all existing compatibility routes remain unchanged",
    "no target, loan, dispatcher, argument-order, recovery, or Call schema behavior changes",
    "the source/check files remain below the 760-line design boundary"
]
no_safe_slice = [
    "owner equality requires converting disjoint brand domains, persistent ambient state, a public session id, unsafe, or a new receipt",
    "the active raw owner cannot be observed in the existing callback without a second AST traversal or synthetic lineage",
    "the implementation needs a target, loan, dispatcher, argument, Call, fallback, recovery, or compatibility change"
]
next_after_closeout = "Return to design_stop for the separate exact source-site/target issuer and package+affine-loan dispatcher cutover; this row does not publish a Callee."
implementation_commit = "2dadcae1c2"
verification = [
    "cargo check --profile quick --lib passed",
    "cargo test --profile quick --lib mir::builder::normal_callable_semantic_loan_port::main_root::tests::raw_callable_owner_witness_rejects_missing_or_foreign_owner -- --exact passed",
    "cargo test --profile quick --lib mir::builder::normal_default_root_catalog_lifecycle::normal_default_root_catalog_lifecycle_tests::source_backed_app_main_root_uses_cataloged_scope -- --exact passed",
    "cargo test --profile quick --lib mir::normal_callable_semantic_package::resolver_deferred_tests::app_main_root_and_nested_direct_call_observations_reject_before_install -- --exact passed",
    "current_state_pointer_guard, row runner, explicit witness phase, and diff/rustfmt checks passed",
    "existing normal_default_root_catalog_lifecycle baseline retains three unrelated failures recorded before this row"
]
closeout = "Owner equality is checked in the live installed App Main callback before source context/body lowering. Missing or foreign raw owners fail closed; no target, loan, dispatcher, argument recovery, or Call schema code changed. The pointer returns to the D1 design stop and semantic implementation permission remains false."

[d14_feedback_reconciliation_2026_08_28]
status = "accepted_as_design_amendment"
observed_commit = "b8e7f350e7"
verdict = "The reported App Main observation fail-open is not current: the forest-wide gate already rejects root, nested, and mixed direct-call observations before install. The real current defect is lifecycle guard routing: an active D1 pointer can still be checked using the landed R0 phase. The real next semantic risk is an unverified owner equality at the live raw callback, not a new Main observation gate."
task_order = [
    "Keep work_mode=design_stop and semantic implementation_permission=false.",
    "Open the existing lifecycle guard family only for explicit D1 phase routing; no new guard family.",
    "Then open the validator-only Main owner-witness row with existing APIs and zero-delta tests.",
    "Only after that design evidence is green, design the separate target/loan/dispatcher atomic cutover."
]
non_claims = "This reconciliation does not claim Main direct-call publication, package+loan completion, raw dispatcher cutover, Resolved retirement, Method(None) retirement, mandatory-Callee schema, or VM/backend changes."

[guard_hygiene]
task_id = "MIR-CALL-D1B-LIFECYCLE-NOARG-DISPATCH-HYGIENE-R0"
execution_row = "MIR-CALL-D1B-LIFECYCLE-NOARG-DISPATCH-HYGIENE-R0"
guard_phase = "main_raw_cataloged_handoff_d0"
status = "landed"
implementation_permission = false
scope = "Reuse the existing lifecycle guard only: no-arg dispatch must select the current D0 guard phase, never fall back to historical bridge_ready; explicit historical phases remain available."
allowed_files = [
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
  "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
forbidden = "No src/mir, fixture, Cargo, production switch, target/Callee/loan/dispatcher, raw lineage, argument, fallback/recovery, new guard family, or D1 semantic permission."
acceptance = "Fast-open no-arg selected this task; unknown phase failed closed; after closeout no-arg selects the active D0 design phase; the already-supported bridge and accepted historical phases remain selectable; pointer guard passes; broad D1 and active semantic permission remain false."
implementation_commit = "afb0d79054"
closeout = "bash -n, current_state_pointer_guard, run_row_guard --only mir-call-d1b-cataloged-affine-loan-lifecycle, explicit bridge_ready, and unknown-phase rejection passed; no src/mir or semantic Call files changed; work_mode returned to design_stop and D1 remains implementation-closed."

[external_feedback_reconciliation_2026_08_28]
status = "reconciled_without_opening_a_second_semantic_row"
main_observation = "The reported App Main fail-open is already closed by 012099e01b/605cf718d5: the dedicated pre-install validator calls the forest-wide observation helper, and root-only, nested-only, and mixed direct-call negatives are present. No new Main gate task is opened."
t0_scope = "MIR-CALL-D1B-T0-SCOPE-RECONCILE-D0 is a docs-only follow-up: identity/forest T0/C0/R0 gates are closed, but Main raw Cataloged handoff is not. Keep implementation_permission=false and do not treat T0 as route connection evidence."
guard_source = "MIR-CALL-D1B-LIFECYCLE-GUARD-CURRENT-CARD-SOURCE-R1 is a later hygiene follow-up: remove the remaining historical readiness-audit dependency from the shared lifecycle guard without adding a guard family or opening D1 semantics."
diagnostic_payload = "MIR-CALL-D1B-BARE-STATIC-REJECT-DIAGNOSTIC-D0 remains parked: the bare-static route already rejects before arguments/effects, but Rejected.error and its callers still carry String. Typed diagnostic payload requires its own design row and is not part of the raw handoff."
active_blocker = "The only active D1 blocker remains the live Main raw-scope callback/session witness and exact owner/site/body-kind relation. ScriptRoot-to-Cataloged switching, target/Callee, package+loan, dispatcher, and Call changes remain closed."

[d14_feedback_recheck_2026_08_28]
status = "reconciled_against_current_head"
observed_commit = "a73c10c055"
verdict = "The reported App Main install fail-open is stale at this HEAD: issuer.rs applies the existing forest-wide observation predicate to every owner, and root-only, nested-only, and mixed App Main direct-call negatives reject before install. Do not add a second Main gate or new issue hierarchy."
main_witness = "The old 'Main raw-scope caller absent' statement is also stale: the installed App Main Cataloged root scope landed at 46fdf7a4fa and the live raw callable-owner witness landed at 2dadcae1c2. The remaining D1 question is the exact source-site-to-target issuer, not another owner check."
t0_scope = "T0 has no recorded execution, but it belongs to parked cleanup/guard hygiene. It is not D1 source authority and must not be promoted into a semantic implementation row or used as evidence for Main target publication."
bare_static_diagnostic = "Bare StaticBoxMethod remains a pre-effect typed terminal, while its diagnostic payload still uses String. Keep MIR-CALL-D1B-BARE-STATIC-REJECT-DIAGNOSTIC-D0 separate; do not mix it into lineage, target, loan, or dispatcher work."
qualified_methodcall_audit = "A read-only audit found an existing exact qualified MethodCall source-site/caller/declaration/receiver/arity inventory and a source-owned static target/result issuer with a live raw caller. Its ingress still re-derives the target from owner/method/arity strings before consuming the handoff, so this is a separate design candidate, not a completed cutover."
next_design_candidate = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-D0"
next_design_scope = [
  "specify the existing (Cataloged caller, exact SourceExprSiteV1) -> selected typed handoff or Absent API",
  "make the existing publication owner the sole issuer and keep raw receiver spelling, plan_member_call_route, and declaration_for non-authoritative",
  "consume the already issued target before receiver/argument descent at the existing qualified MethodCall ingress",
  "enumerate missing/unlocated/foreign source, catalog/import mismatch, absent/already-consumed handoff, and target/result-brand mismatch as typed outcomes",
]
next_design_forbidden = [
  "bare FunctionCall, Main package+loan/dispatcher, receiver re-evaluation, dynamic/instance member dispatch, name/arity target synthesis, fallback/recovery, Method(None), Call schema, JSON, VM, or backend changes",
  "new public Verified*/Prepared* receipt, AST rescan, or a new guard family",
]
decision = "Keep the current D1 design_stop and implementation_permission=false. Record the qualified MethodCall D0 as the only clean exact-authority candidate; do not claim that it resolves the bare FunctionCall/Main blocker until its string-rebuild ingress is designed and accepted."

[qualified_method_target_issuer_d0]
task_id = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-D0"
status = "accepted_design_no_safe_slice"
observed_at = "2026-08-28"
implementation_permission = false
guard_phase = "qualified_method_target_issuer_d0"
decision = "For the qualified StaticBoxMethod family, reuse the existing source-side exact products and make the publication owner the sole target issuer. The raw ingress must consume the existing (Cataloged caller, SourceExprSiteV1) handoff; it must not reconstruct a target from receiver/owner/method/arity strings."
source_authority = "VerifiedSourceMethodCallSiteV1 supplies catalog, caller, declaration, exact source site, receiver site, receiver, method, arguments, and arity. VerifiedQualifiedCallRouteFactsV1 supplies the catalog-branded import/lexical/reserved-route admission. VerifiedSourceStaticCallTargetCatalogV1 maps (caller, site) to the sealed static target."
canonical_issuer = "VerifiedStaticCallResultPublicationOwnerV1, issued from the existing source target/result catalogs, is the sole issuer. Its selected row already contains the typed target/result handoff; a later consumer may expose an exact-site take without a second declaration lookup."
live_consumer = "The existing raw MethodCall path is raw_expression_dispatch -> member_route -> static_result_publication_ingress -> static_result_publication_physical_bridge. The bridge already consumes a sealed handoff before argument descent; only the ingress target reconstruction must be designed away."
non_authority = [
  "resolve_static_receiver_box_name, plan_member_call_route, raw receiver spelling, owner/method/arity strings, declaration_for in the Builder consumer, StaticMethodId parsing, CalleeResolverBox, and legacy MethodCall lowering",
  "bare FunctionCall, receiver evaluation, dynamic/instance/member dispatch, EnvMethod, me, reserved routes, Main package/loan, and Call schema",
  "AST rescan, physical symbol, fallback/retry, or a new public Verified*/Prepared* receipt",
]
boundary = "Existing source MethodCall site/route facts and source target/result owner -> exact (Cataloged caller, SourceExprSiteV1) handoff or typed Absent -> qualified static ingress consumes the handoff before receiver/argument descent; standard and compatibility routes remain outside."
finite_states = [
  "Exact source row + same catalog/import brand + selected handoff -> consume once and enter the existing static physical bridge",
  "Exact source row but no selected handoff -> Absent; do not enter the target issuer or synthesize a target",
  "missing/unlocated/foreign raw context, catalog/import mismatch, missing caller/site, duplicate site, target/result brand mismatch, or already-consumed handoff -> typed reject before receiver/argument effects",
  "reserved, dynamic, instance, EnvMethod, me, bare FunctionCall, RawCompatibility, and nested callable boundary -> existing owner or explicit unavailable/park; no qualified-target promotion",
]
acceptance = [
  "two positive source cases (unshadowed canonical owner and imported alias) resolve one exact caller/site row with one typed handoff",
  "ingress no longer calls Builder-side declaration_for or rebuilds target from owner/method/arity; the existing publication owner remains the only issuer",
  "handoff consumption precedes receiver/argument descent, receiver/arguments are lowered once, the existing terminal/Call is emitted once, and failures do not retry or re-enter legacy lowering",
  "missing/foreign/duplicate/ambiguous/wrong-kind/wrong-arity/reserved/dynamic/instance cases remain typed negative outcomes and no unrelated route changes",
]
design_allowed = [
  "existing source_call_target products and callable_result_representation owner/consumer API shape",
  "static_result_publication_ingress and static_result_publication_physical_bridge handoff boundary",
  "focused source/owner/ingress tests and reuse of the existing D1 lifecycle guard family",
]
allowed_files = [
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
  "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
design_forbidden = [
  "bare FunctionCall or Main package+loan/dispatcher, receiver re-evaluation, dynamic/instance route, Method(None), Call schema, JSON, VM/backend, fallback/recovery, and new guard family",
  "name/arity/symbol target synthesis, second AST traversal, target Clone as authority, or a new public semantic receipt",
]
no_safe_slice = [
  "the live qualified caller cannot provide exact caller/site without an AST rescan or synthetic site",
  "same catalog/import brand cannot be proven at the existing owner/ingress boundary",
  "consumer must retain declaration_for/string lookup to select the target",
  "qualified static cannot be isolated from standard/instance/dynamic routes",
  "take-once, failure abort, or residual-zero cannot be represented without Clone/Rc/RefCell/unsafe/a third lifetime/package bypass",
]
next_row = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-I0: after this D0 is selected in CURRENT_STATE, connect the existing exact handoff at ingress and delete only the qualified static string-rebuild edge; implementation permission remains separately scoped."

[qualified_method_target_issuer_i0]
task_id = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-I0"
status = "landed"
observed_at = "2026-08-28"
implementation_permission = false
implementation_commit = "71b8e240eb"
guard_phase = "qualified_method_target_issuer_i0"
parent_row = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-D0"
decision = "Switch only the existing qualified StaticBoxMethod ingress from Builder-side declaration_for(owner, method, arity) reconstruction to a source-keyed take from VerifiedStaticCallResultPublicationOwnerV1. The source owner has already issued the typed handoff; this row changes its consumer, not its meaning."
source_authority = "VerifiedSourceMethodCallSiteV1 and VerifiedQualifiedCallRouteFactsV1 already establish the exact Cataloged caller/site relation; VerifiedStaticCallResultPublicationOwnerV1 remains the sole typed target/result issuer."
canonical_consumer = "static_result_publication_ingress -> ModuleLoweringPortV1 -> ModuleDraftCollectorV1 -> VerifiedStaticCallResultPublicationOwnerV1::take_for_source; the existing physical bridge consumes the returned handoff before receiver/argument descent."
non_authority = [
  "owner/method/arity strings, resolve_static_receiver_box_name, plan_member_call_route, declaration_for in the Builder consumer, StaticMethodId parsing, CalleeResolverBox, and any target reconstruction",
  "bare FunctionCall, Main package/loan/dispatcher, dynamic/instance/me/EnvMethod routes, Method(None), Call schema, JSON, VM/backend, fallback, retry, and unrelated compatibility paths",
]
boundary = "Existing qualified Cataloged MethodCall source context -> exact source-keyed publication owner handoff -> existing static physical bridge; only the ingress lookup edge is replaced."
allowed_files = [
  "src/mir/callable_result_representation/static_call_result_publication_owner.rs",
  "src/mir/callable_result_representation/tests/static_call_result_publication_owner.rs",
  "src/mir/callable_result_representation/README.md",
  "src/mir/builder/module_draft_collector/static_result_publication_owner.rs",
  "src/mir/builder/module_lowering_invocation.rs",
  "src/mir/builder/static_result_publication_ingress.rs",
  "src/mir/builder/README.md",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
  "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
forbidden = "No new receipt, package/loan, RawInvocationChildPort field/lifetime, second AST scan, target/argument re-evaluation, resolver/recovery, direct Call writer, or changes to bare/Main/instance/dynamic/compatibility routes."
acceptance = [
  "The existing owner exposes one crate-private source-keyed take and no longer requires a consumer-supplied target.",
  "Collector and module lowering port forward caller/site only; static_result_publication_ingress contains no declaration_for, StaticBoxMethod namespace lookup, or TargetUnavailable reconstruction edge.",
  "Selected qualified handoff consumption still precedes receiver/argument descent, the existing bridge emits exactly once, absent/unavailable/foreign/consumed rows remain distinct, and no retry/re-entry is added.",
  "Focused owner tests cover selected, absent, wrong-site, foreign-brand, and second-take outcomes; existing D1 lifecycle and line-count guards remain green.",
]
closeout = "After focused tests, rustfmt/diff checks, and the reusable lifecycle guard pass, mark this row landed and return to the D1 design stop. Do not claim bare FunctionCall/Main issuer, package+loan, or Call schema completion."
next_row = "MIR-CALL-D1B-QUALIFIED-METHOD-TARGET-ISSUER-R1: only if the source-keyed qualified consumer is landed with zero old ingress reconstruction callers; otherwise return the residual to design_stop."

[qualified_method_target_issuer_i0.receipt]
status = "landed"
source = "static_call_result_publication_owner.rs::take_for_source; module_draft_collector/static_result_publication_owner.rs; module_lowering_invocation.rs; static_result_publication_ingress.rs"
old_edge = "qualified Cataloged ingress reconstructed a StaticBoxMethod target with Builder declaration_for(owner, method, arity) before selecting the source row"
new_edge = "qualified Cataloged ingress forwards only the exact caller/site key to the existing publication owner, which returns the already-issued handoff"
focused_tests = "static_call_result_publication_owner:: 5 passed; selected, unselected, wrong-site, foreign-catalog, and second-take outcomes covered"
guards = "rustfmt changed files, git diff --check, current_state_pointer_guard.sh, and mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh phase=qualified_method_target_issuer_i0 passed"
source_limits = "static_call_result_publication_owner.rs 225 lines; owner test 254; module_lowering_invocation.rs 743; all below the 760/800 boundary"
non_claims = "No bare FunctionCall/Main issuer, package+loan, RawInvocationChildPort loan, dispatcher cutover, Resolved recovery retirement, Method(None), Call schema, JSON, VM/backend, fallback, or retry change"

[main_bare_freestatic_relation_boundary_d0]
task_id = "MIR-CALL-D1B-MAIN-BARE-FREESTATIC-RELATION-BOUNDARY-D0"
status = "design_stop_no_safe_slice"
observed_commit = "ea25552c29"
implementation_permission = false
decision = "The existing normal_source_plan/resolved_value_profile can issue an exact FreeStatic product for a sealed canonical Main source, but no production bridge carries that product into live Builder raw lowering. Do not treat Main catalog identity, raw Cataloged lineage, or name/arity as a bare-call target issuer."
source_authority = "VerifiedTrivialDirectCallV1::seal and its normal_source_plan owner co-seal exact source site, owner, callable-index relation, FreeStatic target, argument sites, result, and conservative effect for the canonical resolved route."
canonical_consumer = "No live raw Builder consumer exists yet; the existing product is consumed only by resolved_lowering/trivial_ssa. A future bridge must be explicitly designed before package+loan or raw dispatcher implementation."
non_authority = "AppMainCatalogCoSealV1, RawInvocationRootLineageV1::Cataloged, FunctionOwnerIdV1 witness, name/arity/box strings, static_candidates, CalleeResolverBox, MIR Call/Callee, and canonical resolved-route physicalization cannot issue or transport a bare Main target by inference."
fail_fast = "The existing forest-wide direct-call observation gate rejects Main bare observations before package install. If an exact source/profile-to-Builder relation cannot be proven without a second scan, synthetic key, or new unowned receipt, remain TypedRejectBeforeArguments or explicit ParkedSealed; no effects, retry, fallback, or partial publish."
smallest_next_slice = "Design-only premise audit for an explicit FreeStatic source/profile-to-Builder bridge: name the issuer, session/brand witness, raw site mapping, package/loan owner, and failure/rollback contract. If those cannot be named from existing owners, ratify the bare Main park rather than adding a compatibility adapter."
acceptance = "One compact authority decision distinguishes canonical FreeStatic production from raw Builder consumption; all required products, live caller, exact site relation, and ownership/lifetime boundary are either proven or recorded as NoSafeSlice."
non_claims = "No code, fixture, target issuer, package+loan, RawInvocationChildPort field/lifetime, dispatcher, Resolved retirement, Method(None), Call schema, JSON, VM/backend, fallback, or retry change. Qualified MethodCall I0 remains the only newly landed ingress switch."

[main_raw_freestatic_bridge_recheck_2026_08_28]
status = "superseded_by_d2_b_double_prime"
observed_commit = "f7874d4135"
decision = "Main-to-helper FreeStatic support is selected as the language contract. A transport-only source/profile-to-live-raw bridge is disproven: canonical-core and live normal/default currently issue different resolver-session and FunctionOwner brands. Keep the pre-effect typed park until one live source owner and resolver session co-issue the existing FreeStatic profile and package owner; never rebuild a target from name/arity, catalog key, raw lineage, or Builder recovery."
source_authority = "NormalMainDirectCallPreflightV1 and VerifiedTrivialDirectCallV1 remain the sole issuer of the exact Main FreeStatic source product; ResolvedDirectCallTargetV1 and the callable index establish the target before raw lowering."
non_authority = "Main parser/catalog identity, RawInvocationRootLineageV1::Cataloged, FunctionOwnerIdV1 alone, SameModuleCallableCatalogBrandV1, ModuleInvocationBrandV1, name/arity/symbol/static_candidates, CalleeResolverBox, and a canonical-core frontdoor switch cannot issue or repair the target."
existing_bridge = "normal_source_plan main_direct_call_source/main_direct_call_plan -> normal_callable_transaction_handoff -> resolved_lowering/trivial_ssa; this HRTB bridge does not reach RawInvocationChildPortV1. The parser-retained normal-source surface is borrowable only through its parser owner and is not an existing FreeStatic handoff into the Builder package."
live_raw_gap = "program_root_lowering -> installed package Main callback -> RawInvocationChildPortV1 has no FreeStatic profile/site cursor or exact source-to-raw handoff input. The package issuer still rejects any unissued direct-call observation before install, and the live callback currently exposes only lowering input/identity/raw lineage."
callback_chain_reaudit = "Named live chain is normal_default_root_catalog_lifecycle::complete_normal_default_program_root_catalog_lifecycle_with_target -> finish_normal_default_root_after_pre_effect_bind -> MirBuilder::lower_program_root_after_catalog_install_v1 -> RawInvocationChildPortV1 -> normal_callable_semantic_loan_port::main_root::lower_app_main_root_body_v1; no callback in this chain receives the existing FreeStatic profile and raw scope together."
package_batch_reaudit = "The live package batch is issued with ObserveForCatalogedValidation and retains only ResolvedDirectCallObservationV1 facts; its inputs have no callable index, argument-site inventory, or existing VerifiedTrivialDirectCallV1 target. Issuing a target from this batch would require a second resolver/AST scan or a new name/arity authority, so it is not a bridge."
parser_surface_reaudit = "ParserNormalRootExecutionSourceV1 retains callable identities and the normal-source surface, but the live Builder consumes the root execution owner separately and has no source-backed callback that can borrow both that surface-derived FreeStatic plan and the raw Main scope. Reusing whole_source_inventory/static_call_result_publication_owner would cross from qualified MethodCall/result publication into bare FunctionCall and create a second authority."
next_design_task = "MIR-CALL-D1B-MAIN-FREESTATIC-SAME-SESSION-PRODUCER-COSEAL-D2: identify one live source owner and one FunctionSemanticResolverSessionV1 that can issue the package owner, callable index, and existing FreeStatic profile exactly once; transport, loan, dispatcher, and Call remain closed."
candidate_disposition = [
    "A selected language contract: support Main-to-helper FreeStatic, but issue its existing profile family inside the same live source/resolver ownership chain as the normal/default package; do not carry a foreign-session canonical-core product.",
    "B parked as a separate frontdoor decision: switching Main wholesale to canonical-core would replace the root lifecycle, package installation, collector, compatibility routing, and AOT/EXE entrance in one unbounded row.",
    "C current runtime state only: keep the forest-wide pre-install typed reject until A is proven; C is not the final language contract because Main-to-top-level-helper is an accepted FreeStatic shape.",
]
bridge_contract = "The D2 must first co-issue Main FunctionOwnerIdV1, exact SourceExprSiteV1, ordered argument sites, callable-index target, and package owner from one live source owner and resolver session. Only then may the existing lifecycle callback move that product into an owner-private affine inventory and join it with live raw SourceNodeSiteV1/lineage; no foreign-session product, public semantic receipt, second target issuer, parser seed re-consumption, or AST rescan."
package_loan_followup = "Only after the bridge is exact: MIR-CALL-D1B-CATALOGED-SOURCE-RELATION-AND-AFFINE-LOAN-I0 adds a Builder-private #[must_use] package+loan bundle, consuming lowering callback, existing-port-lifetime &mut reborrow, take_once/finish_empty, Aborted/no-retry, and atomically deletes the CatalogedTargeted direct Callee payload plus selected package-only bypass."
ordered_followups = [
    "D2 current: close same-live-session producer co-seal for source owner, callable index, FreeStatic profile, and package owner; otherwise retain the pre-effect typed park.",
    "I0: exact issuer projection -> package+affine loan -> raw take_once -> arguments once -> Call once; delete CatalogedTargeted direct payload and selected package-only bypass in the same row.",
    "R2: classify every remaining lineage pre-effect, then remove Resolved, caller=None source collapse, unique/tail recovery, direct name Const, and direct legacy Call edges without touching compatibility-owned siblings.",
    "R3: retire Method(None), methodize, receiver heuristics/repair, and the related VM/optimizer recovery as one Method corridor family.",
    "R4: dispose every remaining typed_global_target_from_selected_symbol caller by its source owner or typed reject.",
    "R6: atomically remove Call.func, Option<Callee>, and INVALID target sentinels after all production writers/readers are mandatory-Callee ready.",
]
finite_states = [
    "ExactReady: existing FreeStatic profile, semantic owner, source site, raw site/lineage, and same-session/catalog brand are all uniquely co-sealed; this is only the prerequisite for a later issuer row.",
    "NoExactRelation: the FreeStatic product exists only on the canonical resolved route or cannot be joined to the live raw site; remain NoSafeSlice and do not publish a target.",
    "Missing: profile, owner, site, raw context, or brand witness is absent; typed reject before body/argument/effect and no retry.",
    "ForeignOrMixedBrand: session, catalog, module, owner, or lineage identity differs; typed reject before effects.",
    "DuplicateOrAmbiguous: more than one site/owner/target relation is possible; typed reject without selecting a winner.",
    "WrongKindOrArity: relation is not the exact FreeStatic family or arity does not match; reject without repair.",
    "UnsupportedRoute: nested owner, TopLevel/Constructor, RawCompatibility, or compatibility Main route lacks an exact relation; keep its named owner as ParkedSealed and do not borrow Main meaning.",
]
guard_contract = "Reuse the existing mir-call-d1b-cataloged-affine-loan-lifecycle guard and keep its current main_raw_lineage_handoff_d1 phase; create no new guard family or phase merely for this design subtask. While D0 is open, work_mode=design_stop, implementation_permission=false, package/loan/dispatcher changes=0, and fallback/retry=0."
acceptance = "The bridge D0 is complete only when the existing source product, exact caller, semantic/raw site bijection, same-session witness, private callback owner, and failure/no-retry boundary are named from existing owners or the family is explicitly retained TypedReject/ParkedSealed. A worker audit or local green test cannot open implementation."
no_safe_slice_conditions = [
    "name/arity/symbol/static candidates are the only available relation evidence",
    "a second AST/source traversal or synthetic site/key is required",
    "no named production callback can receive both the existing profile and the live raw Main scope",
    "Cataloged lineage or Main catalog key would have to be treated as a target issuer",
    "nested or compatibility routes must be mixed into the Main bridge",
    "package+loan/dispatcher implementation or a new guard/document family is required to express the D2",
]
decision_recorded = "The user selected Main-to-helper FreeStatic support. This selects the language behavior, not an unsafe transport: same-live-session producer co-seal must close before implementation, and the current pre-effect typed park remains until then."
implementation_permission = false
non_claims = "No target/Callee issuer, package+loan, RawInvocationChildPort field/lifetime, dispatcher, Call schema, Resolved retirement, fallback, retry, or canonical-core-to-raw route change is opened by this recheck."
superseded_by = "main_freestatic_same_session_producer_coseal_d2_2026_08_29; the existing whole trivial profile is evidence for target semantics, not the live Builder product to reissue."

[main_freestatic_same_session_producer_coseal_d2_2026_08_29]
task_id = "MIR-CALL-D1B-MAIN-FREESTATIC-SAME-SESSION-PRODUCER-COSEAL-D2"
status = "accepted_b_double_prime"
observed_commit = "a796ab1d51"
implementation_permission = false
decision = "Select B-double-prime. In the existing live normal/default source lifecycle, one FunctionSemanticResolverSessionV1 and one shadow traversal retain both the exact direct-call observation (caller/site plus ordered argument sites) and the same-session ResolvedDirectCallTargetV1. Do not invoke or transport the whole VerifiedTrivialDirectCallV1 profile: it additionally claims argument/result representation that this source pass has not proved."
source_authority = "complete_normal_default_program_root_catalog_lifecycle_with_target owns the consumed normal-program source and creates the sole live FunctionSemanticResolverSessionV1. That resolver issues FunctionOwnerIdV1, ResolvedDirectCallTargetV1, and VerifiedCallableIndexV1 membership; the same traversal issues SourceExprSiteV1 and ordered argument sites. ConsumedNormalRootCallableSourceV1::consume_into_semantic_package is the sole package co-issue boundary."
canonical_issuer = "ResolvedDirectCallTargetV1 is the target authority. VerifiedCallableIndexV1::header_for_callable is an identity-membership and ABI lookup after target issuance, not a name resolver. In I0, the existing VerifiedCanonicalDirectCallEmissionV1::conservative_from_header may project the already-issued target and header effect exactly once."
non_authority = "NormalMainDirectCallPreflightV1, VerifiedTrivialDirectCallV1, the value-profile analyzer, canonical-core re-entry, a second resolver/traversal, SourceExprSiteV1 shape matching across sessions, FunctionOwner slot equality, name/arity/symbol/static_candidates, Builder resolver calls, cloned parser source, raw lineage, and catalog brand alone cannot join or reissue the target."
finite_disposition = "CallFree or Exact(package-private inventory). Exact owns the App Main caller, its same-session callable index, and source-site-keyed rows of ordered argument sites plus ResolvedDirectCallTargetV1. Missing, duplicate, target-without-observation, observation-without-target, foreign brand/owner, wrong kind/arity, nested-owner use, and mixed compatibility are typed pre-install rejects; no partial inventory is installed."
fail_fast = "Before package install, source-observation and target site sets must be equal, every target must be an identity member of the owned index, header arity must equal the ordered argument-site count, and all owners/brands must match. Before argument effects, raw Cataloged lineage and exact raw/source site parity must match. After take_once, unknown or mismatched actual argument ABI aborts the candidate lowering session before Call; the consumed row is not restored and no retry/fallback occurs."
smallest_next_slice = "Open the existing Cataloged source-relation plus affine-loan I0 as one bounded implementation row. First make a behavior-neutral owner-specific split of resolved_semantics/resolver.rs (744 lines), then connect source facts + same-session target -> private disposition -> package+loan -> raw take_once -> arguments once -> header ABI validation -> Call once -> finish_empty, deleting the selected direct payload/bypass in the same row."
acceptance = [
    "normal source owner count = 1 and resolver session count = 1",
    "the existing shadow traversal records ordered argument sites while resolving arguments; AST/source traversal count remains 1",
    "RequireCallableIndex retains observation and ResolvedDirectCallTargetV1 at the same site; ObserveOnly remains facts-only and RejectUnindexed remains empty",
    "Main package owner, caller target, callable index, and raw Cataloged lineage use the same compilation/session brand",
    "observation-site set equals target-site set; exact source call key includes owner plus SourceExprSiteV1 and ordered argument sites",
    "the package-private row contains no name/arity, AST, ValueId, Callee, physical symbol, effect guess, result representation, Rc/Arc/RefCell, Clone, or public into_parts",
    "canonical-core frontdoor re-entry, main_direct_call_source re-execution, value-profile analyzer import, Builder resolver call, AST rescan, Clone/reparse, fallback, and retry = 0",
    "the existing pre-effect typed park remains production behavior until the atomic I0 cutover",
]
implementation_followup = "Use MIR-CALL-D1B-CATALOGED-SOURCE-RELATION-AND-AFFINE-LOAN-I0 as one active production row with internal S0/I0-A/I0-B commits: S0 behavior-neutral resolver split; I0-A one-pass facts+target and inseparable package disposition; I0-B short existing-port-lifetime &mut reborrow, exact-site take_once before recursive arguments, header ABI validation, Call once, finish_empty, and deletion of the selected CatalogedTargeted/package-only/resolver bypass. Do not create a separate producer or I1 card."
implementation_steps = [
    "S0 BoxShape: split resolved_semantics/resolver.rs before semantic growth; preserve module path, visibility, errors, tests, and behavior.",
    "I0-A BoxCount: collect ordered argument sites in the existing traversal, retain observation plus same-session target under RequireCallableIndex, verify two-way site coverage, and move CallFree/Exact into the package-private lifecycle.",
    "I0-B cutover: lend the non-Clone loan through the existing raw-port lifetime, take the owned row before recursive arguments, validate header ABI after argument lowering, emit one MirInstruction::call, require finish_empty, and delete only the selected App Main direct payload/package-only/resolver edge.",
]
i0_gates = [
    "positive: helper(a,b) preserves argument-site order and outer(inner(1),2) can take outer then inner without overlapping loan borrows",
    "negative: missing/duplicate/foreign/wrong-site/wrong-owner/wrong-arity, target without observation, observation without target, nested-lambda owner, second take, and residual rows reject",
    "abort: argument type unknown/mismatch or recursive lowering failure publishes no Call/module result, restores no row, and enters no fallback/retry/resolver",
    "structural: source traversal=1, resolver session=1, target take precedes argument descent, Call=1, full trivial-profile/value-analyzer imports=0, and downstream name/arity lookup=0",
    "retirement: the selected App Main CatalogedTargeted/direct Callee payload, package-only lowering bypass, and consumer resolver handoff each have production caller count 0; unrelated siblings remain explicitly owned",
    "limits: every touched source stays below 760 lines after the required split; 800 lines is a hard stop",
]
allowed_owner_files = "resolved_semantics/shadow product/expr/resolver, resolved_semantics/direct_call and a split resolver sibling, source-bound semantic batch/package issuer/model, one owner-specific normal-callable package/loan sibling, recursive raw-port loan scope, and a small calls consumer sibling; wiring in large integration files must remain narrow."
guard_scope = "Reuse the current D1 lifecycle and exact-target guard families. The future cataloged_i0 global-resolver-zero assertions belong to R2 and must not be claimed as evidence for a partial selected-family cutover."
boxshape_stop = "Do not add semantic code to normal_callable_semantic_loan_port.rs (748 lines), builder.rs (748 lines), or module_lowering_invocation.rs (743 lines). Use an owner-specific sibling; perform a behavior-neutral split only if a touched source would reach 760 lines, with 800 as hard stop."
non_claims = "No package+loan type, RawInvocationChildPort capability, dispatcher, target/Callee publication, argument lowering, Call emission, CatalogedTargeted deletion, Resolved recovery retirement, Method(None), Call schema, VM/backend, JSON, JoinIR, or compatibility change is authorized by D2."

[cataloged_source_relation_affine_loan_i0]
task_id = "MIR-CALL-D1B-CATALOGED-SOURCE-RELATION-AND-AFFINE-LOAN-I0"
execution_row = "MIR-CALL-D1B-CATALOGED-SOURCE-RELATION-AND-AFFINE-LOAN-I0"
status = "landed"
implementation_permission = false
implementation_commit = "99bdfc856c"
guard_phase = "cataloged_source_relation_affine_loan_i0"
parent_row = "MIR-CALL-D1B-MAIN-FREESTATIC-SAME-SESSION-PRODUCER-COSEAL-D2"
decision = "Implement the selected App Main FreeStatic family using B-double-prime only: one live source/resolver traversal co-issues exact source/ordered-argument facts and ResolvedDirectCallTargetV1, then a private non-Clone package disposition is consumed by one scoped affine loan. Do not broaden this row to a whole trivial profile or to global resolver retirement."
source_authority = "complete_normal_default_program_root_catalog_lifecycle_with_target and its consumed normal source own the one FunctionSemanticResolverSessionV1, caller owner, source site, ordered argument sites, and identity-based callable-index target."
canonical_issuer = "ResolvedDirectCallTargetV1 is issued by that same resolver; VerifiedCallableIndexV1::header_for_callable supplies identity membership and ABI. Existing emission/header projection may be used only after the owned row is taken."
non_authority = "NormalMainDirectCallPreflightV1, VerifiedTrivialDirectCallV1, value-profile analysis, name/arity/symbol parsing, raw lineage alone, Builder resolver calls, canonical-core re-entry, second traversal, retry, and fallback."
boundary = "Selected installed App Main FreeStatic source owner -> same-session exact disposition -> package+loan -> RawInvocationChildPort exact take -> arguments -> one Call -> residual-zero; all other lineages and compatibility routes remain outside this row."
allowed_files = [
    "src/mir/resolved_semantics/shadow/product.rs",
    "src/mir/resolved_semantics/shadow/expr.rs",
    "src/mir/resolved_semantics/shadow/resolver.rs",
    "src/mir/resolved_semantics/direct_call.rs",
    "src/mir/resolved_semantics/direct_call_inventory_gate.rs",
    "src/mir/resolved_semantics/owner_resolver_app_main.rs",
    "src/mir/resolved_semantics/resolver.rs",
    "src/mir/resolved_semantics/owner_resolver.rs",
    "src/mir/callable_semantic_batch/issuer.rs",
    "src/mir/normal_callable_semantic_package/issuer.rs",
    "src/mir/normal_callable_semantic_package/model.rs",
    "src/mir/normal_callable_semantic_package/install.rs",
    "src/mir/builder/normal_callable_package_bridge.rs",
    "src/mir/builder/normal_callable_semantic_loan_port/main_root.rs",
    "src/mir/normal_callable_semantic_package/direct_call_loan.rs",
    "src/mir/normal_callable_semantic_package/app_main_relation.rs",
    "src/mir/builder/recursive_child_lowering.rs",
    "src/mir/builder/recursive_child_lowering_port.rs",
    "src/mir/builder/calls/function_call_preflight_route.rs",
    "src/mir/builder/calls/build.rs",
    "src/mir/builder/normal_default_root_catalog_lifecycle.rs",
    "src/mir/builder/program_root_lowering.rs",
    "src/mir/resolved_semantics/*_tests.rs",
    "src/mir/normal_callable_semantic_package/*_tests.rs",
    "src/mir/builder/*_tests.rs",
    "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "tools/checks/guard_rows.toml",
    "docs/development/current/main/CURRENT_STATE.toml",
    "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
]
old_edge_delete_set = [
    "selected App Main CatalogedTargeted direct Callee payload",
    "selected package-only lowering/opening bypass",
    "selected consumer-side resolver/target handoff",
]
implementation_steps = [
    "S0 BoxShape: behavior-neutral split of resolved_semantics/resolver.rs before semantic additions; preserve module path and visibility.",
    "I0-A: collect ordered argument sites during the existing shadow traversal, co-seal source observations with same-session target/index membership, and install only a complete private CallFree/Exact disposition.",
    "I0-B: consume package+loan once, reborrow through the existing raw-port lifetime, take the exact row before recursive arguments, validate header ABI, emit one Call, finish_empty, and delete the selected old edges.",
]
acceptance = [
    "one live resolver/session and one AST traversal issue the source facts and target; no canonical-core bridge, profile analyzer, clone, reparse, or second resolver",
    "source-site set equals target-site set, ordered argument sites are exact, callable-index membership and same compilation brand are proven, and nested lambda owners are rejected rather than inferred",
    "package disposition is private/non-Clone and inseparable from the loan; unknown/duplicate/foreign/missing/residual rows hard-reject before partial publish",
    "take_once precedes recursive argument descent; outer(inner()) works without overlapping loan borrows; argument mismatch/failure aborts the candidate session with no restore/retry/fallback",
    "actual arguments are checked against the target header after lowering and before Call; no unproved InlineI64/result/effect profile is manufactured",
    "selected old edges have production caller count zero; unrelated CatalogedTargeted/Resolved compatibility siblings remain explicitly owned",
    "focused positive/negative tests, existing D1 lifecycle guard, exact-target guard, rustfmt, diff check, and quick library checks are green; every touched source stays below 760 lines",
]
no_safe_slice = [
    "the live source/resolver cannot issue exact target rows in the same session",
    "ordered argument sites require a second traversal or synthetic key",
    "actual ABI validation needs an invented representation/effect/result receipt",
    "package-only install remains callable outside the selected consuming scope",
    "the selected old edge cannot be deleted in the same bounded row",
    "any change reaches Method(None), global Resolved recovery, VM/backend, JSON, JoinIR, or Call schema",
]

[cataloged_source_relation_affine_loan_i0_verification_2026_08_29]
status = "landed"
implementation_commit = "99bdfc856c"
positive = "The selected App Main FreeStatic path co-issued source observations and same-session indexed targets, carried a private non-Clone loan into the live raw port, consumed it before argument descent, and required residual-zero completion."
negative = "Nested-owner, wrong-arity, unsupported non-FreeStatic header, foreign-owner, duplicate-site, second-take, residual-loan, and observation/target coverage mismatches have named fail-closed paths."
typed_failure = "App Main owner-relation failures use AppMainRootRelationIssueV1; direct-call disposition failures remain AppMainDirectCallDispositionIssueV1, including TargetNameMismatch; unsupported headers use the explicit OutsideExactProfile state."
guards = [
    "bash -n tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "bash tools/checks/current_state_pointer_guard.sh",
    "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh cataloged_source_relation_affine_loan_i0",
    "git diff --check",
]
compile_checks = [
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --lib (passed; existing warnings only)",
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --tests --lib -q (passed; existing warnings only)",
]
known_limit = "Full cargo test/link was not rerun because the repository's large test binary previously exceeded the available memory; test targets were type-checked and the reusable lifecycle guard was executed."
closeout = "I0-A/B is complete for the selected App Main FreeStatic family. Package-only bypass is no longer the live route, the affine loan has a live take_once consumer and finish_empty check, and the old selected direct handoff is not claimed as the remaining contract. R2 all-lineage retirement, Method corridor, VM, and Call-schema changes remain closed."
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

[worker_task_synthesis_2026_08_29]
status = "accepted_finite_plan"
source = [
  "d1_lineage_issuer_design: the live root lifecycle is the unique source/package owner; canonical-core and live products cannot be joined across resolver brands",
  "resolver co-issue API audit: one shadow traversal can retain ordered argument sites and same-session ResolvedDirectCallTargetV1 without a whole trivial-value profile",
  "producer co-seal adversary: full VerifiedTrivialDirectCallV1 reuse is rejected as AST-rescanning and representation-overclaiming; B-double-prime exact target disposition is selected",
  "borrow audit: package+loan can close with existing owners, one callback, and the existing port lifetime",
  "call_residual_retirement_audit: residual Call spine and caller/owner boundaries are finite and must be retired by family",
  "freestatic_bridge_adversary + borrow audit: canonical-core and live normal/default use different resolver-session/FunctionOwner brands; transport-only is NoSafeSlice, while existing-port-lifetime affine borrowing is feasible after same-session producer co-seal",
]
definition_of_clean = "MirBuilder ordinary-call meaning is decided once before argument effects, one existing authority issues Callee, one affine handoff reaches the raw consumer, Call is emitted once, and every old resolver/recovery/optional-carrier edge is caller-zero or explicitly compatibility-owned."
carrier_note = "The landed App Main loan carries a verified direct-call emission/disposition whose physical Callee is materialized by the existing one-way emitter after argument lowering; semantic target-before-effects is proven, but a physical Callee object before arguments is reserved for R6 mandatory-Callee work."
remaining_core_rows = 4
remaining_core_rows_note = "D2 and the selected App Main I0 are closed. Four implementation/retirement rows remain. They are bounded responsibility families, not per-file or per-function ceremonies."
ordered_rows = [
  "D2 closed: B-double-prime co-issues exact source/ordered-argument facts and ResolvedDirectCallTargetV1 from one live source/resolver lifecycle; it does not issue a whole trivial-value profile.",
  "I0 landed at 99bdfc856c: same-session target disposition -> private package+loan -> raw take_once -> arguments once -> header ABI validation -> Call once; the selected App Main direct handoff/package bypass is closed.",
  "R2: MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0 — classify remaining lineages before effects and retire Resolved, post-argument unique/tail recovery, direct name-Const/legacy-Call edges; preserve named compatibility siblings (R2a/R2b/R2c provenance landed).",
  "R3: MIR-CALL-METHOD-CORRIDOR-R0 — remove Method(None), methodize, receiver autoscan/strip/repair, and related VM/optimizer recovery as one receiver-authority family.",
  "R4: MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 — move every remaining typed_global_target_from_selected_symbol caller to its real owner or typed reject; no new symbol parser authority.",
  "R6: MIR-CALL-CORE-SCHEMA-CUTOVER-R6 — after all writers/readers are ready, atomically remove Call.func, Option<Callee>, INVALID sentinel, and impossible-state guards.",
]
row_exit_rule = "Each implementation row is at most one active card, one bounded production switch, focused positive/negative evidence, reusable guard, and one closeout; no per-observation child cards. A row is not complete while its old authority edge remains live. Any row touching test code must also run and record a nonzero filtered cargo test for every touched test name/module; 0 tests or any failure blocks closeout, and cargo check alone is insufficient."
expected_shape = "D0 is docs-only; I0 may temporarily add a small private loan owner; R2/R3/R4/R6 are the net source-line reduction points. Do not use temporary line growth as a completion metric; measure old-edge/caller counts and residual-zero instead."
post_core_hygiene = [
  "H1 optional: delete caller-zero compatibility helpers and stale guards only with successor/caller-zero proof.",
  "H2 optional: split remaining integration hubs (CompilationContext/Raw port/adapter) by BoxShape after Call semantics are closed; do not reopen Call authority.",
]
not_in_core = "VM/backend retirement, JSON ingress, JoinIR cleanup, FunctionMetadata sub-owners, and broad guard consolidation remain separate lanes; they do not block the six-row Call spine once their explicit owner gates are satisfied."
no_new_task_families = "Reuse the existing D1 lifecycle guard and current design card. Do not create a new census/owner-map/guard family for each row."
implementation_gate = "D2 and the selected App Main I0 are accepted. The next explicit fast transition may open only MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0; a whole trivial profile, second traversal/resolver, representation guess, or Method/VM/Call-schema change returns to NoSafeSlice. Later rows reopen one at a time after closeout."

[all_lineage_pre_effect_retire_r0_2026_08_29]
status = "design_accepted_bounded_subrow"
task_id = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
decision = "Do not globally delete Resolved yet. First close the finite lineage matrix before argument effects: only the existing App Main exact loan is Targeted; source-backed ordinary calls without an exact relation become typed Reject; named compatibility owners remain ParkedSealed."
source_authority = "RawInvocationRootLineageV1 and exact SourceExprSiteV1 provide provenance; the same-session package/catalog issuer provides target authority. Source identity never synthesizes a Callee."
non_authority = "InstalledNonBrand{caller=None}, name/arity, symbol parsing, static_candidates, variable_map, current_static_box, tail/header suffix scans, env flags, name Const, and legacy Call writers."
fail_fast_boundary = "For an InstalledNonBrand ordinary FunctionCall with no exact caller/site/brand/loan relation, return a typed Rejected disposition before drive_call_arguments_v1; no resolver, recovery, fallback, or retry may run afterward."
smallest_next_slice = "MIR-CALL-D1B-R2-ONE-NAMED-OWNER-FATE-D0: choose exactly one named compatibility owner for a successor-or-park decision; do not delete shared Resolved/recovery while the other three remain live."
non_claims = ["No target issuer for TopLevel/Constructor/ScriptRoot/Main/Nested/non-Main Cataloged; no Method/VM/JSON/JoinIR/Call-schema change; no global Resolved/recovery deletion yet."]
boundary_includes = ["raw FunctionCall dispatch -> preflight -> ordinary completion; Resolved/recovery/name-Const/legacy-Call producer census; six lineage families plus Unlocated/Relationless disposition."]
boundary_excludes = ["AppMainTargeted loan, caller-backed Cataloged, special owners, Method corridor, VM/backend, mandatory-Callee schema."]
lineage_disposition_matrix = ["Cataloged App Main=Targeted; non-Main Cataloged=R4/Parked or Reject; TopLevel/Constructor/Nested=Reject; semantic/raw ScriptRoot, raw Main, RawLegacy=named compatibility; Unlocated/other=Reject unless named."]
worker_evidence = "Fresh read-only owner, successor, and guard audits after 29ca8851b0 agree that all four named compatibility origins lack a common exact successor; R2d already rejects generic unclassified input, so the next decision must select one owner or explicitly keep it ParkedSealed."
acceptance = ["R2a/R2b/R2c named routes remain pre-effect and unchanged; unclassified ordinary sources have zero child/header/MIR/collector delta; Resolved is reachable only from the four named compatibility origins; no generic Relationless fallthrough remains.", "No global Resolved/recovery/name-Const/legacy-Call deletion is claimed while any named owner lacks an exact successor or an approved behavior decision; the next work is one-owner fate only."]
no_safe_slice = ["Reject if nested compatibility must be preserved without an exact root-scope carrier, if special routes cannot be separated before ordinary rejection, or if the row reaches target/loan/dispatcher/recovery/Method/VM/Call schema.", "Return to design_stop when no named owner has an exact source-backed successor or approved pre-effect retirement; do not delete the shared helper or invent a fifth provenance."]

[installed_nonbrand_pre_effect_reject_r2a]
status = "landed"
task_id = "MIR-CALL-D1B-INSTALLED-NONBRAND-PRE-EFFECT-REJECT-R2A"
implementation_permission = false
implementation_commit = "1db9a097ba"
guard_phase = "installed_nonbrand_pre_effect_reject_r2a"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
decision = "For an ordinary FunctionCall admitted as InstalledNonBrand with no exact caller relation, reject before argument descent. Do not issue a target and do not touch App Main, caller-backed Cataloged, earlier special routes, or explicit Relationless compatibility; caller-none Builtin/Extern-shaped ordinary names are inside this rejection boundary."
allowed_files = [
    "src/mir/builder/calls/function_call_preflight_route.rs",
    "src/mir/builder/calls/function_call_installed_nonbrand_reject_tests.rs",
    "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "tools/checks/guard_rows.toml",
    "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
    "docs/development/current/main/CURRENT_STATE.toml",
]
fail_fast_boundary = "The new Rejected disposition must be selected before drive_call_arguments_v1, header lookup, resolver/recovery, name Const, or MIR Call publication."
acceptance = [
    "InstalledNonBrand caller=None ordinary calls have a named typed terminal and zero child descent/instruction delta.",
    "GC retirement keeps its existing terminal; AppMain, caller-backed Cataloged, math/typeop/fastmem/str, and Relationless compatibility behavior is unchanged. Caller-none Builtin/Extern-shaped ordinary names are rejected by the same source-relation terminal unless an earlier special route owns them.",
    "The shared caller=None terminal has a focused pre-effect negative; the R2 lineage matrix maps TopLevel/InstanceConstructor/Nested/Unlocated source-backed paths to that terminal without claiming global Resolved zero.",
    "The scoped lifecycle guard and quick library/test-target type checks pass; the changed production owner remains below 760 lines.",
]
no_safe_slice = [
    "the branch needs name/arity/symbol target construction or a second resolver",
    "the branch changes AppMain loan, caller-backed Cataloged, Relationless compatibility, Method(None), VM/backend, or Call schema",
    "pre-effect rejection cannot prove zero argument descent and zero MIR/collector mutation",
]

[installed_nonbrand_pre_effect_reject_r2a_verification_2026_08_29]
status = "landed"
implementation_commit = "1db9a097ba"
positive = "InstalledNonBrand caller=None ordinary FunctionCall now selects a named Rejected disposition before child descent; the existing App Main loan, caller-backed Cataloged, earlier special routes, and Relationless compatibility remain outside the branch, while caller-none Builtin/Extern-shaped ordinary names are intentionally covered by the reject boundary."
negative = "The focused test records zero child expressions, zero header events, and zero emitted Call instructions for the source-backed caller-none terminal."
guards = [
    "bash -n tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "bash tools/checks/current_state_pointer_guard.sh",
    "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh installed_nonbrand_pre_effect_reject_r2a",
    "git diff --check",
]
compile_checks = [
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --lib (passed; existing warnings only)",
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --tests --lib -q (passed; existing warnings only)",
]
known_limit = "Full cargo test/link was not rerun because the repository's large test binary previously exceeded available memory; test targets were type-checked and the scoped lifecycle guard was executed."
closeout = "R2a is complete as a bounded pre-effect rejection slice. Generic unclassified provenance was outside this row and was subsequently closed by R2d; named compatibility remains separately owned before any global resolver/recovery deletion claim."
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

[r2b_compatibility_provenance_d0_2026_08_29]
status = "landed"
implementation_commit = "b48a072098"
task_id = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-PROVENANCE-R2B-D0"
execution_row = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-PROVENANCE-R2B-D0"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
implementation_permission = false
guard_phase = "resolved_compatibility_provenance_r2b"
allowed_files = ["src/mir/builder/calls/function_call_brand_source_demand.rs", "src/mir/builder/calls/function_call_preflight_route.rs", "src/mir/builder/calls/function_call_preflight_route_tests.rs", "src/mir/builder/calls/function_call_script_compatibility_tests.rs", "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh", "tools/checks/guard_rows.toml", "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml", "docs/development/current/main/CURRENT_STATE.toml"]
decision = "Before deleting any Resolved/recovery edge, preserve explicit compatibility as a named finite owner and prevent source-backed installed paths from falling through the same terminal. Do not globally rename/delete Resolved until every producer is in this owner map."
source_authority = "The semantic ScriptRoot scope owns its existing compatibility entry; raw ScriptRoot, raw-root Main, and RawLegacy now have separate named provenance owners. Exact App Main/Cataloged issuers remain the only target authorities."
non_authority = "callable_ledger absence, lineage spelling alone, name/arity, symbol parsing, variable_map, static/header uniqueness, environment flags, name Const, post-argument ValueIds, and a generic Resolved fallback."
fail_fast_boundary = "FunctionCall brand/source authority selection, after special-form classification and before drive_call_arguments_v1: an installed source with no exact target relation must reject, or enter a named ParkedSealed compatibility owner; it may not enter an unclassified Resolved path."
acceptance = [
    "Semantic ScriptRoot with no callable target has a named ScriptRootParkedCompatibility provenance; source-backed InstalledNonBrand caller-none remains pre-effect Reject.",
    "The existing Resolved terminal remains reachable only from named compatibility origins; generic unclassified fallthrough was a known R2d blocker at this point in history.",
    "Named compatibility behavior is unchanged, with no target synthesis, resolver retry, or fallback; the guard does not claim global Resolved zero.",
]
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

r2b_verification_summary = "b48a072098: quick lib/tests checks, R2b/R2a lifecycle guards, pointer guard, and diff check passed; full test/link remains limited by the known large-binary memory budget."

[r2c_compatibility_provenance_d0_2026_08_29]
status = "landed"
task_id = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-PROVENANCE-R2C-A-D0"
execution_row = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-PROVENANCE-R2C-A-D0"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
implementation_permission = false
guard_phase = "resolved_compatibility_provenance_r2c"
allowed_files = ["src/mir/builder/calls/function_call_brand_source_demand.rs", "src/mir/builder/calls/function_call_preflight_route.rs", "src/mir/builder/calls/function_call_preflight_route_tests.rs", "src/mir/builder/calls/function_call_script_compatibility_tests.rs", "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh", "tools/checks/guard_rows.toml", "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml", "docs/development/current/main/CURRENT_STATE.toml"]
decision = "Name only the remaining raw compatibility provenance at the existing FunctionCall authority boundary. Ledger-less raw ScriptRoot is one compatibility owner for Program/raw-AppMain ordinary calls; Main(locator) and RawLegacy get type-backed owners. All three preserve the existing Resolved terminal and issue no target."
source_authority = "NormalCallableSemanticPackageMode::Compatibility issues the raw ScriptRoot scope; RawInvocationRootLineageV1::Main identifies raw-root Main; RawLegacyChildLoweringPortV1 is the direct RawLegacy issuer. Existing semantic ScriptRoot provenance remains separate."
non_authority = "ledger absence alone, ScriptRoot spelling alone, NestedBoxMethod parent loss, name/arity, symbol parsing, variable_map, static/header scans, environment flags, and any compatibility terminal schedule."
fail_fast_boundary = "Classify the exact source/port provenance before drive_call_arguments_v1. Semantic ScriptRoot must win first; raw ScriptRoot requires both ledgers absent; Main and RawLegacy are type-backed. Missing/foreign/other lineage remains an explicit R2d unclassified-source blocker until it rejects before effects."
bounded_scope = "BoxShape only in function_call_brand_source_demand.rs and function_call_preflight_route.rs, with one sibling test module and the existing lifecycle guard. Do not change recursive port fields, source transport, build.rs, resolver/recovery, target/loan, dispatcher, Method, VM, or Call schema."
variants = "ScriptRootParkedCompatibility = semantic ledger + ScriptRoot (R2b); RawScriptRootParkedCompatibility = both-ledgers-absent ScriptRoot; RawRootMainParkedCompatibility = both-ledgers-absent Main(locator); RawLegacyParkedCompatibility = direct RawLegacy issuer."
required_changes = "Add the three private provenance variants, classify semantic ScriptRoot before the ledger early return, require both ledgers absent for raw ScriptRoot/Main, add focused provenance negatives, and reuse the existing lifecycle guard with one R2c phase."
acceptance = "Only the named raw compatibility sources receive the labels; semantic ScriptRoot, Cataloged, TopLevel, Constructor, Nested, and missing/foreign contexts are not misclassified. Existing brand/special/R2a behavior is unchanged; no target/Callee/loan, resolver rerun, or global Resolved-zero claim is introduced."
no_safe_slice = "Stop if raw provenance needs an ambient flag/recursive carrier, Main/RawLegacy needs name/arity inference or a new semantic receipt, or the row reaches nested inheritance, target/loan/dispatcher, late resolver deletion, Method/VM, or Call schema."
worker_inventory = "consumed (read-only current fixtures/cards/ledgers)"
implementation_commit = "52615fa9a2"
focused_checks = "cargo check --profile quick --lib; cargo check --profile quick --tests --lib; raw compatibility focused tests; R2c lifecycle guard; current-state pointer guard; git diff --check"
known_limit = "Full cargo test/link remains unrun because the large test binary exceeds the available memory budget."
closeout = "R2c landed as a provenance-only BoxShape: raw ScriptRoot, raw-root Main, and RawLegacy now have explicit compatibility owners. Generic unclassified fallthrough was the identified R2d blocker and is handled by the following bounded row; no target/loan/dispatcher/Resolved or recovery deletion was opened."
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

[r2d_unclassified_source_fail_closed_2026_08_29]
status = "landed"
task_id = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-UNCLASSIFIED-FAIL-CLOSED-R2D-D0"
execution_row = "MIR-CALL-D1B-RESOLVED-COMPATIBILITY-UNCLASSIFIED-FAIL-CLOSED-R2D-D0"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
implementation_permission = false
guard_phase = "resolved_compatibility_unclassified_r2d"
allowed_files = ["src/mir/builder/calls/function_call_brand_source_demand.rs", "src/mir/builder/calls/function_call_preflight_route.rs", "src/mir/builder/calls/function_call_preflight_route_tests.rs", "src/mir/builder/calls/function_call_script_compatibility_tests.rs", "src/mir/builder/recursive_child_lowering_rawport_tests.rs", "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh", "tools/checks/mir_call_d1b_exact_target_child_guard.sh", "tools/checks/brand_constructor_installed_callable_consumer_guard.sh", "tools/checks/guard_rows.toml", "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml", "docs/development/current/main/CURRENT_STATE.toml"]
decision = "Replace generic RelationlessCompatibility with UnclassifiedSource. Preserve TypeOp/Math/FastMem/str special precedence, but ordinary unclassified sources must return a typed pre-effect SourceRejected route; only semantic/raw ScriptRoot, raw Main, and RawLegacy named owners may reach Resolved."
source_authority = "Named provenance variants issued by semantic ScriptRoot, raw compatibility ScriptRoot, raw-root Main, or RawLegacy. No ledger absence, lineage spelling, nested parent, name, arity, or symbol may authorize compatibility."
non_authority = "Relationless fallback, NestedBoxMethod inheritance, Brand map probing for unclassified sources, variable/static/header lookup, env tail recovery, name Const, post-argument ValueId, and legacy Call."
fail_fast_boundary = "After special-form selection and before ordinary argument descent; unclassified ordinary input must have zero child/header/MIR/collector delta and no resolver/recovery entry."
smallest_next_slice = "R2d is landed; the next bounded design slice is R2E owner-level fate census for the four named compatibility origins, with no per-function review or new guard family."
acceptance = "Production RelationlessCompatibility caller count is zero; four named compatibility origins remain the only Resolved producers; unclassified ordinary rejection is pre-effect; special routes and AppMain/Cataloged/Constructor owners remain unchanged."
no_safe_slice = "Return to design_stop if nested raw compatibility is a required supported behavior without an existing exact carrier, or if source rejection cannot be separated from special-form selection without a dispatcher/port redesign."
implementation_commit = "80681a8cde"
focused_checks = "CARGO_BUILD_JOBS=4 cargo check --profile quick --tests --lib; cargo test --profile quick --lib unclassified -- (5 passed); R2d lifecycle, exact-target, brand, pointer, TOML, rustfmt, and diff checks passed"
closeout = "R2d landed: generic unclassified ordinary raw calls now terminate at private SourceRejected before argument/header/MIR effects; special TypeOp/Math/FastMem/str routes and four named compatibility owners remain unchanged. Full cargo test/link was not rerun because the large test binary exceeds the available memory budget. Named Resolved/recovery, target/loan/dispatcher, Method, VM, and Call schema remain outside this row."
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

[r2e_named_compatibility_fate_census_d0_2026_08_29]
status = "accepted_design"
task_id = "MIR-CALL-D1B-NAMED-COMPATIBILITY-FATE-CENSUS-R2E-D0"
execution_row = "MIR-CALL-D1B-NAMED-COMPATIBILITY-FATE-CENSUS-R2E-D0"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
implementation_permission = false
decision = "Keep ScriptRoot, RawScriptRoot, RawRootMain, and RawLegacy named compatibility owners as ParkedSealed. UnclassifiedSource remains the only generic pre-effect rejection; no named owner is silently changed to Reject or successor."
source_authority = "Each existing compatibility ingress is its own owner: semantic/raw ScriptRoot scope, raw-root Main scope, or RawLegacy child port. A common ordinary-call target issuer or App Main successor does not exist for these four owners."
non_authority = "name/arity, raw lineage spelling, variable/static/header scans, tail recovery, name Const, legacy Call, consumer-side resolver, and App Main loan generalization."
fail_fast_boundary = "UnclassifiedSource rejects after special-form selection and before argument descent; named compatibility keeps its existing terminal until an explicit behavior or backend decision is recorded."
census_boundary = "raw FunctionCall brand authority -> preflight -> shared Resolved consumer; includes exactly the four named origins and their production ingress/fixtures, excludes AppMain/Cataloged exact routes, Constructor, Method corridor, VM/backend, and Call schema."
smallest_next_slice = "MIR-CALL-D1B-R2-ONE-NAMED-OWNER-FATE-D0: decide the fate of exactly one named origin with its exact ingress, preserved behavior, successor/owner, and observable reopen trigger; do not start another census."
acceptance = "4/4 named owners are classified; generic UnclassifiedSource cannot reach Resolved; no target is synthesized from name/arity/symbol; no supported source behavior is dropped; no new loan, resolver, or guard family is created."
no_safe_slice = "Return to design_stop if any owner requires a new target issuer, source/raw carrier, or unapproved behavior drop; do not force a global Resolved deletion claim."
owner_census = [
    "ScriptRootParkedCompatibility: semantic ScriptRoot ingress at src/mir/builder/calls/function_call_brand_source_demand.rs:88-95, semantic root lifecycle at src/mir/builder/program_root_lowering.rs:212-225 and src/mir/builder/normal_default_root_catalog_lifecycle.rs:364-395; preserves the named Resolved terminal, has no ordinary-call target successor, and reopens only when a source-backed issuer or backend behavior decision exists.",
    "RawScriptRootParkedCompatibility: ledger-less raw ScriptRoot ingress at src/mir/builder/calls/function_call_brand_source_demand.rs:96-102, compatibility root at src/mir/builder/program_root_lowering.rs:243-257 and src/mir/builder/normal_default_root_catalog_lifecycle.rs:615-635; preserves the named Resolved terminal, has no App Main successor, and reopens only with an explicit raw compatibility owner decision.",
    "RawRootMainParkedCompatibility: raw-root Main ingress at src/mir/builder/calls/function_call_brand_source_demand.rs:103-108, raw child path src/mir/builder/raw_root_children.rs:280-294 -> src/mir/builder/module_invocation_brand0.rs:246-255 -> src/mir/builder/recursive_child_lowering.rs:539-568; preserves the named Resolved terminal and cannot borrow the App Main loan without a new exact relation.",
    "RawLegacyParkedCompatibility: RawLegacyChildLoweringPortV1 issuer at src/mir/builder/recursive_child_lowering/legacy_port.rs:56-62, production drivers at :148-170 and src/mir/builder/calls/lowering.rs:378-441; preserves the broad legacy facade and named Resolved terminal, with no common successor; reopens only through an owner-specific replacement.",
]
owner_census_status = "4/4 named origins classified as ParkedSealed; no function-level or guard-level expansion; the next bounded D0 chooses one owner fate and does not imply global Resolved retirement."
pre_implementation_gate = "I0 verifier regression repair is landed with filtered test evidence; before the next implementation row, keep the R2 lineage matrix and named compatibility owners explicit."
next_row = "MIR-CALL-D1B-R2-ONE-NAMED-OWNER-FATE-D0"

[i0_observation_verifier_regression_repair_d0_2026_08_29]
status = "landed"
task_id = "MIR-CALL-D1B-I0-OBSERVATION-VERIFIER-REGRESSION-REPAIR-D0"
execution_row = "MIR-CALL-D1B-I0-OBSERVATION-VERIFIER-REGRESSION-REPAIR-D0"
observer_execution_row = "MIR-CALL-D1B-SELECTED-FUNCTIONCALL-OBSERVATION-COMPLETION-D0"
parent_row = "MIR-CALL-D1B-CATALOGED-SOURCE-RELATION-AND-AFFINE-LOAN-I0"
observed_commit = "dc99360ecd"
trigger_commit = "99bdfc856c"
implementation_permission = false
implementation_commit = "e382a00923"
allowed_files = [
    "src/mir/resolved_semantics/direct_call_verifier.rs",
    "src/mir/resolved_semantics/tests.rs",
    "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
    "tools/checks/guard_rows.toml",
    "docs/development/current/main/investigations/mir-call-d1b-main-raw-cataloged-handoff-d0-2026-08-28.toml",
    "docs/development/current/main/CURRENT_STATE.toml",
]
decision = "Preserve the intentional observer-only facts shape. Do not rewrite the seven observation/package expectations to reject. The verifier must accept an empty target set with a non-empty self-consistent observation set, while still rejecting target-only and partial site coverage. Indexed co-seal remains bidirectional."
source_authority = "Direct-call canonicalization policy owns whether a forest is ObserveOnly or RequireCallableIndex; the observer records source-site facts, while the same-session indexed branch co-issues target rows. The package gate owns the later UnissuedDirectCallObservation rejection for an observer product that is not yet publishable."
canonical_issuer = "RequireCallableIndex co-issues target and observation rows in resolver_canonicalization.rs; ObserveOnly issues observations only. The verifier checks the data-shape invariant and must not manufacture a target or infer the policy from name/arity."
non_authority = "A test expectation, target-set emptiness alone as a target issuer, name/arity/symbol, AST rescan, package error collapse, resolver retry, fallback, or changing ObserveOnly into an implicit rejection."
fail_fast_boundary = "Reject target-only or partial target/observation coverage during function verification; allow ObserveOnly facts through this verifier so the package/source admission gate can emit its named unissued-observation terminal before install."
legal_shapes = [
    "T=empty and O=empty is the valid no-call shape.",
    "T=empty and O=non-empty is valid ObserveOnly facts-only state; observation argument cardinality and duplicate-site checks still run.",
    "T=O and both non-empty is the valid RequireCallableIndex co-seal state.",
    "T=non-empty with O=empty, or both non-empty with unequal sites, is SiteCoverageMismatch.",
]
reproduction = [
    "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib direct_call_observation -- --test-threads=1: 10 tests, 3 passed and 7 failed; every failure is DirectCall(SiteCoverageMismatch).",
    "The seven failures are selected_direct_call_observation_is_a_typed_package_terminal, unissued_direct_call_observation_rejects_package_before_install, nested_lambda_direct_call_observation_rejects_package_before_install, root_and_nested_direct_call_observations_share_one_package_gate, cataloged_static_direct_call_observation_rejects_before_install, cataloged_nested_lambda_direct_call_observation_rejects_before_install, and selected_callable_retains_direct_call_observation_without_issuing_target.",
    "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib normal_callable_transform -- --test-threads=1: 7 passed and 0 failed at this HEAD; no macro CompatibilityLoss regression is reproduced, so no expectation change is authorized without a specific failing test.",
]
smallest_next_slice = "In one fast corrective row, change only the existing direct-call verifier contract and its existing unit-test sibling: allow ObserveOnly facts, reject target-only/partial mismatch, preserve foreign/arity/duplicate checks, rename the contradictory observation-without-target test, and rerun the complete direct_call_observation filter before closeout."
allowed_scope = "src/mir/resolved_semantics/direct_call_verifier.rs, the existing direct-call verifier test module (or a small sibling if its 778-line boundary requires it), the active card, and CURRENT_STATE.toml; no package/loan/dispatcher/Call/VM/Method changes."
acceptance = [
    "The direct_call_observation focused command is green with all 10 tests; a zero-test filter is not evidence.",
    "ObserveOnly facts reach their intended source/package terminal; no target writer or resolver is added.",
    "Target-only and partial site coverage still return SiteCoverageMismatch; indexed co-seal remains exact T=O; empty/empty remains valid.",
    "Existing argument cardinality, duplicate argument-site, and foreign-compilation checks remain active.",
    "A touched test module is run through a filtered cargo test command and the exact command/result is recorded before the row can be marked landed; cargo check alone cannot close it.",
    "The current I0 row is not retroactively called fully green until the corrective test evidence exists; full cargo test/link remains a separate resource-limited known limit.",
]
macro_compatibility_status = "Current HEAD normal_callable_transform filter is green (7/7). Keep the two CompatibilityLoss expectations unchanged unless a named failing test is reproduced on the same HEAD."
row_exit_rule_amendment = "Prospective rule: any implementation row that touches test code must run a focused cargo test covering every touched test name/module, record the command and nonzero count in its card, and treat 0 tests or any failure as blocking. Filtered evidence is required even when cargo check passes; no second Cargo process may run concurrently."
filtered_test_evidence = [
    "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib direct_call_observation -- --test-threads=1: 10 passed, 0 failed, 7477 filtered out.",
    "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib normal_callable_transform -- --test-threads=1: 7 passed, 0 failed, 7480 filtered out.",
]
guards = [
    "bash tools/checks/current_state_pointer_guard.sh: passed",
    "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: phase=observer_i0_verifier_corrective ok",
    "bash -n tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: passed",
    "git diff --check: passed",
]
compile_checks = [
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --lib: passed; existing warnings only.",
    "CARGO_BUILD_JOBS=4 cargo check --profile quick --tests --lib -q: passed; existing warnings only.",
]
known_limit = "Full cargo test/link was not rerun because the repository's large test binary previously exceeded available memory; focused tests and test-target type checks are green."
closeout = "I0 verifier correction landed in e382a00923. ObserveOnly facts (target set empty, self-consistent observations) now pass verification; target-only and partial coverage still fail closed, indexed rows remain bidirectional, and no package/loan/dispatcher/Call/VM/Method authority changed."
historical_allowed_files_audit = "The 99bdfc856c I0 commit changed 40 paths while its declared allowed_files patterns matched 21; 19 paths were outside the declaration. Record this as retrospective scope evidence; do not rewrite the landed history or treat the existing guard's name-only check as proof of git-diff closure."
historical_undeclared_paths = [
    "src/mir/builder/calls/call_argument_descent.rs",
    "src/mir/builder/calls/function_call_brand_source_demand.rs",
    "src/mir/builder/calls/mod.rs",
    "src/mir/builder/normal_script_a/consumer.rs",
    "src/mir/builder/normal_script_semantic_source.rs",
    "src/mir/builder/normal_script_semantic_source_core.rs",
    "src/mir/builder/raw_expression_dispatch/mod.rs",
    "src/mir/builder/raw_structured_child_scope.rs",
    "src/mir/builder/recursive_child_lowering/legacy_port.rs",
    "src/mir/callable_semantic_batch/mod.rs",
    "src/mir/callable_semantic_batch/model.rs",
    "src/mir/compiler/function_input.rs",
    "src/mir/normal_callable_semantic_package/install/lowering_port.rs",
    "src/mir/normal_callable_semantic_package/mod.rs",
    "src/mir/resolved_semantics/direct_call_verifier.rs",
    "src/mir/resolved_semantics/mod.rs",
    "src/mir/resolved_semantics/resolver_canonicalization.rs",
    "src/mir/resolved_semantics/selected_callable_deferred.rs",
    "src/mir/resolved_semantics/tests.rs",
]
no_safe_slice = [
    "Do not update tests to reject ObserveOnly unless the language/semantic contract is explicitly changed first.",
    "Return to design_stop if the verifier must distinguish ObserveOnly from indexed data without an explicit policy or canonicalizer boundary; do not infer mode from an empty target set in a new authority.",
    "Return to design_stop if the fix needs a package/loan/dispatcher/Call schema change, a second traversal, a new receipt, or a compatibility behavior drop.",
]
next_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"

[r2_one_named_owner_fate_d0_2026_08_29]
status = "accepted_design_no_safe_slice"
task_id = "MIR-CALL-D1B-R2-ONE-NAMED-OWNER-FATE-D0"
execution_row = "MIR-CALL-D1B-R2-ONE-NAMED-OWNER-FATE-D0"
parent_row = "MIR-CALL-D1B-ALL-LINEAGE-PRE-EFFECT-RETIRE-R0"
implementation_permission = false
decision = "The four named compatibility origins remain ParkedSealed until one owner has an exact successor or an explicitly approved pre-effect retirement. Do not globally delete shared Resolved, recovery, name-Const, or legacy-Call edges while the other named owners remain live."
source_authority = "Existing named ingress owns the source fact: semantic ScriptRoot, raw ScriptRoot, raw-root Main, or RawLegacy. A selected owner may advance only from an exact source-backed relation and its existing terminal; no App Main loan, name/arity, symbol, or backend inference supplies a successor."
non_authority = "Shared Resolved helper counts, name/arity lookup, symbol parsing, static/header/tail recovery, consumer resolver, methodize/receiver repair, VM/backend routes, and Call-schema state are not owner decisions."
fail_fast_boundary = "For the one selected owner, decide its fate before argument descent: use an exact existing successor or an explicitly approved pre-effect terminal; otherwise keep that owner ParkedSealed and leave the other three unchanged."
census_boundary = "named ordinary FunctionCall ingress -> preflight -> shared Resolved terminal; includes exactly ScriptRoot, RawScriptRoot, RawRootMain, and RawLegacy; excludes AppMain/Cataloged exact loan, special forms, Method corridor, VM/backend, and Call schema."
owner_fate_matrix = [
    "ScriptRoot: ParkedSealed; semantic ScriptRoot owns the existing Resolved compatibility terminal and has no exact ordinary-call target successor.",
    "RawScriptRoot: ParkedSealed; ledger-less raw ScriptRoot preserves the existing compatibility root and has no exact App Main successor.",
    "RawRootMain: ParkedSealed; raw-root Main lacks the exact relation required to borrow the App Main loan.",
    "RawLegacy: ParkedSealed; the broad legacy child facade has no common successor, while independent subedges may retire separately with caller-zero evidence.",
]
selection_rule = "Choose one owner only when an exact successor or approved behavior decision is named from existing authority; otherwise close this D0 as an explicit park and move to the independent Method-corridor lane. Never invent a fifth provenance or infer a target from a shared helper."
allowed_scope = "Design stop permits only the active card, CURRENT_STATE.toml, and existing guard-contract text: record the one-owner fate and reopen trigger. No Rust, fixture, production switch, semantic receipt, new guard, or global Resolved deletion is allowed."
acceptance = "Exactly one named owner is selected for a fate or all four remain explicitly ParkedSealed; the remaining owners and shared helper stay unchanged; no global caller-zero claim is made; any future guard-phase amendment waits until an implementation row opens."
no_safe_slice = [
    "No exact source-backed successor or approved behavior decision exists for the selected owner.",
    "The proposed change needs a new loan/carrier/issuer, shared-helper deletion, Method/VM/Call-schema work, a new semantic receipt, or grep-only caller-zero evidence.",
]
next_row = "MIR-CALL-METHOD-CORRIDOR-R0"

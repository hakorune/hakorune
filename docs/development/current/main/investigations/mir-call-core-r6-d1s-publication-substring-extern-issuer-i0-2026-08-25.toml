title = "MIR Call core R6 D1S publication substring Extern issuer"
status = "fast"
observed_at = "2026-08-25"
observed_commit = "464b5a99d1"
implementation_permission = true
implementation_row = "MIR-CALL-CORE-R6-D1S-PUBLICATION-SUBSTRING-EXTERN-ISSUER-I0"
decision = "MIR-CALL-CORE-R6-D1S-PUBLICATION-SUBSTRING-EXTERN-ISSUER-I0"
owner = "MIR-CALL-RETIREMENT-v1"

[brief]
decision = "Replace only the PublicationSubstring direct Call literal with the thin canonical constructor, preserving composed start/end Add roots, helper operands, and the existing publication schedule."
source_authority_and_canonical_issuer = "PublicationHelperSubstringPlan owns outer destination, left/middle/right operands, outer/inner bounds, and effects; MirInstruction::call is the sole issuer for the exact SUBSTRING_CONCAT3_EXTERN route."
non_authority = "apply-side enum reconstruction, composed ValueIds, value metadata, legacy func, target-string lookup, later publication conversion, hint/DCE/CFG policy, and backend/PURE inference are not target authority."
fail_fast_boundary = "typed publication-helper substring shape, folded route proof, exact helper/copy chain, or value-origin proof failure yields no plan and zero Add/Call/hint publication or retry."
smallest_next_slice = "concat_corridor_apply.rs:433 PublicationSubstring writer only; selected writers 11 -> 10 and concat family 8 -> 7 after the single direct edge is removed."
non_claims = "No remaining concat writer, unified/array/canonicalizer, MaterializationStore, collector/plan schema, publication schedule, Method(None), Closure/Constructor, MirCall/CallFlags, Call schema, backend/wire, fallback, or cleanup change."

[boundary]
starts = "collected PublicationHelperSubstringPlan entries from the concat corridor collector"
ends = "existing composed-start/composed-end Add roots, Extern helper Call, hint/count, CFG, DCE, and folded metadata refresh"
includes = [
  "the one direct Call writer for ResolvedConcatCorridorPlan::PublicationSubstring",
  "exact SUBSTRING_CONCAT3_EXTERN mapping, outer destination, five ordered operands, and plan-owned effects",
  "outer/inner bound composition, spans, helper placement, and existing publication hint/count behavior",
]
excludes = [
  "remaining concat, unified, array, canonicalizer, and publication-host writers",
  "collector/plan schema, route selection, DCE/schedule, and composed-value allocation",
  "Call operand consumers, Method(None), Closure/Constructor, mandatory-Callee schema, MirCall/CallFlags",
  "JSON, VM, native backend, typed bridge errors, metadata split, inverse imports, and cleanup lanes",
]

[authority]
source = "PublicationHelperSubstringPlan.outer_dst, left/middle/right, outer_start, inner_start, inner_end, and effects"
issuer = "MirInstruction::call(Some(*outer_dst), Callee::Extern(SUBSTRING_CONCAT3_EXTERN.to_string()), vec![*left, *middle, *right, *composed_start, *composed_end], *effects)"
non_authority = [
  "apply_concat_corridor_plans target/effect recomputation",
  "composed ValueId allocation, metadata value types, and Add-root inspection",
  "legacy func, target Const, backend lookup, PURE defaults, and retry",
  "hints, counts, spans, CFG, DCE, and folded metadata",
]

[finite_matrix]
positive = [
  "existing publication-helper substring fixture preserves exact Extern, outer dst, left/middle/right order, composed start/end operands, PURE effects, and Add-root parity",
  "existing route selection, helper/copy removal, spans, hint/count, CFG, DCE, and folded metadata remain unchanged",
]
negative = [
  "missing/foreign helper, wrong method/arity, missing Callee, broken copy chain, or failed value-origin proof -> no plan and no Add/Call/hint publication",
  "unsupported representation or absent folded route proof -> typed non-selection with no target retry or late inference",
]
parity = [
  "PublicationHelperSubstringPlan remains the single target/effects authority; only the physical issuer changes",
  "D1Q Return, D1R MaterializationStore, remaining concat families, publication host/write boundaries, and pre/post-DCE schedule remain unchanged",
]

[delete_set]
file = "src/mir/passes/string_corridor_sink/concat_corridor_apply.rs"
lines = "433-441"
remove = [
  "one direct transitional MirInstruction::Call literal",
  "func: ValueId::INVALID",
  "callee: Some(Callee::Extern(...)) wrapper",
]
replace_with = "MirInstruction::call(Some(*outer_dst), Callee::Extern(SUBSTRING_CONCAT3_EXTERN.to_string()), vec![*left, *middle, *right, *composed_start, *composed_end], *effects)"

[acceptance]
focused_tests = [
  "rewrites_publication_helper_substring_via_plan_metadata: exact Extern/dst/five-argument order/composed Add roots/PURE effects parity",
]
static_evidence = [
  "collector branches already reject unsupported helper, copy-chain, route-proof, and value-origin states before plan issue",
  "the shared corridor guard must check the PublicationSubstring writer window and forbid direct transitional fields; no fallback/retry evidence is accepted",
]
guards = [
  "mir_call_canonical_corridor_guard",
  "current_state_pointer_guard",
  "rustfmt on touched Rust and git diff --check",
]
source_limits = "concat_corridor_apply.rs remains below 760/800; focused publication-substring test remains below 760/800; corridor guard remains exactly 777 lines; active card remains exactly 1000 lines"

[worker_review]
selection = "/root/d2c_consumer_selection_audit: fresh post-D1R census selected this one existing PublicationSubstring writer; selected writers 11, concat family 8"
implementation_permission = "fast I0 is permitted only for this one plan-owned writer; all schema/producer/consumer and broader cleanup remains closed"

[stop_if]
conditions = [
  "PublicationSubstringPlan fields must be recomputed, collector/schema changes, or publication semantics change",
  "any other concat, unified, array, canonicalizer, schema, Method(None), Closure/Constructor, metadata, backend, bridge, or cleanup row enters the diff",
  "guard cannot remain at 777 or any touched source reaches 760/800 without a split design",
  "negative state produces any plan, Add, Call, hint, count, retry, or late target inference",
]

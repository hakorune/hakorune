title = "MIR interpreter unreachable externals handler retirement"
status = "accepted_design_no_safe_slice"
observed_commit = "183c1418d7"
observed_at = "2026-08-26"
implementation_permission = false
parent_row = "MIR-CALL-CORE-R6-D1B-ROW-B-PRODUCER-CLOSURE-D0"
evidence_child = "docs/development/current/main/investigations/mir-interpreter-externals-evidence-owner-d0-2026-08-27.toml"

[boundary]
starts = "src/backend/mir_interpreter/handlers/mod.rs:mod externals and handlers/externals.rs::handle_extern_call"
ends = "live-provider preservation, failure/outcome evidence ownership, generated manifests, and scoped reference documentation"
includes = [
  "caller census for handle_extern_call, the externals module declaration, and qualified module references",
  "the unreachable handlers/externals.rs module and its stale provider ownership comment",
  "the live calls/externs.rs -> extern_provider_dispatch route and its callers as preserved evidence",
  "failure-outcome generator/manifest regeneration required by the source deletion",
  "current/reference documentation that claims handlers/externals.rs owns extern dispatch",
]
excludes = [
  "calls/externs.rs execution semantics and extern_provider/* provider implementations",
  "MirInterpreter repl_session/set_repl_session state and the live REPL route",
  "new Rust VM feature/parity gates, selected native activation, JSON/MIR ingress, and Call schema cutover",
  "provider failure semantics, VMValue carrier redesign, or semantic activation of the failure/outcome inventories",
  "archive-only historical prose unless the selected guard requires a current reference correction",
  "warning/panic/allow cleanup outside the dead-route and evidence boundary",
]

[brief]
decision = "Retire only the unreachable handlers/externals.rs route after a typed evidence-owner handoff; keep calls/externs.rs and extern_provider_dispatch as the sole live extern route."
source_authority = "MirInstruction::Call Callee::Extern -> handlers/calls/externs.rs::execute_extern_function -> handlers/extern_provider::extern_provider_dispatch"
canonical_issuer = "The existing calls/externs.rs execution route; this row does not issue a new semantic or runtime receipt."
non_authority = [
  "handlers/externals.rs and handle_extern_call, stale comments, archive prose, and generated source-path rows",
  "the module name alone, provider registry presence, or REPL state as evidence of a live caller",
]
fail_fast = "If a non-document caller of handle_extern_call or handlers::externals appears, or if the provider_status evidence owner is not closed before deletion, stop without removing the module."
smallest_next_slice = "First close the evidence-owner child; only then run the one-file dead-route deletion plus mod declaration/doc/manifest synchronization."
non_claims = [
  "all extern/provider failure outcomes are repaired",
  "provider missing-result fallback is semantically changed",
  "Rust interpreter becomes a selected production backend",
  "Call { func, callee: Option<Callee> } is cut over",
]

[census]
handle_extern_call_definition = 1
non_document_callers = 0
module_declaration = "src/backend/mir_interpreter/handlers/mod.rs:31"
live_route = "src/backend/mir_interpreter/handlers/calls/externs.rs::execute_extern_function -> extern_provider_dispatch"
repl_state = "retained; MirInterpreter::repl_session and set_repl_session have independent live ownership"
stale_references = [
  "src/backend/mir_interpreter/handlers/extern_provider/mod.rs provider ownership comment",
  "docs/ENV_VARS.md",
  "docs/reference/language/repl.md",
  "docs/development/current/main/design/repl-mir-interpreter-interactive-session-ssot.md",
]

[evidence_handoff]
status = "CutoverBlockerOpen"
owner_gap = "tools/docs/failure_outcome_runtime_provider_inventory.py currently recognizes provider_status only through path_filter=backend/.*externals.rs$ and its six-family validator rejects an empty provider_status family."
secondary_gap = "failure_outcome_projection_binding.py, failure_outcome_site_inventory.py, semantic-site graph, control-flow inventory, conflict ledger, and exhaustiveness manifests also contain source-path evidence for the dead file."
required_decision = "Before code deletion, choose one explicit non-semantic evidence owner for provider_status after the old file disappears: a live calls/externs.rs/provider boundary token, or a separate retained historical-evidence source. Do not silently drop the family, widen a regex without a contract, or infer provider meaning from VMValue::Void."
candidate_live_owner = "calls/externs.rs provider dispatch boundary, only if a deterministic evidence token and pending ProviderContractMissing classification are specified and tested."
recommended_owner_candidate = "calls/externs.rs:59 execute_extern_function -> extern_provider_dispatch"
recommended_evidence_token = "provider_dispatch_boundary"
recommended_expected_cardinality = "fixed non-zero count selected by the evidence generator; do not hardcode a count before the generator contract is amended"
activation = 0

[review_2026_08_27]
decision = "The 391-line dead route is a high-value cleanup candidate, but it is not an immediately executable fast row. Caller-zero is strong for code; evidence ownership is not closed."
order = [
  "D0 evidence-owner handoff: choose and test one non-semantic provider_status owner while preserving semantic_activation=0",
  "I0 dead-route deletion: remove mod externals and handlers/externals.rs, then regenerate every source-derived manifest with its owning writer",
]
lane_relation = "Keep this cleanup queued beside the active MIR-CALL-D1B relationless design stop; do not switch CURRENT_STATE or delete code while the evidence child is CutoverBlockerOpen."
reason = "The generated inventory currently filters provider_status by the dead path and rejects an empty family; deleting the file first would turn a proven caller-zero observation into an unowned evidence gap."

[review_2026_08_26_local_reconciliation]
feedback_candidate = "Delete the 391-line handler immediately after the D1B child."
decision = "Keep queued, but do not treat it as a fast row yet. The evidence-owner child must close first, and the active CURRENT_STATE root-lineage design stop still precedes switching to this cleanup lane."
confirmed = [
  "handle_extern_call and mod externals remain non-document caller-zero at observed_commit",
  "calls/externs.rs -> extern_provider_dispatch remains the live route",
  "provider_status ownership is still unresolved because the generated family is keyed to the dead path",
]
rejected_shortcut = "Do not delete the module and then repair generated manifests by dropping provider_status, widening a path filter without a contract, or inferring failure meaning from VMValue::Void."

[baseline_evidence]
observed_at = "2026-08-26"
classification = "known_baseline_debt; not caused by this task's uncommitted documentation"
checks = [
  "failure_outcome_runtime_provider_inventory.py --check: drift",
  "failure_outcome_site_inventory.py --check --strict: drift",
  "failure_outcome_semantic_site_graph.py --check: drift",
  "failure_outcome_exhaustiveness.py --check: pass",
  "runtime-provider inventory unit tests: 4 passed",
]
policy = "Do not claim a green closeout from the current baseline. The selected cleanup row must either reconcile these generated-manifest drifts through their owning writers or record a separately accepted baseline classification before deleting source evidence."

[delete_set]
code = [
  "src/backend/mir_interpreter/handlers/mod.rs: remove only mod externals;",
  "src/backend/mir_interpreter/handlers/externals.rs: delete the unreachable module after evidence handoff",
]
docs = [
  "replace current claims that both calls.rs and externals.rs dispatch providers",
  "update current/reference REPL or environment docs only where they name the dead owner",
]
generated = [
  "regenerate failure_outcome_site_inventory_v0.json and semantic/control-flow/projection/conflict/exhaustiveness manifests with their owning generators",
  "regenerate failure_outcome_runtime_provider_inventory_v0.json only after provider_status has a non-empty, explicit owner",
]
preserve = [
  "handlers/calls/externs.rs::execute_extern_function",
  "handlers/extern_provider/mod.rs::extern_provider_dispatch and all provider submodules",
  "MirInterpreter repl_session, set_repl_session, and live REPL behavior",
]

[finite_state]
states = [
  "DeadRouteCallerZero",
  "LiveProviderRouteRetained",
  "EvidenceOwnerDesignChildOpen",
  "EvidenceOwnerClosed",
  "GeneratedEvidenceSynchronized",
  "CurrentReferenceSynchronized",
  "BlockedUnknownCaller",
  "BlockedProviderStatusOwner",
]
terminal_policy = "Only DeadRouteCallerZero, LiveProviderRouteRetained, EvidenceOwnerClosed, GeneratedEvidenceSynchronized, and CurrentReferenceSynchronized together permit the fast delete row; EvidenceOwnerDesignChildOpen and either blocked state remain NoSafeSlice and leave code untouched."

[acceptance]
positive = [
  "non-document definition/reference count for handle_extern_call is zero after the row",
  "mod externals is zero and calls/externs.rs still routes Callee::Extern through extern_provider_dispatch",
  "provider_status remains a deterministic family with explicit pending classification and semantic_activation=0",
  "all source-derived failure/outcome manifests are regenerated by their scripts and pass --check",
  "current/reference docs no longer claim handlers/externals.rs is a live owner",
]
negative = [
  "no fallback from the removed handler to a second provider route",
  "no removal or rewrite of extern_provider_dispatch, provider modules, or REPL state",
  "no empty provider_status family accepted by weakening the validator",
  "no hand-edited generated JSON and no inferred semantic owner from a carrier value",
]
parity = "The live Callee::Extern execution route, provider dispatch outcomes, and REPL state are unchanged; this is a dead-route/reference cleanup only."
focused_checks = [
  "python3 tools/docs/failure_outcome_runtime_provider_inventory.py --check",
  "python3 -m unittest tools/docs/test_failure_outcome_runtime_provider_inventory.py",
  "python3 tools/docs/failure_outcome_exhaustiveness.py --check",
  "bash tools/checks/current_state_pointer_guard.sh",
]
guard = "tools/checks/mir_interpreter_externals_retire_guard.sh: scoped checks for dead module/handler zero, live provider route presence, explicit provider_status evidence owner, and generated-manifest checks"

[reopen]
triggers = [
  "any non-document handle_extern_call caller or externals module import appears",
  "provider_status rows become empty or their source authority changes without a task amendment",
  "a change is proposed to calls/externs.rs, extern_provider/*, REPL state, backend activation, or Call schema",
  "a generated manifest is edited manually or cannot be reproduced by its owning generator",
]

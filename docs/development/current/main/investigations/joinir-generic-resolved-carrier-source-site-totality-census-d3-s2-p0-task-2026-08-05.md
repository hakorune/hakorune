Status: selected static census; production remains closed
Date: 2026-08-05
Parent: joinir-generic-resolved-carrier-typed-provenance-handoff-d3-s2-d0-design-2026-08-05.md
Task: `JOINIR-GENERIC-RESOLVED-CARRIER-SOURCE-SITE-TOTALITY-CENSUS0-D3-S2-P0`
Ceremony: T1 source-authority census

# Change

Inventory every Generic V0/V1 facts field and producer arm against the existing
resolver-owned outer/inner sites, owner brand, `BindingRefV1` role rows, and
strict-ancestor relation. Classify each field as source-site-backed,
name-only, AST-bearing, derived structural evidence, or unresolved. Record
overlap with NestedPredicate, DirectAccum, and the retained A+ boundary.

# Contract

This row creates no source identity and no production type. Resolver products
remain the sole source authority. `LoopBindingKeyV1` is issued only by a Recipe
producer, never by resolver, neutral facts, Binding SSA, or this census.
Independent preflight seed and invocation-seal products are superseded; a
future single non-`Clone` canonical plan owns invocation pairing. P0 contains
no Return/outer-PHI, winner, debt, Home, selector, Builder, MIR, or runtime
meaning.

# Done

One machine-checkable matrix covers all facts fields and producer arms, exact
source forms, outer/inner/role coverage, and missing/foreign/ambiguous/
duplicate/incomplete stops. It names the first source-projection closeout
candidate without minting a second issuer. Static census remains bounded,
source/check files stay below 800 lines, and pointer/diff guards are green.
Reference documentation is updated with any implementation cell in the same
commit.

# Stop

Return to D3-S2 design if the census needs AST rewriting, loose product
pairing, name/label/`ValueId` inference, a resolver/neutral Recipe key, a
second schedule owner, synthetic debt, fallback/retry, or any production
caller. The follow-up P1 must reuse and close existing projector/source-bridge
evidence; it may not introduce a new resolver or neutral source issuer.

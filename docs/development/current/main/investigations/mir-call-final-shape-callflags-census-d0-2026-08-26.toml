title = "MIR Call final-shape CallFlags retirement census"
status = "accepted_design_no_safe_slice"
observed_commit = "3c306948d4"
observed_at = "2026-08-26"
implementation_permission = false
queued_fast_candidate = "MIR-CALL-R6-CALLFLAGS-RETIRE-I0: CallFlags-only delete/re-export/fixture closure while MirCall transport remains live; do not select until the current direct-target design stop closes."
parent_row = "docs/development/current/main/investigations/mir-call-final-shape-and-ingress-boundary-design-2026-08-25.md"

[boundary]
starts = "MirCall/CallFlags definitions and create_mir_call() construction"
ends = "the existing unified physical terminal that converts MirCall into MirInstruction::Call"
includes = [
  "crates/hakorune_mir_defs/src/call_unified.rs MirCall/CallFlags definitions and constructors",
  "src/mir/builder/calls/call_unified.rs create_mir_call/create_call_flags",
  "src/mir/builder/calls/unified_emitter.rs and unified_emitter/physical_terminal.rs transport consumers",
  "crate re-exports and Rust test/reference construction sites",
  "apps/rust-subset-to-hako expected .hako/.json projections and smoke consumer",
  "tools/rust_lifecycle transport inventories that classify the types",
  "all downstream reads of MirCall.flags or CallFlags semantics",
]
excludes = [
  "MirInstruction::Call schema cutover",
  "Method(None) producer closure or receiver non-optionalization",
  "JSON-v0/v1 ingress, VM/native/backend behavior, and warning cleanup",
  "deleting the live MirCall transport before its physical terminal is replaced",
]

[brief]
decision = "Split the proposal: MirCall is a live transport and cannot be retired by a zero-text-reader claim; CallFlags is only a retirement candidate until its semantic readers, public exports, and construction sites are exhaustively classified. No code changes while the active root-lineage design stop remains selected."
source_authority = "Callee and explicit EffectMask already carried by the canonical Call path; no flag-derived target or effect may be invented by this census."
canonical_issuer = "existing MirInstruction::call() at the unified physical terminal; this child does not issue a new receipt or replace the terminal."
non_authority = [
  "CallFlags::constructor/default classification by itself",
  "the existence of a public re-export",
  "comments claiming a unified call replacement",
  "MirCall transport shape as proof that flags have meaning",
]
fail_fast = "If any downstream reader, public contract, fixture, or external crate relies on CallFlags semantics, stop and name that owner; do not delete, default, or silently drop the field. MirCall remains until the physical terminal has an explicit replacement row."
smallest_next_slice = "Design-only feature/public/projection matrix for the CallFlags retirement candidate; classify external obligations and name a retained owner or exact co-update/delete set, without editing code or MirCall transport."
non_claims = [
  "MirCall is caller-zero (it is not: physical_terminal consumes it)",
  "CallFlags has no semantic reader merely because rg finds no .flags access",
  "the final Call schema is already cut over in source",
  "constructor/closure policy, backend parity, or JSON compatibility is changed",
]

[observed]
live_mircall_transport = [
  "src/mir/builder/calls/unified_emitter/physical_terminal.rs::emit_finalized_generic_call_v1 accepts MirCall",
  "src/mir/builder/calls/unified_emitter.rs constructs MirCall through call_unified::create_mir_call",
]
callflags_construction = [
  "src/mir/builder/calls/call_unified.rs::create_call_flags and create_mir_call",
  "crates/hakorune_mir_defs/src/call_unified.rs::MirCall constructors and CallFlags constructors",
]
callflags_read_status = "No downstream .flags semantic read was observed in the bounded Rust production census; writers and public methods still exist. Public re-exports, generated Rust-subset projections, and parked Python/llvmlite readers remain external/API obligations to classify, so caller-zero is not claimed. This is an observation to verify, not a deletion authorization."
public_surface = [
  "crates/hakorune_mir_defs/src/lib.rs re-exports CallFlags, Callee, and MirCall",
  "src/mir/definitions/mod.rs re-exports CallFlags and MirCall",
]
api_and_projection_rows = [
  "CallFlags::{new,constructor,no_return,with_tail_call,with_inline,Default} are public constructors/methods requiring an API disposition",
  "MirCall::new, constructor/closure flag assignments, and MirCall::with_tail_call are distinct writers",
  "apps/rust-subset-to-hako/examples/hakorune_mir_defs_call_unified_expected.hako and modules/0000.json are generated projection fixtures",
  "apps/rust-subset-to-hako/smoke.sh:299 consumes the expected projection; it is not a CallFlags semantic owner",
  "tools/rust_lifecycle/mirbuilder_domain_object_id_transport_policy_inventory.py and mirbuilder_carrier_type_transport_unclassified_evidence_resolution.py classify the type names",
]
external_disposition = "MirCall remains live through unified_emitter/physical_terminal.rs; CallFlags-only deletion is a later candidate only if the public re-export, generated .hako/.json projections, and parked Python/llvmlite readers are explicitly co-updated or retained with named owners."

[current_head_reaudit]
observed_commit = "6f88c16a91"
status = "design_stop"
decision = "Retain MirCall as the live by-value transport and retire Rust CallFlags only as one co-update; do not touch Hako wire flags.optionality or generic mir_call JSON readers."
current_head_census = "At HEAD 25d4235a49 (2026-08-28), bounded Rust source contains CallFlags in 4 files/19 occurrences: crates/hakorune_mir_defs/src/call_unified.rs, crates/hakorune_mir_defs/src/lib.rs, src/mir/definitions/mod.rs, and src/mir/builder/calls/call_unified.rs. The adjacent generated RustSubset fixtures and lifecycle classifiers add 4 files/22 occurrences (8 files/41 occurrences across the explicitly bounded Rust plus fixture/tool surface); docs are excluded."
classified_edges = [
  "Retire + co-update: hakorune_mir_defs CallFlags definition/API, MirCall.flags, constructor/closure flag writers, builder create_call_flags, crate/definitions re-exports",
  "Retain: src/mir/builder/calls/unified_emitter/physical_terminal.rs is the sole MirCall -> MirInstruction::call terminal",
  "Co-update: RustSubset expected .hako/.json projections and the fourteen lifecycle inventory fixtures; regenerate rather than hand-edit",
  "Retain out-of-boundary: Hako flags.optionality, mir_json_emit flags envelope, Python/llvmlite JSON readers, VM/reference fixtures",
]
external_blocker = "hakorune-mir-defs is a public rlib and local grep cannot prove that workspace-external users are absent. The existing D1-A policy therefore remains the safe default: keep the public surface ParkedSealed with a named reopen trigger; a breaking deletion is not selected in this lane."
completion = "Not CensusExhausted: the Rust edge matrix and retained public disposition are explicit, but the co-update set and one registered reusable guard remain open."
next_action = "Keep the public MirCall/CallFlags surface ParkedSealed, record the co-update/delete boundary and reusable guard in the selected parent row, then select MIR-CALL-R6-CALLFLAGS-RETIRE-I0 only as a later writer-only candidate."

[followup_audit_2026_08_26]
status = "design_stop_no_safe_slice"
reviewers = [
  "h_owner_bijection_audit",
  "h_session_witness_design",
  "i_callee_issuance_adversary",
]
observed_commit = "3c306948d4"
verdict = "MirCallLiveTransport; Rust CallFlags has no bounded production semantic reader, but public API, generated projections, feature routes, parked Python/llvmlite, and Hako wire flags keep retirement unselected."
physical_chain = "call_unified::create_mir_call -> unified_emitter::physical_terminal::emit_finalized_generic_call_v1 -> MirInstruction::call(dst, callee, args, effects); flags are dropped at the terminal."
reader_boundary = "Rust optimizer/backend/physical consumers read dst/callee/args/effects only; Hako JSON flags.optionality is real VM semantics and is a separate wire authority, so the JSON flags envelope cannot be removed in this row."
public_obligations = [
  "crates/hakorune_mir_defs crate-root and src/mir/definitions facades publicly re-export CallFlags/MirCall",
  "RustSubset expected .hako/.json projections and smoke consumer encode the public schema",
  "Python/llvmlite and feature-gated routes remain external or parked obligations until a matrix is explicit",
  "Hako mir_call/mir_call_map_ops consume flags.optionality; do not conflate it with Rust CallFlags",
]
deferred_task = "MIR-CALL-R6-CALLFLAGS-RETIRE-D0: design-only feature/public/projection matrix; classify every external obligation, name retained owners or an exact co-update/delete set, and keep MirCall transport unchanged."
implementation_gate = "false: current D1B Cataloged qualified-static-payload design stop is independent and remains selected; no CallFlags code, fixture, wire, or schema edit is authorized."
safe_shape = "If the matrix reaches CensusExhausted, a later fast row may remove only writer-only Rust CallFlags construction/field/re-export edges while preserving MirCall by-value transport and the single physical terminal. Do not add a replacement schema or private bundle unless a separate selected row requires it."
no_safe_slice = [
  "workspace caller-zero is not sufficient while public or external consumers are unresolved",
  "empty JSON flags and Hako optionality must not be silently dropped or renamed here",
  "MirCall cannot be retired until the physical terminal has a separately accepted replacement",
]

[finite_state]
states = [
  "MirCallLiveTransport",
  "CallFlagsConstructedOnly",
  "CallFlagsSemanticReader",
  "CallFlagsPublicApiObligation",
  "GeneratedProjectionFixture",
  "HakoWireFlagsSemanticReader",
  "FeatureMatrixObligation",
  "ExternalSemanticReader",
  "CensusExhausted",
  "CallFlagsRetirementCandidate",
  "RetainWithNamedOwner",
  "NoSafeSlice",
]
terminal_policy = "Only CensusExhausted may select the next row. CallFlagsRetirementCandidate permits a later fast deletion of CallFlags-only construction/re-export edges only after the public API, generated-projection, and feature-matrix dispositions are explicit; GeneratedProjectionFixture is co-updated at deletion and is not a semantic owner; HakoWireFlagsSemanticReader, ExternalSemanticReader, CallFlagsSemanticReader, FeatureMatrixObligation, or unresolved CallFlagsPublicApiObligation keeps the row NoSafeSlice. MirCallLiveTransport remains open until a separate physical-terminal replacement."

[acceptance]
design = [
  "every MirCall and CallFlags definition, constructor, field read, public method, re-export, Rust/app fixture, smoke consumer, tool classifier, and external-crate reference is classified",
  "MirCall's live physical-terminal consumer is recorded as preserved, not caller-zero",
  "CallFlags-only construction is distinguished from semantic reads and public API obligations",
  "generated .hako/.json projections are marked co-update obligations, not semantic readers",
  "Hako flags.optionality is classified as a separate wire semantic owner and is not conflated with Rust CallFlags",
  "feature-gated and external/parked routes have an explicit matrix disposition before any API deletion",
  "if no CallFlags reader exists, the exact delete set and one focused guard are named without changing MirCall transport",
  "if a reader exists, its authority and a separate bounded owner row are named",
]
negative = [
  "no MirCall deletion in this child",
  "no default/sentinel CallFlags substitution",
  "no schema, receiver, Method(None), JSON, backend, or VM change",
  "no code or fixture edits while work_mode=design_stop",
]
focused_checks = [
  "rg -n --glob '*.{rs,hako,json,sh,py}' '\\bMirCall\\b|\\bCallFlags\\b|MirCall\\.flags|with_tail_call' src crates apps tools",
  "cargo metadata --no-deps --format-version 1 (only if a later selected card permits a build gate)",
  "bash tools/checks/current_state_pointer_guard.sh",
]
implementation_gate = "false until the bounded census is Exhausted and CURRENT_STATE selects a fast child; this task itself is design-only."
guard = "a future scoped guard must reject new CallFlags semantic reads/construction outside the selected owner and must assert MirCall remains owned by the existing physical terminal until the schema replacement row lands"
guard_candidate = "tools/checks/mir_call_r6_mircall_shape_placement_guard.sh: baseline keeps one crate schema owner, one MirCall production edge, one physical MirInstruction::call terminal, and EffectsAnalyzer-only effect authority; cutover may set create_mir_call/create_call_flags/Builder effect shim edges to zero without moving the schema."
delete_boundary = "later CallFlags-only row may delete writer-only Rust constructors, field/re-export edges, and co-updated RustSubset projections only after public/feature/external disposition; it may not delete MirCall transport, JSON flags envelope, Hako optionality, transitional Call schema, Method(None), or backend/VM behavior."

[reopen]
triggers = [
  "a new MirCall or CallFlags reader/re-export appears",
  "the unified physical terminal stops consuming MirCall or gains a second consumer",
  "CallFlags begins carrying an effect/target decision not represented by canonical Call",
  "a request attempts to delete MirCall transport together with the CallFlags candidate",
]

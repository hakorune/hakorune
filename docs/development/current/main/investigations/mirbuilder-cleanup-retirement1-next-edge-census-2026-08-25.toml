title = "MirBuilder cleanup retirement R1 next-edge census"
status = "accepted_design"
observed_at = "2026-08-26"
observed_commit = "f659d3f941"
implementation_permission = false
decision = "MIRBUILDER-CLEANUP-RETIREMENT1-NEXT-EDGE-CENSUS-D0"
owner = "MIRBUILDER-FINAL-PIPELINE-v1"
followup_census = "docs/development/current/main/investigations/mir-semantic-error-type-compression-p1b-followup-census-d0-2026-08-26.toml"

[brief]
decision = "Separate structural seam documentation, typed-error compression, and metadata owner splitting into independent bounded rows; do not turn a broad warning/runtime census into blanket edits."
source_authority_and_canonical_issuer = "Existing compiler issuers, typed issue owners, and FunctionMetadata storage/slot owners remain authorities; this census issues no new semantic product."
non_authority = "Import counts, String formatting, line counts, panic/unwrap totals, dead_code attributes, tests, fixtures, and local green are observations only."
fail_fast_boundary = "Each row must name its finite owner/consumer boundary and reject migration when a neutral owner, typed port, caller, or semantic preservation proof is missing."
smallest_next_slice = "After the active D1N design stop, close the compiler-builder seam census first; then choose either the exact metadata owner split or the isolated RequiredArgumentProof typed row."
non_claims = "No broad warning cleanup, blanket panic removal, compiler/Builder move, common recursive-port redesign, metadata field change, backend activation, or Call schema change is authorized by this census."

[boundary]
starts = "MirBuilder/Compiler structural and error/metadata owner observations at HEAD a0b319a2df"
ends = "one named bounded cleanup row with owner, delete/move set, finite acceptance, and non-claims"
includes = [
  "builder <-> compiler seam counts and the loop_structural_facts neutral-owner candidate",
  "ScriptDirectStaticPhysicalBridgeErrorV1 String variant boundaries",
  "FunctionMetadata post-split storage/checked-callout/linear-slot owner census",
  "selected-scope runtime hygiene census as information only",
]
excludes = [
  "active MIR Call D1N operand-SSOT implementation",
  "Method(None), bare FunctionCall, mandatory-Callee, MirCall/CallFlags, and writer retirement",
  "PyVM/reference/Python/non-selected backend surfaces",
  "automatic cargo fix, warning suppression, blanket unwrap/panic removal, and fixture churn",
]

[observed]
builder_rs_lines = 737
metadata_rs_lines = 718
metadata_hard_stop = "FunctionMetadata owner split landed before this observation at 5b36855ae6; metadata.rs is below the 760-line design threshold and no second split is selected."
reverse_imports = "HEAD census: builder -> compiler direct use 343 lines/148 files including tests (production estimate 224 lines/99 files); compiler -> builder direct use 138 lines/59 files including tests (production estimate 129 lines/55 files); no semantic-new-edge claim is made until the seam ledger is refreshed."
bridge_string_debt = "RequiredArgumentProof(String) and ClaimCompletion(String) lose typed issues; ArgumentDescent(String) is a common recursive port boundary with no bounded typed issue owner"
runtime_hygiene = "finite lexical boundary src/mir/builder.rs + src/mir/builder/**/*.rs: panic 358, unwrap 2798, expect 2976, todo 0, allow(dead_code) 95, allow(unused_imports) 45; test/fixture/debug-only paths remain mixed and selected production count is unclassified until a module-registration-aware census"

[[candidate]]
id = "MIRBUILDER-COMPILER-SEAM-CENSUS-D0"
status = "accepted_design"
kind = "docs_only"
scope = "src/mir/compiler/** <-> src/mir/builder/** <-> src/mir/loop_recipe_contract/**"
source_authority = "compiler resolver/cohort issuers; Builder is the physical/session consumer; loop_structural_facts is the candidate neutral owner for the true-break/continue shape"
canonical_issuer = "none; record existing issuers and owners without moving or reissuing a product"
old_edge = "existing builder -> compiler imports and compiler projections are structurally inverted in places; the current HEAD did not add a semantic new edge"
smallest_slice = "add one seam ledger section to the durable in-place replacement policy with counts, owner map, ExistingApproved/FormattingOnly/NewReverseEdge/OwnerUnresolved states, and a no-new-import/re-export/receipt guard"
acceptance = [
  "semantic new reverse edge at HEAD is recorded as 0",
  "PreparedGenericG0PhysicalEmitterAdmissionV1 remains compiler-issued and Builder-consumed",
  "loop_structural_facts is recorded as a candidate neutral owner only; Generic admission remains OwnerUnresolved",
  "no source move, production switch, new Verified*/Prepared* receipt, or Cargo claim",
]
non_claims = ["no import migration", "no loop contract relocation", "no builder/compiler module split", "no test-only edge deletion"]

[[candidate]]
id = "FUNCTION-METADATA-OWNER-SPLIT-R0"
status = "landed"
kind = "BoxShape_behavior_neutral_refactor"
landed_commit = "5b36855ae6"
scope = "src/mir/function/metadata.rs:1-718 and its split owner modules"
source_authority = "existing FunctionMetadata storage schema; checked-callout table authority remains checked_callout/site_plan.rs; linear slot authority remains dynamic_v2_aot_metadata_slot.rs and a_prime_i64_physical_receipt.rs"
canonical_issuer = "none; exact move of inherent impl blocks only"
old_edge = "storage schema, checked-callout forwarding/lifecycle, and Dynamic/A-prime linear slot APIs shared one oversized file; the behavior-neutral owner split is already landed"
delete_set = [
  "move checked-callout admit/install/lookup/table/verify impl to function/checked_callout_metadata.rs",
  "move Dynamic/A-prime borrow/install/take impl to function/linear_metadata_access.rs",
  "add ordinary mod declarations in function.rs; do not add #[path]",
  "remove only now-unused metadata.rs imports",
]
acceptance = [
  "metadata.rs and each child remain below 760 lines; no source >=800 remains in this row (observed after 5b36855ae6)",
  "FunctionMetadata field set/order/visibility/derive/Default/Clone semantics are byte-for-byte equivalent",
  "method signatures, visibility, bodies, caller paths, and cfg(test) boundaries are unchanged",
  "no new #[path], no caller edits, cargo check and git diff --check green",
]
non_claims = ["no field deletion/substructing", "no slot-state or clone-scrubbing change", "no backend/JSON/performance change", "no warning retirement"]

[[candidate]]
id = "MIR-SEMANTIC-ERROR-TYPE-COMPRESSION-P1B-C1-REQUIRED-ARGUMENT-PROOF-I0"
status = "accepted_but_not_selected"
kind = "typed_error_bounded_row"
scope = "normal_script_direct_static_claim_ledger.rs:76-116 -> calls/script_direct_static_physical_bridge.rs:24-86"
source_authority = "ScriptDirectStaticClaimedRowV1::consume_required_argument_proof; a consume-owned issue enum, distinct from proof-issuance ScriptDirectStaticRequiredArgumentProofIssueV1"
canonical_issuer = "claim-ledger-owned consume issue enum; bridge carries the typed issue until the existing outer String boundary"
old_edge = "RequiredArgumentProof(String) and error.to_owned() erase consume-time proof issue variants before argument descent"
delete_set = [
  "ScriptDirectStaticPhysicalBridgeErrorV1::RequiredArgumentProof(String)",
  "the bridge-local error.to_owned() conversion for proof consumption",
]
acceptance = [
  "duplicate, empty-required, cardinality, ordinal-out-of-bounds, site mismatch, and non-exact proof failures remain distinguishable variants",
  "proof-issuance issues and proof-consumption issues remain separate owners; the existing issuance enum is not reused as a consume authority",
  "target validation -> proof consume -> argument descent order is unchanged",
  "proof failure emits zero child effect, Call, publication, and claim completion; consumption marks success only",
  "ArgumentDescent(String), ClaimCompletion(String), constructor manifest/loan String boundaries are untouched",
]
non_claims = ["no common recursive port typing", "no claim-completion port redesign", "no constructor demand/loan change", "no route/receipt/MIR/backend change"]

[[candidate]]
id = "MIRBUILDER-RUNTIME-HYGIENE-PANIC-TODO-CENSUS-D0"
status = "accepted_design"
kind = "observation_only"
scope = "selected MirBuilder production modules only; test/fixture/non-selected backend code excluded and counted separately"
source_authority = "source tokens and module ownership; no cleanup issuer"
canonical_issuer = "none"
old_edge = "broad lexical panic/unwrap/expect/todo and dead_code totals are not yet a selected production claim; parent-gated fixtures and debug-only paths are mixed"
observed_partition = "explicit test/fixture filenames: panic 251, unwrap 1969, expect 2004, todo 0, allow 1; production-named files with inline cfg(test): panic 84, unwrap 622, expect 857, todo 0, allow 66; file-local cfg(test)-free upper bound: panic 24, unwrap 218, expect 109, todo 0, allow 29"
smallest_slice = "classify panic! 358 and todo! 0 only, with module-registration-aware states SelectedRelease, SelectedDebugOnly, CompatibilityKeep, CallerZeroDisconnected, TestFixture, NonSelected, and Unclassified"
acceptance = [
  "all 358 panic! and 0 todo! tokens enter exactly one state and selected Unclassified is 0",
  "root -> owner -> terminal is recorded for every SelectedRelease row",
  "debug-only panic paths and parent-gated fixtures do not enter the release claim",
  "Compatibility and caller-zero rows carry an owner and observable reopen trigger",
  "selected panic is split into invariant-terminal versus typed-error-required",
]
non_claims = ["no unwrap/expect census in this row", "no panic removal or blanket Result conversion", "no dead_code deletion", "no runtime policy change", "no production gate activation", "lexical counts are not a complete Rust AST census"]

[[candidate]]
id = "MIRBUILDER-WARN-A-IMPORT-PRUNE-S0"
status = "landed"
kind = "mechanical_warning_batch"
implementation_permission = false
implementation_commit = "same commit as this closeout"
scope = "src/mir/builder/calls/build.rs:25-26 and src/mir/generic_method_route_plan.rs:44-45 only"
source_authority = "Rust compiler unused-import/re-export diagnostics; module/type/proof owners remain unchanged"
canonical_issuer = "none; remove only stale import/re-export syntax and its local allow attributes"
old_edge = "two unused import/re-export suppressions obscure the real owner without carrying semantic authority"
delete_set = [
  "build.rs debug_method_routing::* import and its allow(unused_imports)",
  "generic_method_route_plan.rs ScalarI64MapGetStoreFact re-export and its allow(unused_imports)",
]
acceptance = [
  "debug_method_routing module, ScalarI64MapGetStoreFact implementation, and map_set_scalar_get proof functions remain",
  "no Call/resolver/effect/MIR shape change and no new warning suppression",
  "one batch-end cargo check --lib and existing guards are green",
]
implementation_evidence = """
Removed only the two inventoried unused namespace edges and their local allow
attributes. The debug module, proof type, and proof functions remain. Quick
lib check passed. Quick lib-test check still reports the same three private
PublishedStaticMethodCFrameV1::row errors at parent e96f23970a, so that debt is
classified outside this cleanup and no visibility change was mixed in.
Existing pointer, active-surface, lifecycle, and diff checks close the series.
"""
closeout = "WARN-A landed as compiler-proven T0 hygiene; src delta -4, semantic Call delta 0, new guard/receipt/test 0."
next_execution = "none__return_to_family_local_r6_census"
non_claims = ["no module retirement", "no semantic dead_code deletion", "no automatic cargo-fix sweep", "no Call R6/R7 progress claim", "no new active-surface dispatcher row"]

[[candidate]]
id = "MIRBUILDER-GUARD-REGISTRATION-INDEX-D0"
status = "accepted_but_not_selected"
kind = "docs_only_guard_inventory"
scope = "current active MIR Call guards plus their guard_rows.toml routing"
source_authority = "active card evidence and executable guard files; registration profile is routing metadata, not semantic authority"
canonical_issuer = "none; classify registered, intentionally unregistered, missing, and stale guard references"
old_edge = "recent child guards can exist in docs or the filesystem without appearing in the manifest profile used by CI"
smallest_slice = "inventory only the active Call/normal-source guard family, update registration disposition, and name an owner for the absent externals guard; do not manifest every shell in tools/checks"
acceptance = [
  "active D1B guard registration is observed as positive parity",
  "mir_call_canonical_corridor_guard.sh, mir_call_d1b_exact_target_child_guard.sh, mir_call_d1b_selected_normal_duplicate_projection_guard.sh, and normal_source_plan0_transaction_guard.py are each registered or explicitly owned outside the profile",
  "missing externals guard is not represented as a passing command",
  "no guard command or semantic code is changed in this row",
]
non_claims = ["no all-repository guard closure", "no CI policy rewrite", "no Call implementation"]

[[candidate]]
id = "MIRBUILDER-NORMAL-SCRIPT-FACADE-D0"
status = "accepted_but_not_selected"
kind = "design_only_topology"
scope = "normal_script #[path] ingress at builder.rs and normal_script runtime/source child modules"
source_authority = "existing logical normal_script owners and their current module paths; no new semantic receipt"
canonical_issuer = "none; choose a facade/module topology before moving any source"
old_edge = "builder.rs and child modules still use #[path] because normal_script/ has no mod.rs facade"
smallest_slice = "census the six #[path] edges, choose normal_script/mod.rs facade versus ordinary child modules, and define a path-preserving delete/move set"
acceptance = [
  "every #[path] edge has one owner and a stable replacement path",
  "the existing direct-static shelf guard prohibition is preserved",
  "no source move, re-export, semantic change, or new #[path] is made in D0",
]
non_claims = ["no module move", "no direct-static recipe change", "no builder/compiler seam resolution"]

[[candidate]]
id = "MIRBUILDER-LINE-BUDGET-SPLIT-D0"
status = "accepted_but_not_selected"
kind = "design_only_line_budget"
scope = "raw_invocation_source_transport.rs, normal_script_direct_statement_owner.rs, and normal_script_semantic_source_tests.rs"
source_authority = "existing module ownership and current source line budget"
canonical_issuer = "none; define path-preserving split ownership only"
old_edge = "raw transport was the only over-limit production owner; the two normal_script owners are test-surface-heavy at the 760 design threshold, and compression is not an acceptable fix"
smallest_slice = "record the already-landed raw transport split as no-op, then define two path-preserving cfg(test) include! splits for the normal_script owners; no semantic implementation"
acceptance = [
  "raw_invocation_source_transport.rs is recorded as already landed at 0bd6732792; no repeat split is scheduled",
  "normal_script_direct_statement_owner.rs keeps its existing public module path and splits only the cfg(test) body into named include! fragments, with no new #[path] or re-export",
  "normal_script_semantic_source_tests.rs keeps its existing #[path] module ingress and splits only the cfg(test) body into named include! fragments",
  "each planned child boundary is below 760 lines and the production owner/caller surface is unchanged",
  "builder.rs 737 and FunctionMetadata 718 remain outside this row",
  "no test stuffing, #[path] band-aid, source move, semantic change, new authority, receipt, or guard lands during D0",
]
non_claims = [
  "no refactor implementation in this D0",
  "no warning cleanup",
  "no common_v2 relocation",
  "no production caller or owner change",
  "no test deletion, rename, ignore, or baseline refresh",
]

[candidate.line_budget_split_map]
raw_invocation_source_transport = "landed_noop__0bd6732792__mod_rs_57__max_child_408"
normal_script_direct_statement_owner = "selected_boxshape_t0__keep_tests_module_path__ordinary_tests_child"
normal_script_semantic_source_tests = "selected_boxshape_t0__keep_existing_path_ingress__include_root_admission_and_lexical_scope"
public_path_impact = "zero"
authority_impact = "zero"
receipt_impact = "zero"
guard_impact = "one reusable maintenance dispatch; zero per-cell guards"
implementation_permission = false

[boxshape_maintenance_t0]
task_id = "MIRBUILDER-BOXSHAPE-MAINTENANCE-T0"
status = "landed"
implementation_permission = false
base_head = "8342fbf93a"
landed_commit = "a176bada13"
ceremony_tier = "T0"
decision = "Move existing cfg(test) bodies behind path-preserving module/include boundaries; production behavior and test names stay unchanged."
source_authority = "The existing production modules and existing tests remain the sole owners; this row issues no semantic product."
fail_fast_boundary = "Abort on public/test path drift, missing or 760-line owners, positive src line delta, new #[path], or focused compile/test regression."
line_budget_paths = [
  "src/mir/builder/normal_script_direct_statement_owner.rs",
  "src/mir/builder/normal_script_direct_statement_owner/tests.rs",
  "src/mir/builder/normal_script_semantic_source_tests.rs",
  "src/mir/builder/normal_script_semantic_source_tests/root_admission.rs",
  "src/mir/builder/normal_script_semantic_source_tests/lexical_and_scope.rs",
  "src/mir/builder/indexing.rs",
  "src/mir/builder/indexing/tests.rs",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer.rs",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer/session.rs",
]
pre_split_over_limit_paths = [
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer.rs",
]
allowed_files = [
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/design/current-docs-update-policy-ssot.md",
  "docs/development/current/main/investigations/mirbuilder-cleanup-retirement1-next-edge-census-2026-08-25.toml",
  "src/mir/builder/normal_script_direct_statement_owner.rs",
  "src/mir/builder/normal_script_direct_statement_owner/tests.rs",
  "src/mir/builder/normal_script_semantic_source_tests.rs",
  "src/mir/builder/normal_script_semantic_source_tests/root_admission.rs",
  "src/mir/builder/normal_script_semantic_source_tests/lexical_and_scope.rs",
  "src/mir/builder/indexing.rs",
  "src/mir/builder/indexing/tests.rs",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer.rs",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer/session.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_dispatch.py",
  "tools/checks/lib/mir_call_d1b_active_surface_dispatch_helpers.py",
]
required_text = [
  "src/mir/builder/normal_script_direct_statement_owner.rs||mod tests;",
  "src/mir/builder/normal_script_direct_statement_owner/tests.rs||include!(\"../normal_script_direct_statement_owner_fastmem_failure_test.rs\");",
  "src/mir/builder/normal_script_semantic_source_tests.rs||include!(\"normal_script_semantic_source_tests/root_admission.rs\");",
  "src/mir/builder/normal_script_semantic_source_tests.rs||include!(\"normal_script_semantic_source_tests/lexical_and_scope.rs\");",
  "src/mir/builder/normal_script_semantic_source.rs||#[path = \"normal_script_semantic_source_tests.rs\"]",
  "src/mir/builder/indexing.rs||mod tests;",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer.rs||mod session;",
]
forbidden_text = [
  "src/mir/builder/normal_script_direct_statement_owner.rs||mod tests {",
  "src/mir/builder/normal_script_direct_statement_owner.rs||#[path",
  "src/mir/builder/indexing.rs||mod tests {",
  "src/mir/builder/control_flow/plan/loop_accum_physicalizer.rs||struct DirectAccumPhysicalizerV1",
]
focused_tests = [
  "normal_script_direct_statement_owner::tests",
  "normal_script_semantic_source::tests",
  "indexing::tests",
]
observed_line_counts = "direct owner 148; direct tests 592; semantic test ingress 2; root admission 394; lexical/scope 365; indexing owner 355; indexing tests 431; loop physicalizer 198; private session 594; shared dispatcher 757"
observed_src_line_delta = 0
implementation_evidence = "The existing BoxShape lane split now also moves DirectAccumPhysicalizerV1 stateful emission into loop_accum_physicalizer/session.rs at a176bada13; parent is 198 lines and private session is 594 lines, with the combined source line delta at zero. Production entrypoints, test names, and logical behavior are unchanged. Quick lib and vm-reference checks passed; six focused physicalizer tests and two targeted semantic parity tests passed; unrelated broad direct_ tests retain seven known failures."
quick_lib_check = "green"
focused_test_compile = "current and base 8dc84e7588 both stop at the same three pre-existing E0624 private published-backend row errors before test execution"
test_name_inventory = "unchanged across base/current for both selected test modules"
acceptance = "Every listed owner is below 760 lines; src line delta is non-positive; module/test paths and behavior are unchanged; no semantic authority, receipt, per-cell guard, fallback, test deletion, ignore, or baseline refresh is added."
non_claims = "No Call R6/R7 progress, source acceptance, production switch, backend change, broad test cleanup, or raw transport resplit."
closeout = "DirectAccum physicalizer follow-up landed as compiler-proven BoxShape hygiene at a176bada13; parent/session remain below 760 lines, src delta 0, semantic Call delta 0, new guard/receipt 0, no test deletion/ignore, and pointer returns to the family-local R6 closeout."

[[candidate]]
id = "MIRBUILDER-LEGACY-PHI-CANDIDATE-RETIRE-R0"
status = "landed"
kind = "caller_zero_legacy_test_retirement"
implementation_permission = false
base_head = "b9f4e05fc2"
scope = "src/mir/builder/ssa/phi_input_materializer/legacy_candidate.rs, legacy_candidate_cfg.rs, legacy_candidate_tests.rs, their facade declarations, SSA inventory expectation, and fixed test inventory references"
source_authority = "The fixed HEAD git grep caller graph, module declarations, active production PHI repair owners, SSA inventory guard, and baseline test inventory are the authorities; filename, dead_code status, or stale prose alone is not deletion proof."
canonical_issuer = "none; this candidate issues no semantic PHI product. The active production owners remain function_repair.rs::materialize_all_phi_inputs and edge_rematerialization.rs::rematerialize_for_pred/for_pred."
old_edge = "The legacy_candidate subtree is a clone-only PHI repair/preflight proof surface with no non-test caller; keeping its module declarations and eleven baseline test rows preserves a disconnected legacy surface."
replacement_owner = "module_lifecycle.rs (two materialize_all_phi_inputs callers), joinir/merge/rewriter/stages/apply.rs (one caller), and the shared edge_rematerialization/function_repair implementation"
delete_set = [
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate.rs",
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate_cfg.rs",
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate_tests.rs",
  "legacy_candidate and legacy_candidate_cfg/test module declarations in phi_input_materializer.rs",
  "the three legacy files from resolved_binding_ssa_inventory.py expected split manifest",
  "the eleven legacy_candidate_tests rows from the fixed cargo test inventory after a dedicated refresh",
]
preconditions = [
  "replacement owner parity is recorded for artifact/fact closure and post-publication verification",
  "cargo test --list confirms the eleven rows are the only test-surface loss",
  "baseline refresh reconciles total/passed counts without changing the accepted failure-name set",
  "SSA inventory guard is updated only for the intentional file retirement",
  "no production caller, external module path, feature, or generated include re-enters the subtree",
]
smallest_slice = "On a branch, delete the three caller-zero legacy files and facade declarations, update the existing SSA inventory and fixed test-inventory baseline, then run the focused PHI repair tests and existing guards. Do not alter active function_repair/edge_rematerialization behavior or create a new guard."
observed_evidence = "At HEAD b9f4e05fc2, git grep found no non-test caller of prepare_legacy_phi_repair_candidate_v1 or LegacyPhiRepairCandidate; active production repair uses function_repair/edge_rematerialization. The retired subtree was 792 + 26 + 696 lines and contributed eleven named baseline test rows."
allowed_files = [
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mirbuilder-cleanup-retirement1-next-edge-census-2026-08-25.toml",
  "src/mir/builder/ssa/phi_input_materializer.rs",
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate.rs",
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate_cfg.rs",
  "src/mir/builder/ssa/phi_input_materializer/legacy_candidate_tests.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_dispatch.py",
  "tools/checks/lib/mir_call_d1b_active_surface_dispatch_helpers.py",
  "tools/checks/lib/resolved_binding_ssa_inventory.py",
  "tools/checks/fixtures/canonical_ssa_seam_inventory_v1.json",
  "tools/checks/manifests/cargo_lib_red_baseline.toml",
  "tools/checks/manifests/cargo_lib_red_baseline.tests.txt",
]
acceptance = [
  "the three files and facade declarations are absent with no unresolved import or module path",
  "active production PHI materialization callers and focused replacement tests remain present and green",
  "baseline inventory and passed count are refreshed once; the known failure-name set is byte-for-byte unchanged",
  "resolved_binding_ssa_inventory.py passes with the intentional retired subtree absent",
  "tracked source delta is negative, no new receipt/guard/fixture/ignore is added, and pointer/active-surface guards pass",
]
reopen_trigger = "a named non-test caller, generated/include edge, feature-gated consumer, or missing replacement parity evidence re-enters the subtree"
non_claims = [
  "no PHI algorithm rewrite",
  "no canonical Call or LegacyCallV0 schema change",
  "no broad test deletion or baseline failure reclassification",
  "no deletion of edge_verifier, function_repair, edge_rematerialization, remat_fact, or active PHI callers",
]
closeout = "Retired in this closeout: the three caller-zero PHI candidate files and facade declarations are absent; eleven baseline test rows were removed; the SSA inventory was reconciled for already-retired bridge rows; focused replacement tests and the fixed baseline comparator pass at 7407/133/29 with failure hash 299aeee63248e20bf885556baacece21beda0fdf540c6b0d8fdcd74e7d3a8f18; source delta is negative and no new guard, receipt, fixture, or ignore was added."

[[candidate]]
id = "JOINIR-RETIREMENT-CANDIDATE-CENSUS-D0"
status = "candidate_design_no_safe_slice"
kind = "docs_only_retirement_census"
scope = "src/mir/join_ir/**, src/mir/join_ir_to_mir/**, src/mir/builder/joinir_id_remapper.rs, src/mir/builder/control_flow/joinir/merge/**, src/mir/control_tree/normalized_shadow/**, src/mir/join_ir_ops.rs, crates/hakorune_mir_joinir, and their mod/pub/re-export, cfg/feature, generated, Cargo, test, guard, smoke, and docs edges"
source_authority = "fixed review-head Git objects, Cargo manifests, module declarations, explicit production/reference/test callers, and existing JoinIR/legacy fate cards; a numeric seven-candidate claim is not an authority"
canonical_issuer = "none; this row records an exact candidate inventory and fate classification without issuing deletion or successor authority"
old_edge = "The external seven-candidate deletion claim cannot be reconstructed from tracked evidence. JoinIR surfaces include live production utilities, public/re-export boundaries, test/reference callers, and a Cargo package edge; text-zero or apparent dead_code is insufficient."
smallest_slice = "Build one bounded census of exact path/symbol/range candidates and classify each as Retire, RetainNamed, MigrateEvidence, or NoSafeSlice. Record owner, caller graph, feature/env, guard/fixture/docs impact, successor or physical-impossibility proof, reopen trigger, and observed LOC. Do not delete or alter the A2 matrix."
acceptance = [
  "The number and identity of candidates are derived from fixed tracked evidence; no preselected seven-item list is assumed.",
  "Cargo dependency, mod/pub/re-export, cfg/feature, generated/include, production/reference/test, guard, smoke, and documentation edges are checked for every candidate.",
  "Only candidates with exact owner, successor or physical-impossibility proof, residual-zero conditions, and evidence-retarget plan can be classified Retire; all others remain RetainNamed, MigrateEvidence, or NoSafeSlice.",
  "The census states whether each candidate is inside or outside the 185-row A2 owner-map boundary; A2 is not silently bypassed.",
  "No Rust, Cargo, guard, fixture, source, or owner-map change is made by this design row.",
]
non_claims = [
  "no seven-item deletion permission",
  "no caller-zero inference from rg/text absence, package import absence, or stale docs",
  "no JoinIR crate split, VM/backend change, guard removal, or source deletion",
  "no parking of A2 without an explicit CURRENT_STATE decision and named reopen trigger",
]

[selection]
active_lane = "No cleanup row selected; follow CURRENT_STATE.toml and the generic M7-S JSON-v1 closeout pointer"
order_after_d1n = [
  "MIR-CALL-D1B-DIRECT-CALL-SOURCE-INVENTORY-COSEAL-D0 (direct-call coverage prerequisite)",
  "MIRBUILDER-COMPILER-SEAM-CENSUS-D0 (docs-only boundary ledger)",
  "MIRBUILDER-INVESTIGATION-LEDGER-SCOPE-AND-INDEX-D0 (schema/guard topology before index implementation)",
  "MIRBUILDER-GUARD-REGISTRATION-INDEX-D0 (active-family routing closure)",
  "MIR JSON ingress fallback/field-disposition D0 (parked compatibility policy)",
  "MIR-CALL-R6-CALLFLAGS-RETIRE-D0 (census only; MirCall transport remains live)",
  "MIRBUILDER-NORMAL-SCRIPT-FACADE-D0 (#[path] topology design)",
  "MIRBUILDER-LINE-BUDGET-SPLIT-D0 (path-preserving split design)",
  "FUNCTION-METADATA-OWNER-SPLIT-R0 (landed at 5b36855ae6; no repeat split)",
  "MIRBUILDER-WARN-A-IMPORT-PRUNE-S0 (mechanical batch after fast mode opens)",
  "MIR-SEMANTIC-ERROR-TYPE-COMPRESSION-P1B-C1-REQUIRED-ARGUMENT-PROOF-I0 (typed proof row)",
  "MIRBUILDER-RUNTIME-HYGIENE-PANIC-TODO-CENSUS-D0 (finite panic/todo observation before any hygiene edits)",
]
parked_cleanup = [
  "MIRBUILDER-LEGACY-PHI-CANDIDATE-RETIRE-R0 is selected fast; delete only its finite subtree and reconcile the eleven fixed test rows once before closeout.",
  "JOINIR-RETIREMENT-CANDIDATE-CENSUS-D0 remains unselected until an explicit lane switch is recorded; it is the lightweight alternative to an unverified seven-item deletion claim.",
]
not_selected = "ArgumentDescent and ClaimCompletion require distinct port rows; compiler/Builder relocation and broad warning/runtime cleanup remain design stops"

[stop_if]
conditions = [
  "a cleanup row requires a new semantic receipt, default/sentinel, AST/name reconstruction, fallback/retry, or backend inference",
  "metadata split changes a field, method body, caller, cfg(test), or introduces #[path]",
  "typed-error work crosses the common recursive port or combines proof and completion in one row",
  "runtime hygiene counts are used without a finite production boundary",
  "active D1N Call consumer scope is modified by cleanup work",
]

[joinir_census]
status = "census_complete_fate_split_no_deletion_authorized"
observed_head = "e81414b2fd60bdacb375d61db41dfe38449610b5"
boundary = "src/mir/join_ir/**, src/mir/join_ir_to_mir/**, src/mir/builder/joinir_id_remapper.rs, src/mir/builder/control_flow/joinir/merge/**, src/mir/control_tree/normalized_shadow/**, src/mir/join_ir_ops.rs, crates/hakorune_mir_joinir, and all module/Cargo/feature/test/guard/generated/docs edges"
result = "The external seven-item deletion claim is not reproducible from tracked evidence. The census found 15 JoinIR core/utility candidate groups, a 13-file join_ir_to_mir family totaling 1,854 observed LOC, and six fate buckets in the builder/normalized-shadow/package area; no production file is deletion-ready without a successor or an explicit terminal decision."
granularity = "One classification per tracked file or cohesive responsibility family. Exact symbol/range anchors are representative evidence for the file/family owner; this row does not create a function-by-function or reference-by-reference fate ledger."
stop_line = [
  "Do not split this census into every function, caller, guard, fixture, or documentation reference. Those edges are summarized as closure evidence for the file/family row only.",
  "Do not add a second JoinIR owner map, expectation TSV, or per-function retirement card. If a candidate needs deeper semantic proof, open one named implementation/design row for that candidate rather than expanding this census.",
  "The census is complete when each in-scope file/family has one fate bucket and a named reopen/next-row condition; unresolved internal edges keep the bucket MigrateEvidence or NoSafeSlice.",
]
confirmed_retire = "none at the observed head. Merge coordinator, cfg(test) conversion tests, and the hakorune_mir_joinir package are Retire candidates only; each still needs its own bounded fate/edge decision before any fast deletion."
fast_deletion_gate = "A fast deletion row may open only for one named file/family after exact caller graph closure, successor/terminal proof, guard/fixture/Cargo/docs retargeting, focused parity, and residual-zero. No candidate may be selected from the number seven alone."
source_authority = "Fixed observed-head Git objects, Cargo manifests, module declarations, explicit callers, cfg/feature/env conditions, guard/fixture/generated edges, and existing JoinIR fate cards. rg absence and stale README prose are non-authority."
canonical_issuer = "This section issues only a bounded observation/fate split. It does not issue a deletion, owner-map, successor, or backend decision."
a_core_fates = [
  "RetainNamed: JoinIR core (J0), join_ir_ops (J1), carrier/scope/update/error owners (J2-J5), and ConditionEnv (J6 only as a migration boundary) retain live production or public contracts.",
  "MigrateEvidence: inline-boundary, skip_ws/funcscanner_trim, generic Case-A, loop-to-join routing, JoinValueSpace, canonical names, value-id ranges, and type-inference surfaces (J7-J14) need route/fallback/docs evidence before any fate change.",
  "NoSafeSlice: hakorune_mir_joinir package (J15) still has a Cargo path edge and crate tests/docs; external Rust import absence is not package caller-zero proof.",
]
b_conversion_fates = [
  "join_ir_to_mir is 13 files / 1,854 observed LOC: block allocator/core/function/module converters retain internal contracts; call_generator/convert/handlers/utils are MigrateEvidence because the old Call shape and PHI/failure evidence remain.",
  "The parent and child test files are only Retire candidates after an explicit decision to retain them as reference parity or to retarget their fixtures; bridge facade and JoinIrToMirError remain NoSafeSlice until external API/reference fate is decided.",
  "The README claim for NormalizationExecuteBox has no current source caller and is stale-doc evidence, not deletion proof; feature/env debug logging is not route authority.",
]
c_builder_fates = [
  "joinir_id_remapper is 705 LOC with cfg(test), phase256 tests, merge references, and a guard edge: RetainNamed/MigrateEvidence, not caller-zero.",
  "merge coordinator is a Retire candidate, but test-bearing helpers and non-test guard-support remain; split coordinator, test helper, and guard-support before any deletion.",
  "normalized_shadow is 38 files / 7,801 observed LOC with non-test callers through port_aware_function_draft_impl: RetainNamed; its strict/dev error-handling contract drift is a typed blocker.",
  "hakorune_mir_joinir is 4 files / 163 observed LOC with Cargo and docs edges: Retire candidate only after a separate Cargo/API/docs decision; no package deletion now.",
]
external_edges = [
  "A2 overlap is zero for JoinIR source sites; one A2 evidence string mentions JoinIR docs indirectly and is not a site overlap. Do not route JoinIR fate through the unresolved 185-row map.",
  "JoinIR README/lowering README contain absent historical paths or overbroad production claims; classify as MigrateEvidence/stale docs and correct only in the named fate closeout.",
]
fate_states = [
  "Retire requires exact owner, successor or physical-impossibility proof, guard/fixture/Cargo/docs retargeting, focused parity, and residual-zero.",
  "RetainNamed requires owner/evidence/reopen trigger/non-claims; MigrateEvidence requires an explicit evidence handoff before source deletion; unresolved API/Cargo/route remains NoSafeSlice.",
]
next_tasks = [
  "JOINIR-RETIREMENT-CANDIDATE-CENSUS-D1-FATE-SPLIT: split coordinator/test/guard-support and settle core/normalized-shadow fate without deleting source.",
  "JOINIR-TO-MIR-PUBLIC-FACADE-AND-REFERENCE-FATE-D0: decide public facade/reference retention, retarget two external tests and PHI/failure evidence, or park with a named reopen trigger.",
  "Do not open a fast deletion row until one exact candidate passes the full caller graph, successor/terminal, evidence-retarget, focused parity, and residual-zero checks; A2 remains independently governed.",
]
acceptance = [
  "The seven-item claim is explicitly rejected as unverified; candidate identity and counts are derived from the fixed observed head.",
  "Every observed surface has a named fate bucket and reopen/non-authority rule; no production caller-zero claim relies on text absence.",
  "No Rust/Cargo/guard/fixture/source/map change was made by this census; tree remains clean and A2 pointer is untouched.",
]

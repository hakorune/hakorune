title = "MIR Call core R6 D1B Method(None) issuer and recovery census"
status = "active_design_stop"
observed_commit = "da00098e2a"
base_commit = "9ebf6f82c6"
observed_at = "2026-08-31"
implementation_permission = false

[boundary]
starts = "selected source/builder target selection and typed MIR-v0 ingress"
ends = "block publication, selected JSON egress, selected Rust VM dispatch, and selected ny-llvmc pre-effect terminal"
includes = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/resolved_semantics instance-method source/declaration/carrier boundary",
  "src/mir/normal_callable_semantic_package result/effect/physical-signature boundary",
  "src/runner/mir_json_v0/call.rs",
  "src/runner/modes/common_util/core_bridge.rs",
  "src/runner/mir_json_emit/emitters/calls.rs",
  "src/runner/mir_json_emit/helpers.rs",
  "src/backend/mir_interpreter/handlers/calls/**",
  "selected ny-llvmc mir-call and pure-compile dispatch shims",
]
excludes = [
  "PyVM/reference/Python/WASM/non-selected backends",
  "Closure/Constructor/CallFlags schema retirement",
  "nested/upvar me capture ABI",
  "R6 func/Option<Callee> field deletion",
]

[decision]
summary = "Method(None) is not a canonical static-call state. Static calls require a same-brand exact catalog row and issue qualified Global(owner.method/arity); instance calls require an exact receiver and issue Method(receiver)."
source_authority = "same-brand declaration/source catalog plus exact receiver relation"
canonical_issuer = "static families use their landed publication owners; true DeclaredInstance requires one resolver-owned nominal declaration relation, followed by package contract co-seal and Method(Some(receiver)) consumption"
non_authority = [
  "current_static_box by itself",
  "has_method and StaticMethodId/text formatting",
  "func/ValueId::INVALID/Const/name/wire text",
  "env/profile methodize switches",
  "VM registry, singleton, by-name, or args[0] recovery",
  "native metadata/surface retry",
]
fail_fast = "exact target, owner/method/arity, receiver presence/ownership, and profile shape must be settled before argument-effect publication, block insertion, JSON mutation/emission, VM dispatch, native need-scan, or object emission"
no_retry = "Global failure, Method failure, malformed explicit target, and selected C plan miss never retry another target, name, registry, metadata, or singleton route"
smallest_next_slice = "MIR-CALL-ME-DECLARED-INSTANCE-EFFECT-ISSUER-D0 is the next design-only child: name the live parser callable-contract carriage and one same-session semantic-effect issuer for the already-landed source relation. Result/Completion retention landed at e1976b8ba1; target, full-lane selected-C capability, affine consumption, and production cutover remain closed."
non_claims = [
  "production implementation from this design row",
  "package-side or backend-derived target issuance",
  "Closure/NewClosure",
  "Constructor/NewBox",
  "MirCall/CallFlags",
  "JoinIR remap",
  "backend activation",
  "warning cleanup",
]

[counts]
direct_or_dynamic_issuer_rows = 5
selected_terminal_recovery_rows = 3
adjacent_non_authority_rows = 3
unclassified_rows = 0
duplicate_row_ids = 0
parked_selected_backend_rows = 0

[[issuer]]
id = "method_resolution_static_none"
class = "direct_issuer"
owner = "historical src/mir/builder/calls/method_resolution.rs (removed at fea43bdade; final corridor cleanup at 598530d23b)"
caller = "0 production callers; historical build.rs handoff removed at 598530d23b"
edge = "current_static_box + hardcoded has_method issues Callee::Method { receiver: None } and the builder publishes a target/name path"
authority = "not available in this route; existing callable declaration catalog/publication handoff is the required authority"
disposition = "RetiredCallerZero"
coupled_to = "The four named R2 compatibility descendants reach this issuer through shared Resolved after argument descent; do not treat it as an independent Method implementation cell."
replacement = "exact same-brand catalog key -> qualified Global(owner.method/arity) -> CalleeResolver -> MirInstruction::call"
delete_set = ["Method(None) literal", "hardcoded has_method target classification", "name Const publication for this route", "tail/suffix alternate-target retry"]
fail_fast = "before argument-effect publication and before build.rs block insertion"
reopen_trigger = "a new production caller or receiverless Method writer appears"
retirement_commit = "598530d23b"

[[issuer]]
id = "unified_emitter_methodize_none"
class = "direct_reissuer"
owner = "src/mir/builder/calls/unified_emitter.rs:407-461"
caller = "selected unified emission path; physical publication at :700-725"
edge = "the explicit compatibility snapshot reclassifies an incoming RuntimeData Global into Method(None)"
authority = "incoming exact Callee provenance; environment/profile/text cannot reclassify a canonical target"
disposition = "RetiredCallerZero"
coupled_to = "Stage1 exact-1 writers, build_stage1 direct emit, Program(JSON) zero writers, shell empty transport, and separate Hako/JSON readers require a finite ingress-policy decision before this reissuer can change."
replacement = "retain exact Global or exact Method(receiver); do not repair an unproven Global here"
delete_set = ["HAKO_MIR_BUILDER_METHODIZE target mutation", "StaticMethodId text reclassification", "Method(None) trace/branch"]
fail_fast = "before create_mir_call and physical terminal"
reopen_trigger = "a new Rust compatibility selector or Global-to-Method(None) reissuer appears"
retirement_commit = "287e944fe8"

[[issuer]]
id = "mir_json_v0_optional_method_receiver"
class = "compatibility_ingress"
owner = "src/runner/mir_json_v0/call.rs:78-118,253-272"
caller = "owner-private JSON-v0 loader"
edge = "explicit Method receiver is optional and can publish Method(None); missing box name also receives a RuntimeDataBox default"
authority = "JSON-v0 owner-local compatibility relation only; explicit malformed shape never falls back to name/func"
disposition = "CutoverBlockerOpen"
replacement = "explicit Method(Some) -> canonical Method; missing/invalid receiver or missing static relation -> typed reject before Call publication"
delete_set = ["nullable Method receiver publication", "RuntimeDataBox default as target authority", "malformed-explicit retry through name/func"]
fail_fast = "after input state is resolved and before args/block Call insertion"
reopen_trigger = "an explicit receiverless V0 compatibility contract gains a unique exact static mapping"

[[issuer]]
id = "core_bridge_const_methodize"
class = "post_wire_reissuer"
owner = "src/runner/modes/common_util/core_bridge.rs:101-199"
caller = "selected bridge wrapper when methodize env flag is active"
edge = "Const(String) scan maps legacy func text Box.method/N to receiverless Method and drops arity"
authority = "post-wire text, env flags, and register contents are non-authority"
disposition = "CutoverBlockerOpen"
replacement = "typed target is preserved; unsupported legacy payload rejects at compatibility boundary"
delete_set = ["Const target scan", "arity-dropping Method(None) mutation", "func removal followed by target reissue"]
fail_fast = "before JSON object mutation and bridge output publication"
reopen_trigger = "selected bridge activation becomes zero with a durable guard, or an exact source handoff replaces it"

[[issuer]]
id = "core_bridge_module_singleton_methodize"
class = "post_wire_reissuer"
owner = "src/runner/modes/common_util/core_bridge.rs:200-245,290-335"
caller = "selected bridge singleton/module-function compatibility path"
edge = "ModuleFunction/static singleton metadata is rewritten to a receiverless Method"
authority = "singleton table, metadata text, and missing receiver are not target authority"
disposition = "CutoverBlockerOpen"
replacement = "preserve typed Global/Method(Some), or typed reject the compatibility payload"
delete_set = ["receiverless singleton Method mutation", "metadata/name target recovery"]
fail_fast = "before bridge mutation and downstream VM/native dispatch"
reopen_trigger = "bridge caller-zero is proven and guard is updated, or exact catalog handoff is connected"

[[terminal]]
id = "json_egress_method_none"
owner = "src/runner/mir_json_emit/emitters/calls.rs:17-75; src/runner/mir_json_emit/helpers.rs:43-64"
edge = "v0 uses receiver.unwrap_or(func); v1 can serialize nullable Method receiver"
authority = "writer is projection-only and cannot turn func into a receiver"
disposition = "CutoverBlockerOpen"
fail_fast = "before JSON append/emission"
positive = ["qualified Global projection", "Method(Some) exact receiver projection"]
negative = ["v0 receiver <- func", "v1 null receiver", "post-wire static-to-instance reclassification"]
reopen_trigger = "all upstream Method(None) producers are zero and egress has a typed error boundary"

[[terminal]]
id = "rust_vm_method_none_recovery"
owner = "src/backend/mir_interpreter/handlers/calls/mod.rs:70-92,220-242; src/backend/mir_interpreter/handlers/calls/method.rs:394-532"
edge = "HostBridge, registry, qualified-name retry, singleton fallback, and adjacent receiver autoscan/args[0] recovery"
authority = "VM is a typed terminal consumer, not a target issuer"
disposition = "CutoverBlockerOpen"
fail_fast = "at call entry before HostBridge/registry/function execution"
positive = ["Global static dispatch", "Method(Some) instance dispatch"]
negative = ["Method(None) terminal reject", "registry/name/singleton retry zero", "receiver autoscan and args[0] surrogate isolated to a separate blocker"]
reopen_trigger = "upstream issuer count reaches zero and selected parity covers terminal errors/results"

[[terminal]]
id = "selected_native_method_none_preflight"
owner = "selected ny-llvmc pure/need/dispatch shims: hako_llvmc_ffi_pure_compile_generic_lowering_op_dispatch_calls.inc; hako_llvmc_ffi_mir_call_need_policy.inc; hako_llvmc_ffi_mir_call_dispatch.inc"
edge = "missing/null receiver is read as numeric zero; plan miss can retry metadata/name/surface routes"
authority = "selected typed plan/ABI rows; C metadata/name/surface are not target authority"
disposition = "CutoverBlockerOpen"
fail_fast = "before need-scan, plan/origin/declaration checks, LLVM IR emission, or object publication"
positive = ["Method(nonzero receiver) with exact plan", "Global with exact static plan"]
negative = ["missing/null receiver", "explicit receiver zero distinguished from missing", "plan miss/conflict retry zero"]
reopen_trigger = "selected egress no longer emits receiverless Method and C presence/range guard is landed"

[[adjacent]]
id = "callsite_canonicalize_not_method_none_authority"
owner = "src/mir/passes/callsite_canonicalize/pass.rs:119-240"
edge = "None+Const -> Global and Global+args[0] -> Method(Some) observer/rewrite"
disposition = "RetainGuarded"
reason = "adjacent optimizer blocker; it must not be used to repair Method(None) or infer a receiver"
reopen_trigger = "optimizer target issuer census changes"

[[adjacent]]
id = "global_route_arity_observer"
owner = "src/mir/optimizer_passes/global_call_route_plan/generic_i64_body/refine.rs:359-362"
edge = "None is observed as false"
disposition = "RetainGuarded"
reason = "observer only; no target issuance"
reopen_trigger = "observer begins issuing or reclassifying a target"

[[adjacent]]
id = "method_v1_required_receiver"
owner = "src/runner/json_v1_bridge/parse/mir_call.rs:183-224"
edge = "JSON-v1 already requires receiver for Method"
disposition = "RetainGuarded"
reason = "positive boundary; preserve as a parity guard while V0 is narrowed"
reopen_trigger = "schema change or receiver optionality is introduced"

[state_matrix]
ready_static = "unique same-brand owner/method/exact arity -> qualified Global"
ready_instance = "exact source/resolver receiver -> Method(receiver)"
explicit_method_some = "typed Method(Some) accepted and preserved"
missing_static = "missing static identity/arity -> typed reject before effects/publication"
missing_receiver = "Method(None)/null/absent receiver -> typed reject unless a future owner-private exact V0 relation is explicitly accepted"
malformed_explicit = "typed reject; never retry name/func"
foreign_or_ambiguous = "typed reject; no by-name, registry, metadata, or singleton retry"
receiver_zero = "numeric zero is a value; distinguish explicit zero from missing presence in selected C"
plan_miss = "typed terminal; no metadata/name/surface alternate route"
outside_cohort = "named ParkedSealed only for non-selected backends and diagnostic/reference surfaces"

[acceptance]
status = "design_only_no_safe_slice"
reviewers = ["d2_issuer_boundary_audit", "d2c_consumer_selection_audit", "d2b_impl_plan_audit"]
review_scope = "independent read-only source census; no code, fixture, or Cargo evidence"
required_before_fast = [
  "five issuer/reissuer rows have exact caller, lifecycle, disposition, and delete set",
  "selected JSON/Rust/native terminal rows are classified with no unclassified boundary edges",
  "static/instance matrix is total for ready, absent, duplicate, foreign, ambiguous, and arity-mismatch inputs",
  "generic route receives an exact catalog/owner handoff before argument descent, or remains NoSafeSlice",
  "Method(None) producer/runtime edge reaches zero before any core field deletion",
  "receiver.unwrap_or(func), receiver autoscan, registry/name/singleton retry, post-wire Const methodize, and selected C missing->0 retry are zero",
  "positive target/receiver/args/dst/effects/result parity and negative pre-publication rejection are available",
  "unified_emitter remains below 800 lines; any growth requires a path-preserving split before semantic edits",
]
guard = "extend the shared call corridor guard only after the producer-first row is selected; no guard is evidence of implementation permission by itself"
reopen_triggers = [
  "new Method(None) literal or dynamic issuer",
  "new receiver/name/func/Const/registry/metadata retry",
  "new selected backend consumer or egress profile",
  "exact catalog handoff changes or a source brand becomes unavailable",
]

[stage1_full_artifact_lane_fate_2026_08_29]
status = "parked_sealed"
implementation_permission = false
decision = "Stage1 full artifact remains a separate NoSafeSlice. It blocks deletion of Stage1 compatibility writers, Stage1 0/1 parity claims, and final R6 while that residual route is live; it does not block the Rust ingress-policy I0 or a disjoint exact Method(Some) preservation slice. The build_stage1 direct-emission path is a SeparateArtifactBuildLane, not a Rust Canonical or ExplicitLegacy methodize state."
reopen_when = "An existing Stage1 source owner emits a non-empty artifact plus metadata from the same source/semantic/compile transaction without stale binary, wrapper/argv substitution, macro switching, second resolver, fallback, or retry; alternatively the Stage1 route is retired."
non_claims = "Stage1 is not the repository-wide owner of HAKO_MIR_BUILDER_METHODIZE. Its Rust and shell exact-1 injectors are only named Stage1 compatibility ingress owners."

[method_corridor_ingress_policy_d0_2026_08_29]
status = "landed_design"
task_id = "MIR-CALL-METHOD-CORRIDOR-INGRESS-POLICY-D0"
implementation_permission = false
decision = "Freeze the finite reader/writer boundary and use one module-ingress snapshot owner. The env selector chooses Canonical versus ExplicitLegacyCompatibility; it never issues or repairs a target. The Rust boundary is accepted; JSON/Hako readers remain separate owners and build_stage1 direct emission remains a parked artifact lane."
source_authority = "The incoming typed Callee/CanonicalGlobalTarget remains target authority. A module-lifecycle policy snapshot may select compatibility behavior once before Builder mutation."
non_authority = "The env string, config getter, JSON egress profile, Hako methodize pass, StaticMethodId text, name/arity, Stage1 artifact failure, VM/registry, args[0], and autoscan."
census_boundary = "Rust process/env ingress -> module-lifecycle policy snapshot -> unified-emitter policy consumer; includes Rust readers/writers and the shell/direct-build handoff that can change the Rust value, excludes JSON egress and Hako methodize readers, which retain separate owners."
candidate_policy = [
  "Rust unified-emitter reader only: unset or exact 0 -> Canonical; exact 1 -> ExplicitLegacyCompatibility; other or non-Unicode -> typed reject before Builder mutation.",
  "Canonical preserves the incoming typed Global; ExplicitLegacyCompatibility alone may retain the current bounded RuntimeData Global-to-Method(None) projection.",
  "JSON egress and Hako readers remain separately owned and are excluded from the Rust policy claim.",
]
state_matrix = [
  "Unset at the named Rust module ingress -> Canonical; no ambient emitter read.",
  "Exact \"0\" at the named Rust module ingress -> Canonical; no Method(None) projection.",
  "Exact \"1\" from the named Stage1 compatibility writer -> ExplicitLegacyCompatibility; the existing bounded projection remains named and isolated.",
  "Other or non-Unicode Rust selector -> typed reject before Builder mutation; no default-on or retry.",
  "tools/hakorune_emit_mir.sh unset -> forwarding owner supplies exact \"0\" (Canonical); explicit empty remains a typed-reject input and is not coerced.",
  "tools/selfhost/mainline/build_stage1.sh direct emit -> SeparateArtifactBuildLane; it does not claim Rust Canonical or ExplicitLegacy methodize until its own artifact/env provenance is sealed.",
  "JSON/Hako selector -> separate owner state; not evidence for the Rust policy and not changed by this row.",
]
finite_census = [
  "Rust ambient getter and unified-emitter consumer: src/config/env/builder_flags.rs and src/mir/builder/calls/unified_emitter.rs.",
  "One-time snapshot owner: both Rust module ingress paths in src/mir/builder/module_lifecycle.rs write the Copy field on src/mir/builder/compilation_context.rs; unset/exact 0 yields Canonical and the emitter never rereads env.",
  "Stage1 exact-1 injectors: src/runner/stage1_bridge/env/runtime_defaults.rs and tools/selfhost/lib/stage1_contract.sh; build_stage1 direct emit does not currently prove the same pin.",
  "Canonical exact-0 injectors exist in src/main.rs, src/runner/child_env.rs, and src/host_providers/mir_builder.rs.",
  "tools/hakorune_emit_mir.sh now forwards unset as exact \"0\" (Canonical); explicit empty/invalid values remain visible to the Rust typed parser and reject.",
  "Separate excluded readers remain in src/runner/mir_json_emit/root.rs and lang/src/mir/builder/internal/{builder_config_box,call_methodize_box}.hako.",
]
required_before_i0 = [
  "NORMAL-CALLABLE-COMPAT-ORIGIN-TEST-R0 is green and R2 preflight/downstream fence wording is reconciled.",
  "Every implicit unset/empty route is classified Canonical, ExplicitLegacyCompatibility, or typed reject by its actual owner; Stage1 is not assumed to be the sole reader/writer family.",
  "Module lifecycle is accepted as the sole Rust snapshot ingress and invalid/non-Unicode input rejects before clear_callable_declaration_catalog or later Builder mutation.",
  "The I0 diff can keep unified_emitter.rs below 760 lines, reuse the existing lifecycle guard, and avoid a new guard family.",
]
resolved_d0_questions = [
  "tools/hakorune_emit_mir.sh unset forwarding is owned by execute_selfhost_builder_runner and maps to exact \"0\"; explicit empty/invalid values are left for the Rust typed reject.",
  "tools/selfhost/mainline/build_stage1.sh direct emit is explicitly outside the Rust methodize policy as SeparateArtifactBuildLane and retains the Stage1 artifact reopen trigger.",
]
open_blockers = []
future_i0 = "MIR-CALL-METHOD-CORRIDOR-EXPLICIT-COMPAT-INGRESS-I0"
future_i0_old_edge_delete_set = [
  "Rust unified emitter per-call ambient env read",
  "unset or arbitrary selector -> implicit methodize true",
  "Canonical typed Global -> Method(None) through an implicit default",
]
future_i0_non_claims = "No method_resolution producer retirement, repository-wide env normalization, Hako/JSON/VM/backend change, Stage1 artifact, receiver repair, Method(None)=0, or Call-schema cutover."
no_safe_slice = "Remain in design_stop if a route owner for unset/empty cannot be named, the policy must be reread during body lowering, Stage1/JSON/Hako parity is required in the same slice, or target meaning must be reconstructed from text/registry/arguments."

[method_corridor_explicit_compat_ingress_i0_2026_08_29]
status = "landed"
task_id = "MIR-CALL-METHOD-CORRIDOR-EXPLICIT-COMPAT-INGRESS-I0"
guard_phase = "method_corridor_explicit_compat_ingress_i0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-INGRESS-POLICY-D0"
ceremony_tier = "T2"
implementation_permission = false
change = "Snapshot the Rust methodize policy once at module ingress and make unified emission consume that typed value. Unset/exact 0 is Canonical, exact 1 is the named Stage1 compatibility mode, and other/non-Unicode values reject before Builder mutation. Delete only the implicit/default-on edge."
contract = "Incoming typed Callee/CanonicalGlobalTarget remains the sole target authority. The explicit compatibility branch is a bounded projection only; JSON/Hako readers, Stage1 artifact construction, and build_stage1 direct emission remain separate owners or parked lanes."
done = "A focused matrix proves unset/0 preserve Global, exact 1 preserves the existing bounded compatibility projection, malformed/non-Unicode values fail before publication, the module snapshot is stable after env mutation, and the existing call-corridor lifecycle guard remains green."
stop = "Return to design if another Rust writer/reader is found, shell/direct-build semantics require a new authority, policy is reread during body lowering, or the change reaches method_resolution, JSON/Hako, VM/backend, receiver repair, or Call schema."
implementation_commit = "13664f7787"
focused_tests = [
  "CARGO_BUILD_JOBS=4 cargo test --profile quick --lib methodize -- --test-threads=1 (11 passed)",
  "cargo test --profile quick --lib module_ingress_snapshots_canonical_policy -- --test-threads=1 (2 passed)",
]
post_audit_focused_evidence = [
  "At 17cfd7cf83, cargo test -- --list proved the previously unrecorded canonical_snapshot_preserves_runtime_static_global_target path exists, and its exact quick-profile invocation passed 1 test with 7498 filtered out.",
]
verification = [
  "CARGO_BUILD_JOBS=4 cargo check --profile quick --lib --features vm-reference",
  "CARGO_BUILD_JOBS=4 cargo check --profile quick --all-targets",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh method_corridor_explicit_compat_ingress_i0",
  "bash tools/checks/current_state_pointer_guard.sh",
  "git diff --check",
]
closeout = "I0 is landed and pushed. Full cargo test/link was not run because the repository's documented memory/OOM stop applies; focused tests, quick all-targets, vm-reference feature check, and row/pointer guards are the recorded evidence."
next_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
allowed_files = [
  "src/config/env/builder_flags.rs",
  "src/mir/builder/compilation_context.rs",
  "src/mir/builder/module_lifecycle.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/calls/unified_emitter/physical_receipt_tests.rs",
  "src/mir/builder/module_lifecycle_capture_tests.rs",
  "src/runner/stage1_bridge/env/runtime_defaults.rs",
  "tools/hakorune_emit_mir.sh",
  "src/tests/mir_static_box_naming.rs",
  "docs/reference/environment-variables.md",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "tools/checks/guard_rows.toml",
]
forbidden_files = [
  "src/mir/builder/calls/method_resolution.rs",
  "src/runner/mir_json_emit/root.rs",
  "lang/src/mir/builder/internal/builder_config_box.hako",
  "lang/src/mir/builder/func_lowering/call_methodize_box.hako",
  "tools/selfhost/mainline/build_stage1.sh",
  "tools/selfhost/lib/stage1_contract.sh",
  "VM/backend/optimizer/Call schema/receiver repair",
]

[method_corridor_nonstage1_producer_retire_d0_2026_08_29]
status = "landed_exhausted"
task_id = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
guard_phase = "method_corridor_nonstage1_producer_retire_d0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-R0"
ceremony_tier = "T1"
implementation_permission = false
decision = "Enumerate and retire the finite non-Stage1 Method(None) producer, reissuer, consumer, and terminal boundary. Rust exact-1 ingress and its shared reissuer are retired; the unchanged Stage1/Hako/JSON writers remain outside this boundary as named owners. No default, name, registry, receiver, or argument inference may be promoted to target authority."
source_authority = "An exact same-brand callable/source relation or an already-issued typed publication handoff; this design row issues no new semantic product."
canonical_issuer = "Existing qualified Global/Method(Some) publication owners, if an exact relation is proven; otherwise the route remains pre-effect rejected or ParkedSealed."
non_authority = [
  "current_static_box + has_method",
  "Method(None) literal or methodize flag",
  "name/arity, StaticMethodId, registry, singleton, physical symbol, func/ValueId, args[0], autoscan, VM/backend, JSON/Hako output",
  "Stage1 artifact failure, stale binary, wrapper/argv substitution, fallback, retry, and test-only fixtures",
]
census_boundary = "RawBrandCallAuthorityV1 non-Stage1 origin or the now-retired Rust methodize ingress -> pre-effect reject, exact successor, or first MirInstruction::Call insertion; includes the four named R2 origins, the shared Resolved consumer, method_resolution_static_none, the Builder Method(None) publication terminal, the retired unified-emitter reissuer, and the already-closed installed Main/nonbrand preflight terminals; excludes the unchanged Stage1/Hako/JSON writers, VM/backend/native consumers, Method(Some) semantics, and Call.func/Option<Callee> schema retirement."
current_disposition = "Exhausted"
census_status = "exhausted"
safe_i1 = false
in_scope_inventory_count = 11
outside_inventory_count = 8
open_questions = []
safe_i1_reason = "The parent is exhausted; no further implementation row belongs to this boundary. The next selected work is the separate same-module publisher design stop."
required_evidence = [
  "finite producer/reissuer/terminal inventory with start/end boundary and includes/excludes",
  "caller and writer census for method_resolution_static_none, with Stage1 exact-1 separated",
  "typed disposition per row: exact successor, pre-effect reject, or ParkedSealed with reopen trigger",
  "no new semantic Verified*/Prepared* receipt, resolver, target lookup, or production code change",
  "focused source/guard evidence that the inventory is exhaustive within its stated boundary",
]
ordered_tasks = [
  "D0-A: trace each in-scope origin to its first producer, shared Resolved consumer, and terminal; record caller counts without changing source",
  "D0-B: prove or disprove an exact same-brand source/catalog successor for each open row; name the missing issuer instead of inferring one",
  "D0-C: choose one typed pre-effect terminal or exact successor per open row, then remeasure writer, reissuer, consumer, and caller-zero evidence",
  "D0-D: close the census only when in-scope blockers are zero and outside cohorts remain owner/reopen-triggered ParkedSealed",
  "I1: open one producer edge only after D0 is Exhausted; include positive/negative tests, reusable guard, and old-edge deletion in the same bounded slice",
]
acceptance = "D0 may close only when the structured in-scope inventory is Exhausted and every in-boundary CutoverBlockerOpen row has either an exact successor or a pre-effect typed terminal with caller-zero evidence. Every outside row has an owner, non-authority, ParkedSealed disposition, and observable reopen trigger. Until then this row remains an accepted design stop and I1 stays closed; no blocker may be silently parked."
no_safe_slice = "Remain in design_stop if any non-Stage1 producer lacks an exact source/catalog issuer, if a caller crosses the R2 compatibility fence, if method_resolution must be changed to discover the relation, or if the proposed successor requires name/arity/registry/receiver/argument inference, fallback, retry, VM/backend, JSON/Hako, or Call-schema changes."
next_implementation = "none__parent_exhausted__see_MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
allowed_files = [
	"docs/development/current/main/CURRENT_STATE.toml",
	"docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
	"docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
	"src/mir/builder/calls/README.md",
	"tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
]
forbidden_files = [
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/runner/mir_json_emit/root.rs",
  "VM/backend/optimizer/Call schema/receiver repair",
]

d0_a_status = "complete_recount"
d0_a_observed_commit = "5ed21ada52"
d0_a_boundary = "four R2 origin issuers and shared dispatch -> shared Resolved construction/consumer -> method_resolution_static_none production caller, with every in-scope producer/reissuer/terminal row counted by direct sites and upstream classes"
d0_a_remaining_rows = []
d0_b_status = "complete"
d0_b_boundary = "recounted producer/reissuer/terminal -> exact successor, pre-effect typed reject, or open blocker"
d0_b_open_rows = []
d0_b_closed_rows = [
  "installed_app_main_affine_successor",
  "installed_nonbrand_pre_effect_reject",
  "unclassified_preflight_reject",
  "raw_root_main_origin",
  "raw_script_root_origin",
  "script_root_origin",
  "raw_legacy_origin",
  "method_resolution_static_none",
  "unified_emitter_methodize_reissuer",
  "builder_method_none_publication_terminal",
  "resolved_compatibility_consumer",
]
d0_c_status = "complete"
d0_c_observed_commit = "3391ecca92"
d0_c_result = "All 11 in-boundary rows have a named exact successor or typed pre-effect retirement. RawRootMain ordinary direct injection now terminates at RawRootMainRetired, and the caller-zero Resolved variant/consumer plus every exclusive resolver/recovery/name-Const/tail-env observer are physically deleted at 598530d23b. No replacement target was issued."
d0_c_open_rows = []
d0_c_decision_options = [
  "A: open a separate source-backed issuer design that supplies exact owner/site/brand/relation and an already-issued typed target within the same transaction",
  "B: retire the two remaining caller-zero compatibility consumer/terminal rows without replacement, after classifying their test-only witnesses and non-Rust callers",
]
d0_c_selected_option = "B_complete"
d0_c_option_b_scope = "B is landed for ScriptRoot, RawScriptRoot, RawLegacy, RawRootMain, method_resolution_static_none, Rust unified-reissuer, Builder Method(None), and the caller-zero Resolved corridor. It authorizes no replacement target."
d0_c_option_a_fate = "NoSafeSlice: Installed App Main is the only reusable complete source-owner/site/brand/relation/package/loan chain. ScriptRoot and RawScriptRoot lack a bare FunctionCall target product, RawLegacy lacks owner/site/brand/package/target, and RawRootMain is already caller-zero upstream. A would require a second source scan or resolver."
d0_c_language_policy = "ScriptRoot, RawScriptRoot, RawRootMain, and RawLegacy are internal compatibility provenance, not Canonical language semantics or a public Compat2025 profile. No new caller may depend on their post-argument resolver behavior."
d0_c_retirement_order = [
  "script_root_origin",
  "raw_legacy_origin",
]
d0_c_next_action = "Parent boundary is Exhausted at 598530d23b. Select MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 as a separate design stop; no Method corridor implementation remains open."

post_static_none_ret0_observed_commit = "c44376532e"
post_static_none_ret0_recount = "The static-none producer is retired with structural branch=0 and production reach=0. The shared Resolved producer/consumer remains structurally present but its only production origin is RawRootMain, whose ordinary FunctionCall is rejected upstream; the remaining direct witnesses are test-only. Rust exact-1 rejects before mutation, its policy carrier and unified-emitter Method(None) reissuer are zero, and the Builder terminal now rejects receiverless Method before publication."
resolved_consumer_post_ret0_structural_producer_sites = 0
resolved_consumer_post_ret0_production_reachable_callers = 0
resolved_consumer_post_ret0_consumer_definitions = 0
resolved_consumer_post_ret0_test_only_witnesses = []
resolved_consumer_post_ret0_fate = "Retired at 598530d23b. The three direct witnesses now assert RawRootMainRetired or retained special precedence with zero argument/MIR effects."
final_retirement_commit = "598530d23b"
unified_reissuer_user_decision = "Accepted and landed at 287e944fe8: Rust exact-1 HAKO_MIR_BUILDER_METHODIZE=1 is a named typed ingress retirement, not Stage1 provenance; the shared reissuer and policy carrier are deleted."
d0_e_next_action = "Complete: the RawRootMain typed pre-effect terminal and exact old-edge delete set landed at 598530d23b."
d0_e_recommended_decision = "Retire the production-caller-zero RawRootMain Resolved compatibility edge without replacement; preserve str/math/special/Cataloged/AppMain precedence and do not add a target issuer or resolver."
d0_e_no_safe_slice = "Without that decision, any selective non-Stage1 reissuer removal would rely on env/request inference and is not an executable slice."

[rust_exact1_methodize_retire_decision_2026_08_30]
status = "accepted"
task_id = "MIR-CALL-RUST-METHODIZE-RETIRE-D0"
implementation_permission = false
decision = "Retire the Rust-side HAKO_MIR_BUILDER_METHODIZE=1 success contract. Exact 1 must fail with a named typed retirement error at both Rust module ingress paths before Builder mutation. Unset and exact 0 remain Canonical."
source_authority = "The one-time Rust module-ingress selector parser is the failure owner; incoming CanonicalGlobalTargetV1 remains the target authority."
non_authority = "The env string, Stage1 writer identity, request class, name/arity, RuntimeData classification, Method(None), VM/backend, JSON/Hako, and test fixtures are not target or provenance authority."
fail_fast_boundary = "prepare_module and prepare_normal_default_module reject exact 1 before prepare_module_with_callable_main_policy, catalog/tail-index clearing, current_module creation, argument descent, or MIR publication."
finite_state = [
  "unset or exact 0 -> Canonical acceptance with no stored methodize policy",
  "exact 1 -> RetiredExplicitCompatibility typed error",
  "other Unicode or empty -> InvalidSelector typed error",
  "non-Unicode -> NonUnicode typed error",
]
ordered_rows = [
  "MIR-CALL-RUST-METHODIZE-RETIRE-GUARD-S0: behavior-neutral private guard split",
  "MIR-CALL-RUST-METHODIZE-RETIRE-TEST-S0: behavior-neutral module-ingress test-home split",
  "MIR-CALL-RUST-METHODIZE-RETIRE-I0: exact-1 ingress reject plus policy carrier and reissuer deletion",
  "closeout: reissuer/Method(None) writer zero recount; Builder terminal and dormant Resolved remain separate blockers",
]
non_claims = "No Stage1/Hako/JSON writer change, artifact construction, VM/backend, Call schema, builder_emit terminal, shared Resolved cleanup, resolver, provenance receipt, fallback, or retry."

[rust_exact1_methodize_retire_guard_s0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-RUST-METHODIZE-RETIRE-GUARD-S0"
parent_row = "MIR-CALL-RUST-METHODIZE-RETIRE-D0"
ceremony_tier = "T0"
box_kind = "BoxShape"
implementation_permission = false
decision = "Move Method corridor/current-row handlers from the 755-line active guard into one private sibling. Preserve the public shell entrypoint, registry row, phase routing, diagnostics, and behavior."
allowed_files = [
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
acceptance = "The public guard entry/registry remain unchanged; parent and sibling are each below 760 lines; the parent Method D0 and static-none landed checks produce the same result; the new S0 row is recognized; no Cargo, source, test, semantic, or production file changes."
implementation_commit = "f70e43daa9"
observed_line_counts = "active parent 624; private Method corridor sibling 287"
validation = [
  "python3 -m py_compile tools/checks/lib/mir_call_d1b_active_surface_guard.py tools/checks/lib/mir_call_d1b_method_corridor_guard.py: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "historical Method D0 in-memory handler replay: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
next_row = "MIR-CALL-RUST-METHODIZE-RETIRE-TEST-S0"

[rust_exact1_methodize_retire_test_s0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-RUST-METHODIZE-RETIRE-TEST-S0"
parent_row = "MIR-CALL-RUST-METHODIZE-RETIRE-D0"
ceremony_tier = "T0"
box_kind = "BoxShape"
implementation_permission = false
decision = "Move the minimal module-ingress smoke and four methodize ingress tests from the 859-line capture test owner into one focused private sibling without changing names or behavior."
allowed_files = [
  "src/mir/builder/module_lifecycle_capture_tests.rs",
  "src/mir/builder/module_lifecycle_ingress_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
acceptance = "Both test files are below 800 lines, existing five test names are unchanged and listed exactly once, focused tests pass, and production source is unchanged."
implementation_commit = "50b7acf97b"
observed_line_counts = "capture owner 772; focused ingress sibling 94"
validation = [
  "cargo test --lib module_lifecycle_ingress_tests -- --test-threads=1: 10 passed; 0 failed",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
red_classification = "The first parallel focused run produced two current-run failures because the existing process-wide garbage selector test overlapped the env-neutral minimal smoke in the two pre-existing module inclusions. The unchanged tests pass 10/10 with the env-sensitive focused family serialized; no production or test meaning was changed in this BoxShape row."
next_row = "MIR-CALL-RUST-METHODIZE-RETIRE-I0"

[rust_exact1_methodize_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-RUST-METHODIZE-RETIRE-I0"
parent_row = "MIR-CALL-RUST-METHODIZE-RETIRE-D0"
ceremony_tier = "T1"
box_kind = "BoxCount retirement"
implementation_permission = false
decision = "Reject exact 1 at both Rust module ingress paths, delete BuilderMethodizeCompatibilityV1 and CompilationContext policy plumbing, and delete the unified-emitter Global-to-Method(None) reissuer in one atomic semantic row."
allowed_files = [
  "src/config/env/builder_flags.rs",
  "src/mir/builder/module_lifecycle.rs",
  "src/mir/builder/compilation_context.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/calls/unified_emitter/physical_receipt_tests.rs",
  "src/mir/builder/module_lifecycle_ingress_tests.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "docs/development/current/main/design/stage1-mir-dialect-contract-ssot.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
focused_test_filters = [
  "config::env::builder_flags::methodize_ingress_tests",
  "module_lifecycle_ingress_tests",
  "calls::unified_emitter::physical_receipt_tests",
]
old_edge_delete_set = [
  "exact 1 -> ExplicitLegacyCompatibility success",
  "CompilationContext builder_methodize_compatibility carrier",
  "unified emitter RuntimeData Global -> Method(receiver=None) reissuer and trace",
  "old exact-1 success tests",
]
acceptance = "Exact 1 has one named typed pre-mutation error; unset/0 keep typed Global; invalid/non-Unicode remain distinct; Rust reissuer and policy carrier are zero; Stage1/Hako/JSON/VM/Call schema are unchanged; focused test/list coverage and the existing guard are green."
implementation_commit = "287e944fe8"
post_implementation_census = "BuilderMethodizeCompatibilityV1=0; builder_methodize_compatibility field/readers=0; ExplicitLegacyCompatibility=0; builder_methodize_trace=0; unified-emitter RuntimeData Global-to-Method(None) reissuer=0; excluded Stage1 exact-1 writer=1 unchanged."
validation = [
  "cargo check --profile quick --lib: pass",
  "cargo test --lib config::env::builder_flags::methodize_ingress_tests -- --test-threads=1: 2 passed",
  "cargo test --lib module_lifecycle_ingress_tests -- --test-threads=1: 10 passed",
  "cargo test --lib mir::builder::calls::unified_emitter::physical_receipt_tests -- --test-threads=1: 8 passed",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
next_row = "Return to parent Method corridor for Builder terminal hardening and dormant Resolved test-witness fate."

[builder_method_none_publication_terminal_ret0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-BUILDER-METHOD-NONE-PUBLICATION-RET0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
box_kind = "BoxCount retirement"
implementation_permission = false
decision = "Reject Callee::Method(receiver=None) at the sole Builder instruction-publication terminal. Rust Builder literal writers are zero after the exact-1 unified reissuer retirement; no replacement target or route is introduced."
source_authority = "An instance Method must carry an exact receiver. A static method remains an already-issued qualified Global. The typed Callee is the only terminal input authority."
non_authority = "func/ValueId, args[0], box/method spelling, registry, current_static_box, RuntimeData classification, JSON/Hako/Stage1, VM/backend, tests, fallback, and retry."
fail_fast_boundary = "MirBuilder::emit_instruction rejects receiverless Method before receiver materialization, mutable function borrow, metadata observation, block insertion, or publication."
census_boundary = "Rust Builder Callee::Method literal construction -> MirBuilder::emit_instruction receiver materialization -> first block insertion; includes Builder production/test sources and excludes JSON/Hako emitters, VM/backend consumers, Method(Some), Global, and Call schema."
precondition_census = "After 287e944fe8, multiline structural scan finds zero Callee::Method { receiver: None } literals under src/mir/builder; the former sole production reissuer and stored policy carrier are zero."
old_edge_delete_set = [
  "MirBuilder::emit_instruction silently accepts Callee::Method(receiver=None)",
  "Builder terminal relies on a later backend or compatibility consumer to interpret a missing receiver",
]
allowed_files = [
  "src/mir/builder/builder_emit.rs",
  "src/mir/builder/builder_method_none_terminal_tests.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
acceptance = "Receiverless Method returns the stable retired terminal with zero instruction delta; typed Global still publishes; no production Method(None) writer, replacement target, resolver, or Call schema change appears; focused tests, quick check, guard, and pointer are green."
implementation_commit = "cc2d390538"
post_implementation_census = "Rust Builder Method(None) literal writers=0; unified reissuer=0; emit_instruction typed terminal=1; typed Global positive=1; JSON/Hako/Stage1 and VM/backend excluded owners unchanged."
validation = [
  "cargo test --lib builder_method_none_terminal_tests -- --test-threads=1: 2 passed",
  "cargo check --profile quick --lib: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
next_row = "Return to parent design_stop for the dormant Resolved consumer and its three test-only witnesses."

[raw_root_resolved_caller_zero_ret0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-RAW-ROOT-RESOLVED-CALLER-ZERO-RET0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
box_kind = "BoxCount retirement"
implementation_permission = false
decision = "Replace the dormant RawRootMainParkedCompatibility -> Resolved success edge with one payload-free RawRootMainRetired terminal after Brand/TypeOp/Math/FastMem/exact-str precedence, then delete PreparedRawOrdinaryFunctionCompletionV1::Resolved and every exclusive post-argument resolver/recovery/name-Const/tail-env observer. No replacement target is issued."
source_authority = "RawBrandCallAuthorityV1::RawRootMainParkedCompatibility is the exact test-injectable provenance. Production authority remains the raw-root eligibility owner, which rejects FunctionCall as UnsupportedSurface before physical open. prepare_ordinary_function_completion_v1 owns the first origin-specific terminal after special precedence."
canonical_issuer = "None: this is caller-zero retirement. CatalogedTargeted and AppMainTargeted keep their existing exact issuers; the new RawRootMainRetired terminal carries no target payload."
non_authority = [
  "name/arity, variable map, callable registry/catalog recovery, current box, headers, arguments/ValueId, func/name Const, physical symbol, test fixture, env selector",
  "resolve_call_target, unique-static recovery, suffix/tail recovery, Builder autoscan, fallback, retry, VM/backend, JSON/Hako, and Call schema",
]
fail_fast_boundary = "Within prepare_ordinary_function_completion_v1, preserve Brand routing and TypeOp/Math/FastMem/exact str/1 selection first; ordinary non-special RawRootMain then returns RawRootMainRetired before argument descent, function-header access, resolver/recovery, Const creation, collector mutation, or MIR publication."
census_boundary = "RawRootMainParkedCompatibility issuance -> ordinary completion selection -> Resolved argument descent/resolver/recovery -> first Call insertion; includes the one variant, one producer, one consumer, zero production callers, direct RawRootMain test injections, exclusive generic resolver and tail-recovery helpers/env/guards; excludes Brand/TypeOp/Math/FastMem/exact str/1, CatalogedTargeted, AppMainTargeted, Method(Some), VM/backend, JSON/Hako, and Call schema."
precondition_census = "PreparedRawOrdinaryFunctionCompletionV1::Resolved: variant=1, producer=1 RawRootMain, consumer=1, production reachable callers=0. build_resolved_function_call=1 and build_unified_function_call=1 are exclusive consumers. Direct Resolved expectations are test-only."
old_edge_delete_set = [
  "RawRootMainParkedCompatibility -> PreparedRawOrdinaryFunctionCompletionV1::Resolved",
  "Resolved match arm -> argument descent -> function-header access",
  "build_resolved_function_call and build_unified_function_call",
  "generic resolve_call_target wrapper/module and call_resolution suggestion module",
  "unique-static emission recovery plus module/header tail resolver helpers",
  "NYASH_BUILDER_TAIL_RESOLVE Rust reader and auto-detect writer",
  "Resolved-specific name Const plus direct legacy MirInstruction::Call writer",
  "guards/manifests/proofs that require Resolved or tail recovery as current authority",
]
allowed_files = [
  "src/config/env/builder_flags.rs",
  "src/mir/builder.rs",
  "src/mir/builder/call_resolution.rs",
  "src/mir/builder/calls/mod.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_script_compatibility_tests.rs",
  "src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/static_resolution.rs",
  "src/mir/builder/calls/utils.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "tools/checks/mir_call_d1b_exact_target_child_guard.sh",
  "tools/checks/lib/bare_static_recovery_proof.py",
  "tools/checks/lib/headerport_authority_erasure_guard.py",
  "tools/checks/lib/mir_call_global_target_b1_c0_matrix_guard.py",
  "tools/checks/lib/mir_call_global_target_b1_cutover_guard.py",
  "tools/checks/manifests/mir_call_global_target_b1_c0_matrix.toml",
  "tools/smokes/v2/configs/auto_detect.conf",
  "apps/bare-static-recovery-proof/README.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
acceptance = "RawRootMain ordinary non-special direct injection returns the stable RawRootMainRetired terminal with zero argument/header/MIR delta; Brand/TypeOp/Math/FastMem/exact str/1 precedence remains; Cataloged and AppMain typed paths remain green; Resolved, its exclusive build helpers, generic resolver, unique/tail emissions, name-Const writer, tail env reader/writer, and stale guard expectations are zero; focused tests, quick check, active/exact-target/headerport/recovery guards, pointer, and diff check are green."
no_safe_slice = "Return to design_stop if any production caller crosses raw-root eligibility, any public compatibility contract owns Resolved success, a second Resolved producer/consumer appears, a supposedly exclusive resolver/recovery helper has another live owner, special precedence changes, or retirement requires a replacement target, argument effect, VM/backend, JSON/Hako, or Call-schema change."
non_claims = [
  "no new target issuer, Recipe, Verified/Prepared receipt family, resolver, fallback, or retry",
  "no CatalogedTargeted/AppMainTargeted/Method(Some) semantic change",
  "no production selection credit; this removes a caller-zero compatibility corridor",
  "no VM/backend, optimizer, JSON/Hako, Stage1, or Call.func/Option<Callee> schema change",
]
changed_test_names = [
  "raw_root_main_ordinary_call_retires_before_arguments",
  "raw_root_main_special_precedence_survives_resolved_retirement",
  "raw_root_main_gc_name_retires_before_arguments",
]
focused_test_filters = [
  "raw_root_main_",
  "cataloged_target_preflight_applies_total_shadow_order",
  "app_main_direct_call",
]
implementation_commit = "598530d23b"
post_implementation_census = "PreparedRawOrdinaryFunctionCompletionV1::Resolved=0; RawRootMain Resolved producer=0; Resolved consumer=0; build_resolved_function_call=0; build_unified_function_call=0; generic resolve_call_target=0; unique-static emission recovery=0; tail/suffix resolver=0; Resolved name-Const writer=0; NYASH_BUILDER_TAIL_RESOLVE Rust reader/writer=0. RawRootMainRetired is the sole ordinary non-special direct-injection terminal after the retained special precedence."
validation = [
  "cargo test --profile quick --lib raw_root_main_ -- --test-threads=1: 6 passed",
  "cargo test --profile quick --lib cataloged_target_preflight_applies_total_shadow_order -- --test-threads=1: 1 passed",
  "cargo test --profile quick --lib app_main_direct_call -- --test-threads=1: 3 passed",
  "cargo check --profile quick --lib: pass",
  "cargo build --profile quick --features vm-reference --bin hakorune: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/mir_call_d1b_exact_target_child_guard.sh: pass",
  "python3 tools/checks/lib/headerport_authority_erasure_guard.py .: pass",
  "python3 tools/checks/lib/mir_call_global_target_b1_c0_matrix_guard.py .: pass",
  "python3 tools/checks/lib/mir_call_global_target_b1_cutover_guard.py .: pass after removing the two retired adapter users from the exact expected set",
  "python3 tools/checks/lib/bare_static_recovery_proof.py .: pass with nonzero focused catalog tests",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
red_classification = "Workspace-wide cargo fmt -- --check reports the pre-existing unrelated formatting debt in src/runner/core_executor.rs, which this row did not touch. Targeted rustfmt --check for every changed Rust file passed. The stale resolved_callable_l0 guard fails on its unrelated historical with_resolved_function_draft_session contract and is not acceptance evidence for this bounded row. No full-suite green claim is made."
next_row = "The parent Method corridor is Exhausted. The SameModule producer inventory is reconciled at nine production families; the exact static receipt child landed at e5120589dc and the remaining eight families stay blocker-open."

[same_module_all_producer_disposition_r0_2026_08_30]
status = "accepted_with_open_blockers"
task_id = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
guard_phase = "same_module_all_producer_disposition_r0"
ceremony_tier = "T1"
implementation_permission = false
decision = "Treat the former three labels as semantic families, not three files. Finitely classify every Rust MirBuilder SameModule target issuer/reconstructor from its first source/owner product through typed Global publication or pre-effect reject. Exact same-brand keys may project one target; String, env, physical symbol, candidate/header lookup, and retry may not issue meaning."
source_authority = "A same-session CanonicalSameModuleCallableKeyV1 or an already-issued exact typed target owned by the selected source/catalog row."
canonical_issuer = "CanonicalSameModuleCallableKeyV1::canonical_global_target_v1 for exact static keys; each other family must name its existing exact issuer or retire before effects."
non_authority = [
  "owner/name/arity text, formatted MIR symbol, CorePlan func:String, env flags, current_static_box, candidate/header lookup, StaticMethodId parse",
  "typed_global_target_from_selected_symbol, resolver replay, physical thunk symbol, test fixture, fallback, retry, VM/backend, JSON, and Call schema",
]
fail_fast_boundary = "Without an exact issued target, reject before child/argument/operand descent, local_arg or Const emission, collector/block mutation, Call insertion, or result publication."
census_boundary = "Rust src/mir/builder source/owner product that first issues or reconstructs a SameModule Global target -> exact CanonicalGlobalTargetV1 enters CallTarget::Global/Callee::Global or the route reaches a typed pre-effect terminal; includes 10 compatibility-adapter callsites, 5 non-adapter production constructor classes, exact-key projection, and the key-dropping receipt terminal; excludes typed-target consumers, Builtin(Print), Extern, Method(Some), NewBox construction semantics, canonical_direct_call, JoinIR, wire/JSON, VM/backend, Call schema, and tests."
inventory_status = "finite_reconciled__9_production_families__1_test_only_outside"
production_family_count = 9
outside_family_count = 1
current_disposition = "CutoverBlockerOpen"
worker_audit_current_head = "e7d57b448a"
worker_audit_current_refresh = "The static current-owner I0 is now landed at 2b7b3e7489. Three fresh read-only audits at HEAD e7d57b448a agree that true DeclaredInstance me.method is a separate live family: its clean MIR is Method(Some(receiver)) with N source arguments, but no existing same-session issuer co-seals source/site, InstanceBoxMethod declaration, catalog/package brand, bound receiver, ABI, body/return, and effect facts. Preserve the language feature and do not replace the missing issuer with header/name/registry/backend inference."
worker_audit_result = "Read-only audits at audit HEAD 22e3aff4f5 confirm the print caller-zero retirement and ordinary-static target-only I0 are closed. The exact target inventory remains keyed by (caller, SourceExprSiteV1), and target-only rows now retain the existing canonical key without a second scan or resolver. The Builder operator-call selector family is now retired at 44555655ab: one typed validator owns all five compiler ingresses, direct MIR remains the sole compiler route, and runtime-only selectors stay separate. Physical thunk has a finite two-caller boundary and an existing source/physical relation but no identified exact same-session main/0 issuer; selected-exact CorePlan has no production caller; generic ordinary-static has an exact Cataloged handoff only and no source/site/brand route for its remaining compatibility terminal; generic CorePlan GlobalCall is production-live on the legacy scheduler with no source/owner/brand issuer before its func:String/local_arg parse-back. Three fresh audits assign legacy registry V0/V1/debt retirement to the existing portable Loop M10b cutover after accepted-corpus coverage, while the separate source-backed canonical V1 path remains live; this Call family is ParkedSealed and GlobalCall-only deletion is NoSafeSlice. Rewrite/known has landed as the user-selected policy retirement at 7a6fb9e2db, with typed Method(Some(receiver)) retained and no parity claim introduced. The ordinary-new exact constructor cutover landed at d81d91d800; no-claim compatibility remains a separate parked cohort. Bare error retired at 69680b983f and bare now retired at 3e35e4f39c through the canonical unsupported pre-effect terminal. At that audit head panic/exit were both NoIssuer; LANG-PANIC-TERMINAL-FAULT-D0 later accepted panic as a parked terminal-Fault target while exit remains NoIssuer. The 2026-08-30 me.method correction selects one bounded static-current-owner handoff I0 and parks the true declared-instance source/ABI co-seal as its own design row."
latest_worker_audit_head = "a2d2ed9de4"
latest_worker_audit_result = "A fresh read-only audit finds no safe production implementation row under the parent R0. Static current-owner, provider, physical thunk, CorePlan, and panic families are already landed, parked, or issuer-missing. The next local task is DeclaredInstance D0: reconcile source site/owner, InstanceBoxMethod declaration, same-session brand, bound receiver, receiver ABI, result/effect facts, and a sole co-seal issuer; until that relation exists, keep implementation_permission=false and do not infer from header/name/registry/backend or add fallback/retry."
ordered_tasks = [
  "I0 (landed at e5120589dc): exact static receipt — project the existing canonical key before arguments in exactly three production callers; make the receipt terminal a target consumer only",
  "I1-I0 (landed at 2b7b3e7489): StaticCurrentOwner me.method — exact target is consumed before header/arguments and emitted as Global(StaticBoxMethod) without a receiver prefix",
  "I1-D0 (selected design stop): true DeclaredInstance me.method — resolver-owned exact declaration relation, result/effect/full-lane carriage, selected C capability, then Method(Some(receiver)); no package/name/header/registry inference",
  "I2-D0 (ParkedSealed under M10b): generic CorePlan — keep the six GlobalCall writers and legacy registry V0/V1 route unchanged in the Call lane; reopen only at portable Loop M8/M9/precutover deletion-manifest readiness or an exact earlier source-target co-seal",
  "I3 (landed at 44555655ab): arithmetic/comparison/unary env publishers — classify/prune the mixed-age type-fact guard, reject retired selectors at all compiler ingress methods, and atomically remove env/name target issuance before operand or Const effects",
  "I4-D0 (ParkedSealed): physical thunk issuer closure — the finite two-caller audit found RelationPresentIssuerMissing; reopen only with an existing same-session main/0 issuer or an accepted typed pre-effect retirement contract",
  "I4-I0: only after D0 names an issuer, consume the exact header/entry relation without symbol parse-back",
  "I5 (landed at 7a6fb9e2db): rewrite/known policy retirement — remove the optional Known/Unique/equals instance-to-Global writers and selector authority while retaining typed Method(Some) and the early str-like route; historical parity is evidence only",
  "I6 (landed at d81d91d800): ordinary new birth — keep construction authority separate from SameModule call publication; no-claim compatibility remains parked",
  "I7-D0 (landed): cataloged provider-as-Free caller-present census — separate print, GC, math, Extern, and bare-name cohorts; bare error and bare now received one-name retirements and no provider-wide implementation row is open",
  "I7-D0b (design stop): bare error retired at 69680b983f and bare now retired at 3e35e4f39c using the existing unsupported contract; bare panic/exit still have no exact issuer and require separate language/provider decisions",
  "I7-I0: one named provider cohort only after its exact issuer or typed pre-effect retirement is closed; name+arity synthesis is not authority",
  "C0: rerun two-direction producer census; close only when open/reopened blockers are zero and the test-only row remains ParkedSealed",
]
acceptance = "The broad R0 closes only when all nine production families have an exact successor or typed pre-effect retirement, adapter and non-adapter counts agree in both directions, every outside row has owner/non-authority/reopen trigger, and no publisher is silently parked. One bounded child may land without claiming broad closure when its own start/end boundary is exhaustive."
no_safe_slice = "Return to design_stop if a live publisher cannot name an exact issuer or typed terminal, a CorePlan/operator caller cannot be finitely enumerated, target identity still needs String/env/symbol/lookup inference, effects cannot move behind the reject without package/loan/schema/backend work, or the change crosses 760/800 lines."
non_claims = "No B1/D1 package-loan, R6 Call schema, Method(Some), constructor/NewBox meaning, JoinIR, JSON, VM/backend, source rescan, second resolver, fallback, or retry."
smallest_next_slice = "StaticCurrentOwner is landed. The selected child is MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0. Its source relation is landed; ordinary effect issuance, full-lane selected C capability, and common affine consumption remain design blockers."
call_schema_dependency = "R6 remains a later atomic schema row: first close unified-off and emit_legacy_call alternate routes, typed-writer normalization, constructor/closure Call producers, JoinIR func authority, and Callee-only consumers; then delete Call.func, make callee mandatory, and remove Call-specific INVALID. R6 issues no target and cannot unblock a SameModule family lacking an exact issuer."
guard_cleanup_dependency = "The 2026-08-30 guard census found no additional SupersededDelete candidate beyond the already-landed stale type-fact overlay removal. Keep/rehome/park all other guards until finite invocation/profile coverage, caller-zero or an equal successor, focused proof, and a reopen trigger are recorded."
guard_cleanup_status = "ParkedSealed__no_current_deletion_candidate"
guard_cleanup_inventory = "117 registered rows and 2646 unregistered surfaces were sampled; the only proven SupersededDelete was the stale type-fact whole-writer overlay already removed. Pilot-only, unregistered, compatibility-navigation, and red-only guards are not deletion authority."
guard_cleanup_task = [
  "G-D0: when a guard family is next touched, record its finite entry/profile/consumer boundary and classify each row Keep, Rehome, SupersededDelete, or ParkedSealed",
  "G-S0: for SupersededDelete only, prove caller-zero or an equal/stronger successor, remove the guard/test/index claim together, and add an observable reopen trigger",
  "G-C0: run the surviving guard through its declared profile and a focused positive/negative proof; a named path alone is not evidence",
  "Do not open a standalone guard-retirement row until G-D0 yields at least one proven SupersededDelete; otherwise keep this task parked and continue the selected compiler row",
]

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "exact_static_receipt"
owner = "method_call_terminal.rs plus script_direct_static_physical_bridge.rs, static_result_publication_physical_bridge.rs, and script_physical_exit/direct_static_entry_kernel.rs"
disposition = "SuccessorContractSealed"
evidence = "three production callers now project CanonicalSameModuleCallableKeyV1 before descent and the target-only receipt terminal landed at e5120589dc; the remaining eight production families stay open"

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "ordinary_static_terminal"
owner = "src/mir/builder/method_call_handlers.rs and src/mir/builder/calls/method_call_terminal.rs"
disposition = "CutoverBlockerOpen"
evidence = "exact Cataloged rows are closed. StaticCurrentOwner me.method landed at 2b7b3e7489. True DeclaredInstance me.method is the selected design blocker and lacks the live resolver relation, result/effect/full-lane carriage, selected C capability, and common affine consumer. Scalar/inline and generic compatibility remain separate owners."

[same_module_ordinary_static_terminal_d0_2026_08_30]
status = "accepted_design_stop"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-TERMINAL-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxCount finite ordinary-static authority census"
implementation_permission = false
disposition = "CutoverBlockerOpen"
decision = "Keep generic ordinary-static, scalar, and inline compatibility unchanged. StaticCurrentOwner is landed. True DeclaredInstance is the selected D0; retain its syntax and build a resolver-owned Method(Some(receiver)) successor without projecting through the static issuer or repairing from owner/method/arity."
source_authority = "StaticCurrentOwner uses VerifiedCurrentOwnerStaticCallTargetV1 issued from exact caller/site/catalog and Canonical Me under a StaticBoxMethod declaration. True DeclaredInstance requires its exact resolved source row, InstanceBoxMethod declaration, same package/catalog brand, bound receiver, and physical ABI; generic, scalar, and inline branches retain separate owners."
canonical_issuer = "For StaticCurrentOwner, VerifiedWholeSourceStaticCallTargetInventoryV1 and VerifiedStaticCallResultPublicationOwnerV1 already carry the exact CanonicalSameModuleCallableKeyV1 to Builder. For true DeclaredInstance, no complete same-session target/ABI issuer is yet named; no new receipt, resolver, or Global projection is allowed in this D0."
non_authority = [
  "owner/method/arity, func_name, first-Box header classification, static scalar facts, inline catalog, current_static_box, generated symbol, registry, resolver, fallback, retry, and Instance-to-Global projection",
]
fail_fast_boundary = "StaticCurrentOwner must take one exact target or reject before header observation, child/argument descent, ValueId/Const emission, collector mutation, or Call publication. True DeclaredInstance must remain unchanged until its exact source/catalog/receiver/ABI co-seal exists. Generic/scalar/inline order remains unchanged."
census_boundary = "member_route exact Cataloged handoff and generic/static fallback entries -> handle_static_method_call_with_descent -> method_call_terminal; includes generic, scalar, inline, lowered-global Static/Instance, and existing exact Cataloged exclusions; excludes Method(Some) semantics, rewrite/known, CorePlan, ordinary-new, provider, physical thunk, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "exact Cataloged ingress: src/mir/builder/calls/member_route.rs:99-141",
  "generic handler: src/mir/builder/method_call_handlers.rs:423-482",
  "static fallback entry: src/mir/builder/method_call_handlers.rs:303-305",
  "generic terminal: src/mir/builder/calls/method_call_terminal.rs:116-164,282-335",
  "scalar and inline terminals: src/mir/builder/method_call_handlers.rs:440-457",
  "lowered-global Static/Instance path: src/mir/builder/method_call_handlers.rs:310-375",
]
finite_states = [
  "ExactCataloged: existing source/site/brand handoff, already closed by target-only I0",
  "GenericCompatibility: production-live formatted target reconstruction after argument descent; CutoverBlockerOpen",
  "ScalarOwned: zero-argument fact/Const owner; keep separate from direct-call target lane",
  "InlineOwned: helper-inline body/argument owner; keep separate from direct-call target lane",
  "StaticCurrentOwnerExact: landed direct Global(StaticBoxMethod) successor with N source arguments",
  "DeclaredInstanceOpen: production-live instance me.method; clean target is Method(Some(receiver)) with N source arguments, but exact source/catalog/receiver/ABI co-seal is still CutoverBlockerOpen",
]
ordered_tasks = [
  "D0-A: split StaticCurrentOwner and true DeclaredInstance by existing receiver policy and declaration namespace; do not classify them from headers",
  "I0-A: landed at 2b7b3e7489; StaticCurrentOwner exact target is consumed before header/arguments and its selected legacy edge is gone",
  "D0-B: issue true DeclaredInstance target identity in the resolver, then co-seal result/effect/full-lane contracts without a package-side resolver or family-specific loan",
  "I0-B: after D0-B, issue Method(Some(receiver)) with N source arguments and a selected LLVM/AOT direct-call projection; do not add nonselected backend features",
  "R0: after both successors are caller-zero complete, retire first-Box header classification, manual me prefix, formatted Global reissuer, and superseded tests/guards",
]
acceptance = "The ordinary-static boundary is finite and two-directionally reconciled; exact Cataloged, StaticCurrentOwner, true DeclaredInstance, generic compatibility, scalar, and inline are separately owned; the me.method language feature is retained; no name/arity/header/symbol inference is elevated; and each implementation opens only with a named source product and pre-effect boundary."
no_safe_slice = "Return to design_stop if StaticCurrentOwner cannot reach the existing publication owner before header observation, if true DeclaredInstance requires header/name/registry inference or a new family-specific receipt, if scalar/inline/generic semantics would move, if an instance target must be projected into Global, if a post-effect reject is the only option, or if the change crosses VM/backend, Call schema, or 760/800 lines."
non_claims = "No me.method language retirement, no generic/scalar/inline rewrite, no immediate true-instance cutover, no Method(None)/Resolved change, no rewrite/known/provider/CorePlan/ordinary-new work, no VM/backend/JSON feature expansion, Call schema, fallback, or retry."
evidence = "At audit HEAD 2a84b1fff0, exact Cataloged rows use the existing source-keyed handoff; generic production callers enter handle_static_method_call_with_descent and method_call_terminal after argument/effect processing, scalar/inline have independent terminals, and lowered-global Static/Instance remain production-live without an exact typed issuer."
worker_audit_head = "5b6e6397a9"
worker_audit_result = "Six read-only audits at HEAD 5b6e6397a9 found the prior bucket too broad. StaticBoxMethod -> StaticCurrentOwner already issues VerifiedCurrentOwnerStaticCallTargetV1 and the installed publication owner can project it to a direct Global before arguments. InstanceBoxMethod -> DeclaredInstance is separate: the resolved source row and declared-instance contracts exist, but their target/receiver/ABI/effect relation is not yet co-sealed into Builder."
reopen_trigger = "True DeclaredInstance is reopened as an in-bound CutoverBlockerOpen design row. Implementation opens only after the resolver relation, result/effect/full-lane carriage, selected C capability, and common affine lifecycle are all named; header/name/registry inference is never a trigger."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-EFFECT-ISSUER-D0__design_stop"

[mir_call_me_declared_instance_effect_issuer_d0_2026_08_31]
status = "accepted_design_stop"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-EFFECT-ISSUER-D0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
development_state = "DesignReadyForBoundedI0"
implementation_permission = false
ceremony_tier = "T1"
box_kind = "BoxCount one source-backed semantic-effect issuer"
decision = "Issue the ordinary DeclaredInstance effect row from the same resolver-owned source relation used by the landed target relation. No CallableContract means OpaqueObservable; one explicit Query contract takes precedence and remains a declared obligation rather than a body-inference result."
source_authority = "ExplicitMethodSourceRelationV1.callable_contract carried through the existing final parser source, joined to the landed VerifiedDeclaredInstanceCallRelationV1 in the same callable-semantic batch HRTB"
canonical_issuer = "DeclaredInstanceCallEffectIssuerV1, a resolver-side sibling issuer invoked immediately after the relation issuer; package assembly only co-seals and transports the already-issued row"
non_authority = [
  "AST re-read, FunctionSignature, MIR EffectMask, Dynamic envelope, backend/runtime metadata, package-side defaults, headers, names, registry lookup, fixtures, fallback, and retry",
]
fail_fast_boundary = "Missing, duplicate, foreign, outside, or cardinality-drifted contract carriage rejects the whole batch before receiver materialization, argument effects, package publication, MIR, or backend admission; Query never downgrades silently to OpaqueObservable."
census_boundary = "final parser callable-contract source disposition -> same-session DeclaredInstance effect issuer -> callable-semantic batch sibling field; includes direct root InstanceBoxMethod rows with absent or Query contracts, and excludes target/receiver ValueId, full physical lane, selected C capability, package loan, Method(Some), Call schema, and nonselected backends"
finite_states = [
  "OutsideDirectDeclaredInstanceMethod: explicit absence outside the exact root relation; not an effect candidate",
  "NoRootDeclaredInstanceCall: relation has no eligible root call; issue no effect row",
  "PublishedOpaqueObservable: exact direct instance relation with no CallableContract",
  "PublishedDeclaredQuery: exact direct instance relation with one Query contract and its source ordinal",
  "MissingOrDuplicateContractSource: exact relation has zero or multiple source dispositions; typed batch reject",
  "ForeignOrDriftedContractSource: provenance, owner, site, nominal identity, or cardinality differs; typed batch reject",
  "UnsupportedContractVariant: exhaustive match forces a design reopen; no wildcard downgrade",
]
allowed_files = [
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "src/parser/callable_contract_syntax.rs",
  "src/parser/initial_callable_program_source/issue.rs",
  "src/parser/initial_callable_program_source/model.rs",
  "src/parser/normal_callable_program_source/model.rs",
  "src/parser/normal_callable_program_source/transform.rs",
  "src/parser/normal_callable_program_source/semantic_syntax_loan.rs",
  "src/mir/resolved_semantics/declared_instance_call_effect.rs",
  "src/mir/resolved_semantics/mod.rs",
  "src/mir/callable_semantic_batch/issuer.rs",
  "src/mir/callable_semantic_batch/model.rs",
  "focused tests and module README/reference receipt",
]
forbidden_files = [
  "src/mir/resolved_semantics/declared_instance_call_relation.rs",
  "src/mir/normal_callable_semantic_package/**",
  "src/mir/builder/**",
  "MIR/backend/runtime/C call emission",
  "Call schema, Method(Some) production, package loan, fallback, retry",
]
acceptance = "The existing final parser source carries an exhaustive contract disposition with the same source/slot coverage as the callable rows; the resolver-side effect issuer emits exactly OpaqueObservable or DeclaredQuery for each eligible relation, retains no AST/target/physical data, and batch publication is all-or-nothing with typed coverage/provenance failures. No production target, receiver, ABI, package, MIR, backend, or old-edge change is claimed."
no_safe_slice = "Remain in design_stop if the callable contract cannot be carried without source re-scan, if no same-session resolver owner can issue the effect row, if Query conformance is required in this slice, or if package/backend/MIR changes are needed."
non_claims = "No package co-seal, selected-C capability, affine loan, receiver materialization, Method(Some) publication, legacy deletion, Call schema, VM/backend parity, or Result/throw/panic change."
next_design_action = "After this D0 is accepted, open LANG-ORDINARY-DECLARED-INSTANCE-CALL-EFFECT-ISSUER-I0; keep Method(Some) production closed until effect, full lane, selected C, and common affine consumption are co-sealed."
worker_audit_head = "825a23610f"
worker_audit_result = "The read-only effect audit found an existing parser callable-contract source and a single same-session batch HRTB hook immediately after relation issuance. Absence maps to OpaqueObservable, Query is explicit, and no package-side or backend-derived effect issuer is allowed."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "generic_coreplan_globalcall"
owner = "control_flow/plan GlobalCall writers and lowerer/effect_emission.rs"
disposition = "ParkedSealed"
evidence = "Six production writer arms construct CorePlan GlobalCall(func:String); the live legacy-registry lowerer consumes them after local_arg and parse-backs the string. Its V0/V1/debt retirement is owned by portable Loop M10b after accepted-corpus coverage. The source-backed canonical V1 path is a separate retained semantic owner, and the selected exact emission_port is a caller-zero sibling rather than an authorized bridge."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "selected_exact_coreplan"
owner = "control_flow/plan/lowerer/emission_port.rs"
disposition = "ParkedSealed"
evidence = "The existing canonical key/claim chain is structurally exact, but the selected composer and claim consumer have no production caller at HEAD; its reachability child is ParkedSealed and the parent family remains open until a live owner appears."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "operator_env_publishers"
owner = "ops/arithmetic.rs, ops/comparison.rs, ops/unary.rs"
disposition = "RetiredCallerZero"
evidence = "The three-selector Builder operator-call route landed at 44555655ab. A single typed compiler-ingress validator now rejects explicit truthy, malformed, and non-Unicode legacy selectors before effects; unset/false values preserve direct MIR, and the four publisher bodies, downstream Builder reads, prelude coupling, and repo-owned selector writers are removed. Runtime-only NYASH_OPERATOR_BOX_* remains separate."
retirement_commit = "44555655ab"
reopen_trigger = "A new production Builder operator-call publisher or selector writer appears, or an explicit source-backed compatibility owner is approved."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "physical_thunk"
owner = "normal_module_transaction/physical_thunk.rs"
disposition = "ParkedSealed"
evidence = "The exact header/entry relation exists, but no same-session main/0 Callee issuer exists; the dedicated closure D0 therefore parks this family and keeps the current symbol parse-back unchanged until an issuer or explicit typed replacement contract is accepted."
reopen_trigger = "A same-session source-backed main/0 Callee issuer is introduced, or an explicit typed retirement/replacement contract for the physical entry is accepted."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "rewrite_known"
owner = "rewrite/known.rs; production ingress unified_emitter.rs"
disposition = "RetireByPolicy"
evidence = "the user selected retirement of this optional legacy optimization; the existing typed Method(Some) route is the sole canonical successor, no Global issuer is allowed, and the historical ON/OFF matrix is retained without being a gate"

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "ordinary_new_birth"
owner = "ordinary_new_admission.rs and new_expression.rs"
disposition = "ParkedSealed"
evidence = "The selected-normal exact constructor cutover landed at d81d91d800: parser ConstructorSourceIdV1, package Birth(N)/NoBirthZero recipe, shared checked N/N+1 ABI, and pre-argument recipe consumption replace the former birth-text/AST-scan/class-arity reconstruction. No-claim compatibility and other constructor families remain separate parked cohorts."

[[same_module_all_producer_disposition_r0_2026_08_30.inventory]]
id = "cataloged_provider_as_free"
owner = "calls/function_call_preflight_route.rs::prepare_cataloged_target_v1"
disposition = "CutoverBlockerOpen"
evidence = "Caller-present gc_collect/gc_stats now leave through the existing pre-effect GcGlobal retirement at 9e166e5974; bare error and bare now leave through named unsupported retirements at 69680b983f and 3e35e4f39c, while bare panic/exit still have no exact issuer or language/provider decision."
reopen_trigger = "A named bare provider issuer or an explicit typed pre-effect retirement/language decision is accepted for the remaining bare names."

[[same_module_all_producer_disposition_r0_2026_08_30.outside_inventory]]
id = "test_only_static_publication"
owner = "normal_script_direct_static_physical_publication.rs"
disposition = "ParkedSealed"
non_authority = "test fixture publication is not a production issuer"
reopen_trigger = "a non-test production caller or acceptance claim appears"

[same_module_ordinary_static_cataloged_target_only_d0_2026_08_30]
status = "accepted_design_ready_for_i0"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-CATALOGED-TARGET-ONLY-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
primary_missing_boundary = "MaterializationRelationMissing"
implementation_permission = false
decision = "The existing exact source/static target rows can be retained as an owner-local target-only, one-shot disposition. Separate ExactTargetOnly from NoExactStaticTarget; never turn a missing result into Absent until exact-target absence is known. This accepts the bounded I0 design but does not open implementation until the parent pointer selects it."
source_authority = "Existing VerifiedWholeSourceStaticCallTargetInventoryV1 rows keyed by the exact source callable/site relation and the same-session CanonicalSameModuleCallableKeyV1; the design row issues no new target."
canonical_issuer = "Existing source/catalog issuer only: CanonicalSameModuleCallableKeyV1::canonical_global_target_v1 for an already-selected exact row. A target-only row may transport that owned product, but may not reconstruct it."
non_authority = [
  "box_name, method, arity, current_static_box, formatted symbol, scalar facts, header/candidate lookup, StaticMethodId, name/arity, env, registry, resolver, retry, fallback, or physical MIR",
  "target-only census counts, test fixtures, and a missing result treated as a default or FreeFunction",
]
fail_fast_boundary = "Exact source/site and catalog relation must be retained or rejected before scalar Const/inline work, child or argument descent, ValueId/Call emission, collector/block mutation, or generic fallback."
census_boundary = "VerifiedWholeSourceStaticCallTargetInventoryV1 issuance -> installed exact-site publication owner -> first scalar/inline/argument/Call effect; includes selected, unselected, absent, unavailable, foreign, duplicate, site/route/arity drift and lowered-global Instance cases; excludes qualified MethodCall already landed, print/GC, CorePlan, operators, thunk, rewrite, ordinary-new, package/loan, VM/backend, JSON, and Call schema."
finite_states = [
  "SelectedResult: one exact target and an existing result handoff exist; consume the existing Selected handoff once",
  "ExactTargetOnly: one exact source/site target exists but no result handoff exists; move the existing canonical key once",
  "NoExactStaticTarget: no exact static target exists for the source site; use a typed pre-effect terminal, never fallback",
  "Unavailable: no Cataloged owner/capability exists; remain blocker-open until a named successor or retirement is chosen",
  "ForeignOrDuplicate: brand/owner/site relation is not unique; typed pre-effect reject",
  "Drift: route/arity/site relation differs; typed pre-effect reject, no repair",
]
ordered_tasks = [
  "D0-A (closed): reconcile both production exact-target issuers with the publication owner's dropped-row behavior; target rows are keyed by (caller, SourceExprSiteV1)",
  "D0-B (closed): retain an owner-local exact-target map plus selected-result subset and consumed set; add no new semantic receipt and perform no second observation",
  "D0-C (closed): freeze the pre-effect boundary for scalar/inline/argument descent and lowered-global Instance; ExactTargetOnly transports an existing key, all other missing/foreign/drift states reject or remain blocker-open",
  "I0: add one-shot target-only handoff, consume arguments once, and delete only the ordinary-static formatted-name/fallback edge; keep consumer switch and broader families out of scope until the parent opens I0",
]
acceptance = "D0 is accepted when every finite state has one named owner and terminal, ExactTargetOnly is distinct from NoExactStaticTarget, both issuer directions are reconciled, and I0 is expressible as source -> existing target product -> one consumer -> fail-fast boundary without a new resolver or receipt."
no_safe_slice = "Remain in design_stop if exact rows are dropped before the owner, result/effect relation is missing, lowered-global Instance needs a new issuer, any name/symbol/lookup inference is required, or old fallback cannot be deleted atomically."
non_claims = "No ordinary static implementation, no target publication, no argument/Call cutover, no package/loan, no Method(Some), no CorePlan/operator/thunk/rewrite/provider/VM/backend/JSON/Call-schema change."
next_execution = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-CATALOGED-TARGET-ONLY-I0__pending_parent_pointer_switch"

[same_module_ordinary_static_cataloged_target_only_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-CATALOGED-TARGET-ONLY-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
prerequisite = "same_module_ordinary_static_cataloged_target_only_d0_2026_08_30"
implementation_permission = false
base_commit = "7d4da344b5"
decision = "When explicitly selected in fast mode, retain the existing exact target product in the current publication owner as a private one-shot TargetOnly disposition. No target is reconstructed and no result/effect is inferred. The current parent design stop remains selected until the pointer is intentionally switched."
source_authority = "Existing VerifiedSourceStaticCallTargetCatalogV1 exact (caller, SourceExprSiteV1) target and same-session catalog brand."
canonical_issuer = "Existing CanonicalSameModuleCallableKeyV1::canonical_global_target_v1 only; I0 transports the owned key and adds no issuer."
non_authority = [
  "Unselected/Absent labels as authority, result absence as target absence, owner/method/arity strings, name/arity lookup, formatted symbols, resolver/retry/fallback, test fixtures, and physical MIR",
]
fail_fast_boundary = "Brand, exact site, StaticBoxMethod kind, arity, and one-shot consumption are checked before scalar/inline/child/argument effects. Unknown, duplicate, foreign, drift, or residual states terminate without retry."
census_boundary = "Existing exact target issue -> owner-local TargetOnly/SelectedResult disposition -> ingress take -> first ordinary-static effect; excludes all other SameModule families and all Call-schema/backend work."
allowed_files = [
  "src/mir/callable_result_representation/static_call_result_publication_owner.rs",
  "src/mir/callable_result_representation/tests/static_call_result_publication_owner.rs",
  "src/mir/builder/static_result_publication_ingress.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/member_route.rs",
  "src/mir/builder/calls/static_result_publication_physical_bridge.rs",
  "src/mir/builder/calls/method_call_terminal.rs",
  "src/mir/builder/calls/mod.rs",
  "src/mir/builder/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_same_module_target_only_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
planned_focused_tests = [
  "exact target with result handoff remains Selected and is consumed once",
  "exact target without result handoff becomes TargetOnly and is consumed once",
  "no exact target remains NoExactStaticTarget and rejects before scalar/inline/argument effects",
  "foreign, duplicate, wrong-kind, wrong-arity, second-take, and residual states reject",
  "ordinary-static target is selected before arguments and arguments are lowered once",
]
structural_guard = "Existing active-surface/target-before-args guards must prove exact target rows are not silently dropped, TargetOnly is private and one-shot, no new resolver/name lookup exists, no result/effect is inferred, and every changed source file remains below 760 lines."
implementation_commit = "3e7f4a1d7f"
changed_test_names = [
  "source_keyed_take_rejects_wrong_site_and_foreign_catalog",
  "exact_source_target_without_an_i64_result_stays_target_only",
  "state_vocabulary_keeps_unavailable_and_no_exact_target_distinct",
]
focused_test_filters = [
  "static_call_result_publication_owner",
  "static_result_publication_ingress",
  "static_result_publication",
]
focused_test_results = [
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 cargo test --profile quick --lib static_call_result_publication_owner: 5 passed, 0 failed, 7503 filtered out",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 cargo test --profile quick --lib static_result_publication_ingress: 3 passed, 0 failed, 7505 filtered out",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 cargo test --profile quick --lib static_result_publication: 10 passed, 0 failed, 7498 filtered out",
]
focused_check = "cargo check --profile quick --lib: passed before the focused test runs; the focused test binary also compiled and ran with the same bounded environment."
guard_result = "current_state_pointer_guard.sh, mir_call_d1b_active_surface_guard.py, and git diff --check passed; the target-only active guard checks the landed owner/ingress/consumer tokens and projection-before-descent order."
rustfmt_result = "rustfmt --edition 2021 on all eight touched Rust files passed"
line_count_result = "all touched Rust owners remain below 760 lines; method_call_handlers.rs is 637 lines and the target-only guard is 120 lines"
old_edge_delete_set = [
  "exact target rows previously dropped when no result handoff existed",
  "StaticCallResultPublicationTakeV1::Unselected and StaticResultPublicationIngressV1::Absent collapsed state",
  "cataloged target-only ingress falling through to generic compatibility fallback",
]
acceptance = "The existing exact source/static target is retained as a private one-shot TargetOnly disposition when no result handoff exists; Selected rows remain affine; NoExactStaticTarget is distinct and typed; target-only consumers project the owned target before argument descent and lower arguments once; no target resolver/fallback is re-entered."
non_claims = "No Method(None) producer retirement, no shared Resolved/unique/tail/name-Const cleanup, no package/loan, no Call schema, no VM/backend, and no other SameModule family changes."

[same_module_static_receipt_target_before_args_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-STATIC-RECEIPT-TARGET-BEFORE-ARGS-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T1"
box_kind = "BoxShape authority retirement"
implementation_permission = false
decision = "In exactly three production receipt callers, project the already-owned CanonicalSameModuleCallableKeyV1 into CanonicalGlobalTargetV1 before argument/operand descent. Pass that owned typed target to the existing receipt terminal; delete the terminal's owner/method/arity constructor and formatted-symbol field."
source_authority = "The caller-owned same-session CanonicalSameModuleCallableKeyV1, already validated as StaticBoxMethod with exact arity."
canonical_issuer = "CanonicalSameModuleCallableKeyV1::canonical_global_target_v1, called once per production edge before descent."
non_authority = "owner/method/arity strings, PreparedGlobalValueCallRequestV1, formatted symbol, arguments, physical receipt, resolver, target adapter, env, VM/backend, and tests."
fail_fast_boundary = "namespace, arity, malformed target, or projection failure returns before AssociatedMethodCallArgumentsV1::lower_all, lower_node, ValueId/Const/Call emission, or result publication."
census_boundary = "three callers holding CanonicalSameModuleCallableKeyV1 -> key projection -> argument/recipe descent -> emit_static_global_value_terminal_with_receipt_v1 -> physical Call receipt; includes only the receipt-required terminal and its tests; excludes ordinary static compatibility terminal, generic CorePlan, operators, rewrite, physical thunk, birth/provider, Method(Some), VM/backend, and Call schema."
precondition_census = "production receipt callers=3; direct receipt tests=3; receipt terminal target constructors=1; receipt terminal formatted-symbol fields=1; all three callers possess the exact canonical key before descent."
old_edge_delete_set = [
  "receipt caller key -> owner/method/arity strings after descent",
  "emit_static_global_value_terminal_with_receipt_v1 owner/method/arity parameters",
  "PreparedGlobalValueCallRequestV1 symbol field and CanonicalGlobalTargetV1::new_static_box_method reconstruction",
]
allowed_files = [
  "src/mir/builder/calls/method_call_terminal.rs",
  "src/mir/builder/calls/method_call_terminal_tests.rs",
  "src/mir/builder/calls/script_direct_static_physical_bridge.rs",
  "src/mir/builder/calls/static_result_publication_physical_bridge.rs",
  "src/mir/builder/script_physical_exit/direct_static_entry_kernel.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "tools/checks/lib/mir_call_global_target_b1_cutover_guard.py",
  "tools/checks/manifests/mir_call_global_target_b1_c0_matrix.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
planned_focused_tests = [
  "static receipt accepts a preissued typed target and the receipt destination equals the physical Call",
  "instance namespace, wrong arity, empty owner/name, and projection failure reject before argument/recipe descent",
  "each of the three production bridges projects the target before its first descent and lowers arguments exactly once",
  "unified-disabled and failed-emission negatives remain no-retry and issue no receipt",
]
structural_guard = "Existing active-surface/B1 guards must prove three production callers, target projection before descent, a target-only receipt signature, no constructor or formatted symbol in the receipt terminal, no adapter growth, and every touched source below 760 lines. No new guard family."
acceptance = "Exactly three production receipt callers project an owned typed target before descent; the terminal consumes that target without owner/name/arity reconstruction; target/argument/receipt/publication equality remains; positive and all pre-effect negatives pass; arguments lower once; Call publishes once; no other SameModule family changes."
no_safe_slice = "Return to design_stop if a caller lacks the exact key before descent, projection needs text/symbol/lookup, ordinary static fallback must change, a new receipt/resolver is needed, any fourth production caller appears, or the slice crosses B1/D1/R6/VM/schema or 760/800 boundaries."
non_claims = "No broad producer closure, ordinary static fallback, generic CorePlan, operators, rewrite, physical thunk, birth/provider, package-loan, Method(Some), VM/backend, JSON, or Call schema."
implementation_commit = "e5120589dc"
coverage_base_commit = "0817830877"
changed_test_names = [
  "static_global_receipt_publishes_the_owned_typed_target",
]
focused_test_filters = [
  "static_global_receipt",
]
focused_test_results = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=256 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo test --profile quick --lib static_global_receipt -- --test-threads=1: 3 passed, 0 failed, 7502 filtered out",
  "CARGO_BUILD_JOBS=2 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 cargo check --profile quick --tests --lib: passed",
]
guard_result = "mir_call_global_target_b1_cutover_guard.py and mir_call_global_target_b1_c0_matrix_guard.py passed; receipt-specific active-surface validation is updated for the landed typed-target terminal and remains to be run after the pointer closeout."
rustfmt_result = "rustfmt --edition 2021 on the five touched Rust files passed"
resource_note = "The default optimized test-binary link was stopped twice at approximately 6.5 GiB RSS to avoid the repository's 8 GiB OOM safety boundary; the same focused tests executed green with quick opt-level 0, and no full-suite result is claimed."
post_census = "exactly 3 production receipt callers; receipt terminal consumes CanonicalGlobalTargetV1; PreparedGlobalValueCallRequestV1 and CanonicalGlobalTargetV1::new_static_box_method are absent from the receipt function; all three callers project before descent; touched Rust owners are 459/467/160/53/248 lines and the guard is 697 lines."
next_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design stop; select the next exact SameModule family only after a fresh authority audit."

[same_module_cataloged_print_caller_zero_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PRINT-CALLER-ZERO-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T1"
box_kind = "BoxCount caller-zero retirement"
implementation_permission = false
decision = "The ordinary InstalledNonBrand Cataloged print branch has no production ASTNode::FunctionCall caller. Retire that compatibility branch before target synthesis; preserve the dedicated ASTNode::Print owner, which already emits the existing builtin_print() target for its one expression."
source_authority = "The parser's dedicated ASTNode::Print carries exactly one expression, and CanonicalGlobalTargetV1::builtin_print() is the existing typed issuer. The census itself issues no target."
canonical_issuer = "Existing CanonicalGlobalTargetV1::builtin_print() only; no name/arity constructor, classifier, catalog-existence check, resolver, symbol, or VM consumer may issue print."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "source name/arity and CanonicalGlobalTargetV1::new_free_function",
  "CalleeResolverBox validation, physical symbol, registry, env, fixture, fallback, and retry",
]
fail_fast_boundary = "At InstalledNonBrand Cataloged ordinary preflight, after existing TypeOp/Math/FastMem precedence and before argument descent, ValueId/Const allocation, block mutation, resolver entry, or Call publication."
census_boundary = "InstalledNonBrand Cataloged raw FunctionCall name=print -> prepare_cataloged_target_v1 -> first target/reject terminal; includes parser/ordinary-call caller reconciliation and focused preflight tests, excludes ASTNode::Print's existing statement owner, Math, ExplicitExtern, GC, VM/backend, wire, and Call schema."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "src/mir/builder/calls/README.md",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_cataloged_print_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "ordinary static terminal",
  "generic CorePlan",
  "operators",
  "physical thunk",
  "rewrite/known",
  "ordinary-new birth",
  "panic/exit/error/now issuer",
  "Math/Extern",
  "package/loan",
  "VM/backend/JSON/Call schema",
]
ordered_tasks = [
  "I0: issue a typed CatalogedPrintRetired terminal before target synthesis, child descent, ValueId/Const allocation, or block mutation",
  "I0: update the direct preflight test to prove caller-zero retirement and preserve the dedicated ASTNode::Print owner; do not change print statement lowering",
  "C0: rerun the provider census and return to the SameModule parent; do not claim provider-wide closure",
]
acceptance = "The ordinary Cataloged print boundary has zero production FunctionCall callers, emits one typed caller-zero retirement before target synthesis and child descent, preserves the dedicated ASTNode::Print -> builtin_print() owner, and removes the old print name-synthesis edge without a new provider or semantic receipt."
no_safe_slice = "Return to design_stop if a production FunctionCall print caller appears, the dedicated ASTNode::Print owner is not the sole source route, or retirement would require name/arity, resolver, symbol, fallback, retry, VM, wire, or Call-schema authority."
selected_cohort = "print_cataloged_caller_zero_retire"
caller_reconciliation = "The parser's production print token is lowered to ASTNode::Print by src/parser/statements/io_async.rs; a source-wide scan found no production ASTNode::FunctionCall constructor with name=print. The InstalledNonBrand Cataloged print branch is therefore test/compatibility injection only at this boundary. ASTNode::Print remains owned by stmts/print_stmt.rs and is excluded from this retirement."
caller_reconciliation_evidence = [
  "src/parser/statements/io_async.rs:30-45 parses print as ASTNode::Print with one expression",
  "src/mir/builder/raw_expression_dispatch/mod.rs:345-358 is the sole production FunctionCall preflight ingress",
  "source-wide production scan found no name: \"print\" FunctionCall constructor; remaining matches are tests, JSON/MIR projections, or the ASTNode::Print owner",
]
decision_after_d0 = "Do not add a second ordinary-call print issuer. Retire the caller-zero Cataloged print branch; preserve the dedicated ASTNode::Print -> builtin_print() owner."
next_execution = "Return to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design stop for a fresh authority audit; do not claim provider-wide closure."
implementation_commit = "5014903def"
coverage_base_commit = "c38548776a"
changed_test_names = [
  "cataloged_print_caller_zero_retires_before_target_synthesis",
  "cataloged_print_rejection_does_not_descend_or_publish",
  "cataloged_target_preflight_applies_total_shadow_order",
]
focused_test_filters = [
  "cataloged_print",
  "cataloged_target_preflight_applies_total_shadow_order",
  "mir::builder::stmts::print_stmt::tests::",
]
focused_test_results = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo test --profile quick --lib cataloged_print -- --test-threads=1: 2 passed, 0 failed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo test --profile quick --lib cataloged_target_preflight_applies_total_shadow_order -- --test-threads=1: 1 passed, 0 failed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo test --profile quick --lib mir::builder::stmts::print_stmt::tests:: -- --test-threads=1: 3 passed, 0 failed",
]
focused_gate = "The three exact filters above must each report a nonzero run and zero failures; cargo check --profile quick --lib uses the same single-job memory-safe environment."
focused_check = "cargo check --profile quick --lib: passed with CARGO_BUILD_JOBS=1, CARGO_INCREMENTAL=0, CARGO_PROFILE_QUICK_CODEGEN_UNITS=1, CARGO_PROFILE_QUICK_OPT_LEVEL=0"
guard_result = "current_state_pointer_guard.sh, mir_call_d1b_active_surface_guard.py, and git diff --check passed while the row was fast-open"
line_count_result = "function_call_preflight_route.rs=745; function_call_preflight_route_tests.rs=740; function_call_installed_gc_builtin_tests.rs=180; all touched source owners remain below the 760-line split boundary"
old_edge_delete_set = [
  "InstalledNonBrand Cataloged FunctionCall name=print -> CatalogedTargeted target synthesis",
  "ordinary Cataloged print branch -> child descent / target publication",
]
caller_zero_claim = "Production ordinary FunctionCall print callers remain zero; the only production print source is ASTNode::Print, whose dedicated owner remains unchanged."
full_suite_note = "Full cargo test was not run; the exact focused filters and quick library check above are the recorded evidence."

[method_call_method_resolution_static_none_ret0_d0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-METHOD-RESOLUTION-STATIC-NONE-RET0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
implementation_permission = false
decision = "Remove only the dormant current_static_box + has_method branch that issues receiverless Callee::Method. Its sole structural caller is the shared Resolved consumer, whose production reach is zero after RawRootMain eligibility rejects FunctionCall before physical open. No replacement target, terminal, resolver, or new receipt is introduced."
source_authority = "The upstream RawRootMain eligibility owner and the already-landed RawLegacy/RawScriptRoot/ScriptRoot terminals prove that this method-resolution branch has no production success owner."
canonical_issuer = "None: this is an unplaced caller-zero retirement, not a target issuance row. Exact catalog/source handoffs remain owned by the existing qualified publication owners."
non_authority = [
  "current_static_box, has_method, name/arity, StaticMethodId, registry, receiver, args, func/ValueId, physical symbol, resolver/recovery, fallback, retry",
  "test-only direct resolution witnesses and historical compatibility wording",
]
fail_fast_boundary = "Existing upstream RawRootMain eligibility and named compatibility terminals stop ordinary FunctionCall before physical open or argument effects; this row only removes the unreachable static-none resolver branch and its local API plumbing."
census_boundary = "method_resolution.rs static-none branch -> its sole MirBuilder wrapper -> the one build.rs Resolved callsite; includes the local test witness and excludes build.rs/Resolved changes, Method(None) reissuers, Builder publication, VM/backend, JSON/Hako, Stage1, and Call schema."
production_reach_before = "structural method_resolution static-none branch = 1; wrapper caller = 1; production-reachable success caller = 0 because the only Resolved producer is RawRootMain and its FunctionCall is rejected upstream; test-only witnesses remain explicit."
old_edge_delete_set = [
  "method_resolution.rs current_static_box + has_method -> Callee::Method { receiver: None }",
  "is_commonly_shadowed_method and generate_self_recursion_warning helpers used only by that branch",
  "has_method helper and the current_static_box argument in the method-resolution wrapper",
  "test expectation that bare length resolves to receiverless Method",
]
allowed_files = [
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/utils.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "src/mir/builder/calls/README.md",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/builder_emit.rs",
  "shared Resolved consumer, Method(None) reissuers, VM/backend, JSON/Hako, Stage1, and Call schema",
]
focused_gate = "CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_BUILD_JOBS=2 cargo test --profile quick --lib mir::builder::calls::method_resolution::tests:: -- --test-threads=1"
focused_test_filters = [
  "mir::builder::calls::method_resolution::tests::",
]
changed_test_names = [
  "method_resolution_never_issues_receiverless_static_method",
]
acceptance = "The static-none branch, has_method, self-recursion warning helpers, and current_static_box resolver argument are removed; builtin Print, local Value, and Extern resolution remain unchanged; the new negative test is listed and passes; shared Resolved/build.rs and all Method(None) downstream writers remain untouched."
implementation_commit = "fea43bdade"
coverage_base_commit = "e9fa9145d3"
focused_test_results = [
  "CARGO_INCREMENTAL=1 CARGO_PROFILE_QUICK_CODEGEN_UNITS=256 CARGO_BUILD_JOBS=2 cargo test --profile quick --lib mir::builder::calls::method_resolution::tests:: -- --test-threads=1: 2 passed, 0 failed",
]
guard_result = "mir_call_d1b_active_surface_guard.py static-none checks passed before closeout; parent guard was updated to require the landed RET0 and three remaining blockers."
rustfmt_result = "rustfmt --edition 2021 on method_resolution.rs and utils.rs passed"
stop = "Return to design_stop if any production caller reaches this branch, if the wrapper is needed outside build.rs, if an exact source/catalog issuer is required, or if implementation touches build.rs, shared Resolved, Method(None) reissuers, VM/backend, JSON/Hako, Stage1, or Call schema."
next_row = "Return to design_stop for the remaining Resolved consumer, Builder Method(None) terminal, and unified-emitter reissuer rows; no broad Method corridor implementation is implied."

[method_call_compat_raw_script_retire_i0_2026_08_29]
status = "landed"
task_id = "MIR-CALL-COMPAT-RAW-SCRIPT-RETIRE-I0"
guard_phase = "raw_script_root_pre_effect_retire_i0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
box_kind = "BoxCount retirement: remove one internal compatibility success shape"
implementation_permission = false
decision = "After existing Brand, TypeOp, Math, FastMem, and str/1 precedence, an ordinary RawScriptRoot FunctionCall has no source-backed target issuer and enters one origin-specific typed retirement terminal before argument descent. The other three named compatibility origins keep their current Resolved behavior in this slice."
source_authority = "RawBrandCallAuthorityV1::RawScriptRootParkedCompatibility, issued by the existing raw source classifier."
canonical_issuer = "prepare_ordinary_function_completion_v1 is the sole pre-effect terminal issuer after the existing special-form precedence; it issues RawCompatibilityOrdinaryCallTerminalV1::RawScriptRootRetired and no target product."
non_authority = [
  "name/arity, current_static_box, has_method, catalog/registry lookup, receiver, arguments, MIR, ValueId, physical symbol, VM/backend",
  "Resolved, resolve_call_target, unique-static recovery, tail recovery, name Const, compatibility tests, and fallback/retry",
]
fail_fast_boundary = "RawScriptRoot ordinary non-special classification -> typed terminal -> Err, before drive_call_arguments_v1, resolver/header lookup, MIR instruction/function publication, collector/header mutation, fallback, or retry."
finite_state_matrix = [
  "RawScriptRoot + declared Brand -> existing Brand route; unchanged.",
  "RawScriptRoot + TypeOp, Math, FastMem, or str/1 -> existing selected special route; unchanged.",
  "RawScriptRoot + ordinary non-special FunctionCall -> RawScriptRootRetired typed terminal; argument descent 0 and publication 0.",
  "RawRootMain, ScriptRoot, or RawLegacy ordinary FunctionCall -> existing Resolved compatibility route; unchanged in I0.",
  "Installed App Main, installed non-brand, unclassified, Cataloged, extern, and value-call owners -> outside this switch; unchanged.",
]
old_edge_delete_set = [
  "RawScriptRootParkedCompatibility -> PreparedRawOrdinaryFunctionCompletionV1::Resolved",
]
caller_zero_claim = "Only the RawScriptRoot origin-specific Resolved producer becomes zero. The RawScriptRoot classifier remains, and the shared Resolved variant/consumer remains live for the other three origins."
positive_tests = [
  "raw_script_root_ordinary_call_retires_before_arguments: typed origin terminal, stable diagnostic, expression descent 0, event log empty, MIR function/instruction and CompilationContext lowering state zero-delta",
  "raw_script_root_keeps_brand_and_special_precedence: Brand and selected special routes do not enter the retirement terminal",
]
negative_tests = [
  "raw_compatibility_provenance_preserves_resolved_terminal_for_remaining_origins",
  "installed_app_main_and unclassified/source-backed routes remain unchanged",
  "no resolver, recovery, name Const, target lookup, fallback, or retry is introduced",
]
focused_gate = "CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_BUILD_JOBS=4 cargo test --profile quick --lib function_call_preflight_route::tests::script_compatibility_tests -- --test-threads=1; require a nonzero --list match and real passed tests, and treat 0 tests as failure"
focused_test_coverage = "Before closeout, derive the exact script_compatibility_tests paths with cargo test -- --list, record the nonzero list/count, and require every test added or touched by the implementation commit to be present under the recorded filter. A missing name, zero match, or list/run count mismatch blocks closeout."
focused_test_filter = "mir::builder::calls::function_call_preflight_route::tests::script_compatibility_tests"
focused_test_list_count = 9
focused_test_run = "9 passed, 0 failed, 7492 filtered out"
guard = "Reuse mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh; no new guard family or registry row."
done = "The selected edge is 1 -> 0, focused positive/negative tests pass, every changed test is covered by the recorded nonzero filter, route and test files remain below 760 lines, other origins are unchanged, and the existing lifecycle/pointer guards are green."
implementation_evidence = "RawScriptRoot ordinary non-special calls now enter RawCompatibilityOrdinaryCallTerminalV1::RawScriptRootRetired before argument descent; the exact focused cargo test command ran 9 tests with 9 passed."
implementation_commit = "8837cbbab2"
stop = "Return to design_stop if RawScriptRoot cannot be uniquely identified before effects, an external/public compatibility owner appears, special-form precedence changes, a generic String-only terminal is required, or the slice reaches build.rs, method_resolution, unified_emitter, VM/backend, JSON/Hako, or Call schema."
next_row = "MIR-CALL-COMPAT-SCRIPT-ROOT-FATE-D0 after this row closes; do not open it in the same implementation commit."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_script_compatibility_tests.rs",
  "src/mir/builder/calls/README.md",
  "docs/reference/language/function-call-evaluation.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/raw_expression_dispatch/mod.rs",
  "RawCompatibilityChildTerminalPortV1",
  "VM/backend/optimizer/JSON/Hako/Call schema",
]

[raw_root_main_retire_i0_2026_08_29]
status = "superseded_by_resolved_caller_zero_ret0"
task_id = "MIR-CALL-COMPAT-RAW-ROOT-MAIN-RETIRE-I0"
guard_phase = "active_surface"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
implementation_permission = false
decision = "Read-only audit closes the RawRootMain downstream slice as caller-zero: the existing raw-root eligibility owner classifies ordinary FunctionCall as UnsupportedSurface(Call) before physical open. No second RawRootMain terminal is added; reopen only if a new production route crosses that upstream boundary."
source_authority = "RawBrandCallAuthorityV1::RawRootMainParkedCompatibility, issued by the existing raw source classifier."
non_authority = [
  "name/arity, current_static_box, has_method, catalog/registry lookup, receiver, arguments, MIR, ValueId, physical symbol, VM/backend",
  "resolver, unique-static recovery, tail recovery, name Const, fallback/retry, and the RawScriptRoot terminal itself as a cross-origin authority",
]
fail_fast_boundary = "Existing raw-root eligibility -> UnsupportedSurface(Call) before physical open; no downstream RawRootMain FunctionCall terminal is permitted while production reach is zero."
caller_zero_evidence = "The read-only audit found two structural raw caller classes, and production FunctionCall reach = 0: raw-root eligibility maps ordinary Call to UnsupportedSurface(Call) before physical open, so the downstream RawRootMain preflight edge is dormant."
acceptance = "Caller-zero/provenance reconciliation is closed. Reopen only when a new production RawRootMain FunctionCall route reaches preflight or the upstream eligibility owner changes; then remeasure precedence and choose one named terminal or exact successor before implementation."
non_claims = [
  "no RawRootMain code switch or new terminal",
  "no shared Resolved consumer deletion",
  "no ScriptRoot or RawLegacy change",
  "no Method(None), VM/backend, JSON/Hako, or Call-schema change",
]
superseded_by = "MIR-CALL-RAW-ROOT-RESOLVED-CALLER-ZERO-RET0"
supersession_reason = "The earlier row correctly proved production reach zero but retained a test-injectable downstream Resolved success edge. Two independent 2026-08-30 audits found that this edge is the sole remaining Resolved producer and that its exclusive resolver/recovery observers otherwise survive forever. The new row replaces, rather than adds beside, that dormant edge with one typed pre-effect RawRootMainRetired terminal after all existing special-form precedence."

[method_call_compat_script_root_fate_d0_2026_08_29]
status = "selected_retire_before_effects"
task_id = "MIR-CALL-COMPAT-SCRIPT-ROOT-FATE-D0"
guard_phase = "script_root_fate_decision_d0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
implementation_permission = false
decision = "RetireBeforeEffects is selected. ScriptRoot ordinary non-special FunctionCall has no exact source/catalog target product, is not a public compatibility profile, and already has production success reach zero because source-backed Script observation terminates as ObservationDeferred before physical lowering. The remaining downstream Resolved edge is a dormant test-injectable compatibility edge and will be replaced by one origin-specific typed terminal."
source_authority = "RawBrandCallAuthorityV1::ScriptRootParkedCompatibility and the existing R4 ScriptRoot semantic owner"
canonical_issuer = "No new issuer in D0. If retirement is selected, the existing preflight dispatcher is the sole terminal issuer; if retention is selected, the existing R4 owner remains the named compatibility owner."
non_authority = [
  "name/arity, current_static_box, has_method, catalog/registry lookup, receiver, arguments, MIR, ValueId, physical symbol, VM/backend",
  "resolve_call_target, unique-static recovery, tail recovery, Method(None), methodize, fallback/retry, test-only fixtures, or RawLegacy behavior",
]
fail_fast_boundary = "ScriptRoot ordinary non-special classification -> typed terminal -> Err before argument descent, resolver/header lookup, MIR publication, collector mutation, fallback, or retry. The upstream semantic ObservationDeferred boundary and R4 resolver outcome remain unchanged."
finite_state_matrix = [
  "ScriptRoot + declared Brand -> existing Brand route; unchanged.",
  "ScriptRoot + TypeOp, Math, FastMem, or str/1 -> existing selected special route; unchanged.",
  "ScriptRoot + ordinary non-special FunctionCall -> ScriptRootRetired typed terminal; argument descent and publication remain zero.",
  "RawScriptRoot -> existing RawScriptRootRetired terminal; already closed and outside this decision.",
  "RawRootMain -> upstream UnsupportedSurface(Call) caller-zero; unchanged.",
  "RawLegacy -> separate compatibility fate row; unchanged.",
]
decision_options = [
  "RetireBeforeEffects: add one origin-specific typed terminal and update the R4 retention test to assert the intentional retirement; preserves one-way authority and removes the old success edge.",
  "RetainResolved: keep the existing R4 compatibility behavior as ParkedSealed with an explicit owner and reopen trigger; no shared-consumer deletion is allowed yet.",
]
selected_fate = "RetireBeforeEffects"
selection_evidence = "Four independent read-only audits found no public ScriptRoot success contract and no production success caller. The selected Script semantic path rejects ordinary FunctionCall as Deferred/ObservationDeferred before physical open; the sole Resolved witness is a test-only direct authority injection. Building an exact Script issuer is NoSafeSlice because the current source traversal does not distinguish ordinary FreeStatic from TypeOp/Math/FastMem/str special routes and would create a second authority."
semantic_boundary_note = "R4 RetainedExistingTerminal means semantic deferral to the runtime-owned terminal, not successful raw execution. Keep ResolveScriptOutcomeV1::Deferred and ObservationDeferred unchanged; do not rename this selection into Script source activation."
required_evidence = [
  "explicit selected fate and owner/reopen trigger",
  "if RetireBeforeEffects: positive/negative pre-effect tests, updated R4 test meaning, and caller-zero evidence for the ScriptRoot Resolved producer",
  "if RetainResolved: named compatibility owner, source/fixture evidence, and a statement that the shared Resolved consumer remains a blocker",
  "RawLegacy and the shared Resolved consumer remain outside this D0",
]
acceptance = "Closed: RetireBeforeEffects is selected, production success reach is zero, R4 semantic deferral is disjoint from raw success, and the bounded RET0 below is the only open implementation row. D0 issues no target and changes no shared consumer."
non_claims = [
  "no ScriptRoot code switch while this Decision is open",
  "no RawLegacy change",
  "no Method(None), VM/backend, JSON/Hako, or Call-schema change",
  "no Stage1 artifact decision",
]
next_execution = "MIR-CALL-COMPAT-SCRIPT-ROOT-RET0"

[method_call_compat_script_root_ret0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-COMPAT-SCRIPT-ROOT-RET0"
guard_phase = "script_root_pre_effect_ret0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
box_kind = "RET0 BoxCount retirement: remove one dormant internal compatibility success shape; production replacement credit = 0"
implementation_permission = false
implementation_commit = "f1bb472f1d"
decision = "After existing Brand, TypeOp, Math, FastMem, and str/1 precedence, ScriptRoot ordinary non-special FunctionCall enters one origin-specific typed retirement terminal before argument descent. R4 semantic Deferred/ObservationDeferred remains unchanged; no target product is issued."
source_authority = "RawBrandCallAuthorityV1::ScriptRootParkedCompatibility is the sole downstream provenance issuer. The production source path is already fail-closed by NormalScriptPreEffectSourceObservationIssuerV1 before physical lowering."
canonical_issuer = "prepare_ordinary_function_completion_v1 is the sole physical pre-effect terminal issuer and may issue only RawCompatibilityOrdinaryCallTerminalV1::ScriptRootRetired for this origin."
non_authority = [
  "name/arity, callable index, catalog/registry, current_static_box, receiver, arguments, MIR, ValueId, physical symbol, VM/backend",
  "Resolved, resolve_call_target, unique-static recovery, tail recovery, name Const, test-only direct authority injection, RawLegacy, fallback/retry",
]
fail_fast_boundary = "Existing special-call precedence -> ScriptRoot ordinary classification -> ScriptRootRetired -> Err before drive_call_arguments_v1, header/resolver lookup, MIR/function/instruction publication, collector/CompilationContext mutation, fallback, or retry."
finite_state_matrix = [
  "ScriptRoot + declared Brand -> existing Brand route; unchanged.",
  "ScriptRoot + TypeOp, Math, FastMem, or str/1 -> existing selected special route; unchanged.",
  "ScriptRoot + ordinary non-special FunctionCall -> ScriptRootRetired typed terminal; argument descent/header lookup/publication zero.",
  "R4 Script semantic FunctionCall -> existing Deferred then ObservationDeferred before physical open; unchanged.",
  "RawScriptRoot -> existing RawScriptRootRetired; RawRootMain -> upstream caller-zero; RawLegacy -> existing Resolved; all unchanged.",
]
production_reach_census = "Boundary: selected non-App Script ordinary non-special FunctionCall -> pre-effect semantic observation -> raw physical dispatch. structural_sites = 1; production_reachable_callers = 0; test_only_injections = 1; public_contract_owners = 0. Includes the ScriptRoot classifier and downstream preflight edge; excludes declared Brand, special calls, installed App Main, RawLegacy, and VM/backend."
old_edge_delete_set = [
  "ScriptRootParkedCompatibility -> PreparedRawOrdinaryFunctionCompletionV1::Resolved",
]
caller_zero_claim = "The ScriptRoot origin-specific Resolved producer becomes structurally zero. The ScriptRoot provenance classifier remains for Brand/special precedence and the new terminal; shared Resolved remains live for RawLegacy."
positive_tests = [
  "ScriptRoot ordinary direct-injection fixture reaches ScriptRootRetired before arguments with stable typed diagnostic and zero port/header/MIR/function/instruction/collector/CompilationContext delta.",
  "production Script helper-call fixture remains Deferred then ObservationDeferred before physical lowering.",
  "ScriptRoot Brand, TypeOp, Math, FastMem, and str/1 keep their existing precedence.",
]
negative_tests = [
  "RawScriptRoot keeps RawScriptRootRetired, RawRootMain stays upstream caller-zero, and RawLegacy remains Resolved.",
  "shared Resolved consumer and build.rs are unchanged; no target issuer, resolver, recovery, name Const, fallback, or retry is added.",
  "changed test names are all matched by the recorded nonzero cargo --list filter and the run/list counts agree.",
]
focused_gate = "CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_BUILD_JOBS=4 cargo test --profile quick --lib function_call_preflight_route::tests::script_compatibility_tests -- --test-threads=1; separately run the exact normal_script semantic retention filter if its test body/name changes; require nonzero --list matches"
coverage_base_commit = "5546fc2b88"
changed_test_names = [
  "script_root_parked_compatibility_retires_before_arguments",
  "script_function_call_remains_deferred_to_runtime_retirement_terminal",
]
focused_test_filters = [
  "mir::builder::calls::function_call_preflight_route::tests::script_compatibility_tests::",
  "mir::builder::normal_script_semantic_source::call_retention_tests::",
]
coverage_list_command = "CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_BUILD_JOBS=4 cargo test --profile quick --lib -- --list"
coverage_list_count = 7503
focused_test_run = "script_compatibility_tests: 9 passed, 0 failed; script_function_call_remains_deferred_to_runtime_retirement_terminal: 1 passed, 0 failed"
guard_run = "mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: row=ScriptRoot RET0 ok; current_state_pointer_guard.sh: ok"
guard = "Extend the existing mir_call_d1b_active_surface_guard.py current-row phase only; no new shell guard, guard family, or registry row. Pin ScriptRoot -> Resolved = 0, RawLegacy -> Resolved retained, special precedence tokens, changed-test coverage, and touched source files below 760 lines."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_script_compatibility_tests.rs",
  "src/mir/builder/normal_script_semantic_source_call_retention_tests.rs",
  "src/mir/builder/calls/README.md",
  "docs/reference/language/function-call-evaluation.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "raw expression dispatch, package/loan/RawInvocationChildPort, Script semantic resolver/receipt owners",
  "RawLegacy, Method(None), VM/backend, optimizer, JSON/Hako, Stage1, and Call schema",
]
done = "One caller-zero structural retirement replaces the dormant ScriptRoot Resolved edge with ScriptRootRetired; focused positive/negative tests and the reused active guard are green; changed tests have nonzero list/run coverage; source files remain below 760 lines; R4 semantic deferral, special routes, RawLegacy, and shared Resolved are unchanged. No production replacement credit is claimed."
stop = "Return to design_stop if ScriptRoot cannot be identified after special precedence, production source can cross ObservationDeferred, an active public fixture requires old success, a typed special/ordinary source disposition must be invented, or implementation reaches any forbidden owner."
next_row = "Return to design_stop for the RawLegacy fate/caller census; do not open it in the same implementation commit."

[method_call_compat_raw_legacy_fate_d0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-COMPAT-RAW-LEGACY-FATE-D0"
parent_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
ceremony_tier = "T1"
implementation_permission = false
closeout_commit = "7fb4ed1a1f"
decision = "RawLegacy remains the only named compatibility origin entering the shared Resolved completion after the ScriptRoot and RawScriptRoot dormant edges were retired. Freeze its finite fate before any code change: either an effect-free origin-specific terminal or a proven named compatibility owner; do not preserve shared Resolved by inertia."
boundary = "RawLegacy brand issuance -> shared raw child dispatch -> ordinary preflight completion -> pre-effect terminal or existing shared Resolved consumer. Includes the one production RawLegacy route and its direct/test census; excludes ScriptRoot, RawScriptRoot, RawRootMain, installed App Main, exact Cataloged products, Method(None), VM/backend, JSON/Hako, and Call schema."
source_authority = "RawBrandCallAuthorityV1::RawLegacyParkedCompatibility is the only provenance issuer. No exact source/catalog target product or public RawLegacy success contract is currently present."
canonical_issuer = "No target issuer is opened by this D0. The only accepted next issuer is an origin-specific RawLegacy pre-effect retirement terminal; retaining shared Resolved requires a named public/selected compatibility owner with observable acceptance."
non_authority = [
  "name/arity, callable index, current_static_box, has_method, receiver, arguments, MIR, ValueId, physical symbol, registry, singleton, resolver, recovery, fallback, retry",
  "test-only direct injection, shared Resolved existence, and historical compatibility wording without a live owner",
]
fail_fast_boundary = "Existing Brand/TypeOp/Math/FastMem/str/1 precedence -> RawLegacy ordinary classification -> exact fate decision before argument descent, header lookup, resolver/recovery, MIR or collector mutation, fallback, or retry."
production_reach_census = "Boundary: RawLegacy brand issuer -> shared raw dispatcher -> ordinary completion. structural_sites = 1 (logical RawLegacy -> Resolved edge); production_reachable_callers = 1 (logical edge); physical_facade_entries = multiple (static/instance method sessions and body/statement/expression facades are capability entries, not separate target authorities); test_only_authority_injection_helpers = 1; test_only_production_reachable_callers = 0; public_contract_owners = 0. The one live production route is the remaining shared Resolved compatibility edge."
physical_facade_inventory = [
  "src/mir/builder/calls/lowering.rs:377-393 static-method RawLegacy capability session",
  "src/mir/builder/calls/lowering.rs:437-451 instance-method RawLegacy capability session",
  "src/mir/builder/stmts/mod.rs:167-179 body/statement RawLegacy facade",
  "src/mir/builder/ops/logical_shortcircuit.rs:53-55 expression RawLegacy facade",
  "Other RawLegacyChildLoweringPortV1 uses are non-call capability consumers outside this row.",
]
test_only_inventory = [
  "src/mir/builder/calls/function_call_preflight_route.rs:168-179 test-only PreparedRawFunctionPreflightV1::prepare direct authority injection",
  "RawLegacy compatibility tests are internal unit tests, not public success contracts.",
]
fixture_census = [
  "function_call_script_compatibility_tests.rs uses RawLegacy only through internal authority injection.",
  "function_call_preflight_route_tests.rs uses RawLegacy only through internal authority injection.",
  "docs/development/selfhosting/quickstart.md --backend vm is a broad legacy/debug route, not a RawLegacy success contract.",
  "Worker audit found no public RawLegacy profile, API, or production fixture requiring shared Resolved success.",
]
finite_state_matrix = [
  "RawLegacy + declared Brand -> existing Brand route; unchanged.",
  "RawLegacy + TypeOp, Math, FastMem, or str/1 -> existing selected special route; unchanged.",
  "RawLegacy + ordinary non-special FunctionCall -> one open fate row; no target issuer or argument descent during D0.",
  "ScriptRoot/RawScriptRoot/RawRootMain -> previously landed terminals or upstream caller-zero; unchanged.",
  "Installed App Main and exact Cataloged products -> existing source-keyed successors; unchanged.",
]
selected_fate = "RetireBeforeEffects"
selected_fate_reason = "No named public or selected RawLegacy success owner, exact target product, or compatibility acceptance was found. The only production edge is an internal compatibility route, so it must stop before effects rather than preserve shared Resolved by inertia."
open_decisions = []
required_evidence = [
  "finite production/test/public census with start/end/includes/excludes and exact caller paths",
  "shared Resolved consumer and all RawLegacy callers enumerated without a second source scan",
  "a single fate choice with owner, non-authority, reopen trigger, and no silent retention",
]
acceptance = "D0 closes only when the RawLegacy boundary is Exhausted: the one production caller has either an exact successor or an origin-specific pre-effect typed terminal, while every retained compatibility edge has a named owner and observable acceptance. If neither can be proven, keep the row in design_stop; do not invent a resolver, target, receipt, or fallback."
stop = "Stay in design_stop if the one production caller cannot be traced to a finite terminal, if a public/selected success contract is claimed without an owner, if source/raw lineage would require a second scan, or if implementation would touch the shared Resolved consumer, build.rs, method_resolution, VM/backend, JSON/Hako, or Call schema."
next_implementation = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0"
allowed_files = [
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]

[method_call_compat_raw_legacy_fate_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-COMPAT-RAW-LEGACY-FATE-I0"
parent_row = "MIR-CALL-COMPAT-RAW-LEGACY-FATE-D0"
ceremony_tier = "T1"
implementation_permission = false
implementation_commit = "7fb4ed1a1f"
coverage_base_commit = "8936cc98dd"
decision = "Retire RawLegacy ordinary non-special FunctionCall before argument descent. Preserve existing Brand, TypeOp, Math, FastMem, and str/1 precedence; do not create a target, resolver, fallback, retry, or shared-consumer change."
source_authority = "RawBrandCallAuthorityV1::RawLegacyParkedCompatibility is the finite origin authority; no public success contract or exact target product exists."
canonical_issuer = "prepare_ordinary_function_completion_v1 issues RawCompatibilityOrdinaryCallTerminalV1::RawLegacyRetired as the sole origin-specific terminal."
non_authority = [
  "name/arity, callable index, current_static_box, has_method, receiver, arguments, MIR, ValueId, physical symbol, registry, singleton, resolver, recovery, fallback, retry",
  "shared Resolved existence, test-only authority injection, and broad --backend vm behavior",
]
fail_fast_boundary = "Existing Brand/TypeOp/Math/FastMem/str/1 precedence -> RawLegacy ordinary classification -> RawLegacyRetired terminal before argument descent, header lookup, resolver/recovery, MIR or collector mutation, fallback, or retry."
old_edge_delete_set = [
  "RawLegacyParkedCompatibility -> PreparedRawOrdinaryFunctionCompletionV1::Resolved",
]
production_reach_census_before = "structural_sites = 1; production_reachable_callers = 1; physical_facade_entries = multiple; test_only_authority_injection_helpers = 1; test_only_production_reachable_callers = 0; public_contract_owners = 0."
production_reach_census_after = "structural RawLegacy -> Resolved edge = 0; RawLegacyRetired terminal remains as the sole ordinary non-special fate; physical facade entries remain capability entry points; no production replacement credit."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_script_compatibility_tests.rs",
  "src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/reference/language/function-call-evaluation.md",
  "src/mir/builder/calls/README.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "shared Resolved consumer, package/loan, RawInvocationChildPort, Method(None), VM/backend, JSON/Hako, Stage1, and Call schema",
]
focused_gate = "cargo test --profile quick --lib script_compatibility_tests -- --test-threads=1"
focused_test_filters = [
  "mir::builder::calls::function_call_preflight_route::tests::script_compatibility_tests::",
  "mir::builder::calls::function_call_preflight_route::tests::",
]
changed_test_names = [
  "raw_legacy_parked_compatibility_retires_before_arguments",
  "raw_root_main_compatibility_preserves_resolved_terminal",
]
focused_test_results = [
  "script_compatibility_tests: 10 passed, 0 failed, 7494 filtered out",
  "function_call_preflight_route::tests: 22 passed, 0 failed, 7482 filtered out",
]
guard_result = "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: row=MIR-CALL-COMPAT-RAW-LEGACY-FATE-I0 ok"
rustfmt_result = "rustfmt --edition 2021 --check on all four touched Rust files: passed"
guard = "Reuse mir_call_d1b_active_surface_guard.py; no new guard family or registry row. Verify the terminal, precedence, zero-delta tests, changed paths, and source line boundaries."
acceptance = "RawLegacy ordinary non-special calls fail with the typed RawLegacyRetired terminal before any child/header/MIR effect; special routes remain unchanged; shared Resolved remains reachable only from the still-in-scope origins; focused changed tests and the stable guard are green."
stop = "Return to design_stop if a public/selected RawLegacy success owner appears, if special precedence changes, if the shared Resolved consumer/build.rs must change, or if any target/resolver/fallback/retry is required."
next_row = "Return to design_stop for the next named Method(None) producer; do not broaden this RawLegacy slice."

[method_call_compat_raw_legacy_closeout_2026_08_30]
status = "landed"
task_id = "MIR-CALL-COMPAT-RAW-LEGACY-FATE-I0-CLOSEOUT"
implementation_commit = "7fb4ed1a1f"
boundary = "RawLegacy brand issuer -> shared raw dispatcher -> ordinary preflight completion"
production_reach_before = "logical RawLegacy -> Resolved edge = 1; physical capability facades = multiple; test-only authority injection helpers = 1; public contract owners = 0"
production_reach_after = "logical RawLegacy -> Resolved edge = 0; RawLegacyRetired is the sole ordinary non-special terminal; physical capability facades remain structural and are not target authorities"
effect_order = "Brand, TypeOp, Math, FastMem, and str/1 retain precedence; ordinary RawLegacy stops before argument/header/resolver/recovery/MIR/collector effects"
non_claims = "No shared Resolved consumer, build.rs, Method(None), VM/backend, JSON/Hako, Stage1, Call schema, or other-origin behavior changed"
evidence = "10 compatibility tests and 22 route tests are green; pointer guard, reused active-surface guard, and git diff checks are green"
next_row = "MIR-CALL-METHOD-CORRIDOR-NONSTAGE1-PRODUCER-RETIRE-D0 remains the next design stop"

[proof_reliability_followups_2026_08_29]
status = "landed"
task_id = "MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
guard_phase = "active_surface"
implementation_permission = false
observed_head = "83984fe124"
current_row_preemption = "This guard-only proof row is the sole active implementation surface; after its fixed two-commit series, return immediately to the selected ScriptRoot fate Decision."
confirmed = [
  "The method_corridor_explicit_compat_ingress_i0 historical invocation false-reds at HEAD because the reusable guard still carries landed phase handlers tied to current-row dispatch shape; those handlers are retirement debt, not a reason to restore old execution lanes.",
  "The I0 implementation added canonical_snapshot_preserves_runtime_static_global_target, but neither recorded broad filter matched it; the exact test is green at observed_head.",
  "SiteCoverageMismatch has no direct negative test for target-only or partial-overlap inventories.",
  "The legacy-tests plus vm-reference feature set compiles at observed_head, but the two mir_static_box_naming tests fail with GenericLoop MissingTransientType; this is real baseline debt, not a compile failure and not a Method-corridor result.",
]
closed_or_corrected = [
  "The formerly missing I0 test now has exact post-audit execution evidence.",
  "docs/reference/environment-variables.md already records unset/0 as Canonical, exact 1 as the named compatibility projection, and malformed/non-Unicode input as fail-fast; no second env-contract document is needed.",
  "The active guard is now a two-commit proof surface: historical phase commands are tombstones, and changed test names are checked against cargo test -- --list before RawRootMain resume.",
  "SiteCoverageMismatch has direct target-only and partial-overlap negative tests; ObserveOnly with no target remains accepted.",
]
ordered_tasks = [
  "1. MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0: after RawScriptRoot I0, run one predeclared two-commit retirement series. Commit A keeps the stable entrypoint but splits/prunes the 1799-line lifecycle guard so only the current origin-retirement family and no-argument current-row dispatch remain live; landed phases become named superseded_by tombstones backed by Git/card evidence instead of replayable HEAD commands. Commit B adds complete changed-test/filter coverage from cargo test -- --list and the two direct SiteCoverageMismatch negative cases. Every touched/new check file stays below 760 lines and no new per-row shell guard is added.",
  "2. Return immediately to MIR-CALL-COMPAT-SCRIPT-ROOT-FATE-D0; do not extend the proof detour with further cleanup findings.",
]
proof_series_contract = "Exactly two buildable commits: active-surface guard split/prune, then current proof correction plus negative tests. The second commit is the fixed terminal; no third infrastructure row, compiler behavior change, or production-route claim is permitted."
historical_retirement_acceptance = "Each removed explicit phase handler has one owning card section, landed commit, superseded_by target, and zero current manifest/CI/parent caller evidence. Historical verification commands are retained as landed evidence text, not promised to execute at HEAD. Unknown current phases and duplicate live handlers fail closed; the active origin-retirement family remains green."
focused_coverage_acceptance = "The checker consumes an exact implementation commit, maps every added/renamed or lexically modified #[test] function in its diff to cargo test -- --list output, and proves coverage by at least one recorded filter. Unmappable changed test code, a zero-match filter, or an uncovered listed test fails closed. README/reference-only allowed files are evidence files, not test coverage inputs."
site_coverage_negative_acceptance = "Direct verifier tests construct (a) a nonempty target inventory with no observations and (b) a partially overlapping target/observation inventory; both return exactly SiteCoverageMismatch. ObserveOnly observations with no targets remain accepted."
non_claims = "No compiler semantics, source acceptance, RawScriptRoot switch, new semantic receipt, full cargo suite, CI promotion, or legacy-tests fate is decided by these proof rows."
active_surface_contract = "One registered shell entrypoint dispatches only the current proof row without an explicit phase argument. The Python helper is the sole active implementation and remains below 760 lines; historical phase handlers are deleted from executable code."
active_surface_helper = "tools/checks/lib/mir_call_d1b_active_surface_guard.py"
coverage_base_commit = "f75985ec9a"
changed_test_names = [
  "direct_call_verifier_rejects_target_only_inventory",
  "direct_call_verifier_rejects_partial_site_inventory",
]
focused_test_filters = [
  "mir::resolved_semantics::direct_call_verifier::tests::",
]
coverage_list_command = "CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_BUILD_JOBS=4 cargo test --profile quick --lib -- --list"
coverage_list_count = 7503
coverage_test_run = "direct_call_verifier filter: 3 passed, 0 failed, 7500 filtered out"
active_surface_allowed_files = [
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/guard_rows.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "src/mir/resolved_semantics/callable_index.rs",
  "src/mir/resolved_semantics/direct_call_verifier.rs",
  "src/mir/resolved_semantics/tests.rs",
]
[proof_reliability_followups_2026_08_29.historical_phase_tombstones]
  readiness = "owner=d0_sig_close_f_readiness_audit_2026_08_26; landed=9d8b4be35a; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  bridge_ready = "owner=bridge_ready_registration_row; landed=540e12ffd2; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  observer_i0 = "owner=observer_guard_registration_row; landed=2469ce7e57; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  observer_i0_verifier_corrective = "owner=i0_observation_verifier_regression_repair_d0_2026_08_29; landed=e382a00923; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  cataloged_source_coseal_validation = "owner=validation_guard_registration_row; landed=f5d7f5fed5; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_observation_gate_corrective_r0 = "owner=main_observation_gate_corrective_r0; landed=012099e01b; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_root_owner_forest_validation_r0 = "owner=main_root_owner_forest_validation_r0; landed=605cf718d5; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_root_identity_coseal_i0 = "owner=main_root_identity_catalog_coseal_i0; landed=4001f12c37; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_raw_cataloged_handoff_d0 = "owner=guard_hygiene; landed=afb0d79054; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_raw_cataloged_route_r0 = "owner=route_r0; landed=46fdf7a4fa; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_raw_lineage_handoff_d1 = "owner=guard_phase_drift_2026_08_28; landed=c84293c193; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  main_raw_lineage_witness_harden_r0 = "owner=main_raw_lineage_witness_harden_2026_08_28; landed=2dadcae1c2; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  qualified_method_target_issuer_d0 = "owner=qualified_method_target_issuer_d0; landed=23bb082527; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  qualified_method_target_issuer_i0 = "owner=qualified_method_target_issuer_i0; landed=71b8e240eb; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  cataloged_source_relation_affine_loan_i0 = "owner=cataloged_source_relation_affine_loan_i0; landed=99bdfc856c; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  installed_nonbrand_pre_effect_reject_r2a = "owner=installed_nonbrand_pre_effect_reject_r2a; landed=1db9a097ba; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  resolved_compatibility_provenance_r2b = "owner=r2b_compatibility_provenance_d0_2026_08_29; landed=b48a072098; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  resolved_compatibility_provenance_r2c = "owner=r2c_compatibility_provenance_d0_2026_08_29; landed=52615fa9a2; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  resolved_compatibility_unclassified_r2d = "owner=r2d_unclassified_source_fail_closed_2026_08_29; landed=80681a8cde; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  method_corridor_explicit_compat_ingress_i0 = "owner=method_corridor_explicit_compat_ingress_i0_2026_08_29; landed=13664f7787; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  method_corridor_nonstage1_producer_retire_d0 = "owner=method_corridor_nonstage1_producer_retire_d0_2026_08_29; landed=3391ecca92; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
  raw_script_root_pre_effect_retire_i0 = "owner=method_call_compat_raw_script_retire_i0_2026_08_29; landed=8837cbbab2; superseded_by=MIR-CALL-GUARD-ACTIVE-SURFACE-PRUNE-R0"
[legacy_tests_fate_d0_2026_08_29]
task_id = "LEGACY-TESTS-FATE-D0"
status = "parked_sealed_separate_lane"
implementation_permission = false
observed_head = "17cfd7cf83"
current_evidence = "cargo check and cargo test --no-run with features legacy-tests,vm-reference succeed; the exact mir_static_box_naming filter executes two tests and both fail at GenericLoop carrier representation with MissingTransientType."
selected_direction = "RetireHistorical unless a finite caller/cfg census finds a selected current acceptance owner. Do not repair the two reds or add a legacy feature CI lane merely to preserve the old suite."
decision = "D0 proves the selected acceptance owner/caller count. If it remains zero, remove the legacy-tests cfg surface, affected historical tests, and Cargo feature declaration in one bounded retirement and preserve history in Git. Only a newly found selected owner may reopen RetainSupported as a separate Decision. Silent compilation-only retention is forbidden."
does_not_block = "MIR-CALL-COMPAT-RAW-SCRIPT-RETIRE-I0, the queued proof reliability rows, or the Method corridor unless a selected acceptance gate explicitly depends on legacy-tests."
reopen_trigger = "CURRENT_STATE selects this D0, a selected gate names legacy-tests, or the feature gains a nonzero supported caller/CI owner."

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "raw_legacy_origin"
kind = "origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:56-62; src/mir/builder/raw_expression_dispatch/mod.rs:345-357; src/mir/builder/calls/lowering.rs:377-393,437-451"
edge = "RawLegacy child lowering now ends at an origin-specific pre-effect retirement terminal"
authority = "No exact source/catalog target issuer is present in this legacy owner"
disposition = "PreEffectReject"
reopen_trigger = "Every live RawLegacy production caller has an exact successor or an effect-free named retirement terminal"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "script_root_origin"
kind = "origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:77-95; src/mir/builder/calls/function_call_preflight_route.rs:186-193,348-355"
edge = "ScriptRoot ordinary compatibility demand is selected for an origin-specific pre-effect typed terminal; the old downstream Resolved edge is dormant because semantic observation stops before physical open"
authority = "The existing semantic observation owner and ScriptRoot provenance classifier establish production reach zero; no exact target issuer exists or is needed for retirement"
disposition = "SelectedPreEffectReject"
reopen_trigger = "The selected RET0 fails, a production caller crosses ObservationDeferred, or a public ScriptRoot success contract appears"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "raw_script_root_origin"
kind = "origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:96-102; src/mir/builder/calls/function_call_preflight_route.rs:194-201,348-355"
edge = "RawScriptRoot compatibility demand enters an origin-specific pre-effect retirement terminal"
authority = "RawScriptRoot has no exact source/catalog target issuer; the existing classifier is the sole origin authority"
disposition = "PreEffectReject"
reopen_trigger = "RawScriptRoot receives an exact source/raw relation or the named retirement terminal is removed"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "raw_root_main_origin"
kind = "origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:103-107; src/mir/builder/calls/function_call_preflight_route.rs:202-209,348-355"
edge = "RawRootMain compatibility demand is classified upstream as UnsupportedSurface(Call) before physical open; the downstream Resolved completion has no production FunctionCall reach"
authority = "Existing raw-root eligibility owner is the sole authority for the caller-zero boundary; no downstream target issuer or second terminal is added"
disposition = "PreEffectReject"
reopen_trigger = "A new production RawRootMain FunctionCall route crosses the upstream eligibility boundary or that owner changes"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "method_resolution_static_none"
kind = "producer"
owner = "retired source owner recorded by MIR-CALL-METHOD-RESOLUTION-STATIC-NONE-RET0"
edge = "the removed current_static_box + has_method branch emitted Callee::Method with receiver None"
authority = "The current box and method spelling are not an exact declaration-backed issuer"
disposition = "RetiredCallerZero"
retirement_commit = "fea43bdade"
reopen_trigger = "A new production caller reaches the removed branch, or an exact catalog/owner handoff is proposed for bare static names"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "resolved_compatibility_consumer"
kind = "consumer"
owner = "retired at 598530d23b; RawRootMainRetired remains in src/mir/builder/calls/function_call_preflight_route.rs"
edge = "the removed Resolved route lowered arguments, then re-entered resolve_call_target and recovery before Call"
authority = "No post-argument target discovery is permitted; the consumer must receive an issued handoff"
disposition = "RetiredTypedTerminal"
reopen_trigger = "PreparedRawOrdinaryFunctionCompletionV1::Resolved, a post-argument resolver, or a RawRootMain ordinary success edge reappears"
post_ret0_production_reachable_callers = 0
post_ret0_structural_producer_sites = 0
post_ret0_test_only_witnesses = 0
post_ret0_note = "RawRootMain direct witnesses now assert the payload-free RawRootMainRetired terminal or retained special precedence before arguments; the Resolved variant, producer, consumer, and exclusive descendants are zero."
retirement_commit = "598530d23b"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "builder_method_none_publication_terminal"
kind = "terminal"
owner = "src/mir/builder/builder_emit.rs:109-142,275-279"
edge = "Builder publication still accepts the receiverless Method terminal"
authority = "Only an exact receiver or qualified static declaration may reach canonical Call publication"
disposition = "RetiredTypedTerminal"
reopen_trigger = "All in-boundary Method(None) writers are zero and the terminal has a typed pre-publication rejection"
post_ret0_production_literal_writers = 0
post_ret0_writer = "none; unified reissuer retired at 287e944fe8"
post_ret0_pre_effect = true
post_ret0_successor = "none; cc2d390538 rejects the impossible receiverless Method shape before Builder publication"
retirement_commit = "cc2d390538"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "unified_emitter_methodize_reissuer"
kind = "reissuer"
owner = "src/mir/builder/calls/unified_emitter.rs:397-459,698-729"
edge = "Explicit compatibility snapshot can reclassify RuntimeData Global into Method(None)"
authority = "Incoming typed target provenance; the selector is not a Stage1 provenance brand"
disposition = "RetiredCallerZero"
reopen_trigger = "All non-Stage1 selector callers are zero or a source-backed exact compatibility owner replaces this reissuer"
post_ret0_production_reissuer_sites = 0
post_ret0_direct_core_ingress_classes = 3
post_ret0_successor = "none; Rust exact-1 ingress and the shared reissuer retired at 287e944fe8"
post_ret0_user_decision_required = false
retirement_commit = "287e944fe8"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "installed_app_main_affine_successor"
kind = "successor"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:317-318; src/mir/builder/calls/build.rs:128-176"
edge = "Installed App Main consumes the source-keyed affine handoff before arguments"
authority = "Same-session App Main source/catalog identity and exact source site"
disposition = "ExactSuccessor"
reopen_trigger = "App Main source identity, catalog brand, or handoff consumer changes"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "installed_nonbrand_pre_effect_reject"
kind = "terminal"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:319-324,425-449"
edge = "Installed non-brand or bare same-owner static calls reject before argument descent"
authority = "Installed source relation gate with no exact target handoff"
disposition = "PreEffectReject"
reopen_trigger = "A same-brand exact catalog/source relation is added for this installed family"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.in_scope_inventory]]
id = "unclassified_preflight_reject"
kind = "terminal"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:326-347"
edge = "Unclassified ordinary calls reject before argument descent"
authority = "Finite preflight classification with no exact target product"
disposition = "PreEffectReject"
reopen_trigger = "A new named source/brand owner is added and supplies an exact handoff"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "raw_legacy_origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:56-62; src/mir/builder/raw_expression_dispatch/mod.rs:345-357"
production_direct_sites = 1
upstream_classes = 1
evidence = "one RawLegacy issuer and one shared dispatch site; the origin enters Resolved"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "script_root_origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:77-95; src/mir/builder/calls/function_call_preflight_route.rs:186-193,348-355"
production_direct_sites = 0
upstream_classes = 1
evidence = "one structural ScriptRoot issuer and one test-injectable route site remain, but production ordinary FunctionCall stops as Deferred/ObservationDeferred at the semantic seal before physical open"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "raw_script_root_origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:96-102; src/mir/builder/calls/function_call_preflight_route.rs:194-201,348-355"
production_direct_sites = 1
upstream_classes = 1
evidence = "one RawScriptRoot issuer and one shared route site remain observable, but the Resolved producer is caller-zero because the route now issues RawScriptRootRetired before argument descent"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "raw_root_main_origin"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:103-107; src/mir/builder/calls/function_call_preflight_route.rs:202-209,348-355"
production_direct_sites = 0
upstream_classes = 2
evidence = "raw-root eligibility classifies ordinary FunctionCall as UnsupportedSurface(Call) before physical open; the downstream RawRootMain Resolved route has no production FunctionCall reach"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "resolved_compatibility_consumer"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:348-355; src/mir/builder/calls/build.rs:120-146"
production_direct_sites = 1
upstream_classes = 1
evidence = "RawLegacy is the sole live named production origin. ScriptRoot and RawRootMain are caller-zero upstream, and RawScriptRoot is already retired before arguments; structural/test-only route sites are not production success callers."

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "method_resolution_static_none"
owner = "src/mir/builder/calls/method_resolution.rs:37-60; src/mir/builder/calls/utils.rs:45-53; src/mir/builder/calls/build.rs:371"
production_direct_sites = 1
upstream_classes = 4
evidence = "one direct production caller; the four named R2 origins are upstream of the shared Resolved route"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "builder_method_none_publication_terminal"
owner = "src/mir/builder/builder_emit.rs:109-142,275-279,363-368"
production_direct_sites = 1
upstream_classes = 2
evidence = "one Builder publication terminal remains live; two in-scope Method(None) writer classes can reach the receiverless terminal"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "unified_emitter_methodize_reissuer"
owner = "src/mir/builder/calls/unified_emitter.rs:105,119,387-459,698-729; src/mir/builder/calls/request_boundary.rs:67,89"
production_direct_sites = 1
upstream_classes = 3
evidence = "one unified-emitter reissuer remains live and has three direct core-ingress callers; no exact successor or pre-effect terminal is recorded"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "installed_app_main_affine_successor"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:317-318; src/mir/builder/calls/build.rs:128-129,150-176"
production_direct_sites = 1
upstream_classes = 1
evidence = "one installed App Main producer/consumer/publication chain is live; its exact source-keyed affine successor is already the named disposition"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "installed_nonbrand_pre_effect_reject"
owner = "src/mir/builder/calls/function_call_preflight_route.rs:319-324,378-450"
production_direct_sites = 1
upstream_classes = 1
evidence = "one installed non-brand catalog caller reaches two bare same-owner rejection branches and one Rejected completion site before argument descent"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.d0_a_caller_census]]
id = "unclassified_preflight_reject"
owner = "src/mir/builder/calls/function_call_brand_source_demand.rs:110-112; src/mir/builder/calls/function_call_preflight_route.rs:218-244,341-347,552"
production_direct_sites = 1
upstream_classes = 1
evidence = "one unclassified source issuer reaches one pre-effect reject and one SourceRejected sink; no target product is issued"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "stage1_exact_one_writer"
kind = "compatibility_writer"
owner = "src/runner/stage1_bridge/env/runtime_defaults.rs:10-24; tools/selfhost/lib/stage1_contract.sh:279-307"
edge = "Known Stage1 writers select exact value 1, but the shared Rust selector carries no Stage1 provenance"
disposition = "ParkedSealed"
non_authority = "The environment value alone is not an artifact or source authority"
reopen_trigger = "Fresh Stage1 artifact plus metadata from the same source/compile transaction, or explicit Stage1 route retirement"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "json_v0_optional_method_receiver"
kind = "compatibility_ingress"
owner = "src/runner/mir_json_v0/call.rs:78-118,253-272"
edge = "JSON-v0 nullable receiver compatibility can publish Method(None)"
disposition = "ParkedSealed"
non_authority = "JSON-v0 wire shape cannot issue a Rust source target"
reopen_trigger = "JSON-v0 lane is selected for schema retirement or gains an exact typed successor"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "core_bridge_const_methodize"
kind = "post_wire_reissuer"
owner = "src/runner/modes/common_util/core_bridge.rs:101-199"
edge = "Post-wire Const text can be rewritten into receiverless Method"
disposition = "ParkedSealed"
non_authority = "Post-wire text and compatibility flags cannot become source target authority"
reopen_trigger = "Selected bridge caller-zero and typed payload retirement are proven"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "core_bridge_module_singleton_methodize"
kind = "post_wire_reissuer"
owner = "src/runner/modes/common_util/core_bridge.rs:200-245,290-335"
edge = "ModuleFunction/static singleton metadata can be rewritten into Method(None)"
disposition = "ParkedSealed"
non_authority = "Singleton tables and metadata text cannot issue a receiver"
reopen_trigger = "Bridge route retirement or an exact catalog handoff for the selected compatibility payload"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "rust_json_nullable_egress"
kind = "egress"
owner = "src/runner/mir_json_emit/emitters/calls.rs:17-75; src/runner/mir_json_emit/helpers.rs:43-64"
edge = "JSON egress projects nullable Method receiver"
disposition = "ParkedSealed"
non_authority = "Egress is a projection and cannot repair or issue a target"
reopen_trigger = "All upstream Method(None) producers are zero and the egress has a typed error boundary"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "rust_vm_method_none_recovery"
kind = "terminal_consumer"
owner = "src/backend/mir_interpreter/handlers/calls/mod.rs:70-92,220-242; src/backend/mir_interpreter/handlers/calls/method.rs:394-532"
edge = "Rust reference VM performs registry/name/singleton and receiver-surrogate recovery"
disposition = "ParkedSealed"
non_authority = "The VM is a terminal consumer, not a target issuer"
reopen_trigger = "Reference backend selection with upstream producer-zero and selected parity evidence"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "selected_native_method_none_preflight"
kind = "terminal_consumer"
owner = "selected ny-llvmc mir-call pure/need/dispatch shims"
edge = "Native preflight treats missing receiver as zero and may retry plan metadata"
disposition = "ParkedSealed"
non_authority = "C metadata/name/surface and numeric zero are not source target authority"
reopen_trigger = "Selected native egress rejects receiverless Method and its presence/range guard is landed"

[[method_corridor_nonstage1_producer_retire_d0_2026_08_29.outside_inventory]]
id = "hako_methodize_adapter"
kind = "non_rust_compatibility"
owner = "lang/src/mir/builder/internal/{builder_config_box,func_lowering/call_methodize_box}.hako"
edge = "Hako compatibility adapter remains a separate wire/schema owner"
disposition = "ParkedSealed"
non_authority = "Hako adapter output is not evidence for the Rust Method(None) producer census"
reopen_trigger = "Hako pipeline is explicitly selected for migration or route retirement"

[same_module_cataloged_provider_census_d0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-CENSUS-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape finite caller census"
implementation_permission = false
decision = "Do not treat cataloged_provider_as_free as one implementation slice. First enumerate caller-present names at the Cataloged ingress and assign each name to an existing exact issuer, a typed pre-effect retirement, or a separate language/provider decision. No source, target, resolver, or compatibility behavior changes in this row."
source_authority = "Existing named builtin, explicit Extern declaration, Math special owner, or exact same-brand callable/source relation; the census itself issues no semantic product."
canonical_issuer = "Only existing builtin_print() or already-owned qualified Extern/Math/callable publication may be reused. A name plus arity, BuiltinGlobal classification, catalog presence, resolver, symbol, or EffectsAnalyzer is not an issuer."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "source name/arity and CanonicalGlobalTargetV1::new_free_function",
  "CalleeResolverBox, catalog existence-only lookup, physical symbol, env, VM/backend, test fixture, fallback, and retry",
]
census_boundary = "Installed non-brand Cataloged raw FunctionCall -> prepare_cataloged_target_v1 -> first target/typed-reject terminal; includes caller-present provider names and their existing tests, excludes Math special ingress, ExplicitExtern, RawCompatibility, caller-less GC, wire/JSON, VM/backend, and Call schema."
finite_states = [
  "PrintCandidate: exact builtin_print issuer exists; arity contract remains to be confirmed",
  "MathOwned: abs/min/max/sin/cos leave through the existing Math owner; no provider change",
  "ExplicitExternOwned: qualified declared provider remains on the existing Extern owner",
  "NamedUnsupported: bare error and bare now use one-name typed pre-effect retirements; panic is a parked terminal-Fault target and exit remains open for its own language/runtime decision",
  "GcCallerPresent: gc_collect/gc_stats with a caller must be counted separately from caller-less reject; no synthesis is accepted",
  "Ordinary: all other names retain ordinary declaration handling and are not provider evidence",
]
provider_matrix = [
  "print -> existing builtin_print(); exact print/1 is a separate bounded contract check",
  "abs/min/max/sin/cos -> existing Math route; excluded from this provider implementation",
  "error -> named unsupported retirement at 69680b983f; now -> canonical unsupported contract and one-name retirement child",
  "panic -> accepted terminal-Fault target with production 0; exit -> no bare issuer; neither may become Extern by name",
  "gc_collect/gc_stats -> caller-present census first; caller-less reject remains unchanged",
]
ordered_tasks = [
  "D0-A: derive the finite caller-present name list from the Cataloged ingress and record exact file/line callers; no function-level or backend-wide census",
  "D0-B: verify each name against the finite state table and count caller-present GC separately; do not infer from a missing caller",
  "D0-C: choose one cohort only: exact print/1 contract, caller-present GC typed retirement, or a named unsupported bare-provider contract; delegate panic to its terminal-Fault lane and leave exit NoIssuer",
  "I0: open only the selected cohort with positive/negative/pre-effect evidence and delete its old name+arity synthesis edge in the same bounded row",
  "C0: rerun the provider boundary and return to the SameModule parent; no provider-wide closure claim",
]
acceptance = "The caller-present name list is finite and two-directionally reconciled, each name has exactly one state/owner or named unsupported contract, math/Extern/ordinary exclusions are explicit, caller-present GC is not silently treated as caller-less, and no provider-wide implementation or new semantic receipt was added."
no_safe_slice = "Remain in design_stop if any provider name needs a new bare runtime owner, bare exit behavior is undecided, the accepted panic Fault pipeline is incomplete, caller-present GC behavior is unclassified, print arity is not contract-backed, or target meaning still requires name/arity, symbol, registry, resolver, env, or retry."
next_design_card = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-CENSUS-D0"
selected_cohort = "caller_present_gc_typed_retirement"
caller_present_route = "prepare_ordinary_function_completion_v1 -> prepare_cataloged_target_v1"
reconciled_result = "The finite provider names are separated: print has an existing builtin issuer; abs/min/max/sin/cos leave through the Math owner; explicit env/nyash/system names remain Extern; bare error/now use named unsupported retirements; panic leaves the Call lane for its parked terminal-Fault target; exit has no bare issuer; gc_collect/gc_stats are the only caller-present cohort whose existing caller-less retirement policy can be extended without a new authority."
next_implementation = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_cataloged_provider_bare_no_issuer_d0_2026_08_30]
status = "accepted_design_stop"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-CENSUS-D0"
ceremony_tier = "T0"
box_kind = "BoxCount finite bare-provider authority audit"
implementation_permission = false
decision = "Keep bare panic and exit out of Cataloged name synthesis. Bare error/now are retired. LANG-PANIC-TERMINAL-FAULT-D0 accepts panic as a separate parked terminal-Fault target; bare exit remains NoIssuer until its own explicit language/runtime contract."
source_authority = "An existing named builtin, qualified Extern provider, Math owner, or exact same-brand callable/source relation. The parser emits only a generic identifier FunctionCall for these names and therefore does not issue a bare provider product."
canonical_issuer = "None live for bare panic/exit. Bare error/now use the existing Rejected terminal without a Callee issuer. Panic requires its typed source -> common exit -> generic Fault -> selected runtime chain; exit requires a separate Decision. Qualified env/nyash/system Externs, Math handlers, and GC retirement may not be reused by name."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "name/arity and CanonicalGlobalTargetV1::new_free_function",
  "CalleeResolverBox, catalog existence-only lookup, generated symbol, registry, EffectsAnalyzer, VM/backend, fixture, fallback, and retry",
]
fail_fast_boundary = "After TypeOp/Math/FastMem precedence and before prepare_cataloged_target_v1, resolver or argument descent, Const/ValueId, effects, block/Call publication, and any fallback. A selected name with no exact issuer must enter one named typed pre-effect terminal."
census_boundary = "Installed non-brand Cataloged raw FunctionCall -> provider-name classification -> first target/typed-terminal; includes bare error/now/panic/exit, exact arity observations, the two Cataloged caller families, and Math/Extern/ordinary exclusions; excludes qualified Extern, Math special ingress, GC retirement already landed, RawCompatibility, wire/JSON, VM/backend, and Call schema."
finite_inventory = [
  "bare error: retired by same_module_cataloged_provider_bare_error_retire_i0_2026_08_30 before prepare_cataloged_target_v1",
  "bare now: retired by same_module_cataloged_provider_bare_now_retire_i0_2026_08_30 before prepare_cataloged_target_v1",
  "bare panic: accepted terminal-Fault target; current Cataloged name synthesis is non-authority and the typed source/exit/MIR/runtime issuer chain is not live",
  "bare exit: same Cataloged synthesis; qualified env.process.exit is separate and bare exit has no issuer",
  "production Cataloged caller families: nonmain_static_box_method_batch.rs and instance_box_method_batch.rs",
  "excluded owners: special_handlers.rs Math route, qualified Extern declarations, and landed GC pre-effect retirement",
]
finite_states = [
  "BareErrorRetired: existing typed Rejected terminal before target synthesis and argument effects",
  "BareNowRetired: canonical language SSOT marks bare now unsupported and its one-name retirement is landed before target synthesis and argument effects",
  "PanicTargetAcceptedIssuerMissing: panic/1 leaves the Call lane, but remains ParkedSealed until one typed source-to-final-Fault chain is co-sealed",
  "NoIssuer: bare exit has no exact issuer and remains a separate CutoverBlockerOpen language/runtime decision",
  "QualifiedExternOwned: env/nyash/system names retain their existing qualified provider owner",
  "MathOwned: abs/min/max/sin/cos retain the existing Math owner and are outside this row",
  "Ordinary: all other identifiers retain ordinary declaration handling and are not provider evidence",
  "WrongArity: only after a bare issuer contract exists may wrong arity be a typed reject; do not invent one now",
]
ordered_tasks = [
  "D0-A: reconcile the four bare names, arity observations, two production Cataloged caller families, and the first target/terminal in both directions",
  "D0-B: record bare error/now as unsupported, bare panic as a parked terminal-Fault target, and bare exit as NoIssuer; qualified Extern/Math/ordinary/GC exclusions stay explicit",
  "D0-C: use the existing canonical unsupported terminal for bare error and open only a one-name bare-now retirement child",
  "I0: bare error and bare now retirements are landed as separate one-name rows; panic is delegated to its terminal-Fault series and exit remains separate",
  "C0: rerun this finite bare-name census and return to the SameModule parent without claiming provider-wide closure",
]
acceptance = "The four bare names and two production caller families are finite and two-directionally reconciled; bare error/now have named unsupported retirements, panic is delegated to the accepted terminal-Fault target with production 0, exit retains NoIssuer, no name/arity synthesis is elevated, and implementation remains closed until each exact issuer chain is named."
no_safe_slice = "Remain in design_stop if a bare runtime provider must be invented, bare exit behavior is undecided, the common exit transaction/final Fault chain is absent, or the only route is resolver/name/symbol/registry/VM fallback."
non_claims = "No error/now/exit behavior change, no live panic route, no new provider/Extern issuer, no Math/GC change, no ordinary-static/physical-thunk/CorePlan/rewrite/ordinary-new work, no package/loan, VM/backend/JSON, Call schema, fallback, or retry."
evidence = "Read-only audits found the four bare names synthesized as SameModule FreeFunction(name, arity) at Cataloged preflight. Bare error/now have landed one-name retirements. The language reference now selects panic(String) as a noncatchable terminal Fault through cleanup, while current parser/classifier/VM strings and qualified env/nyash/system consumers issue no canonical bare-panic product. Bare exit remains undecided."
reopen_trigger = "For panic, reopen only when one same-session source issuer, pending Fault/exit transaction, generic final Fault terminal, and selected runtime ABI are co-sealed without name/provider/backend recovery. For exit, require its own explicit language/runtime decision."
worker_audit_head = "1a026f82c4"
worker_audit_result = "Read-only audits at audit HEAD 1a026f82c4 found no exact bare-provider issuer. Bare error/now have named unsupported terminals. A later language Decision accepts panic only as a ParkedSealed terminal-Fault target; exit remains NoIssuer. Physical thunk and true DeclaredInstance remain separate issuer-missing families. Legacy guard cleanup has no proven SupersededDelete candidate and remains parked under the parent guard policy."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_cse_same_block_stats_determinism_r0_2026_09_01]
status = "landed"
task_id = "MIR-CSE-SAME-BLOCK-STATS-DETERMINISM-R0"
parent_row = "MIRBUILDER-FINAL-PIPELINE-v1"
ceremony_tier = "T1"
box_kind = "One existing optimizer responsibility; conservative same-block CSE"
implementation_permission = false
decision = "Keep CSE conservative and deterministic by giving each BasicBlock its own expression map. Rewrite only an actually supported pure duplicate in the same block; if no exact same-block proof or rewrite occurs, leave MIR unchanged and report zero elimination."
source_authority = "The existing MIR optimizer CSE owner, MirInstruction::effects(), instruction_key(), dst_value(), and function metadata value types. This row does not create language semantic effects or a target/recipe issuer."
canonical_issuer = "src/mir/passes/cse.rs::cse_in_function is the sole optimizer owner; semantic_simplification::apply remains the existing consumer."
non_authority = [
  "function-wide expression reuse",
  "HashMap block traversal order as a dominance proof",
  "sibling-block values",
  "backend/VM/Call metadata",
  "name/symbol lookup, AST or fixture inference",
  "an elimination counter without a physical Copy rewrite",
]
fail_fast_boundary = "A duplicate is eligible only when its exact key and prior definition are in the same BasicBlock and the existing FAST_INT/support/effect rules permit the rewrite. Otherwise the instruction remains unchanged and the counter is zero; no cross-block fallback or retry exists."
census_boundary = "src/mir/passes/cse.rs expression_map lifetime and elimination accounting -> existing semantic_simplification consumer; includes same-block pure rewrites and sibling-block/unsupported negatives, excludes Call schema, Builder lowering, VM/backend, and general dominance analysis."
old_edge_delete_set = [
  "function-wide expression_map reused across BasicBlocks",
  "sibling-block value reuse without a dominance proof",
  "unconditional eliminated += 1 when no Copy was emitted",
]
finite_states = [
  "SameBlockRewrite: exact prior same-block supported pure expression becomes one Copy and increments once",
  "SameBlockNoRewrite: duplicate exists but FAST_INT/support/effect rules forbid rewrite; MIR and count stay unchanged",
  "CrossBlockNoRewrite: same key in another block is never reused",
  "UnsupportedNoRewrite: unsupported or non-numeric form remains untouched with zero count",
]
ordered_tasks = [
  "I0-A: move the expression map inside the per-block loop without changing the existing instruction-key vocabulary",
  "I0-B: increment the statistic only in the branch that actually replaces the instruction with Copy",
  "I0-C: add same-block positive, sibling-block negative, unsupported/non-numeric zero-count, and deterministic repeat tests",
  "I0-D: run the changed tests, quick library check, active-surface guard, pointer guard, and diff check; close out without Call or backend claims",
]
acceptance = "CSE never reuses a value across BasicBlocks; every counted elimination corresponds to one emitted Copy; same-block supported behavior remains green; sibling-block and unsupported/non-numeric cases are unchanged with zero statistics; source/test/guard evidence is present and no Call/VM/backend semantics change."
no_safe_slice = "Return to design_stop if same-block correctness requires a new dominance analysis, a new semantic receipt, a cross-block rewrite, a source/AST scan, or any Call/VM/backend change."
non_claims = "No MirBuilder Call cutover, no selected-C progress, no language semantic change, no generic dominance/CSE widening, no fallback/retry change, no Call schema change, and no whole-lib green claim."
allowed_files = [
  "src/mir/passes/cse.rs",
  "src/mir/passes/semantic_simplification.rs",
  "docs/development/current/main/design/current-optimization-mechanisms-ssot.md",
  "tools/checks/lib/mir_cse_same_block_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/**",
  "src/mir/instruction.rs",
  "VM/backend/JSON/C ABI",
  "Call schema",
]
focused_test_filters = [
  "same_block",
  "across_sibling_blocks",
  "actual_copy_rewrites",
  "bundle_runs_landed_cse",
]
changed_test_names = [
  "cse_rewrites_only_duplicate_pure_instructions_in_same_block",
  "cse_does_not_reuse_duplicate_pure_instructions_across_sibling_blocks",
  "cse_counts_only_actual_copy_rewrites",
  "bundle_runs_landed_cse",
]
coverage_base_commit = "7e4f9869a7"
implementation_commit = "25ab8fb58a"
implementation_evidence = "Landed at 25ab8fb58a: cse_in_function receives one FAST_INT snapshot, creates expression_map per BasicBlock, rewrites only supported same-block duplicates, and increments only after assigning MirInstruction::Copy. Added same-block positive, sibling-block negative, non-numeric zero-count, and bundle integration evidence."
focused_test_evidence = "cargo test --profile quick --lib cse_ -- --test-threads=1 (6 passed); cargo test --profile quick --lib bundle_runs_landed_cse -- --test-threads=1 (1 passed); cargo check --profile quick --lib passed; active-surface guard, pointer guard, and git diff --check passed."
red_classification = "Whole-lib baseline remains separate and is not a row acceptance gate; any changed focused test red must be classified against the 7e4f9869a7 parent before closeout."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0__design_stop"

[verification_health_quick_lib_baseline_p0_2026_08_31]
status = "accepted_design_stop_prerequisite"
task_id = "DEV-GATE-QUICK-LIB-BASELINE-P0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
box_kind = "BoxShape verification infrastructure only"
implementation_permission = false
decision = "Restore honest daily quick visibility and establish a deterministic full-lib red baseline before any broad Call-schema cutover. Focused green does not waive whole-repository red; known failures remain explicit baseline debt until an exact name-set comparison proves no new red."
source_authority = "tools/checks/lib/dev_gate_quick_steps.sh owns daily quick order; the canonical baseline command owns stack size, jobs, profile, features, and serial test execution. docs/tools/check-scripts-index.md is navigation only."
canonical_command = "RUST_MIN_STACK=16777216 CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib -- --test-threads=1"
observed_head = "d96266852d"
observed_health = "After restoring the naming vocabulary and refreshing all 16 metadata manifest anchors, the metadata checker, manifest guard, and catalog guard pass. dev_gate quick now advances to the phase-1 compatibility emit-program runtime-helper guard, whose ny-llvmc fixture aborts with stack overflow (rc=134). Quick steps contain cargo check but no cargo test --lib; the type-fact guard is green when run directly but is not a daily quick caller; focused DeclaredInstance relation suites are green but are not whole-lib evidence."
red_baseline_note = "A local run observed 7373 passed, 139 failed, 29 ignored, while the fixed-stack worker run observed 7372 passed, 140 failed, 29 ignored. The artifact-dependent receipt test and stack ownership must be made deterministic before recording the exact baseline."
latest_full_lib_run = "At HEAD f3120dd80b, RUST_MIN_STACK=16777216 CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib -- --test-threads=1 completed in 2.20s with 7373 passed, 139 failed, 29 ignored; the final failure-name set SHA256 is fa4e8fc8bd0282cc563a422ff4ecb46c64268dc53d5b39efba086952af4ff71a. This is an observation receipt, not an accepted baseline, because the adjacent fixed-stack run differed by one artifact-dependent failure."
repeatability_requirement = "Before accepting a baseline, fix or isolate both observed nondeterministic owners: the selected-receipt test's repo-local release artifact dependency and the fastmem branch test's HashMap iteration order. Run the canonical command in three separate processes and require identical summary, ignored count, sorted full test inventory, sorted failure-name set, and hashes."
baseline_stack_policy = "RUST_MIN_STACK is an observation parameter, not a waiver. Do not call a 16 MiB run healthy while the selected C quick contract is 8 MiB; settle the baseline command and stack ownership explicitly before seeding the manifest."
fail_fast_boundary = "A missing summary, stack abort, compile/link failure, zero-test run, duplicate or malformed baseline, added failure name, changed failure name, or disappeared baseline name blocks comparison. Do not use count-only, regex waivers, auto-rebaseline, or || true."
census_boundary = "dev_gate quick entry -> quick steps -> serial full-lib result and exact failure-name set; includes naming vocabulary repair, deterministic artifact fixture, baseline runner/manifest, visible known-debt receipt, and changed-test filter execution; excludes compiler/MIR semantics, DeclaredInstance co-seal, Call schema, backend parity, quick-static qualification, and legacy-tests fate."
finite_states = [
  "QuickEntryRed: naming-charter or a later quick step fails; rerun from the first failing step after each repair",
  "DeterministicBaseline: fixed command completes with an exact test/ignored partition and sorted failure-name hash",
  "KnownBaselineDebt: expected failure names are visible as an observation receipt, never silently treated as green",
  "NewOrChangedRed: actual failure set differs from the committed set; stop and classify before semantic work",
  "BaselineShrink: an expected failure disappears; require explicit rebaseline rather than retaining stale debt",
  "LegacyTestsSeparate: legacy-tests remains a separate parked fate row and is not repaired for this prerequisite",
]
ordered_tasks = [
  "P0-A: restore the three naming-charter vocabulary tokens, rerun dev_gate quick, and record the next first failure without claiming overall green early",
  "P0-B: isolate the artifact-dependent lib test and stabilize the HashMap-order-dependent fastmem test without changing production semantics; record both owners before baseline seeding",
  "P0-C: add one exact baseline manifest/runner with an explicitly fixed command, serial execution, counts, complete test inventory, sorted failure names, and hashes; require three identical runs and block both new and disappeared names",
  "P0-D: make changed-test coverage execute every matched filter (not list only), keep the landed relation guard's design-stop and closeout checks distinct, and expose only a reserved known-baseline receipt from successful quick output",
  "P1: decide LEGACY-TESTS-FATE-D0 separately; do not mix its compile/test policy with DeclaredInstance or Call-schema work",
]
allowed_files = [
  "docs/tools/check-scripts-index.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "tools/checks/lib/dev_gate_quick_steps.sh",
  "tools/checks/lib/dev_gate_group.sh",
  "tools/checks/lib/dev_gate_group_test.sh",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_declared_instance_relation_guard.py",
  "tools/checks/manifests/cargo_lib_red_baseline.toml",
  "tools/checks/lib/cargo_lib_red_baseline.py",
  "tools/checks/lib/tests/test_cargo_lib_red_baseline.py",
  "src/runner/modes/common_util/exec.rs",
  "src/runner/modes/common_util/exec_tests.rs",
]
forbidden_files = [
  "tools/checks/guard_rows.toml",
  "tools/checks/lib/manifest_runner.py",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "Call schema",
  "VM/backend/JSON/runtime",
  "legacy-tests repair or fallback",
]
acceptance = "The naming guard and the baseline step are wired exactly once and execute in order; the standalone fixed full-lib command completes without stack abort and owns an exact test inventory/ignored count/failure set/hash after three identical runs; new and disappeared failures both stop; the known-baseline receipt is visible while ordinary successful child logs remain compact; changed-test filters are nonzero and actually run; all touched Rust stays below 760/800; pointer and diff guards pass. The independent Stage1 runtime-helper red must be repaired or classified by its own selected-C row before whole quick can be called green."
no_safe_slice = "Remain in design_stop if the full run has no complete summary, artifact isolation changes production behavior, the baseline requires an inferred HEAD^ or regex waiver, quick-static/manifest pilots must be wired into the daily gate, or the task crosses MIR/Call/backend semantics."
non_claims = "No claim that the 139/140 failures are repaired or accepted forever, no whole-workspace/all-feature green claim, no legacy-tests decision, no DeclaredInstance issuer/co-seal, no Method(Some) production switch, no Call-schema change, and no old-edge deletion."
reopen_trigger = "Reopen the prerequisite only when the canonical command or test inventory changes, a quick step is added/removed, a new failure appears/disappears, or the baseline receipt can no longer be reproduced exactly."
next_execution = "Return to MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0 design_stop after this verification prerequisite is landed; keep production implementation closed until effect issuer and selected-C capability are co-sealed."

[verification_health_quick_lib_baseline_p0_b1_fastmem_order_r0_2026_09_01]
status = "selected_fast"
task_id = "DEV-GATE-QUICK-LIB-BASELINE-P0-B1-FASTMEM-ORDER-R0"
parent_row = "DEV-GATE-QUICK-LIB-BASELINE-P0"
depends_on = "verification_health_quick_lib_baseline_p0_2026_08_31"
ceremony_tier = "T0"
box_kind = "BoxShape test-only exact-membership stabilization"
implementation_permission = true
decision = "Replace one order-sensitive FastMem test assertion with exact order-independent field membership. The production HashMap representation and all MIR semantics remain unchanged; this row only removes nondeterministic evidence from the baseline prerequisite."
source_authority = "The existing FastMem test's emitted MemOpKind/field_id inventory is the observed contract; membership and cardinality are meaningful, while HashMap iteration order is not."
canonical_issuer = "None. This row changes no production issuer or route; it only observes the already-emitted MemOp facts in a deterministic test assertion."
non_authority = [
  "HashMap iteration order, block allocation order, test fixture pass/fail by chance, full-lib counts, selected-C, C/JSON, MIR/Call/backend meaning, fallback, retry, and any production route",
]
fail_fast_boundary = "The test must fail on missing, duplicate, wrong-kind, or wrong-field MemOp membership, but must not fail merely because BasicBlock/HashMap iteration order changes. No production code or MIR is mutated by this row."
census_boundary = "src/mir/builder/fastmem/tests/branch.rs positional field_memops assertion -> order-independent exact membership assertion; includes only the named test and its exact fastmem test module, excludes FastMem production lowering, whole-lib baseline seeding, selected-C, Call schema, and backend changes."
finite_states = [
  "ExactMembership: both expected field MemOps appear exactly once with the expected kind",
  "MissingOrDuplicate: cardinality or membership differs; test fails",
  "OrderOnlyDifference: same exact membership in another iteration order; test passes",
]
ordered_tasks = [
  "R0-A: replace only the positional field_memops equality with a deterministic sorted/exact-membership assertion",
  "R0-B: run the exact target repeatedly plus the complete fastmem test module and a quick library check",
  "R0-C: record that production delta is zero, then close out and return to the selected-C design stop",
]
allowed_files = [
  "src/mir/builder/fastmem/tests/branch.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/function/types.rs",
  "src/mir/builder/fastmem/** production lowering",
  "tools/checks/guard_rows.toml",
  "tools/checks/lib/dev_gate_quick_steps.sh",
  "src/mir/builder/calls/**",
  "MIR Call schema",
  "VM/backend/JSON/runtime",
  "selected-C admission",
]
focused_test_filters = [
  "mir::builder::fastmem::tests::branch::fastmem_source_lowers_owner_eq_branch_cfg_pilot",
  "mir::builder::fastmem::tests",
]
changed_test_names = [
  "mir::builder::fastmem::tests::branch::fastmem_source_lowers_owner_eq_branch_cfg_pilot",
]
verification_prerequisites = [
  "exact target filter must run nonzero and green in at least ten separate processes",
  "the complete mir::builder::fastmem::tests module must run nonzero and green",
  "cargo check --profile quick --lib, current-state pointer guard, and git diff --check must pass",
  "whole-lib red baseline remains separate and is not claimed green or seeded by this row",
]
acceptance = "The named FastMem test accepts any HashMap iteration order while enforcing exact field MemOp kind/name membership and cardinality; ten separate exact runs and the complete fastmem test module are green; production Rust/MIR/Call/backend delta is zero; touched source stays below 760/800 lines; pointer and diff checks pass; then the pointer returns to MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0 design_stop."
no_safe_slice = "Return to design_stop if the assertion cannot prove exact membership without production changes, if a second nondeterministic owner is found, if the full fastmem module has current-change red, if the row needs quick-step wiring or baseline seeding, or if any MIR/Call/backend/selected-C file is touched."
non_claims = "No whole-lib baseline acceptance, no FastMem production change, no CSE/Call convergence credit, no selected-C admission, no Method(Some), no language or backend change, no fallback/retry, and no guard retirement."
reopen_trigger = "Reopen only if the FastMem test's emitted inventory changes shape, another order-dependent assertion is found within the same baseline owner, or the exact test/module execution becomes non-deterministic again."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0__design_stop"

[row_exit_changed_test_exec_p0_2026_08_31]
status = "accepted_design_stop"
task_id = "ROW-EXIT-CHANGED-TEST-EXEC-P0"
parent_row = "DEV-GATE-QUICK-LIB-BASELINE-P0"
box_kind = "BoxShape closeout-proof helper only"
implementation_permission = false
decision = "Separate changed-test execution proof from the active MirCall guards. A row may claim focused evidence only when every added, renamed, modified, or explicitly retired test in the exact implementation diff is mapped to a unique cargo test -- --list path and actually executed by an exact filter or exact test invocation."
source_authority = "The implementation row's declared base_commit and implementation_commit diff, cargo test -- --list output, and the recorded exact test invocations. Prose test names, substring presence, and a later HEAD diff are not authority."
non_authority = [
  "focused test counts selected by hand",
  "cargo test -- --list without execution",
  "substring filters that match zero or multiple paths",
  "a post-implementation HEAD diff",
  "full-lib baseline counts used as a substitute for changed-test coverage",
]
fail_fast_boundary = "Reject before row closeout when a changed test cannot be mapped uniquely, a retired test lacks an explicit removal record, an exact invocation runs zero tests or an ignored test, a declared filter is never executed, or any executed test fails."
census_boundary = "exact base_commit..implementation_commit Rust test diff -> added/renamed/modified/retired test inventory -> cargo --list full paths -> exact executions and filters -> row closeout receipt; excludes production semantics, daily full-lib baseline, legacy-tests fate, and active-guard implementation."
finite_states = [
  "Covered: unique listed path and nonzero exact execution recorded",
  "Unmapped: diff test cannot be identified in the cargo list",
  "Unexecuted: listed path exists but no exact execution occurred",
  "ZeroMatch: declared filter selects no tests",
  "Ignored: selected test is ignored rather than run",
  "RetiredWithRecord: removed test has an explicit retirement reason and owner",
]
ordered_tasks = [
  "D0: define the exact implementation diff boundary and the full-path cargo --list parser",
  "I0: add the reusable helper and unit negatives for unmapped, zero-match, ignored, and unexecuted cases; do not embed it in active_surface_rows.py",
  "C0: apply it to the next semantic row and record the complete exact invocations before closeout",
]
allowed_files = [
  "tools/checks/lib/row_exit_changed_test_exec.py",
  "tools/checks/lib/tests/test_row_exit_changed_test_exec.py",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
forbidden_files = [
  "tools/checks/lib/mir_call_d1b_active_surface_rows.py",
  "tools/checks/guard_rows.toml",
  "src/mir/**",
  "legacy-tests",
  "Call schema",
]
acceptance = "The helper proves exact diff-to-list-to-execution coverage with nonzero results and explicit retirement records; synthetic negative cases fail closed; no active MirCall guard or compiler behavior changes."
no_safe_slice = "Remain in design_stop if the diff cannot distinguish a semantic test edit from unrelated later HEAD changes, if a test path is not unique, or if the helper requires a broad guard registry rewrite."
non_claims = "No full-lib green claim, no baseline seeding, no semantic implementation permission, no legacy-tests decision, and no Call-schema or backend change."
reopen_trigger = "Reopen when the test harness changes its listing format, a row uses a new test macro/fixture shape, or a closeout records a filter that does not execute a nonzero exact path."

[mir_metadata_consumer_manifest_anchor_refresh_d0_2026_08_31]
status = "landed"
task_id = "MIR-METADATA-CONSUMER-MANIFEST-ANCHOR-REFRESH-D0"
parent_row = "DEV-GATE-QUICK-LIB-BASELINE-P0"
box_kind = "BoxShape verification receipt refresh only"
implementation_permission = false
decision = "Keep the metadata source, checker, and guard unchanged. Refresh every stale manifest anchor to the current line containing the same observed token; do not change owners, counts, categories, backend roles, caller states, or observed revision. A single root.rs:140 edit is incomplete: the exhaustive refresh is 16 literal occurrences across 11 old-to-current anchor mappings."
source_authority = "The live source statement is the observation authority. The JSON manifest is a historical census receipt, and the Python checker plus shell guard validate it; none is semantic authority and none may force source movement or weaken validation."
non_authority = [
  "manifest line numbers as semantic meaning",
  "checker/guard success as proof of an exhaustive current owner census",
  "owner/count/category/backend-role/caller-state changes",
  "observed_revision refresh",
  "DeclaredInstance, Call schema, backend, VM, JSON behavior, or source edits",
]
fail_fast_boundary = "Stop before any quick-baseline claim if any manifest anchor is missing, stale, duplicated, or points at a changed statement; do not continue from the first error by updating only one anchor."
census_boundary = "tools/checks/manifests/mir_function_metadata_consumer_manifest_v1.json anchors -> current source token statements -> metadata checker -> metadata catalog guard -> dev_gate quick metadata step; includes only literal anchor refresh, excludes live owner recensus, source behavior, manifest semantics, and gate wiring."
finite_inventory = [
  "pinned_text_access_plans egress: root.rs:140 -> root.rs:153 (1)",
  "pinned_text_residence_backend_carrier egress: root.rs:122 -> root.rs:135 (1)",
  "is_entry_point producer/consumer: module_lifecycle.rs:363 -> module_lifecycle.rs:380 (2)",
  "value_origin_callers consumer: builder_emit.rs:417 -> builder_emit.rs:433 (1)",
  "runes egress: root.rs:219 -> root.rs:232 (1)",
  "string_dead_text_region_plans producer/consumer: string_dead_text_region_plan.rs:149 -> :146 (2)",
  "direct_array_access_plans producer/reference: handlers/calls/mod.rs:140 -> :130 (2)",
  "extern_call_routes producer/consumer: route.rs:222 -> :224 (2)",
  "builtin_global_call_routes producer/consumer: global_call_route_plan.rs:550 -> :551 (2)",
  "exact_seed_backend_route producer: mir/mod.rs:331 -> :365 (1)",
  "exact_seed_backend_route consumer: mir/mod.rs:123 -> :155 (1)",
]
ordered_tasks = [
  "D0: verify the 16 literal occurrences and 11 old-to-current mappings against the current source without changing source or manifest semantics",
  "R0: refresh only those manifest anchors, preserving observed_revision, owners, counts, classes, roles, and caller states byte-for-byte",
  "C0: run the metadata checker, metadata manifest guard, metadata catalog guard, pointer guard, and diff check; rerun dev_gate quick only to record the next first failure, never to claim whole quick green",
  "Separate D0: record the historical-vs-live owner-census temporal semantics and any stale owner paths before using this manifest for retirement or production cutover evidence",
]
allowed_files = [
  "tools/checks/manifests/mir_function_metadata_consumer_manifest_v1.json",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/runner/mir_json_emit/root.rs",
  "src/mir/**",
  "tools/checks/mir_metadata_consumer_manifest.py",
  "tools/checks/mir_metadata_consumer_manifest_guard.sh",
  "tools/checks/guard_rows.toml",
  "dev_gate order or registry",
  "reference/backend/VM/JSON behavior",
  "DeclaredInstance/Method/Call semantics",
]
acceptance = "All 16 stale anchor literals are refreshed and the manifest checker, metadata manifest guard, and metadata catalog guard pass; owners/counts/classes/roles/caller states/observed_revision are unchanged; dev_gate quick advances past this metadata step and records its next first failure; no whole-quick or whole-lib green claim is made."
no_safe_slice = "Remain in design_stop if an anchor token moved semantically, the owner/count/category must change, the checker rejects a current statement, or a source/guard/gate edit is needed. Send those findings to a separate temporal-semantics recensus row."
non_claims = "No current exhaustive metadata owner census, no field retirement authority, no source behavior change, no quick/full-lib green claim, no DeclaredInstance issuer/co-seal, no Method(Some) production switch, no Call-schema or backend change."
reopen_trigger = "Reopen the refresh only when one of the 16 statements moves again, the manifest schema/checker changes, or the manifest is about to be used as current exhaustive evidence; in the latter case require a separate live-owner recensus first."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0__design_stop after the verification prerequisite is closed; keep semantic implementation permission false."

[ny_llvmc_selected_launch_snapshot_stack_retire_r0_2026_08_31]
status = "landed"
disposition = "CutoverBlockerClosed"
task_id = "NY-LLVMC-SELECTED-LAUNCH-SNAPSHOT-STACK-RETIRE-R0"
parent_row = "DEV-GATE-QUICK-LIB-BASELINE-P0"
box_kind = "BoxShape selected-C backend stack ownership repair"
implementation_permission = false
decision = "Treat the phase-1 runtime-helper guard's stack overflow as a real selected-C baseline regression, not as a 16 MiB stack waiver. The 8 MiB process stack is the acceptance boundary; move only the invocation-local launch and planned-definition context snapshots to heap ownership while preserving each save, activate, emit, restore, and free order."
source_authority = "The daily quick order is owned by tools/checks/lib/dev_gate_quick_steps.sh; the behavior contract is stage1_emit_program_json_runtime_helper_guard.sh; the selected physical owners are selected_launch_emit.inc and the planned-definition emission seam split into same_module_function_definition_emit.inc."
canonical_issuer = "The existing selected-C launch and planned-definition snapshot construction/activation owners; no new MIR or source semantic issuer is allowed."
non_authority = [
  "fixture shape, route trace, guard text, RUST_MIN_STACK, ulimit, Rust CLI behavior, capacity reduction, optimization flags, and alternate backend success",
  "DeclaredInstance/Method/Call schema, MIR effect/ABI, runtime helper naming, fallback, retry, or a larger process stack",
]
fail_fast_boundary = "At an 8 MiB process stack, the fresh selected-C FFI must reach the existing exact consumer trace and non-empty object; allocation failure must return a typed error before context activation/save mutation, with no output, fallback, or retry."
census_boundary = "stage1_emit_program_json_runtime_helper_guard.sh -> fresh ny-llvmc selected-C FFI -> selected launch and planned-definition snapshot save/activate/emit/restore/free; includes only these duplicated stack-owned snapshots and their focused guard/smoke evidence, excludes fixture semantics, MIR/Call changes, backend selection, and quick baseline policy."
finite_inventory = [
  "guard contract: tools/checks/stage1_emit_program_json_runtime_helper_guard.sh:37-57",
  "selected-C launch owner: lang/c-abi/shims/hako_llvmc_ffi_selected_launch_emit.inc:10",
  "selected-C planned-definition owner: lang/c-abi/shims/hako_llvmc_ffi_same_module_function_definition_emit.inc",
  "selected-C definition seam: lang/c-abi/shims/hako_llvmc_ffi_same_module_function_emit.inc",
  "selected-C lowering state: lang/c-abi/shims/hako_llvmc_ffi_generic_pure_lowering_state.inc:58-111",
  "context snapshot view: lang/c-abi/shims/hako_llvmc_ffi_same_module_function_context.inc:8-40",
  "fresh compiler artifact owner: crates/nyash-llvm-compiler and existing selected-Dynamic smoke",
]
finite_states = [
  "EightMiBOverflow: current guard aborts with rc=134 before object emission",
  "HeapSnapshotCandidate: both invocation-local snapshots may be heap-owned with order preserved",
  "ExactSelectedConsumer: existing trace and non-empty object at 8 MiB",
  "AllocationRejected: typed failure before save/activation and no output",
  "UnsafeShortcut: 16 MiB waiver, static/TLS snapshot, capacity/fixture change, fallback, or retry",
]
ordered_tasks = [
  "D0: confirm parent/current byte identity for guard, fixture, fresh FFI, and selected-C source; record the 8 MiB overflow and 16 MiB non-authority observation",
  "R0-A: heap-own the selected launch snapshot; preserve save -> activate -> emit -> restore -> free",
  "R0-B: split the planned-definition seam without behavior change, then heap-own saved_context with the same lifecycle",
  "C0: run the existing runtime-helper guard plus selected-Dynamic positive/negative and allocation-failure evidence at 8 MiB; require fresh FFI evidence and return to the health parent design stop",
]
allowed_files = [
  "lang/c-abi/shims/hako_llvmc_ffi_selected_launch_emit.inc",
  "lang/c-abi/shims/hako_llvmc_ffi_same_module_function_definition_emit.inc",
  "lang/c-abi/shims/hako_llvmc_ffi_same_module_function_emit.inc",
  "lang/c-abi/shims/hako_llvmc_ffi_same_module_function_context.inc",
  "lang/c-abi/shims/README.md",
  "tools/checks/stage1_emit_program_json_runtime_helper_guard.sh",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "existing selected-Dynamic smoke/guard evidence",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
]
forbidden_files = [
  "RUST_MIN_STACK/ulimit waiver",
  "fixture or capacity/state-shape changes",
  "Rust CLI route or alternate backend",
  "MIR/Call/DeclaredInstance semantics",
  "fallback/retry/static/TLS global snapshot",
]
acceptance = "At the 8 MiB stack boundary the existing selected-C runtime-helper guard reaches its exact consumer trace and non-empty object with zero stack-owned SameModuleFunctionContextSnapshot values; both allocation-failure branches are typed and side-effect-free before activation; the selected-Dynamic focused evidence remains green; and no fixture/ABI/route/semantic behavior changes."
no_safe_slice = "Remain in design_stop if heap ownership cannot preserve the existing snapshot lifecycle, if the fresh FFI is not proven, or if the repair needs stack enlargement, fixture/capacity changes, static/TLS state, alternate backend, or semantic route edits."
non_claims = "No quick/full-lib green claim, no 139-failure baseline acceptance, no Stage1 artifact completion, no DeclaredInstance issuer/co-seal, no Method(Some) or Call-schema cutover, and no VM/backend policy change."
reopen_trigger = "Reopen when selected-C launch state shape, FFI freshness, the guard contract, or the 8 MiB result changes; keep this row separate from the metadata historical/live census task."
changed_anchor_occurrences = 16
unique_anchor_mappings = 11
stale_anchor_occurrences = 0
verification_commands = [
  "R0-B: bash tools/build_hako_llvmc_ffi.sh: fresh selected-C FFI build passed",
  "R0-B: bash -c 'ulimit -s 8192; bash tools/checks/stage1_emit_program_json_runtime_helper_guard.sh': exact selected-C consumer trace and non-empty object passed at 8 MiB",
  "R0-B: repeated 8 MiB runtime-helper guard: passed with fresh FFI artifact; no stack abort",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
r0_a_commit = "68a030dc95"
r0_b_commit = "46b2ee72e3"
base_commit = "3967435efc"
implementation_commit = "46b2ee72e3"
implementation_evidence = "R0-A heap-owned the selected launch snapshot, and R0-B split the planned-definition helpers into hako_llvmc_ffi_same_module_function_definition_emit.inc and heap-owned saved_context. A fresh FFI rebuild followed by two repeated 8 MiB selected-C runtime-helper guard runs reached the exact consumer trace and non-empty object. The active-surface and pointer guards passed; no fixture, ABI, route, MIR, Call, fallback, retry, or DeclaredInstance behavior changed."
red_classification = "R0-A removed the launch snapshot from the stack. The follow-up R0-B split the planned-definition helpers and heap-owned saved_context; fresh FFI rebuild plus the 8 MiB selected-C guard now pass, so this selected-C stack blocker is closed. Whole-lib and quick-gate baseline health remain separate and unclaimed."
closeout = "Selected-C stack health landed at 46b2ee72e3. Restore CURRENT_STATE to MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0 design_stop with next_execution_card=none; keep full lane/selected-C semantic admission, receiver ValueId, Method(Some), package/loan, MIR, backend, fallback, retry, and Call-schema publication closed."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0__design_stop"

[mir_call_cataloged_print_target_arm_prune_r0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PRINT-TARGET-ARM-PRUNE-R0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-CATALOGED-PRINT-CALLER-ZERO-RETIRE-I0"
ceremony_tier = "T0"
box_kind = "BoxShape caller-zero dead target-preparation arm prune"
implementation_permission = false
decision = "The already-retired InstalledNonBrand Cataloged FunctionCall print branch has one remaining unreachable print-specific target arm inside prepare_cataloged_target_v1. Delete only that arm; preserve the earlier typed BuiltinPrintCataloged terminal, the dedicated ASTNode::Print -> builtin_print() owner, and all generic BuiltinGlobal behavior."
source_authority = "Parser-owned ASTNode::Print and CanonicalGlobalTargetV1::builtin_print() remain the sole print production authority. This row issues no target and does not alter the existing retirement terminal."
canonical_issuer = "Existing stmts/print_stmt.rs builtin_print() only; no ordinary FunctionCall name/arity arm may issue print."
non_authority = [
  "prepare_cataloged_target_v1's dead print arm, CallNameCalleeClassV1::BuiltinGlobal, name/arity synthesis, resolver, catalog, registry, symbol, MIR, runtime, test fixture, fallback, and retry",
]
fail_fast_boundary = "If the top-level InstalledNonBrand caller-present print retirement is not still before prepare_cataloged_target_v1, or if any non-test production caller reaches the helper for print, stop before deleting the arm."
census_boundary = "prepare_ordinary_function_completion_v1's InstalledNonBrand caller-present print terminal -> prepare_cataloged_target_v1 BuiltinGlobal branch; includes the sole helper caller, dedicated Print owner, existing print tests, and reusable active-surface guard; excludes generic BuiltinGlobal names, ASTNode::Print lowering, panic/exit/error/now, Method/Call schema, VM/backend, and guard retirement."
finite_inventory = [
  "sole helper caller: src/mir/builder/calls/function_call_preflight_route.rs::prepare_ordinary_function_completion_v1",
  "dead arm: prepare_cataloged_target_v1 name == print -> CanonicalGlobalTargetV1::builtin_print()",
  "retained terminal: RawOrdinaryFunctionRetirementV1::BuiltinPrintCataloged",
  "sole production print owner: src/mir/builder/stmts/print_stmt.rs::builtin_print",
  "focused behavior evidence: function_call_installed_gc_builtin_tests.rs and function_call_preflight_route_tests.rs",
]
finite_states = [
  "RetiredBeforeHelper: InstalledNonBrand caller-present print reaches BuiltinPrintCataloged before helper; retained",
  "DeadTargetArm: helper-local print target arm has no production path; delete",
  "GenericBuiltinGlobal: all non-print generic handling remains unchanged",
  "UnexpectedCaller: a production helper print caller is a NoSafeSlice",
]
old_edge_delete_set = [
  "prepare_cataloged_target_v1 name == print -> CanonicalGlobalTargetV1::builtin_print()",
]
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "src/mir/builder/stmts/print_stmt.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_cataloged_print_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/resolver.rs",
  "Method/Call schema",
  "panic/exit/error/now",
  "VM/backend/JSON/runtime",
  "package/loan",
  "guard registry deletion",
]
ordered_tasks = [
  "D0: confirm the helper has exactly one production caller and that the earlier typed print terminal dominates it",
  "R0: remove only the helper-local print target arm; preserve generic BuiltinGlobal handling and the dedicated Print owner",
  "C0: run existing cataloged-print positive/negative and Print-owner tests, structural caller-zero guard, quick check, and return to the parent design stop",
]
acceptance = "The helper-local print target arm is absent; the earlier BuiltinPrintCataloged terminal remains before helper entry; the dedicated ASTNode::Print owner still projects builtin_print(); generic BuiltinGlobal names are unchanged; focused positive/negative tests and the reusable guard pass; no new issuer, resolver, target, effect, fallback, or retry is introduced."
no_safe_slice = "Do not land if any production/test caller outside the known helper reaches the deleted arm, the earlier terminal is no longer total, generic BuiltinGlobal behavior must change, or the row crosses print statement semantics, providers, Method/Call schema, VM/backend, package/loan, or 760/800 boundaries."
non_claims = "No provider-wide closure, no panic/exit/error/now decision, no ordinary static/DeclaredInstance/Method/Call cutover, no Call schema, VM/backend/JSON, package/loan, fallback/retry, or guard deletion."
focused_test_filters = [
  "cataloged_print",
  "cataloged_target_preflight_applies_total_shadow_order",
  "mir::builder::stmts::print_stmt::tests::",
]
changed_test_names = []
implementation_commit = "125971b5ed"
focused_test_results = [
  "cargo test --profile quick --lib cataloged_print -- --test-threads=1: 2 passed, 0 failed",
  "cargo test --profile quick --lib cataloged_target_preflight_applies_total_shadow_order -- --test-threads=1: 1 passed, 0 failed",
  "cargo test --profile quick --lib mir::builder::stmts::print_stmt::tests:: -- --test-threads=1: 3 passed, 0 failed",
  "cargo check --profile quick --lib: passed",
]
guard_result = "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass; current_state_pointer_guard.sh: pass; git diff --check: pass"
line_count_result = "function_call_preflight_route.rs remains below the 760-line boundary; no production source crossed 800"
evidence = "At implementation commit 125971b5ed, source-wide production helper census remained one route file with one callsite plus the definition. The unreachable helper-local print projection was deleted; the earlier BuiltinPrintCataloged terminal and dedicated ASTNode::Print -> builtin_print() owner remain. Existing cataloged-print positive/negative tests (2), total-shadow-order test (1), Print-owner tests (3), quick library check, rustfmt, active guard, pointer guard, and diff check passed."
red_classification = "No current-change red. Full cargo test/link was not claimed."
guard = "Reuse the existing cataloged-print active-surface dispatch and extend its structural check to assert the helper-local print arm is absent and the sole caller/earlier terminal relationship remains."
reopen_trigger = "Reopen only if a production caller for the helper-local print arm appears, the dedicated ASTNode::Print owner changes, or the earlier typed print terminal no longer dominates helper entry."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_call_me_declared_instance_relation_issuer_d0_2026_08_31]
status = "accepted_design_stop"
disposition = "CutoverBlockerOpen"
development_state = "NoSafeSlice"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
design_parent = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T2"
box_kind = "BoxShape in-place authority replacement with one new resolver relation prerequisite"
implementation_permission = false
decision = "Retain true DeclaredInstance me.method as ordinary syntax. Its canonical logical MIR is Method(Some(receiver)) with N explicit source arguments; the receiver is never duplicated in args. Target selection is resolver-owned, package assembly is transport/contract co-seal only, and the selected C path receives the full physical lane vector. The legacy Global/header/symbol/receiver-prefix path remains live until one successor-backed atomic cutover deletes it."
method_namespace_decision = "LANG-BOX-METHOD-NAMESPACE-D0 is accepted: one nominal Box owns one flat visible-method namespace keyed by exposed name. Direct static/instance methods and delegate forwarders do not overload by arity or receiver kind. Same-name declarations in one Box reject; same names on different Boxes remain distinct; FreeStatic name/arity overload is a separate namespace."
source_authority = "The exact source authority is the resolver-owned method-call row plus root receiver binding, the same parser provenance and ResolverCatalogBrandV1, and the carrier row's exact nominal box type. Inside that nominal declaration subset selector selects one declaration and arity validates its signature; selector/arity are not identity and never start a second Builder lookup."
canonical_issuer = "No complete production issuer exists at this HEAD. The only accepted target-selection owner is a future resolver sibling relation in src/mir/resolved_semantics/declared_instance_call_relation.rs, co-issued from the same live parser/resolver source as VerifiedInstanceMethodFunctionCarrierCatalogV1 and VerifiedInstanceMethodDeclarationCatalogV1. A package assembler may consume that relation but may not issue or repair it."
package_role = "The normal callable package may only co-seal an already-issued resolver relation with the existing selected batch slot, complete result/completion carriage, semantic effect row, and full PhysicalCallableLaneV1 signature. Builder catalog keys and package brands are validation/transport vocabulary, not source target authority."
effect_decision = "LANG-ORDINARY-DECLARED-INSTANCE-CALL-EFFECT-D0 is accepted in docs/reference/language/callable-contracts.md: no CallableContract means the explicit OpaqueObservable effect-axis contract; Query takes precedence. Production issuer count remains zero."
result_contract = "Reuse DeclaredFunctionResultContractV1 and VerifiedFunctionCompletionV1. Preserve Unannotated, Void, and supported Annotated states through package install; never collapse Unannotated/Void to missing and never infer result meaning from FunctionSignature, MIR, target body, or backend."
selected_physical_boundary = "selected physical consumer count = 1 (Boundary pure-first C path); exact DeclaredInstance consumer count = 0. The later cell must preserve receiver presence separately from numeric ValueId 0, consume the full logical-to-physical lane vector, and perform one direct same-module call without duplicate-receiver stripping or runtime lookup."
non_authority = [
  "package-side declaration_for(owner/name/arity), CanonicalSameModuleCallableKeyV1 text, selected slot, package brand, current_static_box, variable_map, generated symbol, first-Box header, registry, physical MIR/header, backend/VM behavior, fixtures, fallback, and retry",
  "a lexical me binding alone, FunctionSignature, EffectMask, Query's selected-only pipeline, Dynamic's full envelope, C duplicate-receiver repair, VM receiver autoscan, and any result/effect/ABI record not bound to the exact declaration identity",
]
fail_fast_boundary = "The resolver relation, result/effect contracts, full lane signature, and selected C capability must all be sealed and the disposition taken before receiver ValueId materialization, argument descent/effects, Const/MIR publication, collector mutation, backend need-scan, or fallback. Missing, duplicate, foreign, ambiguous, unsupported, second-take, or residual states have one typed terminal and no retry."
census_boundary = "exact root DeclaredInstance me.method source row -> resolver-owned nominal declaration relation -> package result/effect/full-lane co-seal -> common affine disposition -> Method(Some(receiver)) with N explicit args -> selected Boundary C direct call -> caller-zero deletion of the three legacy edges; includes only root lexical receiver binding, and excludes nested/upvar me, StaticCurrentOwner, Method(None), Dynamic/qualified/core calls, Call schema, VM/llvmlite/WASM, panic/throw/Result/?, and unrelated cleanup."
finite_inventory = [
  "source call facts: resolved_semantics/body_shape.rs::VerifiedResolvedMethodCallSourceV1",
  "target source: resolved_semantics/instance_method_function_carrier.rs + instance_method_declaration.rs, same parser provenance/resolver brand/nominal source identity",
  "receiver/input ABI: normal_callable_semantic_package/physical_signature.rs; full logical ordinal to physical lane mapping",
  "result/completion: resolved_control_flow/function_control.rs plus the landed package retention cohort at e1976b8ba1; generic carriage is retained through selected package install",
  "semantic effect: accepted OpaqueObservable/Query precedence in callable-contracts.md; production issuer absent",
  "selected physical seam: Boundary pure-first C path; exact DeclaredInstance capability absent",
  "legacy consumer/delete set: builder/method_call_handlers.rs, calls/method_call_terminal.rs, and unified_emitter.rs",
]
finite_states = [
  "OutsideRootDeclaredInstance: StaticCurrentOwner and non-receiver sources keep named owners; nested/upvar me is ParkedSealed until capture/receiver ABI is explicit",
  "CrossOwnerSameName: the same spelling on different nominal Boxes is valid; the exact owner/receiver relation selects one Box namespace",
  "DuplicateBoxMethodName: a second direct static/instance or generated forwarding name in one Box rejects before resolver/package/Builder publication, even when arity differs",
  "DelegateExposeCollision: an exposed name colliding with a host method or another exposure rejects the whole unpublished forwarding batch; only a distinct explicit alias is valid",
  "MissingTarget: zero nominal declaration rows; typed pre-effect reject",
  "ForeignRelation: parser provenance, resolver brand, nominal type, owner, receiver binding, source site, declaration identity, or selected slot differs; typed pre-effect reject",
  "ShapeDrift: selector/arity, result/completion, effect, or full lane signature differs; typed pre-effect reject without Box-method overload search or repair",
  "SelectedCapabilityMissing: receiver presence, multi-lane formals, result, or effect is unsupported by the selected C cell; typed pre-effect reject, no backend fallback",
  "Ready: one complete disposition may be taken once before receiver/arguments",
  "TakenOrResidual: second take, wrong site/owner/variant, or unconsumed row is a typed abort; successful finish requires residual zero",
]
relation_shape = "The resolver relation owns exact caller/source/receiver binding and an opaque resolver declaration identity under one parser provenance/brand/nominal type. It carries no Builder key/slot, AST, ValueId, Callee, symbol, MIR, ABI, effect, result, or cloned arguments. Package/private disposition adds borrowed contract locators only after exact identity is already issued."
ordered_tasks = [
  "LANG-BOX-METHOD-NAMESPACE-D0 (accepted): keep one visible method name per nominal Box; static/instance, arity, and delegate forwarding create no overload namespace; FreeStatic name/arity remains separate",
  "LANG-ORDINARY-DECLARED-INSTANCE-CALL-EFFECT-D0 (accepted): name unannotated OpaqueObservable and Query precedence; production remains zero",
  "MIR-NORMAL-CALLABLE-RESULT-CONTRACT-RETENTION-D0/I0: retain DeclaredFunctionResultContractV1 plus VerifiedFunctionCompletionV1 through normal package install, distinguishing Unannotated/Void/Annotated",
  "MIR-CALL-ME-DECLARED-INSTANCE-RESOLVER-SOURCE-ADOPTION-D0 then MIR-CALL-ME-DECLARED-INSTANCE-RESOLVER-RELATION-I0: co-issue the root-receiver callsite-to-declaration relation from the live final parser source; selector resolves only inside the exact nominal subset and arity only validates",
  "LANG-ORDINARY-DECLARED-INSTANCE-CALL-EFFECT-ISSUER-I0: issue the effect row from that same live resolver declaration identity; no hidden default, EffectMask projection, or Dynamic envelope reuse",
  "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-CAPABILITY-D0/I0: co-seal selected slot, complete result/effect, and full physical lane vector; prove explicit receiver presence and remove duplicate-receiver repair in the one selected C cell",
  "MIR-CALL-ME-DECLARED-INSTANCE-CANONICAL-I0 (atomic): install one common Ready/take_once/finish_empty disposition, materialize receiver once, lower N source args left-to-right once, emit Method(Some(receiver)) once, and delete the selected legacy first-Box/header, formatted Global/symbol, and manual receiver-prefix edges",
  "MIR-CALL-ME-DECLARED-INSTANCE-CLOSEOUT-C0: recount old edges 3 -> 0 with fallback/retry 0, then return to the SameModule parent; Method(None), Call schema, nested/upvar me, and nonselected backends remain separate",
  "DEL-003 delegate forwarding (separate ParkedSealed row): consume the existing exact field/expose/source relation, require one unique host alias, and connect forwarding without overload search, implicit override, fallback, or partial batch publication",
]
acceptance = "D0 closes only when the root DeclaredInstance boundary has one resolver-owned target relation, complete source effect/result/completion and full-lane ABI carriage, one selected C capability, and one common affine consumption lifecycle. This design row issues no ValueId, Callee, MIR, backend code, fallback, retry, or production success edge."
no_safe_slice = "Remain in design_stop while any E1/R0/T0/P0 prerequisite is absent. Do not compensate with package declaration_for, name/header/registry/symbol lookup, physical/body/backend inference, receiver autoscan, args[0] repair, a family-specific loan, a second resolver, fallback/retry, or a source file at/over the 760/800 boundary."
non_claims = "No current canonical DeclaredInstance replacement implementation or old-edge deletion; the legacy instance path remains live but is not canonical. No nested/upvar receiver support, Method(None), Call schema, Dynamic/core/qualified call, VM/llvmlite/WASM, panic/throw/Result/?, or broad backend parity work."
allowed_files = [
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/reference/language/callable-contracts.md",
  "docs/reference/language/function-call-evaluation.md",
  "docs/reference/language/quick-reference.md",
  "docs/reference/language/field-visibility-and-delegation.md",
  "docs/reference/language/EBNF.md",
  "docs/reference/language/status-index.md",
]
forbidden_files = [
  "src/mir/normal_callable_semantic_package/declared_instance_call.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "Call schema",
  "VM/backend/JSON/runtime",
  "panic/throw/Result/?",
]
worker_audit_result = "Fresh read-only audits at HEAD c95c2e9b97 agree that me.method is retained and package-side target issuance is invalid. The resolver-owned live parser/resolver source is the only candidate for the exact callsite-to-declaration relation; the package may only co-seal already-issued products. Result/Completion retention is landed, while ordinary effect issuance, the relation crosswalk, common affine lifecycle, and an exact selected C capability remain absent. The selected C seam is physically direct but currently reconstructs symbols, conflates receiver absence with numeric zero, assumes narrow lanes, and cannot issue result/effect meaning. Nested/upvar me remains outside the root cohort."
old_edge_progress = "first-Box/header classifier 1, formatted Global/symbol reissuer 1, manual receiver-prefix writer 1; current 3, target 0. Design products and local tests do not reduce this count."
line_budget = "New resolver relation <= 300 lines and tests <= 360. Do not add semantic responsibility to body_shape.rs (733), normal_callable_semantic_loan_port.rs (748), builder.rs (747), or package tests.rs (743); method_call_handlers.rs and unified_emitter.rs are deletion/delegation-only. Every source remains below 760; 800 is a hard stop."
reopen_trigger = "Implementation opens only after E1/R0/T0/P0 are live in the same source/package session, the selected C capability distinguishes receiver presence and supports the borrowed full lane/result/effect contract, and the atomic row can delete all three selected legacy edges with fallback/retry zero."
smallest_next_slice = "MIR-CALL-ME-DECLARED-INSTANCE-EFFECT-ISSUER-D0: decide the live parser callable-contract carriage and one same-session semantic-effect issuer for the already-landed source relation; no target, ValueId, ABI, package loan, or backend meaning is issued."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0__design_stop"

[mir_normal_callable_result_contract_retention_d0_i0_2026_08_31]
status = "landed_with_known_baseline_red"
task_id = "MIR-NORMAL-CALLABLE-RESULT-CONTRACT-RETENTION-D0-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
ceremony_tier = "T1"
box_kind = "BoxShape package carriage; preserve an already-issued language contract"
implementation_permission = false
decision = "Keep one existing verify_function_completion_v1 result for every selected Cataloged callable row through package install. Preserve the three source states Unannotated, Void, and Annotated(name); do not use Option<ExactTrivialScalarAbiV1> as the semantic carrier. The package-private cohort owns the original non-Clone VerifiedFunctionCompletionV1 and borrows its DeclaredFunctionResultContractV1. S6C remains an exclusive MovedToS6CChild disposition; all other rows use RetainedGeneric. Physical header remains a sparse projection (None for Unannotated/Void, ExactTrivialScalar(I64) for the existing supported scalar) and never owns or clones Completion."
source_authority = "The existing resolver-owned verify_function_completion_v1 result and DeclaredFunctionResultContractV1 are the sole semantic authority. issue_callable_completion_seed_cohort_v1 is the one package invocation that obtains them; the new result_contract.rs assembler only retains and co-seals those products."
canonical_issuer = "Existing verify_function_completion_v1; no second result/completion verifier is introduced. result_contract.rs may issue only a package-private retention cohort from the existing seed rows."
non_authority = [
  "FunctionSignature, MirType, MIR/body shape, physical header presence, backend plan, tests, fixtures, name/arity, and target symbols",
  "None as a semantic result state, empty/default receipt, result inference, Completion clone, fallback, or retry",
]
fail_fast_boundary = "Missing, duplicate, foreign, owner/identity/role mismatch, unsupported annotation, or storage-disposition collision rejects during package assembly/install before Builder receiver/argument effects, physical-header projection, backend emission, or fallback."
census_boundary = "selected Cataloged completion seed -> package-private retained result/completion row -> optional sparse physical-header projection -> install/selected input borrow; includes ordinary selected Cataloged rows and the one S6C child disposition, excludes TopLevel, Dynamic semantic meaning, DeclaredInstance target issuance, new ABI carriers, Call schema, VM/backend, and language syntax changes."
finite_states = [
  "Unannotated: semantic result contract retained; physical header absent",
  "Void: explicit Unit contract retained; physical header absent",
  "AnnotatedI64: semantic contract retained; sparse ExactTrivialScalar(I64) projection may be borrowed",
  "AnnotatedUnsupported: typed UnsupportedResultAnnotation before package publication",
  "MovedToS6CChild: one selected S6C row owns Completion in the existing child; generic cohort must not also retain it",
  "RetainedGeneric: non-S6C selected Cataloged row owns Completion in the generic cohort",
  "MissingOrDuplicate: typed issue before install; no silent drop",
]
owned_shape = [
  "exact batch_slot + owner + parser CallableDeclarationIdentity + selected role",
  "original VerifiedFunctionCompletionV1 moved once",
  "DeclaredFunctionResultContractV1 borrowed from that Completion",
  "optional existing ExactTrivialScalarAbiV1 physical projection, never the semantic source",
  "exclusive RetainedGeneric | MovedToS6CChild storage disposition",
]
ordered_tasks = [
  "D0: accept this bounded selected-Cataloged retention boundary without changing target selection, physical ABI, or language syntax",
  "I0-A: add one private result_contract.rs cohort and move remaining completion seeds into it after the existing S6C child consumes its one seed",
  "I0-B: make physical_header.rs a sparse projection over the retained cohort; preserve current header consumers through a composite borrowed view",
  "I0-C: carry the retained cohort through VerifiedNormalCallableSemanticPackageV1 -> InstalledNormalCallableSemanticPackageV1 -> SelectedCallableLoweringInputRefV1",
  "I0-D: extend the existing physical_header_tests.rs with focused Unannotated/Void/AnnotatedI64/S6C exclusivity and mismatch coverage, then run exact changed filters and package guards",
  "C0: record residual non-i64/TopLevel ABI as separate NoSafeSlice and return to MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0 design_stop",
]
allowed_files = [
  "src/mir/normal_callable_semantic_package/completion_seed.rs",
  "src/mir/normal_callable_semantic_package/result_contract.rs",
  "src/mir/normal_callable_semantic_package/physical_header_tests.rs",
  "tools/checks/loop_physical_transfer_authority_guard.sh",
  "src/mir/normal_callable_semantic_package/physical_header.rs",
  "src/mir/normal_callable_semantic_package/model.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/normal_callable_semantic_package/selected_input.rs",
  "src/mir/normal_callable_semantic_package/mod.rs",
  "src/mir/normal_callable_semantic_package/README.md",
  "docs/reference/language/function-exit-and-entry-result.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/resolved_control_flow/function_control.rs",
  "src/mir/resolved_semantics/body_shape.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "Call schema",
  "VM/backend/JSON/runtime",
  "panic/throw/Result/? syntax or parser",
]
line_budget = "result_contract.rs <= 240 lines; result_contract_tests.rs <= 320; issuer.rs <= 680; install.rs <= 630; all touched Rust sources <760 and 800 hard stop; do not add semantic responsibility to tests.rs (743), body_shape.rs (733), normal_callable_semantic_loan_port.rs (748), or builder.rs (747)."
acceptance = "Every selected Cataloged row is either RetainedGeneric or MovedToS6CChild; Unannotated and Void remain present in package storage; AnnotatedI64 retains the existing sparse physical projection; unsupported annotations reject; no Completion is cloned, silently dropped, or verified twice; existing header consumers still receive the same observable view; exact focused tests and quick checks are green. This row does not claim DeclaredInstance target issuance or a production call switch."
no_safe_slice = "Stop in design_stop if retention requires a new language result state, TopLevel completion issuance, non-i64 physical ABI, Completion cloning, body/MIR/backend inference, a second verifier, or changes outside the allowed boundary."
non_claims = "No Method(Some) cutover, resolver source relation, effect issuer, receiver ValueId, direct-call loan, Call schema, VM/backend, panic/throw/Result/? activation, fallback, retry, or legacy deletion."
focused_test_filters = [
  "explicit_result_annotation_lends_one_complete_header_view",
  "absent_result_annotation_keeps_ordinary_package_without_physical_header",
  "valid_void_siblings_do_not_poison_sparse_physical_header_cohort",
  "non_i64_result_annotation_is_not_reclassified_as_a_physical_header",
]
guard = "Use current_state_pointer_guard, exact changed-test --list coverage, package completion/physical-header guards, line-budget and git diff checks. A filtered run matching zero tests is a filter failure, not green evidence."
reopen_trigger = "Reopen the parent DeclaredInstance row only after this cohort is landed and a resolver-owned source-site relation, ordinary effect issuer, selected full-lane capability, and common affine consumer are all named; never widen this row to fill those gaps."
implementation_commit = "e1976b8ba1"
focused_test_results = [
  "cargo test --profile quick --lib mir::normal_callable_semantic_package -- --test-threads=1: 62 passed, 0 failed",
  "cargo check --profile quick --lib -j1: passed; existing warnings only",
  "rustfmt --edition 2021 --check on all touched Rust sources: passed",
]
guard_result = "current_state_pointer_guard.sh: pass; git diff --check: pass; line-budget census: pass; loop_physical_transfer_authority_guard.sh: known baseline red at the pre-existing Main static-child token; normal_callable_complete_batch_guard.sh: known baseline red because it still requires the retired raw_invocation_source_transport.rs path."
evidence = "At implementation commit e1976b8ba1, the existing non-Clone VerifiedFunctionCompletionV1 is moved once from the completion seed into a package-private retained result-contract cohort after exclusive S6C consumption. Unannotated, Void, and Annotated(i64) rows remain observable through install; physical headers borrow the cohort and stay sparse. S6C selected rows intentionally use the child-owned Completion instead of the generic cohort. The package suite ran 62/62 green after this S6C gate correction."
red_classification = "No current-change red in the focused package suite or quick library check. Two broader guards remain known baseline debt: loop_physical_transfer_authority_guard.sh expects an older Main static-child install token, and normal_callable_complete_batch_guard.sh requires the pre-split raw_invocation_source_transport.rs path. They were not repaired in this bounded result-carriage row."
closeout = "Return work_mode=design_stop and current_execution_row/current_design_stop to MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0. Keep target/effect/receiver/full-lane ABI issuance closed; this row only preserves already-issued result/Completion products."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0__design_stop"

[mir_builder_boxshape_generic_lease_tail_test_s0_2026_08_31]
status = "landed_with_baseline_guard_red"
task_id = "MIR-BUILDER-BOXSHAPE-GENERIC-LEASE-TAIL-TEST-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the final two existing tests from generic_resolved_carrier_source_lease_tests.rs into a private include! sibling. Preserve test bodies, private access, historical module/test paths, parent imports, guard anchors, production code, and behavior; this row only removes the test-only 760/800-line breach and does not open the DeclaredInstance issuer stop."
source_authority = "The existing generic_resolved_carrier_source_lease test module, its private helpers/imports, and the two existing test bodies are the only authority. Test placement introduces no semantic issuer, target, receiver, ABI, effect, runtime, or production meaning."
canonical_issuer = "None. This is a test-only BoxShape row; the parent DeclaredInstance row remains NoSafeSlice and no semantic product is issued."
non_authority = [
  "test names or fixtures as production authority",
  "module names as semantic ownership",
  "Call/Method/DeclaredInstance facts, source/name/arity lookup, resolver, target, loan, fallback, retry, VM/backend, JSON, or Call schema",
]
fail_fast_boundary = "Stop before closeout if the historical module path, private access, parent imports, guard-observed anchor, production source, behavior, or 760/800 line budget changes."
census_boundary = "src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tests.rs:663-795 final two cfg(test) functions -> private include! sibling; includes source_lease_rejects_duplicate_unknown_and_mismatched_forest_roles and source_lease_rejects_identity_and_role_input_mismatches, excludes all production semantics, the first test functions, existing child modules, Call/Method work, and runtime/backend routes."
finite_inventory = [
  "parent test owner: src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tests.rs",
  "tail tests: source_lease_rejects_duplicate_unknown_and_mismatched_forest_roles; source_lease_rejects_identity_and_role_input_mismatches",
  "new private sibling: src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tail_tests.rs",
  "path-preserving parent mount: include!(\"generic_resolved_carrier_source_lease_tail_tests.rs\")",
  "historical module path: mir::resolved_semantics::generic_resolved_carrier_source_lease::tests",
]
finite_states = [
  "SplitStable: parent and sibling compile, private access/imports and historical test paths remain unchanged",
  "GuardAnchorRetain: loop_family_observation_contract.sh continues to observe the parent test owner and existing child modules",
  "BaselineRed: any pre-existing focused failure must be reproduced on the parent binary and recorded, never hidden",
  "DriftDetected: semantic, module-path, visibility, fixture, guard, or line-budget drift returns to design_stop",
]
ordered_tasks = [
  "S0-A: record the exact parent/tail ranges, two test names, existing imports, module path, and guard anchors",
  "S0-B: move only the two test bodies verbatim into generic_resolved_carrier_source_lease_tail_tests.rs and add one include! at the parent end; add no imports or helper copies",
  "S0-C: run cargo --list and both exact focused tests, quick library check, targeted rustfmt, line-budget, pointer, and diff checks; reproduce any baseline red on the pre-split parent binary",
  "S0-D: close out to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design_stop; do not claim DeclaredInstance, Method, Call, panic, throw, or Result progress",
]
acceptance = "The parent test file falls below 760/800 lines and the sibling remains below 760/800; both moved tests stay under the historical module path with unchanged names and private access; parent imports and existing child modules remain valid; production files and guards are unchanged; focused evidence executes the moved tests rather than only naming them; and any baseline red is separately recorded."
no_safe_slice = "Remain in design_stop if include! cannot preserve private access or the historical path, a new public API/import/module registration or guard change is needed, production code/fixtures/semantics change, a focused red is new, or either file reaches the 760/800 boundary."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit/Result change, VM/backend/JSON change, fallback/retry, guard deletion, or semantic receipt."
allowed_files = [
  "src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tests.rs",
  "src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tail_tests.rs",
  "src/mir/resolved_semantics/README.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/resolved_semantics/mod.rs",
  "src/mir/resolved_semantics/generic_resolved_carrier_source_lease/*.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "VM/backend/JSON/Call schema",
  "guard registry or guard deletion",
]
focused_test_filters = [
  "mir::resolved_semantics::generic_resolved_carrier_source_lease::tests",
]
changed_test_names = [
  "source_lease_rejects_duplicate_unknown_and_mismatched_forest_roles",
  "source_lease_rejects_identity_and_role_input_mismatches",
]
worker_audit_result = "Read-only audit at HEAD 4f9ed71734 found one unlanded test-only BoxShape candidate: the final two tests occupy lines 663-795 of a 796-line parent. A path-preserving include! sibling keeps private helpers, module paths, and existing loop-family guard anchors unchanged; no semantic or production owner is introduced. Existing candidate_envelope_witness remains a separate child and is not moved."
implementation_commit = "892526b4d6"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::resolved_semantics::generic_resolved_carrier_source_lease::tests -- --test-threads=1: 5 passed; 0 failed; 7532 filtered out",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::resolved_semantics::generic_resolved_carrier_source_lease::tests -- --list: 5 tests listed under the historical module path",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass; existing warnings only",
  "rustfmt --edition 2021 --check src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tests.rs src/mir/resolved_semantics/generic_resolved_carrier_source_lease_tail_tests.rs: pass",
  "line-budget census: parent=664 lines, sibling=132 lines, both below 760/800: pass",
]
guard_runs = [
  "guard_loop_family_observation_contract isolated invocation: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass while S0 selected",
  "git diff --check: pass",
  "bash tools/checks/mirbuilder_inplace_replacement_guard.sh: exit 1 at pre-existing raw Loop callers must share one JoinIR route/freeze owner baseline; no current-change source/guard drift observed",
]
evidence = "Implementation is complete in 892526b4d6: the two tail test bodies moved verbatim to the private include! sibling, parent=664 lines and sibling=132 lines, the historical module path lists and executes all five lease tests, the focused suite passed 5/5, quick library check and rustfmt passed, the isolated loop-family guard passed, and pointer/diff checks passed. The aggregate mirbuilder_inplace_replacement_guard remains a known baseline red at its unrelated raw Loop route/freeze-owner check; the split did not touch that route. No production function, fixture, registry, semantic route, or meaning changed."
red_classification = "Known baseline red only: mirbuilder_inplace_replacement_guard.sh exits at the pre-existing raw Loop callers/JoinIR route-freeze-owner check. The row-specific isolated loop_family_observation_contract, focused five-test suite, cargo check, rustfmt, line budget, pointer guard, and diff check are green. No current-change red was observed."
closeout = "Restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and set next_execution_card=none. Keep true DeclaredInstance source/catalog/receiver/ABI co-seal as the only semantic blocker."
guard = "Use loop_family_observation_contract.sh without changing its parent path, current_state_pointer_guard.sh, cargo --list, exact focused cargo test, targeted rustfmt, line-budget census, and git diff --check. The aggregate mirbuilder_inplace_replacement_guard is recorded as a reproduced baseline red and is not hidden or repaired by this BoxShape row. No new registry row or semantic guard is added."
reopen_trigger = "Reopen only if the historical module path/private access cannot be preserved, a guard starts requiring the inline tail, a production caller/module surface changes, either file reaches 760/800 lines, or the parent DeclaredInstance semantic boundary changes."
next_execution = "MIR-BUILDER-BOXSHAPE-GENERIC-LEASE-TAIL-TEST-S0__implementation_pending_pointer_switch"

[mir_builder_boxshape_function_session_tail_test_s0_2026_08_31]
status = "landed_with_baseline_guard_red"
task_id = "MIR-BUILDER-BOXSHAPE-FUNCTION-SESSION-TAIL-TEST-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the existing two tail tests from function_session_tests.rs into a private sibling through include!. Preserve the test bodies, fixtures, private parent access, historical module/test path, guard anchors, production code, visibility, and behavior exactly; this row only removes the parent source-size breach and does not open the DeclaredInstance issuer stop."
source_authority = "The existing function_session_tests module, its private helpers, and the two current test bodies are the only authority. Test placement introduces no semantic issuer, target, receiver, ABI, effect, runtime, or production meaning."
canonical_issuer = "None. This is a physical test-shape extraction only; the existing function-session production owner and all semantic products remain unchanged."
non_authority = [
  "test filename or include spelling as semantic ownership",
  "fixtures, test output, name/arity/header/symbol/registry lookup",
  "new fields, enums, receipts, resolver, target, loan, fallback, retry, VM/backend, panic, throw, or Call schema",
]
fail_fast_boundary = "Stop before closeout if either test body, private access, historical module/test path, guard anchor, production source, visibility, behavior, or line budget changes. The parent and sibling must remain below 760 lines, with 800 as a hard stop."
census_boundary = "src/mir/builder/calls/function_session_tests.rs:703-796 two existing cfg(test) functions -> src/mir/builder/calls/function_session_tail_tests.rs via include!; includes only box_compilation_session_preserves_the_existing_partial_type_context_action and panic_backstop_restores_without_publishing, excludes production function_session.rs, the first four focused transaction tests, semantic Method/Call work, and all runtime/backend routes."
finite_inventory = [
  "parent production/test owner: src/mir/builder/calls/function_session_tests.rs",
  "retained parent helpers/checkpoints: seeded_builder, assert_outer_state, run_injected_checkpoint, and four existing focused tests",
  "moved test bodies: box_compilation_session_preserves_the_existing_partial_type_context_action; panic_backstop_restores_without_publishing",
  "private sibling: src/mir/builder/calls/function_session_tail_tests.rs",
  "path-preserving parent include!: include!(\"function_session_tail_tests.rs\")",
  "existing guard anchors: resolved_lowering_ingress_inventory.py four-test/checkpoint contract and resolved_region_flow_authority_guard.sh function_session_tests filter",
]
finite_states = [
  "SplitStable: parent is about 703 lines, sibling is below 760, and both tests remain under mir::builder::calls::function_session_tests",
  "PathDrift: module path, test name, private access, or guard anchor changes; stop",
  "BehaviorDrift: any test result, fixture, diagnostic, or production source changes; stop",
  "BudgetExceeded: any touched Rust source reaches 760 lines or the 800-line hard stop; restore design_stop",
]
ordered_tasks = [
  "S0-A: freeze the 796-line parent census, two moved test names, four retained test contract, and exact historical filter before editing",
  "S0-B: move the two tail test bodies verbatim into function_session_tail_tests.rs and add one include! at the parent end; add no imports or helper copies",
  "S0-C: run the exact historical module/list filter, resolved_lowering_ingress_inventory.py, existing function-session guard, rustfmt, quick library check, pointer, diff, and line-budget checks",
  "S0-D: close out as landed and restore MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 / DeclaredInstance design_stop; make no semantic Call or panic/throw claim",
]
acceptance = "function_session_tests.rs and function_session_tail_tests.rs are both below 760/800, the parent retains exactly four focused transaction tests and all five checkpoints, the two moved tests remain listed under mir::builder::calls::function_session_tests with unchanged names and behavior, private access compiles through include!, production function_session.rs and public module paths are untouched, and the focused six-test suite, quick library check, rustfmt, structural census, diff check, and pointer guard pass. The existing resolved_lowering_ingress_inventory guard is separately classified as a parent-reproduced baseline red and is not claimed green by this row."
no_safe_slice = "Stop and restore design_stop if the sibling needs public APIs, copied helpers/import changes, a second module path, guard/fixture changes, production edits, semantic changes, or the 760/800 budget is crossed."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/builder/calls/function_session_tests.rs",
  "src/mir/builder/calls/function_session_tail_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/function_session.rs",
  "src/mir/builder/calls/mod.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/method_call_handlers.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry",
]
focused_test_filters = [
  "mir::builder::calls::function_session_tests",
]
changed_test_names = [
  "mir::builder::calls::function_session_tests::box_compilation_session_preserves_the_existing_partial_type_context_action",
  "mir::builder::calls::function_session_tests::panic_backstop_restores_without_publishing",
]
implementation_commit = "0eedd9a495"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::builder::calls::function_session_tests -- --test-threads=1: 6 passed; 0 failed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass; existing warnings only",
  "rustfmt --edition 2021 --check src/mir/builder/calls/function_session_tests.rs src/mir/builder/calls/function_session_tail_tests.rs: pass",
  "structural split census: parent=703, sibling=94, parent inline tests=4, moved tests remain under historical module path: pass",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass while S0 selected",
  "python3 tools/checks/lib/resolved_lowering_ingress_inventory.py . tools/checks/fixtures/resolved_lowering_ingress_inventory_v1.json: exit 1 at pre-existing B0-L2a anchor drift",
  "parent reproduction at 796-line pre-row state: the same resolved_lowering_ingress_inventory command exits 1 with the same B0-L2a anchor error",
  "git diff --check: pass",
]
evidence = "Implementation is complete in the worktree: the two tail test bodies moved verbatim to the private include! sibling, parent=703 lines and sibling=94 lines, the focused function-session suite passed 6/6, quick library check and rustfmt passed, and the structural census passed. The resolved_lowering_ingress_inventory guard is a known baseline red: it fails at the missing `pub struct LegacyModuleLoweringInputV1` anchor on the current worktree and reproduced with the same exit/status on the pre-row parent, so it is classified outside this behavior-neutral split rather than hidden. No production function-session code, fixture, registry, or semantic route changed."
red_classification = "Known baseline red only: resolved_lowering_ingress_inventory.py fails before reaching the function-session test-count/checkpoint assertions because the existing B0-L2a anchor expects `pub struct LegacyModuleLoweringInputV1` while the live source uses crate-restricted visibility; the same failure was reproduced on the parent commit. No current-change red was observed in the focused six-test suite, quick check, rustfmt, structural split census, pointer guard, or diff check."
closeout = "After the code commit, restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and set next_execution_card=none. Keep DeclaredInstance source/catalog/receiver/ABI co-seal as the only semantic blocker."
reopen_trigger = "Reopen only if the moved tests lose their historical module path/private access, a focused test regresses, production function-session code changes because of the split, a guard begins requiring the inline bodies, or either source reaches 760/800 lines. The unrelated ingress-inventory anchor red must be repaired in its own guard row, not folded into this BoxShape row."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_builder_boxshape_loop_cond_break_continue_facts_test_split_s0_2026_08_31]
status = "landed"
task_id = "MIR-BUILDER-BOXSHAPE-LOOP-COND-BREAK-CONTINUE-FACTS-TEST-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape test-only owner split"
implementation_permission = false
decision = "Move only the existing cfg(test) module from break_continue_facts.rs into a private sibling. Preserve the production extractor, historical module/test paths, private access, guard anchors, and behavior; add no semantic shape or target authority."
source_authority = "The existing loop_cond break/continue facts extractor and its existing Recipe/Facts issuers remain the sole production owners; this row only changes test file placement."
canonical_issuer = "The existing try_extract_loop_cond_break_continue_facts_inner and the existing loop_cond Recipe/Facts pipeline remain unchanged and authoritative."
non_authority = [
  "test helpers and fixtures",
  "Call/Method/VM/backend behavior",
  "production selector or Recipe meaning",
  "resolver, target, registry, fallback, retry",
  "guard deletion or new registry row",
]
fail_fast_boundary = "Move lines 378-746 verbatim and replace them with a cfg(test) path-preserving sibling declaration. Stop on any changed test result, module path, private access, production caller, guard anchor, or 760/800 line-budget violation."
census_boundary = "src/mir/builder/control_flow/plan/loop_cond/break_continue_facts.rs production extractor and its existing cfg(test) module -> sibling break_continue_facts_tests.rs; includes the six tests and nine private helpers, excludes all production facts/Recipe logic, Call/Method, VM/backend, guards, and semantic receipt changes."
finite_inventory = [
  "production owner: src/mir/builder/control_flow/plan/loop_cond/break_continue_facts.rs:1-377",
  "test module: src/mir/builder/control_flow/plan/loop_cond/break_continue_facts.rs:378-746",
  "private sibling: src/mir/builder/control_flow/plan/loop_cond/break_continue_facts_tests.rs",
  "module declaration: #[cfg(test)] #[path = \"break_continue_facts_tests.rs\"] mod tests;",
  "historical test path: mir::builder::control_flow::plan::loop_cond::break_continue_facts::tests",
]
finite_states = [
  "SplitStable: production owner and sibling are below 760 lines; all six tests compile and pass under the historical module path",
  "PathDrift: module path, private access, or test name changes; stop",
  "BehaviorDrift: any test result or extractor behavior changes; stop",
  "BudgetExceeded: any touched Rust source reaches 760 lines or the 800-line hard stop; return to design_stop",
]
ordered_tasks = [
  "S0-A: freeze the production/test line census and six-test filter before editing",
  "S0-B: add break_continue_facts_tests.rs and move only the cfg(test) module; keep the production parent declaration path-preserving",
  "S0-C: run the exact historical test module, quick library check, rustfmt, line-budget, pointer, and diff checks",
  "S0-D: close out to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design_stop; do not claim semantic Call progress",
]
acceptance = "break_continue_facts.rs is below 760 lines, the private sibling is below 760 lines, all six existing tests remain under the historical module path and compile, production extractor code and callers are unchanged, no guard/registry/public API changes occur, the one pre-existing failing test is reproduced at the parent binary and classified as baseline debt, and the parent design stop is restored after closeout."
no_safe_slice = "Return to design_stop if the tests require a public helper, a second module path, a production edit, a guard change, a fixture/semantic change, a non-finite caller, or the 760/800 line budget is crossed."
non_claims = "No BoxCount, no new Facts/Recipe/issuer, no Method/Call/VM/backend change, no panic/throw/exit change, no fallback/retry change, no guard deletion, and no legacy retirement claim."
allowed_files = [
  "src/mir/builder/control_flow/plan/loop_cond/break_continue_facts.rs",
  "src/mir/builder/control_flow/plan/loop_cond/break_continue_facts_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/control_flow/plan/loop_cond/mod.rs",
  "src/mir/builder/calls/**",
  "src/mir/resolved_semantics/**",
  "VM/backend/JSON",
  "tools/checks/guard_rows.toml",
  "guard deletion or new registry rows",
]
focused_test_filters = ["control_flow::plan::loop_cond::break_continue_facts::tests"]
changed_test_names = [
  "policy_recipe_only_when_not_extended",
  "policy_recipe_only_with_then_only_break",
  "accepts_nested_guard_break_if_as_program_block_recipe_only",
  "accepts_nested_loop_if_as_program_block_recipe_only",
  "accepts_multidelta_break_continue_as_recipe_only",
  "program_block_with_exit_signals_prefers_recipe_only",
]
implementation_commit = "ccced92d64"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib control_flow::plan::loop_cond::break_continue_facts::tests -- --test-threads=1: 5 passed, 1 pre-existing baseline failure",
  "parent binary target/quick/deps/nyash_rust-5ac8cdc0fe377915 control_flow::plan::loop_cond::break_continue_facts::tests --test-threads=1: same 5 passed, 1 baseline failure",
]
guard_runs = [
  "rustfmt --edition 2021 --check src/mir/builder/control_flow/plan/loop_cond/break_continue_facts.rs src/mir/builder/control_flow/plan/loop_cond/break_continue_facts_tests.rs: pass",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib control_flow::plan::loop_cond::break_continue_facts::tests -- --test-threads=1: 5 pass, 1 parent-reproduced baseline failure",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUSTFLAGS='-Awarnings' cargo check --profile quick --lib: pass",
  "line census: break_continue_facts.rs 380, break_continue_facts_tests.rs 366; both <760 and no touched Rust source >=800",
  "bash tools/checks/current_state_pointer_guard.sh: pass after closeout state restore",
  "git diff --check: pass after closeout",
]
evidence = "Implementation landed at ccced92d64. The cfg(test) module moved to break_continue_facts_tests.rs through a path-preserving declaration. The production extractor remains byte-for-byte unchanged; the parent is 380 lines and the sibling is 366 lines. All six historical tests compile under mir::builder::control_flow::plan::loop_cond::break_continue_facts::tests. Five pass on the changed tree. program_block_with_exit_signals_prefers_recipe_only fails identically in the pre-split parent binary (5 passed, 1 failed), so this is known baseline debt rather than current-change red. Quick lib check and targeted rustfmt pass; pointer, TOML, and diff guards pass after closeout. No semantic, public API, guard, Method/Call, panic/throw/exit, backend, fallback, or retry change occurred."
red_classification = "known_baseline_debt: program_block_with_exit_signals_prefers_recipe_only fails before and after the test-only split at the same assertion; no current-change red. The full suite is not claimed."
reopen_trigger = "Reopen only if the historical test path or private access breaks, a guard starts requiring the inline body, a production caller changes, or either source crosses the 760/800 budget."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_builder_boxshape_normal_script_direct_statement_fastmem_failure_test_s0_2026_08_31]
status = "landed"
task_id = "MIR-BUILDER-BOXSHAPE-NORMAL-SCRIPT-DIRECT-STATEMENT-FASTMEM-FAILURE-TEST-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the existing direct_fastmem_body_failure_discards_candidate_and_reuses_compiler test from the inline cfg(test) module into a sibling file via include!. Preserve the historical module path, private scope, production owner, test body, guard anchors, diagnostics, and runtime behavior; this row exists only to keep the parent source below the 760/800 line boundary."
source_authority = "Existing normal_script_direct_statement_owner.rs production module and its inline test module; test placement is not semantic authority."
canonical_issuer = "None. No semantic target, receiver, ABI, effect, loan, receipt, or production switch is created."
non_authority = [
  "test file location, include! spelling, module path, fixtures, test output, resolver, target/receiver lookup, fallback, retry, VM/backend, panic/throw/exit, Method, or Call schema",
]
fail_fast_boundary = "Any production-body change, module-path/visibility/signature drift, private-access failure, test-filter mismatch, behavior change, guard-anchor drift, or touched/new Rust source at 760 lines or more stops the row before closeout."
census_boundary = "The existing inline FastMem failure test -> private sibling include!; includes only test placement and parent line-budget relief, excludes selected direct-statement lowering, FastMem semantics, Method/Call, runtime, backend, and guard-registry changes."
finite_inventory = [
  "production owner: src/mir/builder/normal_script_direct_statement_owner.rs remains unchanged",
  "test owner: direct_fastmem_body_failure_discards_candidate_and_reuses_compiler moves verbatim to normal_script_direct_statement_owner_fastmem_failure_test.rs",
  "parent declaration: include! preserves tests module and historical test path",
]
finite_states = [
  "SplitStable: parent and sibling compile, module path/private access/behavior remain unchanged",
  "ProductionRetained: direct-statement production functions and caller remain in the parent",
  "DriftDetected: semantic, path, visibility, guard, filter, or behavior drift returns to design_stop",
  "BudgetExceeded: parent or sibling reaches 760 lines or more (800 hard stop)",
]
ordered_tasks = [
  "S0-A: record the exact test boundary, historical path, and current 783-line parent census",
  "S0-B: replace only the test body with include!(\"normal_script_direct_statement_owner_fastmem_failure_test.rs\"); and move the body verbatim",
  "S0-C: run the exact historical test, cargo --list path coverage, rustfmt, quick library check, line-budget, pointer, and diff checks",
  "S0-D: close out as a landed BoxShape refactor and restore the parent design_stop; do not claim semantic or Method/Call progress",
]
acceptance = "normal_script_direct_statement_owner.rs retains all production definitions and stays below 760/800; the sibling contains the unchanged FastMem failure test and stays below 760/800; the historical tests::direct_fastmem_body_failure_discards_candidate_and_reuses_compiler path remains valid; private access, diagnostics, guard anchors, behavior, focused evidence, pointer guard, rustfmt, and quick check all pass."
no_safe_slice = "Stop and restore design_stop if the sibling needs public visibility/re-export, include! cannot preserve the historical path/private access, the test body must change, production or guard files must change, a second owner is needed, or either source crosses the 760/800 budget."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/builder/normal_script_direct_statement_owner.rs",
  "src/mir/builder/normal_script_direct_statement_owner_fastmem_failure_test.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder.rs",
  "src/mir/builder/normal_script_runtime_block_port.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry",
]
focused_test_filters = [
  "normal_script_direct_statement_owner::tests::direct_fastmem_body_failure_discards_candidate_and_reuses_compiler",
]
changed_test_names = [
  "mir::builder::normal_script_direct_statement_owner::tests::direct_fastmem_body_failure_discards_candidate_and_reuses_compiler",
]
implementation_commit = "7e50c819f3"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib 'mir::builder::normal_script_direct_statement_owner::tests::direct_fastmem_body_failure_discards_candidate_and_reuses_compiler' -- --exact --test-threads=1: 1 passed, 0 failed",
  "target/quick/deps/nyash_rust-bef87a5262fef8ae 'mir::builder::normal_script_direct_statement_owner::tests' --list: exact test listed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass (existing warnings only)",
  "rustfmt --edition 2021 --check src/mir/builder/normal_script_direct_statement_owner.rs src/mir/builder/normal_script_direct_statement_owner_fastmem_failure_test.rs: pass",
  "line budget: normal_script_direct_statement_owner.rs 748 lines; sibling 36 lines; both below 760 and 800 hard stop",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass before closeout",
  "git diff --check: pass before closeout",
]
evidence = "Implementation landed at 7e50c819f3. The existing direct_fastmem_body_failure_discards_candidate_and_reuses_compiler test moved verbatim into the private sibling via include!, preserving the historical tests path and private access. The exact test passed 1/1; cargo --list, quick library check, rustfmt, line budget, pointer guard, and diff check passed. Production direct-statement lowering and its caller were untouched; no semantic target, Method/Call, panic/throw/exit, backend, fallback, retry, or guard registry changed."
red_classification = "No current-change red. Existing workspace warnings and unrelated baseline debt remain outside this BoxShape extraction; the exact changed test was green."
closeout = "Implementation and evidence are recorded; status=landed and implementation_permission=false. Restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and set next_execution_card=none."
reopen_trigger = "Reopen only if the historical test path/private access regresses, a guard requires the inline body, production direct-statement code changes because of the split, the sibling or parent reaches 760/800, or behavior parity changes."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_builder_boxshape_module_lifecycle_failure_test_s0_2026_08_31]
status = "landed"
task_id = "MIR-BUILDER-BOXSHAPE-MODULE-LIFECYCLE-FAILURE-TEST-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the existing deferred static lifecycle failure test into a private sibling so module_lifecycle_capture_tests.rs falls below the 760-line split boundary. Preserve the test body, historical module path, private parent access, production lifecycle, visibility, re-exports, and behavior exactly. This independent BoxShape row does not open the DeclaredInstance issuer stop."
source_authority = "Existing test-module ownership, private parent names, and the current test body are the only authority. No semantic issuer, target, receiver, ABI, effect, backend, or runtime meaning is introduced."
canonical_issuer = "None; this is a physical test-shape extraction only."
non_authority = [
  "test fixtures as production authority",
  "module filename or test name as semantic ownership",
  "name/arity/header/symbol/registry lookup",
  "new fields, enums, receipts, resolver, target, loan, fallback, retry, VM/backend, panic, throw, or Call schema",
]
fail_fast_boundary = "Stop before closeout if the test body, module path, private access, production source, visibility, re-export, behavior, or line budget changes. Parent and sibling must remain below 760 lines, with 800 as a hard stop."
census_boundary = "The final deferred_static_box_lifecycle_restores_context_and_stops_after_failure test in src/mir/builder/module_lifecycle_capture_tests.rs -> private lifecycle_failure_tests sibling; includes only that test and its existing parent-private dependencies, excludes module_lifecycle.rs production code, ingress tests, semantic Method/Call work, and all runtime/backend routes."
finite_inventory = [
  "parent: src/mir/builder/module_lifecycle_capture_tests.rs (772 lines before split)",
  "single moved test: deferred_static_box_lifecycle_restores_context_and_stops_after_failure",
  "new private sibling: src/mir/builder/module_lifecycle_failure_tests.rs (about 25 lines)",
  "existing parent-private dependencies: MirBuilder, RecordingOrdinaryPortV1, parsed_static_box, ProgramDeferredStaticBoxLifecycleV1",
]
finite_states = [
  "SplitStable: parent is about 753 lines, sibling is below 760, private access and historical test path compile",
  "BehaviorDrift: test body/output/module path/production code changes; stop",
  "BudgetExceeded: any touched Rust source reaches 760 or more (800 hard stop); stop",
]
ordered_tasks = [
  "S0-A: add the private lifecycle_failure_tests path module beside the existing ingress test module",
  "S0-B: move only deferred_static_box_lifecycle_restores_context_and_stops_after_failure using super-private imports; do not rewrite its assertions or fixture",
  "S0-C: run the exact historical test path, focused module listing, rustfmt, quick library check, pointer guard, diff check, and line census",
  "S0-D: close out as landed and restore MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design_stop; make no DeclaredInstance or panic/throw claim",
]
acceptance = "The parent and new sibling remain below 760/800, the exact test name remains under mir::builder::module_lifecycle::capture_tests::lifecycle_failure_tests, private parent access compiles, the test output and fixture are unchanged, production module_lifecycle.rs is untouched, and focused evidence plus pointer/diff checks are recorded."
no_safe_slice = "Stop and restore design_stop if the test requires a public API, helper duplication, parent production edit, module-path change, guard change, semantic code change, or the line budget is crossed."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/builder/module_lifecycle_capture_tests.rs",
  "src/mir/builder/module_lifecycle_failure_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/module_lifecycle.rs",
  "src/mir/builder/module_lifecycle_ingress_tests.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/method_call_handlers.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry",
]
focused_test_filters = [
  "mir::builder::module_lifecycle::capture_tests::lifecycle_failure_tests::deferred_static_box_lifecycle_restores_context_and_stops_after_failure",
]
changed_test_names = [
  "mir::builder::module_lifecycle::capture_tests::lifecycle_failure_tests::deferred_static_box_lifecycle_restores_context_and_stops_after_failure",
]
implementation_commit = "389ae344c1"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::builder::module_lifecycle::capture_tests::lifecycle_failure_tests::deferred_static_box_lifecycle_restores_context_and_stops_after_failure -- --exact --test-threads=1: 1 passed; 0 failed",
  "target/quick/deps/nyash_rust-bef87a5262fef8ae mir::builder::module_lifecycle::capture_tests::lifecycle_failure_tests::deferred_static_box_lifecycle_restores_context_and_stops_after_failure --list: exact test listed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass; existing warnings only",
  "rustfmt --edition 2021 --check src/mir/builder/module_lifecycle_capture_tests.rs src/mir/builder/module_lifecycle_failure_tests.rs: pass",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
  "line census: module_lifecycle_capture_tests.rs=753, module_lifecycle_failure_tests.rs=25: pass",
]
evidence = "Implementation landed at 389ae344c1. Only deferred_static_box_lifecycle_restores_context_and_stops_after_failure moved to the private lifecycle_failure_tests sibling; the parent is 753 lines and the sibling is 25 lines. The historical test path now includes capture_tests::lifecycle_failure_tests, private parent access compiles, the exact test passed, the direct test binary listed the exact test, quick library check and rustfmt passed, and production module_lifecycle.rs was untouched."
red_classification = "No current-change red. This T0 row did not claim the full cargo test/link suite; existing workspace warnings and unrelated baseline debt remain outside the test-only extraction."
closeout = "Return work_mode to design_stop with current_execution_row/current_design_stop/next_design_card set to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and next_execution_card=none."
reopen_trigger = "Reopen only if the historical test path cannot compile, private access requires a public API, a guard requires the old inline body, production module_lifecycle.rs changes, the parent/sibling reaches 760/800 lines, or the DeclaredInstance semantic boundary changes."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_call_me_declared_instance_receiver_source_facts_i0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-RECEIVER-SOURCE-FACTS-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
design_parent = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T1"
box_kind = "BoxCount source-facts fidelity correction"
implementation_permission = false
decision = "Preserve the receiver policy already sealed by BodyMeReceiverV1 when issuing VerifiedResolvedMethodCallSourceV1: Lexical(binding) remains Lexical(binding), while StaticCurrentOwner remains CurrentOwner. This removes a source-facts collapse only; it does not issue a target, receiver ValueId, ABI, loan, or MIR Call."
source_authority = "The existing resolver-owned BodyExpressionShapeV1::Me receiver field is the sole authority. The existing issue_resolved_method_call_sources_v1 function is the sole source-product issuer and must forward its finite receiver disposition without re-scanning AST or Builder state."
canonical_issuer = "Existing issue_resolved_method_call_sources_v1 in src/mir/resolved_semantics/body_shape.rs; no new receipt or parallel source pipeline."
non_authority = [
  "AST nodes, variable_map, current_static_box, header/name/arity lookup, formatted symbols, registry, physical ABI, backend/VM, tests, fixtures, fallback, retry, and any target/ValueId/MIR publication",
]
fail_fast_boundary = "A receiver disposition other than BodyMeReceiverV1::Lexical or BodyMeReceiverV1::StaticCurrentOwner, a foreign binding, or a changed source-site relation remains a typed source-product error before any target or argument effect."
census_boundary = "BodyExpressionShapeV1::Me receiver disposition -> VerifiedResolvedMethodCallSourceV1::receiver; includes declared-instance lexical receiver and static current-owner source tests, excludes target issuer, Method(Some), package/loan, ABI/effect/result co-seal, Call schema, and backend/runtime routes."
finite_inventory = [
  "existing source shape: BodyExpressionShapeV1::Me { receiver: BodyMeReceiverV1 }",
  "existing method source product: VerifiedResolvedMethodCallSourceV1::receiver",
  "existing issuer: issue_resolved_method_call_sources_from_rows_v1",
  "existing tests: resolved_shape_keeps_static_current_owner_me_without_forging_a_receiver_binding and declared-instance Me source fixtures",
]
finite_states = [
  "LexicalPreserved: BodyMeReceiverV1::Lexical(binding) becomes ResolvedMethodCallReceiverSourceV1::Lexical(binding)",
  "StaticOwnerPreserved: BodyMeReceiverV1::StaticCurrentOwner becomes ResolvedMethodCallReceiverSourceV1::CurrentOwner",
  "UnsupportedReceiver: any other source shape remains Other or rejects under the existing source contract; no target inference",
  "SourceDrift: site/relation/owner or binding changes; stop",
]
ordered_tasks = [
  "I0-A: replace only the existing Me receiver match arm in the source-product issuer with the two explicit BodyMeReceiverV1 cases",
  "I0-B: add focused positive coverage for both lexical and static-current-owner method source products, preserving existing source-site and owner assertions",
  "I0-C: run exact changed tests, test-name listing, rustfmt, quick library check, pointer guard, diff check, and body_shape.rs line census",
  "I0-D: close out to MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0; do not claim an instance target or production Call switch",
]
acceptance = "Declared-instance Me method source rows retain the exact resolver-owned lexical BindingRefV1, static current-owner rows retain CurrentOwner, all existing source-site/argument relations remain unchanged, no target/ValueId/ABI/MIR/loan/backend code is added, focused tests and quick check pass, and body_shape.rs remains below 760/800 lines."
no_safe_slice = "Stop and return to design_stop if preserving the binding requires AST or Builder state, changes a public/source product shape beyond the existing receiver enum, needs a new receipt/issuer, alters target or argument lowering, or crosses the 760/800 source budget."
non_claims = "No DeclaredInstance declaration relation, Method(Some(receiver)) cutover, direct-call issuer/loan, Call schema change, panic/throw/exit change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/resolved_semantics/body_shape.rs",
  "src/mir/resolved_semantics/body_shape_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/normal_callable_semantic_package/declared_instance_call.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/method_call_handlers.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry",
]
focused_test_filters = [
  "resolved_shape_keeps_static_current_owner_me_without_forging_a_receiver_binding",
  "resolved_shape_issues_declared_instance_method_receiver_source",
]
changed_test_names = [
  "mir::resolved_semantics::body_shape_tests::resolved_shape_keeps_static_current_owner_me_without_forging_a_receiver_binding",
  "mir::resolved_semantics::body_shape_tests::resolved_shape_issues_declared_instance_method_receiver_source",
]
implementation_commit = "01332528ed"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::resolved_semantics::body_shape_tests -- --test-threads=1: 10 passed, 0 failed",
  "target/quick/deps/nyash_rust-bef87a5262fef8ae 'mir::resolved_semantics::body_shape_tests' --list: both source-facts tests listed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass (existing warnings only)",
  "rustfmt --edition 2021 --check src/mir/resolved_semantics/body_shape.rs src/mir/resolved_semantics/body_shape_tests.rs: pass",
  "line budget: body_shape.rs 733 lines; body_shape_tests.rs 390 lines; both below 760 and 800 hard stop",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass before closeout",
  "git diff --check: pass before closeout",
]
evidence = "Implementation landed at 01332528ed. issue_resolved_method_call_sources_v1 now forwards BodyMeReceiverV1::Lexical as ResolvedLexicalRefV1::Local and preserves StaticCurrentOwner as CurrentOwner. The focused body_shape_tests suite passed 10/10, including the new declared-instance receiver-source assertion; quick library check, test-name listing, rustfmt, line budget, pointer guard, and diff check passed. No target, receiver ValueId, ABI, loan, MIR Call, backend, fallback, retry, or new receipt was added."
red_classification = "No current-change red. Existing workspace warnings and unrelated baseline debt remain outside this source-facts row; the focused suite was fully green."
closeout = "Implementation and evidence are recorded; status=landed and implementation_permission=false. Restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and set next_execution_card=none."
reopen_trigger = "Reopen only if a later source/catalog/receiver co-seal cannot use the preserved BodyMeReceiverV1 disposition, the focused source-product tests regress, a new receipt/target/ValueId is proposed, or body_shape.rs reaches the 760/800 budget."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0__design_stop"

[mir_builder_boxshape_normal_callable_source_test_extraction_s0_2026_08_31]
status = "landed"
task_id = "MIR-BUILDER-BOXSHAPE-NORMAL-CALLABLE-SOURCE-TEST-EXTRACTION-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the existing cfg(test) tests from normal_callable_semantic_source.rs into a private sibling. Preserve the normal_callable_semantic_source::tests module path, private access, production semantic owner, signatures, visibility, re-exports, guard anchors, runtime behavior, and all test bodies. This row exists only to keep the parent source below the 760/800 line budget before any future semantic work."
source_authority = "Existing normal_callable_semantic_source.rs module ownership, its production definitions, and the current inline tests. Test placement is not semantic authority."
canonical_issuer = "None. No semantic target, receiver, ABI, effect, loan, receipt, or production switch is created."
non_authority = [
  "test file location, #[path] placement, module-path spelling, fixtures, test output, name/arity/header/symbol/registry lookup, resolver, fallback, retry, VM/backend, panic, or Call schema",
]
fail_fast_boundary = "Any production-body change, module-path/visibility/signature drift, private-access failure, guard-anchor drift, test-filter mismatch, behavior change, or touched/new Rust source at 760 lines or more stops the row before closeout."
census_boundary = "The existing normal_callable_semantic_source.rs production block and inline cfg(test) module -> one private sibling test file; includes only physical test placement and parent declaration, excludes semantic source products, package/loan, Method/Call, runtime, backend, and guard-registry changes."
finite_inventory = [
  "production owner: src/mir/builder/normal_callable_semantic_source.rs lines 1-363 remains in place",
  "test owner: existing inline normal_callable_semantic_source::tests lines 365-730 moves verbatim to src/mir/builder/normal_callable_semantic_source_tests.rs",
  "parent declaration: #[cfg(test)] #[path = \"normal_callable_semantic_source_tests.rs\"] mod tests; preserves the historical module path",
  "builder.rs module declaration and all public/private re-exports remain unchanged",
]
finite_states = [
  "SplitStable: parent and sibling compile, module path and private access are unchanged",
  "ProductionRetained: semantic source definitions and all non-test modules remain in the parent",
  "DriftDetected: any semantic, path, visibility, guard, filter, or behavior drift returns to design_stop",
  "BudgetExceeded: parent or sibling reaches 760 lines or more (800 hard stop)",
]
ordered_tasks = [
  "S0-A: record the exact production/test boundary, historical test path, and current line counts",
  "S0-B: replace only the inline test module with a private path declaration and move its body verbatim to the sibling",
  "S0-C: run the historical exact test path, cargo --list coverage, rustfmt, quick library check, line-budget, pointer, and diff checks",
  "S0-D: close out as a landed BoxShape refactor and restore the parent DeclaredInstance design_stop; do not claim semantic progress",
]
acceptance = "normal_callable_semantic_source.rs retains the complete production owner and stays below 760/800 lines; normal_callable_semantic_source_tests.rs contains the unchanged tests and stays below 760/800 lines; normal_callable_semantic_source::tests::<name> paths remain valid; private access, signatures, re-exports, guard anchors, call order, and runtime behavior are unchanged; every changed test is listed and filtered; quick check, focused tests, pointer guard, rustfmt, and diff check pass."
no_safe_slice = "Stop and restore design_stop if a sibling needs public visibility/re-export, the historical module path cannot be preserved, a guard or production consumer must change, a test body must be edited for compilation, a second test owner is needed, or either source crosses the 760/800 budget."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/builder/normal_callable_semantic_source.rs",
  "src/mir/builder/normal_callable_semantic_source_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/method_call_handlers.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry or guard deletion",
]
focused_test_filters = ["normal_callable_semantic_source::tests"]
changed_test_names = [
  "mir::builder::normal_callable_semantic_source::tests::callable_entry_local_variable_and_rebind_materialization_keeps_parity",
  "mir::builder::normal_callable_semantic_source::tests::mixed_callable_batch_seals_and_reacquires_exact_program_sites",
  "mir::builder::normal_callable_semantic_source::tests::callable_loop_handoff_issues_exact_resolver_sites_before_lowering",
  "mir::builder::normal_callable_semantic_source::tests::function_call_defers_before_unresolved_argument_child",
  "mir::builder::normal_callable_semantic_source::tests::selected_direct_call_observation_is_a_typed_package_terminal",
  "mir::builder::normal_callable_semantic_source::tests::main_methods_are_absent_from_callable_semantic_batch",
  "mir::builder::normal_callable_semantic_source::tests::nonplain_instance_blocker_defers_the_whole_mixed_batch_before_resolution",
  "mir::builder::normal_callable_semantic_source::tests::nonplain_blocker_is_script_only_and_app_remains_complete_eligible",
  "mir::builder::normal_callable_semantic_source::tests::mixed_nonplain_batch_keeps_selected_and_legacy_lowering_in_parity",
  "mir::builder::normal_callable_semantic_source::tests::callable_parameter_and_local_bodies_do_not_borrow_the_script_ledger",
]
implementation_commit = "c1a715eb97"
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib normal_callable_semantic_source::tests -- --test-threads=1: 7 passed, 3 known baseline failures; parent 97e62bd7a3 reproduces the same 3 failures",
  "target/quick/deps/nyash_rust-bef87a5262fef8ae normal_callable_semantic_source::tests --list: 10 matching tests, 0 benchmarks",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass (existing warnings only)",
  "rustfmt --edition 2021 src/mir/builder/normal_callable_semantic_source.rs src/mir/builder/normal_callable_semantic_source_tests.rs: pass",
  "line budget: parent 367 lines, sibling 351 lines; both below 760 and 800 hard stop",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass before closeout",
  "git diff --check: pass before closeout",
]
evidence = "Implementation landed at c1a715eb97. The existing production owner remains in src/mir/builder/normal_callable_semantic_source.rs (367 lines); its cfg(test) body moved to the private sibling src/mir/builder/normal_callable_semantic_source_tests.rs (351 lines), with #[cfg(test)] #[path = ...] mod tests preserving the historical normal_callable_semantic_source::tests path and private access. The exact focused filter executed all 10 tests: 7 passed, while 3 existing runtime-box-fate-retired failures reproduced identically on parent 97e62bd7a3, so they are baseline debt, not a split regression. Quick library check, rustfmt, pointer guard, and diff check passed. No semantic, production, guard, panic/throw, VM/backend, fallback, or retry change was made."
red_classification = "Known baseline red only: callable_entry_local_variable_and_rebind_materialization_keeps_parity, callable_parameter_and_local_bodies_do_not_borrow_the_script_ledger, and mixed_nonplain_batch_keeps_selected_and_legacy_lowering_in_parity all fail with the pre-existing raw-compat/runtime-box-fate-retired terminal on both c1a715eb97 and parent 97e62bd7a3. No current-change red is attributed to this BoxShape extraction."
guard = "Use current_state_pointer_guard, cargo --list exact-path coverage, line-budget and parent-token checks, rustfmt, quick library check, and git diff --check. Do not add a row-specific shell guard or alter existing semantic guards."
closeout = "Implementation and evidence are recorded; status=landed and implementation_permission=false. Restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and set next_execution_card=none."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[mir_call_boxshape_preflight_tail_test_extraction_s0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-BOXSHAPE-PREFLIGHT-TAIL-TEST-EXTRACTION-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving test-only extraction"
implementation_permission = false
decision = "Move only the test-only explicit_source_identity_tests module from function_call_preflight_route.rs into a private sibling test file declared by the same parent. Preserve production routing, module path, visibility, re-exports, guard anchors, test behavior, and diagnostics. The existing function_call_preflight_route_tests.rs is already at the 760-line boundary, so it remains unchanged; the sibling keeps both touched owners below the 760/800 budget."
source_authority = "Existing Rust module/test ownership and the current PreparedRawExplicitExternCallV1 behavior; test placement is not semantic authority."
canonical_issuer = "None. No target, receiver, ABI, effect, loan, or semantic receipt is added."
non_authority = [
  "test location, line count, test output, names, fixtures, header/symbol/registry lookup, resolver, fallback, retry, panic, Method, Call schema, VM/backend",
]
fail_fast_boundary = "Before closeout, the production function_call_preflight_route.rs body and all public/module surfaces must be unchanged; the moved test must compile from a private sibling declared by the same parent and keep its historical module path; either touched Rust source at 760 lines or more, any guard-anchor drift, or any behavior change is a stop."
census_boundary = "The inline #[cfg(test)] explicit_source_identity_tests block at the tail of function_call_preflight_route.rs -> a private sibling file declared by the same parent; excludes replay_function_call_trace, the existing function_call_preflight_route_tests.rs body, every production route/helper, semantic issuer work, and all backend/runtime lanes."
finite_inventory = [
  "inline test module: src/mir/builder/calls/function_call_preflight_route.rs::explicit_source_identity_tests",
  "destination test module: src/mir/builder/calls/function_call_preflight_source_identity_tests.rs",
  "parent declaration: src/mir/builder/calls/function_call_preflight_route.rs::explicit_source_identity_tests",
  "unchanged sibling suite: src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "existing module/re-export surface: src/mir/builder/calls/mod.rs",
]
finite_states = [
  "SplitStable: test compiles and runs from the child module; production parent behavior and surface are unchanged",
  "AnchorDrift: a production guard or module path observes a changed parent token; return to design_stop",
  "BehaviorDrift: test or production behavior changes; no closeout",
  "BudgetExceeded: either touched Rust source reaches 760 lines or 800 hard stop; no compression workaround",
]
ordered_tasks = [
  "S0-A: record the one inline test module and existing child test owner; keep replay_function_call_trace in the production parent",
  "S0-B: remove only the inline test module, add the same three-line private sibling declaration, and move the test body to function_call_preflight_source_identity_tests.rs using ancestor-private access",
  "S0-C: run rustfmt check, the moved exact test by its historical module path, quick library check, line-budget and diff checks; do not change production route code",
  "S0-D: close out to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design_stop with no semantic claim",
]
acceptance = "function_call_preflight_route.rs, function_call_preflight_route_tests.rs, and the new sibling test file are each below 760 lines, explicit_source_identity_tests keeps its historical module/test path and runs with the same two assertions, production module paths/visibility/re-exports/guard anchors and behavior are unchanged, and no semantic or legacy-retirement scope is opened."
no_safe_slice = "Return to design_stop if the test needs a production API change, a new module/re-export, a changed guard token, a second scan, any semantic edit, or either source reaches 760/800 lines."
non_claims = "No DeclaredInstance co-seal, Method(Some)/Method(None) cutover, direct-call issuer/loan, Resolved/Call-schema retirement, panic/throw/exit change, VM/backend/JSON change, or guard deletion/new registry row."
worker_audit_head = "current HEAD ae41e70e83"
worker_audit_result = "A read-only audit found broad production splits unsafe because existing guards anchor parent files. The only conditional GO was this test-only tail extraction; replay_function_call_trace and all production helpers remain in the parent. A follow-up audit rejected the existing test-file destination at 760 lines and approved a private sibling while preserving the historical module path."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_preflight_route_tests.rs",
  "src/mir/builder/calls/function_call_preflight_source_identity_tests.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/method_call_terminal.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/normal_callable_semantic_package/declared_instance_call.rs",
  "tools/checks/guard_rows.toml",
  "VM/backend/JSON/Call schema",
]
focused_test_filters = ["exact_resolver_symbol_is_required_before_argument_lowering"]
changed_test_names = ["explicit_source_identity_tests::exact_resolver_symbol_is_required_before_argument_lowering"]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
focused_test_runs = [
  "rustfmt --edition 2021 --check src/mir/builder/calls/function_call_preflight_route.rs src/mir/builder/calls/function_call_preflight_route_tests.rs src/mir/builder/calls/function_call_preflight_source_identity_tests.rs: pass",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib mir::builder::calls::function_call_preflight_route::explicit_source_identity_tests::exact_resolver_symbol_is_required_before_argument_lowering -- --exact --test-threads=1: 1 passed; 0 failed; 7535 filtered out",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib -- --list | rg exact_resolver_symbol_is_required_before_argument_lowering: pass",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass; existing warnings only",
  "line census: function_call_preflight_route.rs=742, function_call_preflight_route_tests.rs=740, function_call_preflight_source_identity_tests.rs=17: pass",
]
implementation_commit = "626c1fa5d6"
evidence = "The inline test-only module moved to src/mir/builder/calls/function_call_preflight_source_identity_tests.rs, declared by the same production parent, preserving the historical module/test path. Production route logic and the existing 740-line test suite were unchanged; the parent is 742 lines and the new sibling is 17 lines. The exact test passed after a clean lib-test rebuild, cargo --list found the same path, quick library check passed, rustfmt passed, pointer guard passed, and diff check passed."
red_classification = "No current-change red. The full workspace has existing warnings/debt; this BoxShape row did not alter production semantics and did not claim full cargo test/link."
reopen_trigger = "Reopen only if the historical test path cannot compile, a guard begins requiring the old inline body, a production caller/module surface changes, or the parent/test/sibling source reaches 760/800 lines. Semantic DeclaredInstance remains owned by the parent design stop."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_cataloged_provider_bare_panic_contract_d0_2026_08_30]
status = "accepted_target_parked_sealed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-PANIC-CONTRACT-D0"
parent_row = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
ceremony_tier = "T1"
implementation_permission = false
worker_premise_audit_reason = "This is not a Fast-path row because selecting panic(String) -> Never changes the source-language terminal-Fault authority, canonical MIR terminator, and runtime ABI ownership; read-only workers must first prove the existing Fault/Throw/Call surfaces and identify the sole reusable owner without treating VM or Extern strings as authority."
worker_premise_audit_result = "Two read-only audits at HEAD 037c12dc52 found no live exact panic issuer, generic Fault terminal, or runtime ABI. They confirmed ordinary FunctionCall parsing, noncatchable Fault semantics, mandatory cleanup chronology, specialized non-reusable CheckedCallOutFault/Trap terminals, legacy Throw/Catch incompatibility, and Rust VM/Ring0/string handlers as evidence-only consumers."
decision = "LANG-PANIC-TERMINAL-FAULT-D0 accepted: exact bare panic(message) retains ordinary FunctionCall syntax but is a semantic-reserved panic(String) -> Never builtin. It yields a noncatchable pending Fault, runs the common exit transaction and cleanup/Home release, then publishes one successorless final Fault. It is not FreeStatic, SameModule, Extern, Throw, Result, or bare exit. Production activation remains zero."
source_authority = "Exact bare source identifier panic, exact arity 1, a statically verified String result contract, and the exact source call site. Semantic declaration/name admission reserves the bare spelling; qualified obj.panic/provider spellings remain separate declared owners."
canonical_issuer = "No live issuer exists. The required future chain is one finite typed panic source product -> pending Fault(Panic, message) -> common verified exit transaction -> generic final Fault terminal -> selected runtime Fault ABI. The current BuiltinGlobal classifier, SameModule synthesis, Extern strings, Throw/Catch, CheckedCallOutFault, Ring0, and Rust VM are not issuers."
non_authority = [
  "generic FunctionCall AST alone, BuiltinGlobal classification, name/arity, CanonicalGlobalTargetV1::new_free_function, catalog-presence lookup, resolver, generated symbol, registry, effects analyzer, CallFlags::no_return, VM/backend, Ring0, fixture, fallback, and retry",
  "legacy AST/MIR Throw or Catch, checked-provider CheckedCallOutFault, pinned-text trap, process status projection, and a backend unreachable instruction",
]
fail_fast_boundary = "Reserve the exact bare identity and verify arity/String facts before argument effects or generic target synthesis. After admission evaluate the message once; an argument Fault remains primary, otherwise create one pending Panic Fault, drain the common exit transaction, and publish one final successorless Fault. No resolver, retry, fallback, or normal successor exists."
census_boundary = "Bare source panic FunctionCall -> exact semantic admission -> pending Fault -> exit transaction/cleanup -> final Fault -> selected runtime projection; includes wrong arity/type/shadow attempts and both current Cataloged caller families, and excludes bare exit, qualified members/providers, Math, GC, DeclaredInstance, CorePlan, physical thunk, nonselected VM expansion, JSON compatibility, and final Call-schema work."
finite_inventory = [
  "language owners: docs/reference/language/failure-outcome-relations.md, semantic-kernel.md, function-call-evaluation.md, EBNF.md, and status-index.md",
  "current wrong ingress: call_name_classification BuiltinGlobal -> function_call_preflight_route SameModule FreeFunction synthesis",
  "missing owners: typed panic source issuer, live common exit transaction, generic final Fault terminal, and selected runtime Fault ABI",
  "specialized non-reusable siblings: CheckedCallOutFault and PinnedTextResidenceTrap",
  "legacy retirement sibling: source try/throw/catch/finally stay rejected; AST/MIR/JSON Throw/Catch transport is a separate census",
  "qualified provider siblings and bare exit remain separate rows",
]
finite_states = [
  "AcceptedTargetParked: language contract is fixed but production issuer/exit/terminal/ABI chain is incomplete",
  "SelectedPanic: exact bare panic/1 plus verified String facts; evaluate the message once, then issue pending Panic Fault",
  "ArgumentFault: message evaluation Faults; preserve that earlier Fault and do not issue a second Panic Fault",
  "WrongArityOrType: typed reject before argument effects and MIR publication",
  "ShadowRejected: a local, parameter, or source callable cannot claim the bare reserved name",
  "QualifiedCallOwned: obj.panic/provider spelling follows its exact declaration and is not this builtin",
  "BareExitSeparate: no alias to panic and no issuer selected by this row",
]
ordered_tasks = [
  "D0 LANG-PANIC-TERMINAL-FAULT-D0: reference Decision, call chronology, cleanup rule, Never boundary, reserved-name rule, and throw/exit separation (accepted; production 0)",
  "D1 MIR-PANIC-FAULT-PIPELINE-COSEAL-D1: name the exact typed source product, pending Fault record, common exit transaction consumer, final generic Fault terminal, selected runtime ABI, and one atomic switch boundary",
  "I0 MIR-PANIC-FAULT-PIPELINE-I0: only after D1 closes, land a 2-5 commit bounded series with no production switch until typed source -> cleanup -> final Fault -> selected LLVM/C projection is complete",
  "R0 MIR-PANIC-LEGACY-CALL-RETIRE-R0: in the I0 switch commit delete BuiltinGlobal/name -> SameModule/Extern panic publication, normal fallthrough, and resolver/backend retry; retain no parallel success path",
  "R1 MIR-LEGACY-EXCEPTION-TRANSPORT-RETIRE-R0: separately census and retire parser/AST/MIR/JSON-v0 Throw/Catch compatibility only after its named external callers are zero or explicitly retired; never relabel it as panic",
]
acceptance = "Reference SSOT agrees on ordinary call syntax, reserved bare identity, exact String/arity contract, semantic non-source-spellable Never, noncatchable pending Fault, mandatory cleanup/exit transaction, final successorless Fault, and separation from Result/throw/exit. Implementation stays closed until D1 proves the whole chain and an atomic old-edge deletion."
no_safe_slice = "Keep this target ParkedSealed while the common exit transaction has production activation 0, no generic final Fault/ABI owner exists, or the path would require SameModule/Extern strings, Throw/Catch reuse, specialized terminal reuse, backend inference, fallback, or retry."
non_claims = "No production panic support, public Never type, bare exit decision, qualified provider change, assert/unreachable feature, Throw/Catch implementation reuse, nonselected VM parity, Call-schema change, or guard deletion."
reopen_trigger = "Reopen D1 only when the same source/session can co-seal exact panic/1 String admission with the common pending-Fault exit transaction, one generic final Fault terminal, and the selected runtime ABI. Otherwise leave this lane ParkedSealed and continue an independent active lane."
activation = "language_target=1; parser_registry=0; source_issuer=0; exit_transaction_consumer=0; generic_fault_terminal=0; selected_runtime_abi=0; production_switch=0"
corpus_evidence_non_authority = "apps/lib/json_native/parser/parser.hako accumulates parse errors in me.errors and projects them as values through JsonParserFacadeErrorProjectionV1; this demonstrates that ordinary fallible code does not require throw, but it does not activate panic or the typed Result-only ? consumer."
result_qmark_followup = "LANGUAGE-RESULT-EXIT-C-PRIME0-P0 -> I0 remains a separate selfhost-readiness lane because its typed Result-only ? production consumer is 0. It is not a dependency for the panic language Decision and must not reopen throw, catch, or implicit Fault-to-Result conversion."
next_execution = "none__parked_until_common_exit_fault_chain"

[same_module_me_method_canonical_plan_2026_08_30]
status = "accepted_bounded_series"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
decision = "Retain me.method as ordinary language syntax. Replace the old header/symbol/receiver-prefix wiring in two authority-correct cohorts: StaticCurrentOwner becomes a direct same-module Global call, while true DeclaredInstance becomes Method(Some(receiver)). Do not build parallel family-specific pipelines; both cohorts reuse the existing source, declaration, publication, and terminal owners."
finish_line = "Every selected me.method target is fixed before argument effects; canonical MIR contains no header/name/registry-derived target, no manual duplicate receiver, and no retry/fallback. Selected LLVM/AOT lowers both cohorts to direct same-module calls. The old first-Box classifier, formatted Global reissuer, and manual me prefix are physically deleted only after caller-zero."
ordered_rows = [
  "I0: MIR-CALL-SAME-MODULE-STATIC-CURRENT-OWNER-HANDOFF-I0 — existing exact issuer -> Global(StaticBoxMethod), N source args, direct selected LLVM/AOT call",
  "D0: MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0 — exact source/site/declaration/brand/receiver/ABI/effect relation or NoSafeSlice",
  "I1: MIR-CALL-ME-DECLARED-INSTANCE-CANONICAL-I0 — co-sealed relation -> Method(Some(receiver)), N source args, direct selected LLVM/AOT projection",
  "I2: MIR-CALL-METHOD-RECEIVER-ABI-I0 — receiver stays in Callee in canonical MIR and is projected exactly once to physical arg0",
  "R0: MIR-CALL-ME-HEADER-LEGACY-RETIRE-R0 — caller-zero deletion of header classification, manual receiver prefix, formatted Global reissuer, and superseded tests/guards",
  "C0: return to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 and recount the remaining production families",
]
progress_metrics = [
  "StaticCurrentOwner header-first classifications -> 0",
  "true DeclaredInstance Global + manual me-prefix writers -> 0",
  "canonical Method calls with receiver duplicated in args -> 0",
  "me.method name/header/registry target reconstruction -> 0",
  "selected LLVM/AOT runtime name/slot lookup for these exact cohorts -> 0",
]
non_claims = "No me.method feature retirement, no Method(None)/Resolved/Call-schema change, no provider/CorePlan/physical-thunk work, no nonselected VM/WASM feature expansion, and no guard deletion before successor-backed caller-zero."

[same_module_static_current_owner_handoff_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-STATIC-CURRENT-OWNER-HANDOFF-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T1"
box_kind = "BoxShape one production-edge replacement"
implementation_permission = false
decision = "For a StaticBoxMethod caller, canonical me.method has no runtime receiver. Consume the already-issued current-owner target before legacy header observation and arguments, then emit Global(StaticBoxMethod) with exactly N source arguments. OutsideStaticCurrentOwner is a named sibling and must continue unchanged."
source_authority = "VerifiedSourceMethodCallSiteV1 with canonical ASTNode::Me, exact caller/site/catalog, and caller namespace StaticBoxMethod."
canonical_issuer = "VerifiedSourceStaticCallTargetCatalogV1::extend_current_owner issues VerifiedCurrentOwnerStaticCallTargetV1; VerifiedWholeSourceStaticCallTargetInventoryV1 and VerifiedStaticCallResultPublicationOwnerV1 carry its CanonicalSameModuleCallableKeyV1 to the existing one-shot Builder ingress."
non_authority = [
  "first physical parameter is Box, module/header observation, current_static_box, bound-me presence, owner/method/arity text, generated symbol, registry, resolver, fallback, retry, fixture, and backend route",
]
fail_fast_boundary = "A Cataloged StaticBoxMethod source site must yield Selected or TargetOnly before prepare_me_call_execution_v1, header observation, child/argument descent, ValueId/Const emission, collector mutation, or Call publication. NoExactStaticTarget, foreign lineage/catalog, duplicate/double take, or arity drift rejects there."
census_boundary = "raw Cataloged MethodCall whose receiver normalized to Me and caller namespace is StaticBoxMethod -> exact publication-owner take -> existing static-result physical bridge -> typed Global Call receipt; includes exact target/result states, argument order, current header-first bypass, and StaticCurrentOwner tests; excludes true DeclaredInstance, inline record/setter, qualified Math/Extern, generic/scalar/inline compatibility, Method ABI, VM/backend, JSON, and Call schema."
finite_states = [
  "ExactSelected: take the result handoff once, project the owned target, lower N arguments once, publish one direct Global Call and result",
  "ExactTargetOnly: take the canonical key once, project the owned target, lower N arguments once, publish one direct Global Call",
  "StaticCurrentOwnerMissing: typed reject before header and argument effects; no compatibility fallback",
  "OutsideStaticCurrentOwner: do not consume the static owner; preserve the named DeclaredInstance/compatibility sibling for its own row",
  "UnavailableCompatibility: retain the existing compatibility behavior; do not claim source-backed success",
]
old_edge_delete_set = [
  "StaticCurrentOwner -> observe_me_call_parameters/header first-Box classification",
  "StaticCurrentOwner -> PreparedMeReceiverV1::Instance or LoweredGlobal::Instance",
  "StaticCurrentOwner -> generate_method_function_name target reconstruction",
  "StaticCurrentOwner -> manual me prefix or receiver-bearing ABI",
]
ordered_tasks = [
  "I0-A: make the existing publication ingress distinguish StaticCurrentOwner from OutsideStaticCurrentOwner using the Cataloged caller namespace; issue no new semantic receipt",
  "I0-B: invoke that ingress before prepare/header observation; Selected/TargetOnly reuse the existing target-before-arguments physical bridge",
  "I0-C: delete only the StaticCurrentOwner header-first, lowered-instance, symbol-reconstruction, and receiver-prefix edges; preserve DeclaredInstance and compatibility siblings",
  "I0-D: add a dedicated test sibling and dedicated small guard; update source-call/Builder README ownership and record focused positive/negative evidence",
  "I0-E: close out to the DeclaredInstance co-seal D0 without claiming whole me.method or SameModule completion",
]
acceptance = "Exact source target is taken before header observation and arguments; N source arguments are lowered once left-to-right; one Global(StaticBoxMethod) Call is emitted; no runtime receiver or me prefix exists; missing/foreign/duplicate/arity failures are pre-effect; existing inline/helper/Math/DeclaredInstance/compatibility precedence is unchanged; selected LLVM/AOT uses its direct Global lowering; no string/name/slot lookup, resolver, fallback, or retry appears; every touched source is below 760 lines and 800 is a hard stop."
no_safe_slice = "Return to design_stop if the installed publication owner cannot be reached before header observation, the Cataloged caller namespace cannot distinguish StaticCurrentOwner without a second scan, a new semantic receipt/target issuer is needed, an argument/effect must occur before selection, a DeclaredInstance or compatibility behavior must change, a nonselected backend needs a feature addition, or any touched source crosses 760/800."
non_claims = "No true DeclaredInstance cutover, no Method(Some)/Method(None) ABI change, no generic/scalar/inline/Math/Extern change, no Call schema, VM/WASM feature, provider/CorePlan/thunk, fallback, retry, or legacy guard deletion."
allowed_files = [
  "src/mir/builder/static_result_publication_ingress.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/static_result_publication_physical_bridge.rs",
  "src/mir/builder/calls/member_route.rs",
  "src/mir/builder/calls/me_method_canonical_cutover_tests.rs",
  "src/mir/builder/calls/mod.rs",
  "src/mir/source_call_target/README.md",
  "src/mir/builder/README.md",
  "tools/checks/lib/mir_call_d1b_me_method_cutover_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/normal_callable_semantic_loan_port.rs",
  "src/mir/builder/recursive_child_lowering.rs",
  "src/mir/builder/calls/member_route_descent_tests.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "VM/backend/JSON",
  "Call schema",
]
coverage_base_commit = "cd5c8719cd"
planned_test_names = [
  "static_current_owner_target_is_taken_before_header_and_arguments",
  "static_current_owner_lowers_source_arguments_once_without_receiver_prefix",
  "static_current_owner_missing_target_rejects_before_arguments",
  "outside_static_current_owner_preserves_declared_instance_sibling",
  "static_current_owner_argument_failure_does_not_emit_retry_or_fallback",
  "declared_instance_cataloged_source_stays_outside_static_ingress",
]
planned_focused_test_filters = [
  "me_method_canonical_cutover_tests",
  "static_result_publication_ingress",
]
changed_test_names = [
  "static_current_owner_target_is_taken_before_header_and_arguments",
  "static_current_owner_lowers_source_arguments_once_without_receiver_prefix",
  "static_current_owner_missing_target_rejects_before_arguments",
  "outside_static_current_owner_preserves_declared_instance_sibling",
  "static_current_owner_argument_failure_does_not_emit_retry_or_fallback",
  "declared_instance_cataloged_source_stays_outside_static_ingress",
]
focused_test_filters = [
  "me_method_canonical_cutover_tests",
  "static_result_publication_ingress",
]
implementation_commit = "2b7b3e7489"
focused_test_runs = [
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib me_method_canonical_cutover_tests -- --test-threads=1: 5 passed; 0 failed",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib static_result_publication_ingress -- --test-threads=1: 4 passed; 0 failed",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass; existing warnings only",
]
guard_runs = [
  "python3 -m py_compile tools/checks/lib/mir_call_d1b_me_method_cutover_guard.py tools/checks/lib/mir_call_d1b_active_surface_guard.py: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "cargo test --profile quick --lib -- --list: six changed tests uniquely listed and filter-covered",
  "git diff --check: pass",
]
evidence = "At implementation commit 2b7b3e7489, StaticCurrentOwner me.method takes the existing source/site/catalog handoff before legacy header preparation, projects a typed Global target, checks source arity before descent, lowers N arguments once without a receiver prefix, and preserves InstanceBoxMethod as an unavailable sibling. The dedicated five-test cutover suite and four-test ingress suite are green; quick library check is green with existing warnings. The dedicated guard is dispatched by the stable active-surface entry and enforces the finite source/instance boundary plus the 760/800 line budget."
red_classification = "No current-change red. Full cargo test/link was not run because this repository's test binary is memory-heavy; focused tests, cargo --list coverage, quick library check, pointer guard, lifecycle guard, py_compile, and diff check are the claimed evidence."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0__design_stop"
guard_plan = "Create one small dedicated cutover guard and dispatch it from the existing active-surface guard. It must prove target-take before arguments, one argument descent, direct typed Global emission, no generate_method_function_name/header observation/AST rescan/resolver/fallback/retry in the selected branch, the exact old-edge deletion set, cargo --list coverage for every changed test, and <760/<800 line limits. Do not grow the 754-line method-corridor guard."

[me_declared_instance_source_coseal_d0_2026_08_30]
status = "superseded_by_relation_issuer_d0"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T1"
implementation_permission = false
disposition = "SupersededHistoricalBoundary"
superseded_by = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
historical_scope = "This section preserves the pre-2026-08-31 audit. Current authority, task order, and failure states live only in mir_call_me_declared_instance_relation_issuer_d0_2026_08_31."
decision = "Retain true instance me.method and design its canonical path as Method(Some(receiver)) with N source arguments. Before implementation, co-seal the exact resolved source method-call row, ReceiverPolicyV1::DeclaredInstance, exact InstanceBoxMethod declaration, same catalog/package brand, bound me value, physical receiver ABI, return/result/effect facts, and selected direct-call eligibility."
source_authority = "VerifiedResolvedMethodCallSourceV1 plus the exact FunctionOwnerIdV1/SourceExprSiteV1/receiver site, DeclaredInstance receiver policy, InstanceBoxMethod declaration, same-session package/catalog brand, and the bound receiver supplied by the function owner."
canonical_issuer = "No complete existing generic issuer exists at HEAD. The design-only candidate is one private package-side assembler at src/mir/normal_callable_semantic_package/declared_instance_call.rs; it may co-seal existing source/catalog/ABI/result/effect products but may not become a second source or target authority. Until its source-site-to-declaration relation is issued and its existing result/effect references are named, implementation remains NoSafeSlice."
fail_fast_boundary = "Missing, duplicate, foreign, ambiguous, arity/ABI/effect/return mismatch rejects before argument descent, receiver materialization, MIR effects, Call publication, or backend route selection."
census_boundary = "declared instance MethodCall source row -> exact InstanceBoxMethod declaration/brand/bound receiver/ABI/effect relation -> candidate Method(Some(receiver)) Recipe -> selected LLVM/AOT direct call; includes current LoweredGlobal::Instance and Standard Method(Some) producer/consumer boundaries; excludes static current-owner, Method(None), Call schema, providers/CorePlan/thunk, and nonselected backend feature work."
ordered_tasks = [
  "D0-A: reconcile source row, declaration key, package/catalog brand, receiver binding, ABI, return/result/effect owners in both directions",
  "D0-B: name the sole existing issuer/co-seal owner or record NoSafeSlice; no family-specific parallel loan or new resolver",
  "D0-C: freeze canonical MIR as Method(Some(receiver)) plus N source arguments and selected LLVM/AOT direct lowering as receiver physical arg0 plus N",
  "I0: only after D0 closes, replace the true-instance Global+manual-prefix edge atomically with the canonical Method carrier and delete its old edge",
]
acceptance = "One exact same-session relation owns source site, target declaration, receiver, ABI, return/result/effect facts before arguments; canonical MIR has Method(Some(receiver)) and N source arguments; selected LLVM/AOT emits one direct same-module call with receiver projected exactly once; runtime name/slot lookup, second resolver, fallback, and retry are zero."
no_safe_slice = "Remain in design_stop if the exact relation or issuer is absent, a new target/receipt must be invented, box_type/header/name/registry/backend data would decide meaning, effects/return/ABI are incomplete, or implementation must mix Call schema or nonselected backend support."
non_claims = "No current implementation permission, no Global instance target, no dynamic lookup acceptance, no Method(None), Call schema, VM/WASM feature, or legacy header deletion before successor-backed caller-zero."
finite_inventory = [
  "source facts: src/mir/resolved_semantics/body_shape.rs (VerifiedResolvedMethodCallSourceV1 owner/site/receiver-site/arguments/arity/result)",
  "declared instance declaration: src/mir/builder/callable_declaration_catalog/key.rs and catalog.rs (InstanceBoxMethod key; no source-site issuer)",
  "package/brand/ABI facts: src/mir/normal_callable_semantic_package/issuer.rs, physical_signature.rs, normal_callable_semantic_loan_port.rs",
  "live legacy producer/consumer: src/mir/builder/method_call_handlers.rs, method_call_terminal.rs, member_route.rs",
  "selected physical consumer only: user_box_method_route_plan/materialization.rs and lang/c-abi/shims/hako_llvmc_ffi_mir_call_shell.inc",
  "excluded siblings: StaticCurrentOwner, Method(None), providers, CorePlan, physical thunk, Call schema, VM/backend",
]
finite_states = [
  "DeclaredInstanceExact: desired Method(Some(receiver)) with N semantic arguments and receiver+N physical ABI; not issuer-backed at HEAD",
  "StandardMethodSome: existing semantic shape, but generic target/receiver/source co-seal is not a production issuer",
  "LoweredGlobalInstanceLegacy: live Global plus manual receiver-prefix path; ParkedSealed until successor or explicit retirement contract",
  "UserBoxDirectRouteReady: existing backend projection may be reused only after source-backed issuer and body/return/effect parity are co-sealed",
  "MissingForeignAmbiguous: typed pre-effect reject or ParkedSealed; no resolver/fallback/retry",
]
worker_audit_head = "f28067a429"
worker_audit_result = "The last read-only audits, refreshed by a docs-only census correction through HEAD f28067a429, confirm that the live final parser/resolver batch carries opaque parser identity, provenance, source site, and callable syntax, while InstanceBoxMethod declaration/catalog carries nominal identity and brand; no existing same-session issuer co-seals their callsite-to-declaration relation. Result/Completion retention is landed, but ordinary effect issuance and the selected C capability are still absent. The family remains NoSafeSlice; retain true me.method and keep the legacy instance path ParkedSealed."
worker_audit_refresh = "D0-B2/B3 are closed as a design finding: the future relation must be co-issued from the live parser/resolver source, selector must resolve only inside the exact nominal Box subset, and arity only validates. The package may borrow the relation and existing result/effect/ABI products but may not issue target meaning. Freeze missing/foreign/duplicate/receiver/arity/ABI/result/effect rejection before receiver or argument effects; implementation_permission remains false and no name/header/registry/backend inference or fallback/retry is allowed."
evidence = "The desired semantic carrier is Method(Some(receiver)) with N explicit source arguments; physical direct ABI is receiver lane plus N. The current instance path still descends arguments, prepends me, and emits a Global through the shared legacy terminal. Existing source facts, catalog keys, package/ABI facts, and C/LLVM route plans do not form one source-backed production issuer."
reopen_trigger = "Reopen only when an existing same-session owner is found that co-seals exact source/site, InstanceBoxMethod declaration, catalog/package brand, bound receiver, body/return/effect, and physical ABI, or when an explicit language/ABI decision authorizes a typed pre-effect retirement. Header/name/registry/backend inference is never a reopen trigger."
worker_followup_head = "f28067a429"
worker_followup_result = "The follow-up audit confirms no reusable generic DeclaredInstance issuer. The missing product is the exact call-site-to-declaration relation between live parser/resolver source identity and the same-session nominal declaration/brand. A package assembler may co-seal that relation with existing contracts only after issuance; it may not call declaration_for or repair identity from name/arity. The selected C route remains a consumer audit, not a semantic issuer."
worker_followup_refresh = "No bounded production edge is open. Keep this D0 as NoSafeSlice/ParkedSealed until source-adoption D0 names the non-reconstructing crosswalk and the finite failure matrix; do not add a parallel family loan, second resolver, or inferred receiver."
receiver_binding_gap = "Resolved by source-facts row 01332528ed: VerifiedResolvedMethodCallSourceV1 now preserves BodyMeReceiverV1::Lexical(binding) as a lexical local reference and StaticCurrentOwner as CurrentOwner. The remaining DeclaredInstance gap is the source-site-to-declaration/brand/ABI/result/effect relation; no target or receiver ValueId may be inferred until that relation is issued."
followup_worker_result = "Fresh read-only audits at HEAD 3f02266d84 trace the concrete `Ordinary.call(x) { return me.target(x) }` case: source facts, semantic batch, same-brand `InstanceBoxMethod` declarations, selected batch slot, and receiver-lane physical signature are all available, but package model/issuer has no caller-site-to-callee-declaration relation or direct-call loan. `method_call_handlers` still uses ambient `me`, generated symbols, and arguments-first receiver prefix. Existing result/completion/effect references are not a complete generic DeclaredInstance owner; physical header omits unannotated results. Near-limit owners remain body_shape.rs 733 lines and normal_callable_semantic_loan_port.rs 748 lines, so any co-seal orchestration needs a separate bounded owner or a prior BoxShape split."
followup_worker_case = "src/mir/source_call_target/current_owner_tests.rs:119-136 -> Ordinary.call -> me.target: facts in body_shape.rs:210-224,395-454,519-533; batch in callable_semantic_batch/issuer.rs:118-176,228-305; catalog in callable_declaration_catalog/source_backed.rs:114-201,261-280; selected/ABI in normal_callable_semantic_package/selected_mapping.rs:136-168 and physical_signature.rs:211-300; missing relation/loan in normal_callable_semantic_package/model.rs:29-54 and issuer.rs:389-445,617-647; legacy consumer in method_call_handlers.rs:127-170,347-413."
d0_b2_b3_status = "closed_no_reusable_issuer"
d0_b2_b3_conclusion = "D0-B2/B3 are design-complete as a NoSafeSlice finding: the future resolver sibling must co-issue the exact source-site-to-declaration relation from the live parser/resolver source, while a private package assembler may borrow that relation and existing catalog/brand, selected slot, receiver ABI, result, and effect products without reissuing any meaning. Because the relation crosswalk, ordinary effect issuer, and selected C capability do not exist today, no I0 implementation row may open. All missing/foreign/duplicate/receiver/arity/ABI/result/effect states are pre-effect CutoverBlockerOpen."
declared_instance_d0_tasks = [
  "D0-B1: landed at 01332528ed; preserve BodyMeReceiverV1::Lexical(binding) versus StaticCurrentOwner in the resolver-owned source product without AST, ValueId, target, or physical-symbol storage",
  "D0-B2: name one source-backed relation issuer joining caller FunctionOwnerIdV1/SourceExprSiteV1, lexical receiver binding, InstanceBoxMethod declaration identity, resolver/catalog/package brand, selected batch slot, source arity, receiver+N physical ABI, and existing result/effect references; an assembler may only borrow these products",
  "D0-B3: freeze the finite typed rejection matrix (missing/foreign/ambiguous/duplicate receiver, brand, declaration, arity, ABI, result, effect) at the pre-argument boundary; no name/arity consumer lookup, second resolver, fallback, or retry",
  "D0-B4: if no existing relation owner and complete physical/result/effect references can be named after B1-B3, record ParkedSealed with an observable reopen trigger instead of opening an implementation row",
]
source_adoption_d0 = "MIR-CALL-ME-DECLARED-INSTANCE-RESOLVER-SOURCE-ADOPTION-D0 is a design-only child. The live owner is the final parser/resolver source issuance that already produces VerifiedFinalCallableProgramSourceV1 and the same FunctionSemanticResolverSession batch; the standalone test-oriented ParserBoxResolverSourceHandoffV1/InstanceMethodFunctionCarrierIssuerV1 path may not be re-run as a second resolver. The child decides the non-reconstructing crosswalk from the live CallableMethodSourceObservationV1 to the same-session nominal InstanceBoxMethod declaration."
source_adoption_relation_shape = [
  "root lexical DeclaredInstance caller FunctionOwnerIdV1 + exact SourceExprSiteV1/receiver site + parser provenance",
  "same-session ResolverCatalogBrandV1 + exact nominal Box identity + opaque declaration/member identity",
  "selector/name is checked only inside that nominal subset; declaration arity validates after selection",
  "no AST, batch/Builder key, ValueId, Callee, symbol, header, registry, result/effect/ABI, cloned arguments, fallback, or retry",
]
source_adoption_crosswalk_owner = "New resolver sibling src/mir/resolved_semantics/declared_instance_call_relation.rs, invoked exactly once inside the existing issue_resolved_callable_semantic_batch_* with_callable_semantic_syntax HRTB after the existing resolve_source_bound_selected_callable_forests_* call returns. It consumes borrowed final syntax rows plus already-issued root forests/body-shape products and emits only the AST-free callsite-to-declaration relation; it never calls FunctionSemanticResolverSession::resolve, resolve_forest_with_body_shapes, or any standalone parser handoff issuer."
source_adoption_crosswalk_inputs = [
  "FinalCallableSemanticSyntaxRowRefV1: parser CallableDeclarationIdentityV1, final Box source site, owner name, declaration mode, and method observation",
  "VerifiedResolvedCallableSemanticRowV1: batch slot, root FunctionOwnerIdV1, forest, and already-issued resolver function/body facts",
  "VerifiedResolvedMethodCallSourceV1: exact call/receiver/argument sites, lexical receiver reference, selector, and source arity",
  "SourceBindingSiteV1::Receiver plus the root function binding record; Parser provenance and one fresh resolver-owned nominal brand are co-sealed in the relation catalog",
]
source_adoption_crosswalk_output = "Private non-Clone DeclaredInstanceCallSourceDispositionV1 with NoRootDeclaredInstanceCall or Published(VerifiedDeclaredInstanceCallRelationCatalogV1). The catalog owns resolver brand/provenance and rows carrying caller parser identity/owner/call site/receiver site/receiver binding, target parser identity plus DeclaredInstanceMethodIdentityV1, target owner, and source arity. It carries no ValueId, Callee, ABI, effect, result, symbol, key, or arguments."
source_adoption_nominal_inventory = "Within the same HRTB, collect every final direct Box-method row into an exact nominal Box subset keyed by parser provenance and source Box statement. Reject duplicate visible names across direct static/instance rows before relation publication; select a unique instance declaration by selector only inside the caller's nominal Box subset, then validate arity. Generated/delegate-only rows remain outside this child and are checked by the existing parser inventory/DEL-003 lane."
source_adoption_storage = "VerifiedResolvedCallableSemanticBatchV1 owns the non-Clone disposition as a sibling to rows and callable_index. The normal callable package may borrow/move this already-issued relation later; it does not issue, repair, or re-resolve it."
source_adoption_partition = [
  "RootDeclaredInstance: the only candidate for this child; receiver binding must be the root Receiver binding, not the spelling me alone",
  "StaticCurrentOwner: named sibling already landed; never falls back into this relation",
  "NestedOrUpvarReceiver: ParkedSealed until capture and receiver ABI are explicit",
  "DelegateOrGeneratedOnly: separate DEL-003 relation; no implicit forwarding or overload search",
  "ForeignOrDuplicate: typed pre-effect rejection before receiver materialization or argument effects",
]
source_adoption_fail_fast = "The source-adoption child rejects missing/foreign parser provenance or resolver brand, non-root receiver binding, missing/ambiguous nominal declaration, duplicate visible method name, selector mismatch, arity mismatch, and duplicate source site before any target, receiver ValueId, result/effect/ABI, package loan, MIR, backend, or argument effect is issued."
source_adoption_acceptance = "D0 closes only when the live final parser/resolver owner and the non-reconstructing crosswalk are named, the relation shape and root-only partition are fixed, and the seven counterexample groups (root positive, cross-Box same-name, mixed static/instance, foreign provenance/brand, wrong arity, nested/upvar exclusion, duplicate/delegate collision) have observable typed outcomes. This child issues no target and does not open production implementation."
source_adoption_no_safe_slice = "Keep implementation_permission=false if the live final source cannot retain the parser identity and nominal declaration relation in one issuance, if connecting them requires a fresh resolver traversal, or if any package/name/ordinal/header/registry/backend lookup would decide identity. Do not solve the gap with a new family loan or default/empty relation."
declared_instance_d0_acceptance = "The source product preserves the exact receiver policy and binding; one same-session owner can prove caller/site/declaration/brand/slot/ABI/result/effect relation in both directions; all other states reject before receiver materialization or argument effects. This D0 does not issue Method(Some(receiver)), a loan, a Call, or backend code."
box_shape_preparation = [
  "D0-B0: split only existing orchestration/validation responsibilities before semantic edits; preserve module paths, visibility, behavior, and public re-exports",
  "D0-B1: keep each touched Rust source below 760 lines and stop at 800; do not hide growth by compression",
  "D0-B2: after the split, name one private co-seal owner for source/site, InstanceBoxMethod declaration, package/catalog brand, bound receiver, ABI, body/return, and effect; if none exists, remain NoSafeSlice",
]
parked_boxshape_followup = "Read-only audit at HEAD 3dafaeae9c found one additional test-only candidate in src/mir/builder/indexing.rs:737-789 (790-line parent). It is ParkedSealed because it remains below the 800-line hard stop and does not block the next semantic row; do not open another BoxShape split unless the file reaches 800 or physically blocks the DeclaredInstance implementation."
box_shape_non_claims = "The preparation split is not an issuer, target, loan, Method(Some) cutover, or production switch. It must not add a family-specific parallel pipeline, default/empty receipt, resolver, header/name inference, or backend-derived meaning."
minimal_co_seal_shape = [
  "resolver-issued caller parser identity + FunctionOwnerIdV1 + exact call_site + receiver_site + root receiver binding",
  "resolver-issued target parser identity + DeclaredInstanceMethodIdentityV1 under the exact nominal Box subset",
  "source_arity and a borrowed target batch slot; no callee key or Builder catalog key",
  "later package co-seal may borrow the relation with existing result/completion, effect, and physical-signature products; no copied body facts",
]
minimal_co_seal_owner = "The source-adoption resolver sibling is the sole relation issuer and is called from the existing callable_semantic_batch issuer closure. The normal_callable_semantic_package remains a later transport/contract co-seal consumer and must not become a declaration_for or target resolver."
source_to_declaration_gap = "VerifiedResolvedMethodCallSourceV1 has call/receiver sites, selector, and arity but no callee declaration identity; InstanceBoxMethod catalog has the declaration and brand but no call-site relation. The missing relation must be issued by one source-backed owner before any name/arity-derived CanonicalSameModuleCallableKey is accepted."
contract_reference_rule = "A target row may borrow an existing same-package physical signature/header and source body/result/effect contract only after exact identity, catalog/package brand, batch slot, receiver binding, and arity agree. Missing/unannotated result or effect is ParkedSealed or typed pre-effect reject; it is never filled from MIR EffectMask, FunctionSignature, header text, or backend plan."
implementation_gate = "The source-adoption/relation I0 is landed at 845bd72641. A separate design-only effect-issuer child must now name the parser callable-contract carriage and one same-session semantic-effect owner; keep the later Method(Some(receiver)) production cutover closed until effect/full-lane and selected-C owners are also named."
next_design_action = "The source-adoption/relation child is landed at 845bd72641. The remaining design question is the source-backed semantic-effect issuer and its eventual co-seal with result/full-lane/selected-C products; keep target/effect/full-lane/selected-C cutover closed until those owners are named."
latest_worker_audit_head = "a2d2ed9de4"
latest_worker_audit_brief = "Decision: keep true DeclaredInstance in design_stop/ParkedSealed; Source authority: the landed live parser/resolver relation plus InstanceBoxMethod declaration/brand; package role: contract/transport co-seal only; Non-authority: package declaration_for, InstanceMethodFunctionCarrier re-resolve, name/header/registry/backend/MIR inference; Fail-fast: reject before receiver/argument effects; Smallest next slice: effect-source carriage/issuer D0 for the exact relation; Non-claims: no Method(Some) cutover, loan, Call schema, VM/backend, panic/throw/exit, or guard cleanup."
next_execution = "superseded__see_MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"

[mir_call_me_declared_instance_resolver_relation_i0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-RESOLVER-RELATION-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0"
observed_commit = "da00098e2a"
implementation_permission = false
box_kind = "resolver-owned AST-free source-site/declaration relation"
decision = "Within the existing final callable-semantic batch with_callable_semantic_syntax HRTB, after the single existing forest/body-shape resolver returns, issue one non-Clone DeclaredInstanceCallSourceDispositionV1 and retain it on VerifiedResolvedCallableSemanticBatchV1. This is a source relation only; it does not issue a target or open production call lowering."
source_authority = "The live final parser/resolver source, its opaque CallableDeclarationIdentityV1/provenance/final syntax rows, and the already-issued same-session resolved forest/body-shape products."
canonical_issuer = "A new small resolved_semantics relation sibling called exactly once by callable_semantic_batch/issuer.rs in the existing HRTB; no package-side issuer and no second resolver/parser handoff."
non_authority = [
  "name/arity as identity or a second lookup key",
  "batch/Builder keys, ordinals, headers, symbols, registries, MIR, backend, fixtures",
  "InstanceMethodFunctionCarrierIssuerV1 or standalone ParserBoxResolverSourceHandoffV1",
  "package declaration_for, target repair, family-specific loan, fallback, retry",
]
fail_fast = "Reject the whole unpublished disposition before receiver ValueId materialization, argument descent/effects, target/result/effect/ABI/package/loan construction, MIR, or backend publication."
allowed_files = [
  "src/mir/resolved_semantics/declared_instance_call_relation.rs",
  "src/mir/resolved_semantics/instance_method_declaration.rs",
  "src/mir/resolved_semantics/declared_instance_contract.rs",
  "src/mir/resolved_semantics/mod.rs",
  "src/mir/callable_semantic_batch/issuer.rs",
  "src/mir/callable_semantic_batch/model.rs",
  "src/mir/callable_semantic_batch/mod.rs",
  "src/mir/callable_semantic_batch/declared_instance_call_relation_tests.rs",
  "src/mir/resolved_semantics/README.md",
  "src/mir/callable_semantic_batch/README.md",
  "tools/checks/lib/mir_call_d1b_declared_instance_relation_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py (I0 dispatch and post-closeout design-stop check)",
  "docs/reference/language/function-call-evaluation.md (status line only at closeout)",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "parser source issuance and body_shape.rs",
  "normal_callable_semantic_package/**",
  "src/mir/builder/**",
  "effect/result/physical-signature/C/backend/VM/JSON/runtime routes",
  "workstream/taskboard and guard registry/shell",
]
disposition = [
  "NoRootDeclaredInstanceCall: complete root census with no eligible root direct instance call",
  "Published(catalog): one complete same-session root relation catalog",
  "all missing/foreign/duplicate/ambiguous/non-root/static/generated/delegate cases: typed reject; no partial publish",
]
relation_row = "caller parser identity + root FunctionOwnerIdV1 + exact call_site/receiver_site + root Receiver binding -> target parser identity + DeclaredInstanceMethodIdentityV1 + target owner + source arity under the same resolver brand/provenance. No ValueId, Callee, ABI, effect, result, symbol, key, or arguments."
partition = [
  "RootDeclaredInstance only",
  "StaticCurrentOwner remains the landed sibling",
  "Nested/upvar, arbitrary-object local, generated/delegate-only, Dynamic/core/qualified/FreeStatic remain outside this row",
]
acceptance = [
  "existing final batch HRTB invokes the relation issuer exactly once after the existing forest/body-shape resolution",
  "batch owns exactly one non-Clone NoRoot/Published disposition; no empty/default receipt or partial catalog",
  "same nominal Box selector is unique and arity only validates; same-name methods on different Boxes remain independent",
  "foreign parser provenance/brand, wrong root receiver binding, duplicate visible name/site, missing/ambiguous declaration, and wrong arity reject atomically",
  "focused relation tests, quick lib check, reusable guard, diff check, and every touched source under 760 lines",
]
focused_test_filter = "cargo test --profile quick --lib declared_instance_call_relation -- --test-threads=1"
changed_test_names = [
  "declared_instance_relation_publishes_one_exact_me_call",
  "same_method_name_on_different_boxes_keeps_nominal_relations_separate",
  "static_current_owner_me_call_is_outside_declared_instance_relation",
  "declared_instance_relation_rejects_target_arity_mismatch_before_lowering",
]
secondary_test_filter = "cargo test --profile quick --lib callable_semantic_batch -- --test-threads=1"
guard_command = "python3 tools/checks/lib/mir_call_d1b_declared_instance_relation_guard.py ."
line_budget = "relation source <=420 lines (split only if it approaches 760); focused tests <=360; issuer 337-><=490; model 462-><=490; declaration accessor 417-><=440; mod <=370; 800 hard stop"
old_edge_metric = "3 -> 3; this I0 issues no Method(Some), emits no Call, and deletes no legacy edge"
implementation_commit = "845bd72641"
implementation_evidence = "The existing final callable-semantic batch now invokes one resolver-owned relation issuer after the single forest/body-shape resolution. The non-Clone disposition retains either NoRootDeclaredInstanceCall or one complete AST-free catalog keyed by exact parser identity/provenance, owner-branded source sites, root receiver binding, nominal Box declaration, and validated arity. Same-name methods on different Boxes remain distinct; static current-owner calls stay outside this relation; wrong arity rejects before lowering. No target, ValueId, effect, result, ABI, package loan, Builder, MIR, backend, fallback, retry, or legacy edge was changed."
observed_line_counts = "declared_instance_call_relation.rs 392; declared_instance_call_relation_tests.rs 95; callable_semantic_batch/issuer.rs 438; model.rs 470; callable_semantic_batch/tests.rs 302; all touched Rust sources below 760 and 800 hard stop"
validation = [
  "cargo test --profile quick --lib declared_instance_call_relation -- --test-threads=1: 4 passed; 0 failed",
  "cargo test --profile quick --lib callable_semantic_batch -- --test-threads=1: 24 passed; 0 failed",
  "cargo check --profile quick --lib: pass; existing warnings only",
  "rustfmt --edition 2021 --check on all touched Rust sources: pass",
  "python3 tools/checks/lib/mir_call_d1b_declared_instance_relation_guard.py .: pass",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
  "cargo test --profile quick --lib -- --test-threads=1: current 7372 passed, 140 failed, 29 ignored; parent 9ebf6f82c6 7367 passed, 141 failed, 29 ignored; four relation tests are included and no newly failing test name appears in the current-vs-parent comparison",
]
red_classification = "The full lib run is not green and is not presented as green: current 845bd72641 records 7372 passed, 140 failed, 29 ignored, while parent 9ebf6f82c6 records 7367 passed, 141 failed, 29 ignored with no newly failing test name in the comparison; the four added relation tests pass. The remaining 140 failures are pre-existing baseline debt outside this source-only row and must be handled by a separate dev-gate/baseline task before broad Call-schema work. A whole-workspace cargo fmt --all -- --check also reports pre-existing formatting drift in src/runner/core_executor.rs outside this row; touched Rust sources pass individual rustfmt checks."
closeout = "I0 landed at 845bd72641. Restore work_mode=design_stop/current_execution_row/current_design_stop/next_design_card to MIR-CALL-ME-DECLARED-INSTANCE-RELATION-ISSUER-D0 and clear next_execution_card; keep target/effect/result/ABI/selected-C/package/Builder/MIR production cutover closed."
non_claims = "No Method(Some) emission, receiver ValueId, package co-seal, effect/result/ABI/selected-C/common affine consumer, Builder/MIR/backend/VM/JSON/Call schema change, delegate/nested/upvar activation, language change, fallback, retry, or old-edge deletion."

[lang_ordinary_declared_instance_call_effect_issuer_i0_2026_08_31]
status = "landed"
task_id = "LANG-ORDINARY-DECLARED-INSTANCE-CALL-EFFECT-ISSUER-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-EFFECT-ISSUER-D0"
implementation_permission = false
box_kind = "BoxCount parser-contract disposition carriage plus resolver semantic-effect facts"
decision = "Carry one parser-issued CallableContract disposition beside each final callable syntax row, then issue one resolver-owned DeclaredInstance call-effect disposition from the already-landed source relation. Missing contract means OpaqueObservable; explicit Query has precedence. This row remains facts-only and does not publish a target or physical call."
source_authority = "Existing parser ExplicitMethodSourceRelationV1.callable_contract carried through the final callable syntax loan, joined by exact CallableDeclarationIdentityV1 to the resolver-owned DeclaredInstanceCallSourceDispositionV1 relation."
canonical_issuer = "One new small resolver sibling invoked exactly once inside callable_semantic_batch/issuer.rs after the existing relation issuer, using the same final-source HRTB; parser transport only preserves the already-issued syntax disposition."
non_authority = [
  "AST reread, name/arity lookup, resolver/registry/header/MIR/backend inference",
  "EffectMask, physical signature, selected-C capability, package/loan, Method(Some), Callee, Call, fallback, retry",
  "Query body conformance or Home/ABI result reconstruction",
]
fail_fast = "Reject missing/foreign/duplicate/coverage-drifted parser contract carriage or a relation target without exactly one matching final syntax identity before any receiver ValueId, argument descent/effect, target, package, MIR, or backend publication."
allowed_files = [
  "src/parser/callable_contract_syntax.rs",
  "src/parser/initial_callable_program_source/model.rs",
  "src/parser/initial_callable_program_source/issue.rs",
  "src/parser/normal_callable_program_source/model.rs",
  "src/parser/normal_callable_program_source/transform.rs",
  "src/parser/normal_callable_program_source/semantic_syntax_loan.rs",
  "src/parser/normal_callable_program_source/mod.rs",
  "src/parser/mod.rs",
  "src/mir/resolved_semantics/declared_instance_call_effect.rs",
  "src/mir/resolved_semantics/mod.rs",
  "src/mir/callable_semantic_batch/issuer.rs",
  "src/mir/callable_semantic_batch/model.rs",
  "src/mir/callable_semantic_batch/mod.rs",
  "src/mir/callable_semantic_batch/declared_instance_call_effect_tests.rs",
  "src/parser/README.md",
  "src/mir/resolved_semantics/README.md",
  "src/mir/callable_semantic_batch/README.md",
  "docs/reference/language/callable-contracts.md",
  "docs/reference/language/quick-reference.md",
  "docs/reference/language/status-index.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/normal_callable_semantic_package/**",
  "src/mir/builder/**",
  "Method(Some), package/loan, selected-C, physical signature, result/Completion issuer",
  "MIR/Call schema, VM/backend/JSON/runtime, fallback/retry, guard registry/shell",
]
finite_states = [
  "NoRootDeclaredInstanceCall: no relation rows; no effect rows",
  "OpaqueObservable: exact target declaration has no CallableContract syntax",
  "DeclaredQuery: exact target declaration has CallableContract(query) and carries only rune ordinal",
  "CoverageMismatch: relation target cannot match exactly one parser syntax row; typed reject",
  "ForeignOrDuplicate: identity/provenance/contract cardinality drift; typed reject",
]
ordered_tasks = [
  "I0-A: add a private AST-free parser contract disposition and carry it through initial/final syntax products with exact cardinality checks",
  "I0-B: add one resolver effect issuer that maps relation target identity to OpaqueObservable or Query precedence without inferring from MIR/ABI",
  "I0-C: retain the effect disposition as a non-Clone batch sibling and add focused positive/negative/structural evidence",
  "I0-D: update parser/resolver/batch READMEs and close out to the existing Method/DeclaredInstance design stop; do not open production Method(Some)",
]
acceptance = [
  "Every final callable row carries exactly one parser contract disposition without AST, target, ValueId, key, or physical data",
  "No CallableContract maps to Opaque when the exact source declaration says Query; absent contract maps only to OpaqueObservable",
  "Relation target identity matches exactly one final syntax identity or the batch rejects before effects; no target/effect reconstruction occurs downstream",
  "VerifiedResolvedCallableSemanticBatchV1 owns one non-Clone effect disposition sibling; no package/Builder/MIR/backend switch is made",
  "Focused positive/negative tests, targeted rustfmt/check, pointer/active guard, structural effect guard, diff check, and touched Rust sources below 760 lines",
]
no_safe_slice = "Return to design_stop if the parser relation cannot carry the contract without AST reread, if effect must be inferred from EffectMask/ABI/body facts, if a second resolver or new semantic receipt family is required, or if the row reaches receiver/argument/target/physical publication."
focused_test_filter = "cargo test --profile quick --lib declared_instance_call_effect -- --test-threads=1"
secondary_test_filter = "cargo test --profile quick --lib callable_semantic_batch -- --test-threads=1"
changed_test_names = [
  "unannotated_declared_instance_target_is_opaque_observable",
  "query_contract_has_precedence_over_opaque_default",
  "static_current_owner_has_no_declared_instance_effect_rows",
  "wrong_declared_instance_arity_rejects_before_effect_issuer",
]
implementation_commit = "11d9dadada4cf533bdea91429afe4e67469e7a65"
implementation_evidence = "At 11d9dadada4cf533bdea91429afe4e67469e7a65, parser contract disposition is carried through the final syntax loan and the resolver batch issues one AST-free OpaqueObservable/DeclaredQuery sibling from the landed relation. The effect, relation, batch, parser-contract, and normal-callable focused filters passed 4/4, 4/4, 28/28, 13/13, and 38/38; quick library check, rustfmt, structural active-surface guard, pointer guard, and diff check passed. No target, receiver, package, MIR, or backend path changed."
structural_guard_command = "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py ."
red_classification = "No current-change red is present in this bounded row: the effect filter is 4 passed, the relation filter is 4 passed, the batch filter is 28 passed, the parser contract filter is 13 passed, and the normal callable source filter is 38 passed. Whole-lib baseline (7373 passed/139 failed/29 ignored in the audit) and selected-C 8MiB stack health remain separate CutoverBlockerOpen prerequisite lanes and are not silently folded into this row."
closeout = "Landed at 11d9dadada4cf533bdea91429afe4e67469e7a65. Restore CURRENT_STATE to MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0 design_stop with next_execution_card=none; keep receiver/full-lane/selected-C/package/loan/Method(Some)/MIR and Call-schema publication closed."
non_claims = "No Method(Some) emission, receiver ValueId, result/Completion reissuer, physical lane, selected-C admission, package/loan, Builder/MIR/backend/VM/JSON/Call schema change, language syntax change, fallback, retry, or old-edge deletion."

[mir_call_me_declared_instance_package_coseal_d0_2026_08_31]
status = "accepted_design_stop"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T1"
box_kind = "BoxCount source-backed package co-seal boundary"
implementation_permission = false
decision = "The source relation, result/Completion carriage, and resolver-owned OpaqueObservable/Query effect sibling are landed. The semantic package boundary co-seals those products with the full logical-to-physical lane; selected-C remains a separate downstream admission gate and never issues semantic target, result, effect, or lane meaning."
source_authority = "VerifiedDeclaredInstanceCallRelationV1, the retained DeclaredFunctionResultContractV1/VerifiedFunctionCompletionV1 cohort, the resolver-owned DeclaredInstanceCallSemanticEffectV1, and one existing source-backed PhysicalCallableLaneV1 owner under the same parser/resolver session."
canonical_issuer = "No production Method(Some) issuer exists yet. A future package-private co-seal owner may validate and transport already-issued relation/result/effect/physical products, but may not call declaration_for, re-resolve by name/arity, infer from MIR/ABI/backend data, or create a family-specific loan."
non_authority = [
  "package keys/brands, selected slots, current_static_box, variable_map, generated symbols, headers, MIR, EffectMask, FunctionSignature, backend/VM behavior, fixtures, fallback, and retry",
  "selected-C capability as a semantic issuer, receiver presence inferred from numeric ValueId 0, args[0] receiver repair, or any package-side declaration lookup",
]
fail_fast_boundary = "Missing, foreign, duplicate, ambiguous, unsupported, or coverage-drifted relation/result/effect/full-lane products reject before receiver ValueId materialization, argument descent/effects, Const/MIR publication, collector mutation, backend need-scan, or fallback. A later selected-C admission must independently reject the same product drift before C emission; it is not part of semantic issuance."
census_boundary = "landed source relation/result/effect products -> same-session full physical lane -> backend-neutral package locator -> separate selected-C admission view; includes only root lexical DeclaredInstance and excludes StaticCurrentOwner, nested/upvar me, Method(None), delegates, Dynamic/qualified/core calls, panic/throw/Result/?, VM/backend, and Call schema."
finite_inventory = [
  "resolver relation: mir_call_me_declared_instance_resolver_relation_i0_2026_08_31",
  "result/Completion: mir_normal_callable_result_contract_retention_d0_i0_2026_08_31",
  "effect: lang_ordinary_declared_instance_call_effect_issuer_i0_2026_08_31",
  "physical lane: existing PhysicalCallableLaneV1 source-backed owner; no package reconstruction",
  "selected-C: Boundary pure-first C physical consumer; source-backed admission capability is absent and must remain a separate downstream task",
]
finite_states = [
  "InputsIncomplete: any required product is absent; remain design_stop",
  "ForeignOrDrifted: provenance, owner, declaration, result, effect, lane, or selected-C identity differs; typed pre-effect reject",
  "SemanticReadyPhysicalBlocked: relation/result/effect/full lane are exact but selected-C admission is unavailable; keep the semantic locator design-only and ParkedSealed with an observable reopen trigger",
  "ReadyForSelectedAdmission: backend-neutral locator is exact and a separate selected-C capability can borrow it without semantic re-issuance",
  "ReadyForAtomicCutover: selected-C admission and all semantic products are co-sealed and one common affine disposition can be consumed once",
]
ordered_tasks = [
  "D0-A: name the existing source-backed full-lane owner and prove receiver presence is independent of numeric ValueId 0",
  "D0-B: define the separate selected-C admission contract and its stack/heap acceptance without giving it semantic authority; current existing capability count is zero",
  "D0-C: freeze one common non-Clone backend-neutral package locator and the missing/foreign/duplicate/coverage/residual rejection matrix",
  "I0: only after semantic locator plus selected-C admission both close, atomically consume the common disposition, materialize receiver once, lower arguments once, emit Method(Some(receiver)), and delete the selected legacy edges",
]
acceptance = "D0 closes only when exact relation, result/Completion, semantic effect, and full physical lane are named under one source session, the backend-neutral locator and its typed rejection matrix are fixed, and a separate selected-C admission handoff contract is explicit. This row issues no ValueId, Callee, Method(Some), MIR, backend code, fallback, or retry."
no_safe_slice = "Remain in design_stop if no existing source-backed full-lane/effect/selected-C owner can be named, if package lookup or backend inference is required, if a family-specific loan or second resolver is proposed, or if verification-health prerequisites are absent."
non_claims = "No production Method(Some) route, receiver materialization, package/loan implementation, selected-C stack workaround, Call schema change, Method(None) broad cleanup, nonselected backend parity, panic/throw/Result/? change, fallback, retry, or old-edge deletion."
allowed_files = [
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/reference/language/callable-contracts.md",
  "docs/reference/language/function-call-evaluation.md",
  "docs/reference/language/quick-reference.md",
  "docs/reference/language/field-visibility-and-delegation.md",
]
forbidden_files = [
  "src/mir/normal_callable_semantic_package/declared_instance_call.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "Call schema",
  "VM/backend/JSON/runtime",
]
verification_prerequisites = [
  "whole-lib failure-name baseline must be deterministic and compared before any broad Call schema cutover",
  "dev_gate quick must include a comparable cargo test --lib step",
  "selected-C 8MiB stack health is accepted at 46b2ee72e3; semantic admission remains a separate capability prerequisite",
]
worker_audit_head = "60d27c5869d924454430702cdc6a56a966845d78"
worker_audit_result = "Fresh read-only audits agree that PhysicalCallableLaneV1 and its source-backed signature cohort are the only reusable physical-lane vocabulary. The selected Boundary pure-first C route is a real consumer, but it is not yet an admission capability for this family: it reconstructs a target view from plan metadata, conflates missing receiver with numeric zero, strips duplicate args[0], and supports only a narrow one-lane result cell. No existing same-session selected-C capability co-seals receiver presence, full lane, result, and effect. The exact existing selected-C blocker is recorded separately by NY-LLVMC-SELECTED-LAUNCH-SNAPSHOT-STACK-RETIRE-R0; it is not waived here."
full_lane_decision = "PhysicalCallableLaneV1 / PhysicalCallableSignatureRowRefV1 is a named downstream product candidate. Package co-seal may borrow it only after exact declaration identity and result/effect coverage are checked; it may not reconstruct or widen the lane."
selected_c_decision = "Selected Boundary pure-first C remains the intended physical consumer, but its receiver-presence contract, full logical-to-physical lane coverage, and source-backed result/effect admission are absent. The separate 8MiB snapshot stack blocker is closed at 46b2ee72e3; selected-C semantic admission remains a CutoverBlockerOpen."
d0_status = "CutoverBlockerOpen__NoSafeSlice__selected_C_admission_missing"
existing_capability_inventory = "Exhausted: one formal lane cohort and one physical C consumer exist, but zero source-backed selected-C admission capabilities exist for root DeclaredInstance. The C consumer cannot be promoted by reading MIR JSON metadata or repairing args[0]."
common_disposition_shape = "A future common non-Clone package disposition may keep only private locators (caller/target batch slot plus exact relation/effect row ordinals); take must borrow the already-issued relation, effect, result/Completion, and physical signature together, and completion must reject residual rows. This shape is design-only until selected-C admission exists."
transport_blockers = [
  "production result/Completion borrow accessor is not yet exposed; cfg(test)-only access is insufficient for cutover",
  "the existing selected lowering port is definition-oriented and does not carry call-site relation/effect or selected-C capability",
  "normal_callable_semantic_loan_port.rs is at 748 lines and needs a behavior-preserving split before common callsite transport can be added",
]
selected_c_lane = "NY-LLVMC-SELECTED-LAUNCH-SNAPSHOT-STACK-RETIRE-R0 (separate backend health lane; status=CutoverBlockerClosed at 46b2ee72e3)"
selected_c_admission_task = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0"
selected_c_admission_status = "NoSafeSlice__existing_source_backed_capability_count_zero"
selected_c_admission_scope = "Define one versioned downstream admission that borrows the backend-neutral locator plus already-issued relation/result/Completion/effect/full-lane views, distinguishes receiver absence from ValueId 0, validates all lane/result/effect coverage before C need-scan, and emits an opaque admission token without re-issuing meaning. No production implementation is permitted until this contract has an existing owner and focused rejection matrix."
selected_c_admission_non_authority = [
  "MIR JSON target_symbol/target_arity/receiver_value",
  "FunctionSignature/body inference and fixed effect tags",
  "args[0] receiver repair or numeric-zero presence",
  "C registry/header/symbol lookup, VM/llvmlite/A-prime/pinned-Text routes",
]
selected_c_reopen_trigger = "A production non-Clone same-session API exists that accepts the backend-neutral locator, validates relation/result/Completion/effect/full lane including ExactText pairs and receiver presence independent of ValueId 0, then returns an opaque selected-C admission token consumed exactly once before C emission; missing/foreign/duplicate/coverage/residual reject pre-effect."
reopen_trigger = "Open implementation only when the same-session source relation, result/Completion, semantic effect, full lane, and selected-C capability are all exact, verification prerequisites are recorded, and one atomic common disposition can delete the selected legacy edges with fallback/retry zero."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-PRIVATE-LOCATOR-I0__pending_current_pointer_switch"

[mir_call_me_declared_instance_package_private_locator_i0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-PRIVATE-LOCATOR-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0"
ceremony_tier = "T1"
box_kind = "BoxCount backend-neutral semantic package locator co-seal"
implementation_permission = false
decision = "Consume no new meaning: issue one private non-Clone locator catalog by exact crosswalk of the already-issued DeclaredInstance relation, resolver-owned semantic effect, result/Completion cohort, selected identity mapping, and source-backed PhysicalCallableSignature row. Selected-C remains a separate downstream admission gate and is not required or implemented by this row."
source_authority = "VerifiedDeclaredInstanceCallRelationCatalogV1, VerifiedDeclaredInstanceCallEffectCatalogV1, VerifiedCallableResultContractCohortV1, VerifiedSelectedCallableBatchMapV1, and VerifiedCallablePhysicalSignatureCohortV1 from the same resolver/package session."
canonical_issuer = "One package-private locator issuer in src/mir/normal_callable_semantic_package/declared_instance_locator.rs, called once after the existing physical-signature issuer and before package publication; it only stores private row ordinals/slots and exact OwnedExprSiteV1 cross-references."
non_authority = [
  "name/arity lookup, package keys as semantic identity, target symbols, Callee, ValueId, AST/arguments, EffectMask, FunctionSignature/body inference, headers, C/JSON metadata, selected-C, backend/VM, fixtures, fallback, retry",
  "numeric ValueId(0) or args[0] as receiver presence, family-specific loan, or any package-side declaration_for/re-resolution",
]
fail_fast_boundary = "Missing/foreign/duplicate/ambiguous relation, effect, result/Completion, selected mapping, target lane, receiver-lane shape, or coverage drift rejects before receiver materialization, argument effects, Const/MIR publication, C need-scan, fallback, or retry. A locator is unpublished on any row failure."
census_boundary = "same-session resolver/package products -> private DeclaredInstance locator rows; includes root lexical DeclaredInstance me.method only and excludes StaticCurrentOwner, nested/upvar me, delegates, selected-C admission, receiver ValueId, Method(Some), common affine consumption, MIR/Call, backend, and schema changes."
finite_states = [
  "NoRootDeclaredInstanceCall: relation and effect are both explicit absence; publish an empty locator disposition",
  "Ready: every relation row has exactly one effect row, target selected slot, result/Completion row, and full instance physical signature",
  "MissingOrForeign: required product or same-session identity is absent; typed reject",
  "DuplicateOrAmbiguous: crosswalk has more than one candidate; typed reject",
  "CoverageDrift: row counts, source site, owner, declaration, role, receiver lane, or lane indices do not close; typed reject",
]
ordered_tasks = [
  "production-borrow result/Completion only if the existing private row cannot be validated without a test-only accessor; do not copy the contract",
  "issue the private non-Clone locator catalog in a new module and validate relation/effect/result/full-lane exact crosswalk",
  "add focused positive/negative/structural evidence; keep selected-C admission as a separate NoSafeSlice",
]
acceptance = "The package publishes either explicit no-root or a complete private locator catalog. Each row stores only exact site/slot/ordinal locator data; all existing products are borrowed by identity and session, no target/receiver/effect/ABI meaning is reissued, and all missing/foreign/duplicate/coverage/residual cases reject before effects. Touched Rust stays below 760/800 lines."
no_safe_slice = "Return to design_stop if relation/effect/result/Completion/full-lane products cannot be borrowed in production, if the package must look up by name/key/symbol, if the locator needs AST/ValueId/MIR/effect copies, or if selected-C admission must be created in this row."
non_claims = "No selected-C admission, physical token, receiver materialization, Method(Some), common direct-call loan, Builder/MIR/backend switch, Call-schema change, Method(None) cleanup, fallback, retry, or old-edge deletion."
allowed_files = [
  "src/mir/normal_callable_semantic_package/declared_instance_locator.rs",
  "src/mir/normal_callable_semantic_package/mod.rs",
  "src/mir/normal_callable_semantic_package/model.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/normal_callable_semantic_package/result_contract.rs",
  "src/mir/normal_callable_semantic_package/declared_instance_locator_tests.rs",
  "src/mir/normal_callable_semantic_package/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/method_call_handlers.rs",
  "lang/c-abi/shims/**",
  "MIR Call schema",
  "VM/backend/JSON/runtime",
]
focused_test_filters = [
  "declared_instance_locator",
  "normal_callable_semantic_package",
]
verification_prerequisites = [
  "cargo check --profile quick --lib",
  "focused locator tests must be nonzero and green",
  "pointer/active-surface guard and diff check",
  "whole-lib baseline remains separate and must not be claimed green",
]
implementation_base_commit = "b6529cc27b"
implementation_commit = "6f40203999"
changed_test_names = [
  "mir::normal_callable_semantic_package::declared_instance_locator_tests::package_retains_one_private_locator_for_exact_me_method_site",
  "mir::normal_callable_semantic_package::declared_instance_locator_tests::package_publishes_explicit_no_root_for_static_only_source",
  "mir::normal_callable_semantic_package::declared_instance_locator_tests::package_rejects_missing_declared_instance_target_before_locator_publication",
]
focused_test_runs = [
  "cargo test --profile quick --lib declared_instance_locator -- --test-threads=1: 3 passed",
  "normal_callable_semantic_package test binary: 65 passed",
  "cargo check --profile quick --lib: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
]
evidence = "The package issuer now invokes one private non-Clone locator crosswalk after physical-signature issuance. The locator accepts explicit no-root or a complete exact-site/slot/ordinal catalog; all three focused locator tests and the 65-test normal_callable_semantic_package suite pass. No target, receiver ValueId, selected-C token, Method(Some), MIR, backend, fallback, retry, or Call-schema path changed. Whole-lib health remains a separate baseline and is not claimed green."
red_classification = "No current-change red was observed in the focused locator or package suite. The repository's known whole-lib baseline and selected-C admission absence remain outside this row."
closeout = "After committing the bounded locator row, set work_mode=design_stop and select MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0. Keep selected-C implementation_permission=false until a source-backed admission owner exists."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0__design_stop"

[mir_call_me_declared_instance_locator_install_bridge_i0_2026_08_31]
status = "landed"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-LOCATOR-INSTALL-BRIDGE-I0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0"
depends_on = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-PRIVATE-LOCATOR-I0"
ceremony_tier = "T1"
box_kind = "BoxCount callback-scoped transport of an existing backend-neutral locator"
implementation_permission = false
implementation_commit = "same row closeout commit; see git history"
decision = "Retain the already-issued DeclaredInstance locator in InstalledNormalCallableSemanticPackageV1 and lend it only through a private callback-scoped Builder view. This is transport of an existing product, not a new semantic issuer."
source_authority = "The package-owned DeclaredInstance locator issuer landed at 6f40203999; install is the sole owner that currently drops it, and the Builder package port is the only downstream transport surface opened by this row."
canonical_issuer = "No new meaning issuer. InstalledNormalCallableSemanticPackageV1::commit must move the existing locator disposition into the installed package, and NormalCallableSemanticPackagePortV1 must expose only a bounded HRTB borrow/view."
non_authority = [
  "selected-C/C JSON metadata, target_symbol/target_arity, receiver ValueId, args[0], ABI or FunctionSignature inference, MIR/EffectMask, C registry/header lookup, name lookup, fallback, retry",
  "the Builder bridge does not issue or reclassify target/effect/result/physical-lane meaning and does not consume a selected-C token",
]
fail_fast_boundary = "The exact package locator disposition is moved intact at install; the callback-scoped view cannot escape or be independently cloned/split. No receiver materialization, argument effect, MIR/JSON publication, C need-scan, fallback, or retry is opened by this row."
census_boundary = "VerifiedNormalCallableSemanticPackageV1 locator field -> InstalledNormalCallableSemanticPackageV1 -> NormalCallableSemanticPackagePortV1 callback view; includes only locator retention/borrow and excludes selected-C admission, physical wire, receiver ValueId, Method(Some), common loan consumption, MIR, C/backend, and Call schema."
finite_states = [
  "NoRoot: explicit no-root disposition is borrowed without inventing a row",
  "Published: complete private locator catalog is borrowed from the installed package",
  "BorrowClosed: callback returns and no locator view escapes",
  "TransportReject: package/install lifecycle cannot expose a foreign or independently reconstructed locator",
]
acceptance = "Install retains the exact locator disposition; the package port offers one callback-scoped neutral view; focused tests prove both no-root and published rows survive install; no semantic or backend route changes occur."
no_safe_slice = "Return to the selected-C design stop if the bridge requires a C/JSON lookup, a new semantic receipt, target/effect/lane inference, a ValueId/args[0] repair, or a family-specific loan."
non_claims = "No selected-C admission issuer, token/wire, receiver materialization, Method(Some), common direct-call loan, MIR/Call schema change, C/LLVM/VM backend change, fallback, retry, or legacy deletion."
allowed_files = [
  "src/mir/normal_callable_semantic_package/declared_instance_locator.rs",
  "src/mir/normal_callable_semantic_package/mod.rs",
  "src/mir/normal_callable_semantic_package/model.rs",
  "src/mir/normal_callable_semantic_package/install.rs",
  "src/mir/normal_callable_semantic_package/declared_instance_locator_tests.rs",
  "src/mir/normal_callable_semantic_package/README.md",
  "src/mir/builder/README.md",
  "src/mir/builder/normal_callable_package_bridge.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/compiler/selected_c_admission.rs",
  "lang/c-abi/shims/**",
  "MIR Call schema",
  "VM/backend/JSON/runtime",
]
focused_test_filters = [
  "declared_instance_locator",
  "normal_callable_package_bridge",
]
verification_prerequisites = [
  "locator issuer row remains landed and non-Clone",
  "selected-C admission remains a separate design_stop with source-backed capability count zero",
  "whole-lib baseline is compared separately and is not replaced by focused green",
]
evidence = "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo check --profile quick --lib passed; cargo test --profile quick --lib declared_instance_locator ran 4 passed; cargo test --profile quick --lib normal_callable_package_bridge ran 3 passed; cargo test --profile quick --lib normal_callable_semantic_package ran 65 passed; active-surface guard, current-state pointer guard, and git diff --check passed. Whole-lib health is not claimed green."
red_classification = "No current-change red in the bridge/package focused suites. Existing whole-lib baseline remains outside this row and is not relabeled."
closeout = "The installed package now retains the exact locator disposition and lends it through a non-Clone callback-scoped view. Return to the selected-C admission design stop; no selected-C issuer or physical consumer was invented."
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0__design_stop"

[mir_call_me_declared_instance_selected_c_admission_d0_2026_08_31]
status = "accepted_design_stop"
task_id = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0"
parent_row = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-COSEAL-D0"
depends_on = "MIR-CALL-ME-DECLARED-INSTANCE-PACKAGE-PRIVATE-LOCATOR-I0"
ceremony_tier = "T1"
box_kind = "Downstream selected-C admission over an existing semantic locator"
implementation_permission = false
decision = "Keep selected-C as a downstream physical admission, separate from the semantic package locator. No production source-backed selected-C capability currently exists; the lane is NoSafeSlice rather than a reason to infer meaning from C/JSON metadata."
source_authority = "The landed package-private locator plus the same-session DeclaredInstance relation, result/Completion, semantic effect, selected mapping, and full physical-lane products; a future selected-C owner must borrow them without reissuing meaning."
canonical_issuer = "No existing production issuer. Reopen only when one same-session non-Clone admission owner validates the complete crosswalk and emits an opaque consume-once token before C emission."
non_authority = [
  "MIR/JSON target_symbol, target_arity, receiver_value, FunctionSignature/body inference, fixed effect tags, C registry/header/symbol lookup, VM/llvmlite/A-prime/pinned-Text routes",
  "ValueId(0) as receiver absence, args[0] receiver repair, name lookup, fallback, retry, or backend-side target/effect/lane reconstruction",
]
fail_fast_boundary = "Missing/foreign/duplicate/coverage/residual relation, result/Completion, effect, full-lane, receiver-presence, or selected capability must reject before receiver materialization, argument effects, MIR/JSON publication, C need-scan, fallback, or retry."
census_boundary = "The landed backend-neutral locator -> a future selected-C admission consumer; includes only the downstream admission contract and excludes semantic issuer changes, receiver ValueId, Method(Some), C emission, Call schema, and other backends."
finite_states = [
  "NoSafeSlice: source-backed selected-C capability count is zero",
  "DesignReady: one owner and a complete rejection matrix exist, but no production switch yet",
  "AdmissionReady: non-Clone opaque token validates the complete same-session crosswalk",
  "Consumed: exactly one C consumer takes the token before physical emission",
]
acceptance = "Remain design_stop until a real source-backed selected-C owner exists. Do not promote the current JSON consumer, args[0] repair, numeric-zero check, N+1 ABI, or fixed effect tag into authority."
no_safe_slice = "Any implementation request that needs a C/JSON lookup, target/effect/lane inference, receiver repair, a second resolver, fallback/retry, or semantic meaning in the physical consumer returns to this design stop."
non_claims = "No selected-C production implementation, receiver materialization, Method(Some), common direct-call loan, MIR/Call schema change, C/LLVM backend rewrite, VM change, or legacy deletion."
allowed_files = [
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/method_call_handlers.rs",
  "lang/c-abi/shims/**",
  "MIR Call schema",
  "VM/backend/JSON/runtime",
]
focused_test_filters = [
  "selected_c_admission",
]
verification_prerequisites = [
  "A production source-backed admission owner must exist before implementation permission is reconsidered",
  "The locator and all existing relation/result/Completion/effect/full-lane products remain exact and same-session",
  "Whole-lib baseline is compared separately and is not replaced by focused green",
]
next_execution = "MIR-CALL-ME-DECLARED-INSTANCE-SELECTED-C-ADMISSION-D0__design_stop"

[mir_builder_boxshape_port_surface_r0_2026_08_31]
status = "landed"
task_id = "MIR-BUILDER-BOXSHAPE-PORT-SURFACE-R0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-ME-DECLARED-INSTANCE-SOURCE-COSEAL-D0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-preserving private implementation extraction"
implementation_permission = false
implementation_commit = "f0413b3470"
decision = "Open one independent fast row only to move the unguarded recursive port helper blocks into a private child module. The normal callable RootCallableCapturePortV1 implementation remains in its parent because existing source guards anchor its exact parent-file surface. This creates no semantic shape and does not open the DeclaredInstance source/catalog/receiver/ABI co-seal. Parent module paths, visibility, signatures, fields, call order, guard-observed tokens, and runtime behavior remain unchanged."
source_authority = "Existing Rust module ownership, trait implementations, and current callers are the only authority. The split introduces no issuer, target, loan, receiver, ABI, effect, or backend meaning."
canonical_issuer = "None. This row is BoxShape-only; the parent DeclaredInstance row remains NoSafeSlice until one existing source-backed co-seal owner is named."
non_authority = [
  "module names as semantic ownership",
  "name/arity/header/symbol/registry lookup",
  "tests or fixtures as production authority",
  "new fields, enums, receipts, resolver, target, loan, fallback, retry, VM/backend, panic, or Call schema",
]
fail_fast_boundary = "Any changed behavior, module path, visibility, signature, field, call order, public re-export, guard-observed token, or source line budget stops the row before closeout."
census_boundary = "Existing implementation blocks in normal_callable_semantic_loan_port.rs and recursive_child_lowering.rs -> private child modules -> the same type/trait callers; includes only unguarded adapter/port helper blocks, excludes semantic issuer design, guarded implementation anchors, method/Call changes, and all backend/runtime routes."
finite_inventory = [
  "normal callable adapter RootCallableCapturePortV1 implementation -> retained in normal_callable_semantic_loan_port.rs because existing source guard anchors require the parent surface",
  "recursive raw-port pending/owner helper implementations with no current guard anchor -> recursive_child_lowering/pending_helpers.rs",
  "parent module declarations and existing private callers remain the same",
]
finite_states = [
  "SplitStable: child modules compile, parent paths/re-exports/visibility and behavior are unchanged",
  "GuardAnchorRetain: existing guard-observed RawInvocationChildPortV1 and adapter tokens remain in their parent files",
  "DriftDetected: any semantic or observable surface changes; return to design_stop",
  "BudgetExceeded: a touched/new Rust source reaches 760 lines or more (800 hard stop); return to design_stop",
]
ordered_tasks = [
  "R0-A: record the exact unguarded implementation blocks and existing parent guard anchors",
  "R0-B: retain the guard-anchored adapter RootCallableCapturePortV1 implementation in its parent and record why it is not moved",
  "R0-C: extract only unanchored recursive raw-port pending/owner helpers into a private sibling; leave all guard-anchored impls in the parent",
  "R0-D: run rustfmt/check, focused existing tests, pointer/line/diff checks, then close out to the parent design stop",
]
acceptance = "normal_callable_semantic_loan_port.rs remains below 760 lines and recursive_child_lowering.rs at 667 lines, both below the 800 hard stop; pending_helpers.rs is 98 lines; module paths, visibility, signatures, fields, public re-exports, guard-observed tokens, call order, test names, and runtime behavior are unchanged; quick library check and the two exact existing port tests pass; no semantic implementation claim is made."
no_safe_slice = "Stop and restore design_stop if moving a block requires changing an existing guard or source consumer, adding a public API/re-export, changing a signature/field/enum/call order, touching semantic/Call/VM/backend/panic code, creating a new issuer/receipt, or crossing the 760/800 line budget."
non_claims = "No DeclaredInstance co-seal, Method(Some) cutover, direct-call loan, target issuer, Method(None)/Resolved retirement, panic/throw/exit change, Call schema change, VM/backend/JSON change, fallback/retry change, or guard deletion/new registry row."
allowed_files = [
  "src/mir/builder/normal_callable_semantic_loan_port.rs",
  "src/mir/builder/recursive_child_lowering.rs",
  "src/mir/builder/recursive_child_lowering/pending_helpers.rs",
  "src/mir/builder/README.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/normal_callable_semantic_package/declared_instance_call.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/method_call_handlers.rs",
  "VM/backend/JSON/runtime/Call schema",
  "guard registry or guard deletion",
]
focused_test_filters = [
  "raw_callable_owner_witness_rejects_missing_or_foreign_owner",
  "recursive_child_lowering_rawport_tests::raw_invocation_nested_main_keeps_existing_root_only_terminal",
]
changed_test_names = [
  "mir::builder::normal_callable_semantic_loan_port::main_root::tests::raw_callable_owner_witness_rejects_missing_or_foreign_owner",
  "mir::builder::recursive_child_lowering_rawport_tests::raw_invocation_nested_main_keeps_existing_root_only_terminal",
]
focused_test_runs = [
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo check --profile quick --lib -j1: pass",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib raw_callable_owner_witness_rejects_missing_or_foreign_owner -- --test-threads=1: 1 passed",
  "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo test --profile quick --lib recursive_child_lowering_rawport_tests::raw_invocation_nested_main_keeps_existing_root_only_terminal -- --test-threads=1: 1 passed",
  "direct pre-existing broad normal_callable binary: 47 passed, 5 failed; the same 5 baseline failures reproduce on the pre-row binary and are outside this BoxShape split",
]
guard = "Use current_state_pointer_guard, git diff --check, exact line-budget and parent-token audits. Existing normal-callable/header-port guards remain unchanged; normal_callable_semantic_source_row_i0_guard.sh is a pre-row baseline red because its exact single-line anchor is absent at the parent commit, so it is not claimed as this row's evidence. The registered D1B lifecycle guard is not used as an independent BoxShape row and no new registry entry is added."
evidence = "The adapter RootCallableCapturePortV1 implementation remains in the parent to preserve existing guard-observed source anchors. Five unanchored RawInvocationChildPortV1 helper methods moved to private pending_helpers.rs; cargo check passed, both exact existing port tests passed, and all touched Rust files remain below the split boundary. The broad normal_callable filter was run against both the post-row and pre-row binaries: 47 passed and the same 5 known baseline failures reproduced, so no current-change red is attributed to this refactor."
red_classification = "normal_callable_semantic_source_row_i0_guard.sh remains a known parent-baseline red (its expected multi-token exact anchor was already absent at HEAD); canonical-trivial and complete-batch guards are context/registry mismatches for this independent row and are not altered. The broad normal_callable 5-test red is reproduced by the pre-row binary and is known baseline debt."
closeout = "Record implementation commit and focused evidence, then set status=landed, implementation_permission=false, work_mode=design_stop, current_execution_row/current_design_stop/next_design_card back to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0, and next_execution_card=none."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_cataloged_provider_bare_error_contract_d0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-ERROR-CONTRACT-D0"
parent_row = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
design_parent = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
ceremony_tier = "T0"
box_kind = "BoxCount one-name language-contract boundary"
implementation_permission = false
decision = "Use the existing canonical Call language contract: bare error is unsupported and rejects before argument evaluation. This D0 names the contract and its existing typed terminal; it does not invent a provider or change qualified Extern behavior."
source_authority = "docs/development/current/main/design/mir-canonical-callsite-lane-ssot.md final core decision, mirrored by docs/reference/language/function-call-evaluation.md; qualified env/nyash.console.error remains its own declared Extern owner."
canonical_issuer = "None. The existing PreparedRawOrdinaryFunctionCompletionV1::Rejected terminal is the only allowed terminal for this named unsupported cohort; no Callee issuer is created."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "name/arity, CanonicalGlobalTargetV1::new_free_function, current_static_box, catalog existence, resolver, symbol/registry, VM/backend, fixture, fallback, and retry",
  "qualified env/nyash/system error providers, which are separate Extern owners",
]
fail_fast_boundary = "After TypeOp/Math/FastMem precedence and before prepare_cataloged_target_v1, resolver, argument descent, Const/ValueId allocation, effects, block mutation, or Call publication. Bare error must enter the existing typed Rejected terminal at this boundary."
census_boundary = "Installed non-brand raw FunctionCall with bare name error -> first provider classification/terminal; includes both Cataloged caller families and observed arities, excludes bare now/panic/exit, qualified Extern, Math, GC, RawCompatibility, physical thunk, CorePlan, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "bare error ingress: src/mir/builder/calls/function_call_preflight_route.rs -> prepare_ordinary_function_completion_v1 -> prepare_cataloged_target_v1",
  "production Cataloged caller families: nonmain_static_box_method_batch.rs and instance_box_method_batch.rs",
  "existing terminal: PreparedRawOrdinaryFunctionCompletionV1::Rejected",
  "qualified error owners remain outside this cohort: env.console.error / declared Extern",
]
finite_states = [
  "UnsupportedBareError: exact language contract; typed pre-effect Rejected terminal",
  "QualifiedExternOwned: declared qualified providers remain unchanged",
  "WrongArity: represented by the same unsupported bare-name terminal until a distinct public contract is approved; no name/arity synthesis",
]
ordered_tasks = [
  "D0-A: reconcile bare error across both Cataloged caller families and its observed arity range in both directions",
  "D0-B: record the existing language/reference decision and distinguish bare error from qualified Extern providers",
  "D0-C: keep the existing Rejected terminal as the sole no-issuer terminal; do not add a provider, receipt, resolver, or fallback",
  "I0: move only the bare error branch to that terminal before target synthesis and argument effects, with focused positive/negative tests and a structural guard",
  "C0: rerun the one-name census and return to the bare-provider parent; now/panic/exit remain separate design rows",
]
acceptance = "The bare error name and both Cataloged caller families are finite and reconciled; the language/reference contract is explicit; every bare error path reaches one existing typed pre-effect Rejected terminal; qualified Extern behavior is unchanged; and no implementation is opened until this D0 is accepted."
no_safe_slice = "Remain in design_stop if the existing Rejected terminal cannot be reached before argument effects, if bare error needs a new provider/receipt, if qualified Extern behavior would change, if arity/effect/diagnostic semantics are ambiguous, or if the row crosses another provider, physical thunk, package/loan, VM/backend, JSON, Call schema, fallback, retry, or 760/800-line boundary."
non_claims = "No bare now/panic/exit change, no provider/Extern issuer, no Math/GC/ordinary-static/CorePlan/physical-thunk/ordinary-new work, no package/loan, VM/backend/JSON, Call schema, fallback, retry, or guard cleanup."
evidence = "The existing canonical Call SSOT already states bare error rejects; the fresh worker audits at HEAD 220a146ae802 found no bare issuer and the existing Rejected terminal is available. This row records that contract without claiming a code switch."
reopen_trigger = "Reopen the parent if the language contract changes, the existing typed terminal cannot be placed before effects, or a same-session exact bare-error issuer is introduced by an explicit provider decision."
next_execution = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-ERROR-RETIRE-I0"

[same_module_cataloged_provider_bare_error_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-ERROR-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-ERROR-CONTRACT-D0"
design_parent = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-ERROR-CONTRACT-D0"
ceremony_tier = "T0"
box_kind = "BoxShape one-name pre-effect retirement"
implementation_permission = false
decision = "Retire only bare error at the existing typed Rejected terminal before target synthesis and argument effects. Do not invent a bare provider, change qualified Extern, or touch now/panic/exit."
source_authority = "The accepted language contract in docs/reference/language/function-call-evaluation.md and mir-canonical-callsite-lane-ssot.md; the parser remains a generic FunctionCall source and issues no bare provider."
canonical_issuer = "None. PreparedRawOrdinaryFunctionCompletionV1::Rejected is the sole terminal; no Callee or new semantic receipt is issued."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "name/arity, CanonicalGlobalTargetV1::new_free_function, current_static_box, catalog lookup, resolver, symbol/registry, VM/backend, fixture, fallback, and retry",
  "qualified env/nyash/system error providers, which remain separate Extern owners",
]
fail_fast_boundary = "After TypeOp/Math/FastMem precedence and before prepare_cataloged_target_v1, resolver, argument descent, Const/ValueId allocation, effects, block mutation, or Call publication."
census_boundary = "Installed non-brand raw FunctionCall with bare name error -> first provider classification/terminal; includes both Cataloged caller families and observed arities; excludes bare now/panic/exit, qualified Extern, Math, GC, RawCompatibility, physical thunk, CorePlan, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "bare error ingress: src/mir/builder/calls/function_call_preflight_route.rs::prepare_ordinary_function_completion_v1",
  "existing terminal: PreparedRawOrdinaryFunctionCompletionV1::Rejected",
  "focused owner tests: function_call_installed_gc_builtin_tests.rs",
]
finite_states = [
  "UnsupportedBareError: one typed pre-effect Rejected terminal",
  "QualifiedExternOwned: qualified providers unchanged",
]
ordered_tasks = [
  "I0-A: add the finite bare-error branch before caller target preparation",
  "I0-B: prove static and instance Cataloged caller forms reject without child descent or Call publication",
  "I0-C: run filtered Cargo tests, active-surface guard, pointer guard, and changed-test coverage",
  "C0: close the one-name row and return to the bare-provider parent; now/panic/exit stay separate",
]
acceptance = "Both Cataloged caller families and every observed bare-error arity reach the same existing Rejected terminal before target synthesis and argument effects; focused tests prove zero child/effect/Call publication; the guard proves the finite path and changed-test coverage; qualified Extern and other provider families are unchanged."
no_safe_slice = "Return to design_stop if the existing Rejected terminal cannot be placed before effects, a new provider/receipt is needed, qualified Extern changes, arity/effect/diagnostic semantics are ambiguous, or the row crosses another provider, physical thunk, package/loan, VM/backend, JSON, Call schema, fallback, retry, or 760/800-line boundary."
non_claims = "No bare now/panic/exit change, no provider/Extern issuer, no Math/GC/ordinary-static/CorePlan/physical-thunk/ordinary-new work, no package/loan, VM/backend/JSON, Call schema, fallback, retry, or guard cleanup."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_bare_error_retire_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
]
forbidden_files = [
  "bare now/panic/exit",
  "qualified Extern, Math, GC, physical thunk, CorePlan, ordinary-static, ordinary-new",
  "package/loan, VM/backend, JSON, Call schema, fallback, retry, guard cleanup",
]
focused_test_filters = ["bare_error"]
changed_test_names = [
  "cataloged_bare_error_rejects_before_target_synthesis",
  "cataloged_bare_error_rejection_does_not_descend_or_publish",
]
coverage_base_commit = "8a9546db2a"
implementation_commit = "69680b983f"
focused_test_runs = [
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --quiet --profile quick --lib bare_error -- --test-threads=1: 2 passed, 0 failed",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "rustfmt --edition 2021 --check on touched Rust files: pass",
  "git diff --check: pass",
]
guard = "The existing active-surface lifecycle guard dispatches a dedicated bare-error checker; fast_open validates only activation scope, while landed validates branch ordering, focused tests, changed-test coverage, and allowed paths."
evidence = "Implementation landed at 69680b983f. Installed non-brand bare error now enters PreparedRawOrdinaryFunctionCompletionV1::Rejected before prepare_cataloged_target_v1 and argument descent; static and instance Cataloged caller forms are covered by two focused tests, with 2/2 passed and no child events or MirInstruction::Call."
reopen_trigger = "Reopen the parent if the language contract changes, the existing terminal cannot precede effects, or an explicit same-session bare-error issuer is introduced."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_cataloged_provider_bare_now_contract_d0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NOW-CONTRACT-D0"
parent_row = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
ceremony_tier = "T0"
box_kind = "BoxCount one-name language-contract boundary"
implementation_permission = false
decision = "Use the existing canonical Call language contract: bare now is unsupported and rejects before argument evaluation. This D0 reconciles the stale NoIssuer wording; it does not invent a provider or change qualified env.now_ms behavior."
source_authority = "docs/development/current/main/design/mir-canonical-callsite-lane-ssot.md final core decision; the parser remains a generic FunctionCall source and issues no bare provider. Qualified env.now_ms remains its own declared Extern owner."
canonical_issuer = "None. PreparedRawOrdinaryFunctionCompletionV1::Rejected is the only allowed terminal for this named unsupported cohort; no Callee or semantic receipt is created."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "name/arity, CanonicalGlobalTargetV1::new_free_function, current_static_box, catalog lookup, resolver, symbol/registry, VM/backend, fixture, fallback, and retry",
  "qualified env.now_ms, which remains a separate Extern owner",
]
fail_fast_boundary = "After TypeOp/Math/FastMem precedence and before prepare_cataloged_target_v1, resolver, argument descent, Const/ValueId allocation, effects, block mutation, or Call publication."
census_boundary = "Installed non-brand Cataloged raw FunctionCall with bare name now -> first provider classification/terminal; includes both Cataloged caller families and observed arities; excludes qualified Extern, Math, GC, other bare names, RawCompatibility, physical thunk, CorePlan, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "bare now ingress: src/mir/builder/calls/function_call_preflight_route.rs::prepare_ordinary_function_completion_v1",
  "production Cataloged caller families: nonmain_static_box_method_batch.rs and instance_box_method_batch.rs",
  "existing terminal: PreparedRawOrdinaryFunctionCompletionV1::Rejected",
]
acceptance = "The bare now contract and both Cataloged caller families are finite and reconciled; qualified env.now_ms is unchanged; the existing typed Rejected terminal is named before any target or argument effect; and no provider/issuer is introduced."
no_safe_slice = "Return to design_stop if bare now requires a provider, a new receipt, a resolver/name lookup, a qualified Extern change, an ambiguous public arity/effect contract, or any other family/backend/schema/fallback/retry change."
non_claims = "No bare panic/exit/error change, no provider/Extern issuer, no Math/GC/ordinary-static/CorePlan/physical-thunk/ordinary-new work, no package/loan, VM/backend/JSON, Call schema, fallback, retry, or guard cleanup."
evidence = "The canonical Call SSOT already states bare now rejects; the worker audit at HEAD 22e3aff4f5 identified the manifest NoIssuer wording as the only blocker to reusing the existing unsupported terminal."
reopen_trigger = "Reopen if the canonical language contract changes, the existing typed terminal cannot precede effects, or an explicit same-session bare-now issuer is introduced."
next_execution = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NOW-RETIRE-I0"

[same_module_cataloged_provider_bare_now_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NOW-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NOW-CONTRACT-D0"
design_parent = "MIR-CALL-SAME-MODULE-CATALOGED-PROVIDER-BARE-NO-ISSUER-D0"
ceremony_tier = "T0"
box_kind = "BoxShape one-name pre-effect retirement"
implementation_permission = false
decision = "Retire only InstalledNonBrand bare now at the existing typed Rejected terminal before target synthesis and argument effects. Keep panic delegated to its parked terminal-Fault target, bare exit NoIssuer, and qualified env.now_ms unchanged."
source_authority = "The accepted canonical language contract in mir-canonical-callsite-lane-ssot.md; parser emits no bare provider."
canonical_issuer = "None. PreparedRawOrdinaryFunctionCompletionV1::Rejected is the sole terminal; no Callee or new semantic receipt is issued."
non_authority = [
  "CallNameCalleeClassV1::BuiltinGlobal",
  "name/arity, CanonicalGlobalTargetV1::new_free_function, current_static_box, catalog lookup, resolver, symbol/registry, VM/backend, fixture, fallback, and retry",
  "qualified env.now_ms, which remains a separate Extern owner",
]
fail_fast_boundary = "After TypeOp/Math/FastMem precedence and before prepare_cataloged_target_v1, resolver, argument descent, Const/ValueId allocation, effects, block mutation, or Call publication."
census_boundary = "Installed non-brand raw FunctionCall with bare name now -> first provider classification/terminal; includes both Cataloged caller families and observed arities; excludes bare error/panic/exit, qualified Extern, Math, GC, RawCompatibility, physical thunk, CorePlan, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "bare now ingress: src/mir/builder/calls/function_call_preflight_route.rs::prepare_ordinary_function_completion_v1",
  "existing terminal: PreparedRawOrdinaryFunctionCompletionV1::Rejected",
  "focused owner tests: function_call_installed_gc_builtin_tests.rs",
]
ordered_tasks = [
  "I0-A: add the finite bare-now branch before caller target preparation",
  "I0-B: prove static and instance Cataloged caller forms reject without child descent or Call publication",
  "I0-C: run filtered Cargo tests, active-surface guard, pointer guard, and changed-test coverage",
  "C0: close the one-name row and return to the bare-provider parent; panic/exit remain separate",
]
acceptance = "Both Cataloged caller families and every observed bare-now arity reach one existing Rejected terminal before target synthesis and argument effects; focused tests prove zero child/effect/Call publication; qualified env.now_ms and other provider families are unchanged; and the guard proves the finite path and changed-test coverage."
no_safe_slice = "Return to design_stop if the existing Rejected terminal cannot precede effects, a provider/receipt is needed, qualified Extern changes, arity/effect/diagnostic semantics are ambiguous, or the row crosses another provider, physical thunk, package/loan, VM/backend, JSON, Call schema, fallback, retry, or 760/800-line boundary."
non_claims = "No bare panic/exit/error change, no provider/Extern issuer, no Math/GC/ordinary-static/CorePlan/physical-thunk/ordinary-new work, no package/loan, VM/backend/JSON, Call schema, fallback, retry, or guard cleanup."
allowed_files = [
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
  "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_bare_error_retire_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/reference/language/function-call-evaluation.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
]
forbidden_files = [
  "bare panic/exit/error, qualified Extern, Math, GC, physical thunk, CorePlan, ordinary-static, ordinary-new",
  "package/loan, VM/backend, JSON, Call schema, fallback, retry, guard cleanup",
]
focused_test_filters = ["bare_now"]
changed_test_names = [
  "cataloged_bare_now_rejects_before_target_synthesis",
  "cataloged_bare_now_rejection_does_not_descend_or_publish",
]
coverage_base_commit = "d5d9674d69"
implementation_commit = "3e35e4f39c"
focused_test_runs = [
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --quiet --profile quick --lib bare_now -- --test-threads=1: 2 passed, 0 failed",
  "python3 tools/checks/lib/mir_call_d1b_bare_error_retire_guard.py (bare-now landed override with cargo --list coverage): pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "rustfmt --edition 2021 --check on touched Rust files: pass",
  "git diff --check: pass",
]
guard = "The existing active-surface lifecycle guard dispatches the shared bare-provider checker for the named bare-now row; fast_open validates only activation scope, while landed validates branch ordering, focused tests, changed-test coverage, and allowed paths."
evidence = "Implementation landed at 3e35e4f39c. Installed non-brand bare now enters PreparedRawOrdinaryFunctionCompletionV1::Rejected before prepare_cataloged_target_v1 and argument descent; static and instance Cataloged caller forms are covered by two focused tests, with 2/2 passed and no child events or MirInstruction::Call."
reopen_trigger = "Reopen the parent if the language contract changes, the existing terminal cannot precede effects, or an explicit same-session bare-now issuer is introduced."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_cataloged_gc_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-CATALOGED-GC-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
implementation_permission = false
implementation_commit = "9e166e5974"
coverage_base_commit = "eb6af37e628fe3f4af735dfed4e93971a72fdde1"
source_authority = "The existing installed-nonbrand GC retirement predicate is the named policy owner; no target issuer is needed."
canonical_issuer = "None: gc_collect/gc_stats remain unissued and terminate before argument descent."
non_authority = [
  "cataloged caller presence, name/arity, BuiltinGlobal classification, new_free_function, CalleeResolverBox, registry, symbol, env, VM/backend, fallback, and retry",
]
fail_fast_boundary = "At Cataloged ordinary preflight, after TypeOp/Math/FastMem precedence and before caller target preparation, child descent, Const/ValueId allocation, block mutation, or Call publication."
census_boundary = "InstalledNonBrand raw FunctionCall names gc_collect/gc_stats -> prepare_ordinary_function_completion_v1 -> pre-effect typed Retired terminal; includes caller=None and caller=Some cataloged paths plus focused tests, excludes Math, Extern, bare error/now/panic/exit, wire/JSON, VM/backend, and Call schema."
decision = "Extend the existing gc-global-retired terminal to caller-present Cataloged calls by moving the predicate before the caller branch. This removes the only caller-present path that synthesized SameModule FreeFunction for an unissued GC provider."
old_edge_delete_set = [
  "InstalledNonBrand caller-present gc_collect/gc_stats -> BuiltinGlobal -> SameModule FreeFunction(name, arity)",
  "caller-present GC -> CalleeResolverBox -> CatalogedTargeted",
]
allowed_files = [
    "src/mir/builder/calls/function_call_preflight_route.rs",
    "src/mir/builder/calls/function_call_preflight_route_tests.rs",
    "src/mir/builder/calls/function_call_installed_gc_builtin_tests.rs",
    "src/mir/builder/calls/build.rs",
    "src/mir/builder/calls/README.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "ordinary static terminal",
  "generic CorePlan",
  "operators",
  "physical thunk",
  "rewrite/known",
  "ordinary-new birth",
  "panic/exit/error/now issuer",
  "Math/Extern",
  "package/loan",
  "VM/backend/JSON/Call schema",
]
focused_test_names = [
  "installed_gc_names_reject_before_arguments",
  "cataloged_gc_names_reject_before_target_synthesis",
  "installed_gc_rejection_does_not_descend_or_publish",
]
focused_test_filters = ["installed_gc_builtin_tests"]
changed_test_names = ["cataloged_gc_names_reject_before_target_synthesis"]
implementation_evidence = "Caller-present gc_collect/gc_stats now select RawOrdinaryFunctionRetirementV1::GcGlobal before caller target preparation; the existing caller-less path uses the same typed terminal. The focused installed_gc_builtin_tests suite ran 4 tests with 4 passed, and cargo check --profile quick --lib passed."
focused_test_command = "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo test --profile quick --lib installed_gc_builtin_tests:: -- --test-threads=1"
focused_test_result = "4 passed; 0 failed; 7502 filtered out"
cargo_check_command = "CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 CARGO_PROFILE_QUICK_CODEGEN_UNITS=1 CARGO_PROFILE_QUICK_OPT_LEVEL=0 cargo check --profile quick --lib"
cargo_check_result = "passed; existing warnings only"
full_suite_note = "Full cargo test was not run; this row is evidenced by the exact focused suite, quick library check, pointer guard, active-surface guard, and diff check."
acceptance = "Both caller=None and caller=Some cataloged GC calls reach one typed retirement variant before target preparation and child descent; no SameModule FreeFunction is synthesized for gc_collect/gc_stats; existing Math/Extern/ordinary routes remain unchanged; focused tests and the reusable active-surface guard are green."
no_safe_slice = "Return to design_stop if caller-present GC has a public success contract, the predicate cannot be evaluated before caller target preparation, a new GC issuer is required, or the change reaches Math/Extern, VM/backend, wire, package/loan, or Call schema."
next_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design stop; select the next exact SameModule family only after a fresh authority audit."

[docs]
durable_policy = "docs/development/current/main/design/mir-canonical-callsite-lane-ssot.md"
active_pointer = "docs/development/current/main/CURRENT_STATE.toml"
active_card = "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md"
prior_writer_manifest = "docs/development/current/main/investigations/mir-call-core-r6-d1-manifest-2026-08-25.toml"
normal_root_manifest = "ParkedSealed: docs/development/current/main/investigations/normal-root-execution-atomic-cutover-manifest-d0-2026-08-23.md; do not copy or slim in this row"
docs_slim_followup = "Move closed R1-R5/D1A/D1-D8 prose out of the 2970-line Call SSOT only after D1B acceptance; preserve this manifest as the finite D1B ledger"

[same_module_physical_thunk_issuer_closure_d0_2026_08_30]
status = "parked_sealed"
task_id = "MIR-CALL-SAME-MODULE-PHYSICAL-THUNK-ISSUER-CLOSURE-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape finite issuer-availability audit"
implementation_permission = false
disposition = "ParkedSealed"
owner = "normal_module_transaction physical-thunk source/entry transaction"
decision = "The audit is closed as RelationPresentIssuerMissing and ParkedSealed. Do not implement or alter the physical thunk until an existing same-session source-backed product issues the exact main/0 target, or an explicit typed retirement/replacement contract is accepted. Never synthesize a target from a symbol, name, arity, or header lookup."
source_authority = "VerifiedNormalMainPhysicalRelationV1 is the existing source-owner/completion/physical-entry relation. It is evidence for the audit, not by itself a Callee issuer."
canonical_issuer = "No exact main/0 issuer is identified at HEAD. CallableIndex FreeStatic issuance explicitly excludes main; CanonicalNormalMainEntryTargetV1 owns physical-entry identity only."
non_authority = [
  "source.symbol().as_mir_name(), physical symbols, name/arity, header lookup, CanonicalNormalMainEntryTargetV1, typed_global_target_from_selected_symbol, fixtures, backend tables, resolver replay, fallback, and retry",
]
fail_fast_boundary = "Verify source/physical arity, semantic owner, completion profile, physical-entry relation, and exact target availability before MIR draft mutation, Call publication, Const/ValueId allocation, collector effects, or any retry/fallback."
census_boundary = "callable_main_physical.rs and main_transaction.rs production physical-thunk callers -> physical_thunk.rs target-construction terminal; includes the two production callers, one test-only helper, existing VerifiedNormalMainPhysicalRelationV1/header/entry witnesses, and current symbol parse-back; excludes ordinary calls, Method(None), CorePlan, operators, rewrite/known, ordinary-new, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "production caller: src/mir/builder/normal_module_transaction/callable_main_physical.rs:193-197",
  "production caller: src/mir/builder/normal_module_transaction/main_transaction.rs:182-186",
  "test-only helper: src/mir/builder/normal_module_transaction/main_transaction.rs:529-534",
  "sole target-construction terminal: src/mir/builder/normal_module_transaction/physical_thunk.rs:80-90",
]
finite_states = [
  "ExactIssuerPresent: an existing same-session source-backed product issues main/0 exactly once; eligible for a later bounded I0",
  "RelationPresentIssuerMissing: source/physical relation exists but no exact Callee issuer exists; choose typed retirement or ParkedSealed, no implementation",
  "RelationMissing: source/physical owner, completion, or entry relation is absent or mismatched; typed pre-effect reject",
  "ForeignOrAmbiguous: owner, session, arity, entry, or target identity is non-unique or foreign; typed pre-effect reject",
]
ordered_tasks = [
  "D0-A: reconcile both production callers and the test-only helper against the sole physical_thunk terminal",
  "D0-B: verify whether an existing source-backed main/0 issuer is present in the same compile transaction; do not add a new semantic receipt",
  "D0-C: record ExactIssuerPresent, RelationPresentIssuerMissing, RelationMissing, and ForeignOrAmbiguous with one owner and terminal each",
  "D0-D: if issuer is absent, select typed pre-effect retirement or ParkedSealed and record an observable reopen trigger",
  "I0: open only after D0 names an issuer; consume the exact handoff before Call/Const effects and delete symbol parse-back atomically",
]
acceptance = "The two production callers and one test helper are two-directionally reconciled; the existing relation, missing issuer, and physical terminal are separately named; no symbol/name/arity synthesis is accepted; and the child is ParkedSealed with an observable issuer-or-retirement reopen trigger."
no_safe_slice = "Remain in design_stop if main/0 requires a new semantic issuer without an existing source-backed product, if the relation cannot be verified in the same session, if target identity still needs symbol/name/arity/header inference, or if the change crosses package/loan, VM/backend, Call schema, or the 760/800-line boundary."
non_claims = "No physical thunk implementation, no ordinary Call cutover, no Method(Some)/Method(None) change, no CorePlan/operator/rewrite/provider/new-birth work, no package/loan, no VM/backend/JSON, and no Call-schema change."
evidence = "Read-only audit at HEAD 1ba49eb4fd confirms that both production callers and the single test helper reconcile to physical_thunk.rs:80-90. VerifiedNormalMainPhysicalRelationV1 seals source owner/completion/physical entry, but CallableIndex excludes main from FreeStatic and CanonicalNormalMainEntryTargetV1 carries physical-entry identity only; no exact same-session Callee issuer exists."
reopen_trigger = "A same-session source-backed main/0 Callee product/issuer is introduced by an accepted language/product decision, or the physical-thunk route receives an explicit typed retirement/replacement contract."
next_execution = "parked_no_issuer__next_selected_exact_coreplan_reachability_d0"

[same_module_selected_exact_coreplan_reachability_d0_2026_08_30]
status = "parked_sealed"
task_id = "MIR-CALL-SAME-MODULE-SELECTED-EXACT-COREPLAN-REACHABILITY-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape finite reachability census"
implementation_permission = false
disposition = "ParkedSealed"
owner = "control_flow/plan located-loop claim composer and claim consumer"
decision = "Do not wire the selected-exact CorePlan successor yet. First reconcile whether its existing source/site/brand/target claim chain has any production caller. A caller-zero result is ParkedSealed; a live caller may be recognized only through the existing exact claim chain, never through raw func text or a new bridge."
source_authority = "Existing VerifiedSourceStaticCallTargetCatalogV1, VerifiedStaticCallResultPublicationOwnerV1, exact (caller, SourceExprSiteV1) claim, and CorePlanEffectEmissionPortV1::Claimed are the only candidate source-backed products."
canonical_issuer = "No new issuer in D0. The existing canonical key/target projection and claimed emission product may be reused only if a production caller consumes them in the same session."
non_authority = [
  "CoreEffectPlan::GlobalCall.func, CoreCallSourceV1::Unlocated, normalizer-generated strings, name/arity, current_static_box, emission fallback, fixtures, activation re-observation, resolver replay, fallback, and retry",
]
fail_fast_boundary = "Before production wiring, reconcile brand, exact source site, claim ownership, result/effect shape, and residual claim count. Missing, duplicate, foreign, or caller-zero claims remain typed ParkedSealed/blocked and never fall through to raw lowering."
census_boundary = "selected-exact CorePlan source target inventory -> existing publication owner claim -> CorePlanEffectEmissionPortV1::Claimed consumer; includes the finite located composer, claim consumer, production caller search, and value/statement shape evidence; excludes generic Unlocated CorePlan GlobalCall, physical thunk, operators, rewrite/known, ordinary-new, providers, Method(None), package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "source/static target inventory and publication owner claim chain",
  "selected-exact composer: src/mir/builder/control_flow/plan/features/generic_loop_located_composer.rs",
  "claim consumer: src/mir/builder/control_flow/plan/lowerer/emission_port.rs and located_loop.rs",
  "production loop entry/composer callers, including raw_loop_child_entry.rs and normal_callable_loop_physical_adapter.rs",
]
finite_states = [
  "ProductionCallerPresent: an existing source/site/brand/target claim is consumed in the same production session; eligible for a later bounded implementation row",
  "CallerZero: the selected-exact composer or claim consumer is test-only/disconnected; ParkedSealed with a production-caller reopen trigger",
  "ClaimMissingOrResidual: source claim is absent, duplicated, foreign, or not fully consumed; typed blocker, no bridge",
  "ShapeUnsupported: statement/value/result shape is outside the existing exact product; remain blocker-open",
]
ordered_tasks = [
  "D0-A: reconcile every selected-exact composer and claim consumer in both directions; record exact file/line callers",
  "D0-B: distinguish production caller from test/activation-only reachability; do not treat a fixture or re-observation as a caller",
  "D0-C: verify exact source/site/brand/target claim, value/statement result shape, and residual/duplicate behavior",
  "D0-D: classify the family as ProductionCallerPresent, CallerZero ParkedSealed, ClaimMissingOrResidual, or ShapeUnsupported",
  "I0: open only if a live caller and existing claim chain are proven; do not add a production bridge in this D0",
]
acceptance = "The selected-exact CorePlan boundary is finite and two-directionally reconciled; every caller/consumer has one state and owner; caller-zero is explicitly ParkedSealed; no raw func/name/arity inference or production bridge was added; and any later I0 has a named existing claim product and first-effect boundary."
no_safe_slice = "Remain in design_stop if the selected-exact chain has no production caller, if activation requires a second source observation, if statement/value effects need a new result authority, if raw func text must be parsed, or if the work crosses generic CorePlan, package/loan, VM/backend, Call schema, or the 760/800-line boundary."
non_claims = "No generic CorePlan implementation, no production bridge, no source rescan, no new issuer/receipt, no physical thunk change, no Method(None)/Resolved change, no operator/rewrite/provider/new-birth work, no package/loan, VM/backend, JSON, or Call-schema change."
evidence = "At HEAD ce24ad5e1346, compose_located_generic_loop_v1 and CorePlanEffectEmissionPortV1::Claimed are referenced only by focused test modules; the production loop entry does not call the located composer. The existing source/site/brand/target claim chain is structurally exact but disconnected from production."
reopen_trigger = "A real production caller consumes the existing located composer and claim chain in the same session, with exact source/site/brand/target claims and a declared value/statement result contract."
next_execution = "parked_caller_zero__next_generic_coreplan_globalcall_d0"

[same_module_generic_coreplan_globalcall_d0_2026_08_30]
status = "parked_sealed_m10b_successor_required"
task_id = "MIR-CALL-SAME-MODULE-GENERIC-COREPLAN-GLOBALCALL-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxCount finite generic CorePlan authority census"
implementation_permission = false
disposition = "ParkedSealed"
decision = "Preserve loop/while language semantics and keep the current legacy registry scheduler unchanged until the existing portable Loop M10b cutover owns the accepted corpus. Retire the internal registry V0/V1/debt route only with that atomic switch; retain the separate source-backed canonical V1 semantic path. The selected exact emission product is a caller-zero sibling and may not be wired as a new bridge."
source_authority = "Until M10b, the production-live legacy registry route remains the temporary execution authority for its accepted corpus. The final authority is the resolver-bound Generic G0 source product co-sealed into the portable Recipe/JoinSig and consumed by the canonical physicalizer. The existing source-backed canonical V1 path owns a bounded callable cohort but does not prove coverage for the whole legacy registry corpus."
canonical_issuer = "No exact issuer exists for this SameModule Call edge. Its named successor is the portable Loop M10b production owner after M8/M9/precutover coverage; CanonicalSameModuleCallableKeyV1 and CorePlanEffectEmissionPortV1::Claimed remain disconnected/selected siblings and cannot be reissued from func text."
non_authority = [
  "CoreEffectPlan::GlobalCall.func, CoreCallSourceV1::Unlocated, name/arity, current_static_box, registry, physical symbol, typed_global_target_from_selected_symbol, normalizer strings, fallback, retry, and a second resolver",
]
fail_fast_boundary = "M10b may open only when every accepted legacy fixture has one portable owner or an approved typed reject before effects. The current lowerer sees ValueId/local_arg effects before parsing func:String, so no Call-lane post-effect terminal, fallthrough, retry, or shortcut deletion is permitted."
census_boundary = "legacy registry CorePlan GlobalCall writers -> LoopRouteId::GenericLoopV0/V1 ordered scheduler -> registry handler/composer/lowerer -> Call publication; includes the separate source-backed canonical V1 bypass, selected exact sibling reachability, local_arg/parse-back ordering, and the 389-row accepted-corpus manifest; excludes ordinary-static, rewrite/known, ordinary-new, provider, physical thunk, Method, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "writers: src/mir/builder/control_flow/plan/normalizer/common.rs:70-75,82-87",
  "writers: src/mir/builder/control_flow/plan/loop_body_lowering_associated_input.rs:214-219,323-328",
  "writers: src/mir/builder/control_flow/plan/helpers_value/lower.rs:250-255,388-393",
  "carrier: src/mir/builder/control_flow/plan/effect.rs:29-35 (GlobalCall.func: String)",
  "live consumer: src/mir/builder/control_flow/plan/lowerer/effect_emission.rs:144-157",
  "legacy registry route: src/mir/builder/control_flow/joinir/route_entry/registry/handlers/generic.rs:1-139",
  "source-backed canonical V1 bypass: src/mir/builder/raw_loop_child_entry.rs and src/mir/builder/normal_callable_loop_physical_adapter.rs",
  "accepted-corpus manifest: docs/development/current/main/design/fixtures/generic-loop-legacy-disposition-v1.tsv (389 case rows)",
  "selected exact sibling: src/mir/builder/control_flow/plan/lowerer/emission_port.rs and located_loop.rs (no production caller)",
]
finite_states = [
  "LegacyRegistryLive: ordered V0/V1 scheduler remains production authority until M10b; no Call-lane deletion",
  "SourceBackedCanonicalV1Live: retained bounded semantic path; not part of registry-route deletion",
  "PortableSuccessorIncomplete: any accepted corpus row lacks a portable owner or approved typed reject; M10b blocked",
  "M10bReady: accepted corpus is exhaustive, M8/M9/precutover gates are closed, and the atomic deletion manifest is green",
]
ordered_tasks = [
  "P1 — GENERIC-LEGACY-ROUTE-OBSERVATION-P1: measure all 389 manifest rows through production without filename/comment inference",
  "D0 — GENERIC-LEGACY-DISPOSITION-D0: classify every accepted row to portable Generic G0, another named portable owner, or an approved typed reject; unclassified accepted rows must be zero",
  "S0 — GENERIC-M10B-DELETION-MANIFEST-S0: freeze the exact registry V0/V1, ordered retry, Generic debt, writer, guard, fixture, and caller delete/migrate set",
  "I0 — M10b-I0-R0: atomically switch production selection to the portable owner and remove the legacy registry V0/V1 route, retry/debt, and old mutation paths",
  "R1 — GENERIC-LEGACY-DEAD-CODE-R1: after caller-zero proof, physically delete residual legacy handler/helper/test surfaces and narrow visibility",
]
acceptance = "This Call child is closed as ParkedSealed with one named successor owner, a finite five-step retirement sequence, and an observable reopen trigger. M10b itself opens only after the 389-row accepted corpus is exhaustive, every accepted row has one portable owner or approved typed reject, M8/M9/precutover gates are green, and the atomic deletion manifest removes the legacy registry route without deleting source-backed canonical V1 semantics."
no_safe_slice = "Keep M10b closed if any accepted row is unobserved/unclassified, V0/V1 semantic differences lack a portable owner, only a post-effect reject is available, source-backed canonical V1 would be deleted, a second resolver/source scan is required, or the work crosses package/loan, Call schema, VM/backend, JSON, or 760/800 lines without its named owner."
non_claims = "No immediate generic CorePlan implementation, no GlobalCall-only retirement, no whole-loop language deletion, no source-backed canonical V1 deletion, no GlobalCall carrier change, no argument reordering, no selected-composer activation, no ordinary-static/rewrite/ordinary-new/provider/thunk work, no Method/Call schema, package/loan, VM/backend, JSON, fallback, or retry."
evidence = "At audit HEAD d1ea0735cd, three independent read-only audits and the final Loop SSOT agree: the legacy registry V0/V1 route is production-live and semantically non-interchangeable in some nested-carrier cases; the 389-row disposition manifest remains unobserved/acceptance-unknown; a separate source-backed canonical V1 production path exists; and the portable Generic G0/Recipe owner is not yet production-selected. The existing M10b contract, not a new language decision, owns the future atomic registry-route retirement."
reopen_trigger = "Portable Loop M8/M9/precutover readiness plus exhaustive 389-row disposition and a green M10b deletion manifest; alternatively, an exact same-session source/site/brand product may reopen only the earlier Call debt edge without authorizing legacy route deletion."
next_execution = "parked_until_generic_m10b_precutover_readiness"

[same_module_rewrite_known_issuer_boundary_d0_2026_08_30]
status = "accepted_policy_retirement"
task_id = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-ISSUER-BOUNDARY-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxCount finite rewrite/known authority census"
implementation_permission = false
disposition = "RetireByPolicy"
decision = "The user-selected product policy retires Known/unique instance-to-Global rewriting as a legacy optional optimization. Preserve the incoming typed Method(Some(receiver)) route as the sole canonical successor; do not invent a Global issuer from rewritten names. The old ON/OFF parity matrix remains historical evidence only and is not a prerequisite for this policy retirement."
source_authority = "The incoming typed Method target and its exact receiver remain authoritative. The existing unified resolver/materializer owns canonical Method(Some) emission; rewrite/known itself has no exact source/site/brand issuer for a new Global target."
canonical_issuer = "Existing typed Method(Some(receiver)) publication owner. No replacement Global issuer or semantic receipt is needed for the retirement path."
non_authority = [
  "cls/name/method/arity, generated method symbols, header/module lookup, suffix candidates, StaticMethodId parsing, env/origin, module maps, typed_global_target_from_selected_symbol, fixtures, resolver replay, fallback, and retry",
]
fail_fast_boundary = "Do not add a post-argument reject: the unified ingress already owns lowered ValueId arguments. Remove the alternate rewrite route before publication and continue through the existing Method(Some) resolver/materializer. If a valid source shape is not handled by Method(Some), stop before changing behavior and reopen this policy row."
census_boundary = "src/mir/builder/rewrite/known.rs four writer families and selector wrappers -> unified_emitter ingress -> rewrite/special.rs equals delegation -> existing Method(Some) resolver/materializer; includes two production to-dst writers, two caller-zero no-dst chains, finite ON/OFF selector inputs, and result/fault/backend parity; excludes early str-like, qualified static/Cataloged, Method ABI, Resolved, ordinary-new, CorePlan/provider/thunk, VM schema repair, JSON, and Call schema."
finite_inventory = [
  "Known-to-dst writer: src/mir/builder/rewrite/known.rs:110-231",
  "Unique-suffix-to-dst writer: src/mir/builder/rewrite/known.rs:234-347",
  "selector wrappers: src/mir/builder/rewrite/known.rs:351-393",
  "equals delegation: src/mir/builder/rewrite/special.rs:93-126",
  "production ingress: src/mir/builder/calls/unified_emitter.rs:354-369",
  "alternate terminal: src/mir/builder/calls/unified_emitter/physical_terminal.rs:40-49",
]
finite_states = [
  "MethodSuccessor: incoming typed Method(Some(receiver)) is the existing canonical semantic owner",
  "RetireByPolicy: user-selected removal of the optional rewrite optimization; no old ON/OFF parity claim",
  "CallerZero: a no-dst writer has no production caller; delete only in the atomic family I0",
  "Reopen: a valid source shape fails through Method(Some), or an explicit product decision restores the optimization",
]
ordered_tasks = [
  "D0-A: reconcile all four writers, both selector wrappers, equals delegation, and unified ingress in both directions; record two live to-dst and two caller-zero no-dst chains",
  "D0-B: establish that incoming typed Method(Some(receiver)) is the existing successor and that no Global issuer is needed for retirement",
  "D0-C: record the existing ON/OFF probes as historical counter-evidence; do not manufacture missing cells or a new issuer",
  "D0-D: classify the family as MethodSuccessor + RetireByPolicy; preserve early str-like and Method(Some), and do not add a post-effect terminal",
  "I0: remove unified KnownOrUnique/equals delegation, both live to-dst writers, selector reads/pins, and caller-zero no-dst chains in one bounded row while preserving early str-like and Method(Some)",
]
acceptance = "The finite rewrite/known boundary is two-directionally reconciled; the existing Method(Some) successor is named; two production-live and two caller-zero writers are fixed; user-selected RetireByPolicy is recorded without claiming old ON/OFF parity; and the I0 has one atomic old-edge delete set without a post-effect reject or new issuer."
no_safe_slice = "Remain in design_stop if a valid source shape is not handled by Method(Some), a new source observer/receipt/resolver is required, a post-effect reject is the only option, early str-like semantics would change, or the change crosses 760/800 lines. Reopen also if an explicit product decision restores the optional optimization."
non_claims = "No old rewrite ON/OFF parity claim, no Global target issuer, no Method ABI/schema change, no early str-like retirement, no qualified static/Cataloged/Resolved/ordinary-new/CorePlan/provider/thunk work, no package/loan, VM/backend/JSON, Call schema, fallback, or retry."
parity_status = "HistoricalOnly__policy_retirement_selected"
outcome_classification = "RetireByPolicy__MethodSomeCanonicalSuccessor__historical_matrix_not_a_gate"
parity_matrix_cells = [
  "K1: module/header-backed Known with dst=Some",
  "K1n: module/header-backed Known with dst=None",
  "K2: dev-override-only Known with dst=Some",
  "K2n: dev-override-only Known with dst=None",
  "U1: unique-suffix with dst=Some",
  "U1n: unique-suffix with dst=None",
  "E-K: equals/1 delegating to Known with dst=Some",
  "E-U: equals/1 delegating to Unique with dst=Some",
]
parity_matrix_contract = "The prior ON/OFF matrix is retained as historical evidence and must not be used to invent a target or a post-effect reject. Policy retirement is accepted when the incoming Method(Some) route is the named canonical successor and the live/caller-zero writer set is finite. A retired no-dst cell is ready only when its production caller census is zero and the old writer is not resurrected."
parity_matrix_classification_2026_08_30 = [
  "K1: ParityOpen__Known_to_dst_observed__three_counterexamples_are_LegacyBugCandidate_and_AOT_equal",
  "K1n: CallerZero__old_no_dst_Known_chain_pruned_at_fb5a4e0e81__no_runtime_probe_allowed",
  "K2: NoSafeSlice__dev_only_trace_differences_observed_in_existing_fixtures_but_module_header_absence_is_not_proven",
  "K2n: CallerZero__old_no_dst_Known_chain_pruned_at_fb5a4e0e81__no_runtime_probe_allowed",
  "U1: NoSafeSlice__unique_suffix_trace_observed_for_OrderedMapBox_length__AOT_blocked_by_unrelated_array_element_write",
  "U1n: CallerZero__old_no_dst_unique_chain_pruned_at_fb5a4e0e81__no_runtime_probe_allowed",
  "E-K: NoSafeSlice__no_existing_instance_equals_Known_trace_or_AOT_cell",
  "E-U: NoSafeSlice__no_existing_instance_equals_Unique_trace_or_AOT_cell",
]
parity_evidence_2026_08_30 = [
  "A current HEAD quick build with --features vm-reference completed with one build job; this is build evidence only, not a full-suite claim.",
  "VM result parity matched for phase285_userbox_field_basic (ON rc=0, 42; OFF rc=0, 42), phase224b_method_call_min (ON/OFF both rc=5, i=5), json_query (ON/OFF both rc=0), and rewriteknown_itoa_complex_step (ON/OFF both rc=0, 12).",
  "VM result/fault parity failed for phase29bq_selfhost_box_member_local_fini_cleanup_min (ON rc=1, Counter.cleanup_bonus/0 got 1; OFF rc=0, 29), phase29bq_selfhost_box_member_loop_cleanup_min (ON rc=1, same arity fault; OFF rc=0, 30), and phase29bq_selfhost_box_member_local_fini_blockexpr_compare_logic_unary_call_cleanup_min (ON rc=1, Counter.step/1 got 2; OFF rc=0, 118).",
  "The probes exercise only the VM backend and a finite sample; LLVM/AOT shape parity and an exhaustive accepted-shape matrix remain unobserved. The counterexamples are sufficient to keep the retirement I0 closed, not to identify a new authority.",
  "Direct current quick-artifact replay at HEAD 6b746822ab05 reproduced all three counterexamples: rewrite OFF returned the accepted values 29, 30, and 118; rewrite ON failed before execution with Counter.cleanup_bonus/0 receiving 1 argument for the first two and Counter.step/1 receiving 2 for the third. This is classified as LegacyRewriteBugCandidate (the canonical Method(Some) successor avoids the receiver-arity defect), not as a language-semantic difference; LLVM/AOT successor evidence is still required before I0.",
  "A current-source quick LLVM build with --features llvm was completed with one Cargo worker, but all three fixtures stop before backend execution at ExactSourceChanged(RootPreservation(CompatibilityLoss)) for both rewrite modes. This is source-transform coverage failure, not product parity evidence; classify the LLVM/AOT successor cell as unobserved until an accepted product fixture or an explicit source-transform owner is available.",
  "At HEAD 1e76945679, the current quick binary with --features llvm was rerun for all three named fixtures with NYASH_REWRITE_KNOWN_DEFAULT={0,1}; all six --emit-mir-json attempts returned rc=1 at ExactSourceChanged(RootPreservation(CompatibilityLoss)) and produced no MIR artifact. The explicit NYASH_LLVM_USE_HARNESS=1 --backend llvm route gives the same RootPreservation terminal for the three counterexamples (the other finite samples stop at their existing GenericLoop/Resolver contracts). This reconfirms the source-transform coverage gap and does not close LLVM/AOT parity or authorize the rewrite/known retirement I0.",
  "Using the explicit --backend vm compatibility emit route at HEAD e2695550f9, all three counterexample fixtures produced MIR for rewrite modes 0 and 1; each of the six artifacts was accepted by target/release/ny-llvmc --driver harness and executed with rc=0, yielding the expected 29, 30, and 118 in both modes. This is backend-only AOT evidence (the source LLVM route still rejects before MIR), so it strengthens but does not close the exhaustive product parity matrix.",
  "Read-only replay against the current source surface at HEAD 4f141c936b confirmed the three Known-to-dst counterexamples: rewrite OFF emitted the incoming Method(receiver) route, rewrite ON emitted a Global projection with resolve.choose reason=userbox-rewrite, and the same six MIR artifacts ran through target/release/ny-llvmc --driver harness with equal outputs 29, 30, and 118. This confirms K1 parity/legacy-bug classification only; it does not cover K2, U1, or equals.",
  "A scan of 224 existing box/new method fixtures with the current quick artifact found 15 dev-toggle-only resolve.choose entries (including Greeter.__get_greeting/0, ParserBox.skip_ws/2, and MatI64._fill_zero_core/2). The toggle difference proves a dev-sensitive candidate but does not prove module/header absence versus new-origin timing, so K2 remains NoSafeSlice and no new fixture or issuer is authorized.",
  "The current quick artifact observed U1 unique-suffix for phase296x_binding_context_native_min.hako (OrderedMapBox.length/0, certainty=Heuristic), changing one Method call to Global when rewrite is enabled. ny-llvmc harness object lowering stops on unrelated array_element_write, so this is a SuccessorBackendGap/NoSafeSlice observation rather than parity evidence; do not repair the Global route.",
  "No existing apps/tests instance-equals fixture reaches the rewrite/special equals delegation: the tracked equals example is a static ParserStmtBox.equals/1 call. E-K and E-U therefore remain unobserved; adding a synthetic fixture in design_stop is forbidden.",
  "The three no-dst cells remain structural CallerZero: their non-to-dst Known/Unique facades and helpers were removed at fb5a4e0e81, and the current source has no production caller for those symbols. Do not resurrect them merely to manufacture runtime matrix rows.",
]
evidence = "At audit HEAD 636344522ce0, the four writer families are finite: the two `_to_dst` chains are production-live and the two no-dst chains are caller-zero. The current route uses generated symbols/header/suffix and typed_global_target_from_selected_symbol, while the existing incoming Method(Some) route is the named retirement successor. A read-only counter-audit confirmed the three receiver-arity failures as LegacyBugCandidate cells: OFF Method(Some) and VM-emitted ON/OFF MIR both yield the accepted values 29, 30, and 118 through AOT, while current-source LLVM stops before MIR at RootPreservation(CompatibilityLoss). The remaining Known/Unique/equals and destination-shape cells are not writer-traced yet; the eight-cell matrix remains open."
reopen_trigger = "The finite ON/OFF result/fault/backend matrix is green for all accepted Known/equals/unique shapes and the exact live/caller-zero writer set is frozen; then open the atomic retirement I0. If parity fails, keep the route live and record the counterexample."
next_execution = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-POLICY-RETIRE-I0__pending_fast_activation"

[same_module_rewrite_known_policy_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-POLICY-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-ISSUER-BOUNDARY-D0"
ceremony_tier = "T1"
box_kind = "BoxShape bounded optional-rewrite retirement"
implementation_permission = false
allowed_files = [
  "src/mir/builder/rewrite/known.rs",
  "src/mir/builder/rewrite/header_lookup.rs",
  "src/mir/builder/rewrite/special.rs",
  "src/mir/builder/rewrite/mod.rs",
  "src/mir/builder/rewrite/README.md",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/calls/unified_emitter/physical_terminal.rs",
  "src/mir/builder/calls/unified_emitter/physical_receipt_tests.rs",
  "src/config/env/builder_flags.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_rewrite_known_policy_guard.py",
  "tools/checks/lib/mir_call_global_target_b1_cutover_guard.py",
  "tools/selfhost/proof/selfhost_smoke.sh",
  "tools/smokes/v2/profiles/integration/json/json_query_vm_llvm.sh",
  "tools/smokes/v2/profiles/integration/selfhost/selfhost_mir_min_vm.sh",
  "tools/smokes/v2/profiles/integration/selfhost/selfhost_mir_m2_eq_false_vm.sh",
  "tools/smokes/v2/profiles/quick/core/json_query_min_vm.sh",
  "docs/design/instance-dispatch-and-birth.md",
  "docs/design/using-and-dispatch.md",
  "docs/development/builder/unified-method-resolution.md",
  "docs/development/builder/BOXES.md",
  "docs/development/selfhosting/quickstart.md",
  "docs/reference/language/quick-reference.md",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
decision = "Apply the user-selected legacy-retirement policy to the optional Known/unique instance-to-Global rewrite. Delete the alternate route and its selector/env authority; preserve the incoming typed Method(Some(receiver)) resolver/materializer and early str-like behavior. Do not claim old ON/OFF parity or add a replacement issuer."
source_authority = "The incoming typed CallTarget::Method with its exact receiver is the sole source of method meaning. The existing CalleeResolverBox, router, materializer, and physical terminal own canonical Method(Some) publication."
canonical_issuer = "Existing Method(Some(receiver)) resolver/materializer; no new Global issuer, source observer, receipt, or target reconstruction is allowed."
non_authority = [
  "Known/unique generated names, class/method/arity strings, header/module lookup, suffix candidates, StaticMethodId parsing, rewrite env flags, new-origin flags, resolve.choose traces, fixtures, and legacy parity guesses",
]
fail_fast_boundary = "Skip the retired rewrite branch before alternate publication and continue through the existing Method(Some) path. Do not reject after argument effects and do not fall back/retry. If Method(Some) cannot handle a valid source shape, stop and reopen the policy rather than adding a terminal."
census_boundary = "rewrite/known.rs live _to_dst writers and selector wrappers -> unified_emitter method ingress -> rewrite/special.rs equals delegation -> existing Method(Some) resolver/materializer; includes dead no-dst chains already pruned, four Rust env readers, repo-owned live env pins, alternate outcome labels, and current rewrite docs; excludes early str-like, ArrayWrite, Method ABI, Resolved, ordinary-static, ordinary-new, CorePlan/provider/thunk, package/loan, VM/backend, JSON, Call schema, fallback, and retry."
finite_inventory = [
  "Known-to-dst writer pair: src/mir/builder/rewrite/known.rs",
  "Unique-suffix-to-dst writer pair: src/mir/builder/rewrite/known.rs",
  "equals delegation: src/mir/builder/rewrite/special.rs",
  "unified method ingress and alternate labels: src/mir/builder/calls/unified_emitter.rs and src/mir/builder/calls/unified_emitter/physical_terminal.rs",
  "selector readers: src/config/env/builder_flags.rs",
  "repo-owned live selector pins: tools/selfhost/proof/selfhost_smoke.sh and tools/smokes/v2/profiles/{integration,quick}/**",
]
old_edge_delete_set = [
  "try_known_rewrite_to_dst[_with_lookup]",
  "try_unique_suffix_rewrite_to_dst[_with_lookup]",
  "try_known_or_unique_to_dst[_with_lookup]",
  "try_special_equals_to_dst[_with_lookup]",
  "UnifiedCallAlternateRouteV1::SpecialEqualsRewrite",
  "UnifiedCallAlternateRouteV1::KnownOrUniqueRewrite",
  "NYASH_REWRITE_KNOWN_DEFAULT / NYASH_BUILDER_REWRITE_INSTANCE / NYASH_DEV_REWRITE_USERBOX / NYASH_DEV_REWRITE_NEW_ORIGIN Rust readers",
]
preserved_edge_set = [
  "early str-like toString/stringify/str handling",
  "incoming typed Method(Some(receiver)) resolver/materializer/router",
  "ArrayWrite alternate route",
  "generic physical MirInstruction::Call terminal",
]
positive_tests = [
  "user-defined Method(Some(receiver)) reaches the generic typed Call terminal without a rewrite alternate",
  "early str-like Method route remains the existing explicit alternate",
]
negative_tests = [
  "equals/1 no longer enters SpecialEqualsRewrite",
  "KnownOrUniqueRewrite alternate is unconstructible after the route deletion",
  "retired rewrite env values have no Rust reader or production effect",
]
focused_test_filters = [
  "physical_receipt",
  "rewrite_retire",
]
guard = "Extend the existing active-surface dispatch with one policy-retirement checker; it must prove the old writer/selector symbols and alternate labels are absent after landing, Method(Some) and early str-like owners remain, all non-archive selector pins are removed, touched source files stay below 760 lines, and focused cargo-list filters match nonzero tests."
acceptance = "Known/unique/equals alternate writers and selector readers are caller-zero or deleted, no Global target issuer was added, canonical Method(Some) and early str-like routes remain, focused positive/negative tests and the active policy guard are green, current docs/reference contract marks the envs retired, and the commit diff is confined to this bounded delete set."
no_safe_slice = "Return to design_stop if a valid source shape fails through Method(Some), any non-archive production reader/writer for the retired env remains, a new target/receipt/resolver is needed, a post-effect reject is proposed, or the change crosses Method ABI, Call schema, VM/backend, JSON, package/loan, fallback/retry, or 760/800 lines."
non_claims = "No old ON/OFF parity claim, no Method(None) whole-family retirement, no Call.func/Option<Callee> schema change, no VM/backend change, no Stage1 artifact, no early str-like or ArrayWrite retirement, no guard registry deletion, and no language semantics change beyond removing the optional optimization."
reopen_trigger = "Reopen only if an explicit product decision restores the optional rewrite or a valid source method call fails through the canonical Method(Some) route after this retirement."
implementation_commit = "7a6fb9e2db"
coverage_base_commit = "7eb80b6609"
changed_test_names = [
  "rewrite_retire_user_method_uses_canonical_method_terminal",
  "rewrite_retire_equals_uses_canonical_method_terminal",
]
focused_test_results = [
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib rewrite_retire -- --nocapture --test-threads=1: 2 passed, 0 failed",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib physical_receipt -- --test-threads=1: 22 passed, 0 failed",
]
focused_check = "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo check --profile quick --lib: passed with existing warnings only"
guard_runs = [
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass while fast-open",
  "python3 tools/checks/lib/mir_call_d1b_rewrite_known_policy_guard.py .: pass while fast-open",
  "python3 tools/checks/lib/mir_call_global_target_b1_cutover_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass while fast-open",
  "git diff --check: pass",
]
rustfmt_result = "rustfmt --edition 2021 --check on the six touched Rust files: pass"
line_count_result = "all touched Rust owners and the policy guard remain below the 760-line split boundary"
evidence = "Implementation landed at 7a6fb9e2db. The optional Known/Unique/equals rewrite writers, selectors, and alternate labels were removed; the incoming typed Method(Some(receiver)) and explicit early str-like routes remain. The two focused canonical-method witnesses and the 22-test physical-receipt filter passed, quick library type-check passed, and the active policy/B1/pointer guards passed. Full cargo test/link was not claimed; unrelated historical/pilot guard cleanup remains outside this row."
red_classification = "No current-change red in the bounded row. Workspace-wide warnings and historical pilot guards are baseline/out-of-scope and were not relabeled."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_ordinary_static_legacy_compat_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-LEGACY-COMPAT-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-ORDINARY-STATIC-TERMINAL-D0"
ceremony_tier = "T1"
box_kind = "BoxCount bounded unissued-static compatibility retirement"
implementation_permission = false
decision = "Apply the user-selected legacy-retirement policy narrowly to unissued generic static member compatibility. Retire StaticReceiver/StaticThis, Me StaticFallback, and LoweredGlobal::Static only when no exact Cataloged handoff is available, before argument effects. Preserve exact Cataloged handoffs, scalar/inline owners, qualified Math compatibility, Env/Extern, standard Method(Some(receiver)), and LoweredGlobal::Instance."
source_authority = "Existing exact Cataloged source/site/brand handoff, static scalar fact, inline helper owner, qualified Math compatibility owner, Env/Extern owner, or exact receiver owner. The retired cohort has no issuer and receives a payload-free typed terminal; this row creates no target."
canonical_issuer = "None for the retired cohort. Exact Cataloged publication, scalar/inline terminals, qualified Math, Env/Extern, Standard Method(Some), and LoweredGlobal::Instance keep their existing owners."
non_authority = [
  "owner/method/arity text, current_static_box, me ValueId, formatted func_name, header/module/plugin lookup, registry, generated symbol, resolver/recovery, fixture, fallback, retry, and any new receipt or target issuer",
]
fail_fast_boundary = "After existing exact Cataloged ingress and scalar/inline checks, but before completion.lower_all, next_value_id, Const/ValueId allocation, block/collector mutation, MIR Call publication, or any retry/fallback. Qualified Math remains the explicitly preserved compatibility owner."
census_boundary = "member_route StaticReceiver/StaticThis and method_call_handlers Me StaticFallback/LoweredGlobal::Static -> existing generic static compatibility terminal; includes claim-ingress Unavailable, non-claim static routes, one typed retirement terminal, and focused route tests; excludes exact Cataloged Selected/TargetOnly/NoExactStaticTarget, scalar, inline, qualified Math, Env/Extern, Standard Method(Some), LoweredGlobal::Instance, CorePlan, provider, physical thunk, rewrite/known, ordinary-new, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "member_route.rs StaticReceiver claim-ingress Unavailable fallback",
  "member_route.rs StaticReceiver non-claim execution",
  "member_route.rs ReceiverNormalizationPlan::StaticThis execution",
  "method_call_handlers.rs PreparedMeCallExecutionV1::StaticFallback",
  "method_call_handlers.rs PreparedMeCallExecutionV1::LoweredGlobal with PreparedMeReceiverV1::Static",
  "method_call_handlers.rs handle_static_method_call_with_descent generic terminal",
  "preserved owners: exact Cataloged, scalar/inline, qualified Math, Env/Extern, Method(Some), LoweredGlobal::Instance",
]
finite_states = [
  "ExactCatalogedKeep: Selected/TargetOnly/NoExactStaticTarget uses the existing source-keyed ingress and never enters generic compatibility",
  "ScalarInlineKeep: existing scalar fact or inline helper completes without generic target synthesis",
  "QualifiedMathKeep: owner Math keeps its existing compatibility terminal because qualified Math is a separate named owner",
  "RetireBeforeEffects: unissued StaticReceiver/StaticThis/StaticFallback/LoweredGlobal::Static enters one stable typed error before argument descent",
  "InstanceKeep: LoweredGlobal::Instance and standard Method(Some(receiver)) retain their current receiver-bearing semantics",
  "UnsupportedTypedReject: missing/foreign/ambiguous exact routes keep their existing typed rejection and never fall through",
]
ordered_tasks = [
  "I0-A: amend the active card and reusable active-surface dispatch with the finite preserved/retired boundary; no production semantic change in this task",
  "I0-B: add one payload-free static-legacy-retired error helper after scalar/inline checks and before argument descent",
  "I0-C: connect StaticReceiver/StaticThis/Me StaticFallback and unissued LoweredGlobal::Static to that terminal while preserving exact Cataloged, Math, Env/Extern, Method(Some), and LoweredGlobal::Instance",
  "I0-D: update focused positive/negative route tests and module/language reference, then prove no MIR/argument effect on the retired cohort",
  "C0: record caller-zero for the retired generic terminal edge, run focused tests/guards, and return the SameModule parent to design_stop without broad closure",
]
acceptance = "The retired generic static cohort returns one stable typed terminal before argument descent and publishes no MIR/effect; exact Cataloged, scalar/inline, qualified Math, Env/Extern, Method(Some), and LoweredGlobal::Instance remain green; no new issuer/resolver/receipt or fallback/retry appears; focused cargo tests, active-surface guard, pointer guard, and line-count guard pass."
no_safe_slice = "Return to design_stop if qualified Math, exact Cataloged, scalar/inline, Env/Extern, Method(Some), or LoweredGlobal::Instance must change; if an exact issuer/new receipt is needed; if the only rejection is post-effect; if another static owner appears; or if the work crosses build.rs, Call schema, VM/backend/JSON, package/loan, fallback/retry, or the 760/800-line boundary."
non_claims = "No whole Method(None) change, no shared Resolved change, no exact Cataloged issuer or loan, no qualified Math/provider decision, no CorePlan/physical-thunk/rewrite/ordinary-new work, no VM/backend/JSON, no Call schema, no guard deletion, and no public language grammar change beyond retiring the unissued internal compatibility edge."
allowed_files = [
  "src/mir/builder/method_call_handlers.rs",
  "src/mir/builder/calls/member_route.rs",
  "src/mir/builder/method_call_handlers_static_legacy_retire_tests.rs",
  "src/mir/builder/calls/member_route_descent_tests.rs",
  "src/mir/builder/calls/README.md",
  "docs/reference/language/function-call-evaluation.md",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_rows.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/method_call_terminal.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "VM/backend/JSON/LLVM",
  "Call schema",
  "package/loan/generalization",
]
focused_test_filters = [
  "static_legacy_retire_tests",
]
changed_test_names = [
  "unissued_static_route_retires_before_argument_descent",
  "qualified_math_static_route_keeps_compatibility_owner",
  "static_this_retires_before_argument_descent",
  "me_static_fallback_retires_before_argument_descent",
  "lowered_global_static_retires_before_argument_descent",
]
guard = "The existing active-surface lifecycle guard dispatches a row-specific checker. It must prove the exact state/pointer/card boundary, the stable retirement tag before lower_all, the qualified Math preservation, the Method(Some)/Instance preservation, the exact focused test inventory through cargo --list, the allowed path set, and all touched Rust owners below 760 lines. No new registry entry is added."
implementation_commit = "b1755febfb"
corrective_commit = "7c834d021a"
coverage_base_commit = "530111b173"
focused_test_results = [
  "CARGO_BUILD_JOBS=4 cargo test --quiet --profile quick --lib static_legacy_retire_tests -- --test-threads=1: 5 passed, 0 failed",
  "CARGO_BUILD_JOBS=4 cargo test --quiet --profile quick --lib member_route_descent_tests -- --test-threads=1: 13 passed, 0 failed",
]
focused_check = "rustfmt --edition 2021 on touched Rust files: pass; no full cargo test/link claimed because the repository-wide test binary is an established OOM risk."
guard_runs = [
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass fast-open and landed validation",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "git diff --check: pass",
  "TOML parse via Python tomllib: pass",
]
line_count_result = "method_call_handlers.rs 687 lines, static retirement tests 155 lines, member_route_descent_tests.rs 749 lines, active row handler 725 lines; all below 760."
evidence = "The unissued generic StaticReceiver, static this, receiverless me StaticFallback, and LoweredGlobal::Static routes now enter one finite retirement terminal before argument descent, Const/ValueId allocation, or MIR publication. Existing scalar/inline handling and qualified Math compatibility remain live; LoweredGlobal::Instance and receiver-bearing Method paths are untouched. Five new negative/positive tests and thirteen member-route regressions pass. The retirement terminal is represented by a private enum and the guard checks ordering inside the actual handler body rather than a file-wide token position."
red_classification = "No current-change red in this bounded row. Existing workspace warnings remain baseline; full cargo test/link is unclaimed."
reopen_trigger = "Reopen only if a preserved exact Cataloged/scalar/inline/Math/Env/Extern/Method(Some)/Instance owner fails, or a newly discovered unissued static owner needs a different source-backed issuer; do not restore generic fallback by name or registry."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_operator_env_disposition_d0_2026_08_30]
status = "accepted_retirement_selected"
task_id = "MIR-CALL-SAME-MODULE-OPERATOR-ENV-DISPOSITION-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape finite operator/env disposition census"
implementation_permission = false
primary_missing_boundary = "none__user_selected_retirement"
decision = "Retire only the legacy NYASH_BUILDER_OPERATOR_BOX_{ALL,ADD,COMPARE}_CALL Builder Call route. Unset and explicit false use the existing direct arithmetic/comparison/unary MIR owners. Explicit true becomes a named typed retirement terminal at all three compiler ingresses. Malformed and non-Unicode values are typed rejects. Preserve the separate NYASH_OPERATOR_BOX_* runtime/observe/adopt lane and --dev behavior."
source_authority = "The source operator enum/AST ingress owns operator meaning; existing arithmetic, comparison, and unary direct lowering own the non-Call successor candidates. The env getters and prelude only select compatibility behavior and do not issue targets."
canonical_issuer = "No replacement SameModule target issuer is needed. Existing arithmetic, comparison, and unary direct lowering remain the sole canonical production owners after the retired selector is accepted at compiler ingress."
non_authority = [
  "builder_operator_box_all_call, builder_operator_box_add_call, builder_operator_box_compare_call values, hardcoded operator names, prelude injection, current-function guards, headers/registry, generated symbols, typed_global_target_from_selected_symbol, fixtures, fallback, and retry",
  "operand/place/child effects, Const/ValueId/MIR Call, rewrite/known, ordinary-new, CorePlan, provider, physical thunk, Method(None), shared Resolved, VM/backend, JSON, and Call schema",
]
fail_fast_boundary = "Parse the three Builder selectors once, in fixed ALL/ADD/COMPARE order, at normal, raw-published, and resolved compiler ingresses. Reject before candidate/session creation, current_module or source binding, operand/place/child lowering, Const/ValueId allocation, or target publication. After successful validation no selector policy is stored or reread."
census_boundary = "OS/TOML/child-inherited Builder selector values -> three compiler ingresses -> four legacy SameModule target publishers and the ALL_CALL prelude coupling -> existing direct arithmetic/comparison/unary MIR; includes all three keys, six structural source ingresses, repo-owned writers/pass-through/smoke pins, malformed/non-Unicode precedence, negative-literal folding, and reusable-after-reject behavior; excludes NYASH_OPERATOR_BOX_* runtime/adopt semantics, --dev behavior, operator library sources, rewrite/known, ordinary-new, CorePlan, provider, thunk, package/loan, VM/backend, JSON, and Call schema."
finite_inventory = [
  "selector owners: src/config/env/builder_flags.rs operator getters builder_operator_box_all_call, builder_operator_box_add_call, builder_operator_box_compare_call",
  "source ingress owners: src/mir/builder/compound_assignment.rs, src/mir/builder/raw_root_body_lowering.rs (two sites), src/mir/builder/ops/binary_expression_descent.rs, src/mir/builder/resolved_lowering/lowerer.rs, and src/mir/builder/ops/unary.rs prepared ingress",
  "target publisher owners: src/mir/builder/ops/arithmetic.rs (two), src/mir/builder/ops/comparison.rs, and src/mir/builder/ops/unary.rs",
  "compatibility sibling/writers: src/runner/modes/common_util/resolve/strip/prelude.rs, src/cli/args.rs (--dev), tools/selfhost/proof/run_stageb_compiler_vm.sh, and tools/dev_env.sh; classify NYASH_OPERATOR_BOX_ALL and legacy NYASH_OPERATOR_BOX_* injection separately from Builder selectors",
  "direct successors: existing arithmetic, comparison, and unary non-Call lowering owners",
]
external_writers = [
  "src/cli/args.rs --dev writes NYASH_OPERATOR_BOX_ALL/STRINGIFY/COMPARE/ADD; it does not enable Builder-call selectors",
  "tools/dev_env.sh enables both legacy runtime/prelude selectors and Builder-call selectors",
  "tools/selfhost/proof/run_stageb_compiler_vm.sh forwards a non-empty parent Builder ALL/ADD value and otherwise supplies 0; COMPARE is not pinned and may arrive from the parent environment",
]
external_invokers = [
  "tools/smokes/v2/profiles/integration/selfhost/phase29bq_selfhost_planner_required_dev_gate_vm.sh explicitly passes Builder-call selectors as 0",
  "tools/smokes/v2/profiles/integration/selfhost/phase29bq_selfhost_stageb_route_parity_smoke_vm.sh explicitly passes Builder-call selectors as 0",
]
public_contract_surfaces = [
  "docs/guides/operator-boxes.md documents the Builder-call master flag as an opt-in compatibility route",
  "docs/guides/dev-mode.md and docs/reference/environment-variables.md describe the separate --dev/runtime and methodize contracts",
]
finite_states = [
  "Direct: every key is unset or exact 0/false/off, ASCII-case-insensitive and without trimming; continue through existing direct MIR lowering",
  "RetiredExplicitCompatibility: at least one key is exact 1/true/on and no key is malformed/non-Unicode; reject with the named retired-route error",
  "InvalidSelector: empty, whitespace, 01, yes, or any other Unicode value; reject before retired truthy precedence and before effects",
  "NonUnicode: any key is non-Unicode; reject before retired truthy precedence and before effects",
  "RuntimeOnly: NYASH_OPERATOR_BOX_* without Builder selectors remains accepted and reaches direct MIR; runtime/prelude/observe/adopt behavior is unchanged",
  "NegativeLiteralFold: existing fold/Core13/direct unary handling remains canonical and is never counted as a legacy target publisher",
]
ordered_tasks = [
  "D0: classify all 15 type-fact subguards as Keep, SupersededDelete, Rehome, or Unresolved; the probe currently shows 9 green and 6 red",
  "S0: delete superseded legacy subguards/tests and rehome still-live invariants to their current owner; do not refresh the obsolete whole-repo writer overlay",
  "I0: add one typed aggregate selector parser and invoke it at normal, raw-published, and resolved compiler ingresses before effects",
  "I0: atomically delete the four legacy publishers and all downstream selector rereads while preserving direct MIR lowering",
  "I0: remove only the Builder ALL_CALL prelude OR and repo-owned Builder writers/pass-through/smoke pins; preserve NYASH_OPERATOR_BOX_* and --dev",
  "I0: update the existing active/B1/type-fact guards and focused compiler tests in the same row; no new registry row",
]
acceptance = "The operator D0 is accepted, but implementation waits for the guard-fate D0. The exact legacy route, direct successor, three conceptual compiler ingress families/five physical public methods, four publishers, prelude coupling, external writers, parser states, and exclusions are finite. GUARD-PRUNE-S0 opens only after every subguard has a fate; I0 remains one atomic retirement row."
no_safe_slice = "Return to design_stop if another production Builder selector reader/writer exists, one of the three compiler ingresses can bypass validation, direct MIR requires a replacement target/resolver, runtime NYASH_OPERATOR_BOX_* behavior must change, typed rejection cannot precede compiler/session/source effects, or the work crosses rewrite/known, ordinary-new, CorePlan, package/loan, VM/backend, JSON, Call schema, or 760/800 lines."
non_claims = "No operator semantics rewrite, no NYASH_OPERATOR_BOX_* runtime/adopt/observe retirement, no --dev behavior change, no operator library deletion, no global emit_legacy_call or typed_global_target_from_selected_symbol deletion, no historical fixture rewrite, no new target issuer/receipt, no rewrite/known or ordinary-new work, no Method(None), package/loan, VM/backend/JSON, Call-schema change, fallback, or retry."
decision_reopen_trigger = "Only an explicit future language/product decision may restore a Builder operator-call compatibility route; existing Hako operator files, fixtures, runtime flags, or external stale selectors are non-authority."
next_execution = "none_until_MIRBUILDER-TYPE-FACT-PARTITION-GUARD-FATE-D0"

[mirbuilder_type_fact_partition_guard_fate_d0_2026_08_30]
status = "accepted_closed"
task_id = "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-FATE-D0"
parent_row = "MIR-CALL-SAME-MODULE-OPERATOR-ENV-DISPOSITION-D0"
ceremony_tier = "T0"
box_kind = "BoxShape finite guard-lifecycle census"
implementation_permission = false
decision = "Do not refresh or delete the composite guard blindly. Inventory each of its 15 checks and retain only current invariants. A red check may be deleted only when its old source/edge is caller-zero or a named current guard owns the same invariant; otherwise rehome it or leave Unresolved. The immutable P1 fixture remains historical evidence, not a live whole-repo map."
red_classification = "known baseline debt at parent HEAD f1b873e467: the composite row is pilot-only and exit 1 before operator work. Independent calls show 9 green checks and 6 red checks; the first aggregate failure is a 20-path active writer-overlay drift (5 removed, 14 added, 1 count change)."
census_boundary = "validate_p1_g0_profile_freeze_v1 plus 14 active validators -> exact current source/guard owner or typed tombstone; includes direct tests and output labels; excludes production semantics, historical task prose, and operator retirement."
finite_states = [
  "Keep: current source owner exists, the check passes, and no equal or stronger current guard owns it",
  "SupersededDelete: old source/edge is absent or an equal/stronger named guard owns the invariant; delete check, test, import, and output claim together",
  "Rehome: invariant is still required but the old lexical owner moved; move the minimal check to the current owner-specific guard without recreating a whole-repo census",
  "Unresolved: authority/successor is unclear; keep operator S0/I0 closed and do not weaken the failure",
]
observed_partition = [
  "green=9: P1 freeze, StaticLoad, CheckSelect, resolved trivial operation, Compare emission, FieldGet, Fastmem receipt, Fastmem FieldLoad, ArrayWrite",
  "red=6: active whole-writer overlay, CONST0 lexical check, literal-postemit check, resolved-direct-call check, CALL-RECEIPT0 check, MapWrite check",
]
accepted_fate = "Keep=9, SupersededDelete=1, Rehome=5, Unresolved=0. SupersededDelete is only ACTIVE_CUTOVER_WRITER_REPLACEMENTS plus its validator/call/test import and unchecked active_writer output. Rehome CONST0 to both current Const emission forms; literal-postemit to literal_lowering.rs; resolved-direct-call to the production emit window excluding cfg(test); CALL-RECEIPT0 to live generic receipt/post-success owners while deleting only the retired preloop fragment; and MAP-WRITE to the current typed method_call issuer/replay window."
scope_caveat = "The unregistered mirbuilder_type_fact_producer_inventory.py standalone check still compares the immutable historical fixture with current source and remains red; it is not the composite live guard and is not advertised as green."
ordered_tasks = [
  "run all 15 checks independently so first-failure masking cannot hide later red checks",
  "for each red check name its old owner, current successor guard or caller-zero evidence, exact fate, and reopen trigger",
  "verify all nine green checks still have live owners and are not duplicated elsewhere",
  "open one prune/rehome S0 only when Unresolved=0; otherwise keep operator work closed",
]
acceptance = "All 15 checks have exactly one state and owner; Keep=9, SupersededDelete=1, Rehome=5, Unresolved=0; every deletion/rehome has exact source or successor evidence; the historical fixture/digest is untouched; and the next S0 boundary is expressible without a whole-repo baseline refresh."
no_safe_slice = "Remain in design_stop if a red check still guards a live invariant without a successor, a green check is duplicated but precedence is unclear, deletion would rely only on age/redness, or production code/fixture changes are required."
non_claims = "No guard deletion or repair, no operator implementation, no fixture refresh, no quick-static promotion, and no production/source change in D0."
next_row = "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-PRUNE-S0"

[mirbuilder_type_fact_partition_guard_prune_s0_2026_08_30]
status = "landed"
task_id = "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-PRUNE-S0"
parent_row = "MIR-CALL-SAME-MODULE-OPERATOR-ENV-DISPOSITION-D0"
ceremony_tier = "T0"
box_kind = "BoxShape"
implementation_permission = false
prerequisite = "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-FATE-D0 is closed with Keep=9, SupersededDelete=1, Rehome=5, Unresolved=0."
decision = "Delete only the SupersededDelete overlay and its tests/output claims; rehome the five live checks to one private call-post-success sibling; retain the nine Keep checks and the five-row anchor sweep. Remove the obsolete whole-repo writer overlay instead of updating its 20-path baseline. Preserve the composite row id and immutable historical fixture, then promote the now-green retained guard to quick-static."
allowed_files = [
  "tools/checks/lib/mirbuilder_type_fact_partition_guard.py",
  "tools/checks/lib/mirbuilder_type_fact_partition_guard_tests.py",
  "tools/checks/lib/mirbuilder_type_fact_call_post_success_guard.py",
  "tools/checks/lib/mirbuilder_type_fact_call_post_success_guard_tests.py",
  "tools/checks/guard_rows.toml",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_operator_retirement_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
rehome_contract = "The new sibling checks current production owners: constant.rs has two prepare/emit/commit forms; literal_lowering.rs owns build_literal and resolved/unary delegates; direct_call.rs production emit is separated from cfg(test) emit_resolved_header; generic receipt/post-success owners remain live while preloop TYPE-I0 is removed; and MAP replay is checked around ssot::method_call::method_call followed by replay."
acceptance = "The retained parent plus private sibling and tests pass from HEAD; every removed check matches the accepted fate table; no output claims a deleted invariant; row id/cmd remain stable and profiles become pilot+quick-static; the parent is below 760 and every file below 800; fixture/digest and production code are unchanged; each of the five rehomes has an independent live test rather than relying on first-failure masking."
no_safe_slice = "Stop if a removed check lacks caller-zero/successor evidence, a rehomed invariant changes meaning, the retained guard remains red, the fixture must change, or production code is needed."
non_claims = "No selector parsing, operator retirement, publisher/prelude/script deletion, production switch, semantic baseline refresh, or operator behavior change."
landed_commit = "24db147172"
focused_tests = [
  "python3 tools/checks/lib/mirbuilder_type_fact_partition_guard.py .",
  "python3 -m unittest tools/checks/lib/mirbuilder_type_fact_partition_guard_tests.py tools/checks/lib/mirbuilder_type_fact_call_post_success_guard_tests.py",
  "bash tools/checks/run_row_guard.sh --profile quick-static --only mirbuilder-type-fact-partition,mir-call-d1b-cataloged-affine-loan-lifecycle",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .",
  "bash tools/checks/current_state_pointer_guard.sh",
]
verification = "The stale overlay symbols are absent; parent=511 lines, post-success sibling=368 lines, active surface guard=755 lines; immutable fixture/digest and production Rust are unchanged; five rehomes have independent tests; the retained row is green in pilot and quick-static."
next_row = "MIR-CALL-SAME-MODULE-OPERATOR-CALL-RETIRE-I0"

[same_module_operator_call_retire_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-OPERATOR-CALL-RETIRE-I0"
parent_row = "MIR-CALL-SAME-MODULE-OPERATOR-ENV-DISPOSITION-D0"
ceremony_tier = "T1"
box_kind = "BoxCount retirement"
implementation_permission = false
prerequisites = [
  "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-FATE-D0 closed with Unresolved=0",
  "MIRBUILDER-TYPE-FACT-PARTITION-GUARD-PRUNE-S0 landed with the retained guard green and below 760",
]
decision = "Retire the complete Builder operator-call compatibility route in one semantic switch: parse all three selectors once, reject explicit true/malformed/non-Unicode at every compiler ingress before effects, keep unset/false and runtime-only selectors on direct MIR, then delete all four legacy publishers, downstream reads, prelude coupling, and repo-owned Builder writers."
source_authority = "AST operator kinds plus the existing direct arithmetic/comparison/unary lowering. The aggregate ingress parser owns only selector admission and never issues a Call target."
canonical_issuer = "Existing direct MIR lowerers only. I0 creates no Global/Method target, symbol adapter, resolver, receipt, or compatibility successor."
selector_grammar = "Fixed key order ALL, ADD, COMPARE; no trimming. Unset or ASCII-case-insensitive 0/false/off is Direct. ASCII-case-insensitive 1/true/on is RetiredExplicitCompatibility. Empty, whitespace, 01, yes, and other Unicode are InvalidSelector; non-Unicode is NonUnicode. Any malformed/non-Unicode key wins over any retired truthy key."
selector_grammar_rationale = "The broad true/false spellings preserve the existing env_bool route vocabulary while making malformed values fail-closed. This differs intentionally from the earlier strict 0/1 methodize tombstone, whose separate purpose was removal of an implicit default-on route."
typed_error_transport = "builder_flags.rs owns BuilderOperatorCallIngressErrorV1. Raw published and canonical resolved compiler results gain explicit bounded ingress variants; normal compile maps the same typed error to one stable terminal string. No generic BuilderContract collapse and no silent bool default."
ingress_topology = "Three conceptual families map to five physical public methods: compile_normal; compile_raw_published_v1; and compile_resolved, compile_resolved_callable_module, compile_resolved_recursive_callable_module. One private compiler validator owns typed rejection and is invoked exactly once at each of the five public methods; placing it only in compile_resolved_first_family is insufficient. Guard counts report conceptual=3 and physical=5 separately."
fail_fast_boundary = "Validate before normal pipeline entry, raw-published request destructure/source binding, and each resolved public-method delegation/plan/session opening. Rejection must precede candidate/session/current_module/source mutation and all operand/place/child/Const/ValueId/MIR effects; the compiler remains reusable after rejection."
coverage_base_commit = "32094aded3"
allowed_files = [
  "src/config/env/builder_flags.rs",
  "src/mir/compiler/normal_default_pipeline.rs",
  "src/mir/compiler/raw_published_compile.rs",
  "src/mir/compiler/mod.rs",
  "src/mir/compiler/lowering_input.rs",
  "src/mir/compiler/operator_call_retirement_tests.rs",
  "src/mir/compiler/README.md",
  "src/mir/builder/ops/arithmetic.rs",
  "src/mir/builder/ops/comparison.rs",
  "src/mir/builder/ops/unary.rs",
  "src/mir/builder/ops/mod.rs",
  "src/mir/builder/ops/README.md",
  "src/runner/modes/common_util/resolve/strip/prelude.rs",
  "tools/dev_env.sh",
  "tools/selfhost/proof/run_stageb_compiler_vm.sh",
  "tools/smokes/v2/profiles/integration/selfhost/phase29bq_selfhost_planner_required_dev_gate_vm.sh",
  "tools/smokes/v2/profiles/integration/selfhost/phase29bq_selfhost_stageb_route_parity_smoke_vm.sh",
  "docs/guides/operator-boxes.md",
  "docs/reference/environment-variables.md",
  "tools/checks/lib/mir_call_d1b_operator_retirement_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
]
allowed_file_groups = [
  "selector/error owner: src/config/env/builder_flags.rs",
  "three ingresses/errors/tests: src/mir/compiler/{normal_default_pipeline.rs,raw_published_compile.rs,mod.rs,lowering_input.rs,operator_call_retirement_tests.rs,README.md}",
  "publisher retirement: src/mir/builder/ops/{mod.rs,arithmetic.rs,comparison.rs,unary.rs,README.md}",
  "prelude unlink: src/runner/modes/common_util/resolve/strip/prelude.rs",
  "repo-owned writers: tools/dev_env.sh, tools/selfhost/proof/run_stageb_compiler_vm.sh, and the two phase29bq selfhost smoke scripts named by D0",
  "contract surfaces: docs/guides/operator-boxes.md and docs/reference/environment-variables.md",
  "existing guard surfaces: active operator sibling, type-fact active inventory/tests, B1 cutover/C0 matrix guards and manifest",
  "current SSOT only: CURRENT_STATE.toml, this active manifest, and the rolling workstream brief",
]
old_edge_delete_set = [
  "four SameModule publishers and 14 targets: Add publisher=1, arithmetic ALL publisher=9, comparison publisher=1, unary publisher=3",
  "all six structural-ingress or downstream builder_operator_box_*_call reads/getters after the aggregate ingress validator",
  "NYASH_BUILDER_OPERATOR_BOX_ALL_CALL as an OR-input to prelude operator module injection",
  "Builder selector exports/default pass-through/reset omissions in repo-owned shell tooling and zero pins in the two selfhost smokes",
]
preserved = [
  "direct arithmetic/comparison/unary MIR, logical short-circuit, negative-literal fold/Core13 path, compound assignment and raw-root direct semantics",
  "NYASH_OPERATOR_BOX_* runtime/prelude/observe/adopt behavior, src/cli/args.rs --dev behavior, VM/runtime code, and apps/lib/std/operators/**",
  "shared emit_legacy_call and typed_global_target_from_selected_symbol for unrelated live callers",
  "historical type-fact fixture and legacy profile evidence",
]
prelude_edit_boundary = "In prelude.rs preserve the adjacent NYASH_OPERATOR_BOX_ALL runtime read and delete only the NYASH_BUILDER_OPERATOR_BOX_ALL_CALL OR-input; unconditional runtime module injection remains unchanged."
focused_tests = [
  "three-key parser matrix including mixed precedence and non-Unicode, serialized with the shared env lock",
  "normal/raw-published/resolved pre-effect rejection, zero mutation, and compiler reuse",
  "unset/false direct MIR for Add, all arithmetic/comparison/unary cohorts, compound/raw-root paths, and negative-literal fold",
  "runtime-only selectors and --dev remain accepted without legacy Builder Call publication",
  "direct structural Site/target publisher absence and script syntax checks",
]
changed_test_names = [
  "selector_matrix_is_finite_and_direct_by_default",
  "malformed_and_non_unicode_values_cannot_hide_behind_truthy_values",
  "operator_non_unicode_selector_is_not_treated_as_unset",
  "normal_ingress_rejects_retired_selector_before_builder_effects_and_reuses",
  "raw_ingress_rejects_malformed_selector_before_source_binding",
  "resolved_ingress_rejects_non_unicode_selector_before_candidate_open",
  "unset_selectors_keep_the_direct_compiler_route",
]
focused_test_filters = [
  "operator_call_ingress_tests",
  "operator_call_retirement_tests",
]
implementation_commit = "44555655ab"
post_implementation_census = "The three Builder selector getters and all four operator publishers are zero; the 14 generated operator target names and downstream Builder selector reads are zero; repo-owned Builder selector writers and prelude OR coupling are zero. NYASH_OPERATOR_BOX_* remains runtime-only, and direct arithmetic/comparison/unary MIR remains the sole compiler route."
validation = [
  "cargo check --profile quick --tests --lib -j 4: pass",
  "cargo check --profile quick --all-targets --features vm-reference -j 4: pass",
  "cargo test --profile quick --lib -j 2 operator_call_retirement_tests: 4 passed",
  "cargo test --profile quick --lib -j 2 operator_call_ingress_tests: 3 passed",
  "cargo test --profile quick --lib -j 2 ordinary_operator_matrix_has_exact_legacy_snapshot_parity: 1 passed",
  "python3 tools/checks/lib/mir_call_d1b_operator_retirement_guard.py .: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass",
  "python3 tools/checks/lib/mirbuilder_type_fact_partition_guard.py .: pass",
  "bash tools/checks/run_row_guard.sh --profile quick-static --only mirbuilder-type-fact-partition,mir-call-d1b-cataloged-affine-loan-lifecycle: pass",
  "bash -n changed shell writers: pass",
  "git diff --check: pass",
]
red_classification = "The focused integer_binary_script_legacy_and_raw_have_the_same_normalized_snapshot test remains a pre-existing raw-public snapshot baseline red (legacy omits condition_fn while raw includes it); the I0 preserves direct BinOp and does not change that snapshot producer. Full cargo test/link is not claimed."
next_row = "Return to MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0 design_stop; operator publishers are closed and physical-thunk D0 is the next named blocker."
guards = [
  "active D1B operator retirement guard: exactly three compiler ingress calls; no downstream selector reads; four publishers and hardcoded targets zero; prelude Builder OR zero",
  "type-fact active replacement inventory updated without rewriting the historical fixture",
  "B1 cutover/C0 matrix allowlists and counts remove only retired operator owners",
  "rg confirms no repo-owned Builder selector writer/pass-through/smoke pin; bash -n passes changed scripts",
  "pointer guard, active guard, focused cargo tests, cargo quick checks, git diff --check, touched files below 760/800",
]
acceptance = "All three ingresses enforce one typed parser before effects; explicit true is retired, malformed/non-Unicode is rejected, unset/false/runtime-only is direct; the old-edge delete set is zero; direct operator behavior and runtime lane stay green; all focused tests and reusable guards execute rather than merely name tests."
no_safe_slice = "Return to design_stop instead of widening scope if a fourth compiler ingress or production Builder writer exists, validation cannot dominate effects, runtime/prelude semantics must change, a replacement target/resolver/receipt is needed, any old publisher must remain, or the change crosses excluded families or 760/800 lines."
non_claims = "No VM/runtime/adopt parity work, no operator language semantics change, no Call schema, Method corridor, CorePlan, rewrite/known, ordinary-new, package/loan, backend/JSON, fallback, retry, or broad helper deletion."

[same_module_ordinary_new_birth_target_d0_2026_08_30]
status = "accepted_design_stop_exact_constructor_relation_required"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-NEW-BIRTH-TARGET-D0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape finite allocation/constructor carrier audit"
implementation_permission = false
disposition = "CutoverBlockerOpen"
owner = "normal_callable_semantic_package ordinary-new co-seal and selected-normal physical admission"
decision = "Preserve ordinary-new language semantics, but replace the birth-text/class/arity reconstruction with one parser-owned typed constructor product and one shared N-to-N+1 ABI. The final parser constructor catalog selects ConstructorSourceIdV1 plus Birth(N) or NoBirthZero once; the normal callable package co-seals that product with the exact allocation fact and same-session owner/brand; the physical consumer only takes the resulting recipe before argument descent. This is a design-approved bounded successor, not a blanket language or VM change."
source_authority = "Final parser constructor catalog and ParserOrdinaryBoxSourceCoverageV1 own the exact source declaration relation and typed ConstructorSourceSignatureV1. The package may join it only with the same-session OwnedExprSiteV1 allocation fact and exact instance-constructor semantic row; downstream class/name/arity text is non-authority."
canonical_issuer = "A small parser-owned ConstructorSourceIdV1/Birth(N) product, co-sealed by the existing ordinary-new package owner as VerifiedOrdinaryNewBirthRecipeV1, is the sole future issuer. InstanceConstructorAbiV1 is the shared ABI owner for source arity N and physical argv arity N+1. No Builder/header/slot/plugin resolver may issue or repair the target."
next_implementation_row = "MIR-CALL-SAME-MODULE-ORDINARY-NEW-EXACT-CONSTRUCTOR-CUTOVER-I0"
non_authority = [
  "birth Option<Box<str>> text, class/name/arity lookup, current_module or header tables, AST re-scan, slot/plugin registry, physical symbol, env, target reconstruction, fallback, retry, and NewBox/Call effects",
  "ordinary-box coverage alone, test fixtures, no-claim compatibility, CorePlan, operator/provider/rewrite routes, Method(None), shared Resolved, VM/backend, JSON, and Call schema",
]
fail_fast_boundary = "Verify exact allocation site, selected package/session, constructor source identity, class and arity relation, carrier shape, duplicate/foreign/missing/residual state before argument descent, Const/ValueId allocation, NewBox emission, birth Call publication, collector/block mutation, or retry/fallback."
census_boundary = "issue_ordinary_new_claims_v1 -> OrdinaryNewClaimLedgerV1::try_take -> lower_ordinary_raw_new_with_port_v1 -> existing parser-issued instance-constructor semantic rows; includes selected-normal direct local initializers, birth/N and birth/0, missing/duplicate/foreign/mismatch/residual cases, and the physical constructor argv shape; excludes no-claim legacy paths, field initializers, constructor body lowering, CorePlan, operators, rewrite/known, providers, physical thunk, package/loan generalization, VM/backend, JSON, and Call schema."
finite_inventory = [
  "semantic claim issuer: src/mir/normal_callable_semantic_package/ordinary_new_coseal.rs::issue_ordinary_new_claims_v1",
  "claim ledger/consumer seam: src/mir/normal_callable_semantic_package/ordinary_new_coseal.rs::OrdinaryNewClaimLedgerV1 and src/mir/builder/raw_ordinary_new_claim.rs",
  "physical consumer: src/mir/builder/ordinary_new_admission.rs::lower_ordinary_raw_new_with_port_v1",
  "parser/constructor semantic owner: src/parser/constructor_source_catalog.rs and src/mir/normal_callable_semantic_package/instance_constructor_semantic.rs",
  "package install/loan path: src/mir/normal_callable_semantic_package/ordinary_new_coseal.rs, install.rs, and src/mir/builder/normal_callable_semantic_loan_port.rs",
]
finite_states = [
  "ExactBirthTarget: one existing source-backed constructor product matches the allocation claim and carrier arity; eligible for a later bounded I0",
  "NoBirth0: exact ordinary allocation has arity zero and no birth constructor; retain construction semantics without inventing a target",
  "MissingOrUnresolved: nonzero birth is absent or no exact constructor row exists; typed pre-effect blocker, no fallback",
  "ForeignOrDuplicate: package/session/source owner/site/class/arity relation is foreign or non-unique; typed reject before effects",
  "CarrierMismatch: source constructor arity N and physical argv arity N+1 cannot be represented by the existing typed carrier; ParkedSealed until an explicit ABI/carrier decision",
  "ResidualClaim: an issued claim remains after the selected physical forest finishes; typed residual reject, no silent drop",
]
ordered_tasks = [
  "I0-SOURCE: add the parser-owned typed constructor kind/source-arity index and select ConstructorSourceIdV1 plus Birth(N) or NoBirthZero exactly once; malformed, missing, duplicate, foreign, and overflow states are typed rejects",
  "I0-ABI: add one InstanceConstructorAbiV1 used by both constructor draft physicalization and the birth recipe; source arity is N and physical argv arity is checked as N+1 with checked overflow",
  "I0-RECIPE: co-seal the exact OwnedExprSiteV1 allocation fact with the same-session ConstructorSourceIdV1 and constructor semantic row; replace birth text with NoBirthZero or the typed recipe and reject missing/foreign/duplicate/kind/arity/ABI drift before effects",
  "I0-CUTOVER: take the recipe before argument descent, lower arguments once, emit NewBox, then argv=[dst]+args and one exact birth Call with dst=None; an argument failure aborts the session with no retry or fallback",
  "I0-OLD-EDGE-DELETE: delete only the claimed birth-string field, Program/Box AST scan, class+arity target reconstruction, hidden name Const/result dst, and claimed emit_legacy_call; retain no-claim compatibility and field/constructor-body lanes",
  "I0-EVIDENCE: focused positive/negative/ordering/residual tests plus source/recipe/consumer/caller-zero census and one compact closeout; no package-wide generalization",
]
acceptance = "The ordinary-new boundary has one parser-owned ConstructorSourceIdV1/Birth(N) product, one package co-seal, and one shared N/N+1 ABI; every exact allocation site has one typed recipe or one named typed reject; birth text and class/arity lookup are non-authority; the recipe is taken before arguments and residual claims fail closed; and only the listed reconstruction edges are deleted in the bounded I0."
no_safe_slice = "Remain in design_stop if exact source selection still requires class/name/arity search, AST re-scan, header/slot/plugin lookup, more than one issuer, an unshared ABI, a new Call-schema change, effects before the typed terminal, no-claim mixing, fallback/retry, or the 760/800-line boundary would be crossed."
non_claims = "No no-claim compatibility change, no field-initializer or constructor-body semantic change, no CorePlan/operator/provider/rewrite/thunk/Method/Resolved work, no package/loan generalization, no VM/backend/JSON, no Call-schema change, and no fallback/retry."
evidence = "At audit HEAD 8d4150a60b, issue_ordinary_new_claims_v1 still emits (OwnedExprSiteV1,class,N,birth text), while lower_ordinary_raw_new_with_port_v1 takes the claim before arguments and reconstructs a Global Class.birth/N target. Parser-issued ConstructorSourceIdV1/VerifiedInstanceConstructorSemanticRowV1 remains in the constructor-body lane and is not joined to the allocation site; the physical argv is [dst]+N args. The read-only worker audit therefore found the old carrier mismatch, and the accepted successor is the explicitly bounded parser product plus shared ABI described here."
worker_audit_result = "Fresh read-only audit confirms that the old allocation claim and constructor row merely coexist and cannot be joined by the existing types. A clean implementation path exists only by adding a small parser-owned typed constructor product, a package-owned birth recipe, and one shared InstanceConstructorAbiV1; no existing carrier can be promoted by inference. The future I0 is therefore bounded and design-approved, while code remains closed until the active pointer switches to it."
reopen_trigger = "Open the bounded I0 only when the parser product, exact allocation-site join, and shared N/N+1 ABI are present in the same package/session; if any relation is missing, keep typed pre-effect rejection or ParkedSealed rather than reconstructing from birth text."
next_execution = "MIR-CALL-SAME-MODULE-ORDINARY-NEW-EXACT-CONSTRUCTOR-CUTOVER-I0__implementation_pending_pointer_switch"

[same_module_ordinary_new_exact_constructor_cutover_i0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-ORDINARY-NEW-EXACT-CONSTRUCTOR-CUTOVER-I0"
parent_row = "MIR-CALL-SAME-MODULE-ORDINARY-NEW-BIRTH-TARGET-D0"
ceremony_tier = "bounded implementation"
box_kind = "BoxCount exact ordinary-new constructor cutover"
implementation_permission = false
decision = "Replace the ordinary-new birth text/class-arity reconstruction with the parser-owned ConstructorSourceIdV1 and typed Birth(N)/NoBirthZero disposition. Co-seal that exact source product with the allocation site in the existing normal callable package, use one checked N-to-N+1 ABI, and consume the recipe before argument descent. This row changes only the selected-normal direct-local ordinary-new claim cohort."
source_authority = "Parser constructor source catalog owns final-box membership, ConstructorSourceIdV1, constructor kind, and source arity. Existing instance-constructor semantic rows preserve the same source id and session brand. The package issuer is the only co-seal owner for allocation site plus constructor product."
canonical_issuer = "VerifiedOrdinaryNewBirthRecipeV1 issued by ordinary_new_coseal from one exact parser constructor row and one exact allocation site; InstanceConstructorAbiV1 owns source N and physical argv N+1. The Builder consumer only takes and emits the already-sealed recipe."
non_authority = [
  "birth Option<Box<str>> text, formatted birth/N keys, class/name/arity lookup, source AST Program/Box scans, current_module or header tables, slot/plugin registry, physical symbol, env, target reconstruction, fallback, retry, and no-claim compatibility",
  "test fixtures, constructor-body lowering, field initializers, CorePlan, operators, providers, rewrite/known, physical thunk, Method(None), shared Resolved, VM/backend, JSON, Call schema, and any new package-wide loan abstraction",
]
fail_fast_boundary = "Reject missing, duplicate, foreign, wrong-kind, wrong-arity, overflow, or residual constructor products before argument descent, ValueId/Const allocation, NewBox emission, birth Call publication, collector/block mutation, or retry/fallback."
census_boundary = "Parser constructor source relation and ordinary-box coverage -> ordinary-new package recipe -> RawInvocationChildPort claim take -> lower_ordinary_raw_new_with_port_v1; includes direct-local selected-normal New sites, exact Birth(N)/NoBirthZero, shared N/N+1 ABI, consumer order, and residual claim closure; excludes no-claim compatibility, field/constructor-body lanes, and every other SameModule family."
allowed_files = [
  "src/parser/source_authority/constructor_source.rs",
  "src/parser/constructor_source_catalog.rs",
  "src/parser/normal_callable_program_source/ordinary_new_source.rs",
  "src/mir/instance_constructor_abi.rs",
  "src/mir/mod.rs",
  "src/mir/normal_callable_semantic_package/instance_constructor_semantic.rs",
  "src/mir/normal_callable_semantic_package/ordinary_new_coseal.rs",
  "src/mir/normal_callable_semantic_package/mod.rs",
  "src/mir/normal_callable_semantic_package/issuer.rs",
  "src/mir/builder/module_lowering_invocation.rs",
  "src/mir/builder/normal_instance_constructor_admission.rs",
  "src/mir/builder/ordinary_new_admission.rs",
  "src/mir/builder/raw_root_physical/callable_main_terminal.rs",
  "src/mir/builder/raw_root_physical/child_terminal.rs",
  "src/mir/builder/README.md",
  "src/mir/normal_callable_semantic_package/README.md",
  "src/parser/source_authority.rs",
  "src/parser/public_api.rs",
  "src/parser/source_authority_tests.rs",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/build.rs",
  "src/mir/builder/calls/function_call_preflight_route.rs",
  "Call schema",
  "VM/backend/JSON",
  "CorePlan/operator/provider/rewrite/known/physical thunk",
]
old_edge_delete_set = [
  "OrdinaryNewAdmissionClaimV1::birth Option<Box<str>>",
  "source_birth_target Program/Box AST scan",
  "format!(\"birth/{N}\") and class.birth/N target reconstruction",
  "claimed branch hidden name Const/result dst and emit_legacy_call reconstruction",
]
focused_test_filters = [
  "ordinary_new_coseal",
  "ordinary_new_admission",
  "instance_constructor_abi",
  "instance_constructor_semantic",
  "ordinary_new_source",
  "source_authority",
]
changed_test_names = [
  "ordinary_new_recipe_preserves_exact_birth_source",
  "ordinary_new_constructor_disposition_accepts_zero_arity_without_birth",
  "ordinary_new_constructor_disposition_rejects_nonzero_without_birth",
  "ordinary_new_constructor_lookup_reports_missing_nonzero_birth",
  "ordinary_new_constructor_lookup_rejects_source_arity_overflow",
  "ordinary_new_source_coverage_returns_exact_box_ordinal",
  "ordinary_new_source_coverage_rejects_duplicate_box_names",
  "constructor_source_signature_preserves_kind_and_arity",
  "instance_constructor_abi_checks_n_plus_one",
]
acceptance = "The parser exposes a typed constructor kind/source-arity product and exact source id; the package issues one typed NoBirthZero or Birth(recipe) disposition per exact allocation site; the same InstanceConstructorAbiV1 validates N and N+1; the consumer takes the recipe before arguments, emits NewBox then one dst=None birth Call only for Birth, and residual claims fail closed. The listed birth-text/AST-scan/reconstruction edges are zero while no-claim compatibility remains unchanged."
no_safe_slice = "Return to design_stop if exact constructor selection needs a downstream key parse or AST/program scan, if source and physical arity lack a shared checked ABI, if a second issuer or resolver is needed, if no-claim paths must change, if Call/VM/backend semantics must change, or if any touched Rust source reaches the 760/800-line boundary."
non_claims = "No general loan/package redesign, no Call schema change, no Method/Resolved/VM/backend/JSON/CorePlan/operator/provider/rewrite/thunk change, no field or constructor-body semantics, no fallback/retry, and no physical deletion beyond the listed reconstruction edges."
reopen_trigger = "If an exact parser constructor relation or shared N/N+1 ABI is unavailable, keep the allocation claim typed-rejected or ParkedSealed; reopen only after the same-session source product is present or the ordinary-new cohort receives an explicit retirement decision."
implementation_commit = "d81d91d800"
coverage_base_commit = "194b0c08b6"
focused_test_runs = [
  "cargo test --profile quick --lib ordinary_new -- --test-threads=1: 10 passed",
  "cargo test --profile quick --lib source_authority -- --test-threads=1: 22 passed",
  "cargo test --profile quick --lib instance_constructor_abi -- --test-threads=1: 1 passed",
  "cargo test --profile quick --lib normal_instance_constructor_admission -- --test-threads=1: 2 passed",
  "cargo test --profile quick --lib mir::normal_callable_semantic_package:: -- --test-threads=1: 62 passed",
]
guard_runs = [
  "cargo check --profile quick --lib -j1: pass",
  "bash tools/checks/current_state_pointer_guard.sh: pass before closeout",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass before closeout",
  "git diff --check: pass",
]
evidence = "Implementation landed at d81d91d800. Parser constructor relations now carry typed Init/Pack/Birth plus source arity, ordinary-box coverage resolves one exact final-box row, and the existing constructor semantic batch exposes an exact Birth(N) lookup. The package replaces the birth-text claim with a non-Clone Birth(recipe) or NoBirthZero disposition, issues one checked InstanceConstructorAbiV1, and the selected-normal consumer validates and takes that disposition before argument descent. The claimed path emits NewBox followed by one recipe-owned birth Call; no-claim compatibility remains unchanged. Nine new focused tests plus the existing constructor/package suites pass under the quick profile. Full cargo test/link was not claimed because the repository's historical binary is large; the focused commands were actually executed. README owners were updated in the same closeout series."
red_classification = "No new red result. Workspace-wide cargo fmt -- --check still reports pre-existing unrelated formatting debt in src/runner/core_executor.rs; targeted rustfmt was applied to every changed Rust file."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[active_surface_guard_rows_s0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-GUARD-ACTIVE-SURFACE-ROWS-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape guard-handler rehome"
implementation_permission = false
decision = "Move the eight existing active-surface row handlers into one private Python sibling so the parent guard stays below the source-size boundary. Preserve the shell entrypoint, registry row/profile/cmd contract, tombstone validation, current-row dispatch, diagnostics, and all handler results; delete no guard and add no new registry row."
source_authority = "The existing registered shell entrypoint, guard_rows.toml row, active card, and active helper's finite current-row dispatch. This refactor introduces no compiler or semantic authority."
canonical_issuer = "The existing mir_call_d1b_active_surface_guard.py dispatch remains the sole guard entry and result owner; the sibling only rehomes handlers."
non_authority = [
  "guard age, profile absence, LOC alone, historical phase names, inactive status, compiler/source/test behavior, rewrite/known parity, fixtures, fallback, retry",
]
fail_fast_boundary = "Preserve shell no-argument rejection, registry id/profiles/cmd, historical tombstone checks, current-row dispatch, success tag, and handler failure behavior. Stop on any changed guard result, public entrypoint, or source-size boundary crossing."
census_boundary = "mir_call_d1b_active_surface_guard.py shared helpers/registry/tombstone checks/main dispatch -> eight existing row handlers -> same card/state evidence; includes proof, GC, RawRoot, ScriptRoot, RawLegacy, type-fact, and ordinary-new handlers; excludes method-corridor sibling, compiler/Rust source/tests, guard deletion, and registry changes."
finite_inventory = [
  "parent shared owner: tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "handlers to rehome: check_proof_row, check_cataloged_gc_retire_i0, check_raw_root_resume, check_script_root_ret0, check_raw_legacy_resume, check_raw_legacy_i0, check_type_fact_guard_prune_s0, check_ordinary_new_i0",
  "new private sibling: tools/checks/lib/mir_call_d1b_active_surface_rows.py",
  "stable entry: tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "stable registry: tools/checks/guard_rows.toml",
]
finite_states = [
  "RehomedStable: parent and sibling are each below 760 lines and every handler returns the prior result",
  "DispatchDrift: current row, entrypoint, registry, tombstone, or output contract changes; stop",
  "BoundaryExceeded: parent or sibling reaches 760 lines (800 hard stop); return to design_stop",
  "UnexpectedCaller: a handler is imported or called outside the finite dispatch; stop and re-audit",
]
ordered_tasks = [
  "S0-A: freeze the eight-handler/parent/shared-contract census and keep all existing guard deletion candidates at Keep/Rehome/Parked",
  "S0-B: add the private rows sibling and move the eight handlers with shared helpers referenced through the loaded parent API",
  "S0-C: keep the shell and registry untouched, dispatch the selected row through the sibling, and compare the current parent design-stop result",
  "S0-D: run py_compile, active-surface/pointer guards, line-budget and diff checks; close out to the parent rewrite/known design stop",
]
acceptance = "The parent and sibling are both below 760 lines, no file reaches 800, the shell entrypoint/registry/tombstone/output contracts are unchanged, all eight handlers are defined only in the sibling and reached through the same finite dispatch, the active guard passes in the selected row and after parent closeout, and no guard is deleted or compiler behavior changed."
no_safe_slice = "Return to design_stop if a handler needs new semantics, a second registry/shell entry, a public API, compiler/source/test edits, a changed guard result, a non-finite caller, or the 760/800 line boundary is crossed."
non_claims = "No SupersededDelete, no guard-family retirement, no new guard registry row, no compiler/Rust/VM/backend/JSON/Call changes, no rewrite/known parity closure, and no fallback/retry change."
allowed_files = [
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_rows.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh",
  "tools/checks/guard_rows.toml",
  "tools/checks/lib/mir_call_d1b_method_corridor_guard.py",
  "compiler/Rust source/tests/VM/backend/JSON",
  "guard deletion or new registry rows",
]
focused_test_filters = []
changed_test_names = []
implementation_commit = "924c7bbd55"
focused_test_runs = [
  "No compiler or Rust test files changed; this BoxShape row is guard-only",
]
guard_runs = [
  "python3 -m py_compile tools/checks/lib/mir_call_d1b_active_surface_guard.py tools/checks/lib/mir_call_d1b_active_surface_rows.py: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass while S0 selected",
  "bash tools/checks/mir_call_d1b_cataloged_affine_loan_lifecycle_guard.sh: pass while S0 selected",
  "bash tools/checks/current_state_pointer_guard.sh: pass after closeout",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass after closeout",
  "git diff --check: pass",
]
evidence = "At HEAD 924c7bbd55, the eight existing active-surface row handlers were moved verbatim into tools/checks/lib/mir_call_d1b_active_surface_rows.py. The stable parent guard now owns shared helpers, registry/tombstones, and finite dispatch (369 lines); the private sibling is 616 lines. The shell entrypoint, guard_rows.toml row/profile/cmd, tombstone inventory, diagnostics, and handler contracts are unchanged. The selected S0 guard and stable shell passed before closeout; the parent design-stop guard and pointer guard passed after closeout. A read-only worker audit found no additional SupersededDelete candidate."
red_classification = "No current-change red. This was a behavior-neutral Python BoxShape split with no compiler, Rust, VM, backend, fixture, or test changes; any unrelated baseline red remains outside this row."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_dead_text_callshape_s0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-GLOBAL-TARGET-DEAD-TEXT-CALLSHAPE-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
ceremony_tier = "T0"
box_kind = "BoxShape behavior-neutral owner split"
implementation_permission = false
decision = "Move only the private match_dead_text_len_call matcher out of the 790-line dead-text region-plan owner into a small sibling module. Preserve the existing module path, public API, MIR metadata, and matching behavior; this row adds no semantic shape and issues no target."
source_authority = "Existing MirInstruction/Callee shapes and the existing StringDeadTextRegionPlan refresh path; the split introduces no new authority."
canonical_issuer = "Existing StringDeadTextRegionPlan refresh functions remain the sole plan producer."
non_authority = [
  "names, symbols, arity inference, resolver/registry lookup, generated targets, env, fixtures, fallback, retry, rewrite/known parity, Call schema, VM/backend",
]
fail_fast_boundary = "The finite split must preserve the parent module path and API, keep every existing caller on the same matcher, keep all touched Rust sources below 760 lines (800 hard stop), and stop on any behavior, metadata, or caller drift."
census_boundary = "match_dead_text_len_call definition -> its four existing dead-text plan callers -> src/mir/mod.rs module declaration; includes Method/Extern/Global length shapes and the existing refresh test, excludes rewrite/known semantics, Call schema, backend, and guard retirement."
finite_inventory = [
  "matcher: src/mir/string_dead_text_region_plan.rs::match_dead_text_len_call",
  "callers: const_len, is_len_value_for_source, match_exit_return, text_content_observed_after_loop",
  "module owner: src/mir/mod.rs::string_dead_text_region_plan",
  "existing proof: src/mir/string_dead_text_region_plan.rs::refresh_function_detects_substring_concat_dead_text_region",
]
finite_states = [
  "SplitStable: sibling matcher is private crate-internal, all callers are rewired, and behavior/API/metadata are unchanged",
  "DriftDetected: any caller, shape, public surface, or metadata result changes; stop before closeout",
  "BudgetExceeded: a touched/new Rust source reaches 760 lines or more; return to design_stop",
]
ordered_tasks = [
  "S0-A: record the finite matcher/caller census and preserve the parent rewrite/known parity stop",
  "S0-B: add src/mir/string_dead_text_call_shape.rs and move the matcher without semantic edits",
  "S0-C: rewire the four callers and the internal module declaration while preserving the public region-plan module",
  "S0-D: run targeted plan tests, quick library check, line-budget and diff checks, then close out to the parent design stop",
]
acceptance = "The parent dead-text owner is below 760 lines, the new sibling is below 760 lines, all four callers resolve through the sibling matcher, public module paths and MIR metadata are unchanged, the existing focused plan test and quick library check pass, and no rewrite/known or guard-retirement claim is made."
no_safe_slice = "Return to design_stop if any caller exists outside the finite inventory, the matcher needs a public API or new semantic authority, behavior/metadata changes, a new resolver/target is needed, or the 760/800 source budget is crossed."
non_claims = "No rewrite/known parity closure or writer retirement, no Method/Call schema change, no Global issuer, no package/loan, no VM/backend/JSON change, and no legacy guard deletion."
allowed_files = [
  "src/mir/string_dead_text_region_plan.rs",
  "src/mir/string_dead_text_call_shape.rs",
  "src/mir/mod.rs",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/rewrite/known.rs",
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/calls/build.rs",
  "Call schema",
  "VM/backend/JSON",
  "guard registry/deletion",
]
focused_test_filters = ["string_dead_text_region_plan"]
changed_test_names = []
implementation_commit = "95bbe25701"
focused_test_runs = [
  "rustfmt --edition 2021 --check src/mir/string_dead_text_region_plan.rs src/mir/string_dead_text_call_shape.rs src/mir/mod.rs: pass",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo check --profile quick --lib: pass; existing warnings only",
  "CARGO_BUILD_JOBS=4 CARGO_INCREMENTAL=0 cargo test --profile quick --lib string_dead_text_region_plan -- --test-threads=1: 2 passed; 0 failed; 7521 filtered out",
  "manual finite split structure check: pass",
]
guard_runs = [
  "bash tools/checks/current_state_pointer_guard.sh: pass after closeout",
  "git diff --check: pass",
]
evidence = "At audit HEAD 1e76945679, a read-only audit confirmed one private matcher definition and four local callers. Implementation landed at 95bbe25701: the matcher moved to src/mir/string_dead_text_call_shape.rs, the parent owner fell from 790 to 750 lines, the new sibling is 44 lines, and the public region-plan module/API and MIR metadata path stayed unchanged. The existing focused suite ran 2 tests with 2 passed, and quick library type-check passed with existing warnings only. The active-surface lifecycle guard is intentionally not used while this independent BoxShape row is selected; after closeout the parent design-stop dispatch is green. Legacy guard census found no additional SupersededDelete candidate; guard cleanup stays parked."
red_classification = "No current-change red. Workspace-wide cargo fmt debt remains unrelated; targeted rustfmt passed. Full cargo test/link was not claimed."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

[same_module_rewrite_known_caller_zero_prune_s0_2026_08_30]
status = "landed"
task_id = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-CALLER-ZERO-PRUNE-S0"
parent_row = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0"
design_parent = "MIR-CALL-SAME-MODULE-REWRITE-KNOWN-ISSUER-BOUNDARY-D0"
ceremony_tier = "T0"
box_kind = "BoxShape caller-zero legacy writer prune"
implementation_permission = false
decision = "Delete only the two no-dst Known/unique rewrite chains proven caller-zero in production source. Preserve the two live _to_dst writers, their equals/unified ingress, selector/env behavior, incoming typed Method(Some(receiver)) successor, and every parity-open live result/fault/backend behavior. This row issues no replacement target."
source_authority = "Incoming typed Method(Some(receiver)) remains the existing successor for any live caller; no-dst writer removal relies only on exact source caller-zero evidence."
canonical_issuer = "Existing Method(Some(receiver)) resolver/materializer for retained to-dst callers; the S0 cleanup has no issuer and no semantic receipt."
non_authority = [
  "generated method names, class/method/arity text, header or module lookup, suffix candidates, StaticMethodId parsing, env/origin, fixtures, parity guesses, fallback, retry, and any new Global target",
]
fail_fast_boundary = "The structural caller-zero boundary must fail closed: if any no-dst symbol has a source caller outside rewrite/known.rs, or if a retained to-dst symbol disappears, stop before landing. No runtime effect or argument order is changed by this row."
census_boundary = "src/mir/builder/rewrite/known.rs no-dst writer/helper/wrapper definitions -> all repo Rust callers; includes rewrite_call_args_for_signature, try_known_rewrite[_with_lookup], try_unique_suffix_rewrite[_with_lookup], try_known_or_unique[_with_lookup], retained _to_dst writers, README/API references, and the existing lookup guard; excludes live to-dst semantics, equals, early str-like, Method ABI, Resolved, ordinary-new, CorePlan/provider/thunk, VM/backend, JSON, and Call schema."
finite_inventory = [
  "no-dst helper: src/mir/builder/rewrite/known.rs::rewrite_call_args_for_signature",
  "no-dst Known writer chain: try_known_rewrite and try_known_rewrite_with_lookup",
  "no-dst unique writer chain: try_unique_suffix_rewrite and try_unique_suffix_rewrite_with_lookup",
  "no-dst selector wrappers: try_known_or_unique and try_known_or_unique_with_lookup",
  "retained live writers: try_known_rewrite_to_dst_with_lookup, try_unique_suffix_rewrite_to_dst_with_lookup, try_known_or_unique_to_dst_with_lookup",
  "production callers: unified_emitter.rs and rewrite/special.rs only for the retained _to_dst family",
  "reusable guard owner: tools/checks/lib/mir_call_d1b_rewrite_known_guard.py dispatched by the existing active-surface guard",
]
finite_states = [
  "CallerZeroDelete: no-dst symbols have no source Rust caller outside known.rs; delete in this S0",
  "LiveToDstRetain: _to_dst writers and their production callers remain present",
  "ExternalCaller: any non-known.rs source caller is a NoSafeSlice",
  "ParityOpenRetain: result/fault/backend parity for live writers remains open and is not changed",
]
ordered_tasks = [
  "S0-A: record the exact source caller scan and retain the live _to_dst/equals boundary",
  "S0-B: remove the no-dst helper, two writer chains, and no-dst selector wrappers; update the module README and lookup-only guard without changing runtime behavior",
  "S0-C: run structural caller-zero/live-retention checks plus a quick library check and existing header-lookup focused tests",
  "S0-D: close out with the parent design stop; keep the live rewrite/known parity I0 blocked until its exhaustive matrix is green",
]
acceptance = "All no-dst symbols and their internal wrappers are absent from source, every retained _to_dst writer and its unified/special caller remains, no new resolver/issuer/target or selector change appears, source files stay below 760/800, the reusable active-surface guard proves the boundary, and quick compile plus existing header-lookup tests pass."
no_safe_slice = "Do not land if a production or test Rust caller outside known.rs exists, a helper is shared with the retained _to_dst family, a selector/env/default/argument/result/fault/backend behavior changes, a new target issuer or terminal is needed, the parity-open live route must be altered, or the 760/800 boundary is crossed."
non_claims = "No live to-dst retirement, no parity closure, no Method(Some) or Method ABI change, no qualified/static/Resolved/ordinary-new/CorePlan/provider/thunk work, no package/loan, VM/backend/JSON, Call schema, fallback, retry, or guard-registry deletion."
allowed_files = [
  "src/mir/builder/rewrite/known.rs",
  "src/mir/builder/rewrite/README.md",
  "tools/checks/lib/rewrite_header_p0_guard.py",
  "tools/checks/lib/mir_call_d1b_rewrite_known_guard.py",
  "tools/checks/lib/mir_call_d1b_active_surface_guard.py",
  "docs/development/current/main/CURRENT_STATE.toml",
  "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md",
  "docs/development/current/main/investigations/mir-call-core-r6-d1b-method-none-manifest-2026-08-25.toml",
]
forbidden_files = [
  "src/mir/builder/calls/unified_emitter.rs",
  "src/mir/builder/rewrite/special.rs",
  "src/mir/builder/calls/method_resolution.rs",
  "src/mir/builder/calls/build.rs",
  "VM/backend/JSON",
  "Call schema",
]
focused_test_filters = [
  "header_lookup",
]
changed_test_names = []
coverage_base_commit = "13b5a3e6e0"
implementation_commit = "fb5a4e0e81"
focused_test_runs = [
  "cargo check --profile quick --lib -j1: pass",
  "cargo test --profile quick --lib header_lookup -- --test-threads=1: pass",
  "python3 tools/checks/lib/rewrite_header_p0_guard.py: pass",
  "python3 tools/checks/lib/mir_call_d1b_active_surface_guard.py .: pass",
  "git diff --check: pass",
]
guard = "Existing mir-call-d1b-cataloged-affine-loan-lifecycle entry dispatches the reusable rewrite-known caller-zero checker; no new registry row is added."
evidence = "At HEAD 13b5a3e6e0, source-only rg showed no production/test Rust caller outside rewrite/known.rs for the no-dst chains, while unified_emitter.rs and rewrite/special.rs retain the _to_dst family. Implementation landed at fb5a4e0e81: the no-dst helper and wrappers were deleted, known.rs is 401 lines, and the live parity-open writers remain unchanged."
reopen_trigger = "Reopen only if a new source caller for a removed no-dst symbol appears, or if the retained _to_dst family changes owner; live parity reopening remains owned by MIR-CALL-SAME-MODULE-REWRITE-KNOWN-ISSUER-BOUNDARY-D0."
next_execution = "MIR-CALL-SAME-MODULE-ALL-PRODUCER-DISPOSITION-R0__design_stop"

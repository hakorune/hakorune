# Current State SSOT
#
# Purpose:
# - Keep the active lane / blocker / phase pointers in one small file.
# - Let tools/checks/current_state_pointer_guard.sh catch stale mirror text.
# - Do not store history here; phase docs and investigations own history.

status = "Active"
date = "2026-04-22"

active_lane = "phase-292x .inc codegen thin tag cleanup"
active_lane_status = "active; .inc analysis-debt no-growth guard landed; array_rmw_window and array_string_len_window len-only MIR-owned route tags landed; next source-reuse len modes"
active_phase = "docs/development/current/main/phases/phase-292x/README.md"
method_anchor = "docs/development/current/main/phases/phase-292x/292x-90-inc-codegen-thin-tag-design-brief.md"
taskboard = "docs/development/current/main/phases/phase-292x/292x-94-array-string-len-window-route-card.md"

current_blocker_token = "array_string_len_window source-reuse modes must move to MIR-owned route metadata before C analyzer deletion"
pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

opened_successors = [
  "phase-290x ArrayBox surface catalog landed",
  "phase-291x CoreBox surface catalog active",
  "StringBox surface catalog first implementation slice landed",
  "MapBox surface catalog first implementation slice landed",
  "CoreBox legacy cleanup first safe deletions landed",
  "MapStd prelude cleanup landed",
]

blocked_successors = [
  "phase-137x perf reopen without app blocker evidence",
  "StringBox lastIndexOf two-arg implementation in the catalog commit",
  "MapBox alias normalization or return-contract changes mixed into the first MapBox catalog slice",
  "std.string sugar treated as semantic owner",
  "apps.lib.boxes.map_std reintroduced as a second Map surface owner",
]

stale_pointer_patterns = [
  "ArrayBox.insert(index, value) = missing runtime surface",
  "ArrayBox.remove(index) = runtime exists, but std/docs/smoke surface is still thin",
  "phase-137x-H owner-first optimization return",
  "active app slice: `apps/kilo_nyash/enhanced_kilo_editor.hako`",
  "phase-290x ArrayBox surface canonicalization active",
  "current-state token: `phase-290x ArrayBox surface canonicalization`",
  "StringBox surface truth is split across runtime surface, dispatch, std sugar, and docs",
  "current implementation focus: `.hako` MapBox extended-route cleanup decision after safe stub deletions",
  "active cleanup card is `docs/development/current/main/phases/phase-291x/291x-95-mapbox-hako-extended-route-cleanup-card.md`",
]

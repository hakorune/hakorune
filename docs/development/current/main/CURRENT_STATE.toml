# Current-state pointer only. Historical task paths and landed prose live in
# the archive index and git history; this file selects the live lane.
status = """
The final S6C TextEq corridor reuses the existing subject/needle ExactText
Residence roots and represents V9 only as a derived UTF-8 scalar slice.
Predicate/index physical I0 and the caller-zero DraftSeal ingress probe are
landed: one ByteLen, one private byte-offset PHI, canonical source-index
Binding SSA, and atomic unpublished discard. Lifecycle identity/materializer
I0 is also landed caller-zero; it holds the exact exit-set borrow and tests
operand -> Residence.finish -> Return ordering without emitting lifecycle MIR.
The canonical-consumer audit closed as NoSafeSlice. The borrowed admission
cannot share an owned envelope with its exit set. The plan-less ingress and
function-owned frame timing I0 are landed caller-zero; the ABI I0 and accepted
lifecycle-carrier I0 are also landed:
one private affine Enter/Finish physical carrier feeds the canonical
unpublished session. The DraftSeal consumer D0 is accepted: the old
single-normal-landing finish capability/state must be replaced by one affine
aggregate consumed once over the existing explicit exit set, with operand ->
Finish -> Return ordering and whole-draft discard on late failure. The selected
backend is the contract-bound LLVM 18 C path, but no lifecycle-bearing module
reaches its target-machine boundary yet. The caller-zero ExactTwo ingress now materializes one real unpublished lifecycle candidate; a caller-zero textual LLVM fixture consumes the carrier; this is not a target-machine candidate. The accepted carrier D0 fixes one versioned transport and source-issued root mapping. The carrier transport I0 is landed caller-zero with strict Rust-side carrier JSON, source-backed root-map tests, and trap-Finish rejection. The C lowering fixture is landed; the separate post-transform no-unwind observer, backend production, fallback, and performance remain closed.
"""
date = "2026-08-19"
development_branch_ssot = "main"
development_branch_cutover = "main remains the canonical integration target; current TextEq work is on codex/dynamic-v2-aot-activation and is not yet integrated into main; do not claim main closeout before an explicit integration step"
current_design_stop = "accepted: the existing pure-first contract-bound C route is the sole selected consumer; first extract only call/mir_call from the 826-line generic dispatch without changing behavior"
work_mode = "fast"
current_execution_row = "TEXT-FORMAL-PINNED-RESIDENCE-SELECTED-C-DISPATCH-SPLIT-R0"
current_blocker_token = "Behavior-neutral BoxShape only: move the existing call/mir_call arms once into a textual child include, preserve cleanup/GEN_ABORT/unknown-op order, and keep both parent and child below 760 lines. PinnedTextOp and Enter/Trap/Finish lowering remain closed until this row lands."
finite_product_goal = "MIRBUILDER-FINAL-PIPELINE-v1"
mirbuilder_north_star = "docs/development/current/main/design/mirbuilder-final-pipeline-ssot.md"
ai_verifiable_development_north_star = "docs/development/current/main/design/ai-verifiable-development-north-star-ssot.md"
ai_verifiable_development_status = "accepted long-term policy; diagnostic/query/repair capability activation remains 0 and current lane is unchanged"
active_lane = "S6C Text Residence selected C generic-dispatch BoxShape"
active_lane_status = """
The source/Facts/Recipe/Join/Completion corridor and caller-zero correctness
route remain closed substrate. The fast route uses the existing subject and
needle ExactText Residence roots; V9 is only a derived scalar slice.
Predicate/index physical I0 and the DraftSeal ingress probe are landed with
canonical source-index SSA, private byte-offset PHI, ByteLen, and unpublished
discard. Lifecycle identity/materializer I0 is caller-zero and emits no MIR.
The selected plan-less ingress and function-owned frame timing I0 are landed
caller-zero. Focused tests, cargo check, fmt, and the reusable structure guard
are green. ABI finish returns only on status 0 and aborts on nonzero. The
lifecycle-carrier I0
is landed caller-zero: a private affine Enter Normal/Trap carrier and
success-only Finish marker connect through the canonical unpublished session;
PreparedFunctionExitSetV1 stays the sole exit inventory and
FunctionDraftSealProjectionV1 the sole Return emitter. The DraftSeal consumer
D0 is accepted and its bounded caller-zero I0 is now landed: one move-only
aggregate is consumed once over the existing explicit exit set. Selected
Dynamic remains the sole production arm. The selected backend is the
contract-bound LLVM 18 C path, but no lifecycle-bearing module reaches its
target-machine boundary. The accepted D0 fixes the versioned carrier and
source-issued root mapping. The Rust transport/guard and C inspection fixture
are caller-zero only, not production. The C-lowering fixture is inspection-only,
not a target-machine candidate. EntryBlockDrift was closed by E0/E1: E0 owns Enter,
E1 owns the physical sidecar and initial seed/cursor. Function-owned
frame timing is landed: the plan-less ingress is consumed once and the frame
issuer reads the canonical function plan table after the three S6C leaf rows.
The next bounded design stop selected explicit PinnedTextResidenceTrap as a new
vocabulary BoxCount. Its
compiler-only I0 is now landed caller-zero: the canonical CFG owner co-writes
Enter plus a successorless Trap, seals the trap block, and rejects
foreign/rejoin/non-empty shapes; canonical SSA also rejects a foreign carrier
owner. The owned Finish consumer I0 is now also landed caller-zero: the exact
exit set is owned by the admission, the pinned consumer owns that admission
plus Finish, and one ordered projection callback replaces the old three-
callback seam. The OpenFunctionDraftSeal integration D0 is accepted. The
detached Finish writer I0 is now landed caller-zero: canonical CFG owns the
detached writer, the projection consumes the same affine consumer, and focused
tests prove Finish -> existing Return while the live function stays unchanged.
The caller-zero ExactTwo ingress now finalizes the frame from the
function-owned plan table, co-writes Enter/Trap before canonical-session close,
seals the deferred E1/cursor graph, and proves the detached Finish -> Return
projection. The real-candidate JSON cell is now landed caller-zero: one carrier lineage is installed on the exact detached candidate and strict JSON emits 3
PinnedTextOp, 1 Enter, 1 Trap, 2 Finish, and 2 Return rows without refresh or
synthetic-fixture promotion. The selected C owner is accepted; the active fast
row extracts call/mir_call from the 826-line dispatch before lowering. Target-machine,
observer, production, fallback, and performance remain closed.
"""
active_phase = "docs/development/current/main/investigations/mirbuilder-inplace-replacement0-task-map-2026-07-28.md"
phase_status = "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md"
method_anchor = "docs/development/current/main/design/mirbuilder-inplace-replacement-policy-ssot.md"
taskboard = "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md"
latest_workstream_card = "docs/development/current/main/workstreams/mirbuilder-inplace-replacement-current.md"
mirbuilder_inplace_replacement_policy = "docs/development/current/main/design/mirbuilder-inplace-replacement-policy-ssot.md"
mirbuilder_inplace_replacement_task_map = "docs/development/current/main/investigations/mirbuilder-inplace-replacement0-task-map-2026-07-28.md"
current_execution_design = "docs/development/current/main/design/loop-common-physical-demand-and-session-ssot.md"
g2_smoke_caller_census = "docs/development/current/main/investigations/llvmlite-shared-smoke-caller-census-v0.json"
latest_card = "loop-common-physical-demand-and-session-ssot"
latest_card_path = "docs/development/current/main/design/loop-common-physical-demand-and-session-ssot.md"
latest_card_summary = "The S6C source corridor through ExactTwo lifecycle ingress and real-candidate strict JSON is landed caller-zero. The selected C owner is fixed to the existing contract-bound pure-first route. The active fast row only extracts call/mir_call from the 826-line generic dispatch; lifecycle lowering, target-machine observation, production, fallback, and performance remain closed."
next_design_card = "none"
next_design_card_path = "docs/development/current/main/design/loop-common-physical-demand-and-session-ssot.md"
next_execution_card = "TEXT-FORMAL-PINNED-RESIDENCE-SELECTED-C-DISPATCH-SPLIT-R0"
next_execution_card_path = "docs/development/current/main/design/loop-common-physical-demand-and-session-ssot.md"
parked_cleanup_task = "docs/development/current/main/investigations/mirbuilder-cleanup-retirement0-d0-task-map-2026-08-04.md"
parked_mirbuilder_hako_mimalloc_promotion_gate = "docs/development/current/main/investigations/mirbuilder-hako-mimalloc-promotion-gate0-task-2026-08-10.md"
parked_llvmlite_graduation_task = "docs/development/current/main/investigations/llvm-native-library-llvmlite-graduation-task-2026-07-22.md"
final_convergence_cleanup_ssot = "docs/development/current/main/design/repo-physical-structure-cleanup-ssot.md"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE plus the active card only unless lane/blocker/restart-order/phase-status/policy changes."
pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"
landed_tail = [
  "2026-08-19: Real-candidate JSON carrier-lineage I0 landed caller-zero: the exact detached MirFunction installs one source-issued carrier through a separate projection seam and emits strict JSON without refresh/synthetic promotion; same-cohort 3 PinnedTextOp/1 Enter/1 Trap/2 Finish/2 Return is green, while missing/foreign/duplicate carrier, duplicate/non-entry lifecycle sites, Trap Finish, and exit-set drift reject. Selected C lowering, target-machine, observer, production, fallback, and performance remain closed.",
  "2026-08-19: Trap-terminal I0 landed caller-zero: `PinnedTextResidenceTrap` is the explicit successorless fail-stop for the Enter trap landing, co-written and sealed only by the canonical CFG owner with the same plan provenance; foreign owner, non-empty/terminated/predecessored trap, and rejoin edges reject before/after mutation as appropriate. Runtime/backend lowering and detached Finish projection remain closed.",
  "2026-08-19: Caller-zero frame-timing I0 is landed: the pre-DraftSeal ingress is plan-less and affine, the retained S6C loan is the signature adapter, and the existing frame issuer finalizes exactly once from the canonical function-owned plan table after three leaf rows. Empty/zero-stamp/preinstalled frames reject; focused frame/DraftSeal test, cargo check, fmt, pointer, and structure guard are green. Explicit trap terminal, detached Finish/Return projection, backend, and production remain closed.",
  "2026-08-19: Caller-zero E0/E1 physical entry co-seal is landed: E0 contains Residence Enter only, E1 is the same-cohort sidecar/seed/cursor execution entry, duplicate boundary issuance rejects, and the focused S6C cursor module is 5/5 green; backend candidate, observer, and production remain closed.",
  "2026-08-19: Caller-zero C lowering fixture consumes strict pinned_text_residence_carrier@1 JSON and emits textual LLVM Enter/normal-trap/success-only Finish shape; missing carrier, cohort drift, and trap Finish reject before output publication. Target-machine observer and production remain closed.",
  "2026-08-18: nyash.string.eq_hh remains active generic C/Python compatibility production, is not S6C TextEq authority, and cannot retire before its independent caller-zero cutover.",
  "2026-08-18: Exact concrete StringBox Residence canary now downcasts by runtime type, rejects StringBox-name spoofs before pinning, blocks slot reuse while pinned, and records the mutable-reachability census as a release gate.",
  "2026-08-18: Published ExactText lane-borrow ingress now validates entry pairs once and delegates to the existing invocation Residence owner; ABI and Residence focused gates are green, with no MIR/V10 or production caller.",
  "2026-08-18: S6C base-root admission co-seals the landed source corridor with the existing ExactText sidecar: root 0 is Subject and root 1 is Needle by binding plus logical ordinal, with green 3/3 focused tests; the effect-free cursor/preheader plan and scalar-equality leaf capability are now landed, without ValueId/MIR/Bool/CFG/runtime/production behavior.",
  "2026-08-19: Finish-or-abort ABI I0 landed caller-zero: the old public status symbol retired, one void wrapper returns only on status 0 and fail-stops on every nonzero status, the Rust status core is module-private, and focused tests/cargo check/fmt/ABI guard are green; lifecycle MIR/backend/production remain closed.",
  "2026-08-19: Lifecycle-carrier and DraftSeal consumer I0s are closed caller-zero: the affine Enter/Finish carrier no longer binds one normal landing, canonical SSA tracks per-exit Finish placement, the new Single/ExactTwo consumer enforces operand -> Finish -> existing Return with late-error discard, 5+2 focused tests, cargo check, fmt, pointer, and structure guards are green; backend/production remain closed.",
  "2026-08-19: Detached Finish consumer ownership and ExactTwo ingress I0 landed caller-zero: the pinned consumer owns the validated exact exit set plus Finish as one affine lineage; the function-owned plan table finalizes the frame, canonical E0/E1 co-writes Enter/Trap and cursor reachability, and the detached projection proves operand -> Finish -> existing Return. Six cursor tests, three writer tests, ingress positive/negative coverage, cargo check, fmt, pointer, structure, and ABI guards are green; target-machine routing, observer, production, fallback, and performance remain closed."
]

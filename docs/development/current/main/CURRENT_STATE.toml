# Current State SSOT
#
# Purpose:
# - Keep the active lane / blocker / phase pointers in one compact file.
# - Point to the latest card without copying landed history into every mirror.
# - Let tools/checks/current_state_pointer_guard.sh catch stale pointers.

status = "Active"
date = "2026-06-11"

active_lane = "Hako Mimalloc typed-object exact slot ABI split"
active_lane_status = "User-box counter-heavy optimization stays on the exact-slot ABI split lane, and the adjacent exact-AOT link/startup attribution ladder now lands through explicit probe rows without changing defaults."
active_phase = "docs/development/current/main/phases/phase-296x/README.md"
phase_status = "docs/development/current/main/phases/phase-296x/296x-651-HAKO-MIMALLOC-TYPED-OBJECT-EXACT-SLOT-ABI-SPLIT.md"
method_anchor = "docs/development/current/main/design/hako-mimalloc-performance-parity-ssot.md"
taskboard = "docs/development/current/main/phases/phase-296x/296x-90-mimalloc-benchmark-taskboard.md"
latest_workstream_card = "docs/development/current/main/workstreams/mimalloc-current.md"

latest_card = "296x-651-HAKO-MIMALLOC-TYPED-OBJECT-EXACT-SLOT-ABI-SPLIT"
latest_card_path = "docs/development/current/main/phases/phase-296x/296x-651-HAKO-MIMALLOC-TYPED-OBJECT-EXACT-SLOT-ABI-SPLIT.md"
latest_card_summary = "296x-651 accepts the typed-object exact slot ABI split and now anchors PERF-USERBOX-001..023 plus NYRT-STARTUP-FLOOR-001..004: floor attribution, startup/loader owner split, static-libgcc, minimal system libs, loader/libc floor summary, loader owner split summary, ld-linux / libc loader split, ld-linux / libc loader percent split, ld-linux / libc loader symbol split, startup executable split, startup executable symbol split, startup executable ret0 stack split, startup executable ret0 family split, startup executable ret0 bucket split, startup executable ret0 bucket variability split, startup executable ret0 bucket symbol variability split, startup executable ret0 bucket subkind variability split, startup executable ret0 bucket top-symbol variability split, startup executable ret0 env top-symbol family variability split, startup executable ret0 env exact top-symbol variability split, plugin-host-off, runtime-hooks-off, minimal-startup, bare-entry A/B, runtime-build-off, entry-path-prep-off, and ring0-init-off probes, while defaults stay unchanged."
current_blocker_token = "HAKO-MIMALLOC-TYPED-OBJECT-EXACT-SLOT-ABI-SPLIT-296X-001"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE latest_card/latest_card_path/latest_card_summary plus the active card. Do not update CURRENT_TASK, 05-Restart, 10-Now, phase README, taskboards, or ledgers unless lane/blocker/restart-order/phase-status/durable-policy changes."

pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

landed_tail = [
  "2026-06-11: PERF-USERBOX-018 exposes repeated ret0.exe bucket trials directly on the startup loader report so the startup executable can be read as a distribution before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-019 exposes repeated ret0.exe bucket representative symbols directly on the startup loader report so env / once / path / minimal_main / nyash_kernel_runtime can be read as stable owner signals before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-020 exposes ret0.exe bucket subkind counts directly on the startup loader report so env / once / path / minimal_main / nyash_kernel_runtime can be split into getenv / pathprep / futex / components / registry signals before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-021 exposes ret0.exe bucket top symbols directly on the startup loader report so env / once / path / minimal_main / nyash_kernel_runtime can be split into their exact top-symbol owners before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-022 exposes ret0.exe env top-symbol family counts directly on the startup loader report so env can be split into var_os / getenv / current_exe / current_dir signals before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-023 exposes ret0.exe env exact top symbols directly on the startup loader report so env can be split into current_exe / minimal_startup_enabled_from_env / gc_mode signals before choosing the next owner.",
  "2026-06-11: PERF-USERBOX-014 exposes the leading ret0.exe symbol directly on the startup loader report so the startup executable can be split at symbol granularity without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-015 exposes the leading ret0.exe symbol stack directly on the startup loader report so the startup executable can be split as a small ret0 stack without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-016 exposes the ret0.exe family buckets directly on the startup loader report so the startup executable can be split by family without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-017 exposes the ret0.exe bucket counts directly on the startup loader report so the startup executable can be split by env/path/ffi/alloc/once/string buckets without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-013 exposes the ret0.exe startup executable contribution directly on the startup loader report so the startup executable can be split from the loader floor without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-012 exposes the leading ld-linux and libc symbols directly on the startup loader report so the loader floor can be split at symbol granularity without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-011 exposes ld-linux versus libc percent sums directly on the startup loader report so the loader floor can be split more precisely without reopening the closed loader/libc summary.",
  "2026-06-11: PERF-USERBOX-010 now checks the ld-linux / libc loader split directly on the startup loader report while keeping the loader/libc floor summary closed, so the next owner can move inside the loader floor itself.",
  "2026-06-11: PERF-USERBOX-009 joined the startup-loader owner split with the loader/libc floor summary so the next owner stays loader/libc while dynamic_loader remains the primary startup owner family.",
  "2026-06-11: PERF-USERBOX-002 now reports dynamic_loader as the primary startup-loader owner family once release artifacts are synced, so the next floor can be treated as loader/libc rather than env/stdio/registry.",
  "2026-06-11: NYRT-STARTUP-FLOOR-004 added the ring0-init-off probe so the current minimal NyRT entry can skip Ring0Context initialization once plugin host, runtime hooks, and runtime build are already off, isolating the final remaining entry bootstrap cost.",
  "2026-06-11: PERF-USERBOX-008 added a loader/libc floor summary probe so the exact-AOT link ladder can package the static-libgcc and minimal-system-libs evidence without changing the default link mode.",
  "2026-06-11: NYRT-STARTUP-FLOOR-003 added the entry-path-prep-off probe so the current minimal NyRT entry can skip current_exe / path shaping once plugin host is already off, isolating the remaining loader floor from path-prep cost.",
  "2026-06-11: NYRT-STARTUP-FLOOR-002 added the runtime-build-off probe so the current minimal NyRT entry can skip NyashRuntimeBuilder / GC controller construction in diagnostic mode before env/stdio/registry are split further.",
  "2026-06-11: NYRT-STARTUP-FLOOR-001 added the bare-entry A/B probe so one ret0 ny_main object can be compared through current minimal NyRT entry and bare libc main before splitting env/stdio/registry owners.",
  "2026-06-10: the PERF-USERBOX link/startup attribution ladder was consolidated into a dedicated SSOT, keeping full/default link and NyRT startup behavior unchanged while making the floor probes easy to rerun.",
  "2026-06-10: the NyRT runtime-hooks-off probe was added on top of the link/startup ladder so the next floor can isolate env and stdio costs without changing default runtime behavior.",
  "2026-06-11: the NyRT minimal-startup probe was added so the next floor can bypass default registry startup and keep moving the env/stdio owner inward without changing the default runtime path.",
  "2026-06-09: 296x-651 opened the typed-object exact slot ABI split lane after user-box counter-heavy perf moved the owner to field_get_hii_direct; exact slot routes are now separate from compat field_get_hii.",
  "2026-06-09: 296x-650 opened the MapBox proof-bearing route cleanup lane so metadata-only same-key fusion candidates cannot affect backend lowering without selected RouteDecision evidence.",
  "2026-06-09: 296x-648 selected the next exact front from the direct-exact Hako vs C mimalloc sweep; the workstream now points to the userbox/counter-heavy optimization lane instead of the docs/status cleanup row.",
  "2026-06-09: 296x-647 docs pointer cleanup and mirror-slim landed; the inventory note is the compact map and the restart docs stay thin instead of carrying the long queue inline.",
  "2026-06-09: 296x-97 selected the post-release direct cached-page source MIR refresh as the next current lane after the release direct cached-page keeper measurement; the workstream now points to the select single-page first-page cache keeper instead of the refresh note.",
  "2026-06-09: 296x-73 selected the LD_PRELOAD shim smoke as the next current lane after the decision row landed; the workstream now points to the bench pilot instead of the decision note.",
  "2026-06-09: 296x-72 selected the LD_PRELOAD decision as the next current lane after the explicit measurement row landed; the workstream now points to the shim smoke instead of the measurement note.",
  "2026-06-09: 296x-71 selected the native provider-package explicit measurement as the next current lane after the native fusion pilot; the workstream now points to the LD_PRELOAD decision instead of the pilot note.",
  "2026-06-09: 296x-643 selected secure_entropy_backed_free_list as the next read-only seam inside the unified production allocator API lane; the workstream now points to the secure-entropy-backed free-list selector instead of the facade-only selection note.",
  "2026-06-09: 296x-644 selected page-map aligned huge OSVM facade integration as the next read-only seam inside the unified production allocator API lane; the workstream now points to the page-map/aligned/huge/OSVM facade instead of the secure-entropy-only selector note.",
  "2026-06-09: 296x-645 selected segment_arena_reclaim_tls_unification as the next remaining medium-priority feature gap after the allocator API read-only seams; the workstream now points to the segment-arena reclaim/TLS unification selection instead of the allocator-api-only note.",
  "2026-06-09: 296x-646 opened a read-only segment-arena reclaim/TLS integration owner after the 645 selection row; the workstream now points to the read-only integration owner instead of the selection note.",
  "2026-06-09: 296x-647 opened a docs pointer cleanup and mirror-slim lane so the restart surface can point to an inventory note instead of carrying the long queue history inline.",
  "2026-06-08: docs-slim maintenance landed for mimalloc-current.md, the phase-296x taskboard, and the capability-gap SSOT; DIRECTARRAY-FMEM-COMMON-001 proof/report adapter sidecar is landed and MIM-PORT-FMEM-109 source-syntax smoke manifest runner opens next.",
  "2026-06-08: 296x-641 landed SELFHOST-SURFACE-000 surface check landing: the selfhost surface gate is now report/check-only, outbox is confirmed as a narrow transfer marker, and MIM-PORT-FMEM-031 AtomicRemoteHead CAS lowering producer selection opens next."
]

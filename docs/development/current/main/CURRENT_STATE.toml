# Current State SSOT
#
# Purpose:
# - Keep the active lane / blocker / phase pointers in one small file.
# - Let tools/checks/current_state_pointer_guard.sh catch stale mirror text.
# - Do not store history here; phase docs and investigations own history.

status = "Active"
date = "2026-04-23"

active_lane = "phase-292x .inc codegen thin tag cleanup"
active_lane_status = "active; .inc analysis-debt no-growth guard landed; array/string window analyzers migrated; generic_method.has metadata landed; exact seed route tags through array_getset_micro landed; no hako_llvmc_match_*seed definitions remain; pure_compile_minimal_paths is fully removed; copy-graph helper is deleted; cross-block use API is tightened; GenericPureProgramView and GenericPureBlockView shells are landed; guard is 3 files / 3 debt lines; next cleanup is generic pure walker view extraction"
active_phase = "docs/development/current/main/phases/phase-292x/README.md"
phase_status = "docs/development/current/main/phases/phase-292x/292x-STATUS.toml"
method_anchor = "docs/development/current/main/phases/phase-292x/292x-90-inc-codegen-thin-tag-design-brief.md"
taskboard = "docs/development/current/main/phases/phase-292x/292x-118-generic-pure-walker-view-extraction-card.md"

current_blocker_token = "generic pure walker view extraction"
pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

opened_successors = [
  "phase-290x ArrayBox surface catalog landed",
  "phase-291x CoreBox surface catalog reference lane",
  "StringBox surface catalog first implementation slice landed",
  "StringBox router length, substring, concat, trim, contains, one-arg lastIndexOf, replace, and indexOf/find families plus ArrayBox length, push, slice, get, pop, set, remove, and insert plus MapBox size, len, has, get, and set moved to Unified value path",
  "MapBox surface catalog first implementation slice landed",
  "CoreBox legacy cleanup first safe deletions landed",
  "MapStd prelude cleanup landed",
  "generic_method.has route policy metadata landed",
  "array_string_store_micro exact seed backend route tag landed",
  "concat_const_suffix_micro exact seed backend route tag landed",
  "substring_views_only_micro exact seed backend route tag landed",
  "substring_concat_loop_ascii exact seed backend route tag landed",
  "array_rmw_add1_leaf exact seed backend route tag landed",
  "sum_variant_tag_local exact seed backend route tag landed",
  "sum_variant_project_local exact seed backend route tag landed",
  "userbox_point_local_scalar exact seed backend route tag landed",
  "userbox_flag_pointf_local_scalar exact seed backend route tag landed",
  "userbox_loop_micro exact seed backend route tag landed",
  "userbox_known_receiver_method_seed local/copy exact seed backend route tag landed",
  "userbox_known_receiver_method_seed chain/micro exact seed backend route tag landed",
  "MapBox duplicate receiver predelete fix landed",
  "Hako LL/provider stack overflow predelete fix landed",
  "pure_compile_minimal_paths path #1/#2 deletion landed",
  "pure_compile_minimal_paths Map path #3 deletion landed",
  "pure_compile_minimal_paths Array path #4 deletion landed",
  "pure_compile_minimal_paths String const-eval deletion landed",
  "string_loop_seed copy-graph helper deletion landed",
  "generic pure cross-block use API tightening landed",
  "GenericPureProgramView shell landed",
  "GenericPureBlockView accessor landed",
]

blocked_successors = [
  "phase-137x perf reopen without app blocker evidence",
  "StringBox lastIndexOf two-arg implementation in the catalog commit",
  "MapBox alias normalization or return-contract changes mixed into the first MapBox catalog slice",
  "std.string sugar treated as semantic owner",
  "apps.lib.boxes.map_std reintroduced as a second Map surface owner",
]

stale_pointer_patterns = [
  "ArrayBox.insert(index, value) = missing runtime surface",
  "ArrayBox.remove(index) = runtime exists, but std/docs/smoke surface is still thin",
  "phase-137x-H owner-first optimization return",
  "active app slice: `apps/kilo_nyash/enhanced_kilo_editor.hako`",
  "phase-290x ArrayBox surface canonicalization active",
  "current-state token: `phase-290x ArrayBox surface canonicalization`",
  "StringBox surface truth is split across runtime surface, dispatch, std sugar, and docs",
  "current implementation focus: `.hako` MapBox extended-route cleanup decision after safe stub deletions",
  "active cleanup card is `docs/development/current/main/phases/phase-291x/291x-95-mapbox-hako-extended-route-cleanup-card.md`",
  "move generic method route policy to MIR metadata",
  "hako ll/provider stack overflow blocks minimal path deletion",
  "retry pure_compile_minimal_paths #1/#2 deletion",
  "pure_compile_minimal_paths Map/Array path cleanup",
  "pure_compile_minimal_paths String const-eval decision",
  "guard is 4 files / 7 debt lines",
  "remaining 7 guard lines",
  "guard is 3 files / 4 debt lines",
  "3 `.inc` files / 4 analysis-debt lines",
]

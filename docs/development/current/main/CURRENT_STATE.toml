# Current State SSOT
#
# Purpose:
# - Keep the active lane / blocker / phase pointers in one compact file.
# - Point to the latest card without copying landed history into every mirror.
# - Let tools/checks/current_state_pointer_guard.sh catch stale pointers.

status = "Active"
date = "2026-06-20"

active_lane = "RUST-SUBSET-TO-HAKO-APP-FRONT compiler construction / selfhost app front"
active_lane_status = """Exact-AOT fastpath optimization is paused: no fresh
compiler optimization owner is selected. Rust VM / .hako VM product-route app
validation is retired; EXE/AOT is the primary app/selfhost validation route.
The active app front is rust-subset-to-hako, with build crate split planning
available but paused while this app-front lane is active. The focused
scan-methods compiler slice, touched-module string-corridor regression, PHI
input rematerialization operand memo slice, and string-corridor stable-length
hint fallback retirement are green. The rust-subset app-front smoke is green
again after reopening the `JsonParser.parse_array` loop(true) shape through
loop_true_break_continue. A dedicated syn-adapter smoke entry is also green.
The Index expression app-front slice is green through Python reference,
syn-adapter handoff, and `.hako` EXE/AOT converter parity. Loop
`break`/`continue` is now fixture-guarded as an explicit Unsupported handoff,
without changing compiler Recipe/CorePlan semantics. Generic Rust function
skeleton transport is green as type-spelling preservation only. The unsupported
trait handoff is hardened with RustSubset JSON and `.hako` EXE/AOT fixture
parity. The rust-subset smoke script is table-driven so future fixture rows do
not duplicate MIR/EXE/adapter boilerplate. Crate/multi-file handoff is now
inventoried as an external-adapter boundary concern before any `creat`-style
pilot. The module schema validation parity gap is closed. RustSubset path/name
normalization is now closed without adding `.hako` syntax: the adapter owns
source_name/emitted_name, tuple field `_0`/`_1`, Hako reserved-name escaping,
structured path provenance, duplicate emitted-name fail-fast, and
simple-pattern-only handoff. The crate manifest v0 contract is accepted as a
transport index plus per-module `RustSubsetModule` artifacts. The synthetic
multi-module adapter probe and `.hako` crate handoff MIR acceptance wrapper are
green. OrderedMapBox is now documented as a `.hako` library collection for
deterministic String-key iteration, not a MapBox or ring0 substrate change. The
small OrderedMapBox v0 library and EXE/AOT smoke are green. A focused
BindingContext-style OrderedMapBox probe is also green without rewriting
MirBuilder, changing MapBox, or touching ring provider registration. The
constructor lifecycle boundary is guarded separately: OrderedMap keeps explicit
v0 create-time initialization while a focused birth/field-initializer EXE/AOT
probe verifies constructor args and per-instance default state. Imported static
factory field-initializer routes now preserve normal `new -> birth` lifecycle
semantics through the app import bundle path. Unknown global-callee diagnostics
now keep the stable reason string while adding callee-aware detail and
next-check hints. The creat-style pilot selection is closed with a focused
crate inventory tool. The `hakorune_box_core` pilot bundle is checked in, and
generated top-level function skeletons are accepted as declarations for MIR
emit without enabling Rust name resolution or generated-program execution. The
next crate pilot is selected as a focused `hakorune_mir_core` slice:
`crate::control_ids` plus `crate::types`. That selected slice is now checked in
and reaches generated-skeleton MIR emit. Crate-wrapper EXE is unblocked by
recovering user-box receiver origin from global factory route result box names;
pure-route unsupported-shape diagnostics now report callee and receiver-origin
detail; the next blocker is pinning the crate-wrapper EXE route as a stable
smoke. The crate-wrapper EXE smoke is now pinned; the next blocker is selecting
the next app-front or crate pilot task. The next selected pilot is a focused
`hakorune_mir_core` ID-module slice: `basic_block_id`, `binding_id`, and
`value_id`. Tuple-struct constructor expressions, compound assignment, and
Self-qualified calls are skeleton-safe. The selected ID-module bundle is now
checked in and guarded through generated-skeleton MIR emit and wrapper EXE
parity. The next selected app-front task is materializing the
`hakorune_mir_core::value_kind` single-module slice. That value_kind bundle is
now checked in and guarded through generated-skeleton MIR emit, wrapper EXE
parity, and the full rust-subset smoke. The next selected blocker is making
enum variant value references skeleton-safe after `crate::effect` exposed
`Effect_Mut` as an undefined generated symbol. Enum variant value references
are now skeleton-safe as unsupported expression handoffs; `crate::effect` now
advances to the next first failure, `Vec_new`. `Vec::new()` calls are now
skeleton-safe as unsupported expression handoffs, and `crate::effect` reaches
generated-skeleton MIR emit. The `hakorune_mir_core::effect` bundle is now
checked in and guarded through generated-skeleton MIR emit, wrapper EXE parity,
and full rust-subset smoke. The next selected task is materializing the
`hakorune_mir_builder::binding_context` single-module bundle because it already
reaches generated-skeleton MIR emit and is directly relevant to MirBuilder
migration. That binding_context bundle is now checked in and guarded through
generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.
The next selected task is materializing the
`hakorune_mir_builder::variable_context` single-module bundle because it
already reaches generated-skeleton MIR emit and is directly relevant to
MirBuilder variable-map / SSA carrier migration. That variable_context bundle
is now checked in and guarded through generated-skeleton MIR emit, wrapper EXE
parity, and full rust-subset smoke. The next blocker is selecting the next
app-front task before implementation. The next selected blocker is making
Rust associated function calls skeleton-safe after `BindingId::new` and
`CallFlags::new` exposed unresolved generated globals. Associated function
calls are now explicit Unsupported handoffs instead of unresolved globals;
`hakorune_mir_builder::core_context` reaches generated-skeleton MIR emit and
`hakorune_mir_defs::call_unified` advances to `EffectMask_IO`. The next blocker
is selecting the next app-front task before implementation. The next selected
blocker is making type-qualified associated const/value paths skeleton-safe
after `EffectMask::IO` exposed `EffectMask_IO` as an undefined generated
variable. Associated const/value paths are now explicit Unsupported handoffs
instead of undefined variables; `hakorune_mir_defs::call_unified` reaches
generated-skeleton MIR emit. The next blocker is selecting the next app-front
task before implementation. The next selected task is materializing the
`hakorune_mir_builder::core_context` single-module bundle because it already
reaches generated-skeleton MIR emit and is directly relevant to MirBuilder ID
generation migration. That core_context bundle is now checked in and guarded
through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset
smoke. The next blocker is selecting the next app-front task before
implementation. The next selected task is materializing the
`hakorune_mir_builder::context` single-module bundle because it is the
smallest remaining green builder-owned context candidate after core_context.
That context bundle is now checked in and guarded through generated-skeleton
MIR emit, wrapper EXE parity, and full rust-subset smoke. The next blocker is
selecting the next app-front task before implementation. The next selected
task is materializing the `hakorune_mir_defs::call_unified` single-module
bundle because it now reaches generated-skeleton MIR emit after prior
skeleton-safety blockers were closed. That call_unified bundle is now checked
in and guarded through generated-skeleton MIR emit, wrapper EXE parity, and
full rust-subset smoke. Enum payload comments are now aligned between the
Python reference and `.hako` converter. 296x-1362 selects generic impl target
skeleton-safety as the next blocker after `metadata_context` exposed
`MetadataContext<SpanT, RegionIdT>_new` as an invalid generated function name.
Generic impl target emitted names are now parser-safe without adding generic
semantics, and `metadata_context` advances to the existing
`Option<&str>`/closure handoff boundary. 296x-1364 selects reference type
spelling skeleton-safety as the next blocker because both `metadata_context`
and `type_context` fail first on `Option<&str>` while closure remains an
unsupported expression handoff. Reference type spelling is now parser-safe;
`type_context` reaches generated-skeleton MIR emit and `metadata_context`
advances to the undefined `Self` value boundary. 296x-1366 selects
`hakorune_mir_builder::type_context` materialization as the next task because
it is the next green MirBuilder-owned context module."""

active_phase = "docs/development/current/main/phases/phase-296x/README.md"
phase_status = "docs/development/current/main/phases/phase-296x/296x-90-mimalloc-benchmark-taskboard.md"
method_anchor = "docs/development/current/main/design/hako-app-front-boundary-template-ssot.md"
taskboard = "docs/development/current/main/phases/phase-296x/296x-90-mimalloc-benchmark-taskboard.md"
latest_workstream_card = "docs/development/current/main/phases/phase-296x/296x-1367-HAKORUNE-MIR-BUILDER-TYPE-CONTEXT-MATERIALIZATION-001.md"

latest_card = "296x-1367-HAKORUNE-MIR-BUILDER-TYPE-CONTEXT-MATERIALIZATION-001"
latest_card_path = "docs/development/current/main/phases/phase-296x/296x-1367-HAKORUNE-MIR-BUILDER-TYPE-CONTEXT-MATERIALIZATION-001.md"
latest_card_summary = """296x-1367 materializes the
hakorune_mir_builder::type_context single-module RustSubset bundle."""

current_blocker_token = "HAKORUNE-MIR-BUILDER-TYPE-CONTEXT-MATERIALIZATION-001"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE plus the active card only unless lane/blocker/restart-order/phase-status/policy changes."

pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

landed_tail = [
  "2026-06-19: rust-subset source-shape coverage batch is closed; source transport waits for a new real input blocker.",
  "2026-06-19: generic_loop_v1 accepts scanner multi-exit loop bodies via ThenOnlyExit.",
  "2026-06-19: continue-in-staged-loop is promoted green through edge-local ContinueWithPhiArgs.",
  "2026-06-19: compiler loop tasks are re-aggregated around route-owner debt and B-lite loop resolver shadowing.",
  "2026-06-19: B-lite loop resolver shadow is wired; it remains an observer, not a selection owner.",
  "2026-06-19: registry candidate suppression is selected as the first route-retire target, gated by focused evidence.",
  "2026-06-19: scan-methods focused blocker moves from loop-route ambiguity to compile-time owner reduction.",
  "2026-06-19: def-map sharing, dense DCE/string-corridor containers, typed-object query contexts, and route-flow memoization reduce the focused timeout.",
  "2026-06-19: 296x-1304 closes the focused scan-methods command with result=0.",
  "2026-06-19: 296x-1305 closes the touched-module `string_corridor_sink` regression; next blocker is PHI input remat operand memoization.",
  "2026-06-19: 296x-1306 closes PHI input remat operand memoization; next blocker is string-corridor stable-length hint fallback retirement.",
  "2026-06-19: 296x-1307 retires stable-length hint parsing as correctness evidence; next blocker is loop actual-selection trace.",
  "2026-06-19: 296x-1308 restores rust-subset app-front smoke through loop_true_break_continue ExitAllowed coverage for parse_array-class loop(true) shapes.",
  "2026-06-19: 296x-1309 adds a dedicated syn-adapter smoke wrapper; converter core remains separate from Rust parser/input route ownership.",
  "2026-06-19: 296x-1310 adds RustSubset Index expression transport for xs[i] through Python reference, syn adapter, and .hako EXE/AOT converter parity.",
  "2026-06-19: 296x-1311 fixture-guards loop break/continue as an explicit Unsupported handoff; compiler Recipe/CorePlan semantics are unchanged.",
  "2026-06-19: 296x-1312 fixture-guards generic function skeleton transport as type-spelling preservation only; generic semantics are not claimed.",
  "2026-06-19: 296x-1313 hardens unsupported trait handoff with RustSubset JSON and .hako EXE/AOT parity; trait semantics are not claimed.",
  "2026-06-19: 296x-1314 refactors rust-subset smoke fixture handling into table-driven runners; behavior is unchanged.",
  "2026-06-19: 296x-1315 inventories crate/multi-file handoff boundaries before any creat-style pilot; next P0 gaps are validation parity and path/name normalization.",
  "2026-06-19: 296x-1316 aligns `.hako` module schema validation with the Python reference; next P0 gap is path/name normalization.",
  "2026-06-20: 296x-1317 closes RustSubset path/name normalization; next blocker is crate manifest v0 contract.",
  "2026-06-20: 296x-1318 accepts RustSubset crate manifest v0; next blocker is synthetic multi-module syn-adapter probe.",
  "2026-06-20: 296x-1319 adds synthetic multi-module syn-adapter probe; next blocker is crate handoff MIR acceptance.",
  "2026-06-20: 296x-1320 closes crate handoff MIR acceptance; next blocker is creat subset pilot selection.",
  "2026-06-20: 296x-1321 documents OrderedMapBox as a .hako library deterministic-map boundary; next blocker is OrderedMapBox v0.",
  "2026-06-20: 296x-1322 implements OrderedMapBox v0 with focused EXE/AOT smoke; next blocker is BindingContext-style OrderedMapBox probe.",
  "2026-06-20: 296x-1323 closes the BindingContext-style OrderedMapBox probe; next blocker returns to creat subset pilot selection.",
  "2026-06-20: 296x-1324 guards constructor lifecycle separately from OrderedMap v0 initialization; next blocker remains creat subset pilot selection.",
  "2026-06-20: 296x-1325 observes same-file field initializers as green and imported static factory routes as unknown_global_callee; next blocker is imported static/global callee route investigation.",
  "2026-06-20: 296x-1326 fixes imported static factory field-initializer routes by deferring App-mode static method lowering until instance constructors are lowered; next blocker returns to creat subset pilot selection.",
  "2026-06-20: 296x-1327 adds callee-aware reason_detail/reason_hint fields for unknown_global_callee diagnostics without changing route selection.",
  "2026-06-20: 296x-1328 selects hakorune_box_core as the first real RustSubset crate pilot using a read-only manifest inventory tool; next blocker is HAKORUNE-BOX-CORE-RUSTSUBSET-PILOT-001.",
  "2026-06-20: 296x-1329 checks in the hakorune_box_core crate bundle and verifies full skeleton parse plus leaf-module MIR emit; next blocker is generated top-level function MIR acceptance.",
  "2026-06-20: 296x-1330 accepts generated top-level function skeletons at MIR emit level; next blocker is next crate pilot selection.",
  "2026-06-20: 296x-1331 selects a focused hakorune_mir_core crate pilot slice: crate::control_ids plus crate::types.",
  "2026-06-20: 296x-1332 checks in the hakorune_mir_core selected slice and verifies generated skeleton MIR emit; next blocker is crate-wrapper EXE pure-route unblock.",
  "2026-06-20: 296x-1333 unblocks crate-wrapper EXE by recovering user-box receiver origins from global factory route result box names; next blocker is unsupported pure-shape diagnostic hardening.",
  "2026-06-20: 296x-1334 hardens unsupported pure-shape diagnostics with callee/receiver-origin detail in a dedicated shim helper; next blocker is crate-wrapper EXE smoke pinning.",
  "2026-06-20: 296x-1335 pins crate-wrapper EXE success with a focused wrapper-only smoke; next blocker is rust-subset next app-front task selection.",
  "2026-06-20: 296x-1336 selects the next rust-subset task as the hakorune_mir_core ID-module slice: basic_block_id, binding_id, and value_id.",
  "2026-06-20: 296x-1337 probes the selected ID-module slice and exposes tuple-struct constructor expressions as the next skeleton-safety blocker.",
  "2026-06-20: 296x-1338 makes tuple-struct constructor expressions skeleton-safe through an adapter-side tuple-struct context and fixture-guards the handoff; next blocker is compound assignment skeleton safety.",
  "2026-06-20: 296x-1339 makes compound assignment skeleton-safe as a statement-level Unsupported handoff; next blocker is Self-qualified call skeleton safety.",
  "2026-06-20: 296x-1340 makes Self-qualified calls skeleton-safe and verifies the selected hakorune_mir_core ID-module generated skeleton reaches MIR emit.",
  "2026-06-20: 296x-1341 materializes the selected hakorune_mir_core ID-module bundle and guards it through generated-skeleton MIR emit plus wrapper EXE parity.",
  "2026-06-20: 296x-1342 selects hakorune_mir_core::value_kind as the next single-module RustSubset materialization task.",
  "2026-06-20: 296x-1343 materializes the hakorune_mir_core::value_kind bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1344 selects enum variant value skeleton-safety as the next app-front blocker after crate::effect exposes Effect_Mut as an undefined symbol.",
  "2026-06-20: 296x-1345 makes enum variant value references skeleton-safe as unsupported expression handoffs; crate::effect now advances to unresolved Vec_new.",
  "2026-06-20: 296x-1346 makes Vec::new calls skeleton-safe as unsupported expression handoffs; crate::effect now reaches generated-skeleton MIR emit.",
  "2026-06-20: 296x-1347 materializes the hakorune_mir_core::effect bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1348 selects hakorune_mir_builder::binding_context as the next MirBuilder-relevant single-module materialization task.",
  "2026-06-20: 296x-1349 materializes the hakorune_mir_builder::binding_context bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1350 selects hakorune_mir_builder::variable_context as the next MirBuilder-relevant single-module materialization task.",
  "2026-06-20: 296x-1351 materializes the hakorune_mir_builder::variable_context bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1352 selects Rust associated function call skeleton-safety after BindingId::new and CallFlags::new exposed unresolved generated globals.",
  "2026-06-20: 296x-1353 makes associated function calls skeleton-safe as explicit Unsupported handoffs; core_context reaches generated-skeleton MIR emit and call_unified advances to EffectMask_IO.",
  "2026-06-20: 296x-1354 selects associated const/value path skeleton-safety after EffectMask::IO exposed EffectMask_IO as an undefined generated variable.",
  "2026-06-20: 296x-1355 makes associated const/value paths skeleton-safe as explicit Unsupported handoffs; call_unified reaches generated-skeleton MIR emit.",
  "2026-06-20: 296x-1356 selects hakorune_mir_builder::core_context as the next MirBuilder-relevant single-module materialization task.",
  "2026-06-20: 296x-1357 materializes the hakorune_mir_builder::core_context bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1358 selects hakorune_mir_builder::context as the next builder-owned context materialization task.",
  "2026-06-20: 296x-1359 materializes the hakorune_mir_builder::context bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1360 selects hakorune_mir_defs::call_unified as the next green materialization task.",
  "2026-06-20: 296x-1361 materializes the hakorune_mir_defs::call_unified bundle and guards it through generated-skeleton MIR emit, wrapper EXE parity, and full rust-subset smoke.",
  "2026-06-20: 296x-1362 selects generic impl target skeleton-safety after metadata_context exposes invalid generated target spelling.",
  "2026-06-20: 296x-1363 makes generic impl target emitted names parser-safe and advances metadata_context to the reference-type/closure boundary.",
  "2026-06-20: 296x-1364 selects reference type spelling skeleton-safety after metadata_context and type_context both fail first on Option<&str>.",
  "2026-06-20: 296x-1365 makes reference type spelling parser-safe; type_context reaches MIR emit and metadata_context advances to undefined Self.",
  "2026-06-20: 296x-1366 selects hakorune_mir_builder::type_context as the next green MirBuilder context materialization task.",
]

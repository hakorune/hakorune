# Current State SSOT
# Keep live pointers compact. Detailed scope belongs in the active card.

status = "D′ P0c-S0a closed; disconnected direct-call profile P0c-S0b next"
date = "2026-07-15"

active_lane = "MIRBUILDER D′ Binding SSA Final Form"
active_lane_status = """D′ is the current canonical control-lowering authority:
pre-Builder products retain exact coverage/topology/ports/cleanup, while one
function-owned Binding SSA becomes the only BindingRef-to-ValueId/PHI merge
owner. B0-L4-S2′, SSA-P0's guarded 92-row inventory, behavior-neutral SSA-L0,
SSA-C1, SSA-P1, SSA-V0, SSA-S1, SSA-S2, SSA-E0, SSA-S3, and SSA-M0 are closed.
A′ accepts explicit CopyOwned/DestroyOwned Ownership SSA, keeps Copy neutral,
and isolates legacy ReleaseStrong. SSA-RC-L0a repairs stale instruction-diet
test/reference literals to the unchanged 41/16/57 implementation vocabulary.
SSA-RC-L0b closes the static-const-table load guard false-green by explicitly
enabling `vm-reference`; all three existing module tests now execute.
SSA-RC-L0 closes the ownership transport seam split: backend opcode
vocabulary/allowlists/tests and MIR JSON v0 lifecycle parsing now live behind
thin facades, all touched source/check files remain below 800 lines, and
behavior/API/grammar deltas are zero. SSA-RC-L1 closes one closure-scoped Rust
interpreter function-frame transaction across success and every typed error,
including PHI/step failures and primary-plus-restoration error retention. The
SSA-RC-P0 closes a guarded 17-row origin/storage matrix: exact BoxRef source
producers are zero, StorageClass remains inventory-only, direct JSON v0 lacks
an authoritative representation ingress, and the first SSA-I1 cutover is
trivial-only until SSA-I1-O1. SSA-RC-A0 closes passive Ownership SSA
vocabulary and exact direct-JSON BoxRef transport with production callers and
backend execution still zero. SSA-RC-A1a closes the temporary Rust semantic-
oracle handlers: CopyOwned clones one exact BoxRef into an undefined register,
DestroyOwned takes only the named BoxRef register, and invalid operands fail
explicitly. SSA-RC-V0 closes one disconnected, owner-branded Ownership SSA
verifier: exact live-Owned block-entry sets, edge-indexed parallel Phi
forwarding, finite-exit closure, Borrowed/edge-argument/unwitnessed-call
rejection, and zero production callers. SSA-RC-A1b closes one explicit Rust
verified-function session: sealed witness install/restore, owned argument move,
selected-edge parallel Phi take-before-publish, and exact Owned Return take,
with ordinary execution and canonical production callers unchanged.
SSA-RC-A1c closes exact llvm_py plus nyash_kernel ownership materialization.
SSA-RC-RET-P0 closes a behavior-neutral 118-surface, 266-occurrence legacy
ReleaseStrong retirement ledger with one private authority-guard helper.
SSA-RC0 closes a disconnected pure local-binding ownership-transition planner:
exact BindingRef self-assignment, next-before-previous replacement, BlockExpr
tail transfer/copy, reverse-order scope/function close, terminal Return ABI,
and draft discard are sealed with zero MIR/materialization authority. A′ is
accepted and SSA-I0-PROFILE is closed: one exact whole-owner
trivial profile is co-sealed with completion and carrier-free If control,
admits only InlineI64/InlineBool/InlineF64 value propagation, records If merge
profiles without deciding PHIs, and gives explicit no-value dispositions to
`return;` and implicit fallthrough. Parameters, receiver, Outbox, String,
Void values, Null values, and mixed merges reject before Builder effects.
SSA-I1-T is closed: an admitted trivial whole owner now uses one function-owned
Binding SSA, carrier-free If control, exact coverage, C1 CFG sealing, and
demand-driven PHIs. The selected route skips legacy `insert_rc_instructions`;
temporary A+ remains a once-selected non-admitted whole-unit owner with no
mixing or failure retry. Production Ownership SSA remains zero.
SSA-I1-COMPAT-N0a is closed: exact source Null gains one NullSentinel profile
over the existing ConstValue::Null / MirType::Void / runtime no-value
representation. Local/read/assignment/BlockExpr, homogeneous If, and Bool
comparison are production green with zero ownership operations. Null return,
Void, Outbox, BorrowedText, parameters, and receiver remain separate rows.
SSA-I1-COMPAT-V0a is closed: exact source Void has a distinct
ExplicitVoidValue profile over existing ConstValue::Void / MirType::Void /
runtime no-value representation. It keeps `return void`, `return;`, and
implicit fallthrough as separate source/profile dispositions. Outbox,
BorrowedText, parameters, receiver, Ownership SSA, opcodes, backend vocabulary,
and Loop activation remain inactive. Profile 13/13, focused Void 3/3,
resolved-lowering 89/89, production inventory 18/18, authority guard, release
build, and quick 66/66 are green. The P0 exact typed parameter ABI selection
stop has now selected P0a; receiver remains a separate owner family.
SSA-I1-COMPAT-P0a is closed: one non-main static function owner with one
or more parameters, every declared source type exactly `i64`, no typed return,
and only the current SSA-I1-T body grammar. Sealed parameter-entry rows adopt
reserved formal ValueIds into the function-owned Binding SSA, preserve exact
MirParamDecl transport, and reuse the existing exact-numeric final-callee
contract. Parameter reassignment and If PHIs use the same Binding SSA owner;
unsupported typed signatures reject before Builder effects and never retry
A+. Strict corpus candidates remain zero, so this is an ingress proof rather
than a selfhost coverage claim. Source calls, receiver, broader numeric types,
Box ownership, Ownership SSA/opcodes, and backend widening remain inactive.
R0a is closed: exact static `i64` return metadata is consumed only from the
sealed profile, the existing Binding SSA supplies the returned value, and the
existing ReturnExitContract remains runtime authority. P0c-i64 A′ is accepted:
the first executable source Call will be one exact current-owner self call over
a generic one-entry callable index, generic callable identity, co-sealed call
projection, conservative effect, and explicit VM-only direct-call capability.
P0c-L0 is closed: one bounded body-free callable-header view seals one exact
static-i64 header into generic key/reference/symbol vocabulary; exact lookup
has no fallback, physical symbol spelling is rejected at source, direct-call
arity drift is a typed pre-materialization error, and the conservative effect
plus passive backend capability are guarded. Production callable/capability
callers, source Call activation, grammar, route, and runtime behavior remain
zero. P0c-S0a now co-seals one exact self-call target with a source-unit-owned
callable-index/forest sidecar; disconnected S0b profile sealing is next.
Historical
lane chronology follows and is non-authoritative.
Explicit user priority change on 2026-07-12 reopens
the parked Source Selfhost/MirBuilder lane before LANGV1 conformance closeout.
Language-v1 behavior remains unchanged and its closeout stays parked. 3456,
3454, 3455, and the focused 3457 inventory are closed. The active slice
reuses ExactNumericValueFact for a narrow MapStoreI64 exact-i64 witness;
I64Value remains a derived projection and no route, runtime authority, or
Source Selfhost claim is added. 3478 closes LANGV1-GRAMMAR-001 with
one physical registry, two independent recursive parser witnesses, 106/106
corpus fixtures, zero Canonical-rejected spellings under lang/src, and bounded
differential composition. 3479 accepts one eventual semantic meaning for every
canonical :T site, a two-axis guarantee/enforcement vocabulary, and the
existing exact-numeric Box field owner as the first slice. 3480 closes the
11-site matrix, structural proof, dynamic type/range check, and backend
preflight boundary. 3481 accepts FunctionEntryContractOwner, a typed
exact-numeric parameter carrier, final-callee checks, and VM-only first-slice
support. 3482 closes that carrier, final-callee VM entry check, MIR JSON
transport, and unsupported-backend preflight. 3483 accepts the final-callee
return owner, typed carrier, Void/fallthrough veto, and VM-only first slice.
3484 closes the return carrier, shared value checker, final-outcome verifier,
VM exit check, MIR JSON, and backend preflight. 3485 accepts existing BindingId
wrapped as LocalSlotId, explicit LocalContractWrite, checked-edge PHI/loop
identity, U1 uninitialized rejection, and VM-only first support. 3486 closes
the behavior-preserving BindingId owner/bypass prerequisite. 3487 closes the
narrow exact-numeric local write contract, PHI/loop identity evidence, MIR
JSON, VM owner, and backend preflight. 3488 accepts semantic_refresh as the
single contract carrier owner and fixes the one-way source-contract to
representation-fact boundary. 3489 closes the facade, consumer bypass guards,
representation inventory, and mutable status ledger. 3490 audits the four
active exact-numeric boundaries. 3490 closes the four-owner island and shared
runtime value checker. 3491 selects record construction/update under one
RecordValueContractOwner. 3492 closes the instance-method parameter BindingId
gap, retires string-based assignment exceptions, and restores FULL 12/12
grammar parity. 3493 closes the record carrier, VM owner, MIR JSON, and
unsupported-backend gate. 3494 selects Static Table readonly U16 contract
closeout without new semantic activation. 3495 closes its source spec, derived
plan, refreshed carrier, VM consumer, and backend preflight. 3496 accepts
explicit ArrayElementWrite and shared-state identity. 3497 closes producer,
planner, VM, JSON, backend projection, and shared-state convergence. 3498
accepts annotation-only source authority, invariant state attachment, a
seven-type exact-numeric subset, one-lock mutation, VM-only support, and no
check elision. 3499 closes all seven source boundaries, atomic state adoption,
checked mutation, one VM consumer, JSON transport, and non-VM fail-fast. 3500
accepts A + W1 + S1 + R1 with an atomic owner and thin VM-reference adapter.
3501 closes WeakRef token equality and Dead/Freed upgrade law. 3502 closes
source Weak field specs, declaration-indexed slots, explicit writes, refresh,
runtime convergence, JSON, and backend fail-fast. 3503 now selects whether to
defer product lowering or name one carrier-aware product backend consumer;
the VM remains a narrow semantic-reference adapter, not a product fallback.
3503 accepts defer_with_failfast with declaration-level product rejection and
no capability activation. 3504 accepts relation/spec plus exhaustive
inventory-only Failure/Outcome design. Repository artifact C2 classification
is 91% complete with 77 warning-unregistered consultation rows; no physical
move was started. 3505 S0-S5 and the narrow 3511 activation slice are landed
history; the exact-i64 witness and LocalContractWrite Fact bridge slices are
closed. The active next slice is BoundedBodyAnalysisSnapshotV0: one bounded,
read-only Hako analysis capability over validated Program(JSON v0), with
independent Rust/Hako traversal parity before another Fact facade moves.
The consultation accepts wire_vocabulary: V0 is an explicitly lossy,
removable Program(JSON v0) observational quotient and never recovers source
kind. S0 ProgramV0WireContractInventoryV0 is closed with 40 classified union
rows, 7 exact SchemaMismatchStop tags, field/root seams, and parser strictness
gaps. S1 SnapshotSchemaV0 is closed with neutral Rust/Hako declarative schema,
immutable model, limits, PathV0, budget, exact equality, three-way outcomes,
focused tests, Hako syntax evidence, and an analysis-only dependency guard.
S2 is closed with a Rust analysis-only strict JSON/ProgramV0 compat adapter,
decoded duplicate-key and trailing-input rejection, closed field/tag/scalar
validation, explicit Unsupported versus InvalidInput outcomes, ten focused
tests, and no MIR lowering/raw-scanner dependency. The active S2.5 task closes
SnapshotAlgebraV0 in Rust before Hako traversal: canonical ordered atoms and
edges, closed paths, normalized validated accessors, construction invariants,
and one executable Rust witness. A1-A3 are closed with typed ordered atom and
child schemas, exact operator encodings, eighteen closed path fields, fixed
depth conventions, fourteen tests, and Rust/Hako declarative parity. A4
is now closed with opaque borrowed nodes, canonical i64, typed text bundled
with exact UTF-8 bytes/class, and schema-ordered read-only children. Eighteen
focused tests are green. A5 is closed with one-shot poisoned drafts, complete
seal checks, canonical schemas, forward paths, flat DFS preorder/depth, and
read-only publication. A6 is closed with a verification-only validated-view
witness, builder-only publication, exact integer equivalence, all accepted
kinds/roles/operators, and inclusive limit boundaries. Twenty-six tests are
green. S2.5 is complete and S3 Hako traversal is active;
its B1 flat ordered publication model is VM-reference green and exactly uses
ordered atom/child records rather than maps or Hako-only ordinals. B2 typed
validated carrier now follows the accepted B-led hybrid: declarative
DecodedUtf8ByteLengthContractV0 is authority, the first executable leaf is an
analysis/internal environment-independent capability, and RHako/HHako build
local checked text witnesses independently. Rust normalized-carrier replay is
non-authority. U0/U1 are closed: the RHako internal leaf owns only decoded
UTF-8 octet length and is proven independent of `NYASH_STR_CP`; U2 closes one
internal HHako `Callee::Extern` route, Rust reference direct dispatch,
metadata-only shared product-backend preflight/rejection, and a two-mode Hako
fixture. U3 closes sealed RHako construction, an HHako scalar carrier factory,
replay-only count verification, repository construction guards, and the Hako
literal/atom budget split. U4 now selects an invocation-scoped opaque strict
JSON arena: Rust owns generic strict JSON syntax/access only, while HHako owns
ProgramV0 validation, classification, paths, budgets, traversal, and snapshot
publication. B and C remain replay-only. Direct ingress is raw JSON -> generic
strict tree handle -> independent HHako ProgramV0 reader; a
ValidatedProgramV0BodyView bridge, MapBox reader input, and Rust text/count
witness bridge are forbidden. The reference MIR interpreter is the only
first-slice backend and all other backends fail before parse/session effects.
U4 implementation replaces the earlier stop at the strict
structured-ingress implementation boundary. U4-A0 through U4-A2 now cover the
neutral arena/session/accessor and reference-VM capability; U4-A3 through U4-A5
cover the HHako reader, parity gates, and one read-only consumer. Both
recursive prototypes remain parked and must not be revived directly.
LoopFeature parity remains a parked follow-up;
call-return/provider/cast roots remain deferred."""

active_phase = "docs/development/current/main/investigations/mirbuilder-dprime-binding-ssa-final-form-task-2026-07-14.md"
phase_status = "docs/development/current/main/investigations/mirbuilder-dprime-binding-ssa-final-form-task-2026-07-14.md"
method_anchor = "docs/development/current/main/design/current-docs-archive-policy-ssot.md"
taskboard = "docs/development/current/main/investigations/mirbuilder-dprime-binding-ssa-final-form-task-2026-07-14.md"
latest_workstream_card = "docs/development/current/main/design/binding-ssa-first-control-lowering-ssot.md"

latest_card = "mirbuilder-ssa-i1-compat-p0c-i64-call-design-stop-2026-07-15"
latest_card_path = "docs/development/current/main/investigations/mirbuilder-ssa-i1-compat-p0c-i64-call-design-stop-2026-07-15.md"
latest_card_summary = """P0c-i64 selects A′: exact current-owner self call is
the first executable family, but its semantic target is generic
ResolvedCallableRefV1 resolved through a source-unit-owned one-entry
VerifiedCallableIndexV1. The index is the sole callable-header authority; each
call row co-seals only a Lower-ready target/signature/symbol projection with
exact i64 arguments/result, exact coverage, and a conservative effect. P0c-L0
is closed with behavior-neutral callable vocabulary/facades, exact caller-zero
guards, and a passive durable canonical_direct_static_call_v1 backend
capability. P0c-S0a is closed: a source-unit-owned callable-index/forest
sidecar records only call-site target identity per function. P0c-S0b seals the
Lower-ready direct-call profile. P0c-I1 atomically activates one FunctionCall for
the Rust MIR interpreter. CurrentFunction-specific target variants, raw name
lookup in Lower, legacy recovery, generic backend inference, fallback, sibling
calls, MethodCall/receiver, ownership operations, and backend widening are
rejected. Source FunctionCall production activation remains zero until I1.
Historical D′ context follows.
D′ is accepted and taskized through the final canonical
source form. Exact source coverage, ScopeId/RegionId topology, typed ports,
exact targets, cleanup obligations, and unsupported preflight remain
pre-Builder authority. One per-function BindingSsaBuilderV1 will become the
only canonical BindingRef-to-ValueId merge/PHI authority. Existing canonical
If moves to that same owner before Loop production activation; family-specific
CFG boxes remain separate. Closed B0-L4-S1 Loop identity is retained. The
S2′ is closed with checked exact suffix ranges, owner-branded ordered coverage,
zero production consumers, and zero Binding SSA/Loop activation; existing A+
If production remains unchanged. SSA-P0 closes the exhaustive guarded seam
inventory, SSA-L0 closes the mandatory oversized PHI-helper split, and SSA-C1
closes the fallible terminator-truth CFG/seal substrate with fifteen focused
fixtures and zero production callers. SSA-P1 closes all-attempt PHI rollback,
typed primary-plus-cleanup error retention, and failed-commit cleanup with six
focused fixtures. SSA-V0 closes typed post-RC verifier failure, commit-before-
verification exclusion, and checked same-name canonical function publication.
SSA-S1 closes one disconnected function-owned sealed-block Binding SSA with
twelve focused fixtures, immutable C1 predecessor witnesses, rollback-and-
poison failure handling, zero production callers, and zero grammar delta.
SSA-S2 closes identity/value separation. SSA-E0 closes exact root-terminal and
implicit completion transport, exact consumption, direct canonical Return
emission, and post-Lower ready-witness finalization with zero grammar delta.
SSA-S3 closes exact source-preorder carrier-free If rows, sealed topology,
exclusive nested coverage, one internal coverage consumer, and zero new
production callers or behavior delta. SSA-M0 closes the borrowed real-MIR
adapter, conservative Unknown PHI facts, exact C1 witnesses, pending-only
rollback, and five focused fixtures. SSA-RC0-D0 is closed by A′: CopyOwned
creates a fresh independent Owned token, DestroyOwned consumes exactly the
named token, Copy stays neutral, and ReleaseStrong remains legacy. Before RC0
or SSA-I1, L0a/L0b/L0/L1/P0/A0/A1a/V0/A1b/A1c/RET-P0 close physical seams, the exact BoxRef
profile, transport, supported backends, path-sensitive ownership forwarding,
and legacy retirement inventory. If no exact BoxRef source producer exists,
SSA-I1 is a trivial-only Binding SSA cutover and production Ownership SSA waits
for the separate SSA-I1-O1 producer/ABI witness.
SSA-RC-L0 is closed with thin 8-line backend contract and 742-line JSON v0
facades, isolated vocabulary/allowlist/test/lifecycle boxes, 20/20 and 10/10
focused fixtures, release build, authority guard, and quick 66/66 green. The
SSA-RC-L1 frame transaction is also closed with 6/6 focused restoration
fixtures, 12/12 return-contract fixtures, release build, and quick 66/66
green. SSA-RC-P0 is closed with 17/17 guarded origin/storage rows, zero exact
BoxRef producers, and a trivial-only first cutover. SSA-RC-A0 is closed with
two passive WRITE-effect opcodes, exact JSON BoxRef witness validation, and a
43/16/59 instruction ledger. SSA-RC-A1a is closed with two exact Rust
interpreter handlers, 5/5 focused vm-reference fixtures, 21/21 backend
contract fixtures, and no alias scan. SSA-RC-V0 is closed with one separated
Ownership SSA box, 17/17 path-sensitive fixtures, exact parallel Phi-edge
transfers, and zero production callers. SSA-RC-A1b is closed with one explicit
verified Rust function session, 3/3 focused forwarding fixtures, and 6/6 frame
restoration fixtures. SSA-RC-A1c is closed with one exact sealed-witness JSON
transport, two llvm_py nyash_kernel handle materializers, explicit unsupported-
backend and PyVM rejection, and zero canonical production callers. The active
prerequisite is SSA-RC-RET-P0; canonical ownership opcode production remains
zero.
B′ is accepted and taskized as a parked runtime/substrate continuation:
explicit `fini()` owns eager tombstone teardown, DestroyOwned/last-strong own
structural drop only, ObjectIdentity is separate from owner/root tokens, the
first ObjectCell strategy is atomic SharedRc, normal Box exposes no manual
physical-free surface, and StaticUnique/LocalRc are later derived
optimizations. StaticUnique is a closed no-alias proof; lazy first-alias
PromotableUnique and source-level unique/reclaim remain optional parked
Decisions. The taskboard covers current-authority inventory,
passive cell/lease/weak contracts, one-family atomic activation, plugin ABI
split, backend/family rollout, global legacy retirement, StaticUnique
proof/materialization/parity, and final closeout.
It does not change the active SSA-RC-RET-P0 blocker.
Rust `MirInterpreter` retirement is now an explicit later selfhost branch:
after the ownership MIR vocabulary stabilizes, a sealed `.hako` MIR
interpreter subset gains normalized parity, becomes the no-fallback semantic
reference owner, and retires Rust code only after repository caller zero.
Old A+A2′ carrier-bearing
S3/I1/I2 and effect-bearing V2 extraction are superseded. Everything below is
historical evidence and is not current implementation authority. Historical
owner-forest context follows. Resolved Semantic Owner Forest V1
accepts A-double-prime `sealed_owner_forest_with_structural_upvar_edges`. One recursive forest
resolver session reuses the existing function-owner issuer, builds owner-local
sealed products, and publishes only a forest-verified cross-owner authority.
BindingId remains source lexical declaration identity. A child reference to a
strict ancestor is `UpvarRefV1(capturing_owner, source BindingRefV1)`; the
resolver creates neither CaptureId, child synthetic capture BindingId, capture
mode, forwarding binding, nor runtime slot. Roots, child-at, and Upvar indexes
are derived seal witnesses rather than mutable parallel authorities. Exact
control exits use one Statement/Expression site enum and one record containing
source region, origin, and transfer, replacing the current parallel maps.
QMark and source Throw are not activated as canonical language syntax, and
BlockExpr scope requires its own language decision. P0 is closed with 19 closed
Lambda/QMark/Match/BlockExpr/Try/Cleanup source path roles, passive
owner-branded cross-owner expression provenance, exact owner distinction, and
zero resolver acceptance change. E0 is closed with canonical and shadow
parallel exit maps retired, one atomic source-region/origin/transfer record,
closed root/member source-region containment, statement-only explicit
Break/Continue/Return publication, normalized parity, and unchanged QMark/Throw
acceptance. OF0 is closed with one resolver session, directly forest-owned
sealed owner products, one derived root, exact child-at provenance, normalized
forest parity, child-local Return targeting, exact lexical parent-scope
verification, and independent child owner identity. UP0 is closed with
ResolvedLexicalRefV1(Local|Upvar), structural UpvarRefV1 relations to original
strict-ancestor BindingRefV1 declarations, local/parameter/receiver reads,
deduplication, grandparent flattening, nearer-shadow and declaration-order
verification, and forest-level normalized parity. UP1 is closed with a
distinct UpvarRebind assignment target, exact-site Read/Rebind observations,
one deduplicated structural Upvar inventory, grandparent flattening, and
normalized access parity. CaptureId, synthetic child BindingId, capture mode,
runtime layout/slot, AST clone, ValueId, BasicBlockId, Planner, RegionFlow, and
Lower wiring remain zero. B0-D accepts lexical BlockExpr plus inventory-gated
compatibility isolation: every canonical source BlockExpr owns ScopeId and
RegionId, inner bindings end after the tail, outer BindingId rebinds propagate,
and condition scope does not extend into branch/body regions. ProgramV0 stays
lossy and gains neither source-kind recovery nor a new compatibility tag. A
non-wire compiler-private CompatSequenceExpr was mechanically skipped because
B0-P proves zero InternalSequenceRequired callers. B0-P closes the exhaustive
producer inventory without changing resolver, Planner, RegionFlow, or Lower
behavior. B0-S closes the passive BlockExpr scope/region kind pair, exact
shared origin, containment, normalized parity, and resolver-acceptance veto.
B0-F lexical fixture lock is closed. B0-L0/B0-L1 close the accepted design and
ingress inventory. B0-L2a closes a constructor-bounded typed compiler ingress,
one request match, explicit bare-AST/ProgramV0/REPL legacy provenance, and a
typed pre-Builder canonical stop with production resolved callers still zero.
B0-L2b closes immutable exact source navigation with one shared path grammar,
sealed syntax/forest projection, owner-branded located carriers, Lambda child
navigation, unforgeable carrier construction, and four focused fixtures.
B0-L2c closes one closure-scoped function transaction for static and instance
Lower: complete caller snapshots, unpublished function drafts, explicit
success/error cleanup, panic restoration, and post-cleanup module commit.
SA3-B closes the first canonical owner family atomically: one preflighted
non-main static/free straight-line source unit, exact located carriers,
BindingRef-keyed values, legacy BindingId allocator veto, complete source and
identity coverage, and function/module publication only after cleanup. The
default source route remains legacy; Planner/CorePlan, Loop, Lambda, and
legacy retry remain disconnected.
B0-L3a closes straight-line BlockExpr sealed ScopeId/RegionId consumption,
located prelude/tail Lower, active/retired BindingRef disposition, and two
VM-reference runtime fixtures. B0-L3b closes A+ pre-Builder verified If flow,
disconnected materialization infrastructure, and canonical fallthrough
statement-If runtime. B0-L4-P0 closes the bounded production-seam inventory:
canonical located Lower, the dev-only raw suffix normalization route, and the
legacy Builder-time CorePlan route are three distinct paths. Current CorePlan
already owns ValueId/BasicBlockId/PHI/name-keyed state and cannot be the first
pre-Builder source authority. The B0-L4 consultation accepts A + A2′. S1
closes a seal-derived exact Loop/LoopBody pair query, reciprocal verifier,
focused identity fixtures, and reusable line-neutral authority guard. Generic
coverage, Loop flow, materialization, and production activation remain later
separately gated slices.
Historical decision follows.
Resolved Region Flow V1 accepts
`function_semantic_resolver_v1_owner_scoped_arena`. One pre-plan
`FunctionSemanticResolverV1` constructs and seals
`VerifiedResolvedFunctionV1`, whose owner-scoped binding, scope, and region
arenas are the lexical/control semantic authority for one canonical function
AST. BindingOrigin/RegionOrigin/source sites are checked provenance and
Rust/Hako normalized-graph keys, not a second identity authority. Lower later
materializes BindingId -> ValueId and RegionId -> BasicBlockId only; it does
not allocate lexical identities or rediscover control targets. The migration
is SA0 passive schema/guard, SA1 non-authoritative shadow resolver, SA2
verifier/seal/dump, SA3 atomic canonical BindingId authority cutover, SA4
exact RegionId target cutover, and SA5 duplicate-owner retirement before R1
ResolvedRegionView begins. The same function may never receive independent
BindingIds from resolver and Lower, unsupported resolver syntax never retries
legacy resolution, and partial semantic drafts never publish. SA0 is closed
with a crate-private passive schema, owner-branded BindingRef/ScopeId/RegionId,
non-dense BindingId arena, explicit function roots, six focused tests, and one
reusable authority guard. External consumers, AST clones, ID allocation,
Planner, and Lower connections remain zero. SA1 is closed with a physically
separate ShadowResolvedFunctionV0, shadow-only binding/scope/region handles,
closed lexical/control traversal, typed fail-fast outcomes, exact nearest-loop
targets, twenty-two focused fixtures, and an expanded authority guard. Canonical
BindingId/product construction and Facts/Planner/Recipe/Lower connections
remain zero. SA2 is next: name the unique function-owner brand issuer, verify
complete arena/index/ancestry contracts, seal the canonical product, remove
the schema-test bypass, and publish a deterministic origin-keyed parity graph.
Historical decision follows. Resolved Region Flow V1 generalizes generic-loop
state closure into one recursive structured-region summary. Each region
publishes Port -> MayRebind(canonical BindingId set); analysis allocates no
BindingIds or ValueIds, uses no name-based identity, simulates no SSA versions,
and publishes no normalized AST clones. Loop header/after state domains and
edge binding requirements are derived from resolved port summaries. Lower
only materializes required BindingIds through its current BindingId->ValueId
environment. SourceStmtSite, BindingId, RegionId, and ValueId remain distinct.
ScopeBox is execution-inline for Recipe coverage and a real lexical boundary
for region flow. Binding rebind and non-binding heap effects are separate.
Active R0 inventories the canonical binding_ctx snapshot seam, scope/control
target owners, and the private join_ir/ownership BindingId disposition before
implementation. WIP `f74e5961e1` is acceptance-neutral and green but must be
restored selectively; R1 first removes ProjectedStmt AST clone ownership.
R1-R8 build the resolved view, recursive summaries, loop contracts, verifier,
disconnected plan, reference Lower, differential corpus, and authority
cutover. R9 caller-zero retirement of mandatory loop_var is Epic completion.
Historical generic-baseline decision follows. Generic Loop Baseline V1 supersedes mandatory
progression-owner acceptance. Generic correctness is condition + complete body
Recipe + exact source coverage + loop-carried binding set + closed ports. A
selected loop_var, loop_increment, step filtering, BodyManagedCursor, and
candidate Unique/Zero/Multiple are absent from generic acceptance. Canonical
step extraction becomes an optional proven optimization over an already
verified baseline; NotApplicable is nonfatal and Lower never falls back.
Active G0 keeps SourceStmtSiteV0 and identity-preserving projection, now as
complete-body coverage infrastructure. The interrupted pre-decision WIP is
parked at `05fb9b0577` and must be restored selectively because review found
node/child double-truth, orphan ownership, Scope ordering, and corpus-parity
gaps. G1-G5 add a disconnected baseline witness, state/port closure, neutral
latch skeleton, differential gates, and acceptance cutover. O0 isolates
canonical optimization. R0 mandatory caller-zero retirement of the legacy
single-loop-var family is part of Epic completion. Historical identity decision follows. The C2 identity consultation accepts a hybrid:
SourceStmtSiteV0 is canonical identity, generic-loop untyped flatten becomes
an identity-preserving typed projection, and Recipe provenance is a sealed,
co-constructed positional witness rather than RecipeItem fields or a mutable
path map. ScopeBox is a separately certified transparent container. Existing
Recipe remains source-independent and VerifiedRecipeBlock keeps its current
contract/port meaning. C2-I0 is acceptance-neutral and adds only path/schema,
typed projected bodies/statements, transparent expansion, sealing, and old
flatten sequence parity. Recipe, candidate selection, Lower, ProgramV0, and
parser/source-carrier P1 connections remain zero. C2-I1 through I4 later add
paired registration, source/Recipe bijection, exact canonical-step identity,
and composite E+R selection. Historical A2 evidence follows. A2-C0 acceptance-neutral candidate capture and C1
neutral progression proof plus C2-P0 Recipe coverage preflight are closed.
Eighteen focused progression tests and
the reusable lane guard are green. The exact
isolated-probe record inventory contains j, fields, and field_count diagnostic
rows with stable structural sites, provisional update shape/rank, and tied
comparison. The clean product route remains at its pre-C0 owner/reason;
acceptance widening, product-path connection, and parser changes are zero.
The C1 isolated proof probe yields exactly one proof: j has two
CurrentLoopExitGuard anchors and is ControlAnchoredBodyManaged; fields and
field_count have no anchors and remain Unproven. No rank, name, ShapeId,
Recipe, or new constructor participates, and product-path connection remains
zero. C2-P0 proves the existing verifier owns contract/reference-bounds/ports
but not exact statement coverage: omission and duplicate references are
accepted while out-of-range is rejected. Recipe-local coverage is
representable, but flattening and canonical filtering retain no exact nested
source-site identity. C2 is therefore stopped for a decision on the source
path owner, ScopeBox accounting, canonical-step witness transport, and the
source/Recipe bijection verifier. E proves identity; R only gates execution
and may not rank candidates. A2 uses an asymmetric E+R
hybrid: a new neutral
algebra fits, A2 uses an asymmetric E+R hybrid: a new neutral
LoopProgressionCandidatePolicyV0 proves candidate identity/control role, while
a candidate-independent Recipe verifier gates complete execution. Recipe
success never ranks candidates. Unique accepts, Zero is Unsupported, and
Multiple is Ambiguous. Numeric ranks, names, ShapeIds, source inspection,
candidate-local early return, BodyManaged write filtering, synthetic steps,
and Lower rediscovery are forbidden. A2 WIP `a8f50bbb` is restored alone on a
clean temporary branch for C0; stale current pointers and P1 `ce69e049` remain
parked. A2 must land independently and clean-HEAD ProgramV0 must be green
before P1 restoration. Historical context follows. A2 was previously at an evidence-incomplete design stop. The
progression-role WIP has pure observation, role classification, deterministic
aggregate selection, 19/19 focused role tests, 3/3 v0 tests, and 18/18 v1
tests, with every generic-loop Rust source below 800 lines. Structural
evidence advances the full ProgramV0 contract-pin through read-digits-like and
parse-block-like loops, but the record-declaration terminal Ambiguous did not
preserve candidate tuples. Adding further evidence ranks now would be
heuristic corpus tuning. The WIP is stashed as `a8f50bbb` and the next allowed
slice is bounded, acceptance-neutral observation capture. Only after that
evidence may consultation choose semantic discriminator, neutral owner, and
Unique/Zero/Multiple outcome axes. `BodyManagedCursor` remains a provisional
update-shape spelling, not cursor authority. Parser/source-carrier changes,
names, ShapeIds, fallback, Recipe/CFG widening, and P1 restoration remain
forbidden until that decision. Historical S3/3226 stashes are reference-only
archive candidates, while A2 and P1 remain separate active WIPs.
Historical context follows. P1-S consultation selects A:
compiler_boxshape_cleanup must close before the typed-parser P1 stash is
restored. LoopProgressionRoleObservationV0 separates CanonicalInduction from
BodyManagedCursor using condition-anchored candidates and complete write/use
observation, never Box/function/callee/variable names. StepModeV0 explicitly
distinguishes ExtractedCanonical from BodyManaged; every BodyManaged update
stays in a complete VerifiedRecipe and Lower performs zero role rediscovery.
Candidate evaluation becomes side-effect-free and order-independent before
stable final selection. A is an isolated generic-loop BoxShape commit:
parser/source-carrier files, ProgramV0 projection, B lightweight seam, and P1
wiring are forbidden. A0 is closed with two responsibility-split structural
cursor fixtures, one shared test environment owner, and a reusable A0-A4 lane
guard. The minimized per-loop fixtures stably record no accepted progression
role while the real-source ProgramV0 contract-pin remains explicitly
known-red; first-freeze order is not semantic authority. A1 is closed with a
pure CandidateObservationV0 owner for evaluation-ordered write/use sites,
canonical-step sites, post-update uses, conditional writes, and stable source
indices. It consumes condition anchoring and the existing canonical matcher
without rediscovering policy, excludes nested-loop state, and has no
selection/Recipe/Lower/parser/env dependency. Six progression-role and all 18
generic-loop-v1 tests are green; acceptance change remains zero. Active A2
adds structural role classification and deterministic aggregate selection.
Clean HEAD ProgramV0 contract-pin must become green before A commits; then P1
WIP is restored and gated separately. Historical
source-carrier context follows. Decision A selects one Hako parser authority with
explicit ParserNodeProductV1, a parser-private Open/Poisoned/Sealed source
arena, and ProgramV0 as a lossy compatibility projection from migrated nodes.
P0 is closed with seven responsibility-split Hako modules, bottom-up refs,
complete node/list reachability, defensive reconstruction, exact failure
domains, release/VM lifecycle fixtures, zero parser connection, and every
source file below 800 lines. Mutation and sealing were physically separated
after a combined reachability loop hit the existing JoinIR shape boundary;
compiler acceptance was not widened. Active P1 is the first Return/unary-minus
vertical slice. Its focused source-distinction, compat-projection, outcome,
and publication gates are green in the unlanded WIP. Mutation and publication
are now physically isolated, and ParserBox does not import the V1 sealer.
P1 is not mergeable: the ordinary ProgramV0 contract-pin smoke is also red on
clean HEAD and reaches existing generic-loop compile boundaries. Two worker
inventories identify ParserDelegateExposesBox._parse_delegate/3 cursor `j`
being treated as an in-body induction step; another clean-closure observation
also exposes FuncScannerBox._scan_methods/4 first, so closure/order dependence
is part of the BoxShape evidence. Active P1-S is now a design consultation:
choose compiler BoxShape cleanup, a genuinely narrow parser-private compile
seam, or P1 deferral before planner or parser code changes. ProgramV0
recovery, hidden side channels, mixed trees, planner widening in the parser
commit, and fallback remain forbidden.
The first semantic target is bool-predicate summary parity, with planner
connection zero. V0 remains explicitly lossy/removable and never recovers
source distinctions. Historical detail follows.
S0 ProgramV0WireContractInventoryV0 closes with 40 classified rows, 7 exact
SchemaMismatchStop tags, explicit field/root/parser seams, and one generated
fixture/guard. S1 closes the neutral Rust/Hako schema/model/path/budget/outcome
slice with parity and dependency guards. S2 closes the strict Rust
analysis-only JSON/ProgramV0 body view with decoded duplicate rejection,
full-input and closed field validation, and distinct Unsupported/InvalidInput
outcomes. S2.5 A1-A4 close the exact vocabulary and normalized validated
carrier, A5 construction invariants, and A6 executable Rust witness. S2.5 is
complete; active S3 now rewrites the parked recursive prototype around a
validated typed carrier, one-node observations, an explicit preorder
coordinator, and one-shot sealed publication. Existing permissive
typed decode and MIR lowering entry are not reused as analysis authority.
U4 now selects an invocation-scoped opaque generic strict JSON arena/session.
Rust owns only strict JSON syntax, decoded duplicate/trailing rejection,
generic kinds, ordered access, and handle validity. HHako independently owns
ProgramV0 validation/classification, paths, budgets, traversal, and snapshot
publication. B/C are replay-only; the ValidatedProgramV0BodyView, MapBox, and
Rust-normalized carrier are forbidden on the direct reader path. U4-P1 now
identifies the long pause as compilation inside `build_module` on an
unoptimized Rust compiler: the same tracked root source takes about 11.96s
debug versus 0.825s release, while semantic refresh remains small and reader
execution is never reached. The removed historical 30-second source is still
unavailable, so exact source-shape causality is not claimed. The stable gate
uses a fixed release+vm-reference binary with a ten-second boundary, one reader
family per file, and accepted-kind fanout at most 12. U4-A3.1 through A3.6,
U4-A4, U4-A5, and S4-S8 are closed. The bounded snapshot card is complete;
selection of the next snapshot-backed Fact family is a design consultation.
The reference
MIR interpreter is the only first-slice backend and unsupported backends fail
before parse/session effects. U4-A0 generic arena, U4-A1 invocation session,
and U4-A2 accessor/route/preflight are implemented and tested; U4-A3 now has
the generic facade, root reader, and leaf-expression reader. A first
monolithic Hako reader prototype was
removed after the common/builder imports stayed fast but the expression-reader
import exceeded the 30-second diagnostic boundary. U4-P1 now preserves a
generated compile-shape matrix, paired debug/release phase evidence, and the
safe release source shape above. U4-A3.1 is now
closed with Hako-owned thirteen-field root closure, exact Rust/Hako root-error
path/reason parity, dynamic invocation-scoped handle/root injection, empty
snapshot execution, strict-ingress-before-Hako failure, and cleanup on every
outcome. Non-empty bodies remain explicit pending Unsupported. U4-A3.2 is now
closed with five Hako-owned leaf observations, full signed canonical-i64
number/string normalization, decoded UTF-8 Atom/Literal budgets, exact scalar
values, Rust/Hako outcome path/reason parity, two-mode `NYASH_STR_CP` evidence,
and no partial snapshot publication. U4-A3.3 is closed with a separate
recursive Binary/Compare/Logical coordinator, closed operator partitions,
preorder node/depth budget, exact lhs/rhs order, nested failure propagation,
and exact Rust/Hako outcome parity. U4-A3.4 is closed with one unified accepted
expression recursion owner, Call/Method/Field closure, preflight argument
limits, and exact recv/args order. U4-A3.5 is closed with all eight statement
kinds, optional else, Local declared-type closure, top/nested body preflight,
and one generalized reserve/seal/reconstruct mixed flat publisher with ordered
roots and list paths. Record factories structurally preserve loop-created
birth calls. U4-A3.6 is closed with one direct reader, defensive one-shot
seal, exact Rust/Hako node/atom/edge/path/depth equality over every accepted
kind/role and all eighteen operators, separate internal contract failures,
and zero partial snapshot publication. U4-A4 is closed with nested negative
path/reason parity, direct inclusive limit boundaries, exact large-budget
component boundaries, replay/raw-scanner isolation, and backend rejection
before parse/session/HHako effects. U4-A5 is closed with a snapshot-only
LoopFeatureSummaryV0, nested-loop exit isolation, exact non-Ready outcome
preservation, and zero planner/route/backend/runtime connection. S4 is closed
with a test-only direct AST-to-snapshot oracle, source-loss equivalence,
multi-binding Local expansion, all operator partitions, explicit
context-sensitive Unsupported, and exact authoritative serializer-to-HHako
parity. S5/S6 close with a seven-source manifest permitting only three Ready
exact-parity and four explicit-Unsupported outcomes, zero skip/NoMatch/
InvalidInput, explicit declared_type/Float/FastMemRegion seams, and aggregate
isolation gates. S7 was already closed by the snapshot-only LoopFeatureSummary
consumer. S8 removes only the obsolete Hako loop-feature token facade and its
two dedicated gates, preserves historical fixtures, and leaves the Rust
planner Fact unchanged. The next Fact consumer is intentionally unselected.
The LocalContractWrite exact-i64 Fact bridge is
closed with canonical contract-id ownership, fresh identity matching,
dst-only publication, Copy/same-slot PHI propagation, 17 focused tests, and
the existing MapStoreI64 inventory guard. BoundedBodyAnalysisSnapshotV0 is
the accepted next card. It adds a bounded read-only analysis snapshot over
validated Program(JSON v0), requires independent Rust/Hako traversal parity,
and does not move Fact, planner, route, backend, MIR mutation, ID allocation,
or Source Selfhost authority. Historical context: 3456 closed the BoxShape-only typed RoutePolicyRow
prerequisite on 2026-07-12. 3454 is fixture-backed green, 3455 accepted
focused inventory first, and 3457 closed the authority inventory. The
exact-i64 witness slice is complete and the active LocalContractWrite bridge
reuses the same ExactNumericValueFact owner. The parked MapStore
caller-orientation pilot is not reopened. The
3456/3454 work already separated key and stored-value domains, generated one
typed decision payload and caller projection, retained the independent Rust
matcher/oracle, and centralized validation. Language-v1
runtime/semantic behavior remains unchanged; no route, mutation, backend, or
Source Selfhost claim is activated. 3505/3511 remain landed history and the
LANGV1 conformance closeout is parked until this user-priority lane closes.
The normative relation SSOT and 602-row evidence queue are landed; the
line-independent semantic site graph now contains 54 sites over those
occurrences, with 10 explicit projections and 52 pending sites. 55 explicit
reference/provider rows remain classified in the evidence queue while
semantic ownership stays pending. The VMValue::Void owner-boundary and S2
projection-source consultations are accepted; S1 through S5 are complete and
the accepted first activation slice is 3511 hako_mem_free Unit/no-result;
S0-S5 are complete: hako_mem_free semantic activation is narrow and
backend-independent, LLVM/object support is the sole consumer, and VM/Wasm/
PyVM remain fail-fast. Global Unit activation remains zero.
The design registry has 657 rows
and 119 owned sidecars; 77 files remain
warning-unregistered for focused consultation. C3 supersession movement is
not started. Design Registry V1 sharding is accepted-for-tasking as a parked
BoxShape series with production activation 0; its first implementation row is
mandatory CLEAN0, followed by explicit SELECT0, full normalized V0/V1 parity,
atomic cutover, V0 retirement, and RETURN0. It does not replace the active
SSA-I1-T blocker. 3511 now owns the narrow semantic activation claim and its
LLVM/object support row; global Unit, VM/Wasm support, and unrelated Void/Null
sites remain inactive."""

current_blocker_token = "RESOLVED-SEMANTIC-OWNER-FOREST-V1-DPRIME-SSA-I1-COMPAT-P0C-I64-S0B-PROFILE-IMPLEMENTATION-001"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE plus the active card only unless lane/blocker/restart-order/phase-status/policy changes."

pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

landed_tail = [
  "2026-07-15: D′ P0c-S0a closes one disconnected resolved self-call target product: CallableFunctionSyntaxViewV1 derives header/body from one AST, the root owner is issued once, a one-entry VerifiedCallableIndexV1 and VerifiedSemanticOwnerForestV1 are co-sealed as one source-unit sidecar, and VerifiedResolvedFunctionV1 stores only exact SourceExprSiteV1-to-ResolvedCallableRefV1 target identity; body-only production resolution still produces zero call targets, callable headers are not duplicated per function, Builder/value-profile/Lower/runtime connections remain zero, focused 5/5, resolved semantics 128/128, callable/full authority guards, release build, quick gate, and <800-line touched source/check files are green; disconnected P0c-S0b profile sealing is next.",
  "2026-07-15: D′ P0c-L0 closes one behavior-neutral generic callable substrate: a bounded body-free header view seals one exact static-i64 header into CanonicalCallableKeyV1, ResolvedCallableRefV1, CanonicalCallableSymbolV1, and a one-entry VerifiedCallableIndexV1; exact lookup has no fallback, physical symbol spelling is rejected from source, emission arity mismatch fails before materialization, the effect is an exact conservative known non-Pure barrier mask, and passive canonical_direct_static_call_v1 capability metadata is VM-only when explicitly present; production callers/producers, grammar, route, runtime behavior, and source Call activation remain zero; focused tests, caller-zero guard, full authority guard, release build, quick gate, and <800-line touched source/check files are green; disconnected P0c-S0 is next.",
  "2026-07-15: D′ P0c-i64 selects A′ exact current-owner self call as the first executable source-call family while keeping generic ResolvedCallableRefV1 identity, a source-unit-owned one-entry VerifiedCallableIndexV1, and a reusable co-sealed direct-call projection/materializer; P0c-L0 is behavior-neutral and must add an explicit passive canonical_direct_static_call_v1 backend capability, P0c-S0 remains disconnected, and only atomic P0c-I1 may activate exactly one FunctionCall for the Rust MIR interpreter; current-function target variants, duplicated header authority, raw Lower name lookup, legacy recovery, generic backend inference, fallback, sibling calls, MethodCall/receiver, ownership operations, and backend widening remain zero.",
  "2026-07-15: D′ P0c-i64 design stop proves that canonical ingress currently owns one function and no sibling callable catalog; MIR Call, P0a/R0a contracts, recursive VM enforcement, and backend gates are reusable but source callee identity/call-site ABI are absent; consultation must choose exact reusable self-call first or module callable catalog plus atomic multi-function publication first; legacy global-name recovery, callee=None, raw Lower rediscovery, MethodCall/receiver, ownership operations, backend widening, and source FunctionCall activation remain zero.",
  "2026-07-15: D′ SSA-I1-COMPAT-R0a-I1 closes atomic static exact-i64 typed returns: only the co-sealed R0a row enters Binding SSA; resolved Lower installs callable metadata only from sealed parameter/return witnesses; existing terminal/Binding SSA/ReturnExitContract authorities are preserved; unsupported typed returns never retry A+ and non-VM backends fail before effects; return-specific ValueIds and ownership operations remain zero; R0a 5/5, P0a 5/5, profile 22/22, authority guards, release build, quick gate, pointer guard, and <800-line touched source/check files are green; P0c-i64 source Call is the next design stop.",
  "2026-07-15: D′ SSA-I1-COMPAT-R0a-L0 closes one behavior-neutral callable ABI seam: ExactTrivialScalarAbiV1 is site-neutral exact i64 physical ABI authority; parameter ABI delegates to it; one route-local callable_abi facade installs sealed parameter metadata with no typed result and refreshes parameter then return contracts on the unpublished draft; resolved trivial raw return annotation reads are zero; grammar/route/runtime/typed-return/Ownership SSA deltas remain zero; parameter 5/5, resolved-lowering 94/94, profile 18/18, authority guard, release build, quick 66/66, and <800-line touched source/check files are green; disconnected R0a-S0 is next.",
  "2026-07-15: D′ SSA-I1-COMPAT-R0a selects static exact-i64 typed return as the next row: exact :i64 is co-sealed with the existing final explicit InlineI64 terminal; the terminal remains return-value authority and a new function-level witness owns only callable-boundary ABI; resolved Lower must consume sealed metadata without raw annotation reclassification; ReturnExitContract with FunctionReturnContract owner remains runtime authority; typed-return NotAdmitted never retries A+, non-VM backends fail before effects, and source Call/receiver/Box/Ownership SSA remain inactive; R0a-L0 behavior-neutral implementation is next and another docs-only closeout is forbidden.",
  "2026-07-15: D′ SSA-I1-COMPAT-P0a closes atomic static exact-i64 parameter ingress: sealed parameter rows install exact MirParamDecl metadata and adopt reserved formal ValueIds into the one function-owned Binding SSA without allocation or legacy name maps; parameter reassignment and If PHIs share that authority; existing final-callee exact type/arity validation and shared non-VM backend fail-fast are preserved; unsupported typed signatures reject before Builder effects and never retry A+; resolved-lowering 94/94, P0a 5/5, profile 18/18, final-callee 8/8, backend capability 2/2, parameter-entry 9/9, and authority guard are green; source calls, typed returns, receiver, broader numeric/Box rows, Ownership SSA/opcodes, and backend widening remain inactive; next-row selection is the design-stop frontier.",
  "2026-07-15: D′ SSA-I1-COMPAT-P0a selects one static exact-i64 parameter ingress proof: one or more exact i64 parameters, no typed return, and the existing SSA-I1-T body grammar; sealed parameter-entry rows adopt reserved formal ValueIds into the function-owned Binding SSA and reuse the existing exact-numeric final-callee contract; strict corpus candidates are zero, so this is infrastructure rather than selfhost coverage; implement behavior-neutral L0, disconnected S0, then atomic I1 only; source Call/MethodCall, receiver, bool/f64/usize, typed return, Box ownership, backend widening, and A+ fallback remain inactive.",
  "2026-07-15: D′ SSA-I1-COMPAT-V0a closes one exact ExplicitVoidValue production row: LiteralValue::Void is exact source/profile authority over existing ConstValue::Void / MirType::Void / runtime no-value representation; local/read/assignment/BlockExpr, homogeneous one-sided/two-sided/nested If, Bool comparison, explicit return void, and Void expression completion use the existing Binding SSA; return void / return; / implicit completion remain distinct profile dispositions; Outbox, Null return, mixed Null/Void, BorrowedText, parameters, receiver, Ownership SSA/opcodes, backend vocabulary, and Loop remain inactive; profile 13/13, focused Void 3/3, resolved-lowering 89/89, capability 5/5, production inventory 18/18, legacy lifecycle ledger 125/276, authority guard, release build, and quick 66/66 are green; P0 exact typed parameter ABI is the next design-selection stop and receiver stays separate.",
  "2026-07-15: D′ SSA-I1-COMPAT-V0a selects exact ExplicitVoidValue after three independent audits and a lang/src census: LiteralValue::Void is source authority over existing ConstValue::Void / MirType::Void / runtime no-value representation; return void remains ExplicitValue while return; and implicit fallthrough retain their distinct no-value terminal profiles; the implementation may add only local/read/assignment/BlockExpr, homogeneous If, Void comparison, and explicit Void return under the existing Binding SSA; Outbox, BorrowedText, parameter/receiver ABI, Ownership SSA/opcodes, backend vocabulary, and Loop activation remain inactive; exact typed parameter ABI follows after V0a closes, while receiver stays a separate owner-family decision; the next card must contain code or artifact delta.",
  "2026-07-15: D′ SSA-I1-COMPAT-N0a closes one exact NullSentinel local-flow row: LiteralValue::Null is exact profile authority over existing ConstValue::Null / MirType::Void / runtime no-value representation; local/read/assignment/BlockExpr, homogeneous one-sided/two-sided/nested If, and Bool comparison use the same production Binding SSA; Null return, Void, Outbox, BorrowedText, parameters, receiver, Ownership SSA/opcodes, and Loop activation remain inactive; profile 12/12, focused VM/reference 3/3, resolved-lowering 86/86, capability 5/5, production inventory 18/18, legacy ledger 124/275, authority guard, release build, and quick 66/66 are green; ownership operations and legacy RC insertion on the selected route remain zero; the next compatibility row returns to a design-selection stop.",
]

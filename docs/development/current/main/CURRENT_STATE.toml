# Current State SSOT
# Keep live pointers compact. Detailed scope belongs in the active card.

status = "FINALIZE0-CENSUS0-P0a-S0b and VERIFY-SPLIT0-S0/P0/I0/FUNCTION-G0-D0/S0/P0/G0 and PHI-SPLIT0-D0/S0/M0/P0/I0-SELECT/MODULETX0-S0/REMATFACT0-D0/S0/M0/P0-SELECT/P0-S0/P0-P0/P0-G0, FACTSESSION0-I0-SELECT, MODULEDRAFT0-S0/M0/P0 and HEADERPORT0-SELECT/S0/P0, and HEADERPORT0-RAWPORT0-SELECT/S0/M0-T0/R0/LEGACYTERM0-S0/P0/I0/G0/LOOPBRIDGE0-SELECT/S0/P0/I0/G0 are closed; HEADERPORT0 re-entrant raw child terminal is a DESIGN-STOP before I0, and CUT0 remains forbidden"
date = "2026-07-21"

active_lane = "MIRBUILDER D′ Binding SSA Final Form"
active_lane_status = """D′ is the current canonical control-lowering authority:
pre-Builder products retain exact coverage/topology/ports/cleanup, while one
function-owned Binding SSA becomes the only BindingRef-to-ValueId/PHI merge
owner. B0-L4-S2′, SSA-P0's guarded 92-row inventory, behavior-neutral SSA-L0,
SSA-C1, SSA-P1, SSA-V0, SSA-S1, SSA-S2, SSA-E0, SSA-S3, and SSA-M0 are closed.
A′ accepts explicit CopyOwned/DestroyOwned Ownership SSA, keeps Copy neutral,
and isolates legacy ReleaseStrong. SSA-RC-L0a repairs stale instruction-diet
test/reference literals to the unchanged 41/16/57 implementation vocabulary.
SSA-RC-L0b closes the static-const-table load guard false-green by explicitly
enabling `vm-reference`; all three existing module tests now execute.
SSA-RC-L0 closes the ownership transport seam split: backend opcode
vocabulary/allowlists/tests and MIR JSON v0 lifecycle parsing now live behind
thin facades, all touched source/check files remain below 800 lines, and
behavior/API/grammar deltas are zero. SSA-RC-L1 closes one closure-scoped Rust
interpreter function-frame transaction across success and every typed error,
including PHI/step failures and primary-plus-restoration error retention. The
SSA-RC-P0 closes a guarded 17-row origin/storage matrix: exact BoxRef source
producers are zero, StorageClass remains inventory-only, direct JSON v0 lacks
an authoritative representation ingress, and the first SSA-I1 cutover is
trivial-only until SSA-I1-O1. SSA-RC-A0 closes passive Ownership SSA
vocabulary and exact direct-JSON BoxRef transport with production callers and
backend execution still zero. SSA-RC-A1a closes the temporary Rust semantic-
oracle handlers: CopyOwned clones one exact BoxRef into an undefined register,
DestroyOwned takes only the named BoxRef register, and invalid operands fail
explicitly. SSA-RC-V0 closes one disconnected, owner-branded Ownership SSA
verifier: exact live-Owned block-entry sets, edge-indexed parallel Phi
forwarding, finite-exit closure, Borrowed/edge-argument/unwitnessed-call
rejection, and zero production callers. SSA-RC-A1b closes one explicit Rust
verified-function session: sealed witness install/restore, owned argument move,
selected-edge parallel Phi take-before-publish, and exact Owned Return take,
with ordinary execution and canonical production callers unchanged.
SSA-RC-A1c closes exact llvm_py plus nyash_kernel ownership materialization.
SSA-RC-RET-P0 closes a behavior-neutral 118-surface, 266-occurrence legacy
ReleaseStrong retirement ledger with one private authority-guard helper.
SSA-RC0 closes a disconnected pure local-binding ownership-transition planner:
exact BindingRef self-assignment, next-before-previous replacement, BlockExpr
tail transfer/copy, reverse-order scope/function close, terminal Return ABI,
and draft discard are sealed with zero MIR/materialization authority. A′ is
accepted and SSA-I0-PROFILE is closed: one exact whole-owner
trivial profile is co-sealed with completion and carrier-free If control,
admits only InlineI64/InlineBool/InlineF64 value propagation, records If merge
profiles without deciding PHIs, and gives explicit no-value dispositions to
`return;` and implicit fallthrough. Parameters, receiver, Outbox, String,
Void values, Null values, and mixed merges reject before Builder effects.
SSA-I1-T is closed: an admitted trivial whole owner now uses one function-owned
Binding SSA, carrier-free If control, exact coverage, C1 CFG sealing, and
demand-driven PHIs. The selected route skips legacy `insert_rc_instructions`;
temporary A+ remains a once-selected non-admitted whole-unit owner with no
mixing or failure retry. Production Ownership SSA remains zero.
SSA-I1-COMPAT-N0a is closed: exact source Null gains one NullSentinel profile
over the existing ConstValue::Null / MirType::Void / runtime no-value
representation. Local/read/assignment/BlockExpr, homogeneous If, and Bool
comparison are production green with zero ownership operations. Null return,
Void, Outbox, BorrowedText, parameters, and receiver remain separate rows.
SSA-I1-COMPAT-V0a is closed: exact source Void has a distinct
ExplicitVoidValue profile over existing ConstValue::Void / MirType::Void /
runtime no-value representation. It keeps `return void`, `return;`, and
implicit fallthrough as separate source/profile dispositions. Outbox,
BorrowedText, parameters, receiver, Ownership SSA, opcodes, backend vocabulary,
and Loop activation remain inactive. Profile 13/13, focused Void 3/3,
resolved-lowering 89/89, production inventory 18/18, authority guard, release
build, and quick 66/66 are green. The P0 exact typed parameter ABI selection
stop has now selected P0a; receiver remains a separate owner family.
SSA-I1-COMPAT-P0a is closed: one non-main static function owner with one
or more parameters, every declared source type exactly `i64`, no typed return,
and only the current SSA-I1-T body grammar. Sealed parameter-entry rows adopt
reserved formal ValueIds into the function-owned Binding SSA, preserve exact
MirParamDecl transport, and reuse the existing exact-numeric final-callee
contract. Parameter reassignment and If PHIs use the same Binding SSA owner;
unsupported typed signatures reject before Builder effects and never retry
A+. Strict corpus candidates remain zero, so this is an ingress proof rather
than a selfhost coverage claim. Source calls, receiver, broader numeric types,
Box ownership, Ownership SSA/opcodes, and backend widening remain inactive.
R0a is closed: exact static `i64` return metadata is consumed only from the
sealed profile, the existing Binding SSA supplies the returned value, and the
existing ReturnExitContract remains runtime authority. P0c-i64 A′ is accepted:
the first executable source Call will be one exact current-owner self call over
a generic one-entry callable index, generic callable identity, co-sealed call
projection, conservative effect, and explicit VM-only direct-call capability.
P0c-L0 is closed: one bounded body-free callable-header view seals one exact
static-i64 header into generic key/reference/symbol vocabulary; exact lookup
has no fallback, physical symbol spelling is rejected at source, direct-call
arity drift is a typed pre-materialization error, and the conservative effect
plus passive backend capability are guarded. Production callable/capability
callers, source Call activation, grammar, route, and runtime behavior remain
zero through L0. P0c-S0a and P0c-S0b co-seal the target and direct-call profile.
P0c-I1 is closed: an explicit call-enabled ingress admits one exact
current-owner FunctionCall; canonical Lower consumes the whole row without
name lookup or fallback; recursion rechecks parameter/return contracts; and an
explicit VM-only capability rejects unsupported backends. Ownership ops,
sibling/multiple calls, MethodCall/receiver, and backend widening remain zero.
P0c-CAT0 A′+ is accepted. CAT0-L0 is closed: one primary header store,
key-only callable/symbol reverse indexes, indexed lookup, and a checked
sole-header facade preserve the one-entry self-call behavior without grammar,
route, runtime, or ownership delta. CAT0-S0 is also closed: one owned non-empty
function-only Program exposes only opaque declaration sites and body-free
headers; bodies, owner issuance, catalog sealing, and production callers remain
zero. CAT0-C0a is also closed: exact-i64 candidates and duplicate key/symbol
laws are sealed before identity issuance. CAT0-C0b is also closed: all
top-level origin/owner pairs are reserved from one resolver session, the exact
Program and immutable catalog are co-sealed, and one non-Clone single-use
continuation preserves the next origin ordinal and same-branded owner issuer.
CAT0-G0 is also closed: normalized catalog rows exclude invocation-local
identity, declaration reorder preserves normalized/lookup meaning, malformed
private drafts own duplicate identity/symbol invariant tests, and the final
Program/catalog pairing and caller-zero boundaries are guarded. MP0-S0 is also
closed: one passive compiler-layer carrier owns the CAT0 source unit and one
CanonicalCallableKeyV1-keyed primary function map; each function row keeps its
declaration site, single-root forest, and exact projection together. It has no
constructor, body authority, resolver continuation consumer, production caller,
Builder, MIR, runtime, backend, or ownership delta. MP0-R0 is also closed: one
consuming body-resolution view keeps Program/catalog inseparable, every root
reuses its CAT0-reserved identity, every body sees the complete index, nested
Lambda owners consume the same continuation/brand, and each canonical key owns
one exact forest/projection pair. Self/forward/backward calls resolve without
Builder/MIR/runtime activation. MP0-P0 is also closed: every canonical-keyed
function plan is sealed before Builder effects. MP0-TX0 is now closed: every
function lowers to an unpublished verified draft; exact key/symbol/arity/
cardinality correspondence precedes one atomic candidate-module batch
insertion; declaration reorder is stable; and late lowering, symbol, or batch
collision failures publish no partial callable set. Production transaction
callers and sibling-call activation remain zero through MP0-TX0. P0c-B1 is
now closed: one explicit compiler ingress accepts exactly two static exact-i64
functions and one non-self sibling edge; caller and target headers remain
separate sealed authorities; declaration order is stable; MIR interpreter
execution and VM-only backend fail-fast are green; ownership operations and
fallback remain zero. P0c-F is now selected and absorbs P0c-N: multiple and
nested calls are fixtures inside one exact acyclic-module row. P0c-F-DX0a is
closed: one disconnected one-or-more exact-call analysis/preflight request
records nested calls child-before-parent with checked cardinality while the
existing exact-one self-call ingress remains unchanged and generalized
production callers stay zero. DX0b is closed: the sealed profile installs one
capability per calling function before expression lowering, and call emitters
only verify it. S0 is closed: resolved call sites project to canonical keys,
retain multiplicity, collapse only unique topology edges, and seal a
deterministic DAG witness with self/cycle rejection. V0 is closed: it combines
that topology proof with canonical-keyed finite trivial plans, seals exact
graph/function/plan cardinality and per-function call-row correspondence, and
keeps Builder/MIR/effect/symbol/publication authority and production callers
zero. The read-only `lang/src` census is closed with zero exact P0c-F
candidates, so I1 remains an ingress proof rather than a corpus-coverage
claim. I1 activates the existing atomic module ingress. C-prime is accepted
for P0c-MR: G0 extracts one behavior-neutral non-Clone callable graph
inventory, S0 seals a deterministic SCC partition, V0 seals the recursive
exact-i64 plan, C0 adds one passive module-level capability, I1 explicitly
activates VM-only recursive modules through the existing atomic draft
transaction, and R0 later retires the separate one-function self-call
authority. G0 is closed: one non-Clone inventory owns canonical-key nodes,
exact function-relative call sites, unique edges, target projection, and
inventory errors; the existing acyclic proof consumes it by value and retains
the prior P0c-F behavior. S0 is closed: one non-Clone deterministic SCC
partition consumes and retains that inventory by value, uses iterative
host-stack-safe traversal, assigns each component its minimum canonical key,
classifies recursion, and seals canonical condensation edges/order. V0 is
closed: one disconnected plan seals recursive-module admission plus exact
inventory/function/component/typed-plan/call-row correspondence. C0 is the sole
next code-facing row and adds only a passive module capability schema. C0 is
closed with one Option marker, exact schema validation, VM acceptance, and
stable fail-fast for every other backend. I1 is the sole next code-facing row
and adds one explicit atomic recursive-module ingress. Recursion production
producers remain zero before I1. I1 is closed: one explicit recursive compiler
ingress consumes the typed plan, reuses unpublished draft collection, atomically
publishes the complete function set, installs one module marker, and commits
only the finished isolated candidate. The MR-specific MAX_CALL_DEPTH and inner
parameter/return contract failure fixtures now prove caller-frame restoration
and interpreter reuse. R0 selected Program-only authority with
`S0 -> P0 -> CUT0 -> G0`. S0 and P0 are closed: test-only normalized
snapshots prove old/new header, semantic-shape, call ABI, profile order,
MIR control/value/call/return relation, contract, runtime, backend fail-fast,
ownership-op, and compiler-reuse parity; the Program route's one recursive
module marker is the sole intentional difference. CUT0 is also closed:
singleton production uses the Program authority and the old RootCallable,
one-entry, temporary selector, and exact-one authorities are deleted. R0-G0 is
closed with old-symbol/caller zero, one explicit Program ingress, and no retry.
Historical
lane chronology follows and is non-authoritative.
Explicit user priority change on 2026-07-12 reopens
the parked Source Selfhost/MirBuilder lane before LANGV1 conformance closeout.
Language-v1 behavior remains unchanged and its closeout stays parked. 3456,
3454, 3455, and the focused 3457 inventory are closed. The active slice
reuses ExactNumericValueFact for a narrow MapStoreI64 exact-i64 witness;
I64Value remains a derived projection and no route, runtime authority, or
Source Selfhost claim is added. 3478 closes LANGV1-GRAMMAR-001 with
one physical registry, two independent recursive parser witnesses, 106/106
corpus fixtures, zero Canonical-rejected spellings under lang/src, and bounded
differential composition. 3479 accepts one eventual semantic meaning for every
canonical :T site, a two-axis guarantee/enforcement vocabulary, and the
existing exact-numeric Box field owner as the first slice. 3480 closes the
11-site matrix, structural proof, dynamic type/range check, and backend
preflight boundary. 3481 accepts FunctionEntryContractOwner, a typed
exact-numeric parameter carrier, final-callee checks, and VM-only first-slice
support. 3482 closes that carrier, final-callee VM entry check, MIR JSON
transport, and unsupported-backend preflight. 3483 accepts the final-callee
return owner, typed carrier, Void/fallthrough veto, and VM-only first slice.
3484 closes the return carrier, shared value checker, final-outcome verifier,
VM exit check, MIR JSON, and backend preflight. 3485 accepts existing BindingId
wrapped as LocalSlotId, explicit LocalContractWrite, checked-edge PHI/loop
identity, U1 uninitialized rejection, and VM-only first support. 3486 closes
the behavior-preserving BindingId owner/bypass prerequisite. 3487 closes the
narrow exact-numeric local write contract, PHI/loop identity evidence, MIR
JSON, VM owner, and backend preflight. 3488 accepts semantic_refresh as the
single contract carrier owner and fixes the one-way source-contract to
representation-fact boundary. 3489 closes the facade, consumer bypass guards,
representation inventory, and mutable status ledger. 3490 audits the four
active exact-numeric boundaries. 3490 closes the four-owner island and shared
runtime value checker. 3491 selects record construction/update under one
RecordValueContractOwner. 3492 closes the instance-method parameter BindingId
gap, retires string-based assignment exceptions, and restores FULL 12/12
grammar parity. 3493 closes the record carrier, VM owner, MIR JSON, and
unsupported-backend gate. 3494 selects Static Table readonly U16 contract
closeout without new semantic activation. 3495 closes its source spec, derived
plan, refreshed carrier, VM consumer, and backend preflight. 3496 accepts
explicit ArrayElementWrite and shared-state identity. 3497 closes producer,
planner, VM, JSON, backend projection, and shared-state convergence. 3498
accepts annotation-only source authority, invariant state attachment, a
seven-type exact-numeric subset, one-lock mutation, VM-only support, and no
check elision. 3499 closes all seven source boundaries, atomic state adoption,
checked mutation, one VM consumer, JSON transport, and non-VM fail-fast. 3500
accepts A + W1 + S1 + R1 with an atomic owner and thin VM-reference adapter.
3501 closes WeakRef token equality and Dead/Freed upgrade law. 3502 closes
source Weak field specs, declaration-indexed slots, explicit writes, refresh,
runtime convergence, JSON, and backend fail-fast. 3503 now selects whether to
defer product lowering or name one carrier-aware product backend consumer;
the VM remains a narrow semantic-reference adapter, not a product fallback.
3503 accepts defer_with_failfast with declaration-level product rejection and
no capability activation. 3504 accepts relation/spec plus exhaustive
inventory-only Failure/Outcome design. Repository artifact C2 classification
is 91% complete with 77 warning-unregistered consultation rows; no physical
move was started. 3505 S0-S5 and the narrow 3511 activation slice are landed
history; the exact-i64 witness and LocalContractWrite Fact bridge slices are
closed. The active next slice is BoundedBodyAnalysisSnapshotV0: one bounded,
read-only Hako analysis capability over validated Program(JSON v0), with
independent Rust/Hako traversal parity before another Fact facade moves.
The consultation accepts wire_vocabulary: V0 is an explicitly lossy,
removable Program(JSON v0) observational quotient and never recovers source
kind. S0 ProgramV0WireContractInventoryV0 is closed with 40 classified union
rows, 7 exact SchemaMismatchStop tags, field/root seams, and parser strictness
gaps. S1 SnapshotSchemaV0 is closed with neutral Rust/Hako declarative schema,
immutable model, limits, PathV0, budget, exact equality, three-way outcomes,
focused tests, Hako syntax evidence, and an analysis-only dependency guard.
S2 is closed with a Rust analysis-only strict JSON/ProgramV0 compat adapter,
decoded duplicate-key and trailing-input rejection, closed field/tag/scalar
validation, explicit Unsupported versus InvalidInput outcomes, ten focused
tests, and no MIR lowering/raw-scanner dependency. The active S2.5 task closes
SnapshotAlgebraV0 in Rust before Hako traversal: canonical ordered atoms and
edges, closed paths, normalized validated accessors, construction invariants,
and one executable Rust witness. A1-A3 are closed with typed ordered atom and
child schemas, exact operator encodings, eighteen closed path fields, fixed
depth conventions, fourteen tests, and Rust/Hako declarative parity. A4
is now closed with opaque borrowed nodes, canonical i64, typed text bundled
with exact UTF-8 bytes/class, and schema-ordered read-only children. Eighteen
focused tests are green. A5 is closed with one-shot poisoned drafts, complete
seal checks, canonical schemas, forward paths, flat DFS preorder/depth, and
read-only publication. A6 is closed with a verification-only validated-view
witness, builder-only publication, exact integer equivalence, all accepted
kinds/roles/operators, and inclusive limit boundaries. Twenty-six tests are
green. S2.5 is complete and S3 Hako traversal is active;
its B1 flat ordered publication model is VM-reference green and exactly uses
ordered atom/child records rather than maps or Hako-only ordinals. B2 typed
validated carrier now follows the accepted B-led hybrid: declarative
DecodedUtf8ByteLengthContractV0 is authority, the first executable leaf is an
analysis/internal environment-independent capability, and RHako/HHako build
local checked text witnesses independently. Rust normalized-carrier replay is
non-authority. U0/U1 are closed: the RHako internal leaf owns only decoded
UTF-8 octet length and is proven independent of `NYASH_STR_CP`; U2 closes one
internal HHako `Callee::Extern` route, Rust reference direct dispatch,
metadata-only shared product-backend preflight/rejection, and a two-mode Hako
fixture. U3 closes sealed RHako construction, an HHako scalar carrier factory,
replay-only count verification, repository construction guards, and the Hako
literal/atom budget split. U4 now selects an invocation-scoped opaque strict
JSON arena: Rust owns generic strict JSON syntax/access only, while HHako owns
ProgramV0 validation, classification, paths, budgets, traversal, and snapshot
publication. B and C remain replay-only. Direct ingress is raw JSON -> generic
strict tree handle -> independent HHako ProgramV0 reader; a
ValidatedProgramV0BodyView bridge, MapBox reader input, and Rust text/count
witness bridge are forbidden. The reference MIR interpreter is the only
first-slice backend and all other backends fail before parse/session effects.
U4 implementation replaces the earlier stop at the strict
structured-ingress implementation boundary. U4-A0 through U4-A2 now cover the
neutral arena/session/accessor and reference-VM capability; U4-A3 through U4-A5
cover the HHako reader, parity gates, and one read-only consumer. Both
recursive prototypes remain parked and must not be revived directly.
LoopFeature parity remains a parked follow-up;
call-return/provider/cast roots remain deferred."""

active_phase = "docs/development/current/main/investigations/mirbuilder-clean-architecture-consolidation-task-2026-07-19.md"
phase_status = "docs/development/current/main/investigations/mirbuilder-clean-architecture-consolidation-task-2026-07-19.md"
method_anchor = "docs/development/current/main/design/current-docs-archive-policy-ssot.md"
taskboard = "docs/development/current/main/investigations/mirbuilder-clean-architecture-consolidation-task-2026-07-19.md"
latest_workstream_card = "docs/development/current/main/investigations/mirbuilder-clean-architecture-consolidation-task-2026-07-19.md"
parked_loop_route_retirement_card = "docs/development/current/main/investigations/generic-loop-named-route-retirement-task-2026-07-19.md"
mirbuilder_clean_architecture_card = "docs/development/current/main/investigations/mirbuilder-clean-architecture-consolidation-task-2026-07-19.md"
joinir_dprime_succession_task = "docs/development/current/main/investigations/mirbuilder-joinir-dprime-succession-task-2026-07-20.md"
copy_unknown_consultation = "docs/development/current/main/investigations/mirbuilder-copy-unknown-origin-consultation-2026-07-20.md"
copy_unknown_task = "docs/development/current/main/investigations/mirbuilder-copy-unknown-origin-task-2026-07-20.md"
exact0_const_task = "docs/development/current/main/investigations/mirbuilder-exact0-const-task-2026-07-20.md"
staticload0_task = "docs/development/current/main/investigations/mirbuilder-staticload0-task-2026-07-20.md"
checkselect0_task = "docs/development/current/main/investigations/mirbuilder-checkselect0-task-2026-07-20.md"
literal_postemit_retirement_task = "docs/development/current/main/investigations/mirbuilder-literal-postemit-retirement-task-2026-07-20.md"
resolved_trivial_operation_task = "docs/development/current/main/investigations/mirbuilder-resolved-trivial-operation-task-2026-07-20.md"
resolved_direct_call_task = "docs/development/current/main/investigations/mirbuilder-resolved-direct-call-task-2026-07-20.md"
compareemit0_task = "docs/development/current/main/investigations/mirbuilder-compare-emission-receipt-task-2026-07-20.md"
fact0_tx0_task = "docs/development/current/main/investigations/mirbuilder-fact0-transaction-boundary-task-2026-07-20.md"
fastmem_receipt0_task = "docs/development/current/main/investigations/mirbuilder-fact0-transaction-boundary-task-2026-07-20.md"
fastmem_fieldload0_task = "docs/development/current/main/investigations/mirbuilder-fact0-transaction-boundary-task-2026-07-20.md"
weakfield_classify0_task = "docs/development/current/main/investigations/mirbuilder-weakfield-classification-task-2026-07-20.md"
finalize0_boundary_consultation = "docs/development/current/main/investigations/mirbuilder-finalize0-boundary-consultation-2026-07-20.md"
finalize0_p0_production_census_consultation = "docs/development/current/main/investigations/mirbuilder-finalize0-p0-production-census-consultation-2026-07-20.md"
finalize0_p0_production_census_task = "docs/development/current/main/investigations/mirbuilder-finalize0-p0-production-census-task-2026-07-20.md"
module_draft_headerport_i0_task = "docs/development/current/main/investigations/mirbuilder-module-draft-headerport-i0-task-2026-07-21.md"

latest_card = "mirbuilder-headerport-reentrant-terminal-consultation-2026-07-21"
latest_card_path = "docs/development/current/main/investigations/mirbuilder-headerport-reentrant-terminal-consultation-2026-07-21.md"
latest_card_summary = """HEADERPORT0-I0 source revalidation found that the selected legacy child terminal holds the mutable invocation port across child lowering, so nested raw Box children cannot reborrow the one collector capability. A constructor route also still restores then bare-publishes. Candidate R-prime separates pending capture from sole ModuleLoweringPort commit and injects a short signature-only header lookup into child finalization. This is an explicit DESIGN-STOP; no partial I0 activation is authorized."""
latest_landed_card_summary = """COMPAREEMIT0 is closed: shared builder Compare now
requires strict current-function/current-block membership, emits one physical
Compare, then commits Bool through one prepared decision; JSON-v0 cf_common is
unchanged. Three independent audits then close FACT0-TX0-D0: normal FieldGet
has a distinct pre-emission typed-reservation residual, while the canonical
generic unified Call has a post-failure annotation residual. No generic
transaction API is selected. FACT0-TX0-CALL-RECEIPT0-S0 is closed: one
disconnected post-success payload retains only final existing annotation
descriptors for the canonical generic unified Call. M0 fixes exactly one future
canonical consumer while excluding receipt-ordered compatibility routes. P0
records signature, Array, and Map failed-Call residuals; I0 receipt-gates all
three existing annotation families after one successful physical Call; G0
freezes that one receipt consumer in the existing FACT0 partition guard. The
distinct typed FieldGet reservation design stop is now closed: one ordinary
non-FastMem FieldGet receipt owner will commit its declared type, field-access
metadata, and origin only after physical FieldGet success. Its disconnected S0
product is closed with no production callers; M0 is the sole next row;
FieldGet/FastMem, ArrayWrite, metadata propagation, and other Call routes stay
parked. FIELDGET0-M0 now fixes one future ordinary `region == None` consumer;
P0 fixes the current typed/untyped success/failure temporal matrix. I0 and G0
now receipt-gate and guard exactly one ordinary consumer. The next producer
family is an explicit selection frontier; FastMem and CorePlan remain excluded.
Historical retained detail: STATICLOAD0-D0 is closed: the
previous sole producer was an
exact StaticDataLoad from an existing sealed static u16 plan. That contract,
not index syntax/value/name/runtime behavior, was Integer representation
authority. STATICLOAD0-S0 was closed as one disconnected prepare-only owner
with zero production consumers; M0 was closed: no lowering-time consumer needed
the current early metadata Integer because successful load already published
transient Integer. P0 was closed: direct Builder and VM/JSON fixtures prove the
pre-I0 transient/metadata baseline, finalization snapshot, unsupported-plan
preflight, failed-emission isolation, and no origin. I0 connected one
successful StaticDataLoad transient-fact commit and removed only its early
metadata write. Successful StaticDataLoad now commits
only transient Integer after emission, and finalized metadata comes only from
normal function finalization. G0 is closed: the reusable manifest-backed FACT0
guard freezes COPY0, CONST0, and STATICLOAD0 together. The next blocker is an
explicit next-producer selection; final metadata is never a lowering authority.
Compare, Select,
operator/call-backed writers, static u16 load, and post-emit duplicate
annotations remain separate rows. Historical summary follows. CARRIER-TYPE-D0
selects the NumericProgression
representation-precondition rebase: the actual R0 rejection stays source-faithful
and no type backfill, retry, or fixture rewrite is authorized. FSESSION0-CENSUS0
is closed with one checked inventory: all 41 expanded prepare/restore leaves,
all 27 direct MirBuilder fields, and two uncovered ValueId-keyed metadata gaps
are explicit. BoxCompilationContext's three uncleared FunctionOwned TypeContext
maps are recorded rather than repaired. S0a-V0 now fixes one private,
fieldful FunctionLoweringState vocabulary and a structural partition guard
without adding Builder storage, consumers, constructors, or Deref
compatibility. The selected next hand is MIRBUILDER-CLEAN0-FSESSION0-S0a-G0.
T0 fixes legacy child-entry,
success/error/panic restoration across every captured FunctionOwned surface and
the existing BoxCompilationContext three-clear/three-retain type-map behavior.
The two metadata-origin gaps remain explicit and unhealed. S0a-G0 now selects
one Census-owned selector/destination map plus an adjacent occurrence guard;
S0b's physical storage cutover follows only after that evidence freezes every
FunctionOwned direct access. I0 is closed: 32 selector/destination routes and
64 broad production/test observation rows record 1,787 syntactic accesses with
no Builder storage or behavior change. P0-S0 replaces that disposable broad
snapshot with a lexical receiver-owner three-domain proof: 96 rows and 1,776
observations (1,175 production, 599 test, 2 shared). P0-R0 closes the nine
mixed-context API owners and the unhealed metadata-origin control: the snapshot
now records 1,792 observations (1,175 production, 615 test, 2 shared), while
outer-to-child visibility and child-to-outer retention remain deliberately
unrepaired. P0-P0 and P0-G0 are closed: Census, the corrected snapshot,
focused lifecycle tests, all-target check, pointer, format, diff, and line
checks are green. S0b-D0 selects one all-route mechanical cutover. S0b-I0/P0/G0
are now closed: all 32 FunctionOwned routes live in one private
FunctionLoweringStateV1, the historical 1,792-route observation remains
separately frozen, and the active 96-row direct-access guard proves old-route
zero. Scope/debug storage is split, the BoxCompilationContext
three-clear/three-retain action and metadata-origin no-isolation control are
preserved, and the individual saved_* transaction is unchanged. S0c-D0 is now
closed: it selects one private move-only captured-subset transaction, never a
whole FunctionLoweringState swap. Legacy and Box mode preserve their exact
existing state actions, metadata origins remain an explicit METAISO control,
and C0 fresh-session construction remains separate. S0c-S0 is closed: one
private transaction vocabulary exists with production consumers still zero.
S0c-P0 is closed: direct legacy/Box parity proves the six-map legacy restore
and three-clear/three-retain Box action while the existing session error/panic
matrix remains green. S0c-I0/G0 are closed: canonical lifecycle now has one
move-only FunctionOwned transaction, separate LegacyCompatibility and
ObservationBorrow snapshots, zero individual FunctionOwned saved captures, and
one Census-owned transaction guard; existing close/error/panic/publication
ordering and METAISO control remain unchanged. S0d is closed: the same Census
now proves the exact captured payload, capture modes, and Box three-clear/
three-retain law with no second fixture or scanner. C0-D0 is closed: three
read-only audits fix the future child borrow contract and prohibit fresh-session
activation until FACT0, PHI0, FINALIZE0, PLAN0, COMPCTX0, CONFIG0, and RAWADAPT0
close. The child may borrow only frozen module truth, entry-sealed configuration,
and a non-restoring observation capability; all mutable compatibility, shared
allocation, direct maps, finalization repair, and parent publication remain
non-authorities. FACT0-S0 is closed: one pure map-free decision treats missing
and Unknown as non-facts, Void as exact, rejects explicit Unknown publication,
and has zero TypeContext/Builder/PHI/production consumers. FACT0-P0-INV0 is
closed: one static source census freezes 47 direct transient-writer paths and
99 occurrences, classified once with five primary family anchors; it is not a
runtime timing proof. FACT0-P0-T0 is closed: eight in-process cases observe
receiver Parameter, Copy, canonical Phi, generic Call, and typed FieldGet
against transient facts, instruction commit, and finalized-draft metadata.
They preserve the observed generic-Call and typed-FieldGet failure residuals,
while explicit Unknown parameters and FastMem FieldLoad remain separate legacy
controls. FACT0-I0-RCV0 is closed: only receiver parameter zero consumes the
monotone decision, from the existing method-skeleton Box(owner) fact after
preflight and before receiver-state mutation. Existing direct parameter commit
remains the one identity-write site, explicit/static Unknown formals remain
legacy, and conflict tests prove no binding/variable/kind/type/origin/slot
publication. FACT0-P1-S0 and P0 are closed: schema v2 now partitions the
unchanged 47-path/99-occurrence census into 58 gap-free ordinal slices and 38
semantic profiles, with two explicit shared lexical sites. The partition fixes
PHI lifecycle, literal/operator, call annotation, field, allocation, and
metadata-facade retirement owners without migrating any raw writer.
FACT0-P1-G0 is also closed: one independent projection guard fixes the 38
profile/prerequisite schedule, 58 slices, two shared sites, status baseline,
and the prohibition on a premature FACT0-G0 prerequisite. PHI0-S0 is closed:
one disconnected private completion vocabulary validates logical predecessor
rows and delegates the existing type decision with zero production consumers
or mutation. PHI0-M0 is closed as an evidence row: raw, final, patch, and
batch do not share one existing predecessor-readiness authority. In particular,
provisional patch and loop-header batch may intentionally name future edges
that a recomputed current CFG cannot see. `PHI0-PRED0-D0` is closed: generic
input/type completion is the one four-entry semantic transaction, while
CFG-ready predecessor validation is a distinct route-scoped capability.
`PHI0-PRED0-S0` is closed: the private vocabulary now separates generic
input/type completion from CFG-ready preparation with zero production
consumers. `PHI0-PRED0-P0` is closed: focused fixtures freeze the legal
unsealed provisional patch and future loop-host edge, while resolved-If and
CorePlan select-as-PHI prove their existing CFG-ready routes. The shared guard
now fixes private CFG-ready consumers at zero and forbids generic patch/batch
predecessor recomputation. `PHI0-I0` is closed: raw/final/patch/batch now use
one generic completion connection, prepare from logical rows, and commit type
facts only after their existing instruction success. Duplicate predecessor rows
fail before mutation, while unsealed patches and batch candidate atomicity stay
intact. `PHI0-CFGREADY0-D0` and S0 are closed: canonical resolved-If is the
sole first route-owned exact predecessor witness, and its disconnected bridge
co-seals reverified CFG truth with exact join rows while remaining unconnected
to PHI completion. P0 closes implicit/explicit actual-exit coverage. I0 now
connects exactly that bridge to the shared successful final-PHI physical commit:
the route-local sidecar is the only CFG-ready consumer, while generic
raw/final/patch/batch remain the four input/type-only consumers and perform no
CFG analysis. G0 freezes that one-consumer/route-exclusion inventory. PHI0-G0
now closes the whole completion series: the exact generic four, one resolved-If
CFG-ready consumer, raw-only origin, and the shared physical continuation are
guarded without new PHI admission. `FACT0-I1-COPY0-D0` is closed as a design
stop: successful LocalSSA Copy currently transfers stored `Unknown`, and that
entry suppresses the adjacent receiver-origin `Box(owner)` fallback. The next
owner is `COPY-UNKNOWN0-D0`; no COPY0 publisher is authorized beforehand.
PHI0 does not widen
origin, provisional Unknown,
finalization, or function-level type authority. `metadata::propagate` remains a distinct
METAPROP0 owner after COMPCTX0/CONFIG0 and before PLAN0-CUT0; FACT0-G0 remains
the final global-zero gate after that owner plus FINALIZE0, PLAN0, COMPCTX0,
CONFIG0, and RAWADAPT0 retire their named legacy writers.
NESTED-REP0 remains parked. A read-only three-worker next-producer inventory
selects only the shared FastMem MemOp receipt: `emit_fastmem_memop` must
preflight function/region, emit the physical MemOp, then publish its region
counter. FastMem FieldLoad reservation, Array write observation, array
receiver/result facts, CorePlan FieldGet, metadata propagation, and finalization
remain separate parked owners. FASTMEM-RECEIPT0-S0 now owns a disconnected
private prepared receipt with no production emitter consumer; M0 must inventory
the existing facade/timing matrix before the single I0 connection. The M0
inventory fixes one physical owner and eight direct helper call sites; P0
freezes the direct/value no-block counter residual. I0 moves the one shared
receipt commit after successful physical MemOp emission, and G0 seals that
single owner through the existing partition guard. The next decision stop is
FASTMEM-FIELDLOAD0-D0 is closed with a behavior-preserving split: FastMem
FieldLoad site plus declared type remain pre-emission reservations, while its
missing-declared Integer compatibility and field-result origin remain
post-success completions. The selected lifecycle must not be silently merged
with ordinary FieldGet or the closed generic MemOp receipt. FASTMEM-FIELDLOAD0
S0 now owns a disconnected Builder-free lifecycle payload with zero production
consumers; M0/P0 now freeze one FieldLoad owner, two semantic entrances, and
declared/missing success/failure timing. I0 alone may connect the prepared
lifecycle to that FieldLoad arm while preserving its pre-emission reservations
and post-success completions. FASTMEM-FIELDLOAD0 is now closed: one selected
lifecycle owns the reservation/completion split, and the existing partition
guard seals its two scoped type lanes and one site/origin owner. Return to
ARRAY-WRITE-OBSERVE0-D0 before opening another code row. FASTMEM-EXPECT0 is
closed: the two stale FastMem tests now assert the existing FieldLoad/FieldStore
MemOp surface and legacy FieldGet/FieldSet absence; full FastMem is 91/91.
Three independent read-only inventories identify Array write receiver
observation as the next distinct pre-emission seam. ARRAY-WRITE-OBSERVE0-D0
is closed: two direct ArrayElementWrite specializations retain the existing
semantic observer and move only its invocation after successful physical
emission. Six temporal witnesses and the existing partition guard seal two
post-success direct consumers while generic Call/Map, FieldStore/index
reservations, and FastMem remain excluded. Three read-only Map audits then
select MAP-WRITE-OBSERVE0-D0: Map Set/Delete/Clear needs a route-specific,
success-only replay chain, not a naive observer move. The chain preserves the
existing semantic-source to LocalSSA receiver fact propagation: terminal
BoxCall commits S, direct Unified commits S then its finalized receiver R when
distinct, and BoxCall-to-Unified commits S then the delegated L/R chain after
one physical Call receipt. No persistent alias map, generic Call API rewrite,
Map Get policy, router/environment change, FieldStore/index, or FastMem work
is authorized. MAP-WRITE-OBSERVE0-S0 is closed: one private non-Clone,
Builder-free Map write replay product retains only existing Set/Delete/Clear
semantic descriptors and the S/L/R schedule. It has zero production consumers
and makes zero fact-map writes. M0 must now freeze the direct Unified,
terminal BoxCall, and BoxCall-to-Unified route inventory plus the exact
failure/success witness matrix before any production connection."""
current_blocker_token = "DPRIME-MIRBUILDER-CLEAN0-FINALIZE0-MODULEDRAFT0-HEADERPORT0-REENTRANT-TERMINAL-DESIGN-STOP-127"
finalize0_census_task = "docs/development/current/main/investigations/mirbuilder-finalize0-census-task-2026-07-20.md"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE plus the active card only unless lane/blocker/restart-order/phase-status/policy changes."

pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

landed_tail = [
  "2026-07-21: FINALIZE0-FACTSESSION0-P0-S0 closes both disconnected products. A cfg(test) harness owns fact-session open/seed/seal/collect and consuming abort beside one test-only Builder restore observer, without moving Builder maps. One parser-backed static guard classifies 11 exact unresolved syntax sites across all seven P0 dispositions and rejects duplicate/unclassified rows. FACTSESSION0-P0-P0 is next; production lifecycle, receipt, repair, JoinIR, and CUT0 consumers remain zero.",
  "2026-07-21: FINALIZE0-FACTSESSION0-P0-P0 closes the disconnected lifecycle proof. Main, synthetic condition_fn, and test-adapter drafts each transport all eight test lanes; nested child success seals before parent restore, while primary/cleanup error and panic abort before restore and panic is resumed. Reused sibling ValueIds receive distinct generations. A test-only prepare_module baseline records the current local ValueId reuse plus stale type fact without claiming isolation; I0 alone may remove it. The parser-backed 11-site syntax matrix remains complete. FACTSESSION0-P0-G0 is next; production lifecycle, issuer, repair, JoinIR, and CUT0 consumers remain zero.",
  "2026-07-21: FINALIZE0-FACTSESSION0-P0-G0 closes one pilot guard. It pins the P0 terminal matrix (success, primary error, cleanup error, panic), exactly four terminal observations, and all P0-only symbols to cfg(test) files, leaving production P0 consumers at zero. The guard also rejects an oversized P0 source/check file. FACTSESSION0-I0 is next; PHI repair, direct repair, unused-Phi deletion, JoinIR, and CUT0 remain forbidden.",
  "2026-07-21: FINALIZE0-FACTSESSION0-I0 source audit found a draft/fact collection cycle. Candidate F-prime requires an unpublished draft and its sealed eight fact lanes to remain inseparable, but current child/root completion publishes bare drafts before any ModuleFactSession collector exists. A facts-only collection, parallel TypeContext, or one-root cutover would violate the selected law. FACTSESSION0-I0-SELECT is a design stop; Candidate A recommends a prior MODULEDRAFT0 collection boundary. No production FACTSESSION connection, receipt issuer, PHI repair, JoinIR, or CUT0 is authorized.",
  "2026-07-21: FINALIZE0-FACTSESSION0-I0-SELECT accepts Candidate A-prime. MODULEDRAFT0 becomes the one unpublished-draft collection prerequisite, followed by an active binding over the existing FunctionLoweringState fact lanes and then FACTSESSION I0. The collector supplies only a same-owned draft signature/header view after M0 census, preserves legacy replace versus canonical reject through prepared admission, and collects before parent restore. MODULEDRAFT0-S0 is next; repair, receipt issuance, type-pipeline/call-await redesign, JoinIR, MODULETX activation, and CUT0 remain forbidden.",
  "2026-07-21: FINALIZE0-MODULEDRAFT0-S0 closes one disconnected private collector. Prepared admission exclusively borrows it through signature/arity sealing, making collect infallible; explicit legacy-replace/canonical-reject policies preserve current duplicate behavior; and the only view borrows a same-owned signature. Four fixtures pass with zero Builder, module, fact-session, finalizer, or production consumer. MODULEDRAFT0-M0 is next; PHI repair, receipt issuance, JoinIR, MODULETX activation, and CUT0 remain forbidden.",
  "2026-07-21: FINALIZE0-MODULEDRAFT0-M0 closes the current_module.functions and publication census. Every eligible lowering-time reader is signature/header-only; no completed body or FunctionMetadata view is needed. The existing signature-only view must gain same-owned symbol presence plus deterministic inventory/count access in P0, never a cloned header cache. Current child publication restores before publishing, so I0 must replace the terminal handoff rather than redirect one helper. Legacy replacement, canonical reject, BindingSSA transaction validation, main, and condition_fn remain separate frozen route laws. MODULEDRAFT0-P0 is next; production collector consumers, parent-restore rewrites, fact sessions, PHI repair, MODULETX activation, and CUT0 remain forbidden.",
  "2026-07-21: FINALIZE0-MODULEDRAFT0-P0 closes disconnected collection proof. The sole collector view now borrows signature, symbol-presence, deterministic inventory, and count from its owned BTreeMap without a header cache. Nine root/child policy profiles, main/condition_fn, replace/reject, preflight failures, and unwind leave correct collector state; existing child success/primary/cleanup/panic restore observations remain green. Existing production still restores before bare publication, so only I0 may replace that handoff. MODULEDRAFT0-I0 is next; facts, PHI repair, finalization reorder, and CUT0 remain forbidden.",
  "2026-07-21: HEADERPORT0-RAWPORT0-LOOPBRIDGE0-SELECT accepts Candidate C-prime, raw Loop child-open quarantine. A pure exact-syntax preflight admits only NoChildFunctionEntry to the unchanged cf_loop and rejects reachable BoxDeclaration before Builder/JoinIR effects; Lambda remains deferred. ModuleLoweringPortV1 never enters JoinIR router, recipe composer, normalized shadow, StepTree, or PlanLowerer. Any successful child-opening Loop or need for a port inside JoinIR reopens a pure-plan/function-session bridge design. LOOPBRIDGE0-S0 is next with zero production consumers.",
  "2026-07-21: HEADERPORT0-RAWPORT0-LOOPBRIDGE0-S0 closes one disconnected pure RawLoopChildEntryDispositionV1. It uses the AST child-topology SSOT, detects direct/expression/nested executable BoxDeclaration, and leaves Lambda/function-declaration bodies deferred. It has no Builder, port, collector, header, JoinIR, source-identity, clone, or mutation authority; four focused fixtures pass. LOOPBRIDGE0-P0 is next; no production Loop route changed.",
  "2026-07-21: HEADERPORT0-RAWPORT0-LOOPBRIDGE0-P0 extends the reusable HeaderPort guard. The raw dispatcher retains exactly one cf_loop delegate and zero classifier consumers; the disconnected classifier retains AST-child-topology/Box/deferred-body laws. JoinIR control-flow has zero module or raw invocation port consumers, and the plan subtree has zero BoxDeclaration or legacy child-function opener. LOOPBRIDGE0-I0 alone may wire the raw invocation boundary before cf_loop.",
  "2026-07-21: HEADERPORT0-RAWPORT0-LOOPBRIDGE0-I0/G0 closes one raw Loop child-open quarantine. One dispatcher Loop call reaches one port boundary; the legacy port delegates unchanged to cf_loop, while the invocation port classifies original syntax and freezes reachable BoxDeclaration before Builder, JoinIR, collector, or header effects. No-child raw invocation/legacy parity and rejection zero-delta fixtures pass. The shared guard fixes one trait, two implementations, no direct bypass, and zero JoinIR/plan port or child-opener consumers. FINALIZE0-MODULEDRAFT0-HEADERPORT0-I0 is next.",
]

# Current State SSOT
#
# Purpose:
# - Keep the active lane / blocker / phase pointers in one compact file.
# - Point to the latest card without copying landed history into every mirror.
# - Let tools/checks/current_state_pointer_guard.sh catch stale pointers.

status = "Active"
date = "2026-06-17"

active_lane = "CURRENT-ENTRY-MANUAL-SYNC-001 manual/current-entry synchronization"
active_lane_status = """Compiler foundation is paused at
COMPILER-FOUNDATION-CHECKPOINT-001 after BoxCallable / TypeAbiCatalog
reconciliation, collection visible semantics closeout, CorePlan variable_map
reseal, PhiTxn, JoinIR exit PHI migration, and normalizer statement-only
prelude adapter. PHI lane has now progressed through
PHI-IMPORT-BOUNDARY-CLOSEOUT-001 and the PHI lifecycle lane is closed for
semantic PHI builders. Exact-AOT optimization resumed through
MIMALLOC-AOT-KERNEL-FRONT-SELECT-002. That row found no meaningful Hako-slower
resident exact-kernel owner after boot amortization and fixed the measurement
harness link path for runtime/string kernels. MIMALLOC-BODY-TIMING-FRONT-SELECT-001
then selected the product-route object-lifecycle body-timing surface:
body_elapsed_ratio=112.969, gap_owner=compiler_lowering, and MIR body owner
local_ssa_copy_materialization. EXPRESSION-MATERIALIZATION-COPY-ORIGIN-PROBE-002
initially selected a param-origin copy-chain path, but the narrow
direct-consumer LocalSSA implementation attempt was rejected because the target
candidate count stayed at seven. PARAM-ALIAS-COPY-OWNER-REFRESH-001 repaired
the diagnostic attribution to use function-wide producers and showed the
current owner is field_get-origin expression chains, not param-origin chains.
The active
row refreshed current-MIR field_get direct-consumer forwarding and selected
cross_block_field_get_alias_copy_chain: forwarding_candidate_copy_count=4,
same_block_candidate_count=1, cross_block_candidate_count=3, and
covered_by_existing_rule_count=0. CROSS-BLOCK-FIELD-GET-ALIAS-FORWARDING-DESIGN-001
selected keeper_shape=dominance_alias with safe_alias_candidate_count=4,
root_dominates_candidate_count=4, and same_field_mutation_candidate_count=0.
The active row implements only that narrow keeper and must remeasure body timing
before any winner claim. CROSS-BLOCK-FIELD-GET-ALIAS-FORWARDING-KEEPER-001
removed the selected candidate family: forwarding_candidate_copy_count_after=0,
copy_count=69, expression_materialization_copy_count=3. Body timing was
remeasured with hako_body_elapsed_ns=364000000, c_body_elapsed_ns=3922424, and
body_elapsed_ratio=92.800, which is not enough for a winner claim. The active
row refreshes the post-keeper owner because the dominant shape moved toward
page_hotpath_helper_attribution / result_materialization.
POST-FIELD-GET-ALIAS-KEEPER-OWNER-REFRESH-001 selected
page_hotpath_helper_result_materialization_copy_chain with
page_hotpath_helpers_call_count=5, page_hotpath_helpers_attributed_copy_count=22,
and result_materialization_copy_count=21.
PAGE-HOTPATH-HELPER-RESULT-MATERIALIZATION-INVENTORY-001 separated the broader
helper attribution from the narrower helper-call-result descendant family:
page_hotpath_helper_result_copy_count=14, dominant_helper=acquire_usize,
dominant_result_sink=copy_only, and selected
page_hotpath_helper_result_copy_chain_narrowing.
PAGE-HOTPATH-HELPER-RESULT-COPY-CHAIN-NARROWING-DESIGN-001 selected
selected_keeper_shape=same_block_call_result_terminal_consumer_rewrite with
terminal_consumer_rewrite_candidate_count=4 and
dependent_dead_copy_candidate_count=10.
PAGE-HOTPATH-HELPER-RESULT-COPY-CHAIN-NARROWING-GUARD-SURFACE-001 fixed the
post target as terminal_consumer_rewrite_candidate_count_after=0 and
candidate_result_copy_count_after<=10, without requiring full 14-copy removal.
PAGE-HOTPATH-HELPER-RESULT-COPY-CHAIN-NARROWING-IMPLEMENTATION-001 rejected the
LocalSSA trial before commit because the post MIR shape did not change and the
trace did not prove the proposed terminal-consumer rewrite seam.
PAGE-HOTPATH-HELPER-RESULT-EMISSION-OWNER-REFRESH-001 then refreshed the actual
emission owner as LocalSSA::ensure_fallback_copy with
first_hop_call_result_copy_count=4, chain_internal_copy_count=10, and
terminal_compare_operand_count=4. The active row designs the narrow fallback
Copy policy before another implementation attempt.
LOCAL-SSA-CALL-RESULT-FALLBACK-COPY-POLICY-DESIGN-001 selected
same_block_call_result_root_for_compare_operand with
post_candidate_result_copy_count_upper_bound=4, allowed_use_kind=CompareOperand,
arg_forwarding_enabled=0, and helper_name_special_case=0. The active row fixes
the post-implementation guard surface before code changes.
LOCAL-SSA-CALL-RESULT-FALLBACK-COPY-POLICY-GUARD-SURFACE-001 fixed the post
target as post_terminal_compare_operand_count=0 and
post_candidate_result_copy_count<=4. The active row implements only that narrow
CompareOperand policy.
LOCAL-SSA-CALL-RESULT-FALLBACK-COPY-POLICY-IMPLEMENTATION-001 removed the
selected family: post_terminal_compare_operand_count=0 and
post_candidate_result_copy_count=0. The active row remeasures body timing before
winner or next-owner selection.
POST-LOCAL-SSA-CALL-RESULT-FALLBACK-COPY-POLICY-MEASUREMENT-001 measured
body_elapsed_ratio=88.801 and selected post_keeper_owner_unclear with low
confidence. POST-LOCAL-SSA-CALL-RESULT-FALLBACK-COPY-POLICY-OWNER-REFRESH-REPEAT-001
selected call_operand_materialization_copy_chain_inventory with
copy_count=55, call_adjacent_copy_count=29, call_operand_route_carrier_copy_count=29,
and selected_owner_confidence=medium. CALL-OPERAND-MATERIALIZATION-COPY-CHAIN-INVENTORY-001
found call_operand_chain_count=26, call_operand_unique_copy_count=29,
safe_forwarding_candidate_count=9, dominance_required_candidate_count=14, and
unknown_root_call_operand_chain_count=3. The active row designs a narrow
forwarding policy before another implementation attempt.
CALL-OPERAND-MATERIALIZATION-FORWARDING-DESIGN-001 selected the tiny
same_block_root_receiver_operand_forwarding keeper with
selected_keeper_candidate_count=2, arg_forwarding_enabled=0,
requires_dominance_guard=0, and helper_name_special_case=0. The active row
fixes the post-implementation guard surface before code changes.
CALL-OPERAND-MATERIALIZATION-FORWARDING-GUARD-SURFACE-001 fixed the post target
as post_selected_keeper_candidate_count=0 and
post_call_operand_unique_copy_count<=27.
CALL-OPERAND-MATERIALIZATION-FORWARDING-IMPLEMENTATION-001 removed the selected
carrier family: post_selected_keeper_candidate_count=0 and
post_call_operand_unique_copy_count=27.
POST-CALL-OPERAND-MATERIALIZATION-FORWARDING-MEASUREMENT-001 measured
body_elapsed_ratio=115.195, so there is no winner claim. The active row refreshes
the owner before another implementation attempt.
POST-CALL-OPERAND-MATERIALIZATION-FORWARDING-OWNER-REFRESH-001 selected
call_operand_residual_policy_selection with arg_same_block_root_call_operand_chain_count=7,
dominance_required_candidate_count=14, unknown_root_call_operand_chain_count=3,
and selected_owner_confidence=medium. The active row chooses the next residual
policy family before another implementation attempt.
CALL-OPERAND-RESIDUAL-POLICY-SELECTION-001 selected
dominance_required_call_operand_forwarding with selected_policy_candidate_count=14.
CALL-OPERAND-DOMINANCE-REQUIRED-FORWARDING-DESIGN-001 selected
dominance_guarded_receiver_operand_forwarding with selected_keeper_candidate_count=13,
rejected_arg_forwarding_count=1, and requires_dominance_guard=1.
CALL-OPERAND-DOMINANCE-REQUIRED-FORWARDING-GUARD-SURFACE-001 fixed the post
target as post_selected_keeper_candidate_count=0 and
post_call_operand_unique_copy_count<=14.
CALL-OPERAND-DOMINANCE-REQUIRED-FORWARDING-IMPLEMENTATION-001 rejected the
LocalSSA emission-time seam before commit: the best narrow attempt left one
candidate, while observed-CFG dominance perturbation regressed the target family
to 12 candidates. CALL-OPERAND-CFG-STABLE-RECEIVER-REWRITE-DESIGN-001 selected
mir_passes_callsite_canonicalize_receiver_operand_rewrite as the CFG-stable
owner. The active row fixes the implementation guard surface before code
changes. CALL-OPERAND-CFG-STABLE-RECEIVER-REWRITE-GUARD-SURFACE-001 fixed the
post target as post_selected_keeper_candidate_count=0 and
post_call_operand_unique_copy_count<=14. CALL-OPERAND-CFG-STABLE-RECEIVER-REWRITE-IMPLEMENTATION-001
removed the selected receiver family with post_selected_keeper_candidate_count=0
and post_call_operand_unique_copy_count=13. The active row remeasures body
timing before winner or next-owner selection.
POST-CFG-STABLE-RECEIVER-REWRITE-MEASUREMENT-001 measured
measurement_repeat_count=4, median hako_body_elapsed_ns=6500000,
median c_body_elapsed_ns=3962131, and body_elapsed_ratio=1.640. The active row
repeats stability measurement before closeout or next-owner selection.
POST-CFG-STABLE-RECEIVER-REWRITE-STABILITY-MEASUREMENT-001 confirmed a stable
6-7ms Hako body floor with median hako_body_elapsed_ns=6000000,
c_body_elapsed_ns=3352143, body_elapsed_ratio=1.790, and winner_claim=1. The
active row closes the receiver operand copy-chain owner before any new
implementation family.
MIMALLOC-BODY-TIMING-CFG-STABLE-RECEIVER-REWRITE-CLOSEOUT-001 closed the
receiver operand copy-chain owner with winner_claim=1. The active row selects a
fresh next owner or pauses from the current body-timing evidence.
MIMALLOC-BODY-TIMING-NEXT-OWNER-SELECTION-001 found current_body_elapsed_ratio=1.865
and fresh_gap_owner=hako_runtime_baseline with low confidence, so no fresh
compiler implementation owner is selected. The active row checkpoints the
compiler-lowering optimization run.
MIMALLOC-COMPILER-LOWERING-OPTIMIZATION-CHECKPOINT-001 landed the pause.
MIMALLOC-BODY-TIMING-PRECISION-001 classified the Hako/C body timer mismatch:
hako_timer_family=workload-body-env-now-ms-v0,
c_timer_family=workload-body-monotonic-v0, timer_family_matched=0,
measurement_boundary_confidence=low, and body_elapsed_ratio_raw=1.579. The
active row inventories runtime/object/generated-runtime boundaries before any
implementation. MIMALLOC-RUNTIME-BOUNDARY-INVENTORY-001 found visible
BoxCallable/RoutePlan, Arc/ObjectHandle, host handle, runtime helper, and
generated-runtime seams, but selected_owner=none with selected_owner_confidence=low
because measurement_boundary_confidence=low. Closed-world route specialization
therefore remains gated. MIMALLOC-BODY-TIMER-ALIGNMENT-PROBE-001 scaled the
in-process repeat to 65536 and reduced Hako timer resolution to 1.887% of the
measured body window; scaled_body_elapsed_ratio=2.039.
MIMALLOC-RUNTIME-BOUNDARY-DIRECT-PROBE-001 then found top_symbol=nyash_array_length_h
with top_symbol_percent=70.61 in a scaled perf run.
MIMALLOC-DIRECT-ARRAY-LENGTH-BOUNDARY-DESIGN-001 selected
direct_array_length_route_cache, because MIR already publishes
generic_method.len/array_slot_len but the helper remains a runtime boundary.
MIMALLOC-ARRAY-LEN-HELPER-FASTPATH-PROBE-001 selected host_handle_lookup as
the helper-internal owner and allowed one narrow implementation.
MIMALLOC-ARRAY-LEN-HELPER-BORROWED-READY-IMPLEMENTATION-001 switched only
`nyash_array_length_h` to the existing borrowed-ready path and kept a small
win: body_elapsed_ns 54ms -> 53ms and top_symbol_percent 72.06 -> 68.13. The
post-helper owner refresh classified the remaining residue as the handle
registry / typed handle boundary, with implementation_allowed=0,
closed_world_route_required=1, and object_substrate_required=1. The active row
inventoried Arc / HostHandle / runtime helper / Box method / dynamic route
boundaries: closed_world_direct_method_candidate_count=31,
exact_native_struct_candidate_count=9, scalarized_object_candidate_count=5,
and selected_object_boundary_owner=object_handle_boundary_inventory. The active
row adds the ObjectStoragePlan vocabulary/guard surface before shadow planning.
OBJECT-STORAGE-PLAN-SSOT-001 added the passive
`src/object_storage_plan.rs` vocabulary with execution disabled.  The shadow row
selected `HakoAllocObjectLifecycleAlignmentResult` as the first medium
confidence primitive-only exact-object pilot candidate.  EXACT-OBJECT-PILOT-001
then blocked implementation because the candidate is published through
`HakoAllocObjectLifecycleFacade.alignment_result`: publication_boundary_count=8
with one set, seven gets, and eight candidate method calls.
EXACT-OBJECT-NESTED-PUBLICATION-PLAN-001 selected
representation_choice=flatten_nested_fields after proving nested_handle_escape_count=0.
EXACT-OBJECT-PILOT-001R then blocked the direct pilot retry because the backend
has no explicit flattened nested field consumer:
backend_flattened_nested_field_consumer=0 and
existing_known_receiver_direct_call_requires_handle=1.
EXACT-OBJECT-FLATTENED-NESTED-FIELD-LAYOUT-SSOT-001 defined the passive layout:
four flattened fields named alignment_result.last_requested,
alignment_result.last_normalized, alignment_result.last_reason, and
alignment_result.last_supported, with backend_lowering_enabled=0.
EXACT-OBJECT-FLATTENED-NESTED-FIELD-SHADOW-001 found seven get candidates, one
set candidate, seven nested method candidates, and fallback_reason_count=0.
EXACT-OBJECT-FLATTENED-NESTED-FIELD-BACKEND-SEAM-001 added a passive exact-AOT
backend seam with backend_flattened_nested_field_consumer=1 while keeping
backend_lowering_enabled=0.  EXACT-OBJECT-PILOT-001S blocked enablement because
state_sharing_seam_ready=0: field_access_flattened_nested_route_enabled=0,
method_call_flattened_nested_route_enabled=0, and the typed newbox path still
preempts local aggregate state.  EXACT-OBJECT-FLATTENED-NESTED-FIELD-STATE-SEAM-001
defined the passive backend state-sharing seam with state_sharing_seam_defined=1
while keeping field access routes, method-call routes, and backend lowering
disabled.  The active row re-runs the guarded pilot preflight and selects the
next route-wiring row if consumers still cannot use the shared state seam.
EXACT-OBJECT-PILOT-001T confirmed state_sharing_seam_defined=1 but
route_wiring_ready=0 because field_access_flattened_nested_route_enabled=0 and
method_call_flattened_nested_route_enabled=0.  The active row wires those
consumers to the passive state seam while keeping backend_lowering_enabled=0.
EXACT-OBJECT-FLATTENED-NESTED-FIELD-ROUTE-WIRING-001 added disabled passive
hooks in field_access.py and method_call.py, yielding route_wiring_ready=1 with
backend_lowering_enabled=0.  EXACT-OBJECT-PILOT-001U enabled the first guarded
exact-object pilot for this single closed-world front:
state_sharing_seam_defined=1, route_wiring_ready=1, backend_lowering_enabled=1,
object_storage_plan_execution_enabled=1, and pilot_exact_object_enabled=1 while
keeping MIRBuilder object management, benchmark/helper-name branches, product
default changes, and global Arc retirement claims at zero.
EXACT-OBJECT-PILOT-MEASUREMENT-001 measured the enabled pilot with
body_elapsed_ratio_before=112.969 and body_elapsed_ratio_after=114.326, so
winner_claim=0.  EXACT-OBJECT-PILOT-EFFECT-ATTRIBUTION-001 found
flattened_nested_route_reached=0:
alignment_result.last_requested/last_normalized/last_reason/last_supported
synthetic owner-field evidence is absent from generated artifacts.  The active
row found that the measured exact-EXE route uses ny-llvmc's boundary C ABI
shim, not the Python llvmlite seam, and that MIR JSON has no ObjectStoragePlan
metadata for the boundary driver to consume. The active row exports the selected
flattened nested ObjectStoragePlan into MIR JSON. The boundary consumer row
teaches the measured ny-llvmc boundary C ABI route to consume that exported
plan, EXACT-OBJECT-PILOT-001V confirmed the guarded pilot is enabled through
the measured route, and EXACT-OBJECT-PILOT-MEASUREMENT-002 measured no
performance win. The active row closes this first exact-object pilot as a
no-keeper boundary experiment and returns the lane to fresh owner selection.
RECORD-BOX-SURFACE-000 then fixed the user-facing language split as
two-surface / one-substrate: record is identity-free value aggregate, box is
identity / behavior / lifecycle boundary, and the compiler may share
AggregateStoragePlan / ObjectStoragePlan only where proof permits.
RECORD-BOX-DOCS-001 updated the reference docs with data/value -> record and
thing/owner/behavior/lifecycle -> box wording. AGG-STORAGE-PLAN-000 added the
passive `src/aggregate_storage_plan.rs` vocabulary with execution disabled.
AGG-OBJECT-STORAGE-BRIDGE-001 documented the shared backend lowering concepts
without merging source semantics. RECORD-METHODS-GATE-000 and
RECORD-WITH-BOX-GATE-000 kept record methods disabled and `with` record-only.
SIMPLE-BOX-EXACT-OBJECT-CANDIDATE-001 parked simple-box exact-object
implementation because fresh_high_confidence_owner_evidence=0; simple boxes may
later use the same backend substrate only through ObjectStoragePlan / RoutePlan,
not by treating record semantics as proof for boxes.
MANUAL-SYNC-RECORD-BOX-001 synchronized README and boxes-system manual entry
points with the current two-surface / one-substrate model, while keeping older
Everything-is-Box pages as historical references. MANUAL-SYNC-CONCURRENCY-001
synchronized the manual concurrency entry points: co is the canonical
structured Future ownership surface, nowait is not an OS-thread spawn promise,
worker_scope remains design-only, workers=N is an upper-bound hint, raw thread
syntax remains closed, and ThreadApi / WorkerPoolScheduler remain runtime
substrate. CURRENT-ENTRY-MANUAL-SYNC-001 aligned root/current restart pointers
and docs navigation with those manual sync rows.
The
previous keeper is
BOOL-SCALAR-LOWERING-001 for
kilo_micro_userbox_flag_toggle. `kilo_micro_userbox_counter_step_chain` remains
a startup sentinel, not the kernel optimization front. Product NyRT startup,
Type ABI / plugin ABI, MIRBuilder truth changes, Arc retirement, and object
substrate replacement remain out of scope. MIMALLOC-BODY-TIMING-FRESH-OWNER-SELECTION-002
remeasured the object-lifecycle body surface, selected
mir_call_selectPage_compare_eq_expression_copy_chain as the fresh narrow owner,
and keeps implementation closed until MIR-CALL-EXPRESSION-COPY-CHAIN-POLICY-SELECTION-001
pins the policy. MIR-CALL-EXPRESSION-COPY-CHAIN-POLICY-SELECTION-001 selected
mir_call_compare_operand_value_forwarding_candidate_probe with medium
confidence and keeps implementation closed until a candidate probe fixes the
safe set. MIR-CALL-COMPARE-OPERAND-FORWARDING-CANDIDATE-PROBE-001 found two
dominance-required compare_eq candidates and unsafe_candidate_count=0; the next row
must pin the guard surface before implementation. MIR-CALL-COMPARE-OPERAND-FORWARDING-GUARD-SURFACE-001
opened exactly one narrow seam: dominance-guarded MIR-call-result compare
operand forwarding, with post candidate count 0 required. MIR-CALL-COMPARE-OPERAND-FORWARDING-IMPLEMENTATION-001
implemented only that seam and reduced post_compare_operand_forwarding_candidate_count
to 0. MIR-CALL-COMPARE-OPERAND-FORWARDING-MEASUREMENT-001 fixed the stale
LLVM C shim declaration surface that blocked remeasurement, then measured
hako_body_elapsed_ns=365000000, c_body_elapsed_ns=3727908, and
body_elapsed_ratio=97.910. The MIR shape cleanup is not a body-time winner.
POST-MIR-CALL-COMPARE-OPERAND-FORWARDING-OWNER-REFRESH-001 selected
local_ssa_block_entry_phi_edge_copy_family with medium confidence:
local_ssa_copy_materialization_copy_count=20, phi_edge_copy_count=18,
block_entry_copy_count=10, and compare_operand_route_carrier_copy_count=0.
LOCAL-SSA-BLOCK-ENTRY-PHI-EDGE-COPY-DESIGN-001 kept implementation closed:
PHI-edge copies require PHI lifecycle ownership, block-entry copies require a
split candidate probe, and broad LocalSSA coalescing remains forbidden.
LOCAL-SSA-BLOCK-ENTRY-PHI-EDGE-COPY-CANDIDATE-PROBE-001 found
safe_candidate_count=0: phi_edge_copy_count=18, block_entry_copy_count=10,
block_entry_route_none_count=7, and block_entry_route_carrier_count=3. This
family is a no-safe-candidate non-keeper until a separate freshness/PHI
lifecycle owner exists.
LOCAL-SSA-BLOCK-ENTRY-PHI-EDGE-NO-SAFE-CANDIDATE-CLOSEOUT-001 closed that
family as a non-keeper and returns the lane to owner-first selection before any
new implementation. FRESH-OWNER-SELECTION-AFTER-LOCAL-SSA-NO-SAFE-CANDIDATE-001
selected body_timing_rebaseline_after_local_ssa_nonkeeper, because the last
measurement predates the no-safe-candidate closeout and no fresh high-confidence
implementation owner is open.
MIMALLOC-BODY-TIMING-REBASELINE-AFTER-LOCAL-SSA-NONKEEPER-001 remeasured
body_elapsed_ratio=79.586 and confirmed compiler_lowering remains visible, but
the dominant residue is PHI-edge / block-entry copy materialization requiring
PHI lifecycle and freshness design before any further implementation.
PHI-LIFECYCLE-FRESHNESS-DESIGN-001 fixed that boundary: PHI lifecycle owns
Reserve/Define/Populate/Finalize, variable_map is only a defined-value emission
cache, LocalSSA remains block-local operand materialization, and PHI-edge /
block-entry copy optimization cannot reopen without freshness / PHI lifecycle
proof. PHI-LIFECYCLE-FRESHNESS-GUARD-SURFACE-001 fixed the guard vocabulary and
keeps implementation closed until a future inventory proves exactly one owner.
PHI-LIFECYCLE-FRESHNESS-INVENTORY-001 applied that guard surface and found no
proven owner: phi_edge requires PHI lifecycle proof, route-none block-entry
requires freshness proof, and route-carrier block-entry requires route-specific
operand policy. Implementation remains closed and the lane returns to fresh
owner selection. MIMALLOC-BODY-TIMING-FRESH-OWNER-SELECTION-AFTER-PHI-FRESHNESS-NO-OWNER-001
kept the PHI freshness family closed and selected route_carrier_residual_inventory
as the next non-implementation row. ROUTE-CARRIER-RESIDUAL-INVENTORY-001
classified the remaining route-carrier residue as call_operand-only and selected
call_operand_route_carrier_policy_selection next, with implementation still
closed. CALL-OPERAND-ROUTE-CARRIER-POLICY-SELECTION-001 selected
cfg_stable_call_operand_route_carrier_revalidation because the residual count
matches the previously selected receiver-only CFG-stable family; implementation
remains closed until a revalidation guard surface fixes the current post target.
CALL-OPERAND-ROUTE-CARRIER-REVALIDATION-GUARD-SURFACE-001 fixed that the
current coarse call_operand role is insufficient and selected a receiver/arg
split probe before any implementation owner can reopen.
CALL-OPERAND-ROUTE-CARRIER-RECEIVER-ARG-SPLIT-PROBE-001 extended the observation
probe and measured receiver=2 / arg=11; arg remains closed and the next row
classifies the two receiver residue samples. CALL-OPERAND-RECEIVER-RESIDUE-CLASSIFICATION-001
classified both receiver samples as same_block_self_receiver_materialization and
selected a narrow design row; the prior CFG-stable receiver keeper and arg
forwarding remain closed. CALL-OPERAND-SAME-BLOCK-SELF-RECEIVER-MATERIALIZATION-DESIGN-001
found that the same-block receiver seam already landed, selected no new
receiver owner, and moved the lane to explicit arg residue policy selection.
CALL-OPERAND-ARG-RESIDUE-POLICY-SELECTION-001 found no safe narrow arg owner,
kept arg forwarding closed, and selected call-operand route-carrier closeout
plus fresh owner selection.
CALL-OPERAND-ROUTE-CARRIER-CLOSEOUT-AND-FRESH-OWNER-SELECTION-001 closed
receiver and arg route-carrier lanes, selected no implementation owner, and
selected body timing rebaseline after call-operand closeout.
MIMALLOC-BODY-TIMING-REBASELINE-AFTER-CALL-OPERAND-CLOSEOUT-001 reran the
canonical direct-exact Hako/C pair and current MIR copy-position probe, found
body_elapsed_ratio=2.138 with gap_owner=measurement_harness, and kept
implementation closed pending measurement hygiene refresh.
MIMALLOC-MEASUREMENT-HYGIENE-REFRESH-001 repeated the canonical direct-exact
pair five times, found ratio median 2.119 / max 2.363, did not reproduce the
old 79.586x body gap, and selected measurement state provenance inventory
before any implementation can reopen.
MIMALLOC-MEASUREMENT-STATE-PROVENANCE-INVENTORY-001 compared row753 and current
provenance, found current root/empty reprobes do not reproduce 79.586x, and
closed the old large gap as stale/transient measurement evidence before moving
to current 2x runtime boundary inventory.
MIMALLOC-RUNTIME-BOUNDARY-INVENTORY-FOR-CURRENT-2X-GAP-001 found visible
BoxCallable/RoutePlan, Arc/ObjectHandle, HostHandle, runtime helper, and
generated-runtime seams under the current ~2x gap, but selected no
implementation owner because no single hot owner is proven yet. The active row
must attribute the current ~2x gap with asm/perf boundary evidence before any
implementation can reopen.
MIMALLOC-CURRENT-2X-ASM-BOUNDARY-ATTRIBUTION-001 measured the current direct-exact
route with helper-symbol annotation: top_symbol=nyash_array_length_h at 70.71%,
with the helper already on the borrowed-ready path. The remaining owner is
handle_registry_typed_handle_boundary with high confidence, but implementation
stays closed because the next step is a design row for the clean
RoutePlan/ObjectStoragePlan boundary.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-DESIGN-001 selected
closed_world_handle_resolution_plan as the design-only seam: RoutePlan proves
the callable route, ObjectStoragePlan proves the receiver representation, and
the exact-AOT backend consumes both plans. Helper patching, global
HostHandle/Arc retirement, MIRBuilder object management, and product default
changes remain closed until a proof surface exists.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-PROOF-SURFACE-001 defined the
required report surface for that plan: route proof, storage proof, escape /
publication rejection fields, and explicit fallback requirements. Backend
direct handle bypass remains disabled and the next row is a read-only candidate
inventory, not implementation.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-CANDIDATE-INVENTORY-001 found
the route proof is present for ArrayBox.length / array_slot_len, but the hot
Array receiver lacks an ObjectStoragePlan proof. The candidate is rejected with
eligible_site_count=0 and selected_blocker=receiver_storage_plan_missing, so
implementation remains closed and the next row is storage proof design.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-STORAGE-PROOF-DESIGN-001 selected
array_receiver_storage_proof_via_object_storage_plan: storage proof must come
from ObjectStoragePlan / ArrayRepr residence, not public ArrayBox handle
reinterpretation or backend raw layout inference. Implementation remains
closed; next row defines the concrete storage proof report surface.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-STORAGE-PROOF-SURFACE-001 fixed
the concrete Array receiver storage proof fields: owner, residence, direct
facts, materialization route, public-handle reinterpretation ban, raw-layout
inference ban, and fallback to public ArrayBox. Backend direct handle bypass
remains disabled; next row inventories those fields.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-STORAGE-PROOF-INVENTORY-001
filled the inventory and rejected the current bypass candidate: route proof is
available for ArrayBox.length / array_slot_len, but the hot receiver has
array_receiver_storage_owner=none, array_receiver_storage_residence=none,
array_receiver_direct_facts_proven=0, and
array_receiver_host_handle_publication_before_read=1. Implementation remains
closed; next row designs an explicit Array residence proof.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-DESIGN-001
selected array_receiver_residence_proof_via_arrayrepr: RoutePlan proves the
array_slot_len callable route, while ArrayRepr / ObjectStoragePlan must prove
the receiver residence and materialization route. Public ArrayBox handle
reinterpretation, backend raw layout inference, route-proof-as-storage-proof,
MIRBuilder object management, and backend direct handle bypass remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-SURFACE-001
fixed the concrete Array residence proof fields: owner, residence, direct facts
source, materialization route, public-handle ban, raw-layout ban,
route-proof-as-storage-proof ban, and public ArrayBox fallback. Implementation
remains closed; next row inventories those fields.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-INVENTORY-001
filled the residence inventory and rejected the current direct bypass:
array_receiver_residence_owner=none, array_receiver_residence=none,
array_receiver_direct_facts_source=none, direct facts/materialization are not
proven, and HostHandle publication still occurs before the read. Implementation
remains closed; next row designs the producer for explicit Array residence
evidence.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-DESIGN-001
selected array_receiver_residence_fact_producer owned by RepresentationPlanner /
ArrayReprFactProducer. The producer emits analysis facts from RoutePlan,
DirectArrayAccessPlan, ObjectStoragePlan, and escape facts before backend
lowering; MIRBuilder, backend layout inference, helper-name inference, and
public ArrayBox handle reinterpretation remain rejected.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-SURFACE-001
fixed ArrayReceiverResidenceFact producer report fields: owner, input
availability, output, ordering, rejection of runtime/backend/MIRBuilder/helper
inference, fallback preservation, and materialization route requirements.
Producer implementation and backend direct handle bypass remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INVENTORY-001
filled the producer inventory: RoutePlan input is available, but DirectArrayPlan
and ObjectStoragePlan inputs are not available for the hot receiver, so no
ArrayReceiverResidenceFact producer can be implemented yet. Implementation
remains closed; next row designs the required producer input.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-DESIGN-001
selected array_receiver_residence_input_surface as the missing representation
input. The input may reference DirectArrayAccessPlan but is not
DirectArrayAccessPlan alone; it must support length receiver residence, preserve
public ArrayBox fallback, and reject backend layout inference, helper-name
inference, public handle reinterpretation, and MIRBuilder ownership.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SURFACE-001
fixed ArrayReceiverResidenceInput report fields and the open gate. Input
implementation, producer implementation, and backend direct handle bypass remain
closed until an inventory proves a high-confidence input candidate.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-INVENTORY-001
filled the input inventory: RoutePlan and escape facts are available, but
DirectArrayAccessPlan, ObjectStoragePlan, and ArrayRepr input sources are not
available for the hot receiver, so ArrayReceiverResidenceInput implementation
remains closed. Next row designs the missing input source.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-DESIGN-001
selected ArrayReceiverResidenceInputSource as a representation-planner source
record consumed by ArrayReceiverResidenceInput. It may reference
DirectArrayAccessPlan but must also include ArrayRepr or ObjectStoragePlan
evidence, escape/publication evidence, and a materialization route.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-SURFACE-001
fixed ArrayReceiverResidenceInputSource report fields and the open gate. Source
implementation, input implementation, producer implementation, and backend
direct handle bypass remain closed until source inventory proves a
high-confidence candidate.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-INVENTORY-001
filled the source inventory: route, publication, and materialization evidence
exist, but no ArrayRepr or ObjectStoragePlan source reaches the hot receiver
before the length read. Source implementation remains closed; next row designs
the construction seam.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-CONSTRUCTION-DESIGN-001
selected ArrayReceiverResidenceSourceConstructor as an analysis-only
RepresentationPlanner / ArrayReprSourcePlanner seam that joins RoutePlan,
escape/publication facts, materialization route, ArrayRepr, ObjectStoragePlan,
and optional DirectArrayAccessPlan input.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-CONSTRUCTION-SURFACE-001
fixed ArrayReceiverResidenceSourceConstructor report fields and the open gate.
Constructor implementation, source implementation, input implementation,
producer implementation, and backend direct handle bypass remain closed until
constructor inventory proves a high-confidence candidate.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-RESIDENCE-PRODUCER-INPUT-SOURCE-CONSTRUCTION-INVENTORY-001
filled the constructor inventory: RoutePlan, escape/publication, and
materialization inputs are present, but the required ArrayRepr or
ObjectStoragePlan input is missing. Constructor implementation remains closed;
next row designs the upstream representation source.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-DESIGN-001
selected ArrayReceiverRepresentationSource as the upstream representation
source consumed by the constructor. It is owned by RepresentationPlanner /
ObjectStoragePlan / ArrayRepr and may provide fallback or direct representation
evidence without backend layout inference.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-SURFACE-001
fixed the ArrayReceiverRepresentationSource report surface and open gate:
owner/output/scope/route kind, ArrayRepr/ObjectStoragePlan/DirectArrayAccessPlan
fields, materialization route, public ArrayBox fallback preservation, and
backend/MIRBuilder/helper/public-handle inference bans. Source implementation
remains closed until inventory proves a high-confidence candidate.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-INVENTORY-001
selected the high-confidence fallback-only representation source:
ArrayRepr::PublicArrayBoxFallback. DirectI64 / ObjectStoragePlan direct proof
is still absent, so backend direct handle bypass stays closed; the next row may
implement only the passive fallback source.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-IMPLEMENTATION-001
implemented the passive fallback-only ArrayReceiverRepresentationSource
vocabulary in src/array_receiver_representation_source.rs. It reports
PublicArrayBoxFallback and explicitly proves no direct storage or backend
bypass; constructor, MIR JSON, and backend consumers remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-DESIGN-001
designed the next constructor handoff: PublicArrayBoxFallback may flow as a
fallback residence candidate, but it still proves no direct storage and
authorizes no backend direct handle bypass.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-IMPLEMENTATION-001
implemented passive fallback handoff vocabulary in
src/array_receiver_representation_source.rs. It keeps source_connected_to_constructor=0,
source_consumed_by_backend=0, and backend direct handle bypass disabled.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-CONSUMER-INVENTORY-001
inventoried the consumer surface: the only candidate is the design-only
ArrayReceiverResidenceSourceConstructor. The handoff is safe only as a
fallback residence candidate; direct residence proof, MIR JSON export, backend
consumption, and direct handle bypass remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-CONSUMER-IMPLEMENTATION-DESIGN-001
selected the fallback-only consumer implementation shape: the constructor may
accept ArrayReceiverConstructorHandoff only as fallback residence evidence and
may emit ArrayReceiverResidenceInputSource|none, while direct input, MIR JSON
export, backend consumption, and direct handle bypass stay closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-CONSUMER-IMPLEMENTATION-001
implemented passive fallback-only constructor vocabulary in
src/array_receiver_representation_source.rs. It connects the handoff to
ArrayReceiverResidenceSourceConstructor as fallback input-source vocabulary
only; MIR JSON export, backend consumption, direct proof, and direct handle
bypass remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-CONSTRUCTOR-HANDOFF-INPUT-SOURCE-CONSUMER-INVENTORY-001
inventoried the next consumer as ArrayReceiverResidenceInput, but classified
the proof chain as controlled_but_long. Before implementing the next stage, the
lane should add a thin facade / naming map that preserves the proof gates while
reducing patch-surface ambiguity.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-CHAIN-THINNING-DESIGN-001
selected ArrayReceiverResidenceProofChain as the developer-facing facade and
kept the existing staged reports as internal proof gates. ConstructorHandoff is
kept as compatibility vocabulary but is no longer the primary mental model.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-CHAIN-THINNING-SURFACE-001
fixed the facade report surface and open gate. The next implementation may add
construct_input_source_from_representation_source, but must preserve stage
reports and keep direct proof, MIR JSON export, backend consumption, and direct
handle bypass closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-CHAIN-THINNING-IMPLEMENTATION-001
implemented ArrayReceiverResidenceProofChain as the developer-facing facade and
added a short navigation note. It preserves the compatibility handoff path and
keeps direct proof, MIR JSON export, backend consumption, and direct handle
bypass closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-CHAIN-THINNING-CLOSEOUT-001
closed the BoxShape thinning detour: ArrayReceiverResidenceProofChain is the
developer-facing entry, ConstructorHandoff is compat vocabulary, proof gates
stay preserved, and the next row returns to ArrayReceiverResidenceInput
consumer design with backend/direct routes closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-INPUT-CONSUMER-DESIGN-001
reconnected the existing ArrayReceiverResidenceInput surface to the current
ProofChain entry. The selected mode remains fallback-only:
ArrayReceiverResidenceInputSource -> ArrayReceiverResidenceInput, with no
direct storage proof, MIR JSON export, backend consumption, or direct handle
bypass.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-INPUT-CONSUMER-SURFACE-001
defined the concrete passive ArrayReceiverResidenceInput surface and open gate:
fallback public ArrayBox evidence may become a fallback residence input, while
direct storage proof, MIR JSON export, backend consumption, and direct handle
bypass remain closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-INPUT-CONSUMER-IMPLEMENTATION-001
implemented passive ArrayReceiverResidenceInput vocabulary and
ArrayReceiverResidenceInput::from_input_source. The implementation records only
fallback public ArrayBox residence evidence and keeps direct proof, MIR JSON
export, backend consumption, and direct handle bypass closed.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-INPUT-CONSUMER-CLOSEOUT-001
closed the passive residence input stage. The next stage would be
ArrayReceiverResidenceFact / backend-consumable proof, so the active blocker is
a design consultation rather than another automatic implementation row.
MIMALLOC-HANDLE-REGISTRY-TYPED-HANDLE-BOUNDARY-ARRAY-REPR-OBJECT-STORAGE-SOURCE-RESIDENCE-FACT-BOUNDARY-DESIGN-001
resolved that consultation as B_plus_C_lite: fallback-only public ArrayBox
evidence stops at ArrayReceiverResidenceInput and does not produce
ResidenceFact. Fact names are reserved for direct/backend-consumable proof, so
the next row selects the direct residence proof owner instead of implementing a
fallback fact producer.
LOCAL-FIRST-OBJECT-MODEL-SSOT-001 then reset the design direction: exact-AOT
starts with local unpublished objects and publishes to public Box / HostHandle /
Arc only at explicit public boundaries. ObjectPlan owns representation plus
publication sites for now; a standalone PublicationPlan is deferred until
ObjectPlan proves too large. The ArrayReceiver residence chain is not extended
into fallback facts.
OBJECT-PLAN-LOCAL-FIRST-000 added passive ObjectPlan vocabulary to
src/object_storage_plan.rs: LocalFirstObjectPlan carries ObjectStoragePlan plus
ObjectPublicationSite entries, while standalone PublicationPlan, MIR JSON
export, backend consumption, and lowering remain disabled.
OBJECT-PUBLICATION-INVENTORY-001 inventoried the target facade body
conservatively: page is the only local object candidate, it has three
pre-publication direct calls, and it publishes through two recordLastAllocPage
sites. The facade body has no Array length direct candidate, so the next shadow
row must not claim an Array length pilot from this inventory alone.
LOCAL-OBJECT-SHADOW-001 built the report-only shadow plan from that inventory:
page is a local identity candidate and published fallback exists, but
array_length_direct_candidate_count=0 and local_direct_array_len_pilot_open=0.
LOCAL-DIRECT-ARRAY-LEN-PILOT-001 preflight therefore did not implement the pilot:
the target facade body has no pre-publication Array.length candidate. The lane
is now at a design consultation point: retarget to the actual page local
candidate, find a new Array.length front, or pause local-first implementation.
LOCAL-FIRST-DIRECT-PILOT-SELECTION-001 resolved that consultation: the current
front Array.length pilot is closed, and the next pilot is
local_known_receiver_direct_call with first target page.acquire_usize/reuse.
The selected scope is direct_call_only; page-specific branches, method-name
special cases, storage direct, HostHandle bypass, Arc retirement, and product
runtime changes remain closed.
LOCAL-PAGE-RECEIVER-CANDIDATE-PROBE-001 confirmed that page is a queue-selected
receiver, not a body-local birth. It has three pre-publication calls and two
publication sites, with storage direct and HostHandle bypass still closed. The
next row is the known receiver direct-call guard surface.
LOCAL-KNOWN-RECEIVER-DIRECT-CALL-GUARD-SURFACE-001 opened shadowing only:
pre-publication receiver, known type, known method surface, no dynamic/plugin/task
escape, and no after-publication calls are required; implementation, storage
direct, HostHandle bypass, Arc retirement, and product runtime changes remain
closed.
LOCAL-KNOWN-RECEIVER-DIRECT-CALL-SHADOW-001 produced a report-only shadow with
three guarded candidates for page acquire_usize/reuse. LOCAL-KNOWN-RECEIVER-DIRECT-CALL-PILOT-001
found that the existing `user_box_method_routes` C shim consumer already
provides the generic RoutePlan backend seam, so no new lowering code was added;
LOCAL-KNOWN-RECEIVER-DIRECT-CALL-MEASUREMENT-001 measured the current
direct-exact body timing with in_process_repeat=65536 and found
hako_body_elapsed_ns=24000000, c_body_elapsed_ns=28710275,
body_elapsed_ratio=0.836, winner_claim=1. The row does not attribute this to a
new lowering change. LOCAL-KNOWN-RECEIVER-DIRECT-CALL-CLOSEOUT-001 closed the
lane with lane_closed=1, new_speedup_claim=0, and selected the next row as
fresh owner selection after closeout.
MIMALLOC-BODY-TIMING-NEXT-OWNER-SELECTION-AFTER-LOCAL-KNOWN-RECEIVER-CLOSEOUT-001
selected no implementation owner for the current front:
hako_slower_current_front=0, selected_next_owner=none_current_front_not_hako_slower,
and next_task=MIMALLOC-CURRENT-FRONT-OPTIMIZATION-PAUSE-CHECKPOINT-001.
MIMALLOC-CURRENT-FRONT-OPTIMIZATION-PAUSE-CHECKPOINT-001 paused this front:
current_front_paused=1, implementation_owner_selected=0, and
selected_next=MIMALLOC-FRESH-FRONT-SELECTION-001. The active goal now moves to
compiler object-shape cleanup before selfhost MIRBuilder growth:
COMPILER-OBJECT-FINAL-SHAPE-001 fixed MIRBuilder=meaning,
SemanticRefresh=facts, RoutePlan=execution, ObjectPlan=representation/publication,
Backend=plan consumer, Runtime=generic fallback.
MIRBUILDER-OBJECT-BOUNDARY-GUARD-001 fixed a lightweight guard over
src/mir/builder so ObjectPlan/ObjectStoragePlan/publication/HostHandle bypass/
Arc retirement truth cannot enter MIRBuilder silently.
SELFHOST-MIR-OBJECT-METADATA-001 fixed the matching .hako MIRBuilder metadata
contract: source_span, receiver_origin, known_type_hint, field_key,
call_site_id, and newbox_origin are allowed; representation/publication/backend
route truth stays out.
OBJECTPLAN-PASSIVE-UNIFY-001 made `ObjectPlan` the canonical passive
representation + publication-site vocabulary while preserving
`LocalFirstObjectPlan` as a compatibility alias.
ROUTEPLAN-OBJECTPLAN-HANDOFF-001 fixed the backend handoff contract:
RoutePlan proves direct call/new/drop execution, ObjectPlan proves
representation/publication, and C-like exact-AOT lowering needs both when it
combines direct call with representation bypass.
PUBLICATION-SITE-INVENTORY-GENERIC-001 fixed the generic
ObjectPublicationReason vocabulary from code: eight publication reasons are
present, Unknown remains conservative, and backend ObjectPlan consumption stays
disabled.
BACKEND-PLAN-CONSUMER-GUARD-001 fixed the backend consumer guard: existing
flattened-nested ObjectPlan consumer remains allowed, no new lowering is added,
and future direct/bypass lowering must consume RoutePlan/ObjectPlan rather than
helper, method, or variable-name inference.
COMPILER-OBJECT-SHAPE-CLOSEOUT-001 closed the compiler object-shape cleanup:
MIRBuilder/selfhost MIRBuilder boundaries, ObjectPlan canonical vocabulary,
RoutePlan/ObjectPlan handoff, generic publication-site vocabulary, and backend
consumer guard are fixed with product default unchanged.
COMPILER-OBJECT-SHAPE-CLOSEOUT-FOLLOWUP-001 aligned the ObjectStoragePlan SSOT
enum with code, tightened the closeout guard to execute source sub-guards, and
parked backend method-name proof drift / legacy array receiver proof-chain
retirement as investigation tasks without risky code changes.
BACKEND-METHOD-NAME-PROOF-AUDIT-001 classified the existing flattened-nested
method-name tables as guarded semantic maps inside an ObjectPlan consumer, not
generic backend route inference; the next residue is legacy array receiver
proof-chain retire inventory.
ARRAY-RECEIVER-RESIDENCE-PROOF-CHAIN-RETIRE-INVENTORY-001 classified the legacy
array receiver proof-chain module and references, kept live
src/mir/array_receiver_proof.rs untouched, and parked deletion behind a retire
gate project.
MIMALLOC-FRESH-FRONT-SELECTION-001 measured resident kernel lanes and selected
kilo_leaf_map_get_missing as the next Hako-slower leaf front, with hot symbols
MapBox::get_opt_key_str and i64::spec_to_string; implementation remains closed
until owner inventory.
MIMALLOC-MAP-MISSING-KEY-OWNER-INVENTORY-001 classified the selected front as a
Hakorune runtime-boundary diagnostic: MapBox uses String keys through
Arc<RwLock<HashMap<...>>>, the Hako path converts i64 key zero through
to_string_box, the C pair is not a cost-equivalent HashMap/RwLock/string
implementation, and the existing MapLookupFusionRoute scope is same-key
MapGet/MapHas rather than get-only missing-key. The active row probes whether a
missing-empty-map route can be proven before any MapBox or backend patch.
MIMALLOC-MAP-MISSING-EMPTY-ROUTE-TRIGGER-PROBE-001 found generic_method_routes=1
for MapGet, map_lookup_fusion_routes=0, route_decisions=0, and
map_repr_plans=1 generic_hash_runtime. The selected front has no MapHas pair and
no backend-active missing route, so the next row designs the proof fields for a
get-only missing-empty-map route.
MIMALLOC-MAP-MISSING-EMPTY-ROUTE-DESIGN-001 selected a proof-bearing
MapMissingEmptyRoute design: only a new local MapBox with no publication,
escape, set, delete, clear, or unknown receiver mutation before get may fold to
the runtime-data zero/null missing result; backend consumption must come from a
selected RouteDecision/LoweringPlan, not helper or literal-key inference.
MIMALLOC-MAP-MISSING-EMPTY-ROUTE-GUARD-SURFACE-001 fixed the post target for a
narrow implementation row: one MapMissingEmptyRoute for MapGet bb19.i3, all
birth/publication/escape/mutation proof fields present, backend consumption via
RouteDecision, and no MapBox storage/semantics/product-default changes.
MIMALLOC-MAP-MISSING-EMPTY-ROUTE-IMPLEMENTATION-001 added the narrow
MapMissingEmptyRoute -> RouteDecision -> backend selected-route consumer:
selected_route=map_get_missing_empty_const_zero, source_plan_kind=MapMissingEmptyRoute,
selected_i64_const=0, and target MIR JSON now reports one route at b19.i3
without MapBox storage/semantics/product-default changes. The active row
measured the route in the product AOT path. MIMALLOC-MAP-MISSING-EMPTY-ROUTE-MEASUREMENT-001
reduced ny_aot_instr from 896474449 to 472935, removed the target
nyash.runtime_data.get_hh call from ny_main, and produced route_winner_claim=1
with product_default_changed=0. MIMALLOC-MAP-MISSING-EMPTY-ROUTE-CLOSEOUT-001
closed this front with front_closed=1 and selected fresh-front selection before
any new implementation row. MIMALLOC-FRESH-FRONT-SELECTION-AFTER-MAP-MISSING-EMPTY-CLOSEOUT-001
selected kilo_leaf_array_string_len as the next Hako-slower leaf front:
ratio_instr=0.16, ratio_cycles=0.10, and ratio_ms=0.40. The follow-up
MIMALLOC-LEAF-ARRAY-STRING-LEN-OWNER-INVENTORY-001 found only a low-confidence
runtime TLS attribution so far: asm_top_symbol_0=std::thread::local::LocalKey<T>::with,
asm_top_symbol_0_percent=97.52, asm_sample_count=25, and implementation_allowed=0.
MIMALLOC-LEAF-ARRAY-STRING-LEN-ATTRIBUTION-REPEAT-001 repeated attribution and
selected array_text_slot_len_handle_cache_tls_boundary as a medium-confidence
owner, allowing one narrow array_string_len readonly ready-path attempt.
MIMALLOC-LEAF-ARRAY-STRING-LEN-READY-PATH-IMPLEMENTATION-001 rejected and
reverted that attempt because cycles and wall time regressed:
ny_aot_cycles 32183346 -> 54242794 and ny_aot_ms 10 -> 13. The active row
selected the next owner before any further implementation.
MIMALLOC-LEAF-ARRAY-STRING-LEN-NEXT-OWNER-SELECTION-001 selected
array_text_slot_len_loop_local_session_boundary with implementation_allowed=0;
MIMALLOC-LEAF-ARRAY-STRING-LEN-ROUTE-SYMBOL-ATTRIBUTION-PROBE-001 fixed
`nyash.array.string_len_hi` and `hako.array_text.slot_len` as same-body aliases
at 0x414930, so alias spelling is not owner evidence. The active row designs
the loop-local session boundary before backend/code changes.
MIMALLOC-LEAF-ARRAY-STRING-LEN-LOOP-SESSION-DESIGN-001 rejected raw
ArrayTextSession/ArrayBox pointer FFI and selected ArrayTextLoopSessionPlan as
the required proof surface before any backend loop-session lowering.
MIMALLOC-ARRAY-TEXT-LOOP-SESSION-PLAN-SURFACE-001 added the passive
src/mir/array_text_loop_session_plan.rs vocabulary and kept refresh, MIR JSON
export, backend consumption, and loop-session lowering disabled. A benchmark
correction then retracted the old `kilo_leaf_map_get_missing` Hako-vs-C winner
claim because the C pair is a volatile compare rather than a map lookup. The
semantic `MapMissingEmptyRoute` remains intact, but the next real map work is
MapGetI64 scalar route inventory. That inventory found MapHasI64 already has
`MapContainsI64 -> nyash.map.probe_hi`, while scalar-proof-positive MapGet still
uses `RuntimeDataLoadAny -> nyash.runtime_data.get_hh`; `nyash.map.slot_load_hi`
exists as substrate but still stringifies i64 keys. The design row selected a
separate helper-backed `MapLoadScalarI64 -> nyash.map.scalar_load_hi` contract
for proof-positive RuntimeDataBox.get only, while preserving mixed get, direct
handle get, String-key storage, and stored-value constant emission as separate
rows. The guard surface fixed post targets for MapLoadScalarI64 and preserves
mixed get, direct handle get, slot_load_hi, benchmark source, typed storage, and
constant emission as non-goals. The implementation row added the proof-backed
Rust route, runtime scalar helper, and LLVM C shim consumer while keeping mixed
get and direct handle get unchanged. The validation row confirmed the rebuilt
C shim emits `nyash.map.scalar_load_hi` for the scalar lower-plan fixture while
mixed get still imports `nyash.runtime_data.get_hh` and direct MapBox.get still
imports `nyash.map.slot_load_hh`. The direct MapBox scalar-demand row then
applied the same scalar-store proof to direct `MapBox.get`, so the
`kilo_leaf_map_getset_has` source front now emits `map_load_scalar_i64` for both
the loop get and final get while preserving unproven handle fallback. The next
row validated source-to-AOT object output: `kilo_leaf_map_getset_has` source MIR
and exact-AOT object now require `nyash.map.scalar_load_hi` and no longer import
`nyash.map.slot_load_hh` or `nyash.runtime_data.get_hh` for the target front.
The measurement-attribution row classified this as route-reaches-AOT only:
`kilo_leaf_map_getset_has` folds to a single store + scalar load shape, so it is
not a repeated body keeper front. The front-selection row selected
`kilo_leaf_map_get_dynamic_covered_i64` as a Hako-only repeated map-get owner:
current AOT still imports `nyash.runtime_data.get_hh` in the loop and no scalar
route is present yet. The proof-design row selects
generic_method_route_plan_map_key_coverage_proof as route-proof owner for the
narrow covered dynamic i64 key shape; MapBox storage, MIRBuilder object
management, LLVM helper-name inference, and winner claims remain closed. The
guard-surface row fixes the post target before code: the loop MapGet must route
to `map_load_scalar_i64`, runtime_data.get_hh must leave the loop, and final
const-key fallback may remain separate. The implementation row adds the narrow
covered dynamic i64 key proof, updates the ny-llvmc metadata need/emit
allowlists for the new proof token, and verifies the selected front now imports
`nyash.map.scalar_load_hi` without importing `nyash.runtime_data.get_hh`.
Winner claims and C-pair measurement remain closed. The measurement row confirms
the route reaches AOT but the hot owner moved into scalar helper internals:
i64 key String conversion, hashing, MapBox::get_opt_key_str, and share/drop
costs dominate. The owner-selection row closes i64 sidecar storage for now
because MapBox public semantics use a stringified-key namespace, and selects
`map_scalar_no_publication_borrowed_lookup` as the next owner. The design row
selects only scalar_load_hi-internal borrowed scalar read plus i64 key text
allocation removal. MapBox storage, public get/set semantics, mixed
RuntimeDataBox.get routing, slot_load_hh/slot_load_hi materialization, and
winner claims remain closed. The implementation row switches only
`nyash.map.scalar_load_hi` to stack i64 key text and `MapBox` borrowed scalar
read, keeping visible MapBox materialization and storage unchanged. Validation
is green, and measurement confirms the old String allocation / visible share
costs left the top symbols. The remaining hot owner is now
HashMap<String>-key hashing / key-domain representation. The owner-selection
row closes direct i64 sidecar implementation for now. The key-domain design row
selects a normalized `MapKeyDomain` vocabulary with `CanonicalI64(i64)` and
`Text(String)`, preserving the public alias between i64 `1` and text `"1"` while
keeping non-canonical numeric-looking text as text. The vocabulary row adds the
module and tests only. The storage design row recognizes that MapBox storage
truth lives in `nyash-rust::boxes`, so the kernel vocabulary is prototype-only.
The core-promotion row adds `src/boxes/map_key_domain.rs` and exports it from
`src/boxes/mod.rs`, while keeping MapBox storage, scalar helper consumption,
sidecars, and winner claims closed. The storage guard-surface row fixes the
next post target as `HashMap<MapKeyDomain, Box<dyn NyashBox>>` plus public
alias/key-output tests. The storage implementation row changes MapBox storage
to `HashMap<MapKeyDomain, Box<dyn NyashBox>>`, preserves public key text output
for keys / values / JSON, and keeps scalar helper consumer rows closed. The
validation row passes the implementation guard, release cargo check, pointer
guard, diff check, and targeted MapKeyDomain / MapBox semantic tests. The
measurement row shows the storage change is reached but `nyash.map.scalar_load_hi`
still stringifies i64 keys: top symbols are BuildHasher::hash_one=39.73%,
scalar_load_hi=31.14%, and SpecToString=23.81%. The scalar-helper consumer
design row selects only `scalar_load_hi -> map_scalar_load_i64 ->
MapBox::get_scalar_i64_key_i64`, while keeping slot_load routes, public MapBox
semantics, sidecars, hashers, MIRBuilder, and route proof unchanged. The
implementation row adds that raw i64-domain helper seam, removes the obsolete
borrowed decimal text helper, and keeps slot_load/public semantics unchanged.
The measurement row confirms a keeper: cycles drop from ~1.18B to ~0.48B,
SpecToString disappears from top symbols, scalar_load_hi drops from 31.14% to
3.30%, and the remaining owner moves to MapBox::get_scalar_i64_key_domain /
BuildHasher::hash_one. The post-domain-consumer owner-selection row chooses
`map_key_domain_hash_lookup_policy` but closes implementation because the next
step requires a Map storage policy design decision: hasher specialization,
typed i64-keyed storage, closeout, or new front. The key-domain closeout row
closes this front as a keeper and opens Map storage policy as a separate design
lane. The Map storage policy SSOT keeps product MapBox as
`HashMap<MapKeyDomain, Box<dyn NyashBox>>` with default hasher, reserves
LocalI64KeyMap / LocalTextKeyMap / LocalScalarValueMap / PublishedMapBoxFallback
for exact-AOT/local-first planning, and keeps product hasher swaps, sidecars,
i64-only product storage, and MIRBuilder storage ownership disabled. The hash
owner inventory confirms the hot loop is canonical-i64 lookup only, keeps
product hasher swap disabled, and hands off to LocalI64KeyMap front selection.
The LocalI64Map front selection chooses
`kilo_leaf_map_get_dynamic_covered_i64` as the first LocalI64KeyMap shadow
candidate: one local MapBox birth, three i64-key writes, one hot-loop i64 get,
one post-loop i64 get, and no visible keys/values/toJSON/plugin/extern/return
publication boundary. The storage shadow row added passive
`local_i64_key_map_shadow` metadata in `src/mir/map_repr_plan.rs` from existing
generic_method_routes. Backend lowering, product MapBox i64-only storage,
hasher swaps, sidecars, and MIRBuilder storage ownership remain closed. The
pilot guard surface permits only a call-site metadata consumer for
`map_repr.local_i64_key_map_shadow` + `map_load_scalar_i64` and keeps
benchmark/helper-name inference closed. The LocalI64Map get pilot wires that
consumer in the Python LLVM backend and emits `nyash.map.local_i64_get_hi`;
the helper delegates to existing `map_scalar_load_i64`, so this row proves the
metadata consumer seam without changing product MapBox storage or claiming a
winner. The validation row passes the Python metadata consumer test,
MapReprPlan unit tests, release cargo check, current-state pointer guard, and
diff check. The measurement row confirms the active C ABI `ny-llvmc` route
reaches entry-table materialization for the target front: the `ny_main` hot loop
has no map helper calls, the post-loop public fallback still calls
`nyash.map.slot_load_hh`, and the direct executable returns 4000001 with
~6.46M cycles in the direct micro-ASM runner. The owner-refresh closeout row
therefore retires the stale `MapBox::get_scalar_i64_key_domain` /
`BuildHasher::hash_one` owner for this front: the old owner is no longer in the
hot loop, the remaining post-loop `slot_load_hh` fallback is not a current hot
owner, and the next step is fresh front selection. This is target-front
reachability and a local-entry-table win, not a Hako-vs-C winner claim.
FRESH-COMPILER-OWNER-SELECTION-004 found no fresh Hako-slower performance owner
and selected the blocked `kilo_leaf_array_rmw_add1` exact seed consumer as a
coverage-contract repair. ARRAY-RMW-ADD1-EXACT-SEED-METADATA-CONTRACT-001
removed the stale raw instruction-index pin from the C ABI exact seed consumer:
source-direct MIR now reports get/set positions 7/12, the route is consumed,
the perf lane returns `aot_status=ok`, and kernel ratio is 1.00. The next step
is fresh compiler owner selection. FRESH-COMPILER-OWNER-SELECTION-005 found no
fresh Hako-slower exact-AOT compiler owner after the array RMW repair. It also
added a real C i64-hashmap pair for `kilo_leaf_map_get_dynamic_covered_i64`,
replacing the Hako-only measurement hole; that front is now comparison-ready
and remains Hako-smaller on kernel instr/cycles. FASTPATH-OPTIMIZATION-CHECKPOINT-001
closes the current fastpath burst with no fresh compiler implementation owner
selected. New backend fastpaths remain closed until a fresh Hako-slower front
is selected by evidence. FASTPATH-ELIGIBILITY-RESOLVER-SSOT-001 accepts the
demand-driven recursive resolver design for future fastpaths: plan freeze first,
read-only Allow(LocalFastPathFact)/Deny(reason) resolution, backend fact-only
emission, and post-hoc reachability ledger. V0 is limited to
KnownReceiverDirectCall. FASTPATH-DECISION-VOCAB-000 adds passive
FastPathDecision / PlanEpoch vocabulary, requires LocalFastPathFact.plan_epoch,
and exports plan_epoch in MIR JSON metadata without enabling resolver execution
or changing backend behavior. FASTPATH-DENY-OWNER-MAPPING-001 maps every
passive deny reason to a next owner lane so failed eligibility reports do not
turn into guesswork. FASTPATH-ALIAS-PUBLICATION-MVP-001 adds report-only linear
alias-chain inputs and a 5-hop fixture that feeds publication inventory without
backend consumption. FASTPATH-KNOWN-RECEIVER-DIRECT-CALL-SHADOW-001 makes the
existing KnownReceiver shadow row emit FastPathDecision while keeping backend
behavior and resolver execution unchanged. FASTPATH-REACHABILITY-LEDGER-POSTHOC-001
adds passive post-hoc reachability vocabulary so selected/preempted/unreachable
status stays separate from eligibility decisions. FASTPATH-KNOWN-RECEIVER-DIRECT-CALL-PILOT-001
closes the B-lite pilot by routing exported known-receiver facts through
FastPathDecision::Allow while keeping deny decisions report-only and backend
behavior unchanged. FASTPATH-VOCAB-SLIM-CLOSEOUT-001 closes the vocabulary
slimming row by adding a code-side object_storage_plan README with seven allowed
concept groups and report fields forbidding new near-synonym fastpath types
without a design row. FASTPATH-GAP-INVENTORY-001 adds a read-only MIR JSON
inventory for known receiver direct user-box method routes that have not become
positive LocalFastPathFact entries. The current mimalloc object-lifecycle MIR
shows 19 gaps in HakoAllocObjectLifecycleFacade.objectLifecycleSmallAlloc/1 and
4 gaps in Main.runOne/2, with backend lowering and winner claim still closed.
CALLSITE-CANONICALIZE-ENTRY-INVENTORY-001 makes the residue explicit:
production_entry_count=4, known_entry_count=4, unknown_entry_count=0, and
single_transform_owner=1 while centralized_schedule_owner remains 0. No
canonicalize entry is removed or reordered; the next step is a separate owner
design row if this cleanup is reopened.
OBJECT-STORAGE-PASSIVE-VOCAB-USAGE-INVENTORY-001 extends the vocabulary audit
for the remaining passive residue: exact_stack_object_external_producer_count=0,
fastpath_decision_non_test_consumer_count>=1, and
fastpath_reachability_non_test_consumer_count=0. It keeps resolver/backend
execution disabled and requires a separate retire/design row before deletion or
merge. RESIDUE-RETIRE-DESIGN-QUEUE-001 fixes the stop-line and queue:
CALLSITE-CANONICALIZE-ENTRY-OWNER-DESIGN-001 first, then
EXACT-STACK-OBJECT-RETIRE-DESIGN-001 or
FASTPATH-REACHABILITY-RETIRE-OR-ENABLE-DESIGN-001. No deletion, schedule
change, backend behavior change, or implementation is allowed by the queue row.
CALLSITE-CANONICALIZE-ENTRY-OWNER-DESIGN-001 selects option B: keep the four
production entries but route them through a thin schedule facade under the
existing callsite_canonicalize transform owner. Entry removal, schedule reorder,
semantic refresh timing changes, and Program(JSON v0) / MIR JSON policy merging
remain disabled. CALLSITE-CANONICALIZE-SCHEDULE-FACADE-001 implements that
narrow facade: production_entry_count=4, centralized_schedule_owner=1,
entry_removal_enabled=0, schedule_reorder_enabled=0, and
semantic_refresh_timing_changed=0. CALLSITE-CANONICALIZE-SCHEDULE-FACADE-CLOSEOUT-001
closes the callsite canonicalize entry residue with
callsite_canonicalize_entry_residue_closed=1; remaining cleanup decisions are
ExactStackObject retire design or FastPathReachability retire/enable design.
EXACT-STACK-OBJECT-RETIRE-DESIGN-001 selects retiring ExactStackObject from
active vocabulary because exact_stack_object_external_producer_count=0 and no
backend consumer exists. Active exact storage forms remain ExactNativeStruct,
Scalarized, and FlattenedNestedFields; implementation is split to a separate
row. EXACT-STACK-OBJECT-RETIRE-IMPLEMENTATION-001 retires the inactive variant
from source vocabulary and guards the active exact forms as ExactNativeStruct,
Scalarized, and FlattenedNestedFields with stack_allocation_support_claimed=0.
FASTPATH-REACHABILITY-RETIRE-OR-ENABLE-DESIGN-001 selects retiring the Rust-side
FastPathReachability passive vocabulary while keeping the Python hako_check
reachability ledger as the selected/preempted/unreachable report owner.
FASTPATH-REACHABILITY-RUST-VOCAB-RETIRE-001 removes the Rust-side
FastPathReachability type and keeps reachability as hako_check-owned post-hoc
tooling, with preemption still outside Deny reasons."""
active_phase = "docs/development/current/main/phases/phase-296x/README.md"
phase_status = "docs/development/current/main/phases/phase-296x/296x-90-mimalloc-benchmark-taskboard.md"
method_anchor = "docs/development/current/main/design/perf-owner-first-optimization-ssot.md"
taskboard = "docs/development/current/main/phases/phase-296x/296x-90-mimalloc-benchmark-taskboard.md"
latest_workstream_card = "docs/development/current/main/phases/phase-296x/296x-1052-FASTPATH-REACHABILITY-RUST-VOCAB-RETIRE-001.md"

latest_card = "FASTPATH-REACHABILITY-RUST-VOCAB-RETIRE-001"
latest_card_path = "docs/development/current/main/phases/phase-296x/296x-1052-FASTPATH-REACHABILITY-RUST-VOCAB-RETIRE-001.md"
latest_card_summary = """FastPathReachability Rust vocabulary retirement is
landed. The Rust-side passive type is removed from object_storage_plan, while
hako_check remains the owner for post-hoc selected/preempted/unreachable
reachability reports."""
current_blocker_token = "FRESH-COMPILER-OWNER-SELECTION-006"
current_update_policy = "docs/development/current/main/design/current-docs-update-policy-ssot.md"
current_update_rule = "Per-card: update CURRENT_STATE plus the active card only unless lane/blocker/restart-order/phase-status/policy changes."

pre_perf_gate = "phase-137x stays observe-only unless app work reopens a real blocker"
pre_perf_gate_status = "active guardrail"
optimization_return_lane = "phase-137x observe-only guardrail"

landed_tail = [
  "2026-06-09: 296x-649 selects the userbox/counter-heavy exact-front optimization lane with kilo_micro_userbox_counter_step_chain as the primary front.",
  "2026-06-09: 296x-648 selects the direct-exact optimization sweep owner from the Hako vs C mimalloc comparison evidence.",
  "2026-06-08: 296x-639..647 close the selfhost surface preflight, production allocator API selections, and current docs pointer cleanup before returning to exact-front optimization.",
  "2026-06-06: MIM-PORT-FMEM-005 and MIM-PORT-FMEM-006 are historical Done rows: owner_worker_id scalar read and free_head read-only pointer observation.",
  "2026-06-12: MIM-PORT-FMEM-004 PageMeta MIR-to-LLVM producer evidence is verified through LayoutRef Copy propagation, layout-table producer profile evidence, fastmem source syntax, inventory/check/parity smokes, pytest, cargo check, and current-state guard.",
  "2026-06-11: PERF-USERBOX-056..064 tighten the libc_process ladder from variability to env ladder and callgraph attribution.",
]

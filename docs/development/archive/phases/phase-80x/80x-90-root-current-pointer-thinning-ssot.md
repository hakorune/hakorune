# Phase 80x SSOT

## Intent

`80x` is a small maintenance lane to thin root/current pointer docs after `79x` landed green.

## Fixed Facts

- `phase-79x` is landed.
- `launcher.hako emit_mir_mainline` is green.
- `CURRENT_TASK.md` and current mirrors have grown redundant active/front wording and landed carry-over.
- This lane changes pointer docs only; it does not reopen source/blocker work.

## Acceptance

1. `CURRENT_TASK.md` keeps only the current lane/front plus successor pointer.
2. `05-Restart-Quick-Resume.md`, `10-Now.md`, and `15-Workstream-Map.md` reflect `80x` as current.
3. No source/runtime behavior changes.

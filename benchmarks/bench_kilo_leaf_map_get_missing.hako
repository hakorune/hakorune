// kilo/map leaf-proof microbench:
// stable missing-key map get on a single integer key.
// Zero keeps the path on the immediate-i64 route and avoids handle lookup noise.
static box Main {
  main() {
    local ops = 2000000
    local map = new MapBox()
    local i = 0
    local sum = 0

    loop (i < ops) {
      local v = map.get(0)
      if (v == null) {
        sum = sum + 1
      }
      i = i + 1
    }

    return sum
  }
}

# hako-alloc allocator comparison benchmark execution preflight inventory proof

Row: MIMAP-436A

This proof app inventories explicit preflight conditions for a future allocator
comparison benchmark execution row. It does not run benchmarks or replace the
process allocator.

Run:

```bash
bash tools/checks/run_proof_app.sh --only MIMAP-436A --level L2
```

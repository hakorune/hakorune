# hako-alloc-provider-call-modeled-open-pilot-proof

Row: MIMAP-386A

This proof app exercises the provider-call modeled-open pilot after the
provider-call dry-run unsupported behavior. It proves that the model can record
a provider-call-open state while actual provider API execution remains closed.

Run:

```bash
bash tools/checks/k2_wide_hako_alloc_provider_call_modeled_open_pilot_guard.sh --level L2
```

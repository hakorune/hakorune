// Phase 29y lane-B parity debt probe fixture.
// Nested ternary keeps parser/lowering boundary visible for Rust vs .hako routes.
static box Main {
  main(args) {
    local a = 3
    local b = 5
    local v = (a < b) ? ((b < 0) ? 40 : 50) : 60
    return v
  }
}

// Phase 29cc WSM-P10-min8 error native promotion fixture
// Contract: default hako-lane promotes error+fixed4 shape to native emit.

static box Main {
  main() {
    local i = 0
    loop(i < 4) {
      i = i + 1
    }
    console.error(i)
    return i
  }
}

// generic_loop: allow Print stmt in body (planner-required)
// Expected: stdout=P, RC=0
static box Main {
  main() {
    local i = 0
    local n = 0
    loop(i <= n) {
      print("P")
      i = i + 1
    }
    return 0
  }
}


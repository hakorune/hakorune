// Semantic fixture alias for the minimal loop_simple_while explicit-step route.
// Expected output: 6

static box Main {
  main() {
    local i = 0
    local acc = 0
    loop(i < 4) {
      acc = acc + i
      i = i + 1
    }
    print(acc)
    return 0
  }
}

// RVP-C19 blocked fixture
// Contract:
// - vm-hako route still returns stale scalar zero for MapBox.get("a").

static box Main {
  main() {
    local m = new MapBox()
    m.set("a", 42)
    print(m.get("a"))
    return 0
  }
}

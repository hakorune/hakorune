// RVP-C28 blocked fixture
// Contract:
// - vm-hako route still reports the stale unimplemented tag for `MapBox.setField(non-string key, value)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.setField(456, 1))
    return 0
  }
}

// RVP-C28 ported fixture
// Contract:
// - vm-hako route returns stable `[map/bad-key] field name must be string` text for `MapBox.setField(non-string key, value)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.setField(456, 1))
    return 0
  }
}

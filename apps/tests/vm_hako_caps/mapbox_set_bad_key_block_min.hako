// RVP-C26 blocked fixture
// Contract:
// - vm-hako route still reports the stale unimplemented tag for `MapBox.set(non-string key, value)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.set(123, 1))
    return 0
  }
}

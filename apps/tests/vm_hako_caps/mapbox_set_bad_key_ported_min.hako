// RVP-C26 ported fixture
// Contract:
// - vm-hako route returns stable `[map/bad-key] key must be string` text for `MapBox.set(non-string key, value)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.set(123, 1))
    return 0
  }
}

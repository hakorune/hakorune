// RVP-C27 blocked fixture
// Contract:
// - vm-hako route still reports the stale unimplemented tag for `MapBox.getField(non-string key)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.getField(123))
    return 0
  }
}

// RVP-C19 ported fixture
// Contract:
// - vm-hako route returns the stored scalar value for MapBox.get("a").

static box Main {
  main() {
    local m = new MapBox()
    m.set("a", 42)
    print(m.get("a"))
    return 0
  }
}

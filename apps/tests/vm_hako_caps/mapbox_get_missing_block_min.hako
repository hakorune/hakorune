// RVP-C24 blocked fixture
// Contract:
// - vm-hako route still returns stale scalar `0` for `MapBox.get(missing-key)`.

static box Main {
  main() {
    local m = new MapBox()
    print(m.get("nope"))
    return 0
  }
}

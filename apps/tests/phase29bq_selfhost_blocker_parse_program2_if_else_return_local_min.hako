// parse_program2 if-else-return-local shape (no loops)
// Expected: stdout="", RC=0
static box Main {
  main() {
    local v = 0
    if v == 0 {
      return 0
    } else {
      local t = v
      return t
    }
  }
}

# Root Fixtures

Legacy/adhoc `.hako` fixtures moved from repository root to keep root entrypoints clean.

Notes:
- `basic_test.hako` and `test.hako` stay at repository root because existing smokes/docs reference those paths.
- Other debug/json/string/stageb temporary fixtures are archived here.

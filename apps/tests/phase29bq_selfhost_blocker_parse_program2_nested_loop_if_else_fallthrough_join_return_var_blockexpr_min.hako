// parse_program2 nested loop + inner if-else fallthrough join return-var (var from BlockExpr) shape (return exits func)
// Expected: stdout="", RC=0
static box Main {
  main() {
    // v is derived from a BlockExpr, but the return expression is just `v`.
    local v = {
      local t = 0
      t
    }

    local i = 0
    loop(i < 1) {
      local j = 0
      loop(j < 2) {
        if j == 0 {
          return v
        } else {
          // fallthrough
        }
        j = j + 1
      }
      i = i + 1
    }
    return 0
  }
}

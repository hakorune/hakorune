using "apps/lib/json_native/utils/string.hako" as StringUtils

static box Main {
  main(args) {
    local ok = 0
    if StringUtils.contains("hello", "ll") { ok = 1 }
    print(ok)
    return 0
  }
}

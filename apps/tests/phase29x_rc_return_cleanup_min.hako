// Phase 29x X8: return cleanup minimal fixture
// Contract:
// - Includes an early-return branch and a fallthrough return.
// - Runtime behavior must stay stable (exit 0).
// - Return cleanup correctness is pinned by rc_insertion_selfcheck in smoke.

static box Main {
    main() {
        local x = null
        x = 1
        if x == 1 {
            return 0
        }
        return 0
    }
}

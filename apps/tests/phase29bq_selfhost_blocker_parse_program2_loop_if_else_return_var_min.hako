// parse_program2 loop if-else-return-var shape (return exits loop+func)
// Expected: stdout="", RC=0
static box Main {
  main() {
    local v = 0
    loop(v < 1) {
      if v == 1 {
        return 0
      } else {
        return v
      }
    }
    return 2
  }
}

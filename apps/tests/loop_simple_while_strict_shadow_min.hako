// Semantic alias for historical fixture token `phase286_pattern1_frag_poc.hako`.
// Expected: return 3 (loop 3 times: i=0,1,2 -> i=3 で終了)

static box Main {
    main() {
        local i
        i = 0
        loop(i < 3) {
            i = i + 1
        }
        return i
    }
}

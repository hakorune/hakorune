// Semantic alias for the historical simple-while strict-shadow fixture.
// Expected: return 3 (loop 3 times: i=0,1,2 -> i=3 で終了)

static box Main {
    main() {
        local i
        i = 0
        loop(i < 3) {
            i = i + 1
        }
        return i
    }
}

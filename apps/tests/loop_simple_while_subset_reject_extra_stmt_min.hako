// Semantic alias for the historical simple-while subset reject fixture.
// Expected: return 3 (sum increments each iteration)

static box Main {
    main() {
        local i
        local sum
        i = 0
        sum = 0
        loop(i < 3) {
            sum = sum + 1
            i = i + 1
        }
        return sum
    }
}

# hako-alloc OSVM page-source pilot proof

Row: MIMAP-349A

This proof records a bounded OSVM/page-source fact from an accepted atomic
bitmap report. It keeps provider activation, host allocator replacement, hooks,
worker/TLS, and backend matchers closed.

Run:

```bash
bash tools/checks/run_proof_app.sh --only MIMAP-349A
```

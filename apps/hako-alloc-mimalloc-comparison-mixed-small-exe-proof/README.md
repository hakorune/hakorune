# Hako Alloc Mimalloc Comparison Mixed-Small EXE Proof

Row: `295x-21`

This app mirrors the selected `representative-mixed-small-v0` request sequence
in `.hako` model space and keeps the shape exact-EXE friendly.

Run:

```bash
bash tools/checks/k2_wide_phase295x_mixed_size_contract_refresh_guard.sh
```

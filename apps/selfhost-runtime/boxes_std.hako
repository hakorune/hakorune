// Ny Executor Stage 0 scaffold — std boxes placeholder

static box BoxesStd {
  main(args) { return 0 }
}

static box Main {
  // Entrypoint stub keeps BoxesStd reachable for analyzer rules.
  main(args) { return BoxesStd.main(args) }
}

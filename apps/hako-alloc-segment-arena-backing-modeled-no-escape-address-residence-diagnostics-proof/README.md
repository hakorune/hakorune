# hako-alloc-segment-arena-backing-modeled-no-escape-address-residence-diagnostics-proof

Row: MIMAP-249A

This proof observes the MIMAP-248A modeled no-escape address residence
inventory and publishes scalar diagnostic summary facts without recording new
residence rows.

Run:

```bash
bash tools/checks/run_proof_app.sh --only MIMAP-249A
```

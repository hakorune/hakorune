# hako-alloc-provider-boundary-diagnostic-vocabulary-proof

Row: MIMAP-360A
Owner: `HakoAllocProviderBoundaryDiagnosticVocabulary`
Profile: `scalar-mir`

This proof inventories provider boundary diagnostic reason codes while keeping
provider activation, host allocator replacement, hooks, `#[global_allocator]`,
and backend owner-name matchers inactive.

Run:

```bash
bash tools/checks/run_proof_app.sh --only MIMAP-360A --level L2
```

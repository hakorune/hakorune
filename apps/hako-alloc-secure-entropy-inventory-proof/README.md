# hako-alloc-secure-entropy-inventory-proof

Purpose: MIMAP-049A proof for the read-only secure entropy inventory surface.

This app verifies that deterministic proof-key facts can be classified while
runtime entropy, random externs, secure-list behavior changes, and
cryptographic hardening claims remain inactive.

Run:

```bash
bash tools/checks/k2_wide_hako_alloc_secure_entropy_inventory_guard.sh
```

box InstanceProvider {
    m_seed(x) {
        return x + 100
    }
}

static box StaticProvider {
    m_seed(x) {
        return x + 1
    }
}

static box Consumer {
    use(x) {
        return m_seed(x)
    }
}

print(Consumer.use(1))

static box Alpha {
    m_seed(x) {
        return x + 1
    }
}

static box Beta {
    m_seed(x) {
        return x + 2
    }
}

static box Consumer {
    use(x) {
        return m_seed(x)
    }
}

print(Consumer.use(1))

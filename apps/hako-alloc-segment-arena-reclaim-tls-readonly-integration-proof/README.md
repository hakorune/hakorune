# hako-alloc-segment-arena-reclaim-tls-readonly-integration-proof

Purpose: prove the segment-arena reclaim/TLS read-only integration owner can
compose the readiness matrix, support gate, pointer-derived lookup prerequisite,
and worker/TLS evidence without opening provider activation, replacement, hook,
global allocator, or winner claim behavior.

Run:

```bash
bash apps/hako-alloc-segment-arena-reclaim-tls-readonly-integration-proof/test.sh
```

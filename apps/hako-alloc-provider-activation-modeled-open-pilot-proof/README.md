# hako-alloc-provider-activation-modeled-open-pilot-proof

Row: MIMAP-380A

This proof app exercises the first modeled provider activation-open pilot. It
consumes an accepted dry-run unsupported outcome, records modeled activation as
open, and keeps provider calls, host allocator replacement, hooks, backend
matchers, and thread execution closed.

Run:

```bash
bash tools/checks/k2_wide_hako_alloc_provider_activation_modeled_open_pilot_guard.sh --level L2
```

# hako-alloc-provider-call-real-api-stub-execution-pilot-proof

Row: MIMAP-396A

This proof app exercises the provider-call real API stub execution pilot. It
records model-space provider API call execution evidence while actual provider
API calls, host allocator replacement, hooks, backend matcher additions,
worker/thread execution, and global allocator install remain closed.

Run:

```text
bash tools/checks/run_proof_app.sh --only MIMAP-396A
```

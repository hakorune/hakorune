# hako-alloc-provider-call-execution-capability-preflight-proof

Row: MIMAP-388A

This proof app exercises the provider-call execution capability preflight after
the provider-call modeled-open pilot. It proves that execution readiness can be
recorded while actual provider API execution remains closed.

Run:

```bash
bash tools/checks/k2_wide_hako_alloc_provider_call_execution_capability_preflight_guard.sh --level L2
```

# Hako Alloc Mimalloc Comparison Empty EXE Proof

Row: `295x-34`

This app provides the `.hako` side of `representative-empty-v0`, an empty
baseline workload used to separate fixed process/runtime RSS from workload
incremental RSS.

Run:

```bash
bash tools/checks/k2_wide_phase295x_memory_gap_baseline_guard.sh
```

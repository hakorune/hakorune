// convert_generic_impl_target_fixture.hako — generic impl target fixture wrapper.
//
// This wrapper owns only the fixture file route. Conversion logic stays in
// converter_core.hako.

using apps.rust_subset_to_hako.converter_core as RustSubsetConverter

static box Main {
    main() {
        local file = new FileBox()
        local opened = file.open("apps/rust-subset-to-hako/examples/generic_impl_target_subset.json", "r")
        if opened == 0 {
            print("// error: generic-impl-target fixture open failed")
            return 1
        }

        local text = file.read()
        file.close()
        if text == null {
            print("// error: generic-impl-target fixture read failed")
            return 1
        }

        local converter = new RustSubsetConverter()
        local result = converter.convert(text)
        print(result)
        return 0
    }
}

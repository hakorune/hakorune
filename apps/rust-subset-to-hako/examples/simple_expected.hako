record Point {
    x: i64
    y: i64
}

function Point_len2(receiver: Point): i64 {
    return receiver.x * receiver.x + receiver.y * receiver.y
}

function add(a: i64, b: i64): i64 {
    return a + b
}

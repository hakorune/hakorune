// convert_associated_const_value_fixture.hako — associated const/value fixture wrapper.
//
// This wrapper owns only the fixture file route; converter_core.hako owns
// RustSubset JSON -> .hako skeleton conversion.

using apps.rust_subset_to_hako.converter_core as RustSubsetConverter

static box Main {
    main() {
        local file = new FileBox()
        local opened = file.open("apps/rust-subset-to-hako/examples/associated_const_value_subset.json", "r")
        if opened == 0 {
            print("// error: associated const/value fixture open failed")
            return 1
        }

        local text = file.read()
        file.close()
        if text == null {
            print("// error: associated const/value fixture read failed")
            return 1
        }

        local converter = new RustSubsetConverter()
        local result = converter.convert(text)
        print(result)
        return 0
    }
}

// convert_compound_assign_fixture.hako — compound assignment skeleton fixture wrapper.
//
// This wrapper owns only the fixture file route; converter_core.hako owns
// RustSubset JSON -> .hako skeleton conversion.

using apps.rust_subset_to_hako.converter_core as RustSubsetConverter

static box Main {
    main() {
        local file = new FileBox()
        local opened = file.open("apps/rust-subset-to-hako/examples/compound_assign_subset.json", "r")
        if opened == 0 {
            print("// error: compound assignment fixture open failed")
            return 1
        }

        local text = file.read()
        file.close()
        if text == null {
            print("// error: compound assignment fixture read failed")
            return 1
        }

        local converter = new RustSubsetConverter()
        local result = converter.convert(text)
        print(result)
        return 0
    }
}

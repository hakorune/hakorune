[package]
name = "nyash-mir-defs"
version = "0.1.0"
edition = "2021"

[lib]
name = "nyash_mir_defs"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
nyash-mir-core = { path = "../nyash_mir_core" }


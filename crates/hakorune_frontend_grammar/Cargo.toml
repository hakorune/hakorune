[package]
name = "hakorune-frontend-grammar"
version = "0.1.0"
edition = "2021"

[lib]
name = "hakorune_frontend_grammar"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
once_cell = "1.20"
toml = "0.8"

[dev-dependencies]
tempfile = "3.10"

// ng.hako — restricted-loop violations

static box Main {
  method main(args) {
    local i = 0
    loop (i < 3) {
      if i == 1 { continue }
      i = i + 1
      ConsoleBox.println("x")
      loop (i < 1) {
        i = i + 1
      }
    }
    return 0
  }
}

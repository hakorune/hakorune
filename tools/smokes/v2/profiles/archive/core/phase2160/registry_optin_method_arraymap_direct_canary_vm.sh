#!/usr/bin/env bash
# Archived tombstone probe.
# The direct lowerer owner was retired from the live tree in phase29bq,
# so this script remains only as historical evidence and intentionally
# does not execute any live contract.
set -euo pipefail

echo "[SKIP] archived tombstone: LowerReturnMethodArrayMapBox direct probe retired"
exit 0

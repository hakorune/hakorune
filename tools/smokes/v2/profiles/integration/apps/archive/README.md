# Archived Integration App Smokes

This directory keeps legacy/standalone app smokes that are currently not wired into active gate chains.

Rules:
- Archived scripts are excluded from daily smoke inventory by default.
- Scripts remain runnable manually for bisect/regression archaeology.
- Promote back to active `apps/` only with guard + gate wiring in same change.

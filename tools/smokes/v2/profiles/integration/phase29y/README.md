# phase29y

Live semantic families for phase29y integration smokes.

- `hako/`: selfhost-first `.hako` route families.
- `binary_only/`: next residual hako family after `emit_mir`.
- `using_resolver_parity/`, `lane/`, `rc/`, `core/`, `direct/`, `handle/`, `joinir/`, `loop/`, `mirbuilder/`, `no/`, `observability/`, `optional/`, `ring1/`: remaining phase29y residual families still parked under `integration/apps`.

// Phase 164: Test skip_whitespace from JsonParserBox (if_phi_join-style + break)
// Simulates: loop(p < len) { if is_whitespace(ch) { p++ } else { break } }

static box Main {
  main(args) {
    // Simulate skipping leading whitespace
    local s = "   \t\nhello world"
    local p = 0
    local len = s.length()

    // if_phi_join-style body update: p changes differently across branches
    loop(p < len) {
      local ch = s.substring(p, p + 1)
      // Check if whitespace
      local is_ws = 0
      if ch == " " {
        is_ws = 1
      } else if ch == "\t" {
        is_ws = 1
      } else if ch == "\n" {
        is_ws = 1
      } else if ch == "\r" {
        is_ws = 1
      }

      if is_ws == 1 {
        p = p + 1
      } else {
        break
      }
    }

    // Output result
    local remaining = s.substring(p, len)
    print("Skipped leading whitespace")
    print("Position: " + ("" + p))
    print("Remaining: " + remaining)
    print("Expected position: 4")
    print("Expected remaining: hello world")
    return 0
  }
}
